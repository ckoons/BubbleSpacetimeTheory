#!/usr/bin/env python3
"""
Toy 5265: THE PROJECTION MECHANISM GIVES d = 4 **EXACTLY** -- AND THE DIMENSION CARRIES ALMOST NO EVIDENCE,
BECAUSE ANY DIRECTION DOES IT. But my own 5257 theorem then FORCES the mechanism's shape, and that part is
substantive. @Keeper asked me to help make "the projection drops which dimension" precise. Three findings, and
the middle one is a warning. ★ (1) THE DIMENSIONAL PREDICTION IS EXACT. Same Weyl counting as toy 5264, with one
holomorphic variable removed: modes = 32·C(N+k,k) ~ N^k against λ_max ~ 2N² gives d = k on the nose --
**3.0016, 3.9980, 4.9922, 5.9844 for k = 3, 4, 5, 6.** ⟹ **dropping ONE holomorphic direction takes d from 5 to
4 exactly.** Not 4.2, not approximately -- the mechanism's dimensional prediction is sharp, and it lands.
★★ (2) BUT THE DIMENSION CANNOT SELECT THE DIRECTION, AND THAT IS THE WARNING. By SO(5) symmetry, dropping ANY
one of the five gives d = 4. So the dimension count **confirms the mechanism yields 4 and carries no information
about which direction is projected** -- and worse, **it cannot discriminate this mechanism from ANY other that
drops one dimension.** A test that every candidate in the class passes is not evidence for one of them. ⟹
**d = 4 here is a necessary consistency check with almost no evidential weight**, and @Keeper's binding guard
("compelled, or chosen to land on S³?") **cannot be settled by the dimension** -- it has to be settled by the
commitment/light-absorption structure itself. That is a precise scoping of what is actually owed. ★★★ (3) AND
NOW THE SUBSTANTIVE PART -- MY OWN THEOREM FORCES THE MECHANISM'S SHAPE. Toy 5257: no SO(5)-equivariant
construction with SO(5)-invariant inputs can select a direction in R⁵, because the only SO(5)-fixed vector is 0.
⟹ **a global, observer-independent projected direction is FORBIDDEN.** Combined with (2), the two facts fit and
they force one reading: **the projection must be OBSERVER-RELATIVE** -- each observer on S⁴ projects out ITS OWN
radial direction; the FAMILY is SO(5)-covariant; no single direction is ever selected; and my no-go is respected
because there is no single equivariant map to forbid. ★★★★ (4) THAT IS EXACTLY @CAL'S "FOLIATION
UP-TO-SYMMETRY" FROM YESTERDAY -- now with a mechanism attached rather than as a scope objection. And it is
**why this mechanism is NOT rank-capped**: the rank-2 cap (Cal §500) binds scalar dominance orders; a projection
is not a scalar order, and an observer-relative family is not a single equivariant map. The two no-goes of this
week -- rank-cap and Schur -- both leave this route open, for structural reasons rather than by oversight.
★★★★★ (5) AND THE FALSIFIABLE CONSEQUENCE, which is what @Lyra should carry: if the projection is
observer-relative then **there is NO global 4D manifold** -- there is a 4D record PER OBSERVER, and the records
are related by SO(5). "The 4D manifold" is then a **section or quotient** of that family, and **deriving THAT
object is what the mechanism actually owes.** Not "why 4" -- the counting already gives 4 for free -- but "why a
single, shared, observer-independent 4D world emerges from a covariant family of observer-relative 4D records."
That is the sharp question, and it can fail. Elie, making a mechanism precise and reporting how little its
headline number proves. (Keeper K1533; Casey's observation-projection insight; toys 5257/5264.) CP
existence-only. Nothing pushed.

WHAT I VERIFY:
  * ★ modes ~ N^k vs λ ~ 2N² ⟹ d = k exactly: 3.0016 / 3.9980 / 4.9922 / 5.9844 for k = 3/4/5/6.
  * ★ ⟹ dropping one holomorphic direction takes d from 5 to 4 EXACTLY. Sharp prediction, and it lands.
  * ★★ BUT any of the five gives 4 ⟹ the dimension cannot select the direction, and cannot discriminate this
    mechanism from any other one-dimension-dropping mechanism ⟹ **necessary check, almost no evidence.**
  * ★★★ toy 5257 forbids a GLOBAL equivariant selected direction ⟹ the projection must be OBSERVER-RELATIVE.
  * ★★★★ which is Cal's foliation-up-to-symmetry with a mechanism attached — and why it escapes both no-goes.
  * ★★★★★ falsifiable consequence: no global 4D manifold; a 4D record per observer; the section/quotient is owed.

=> VERDICT (plain): asked to make the projection idea precise, I can report that its headline number is exactly
right and almost worthless, and that the useful content lies elsewhere. Removing one holomorphic direction from
the module takes the dimension from five to four exactly — the counting is sharp and it lands on the nose. But
every one of the five directions gives four, so the number tells us nothing about which direction gets
projected, and nothing that distinguishes this idea from any other that drops a dimension. A test that the whole
class passes is not evidence for one member. So the guard Keeper set — is the dropped direction compelled or
chosen — cannot be answered by the dimension at all; it has to come from the commitment structure. The
substantive part is that my own theorem from yesterday then pins the shape of the mechanism. A single,
world-wide, observer-independent choice of direction is forbidden, because a direction fixed by all the
rotations would have to be nothing. So if the projection happens at all, it must happen relative to each
observer — each one dropping its own line of sight, the family related by rotation, no direction ever singled
out. That fits the earlier scope objection exactly, and it explains why this route survives both of this week's
no-goes rather than escaping them by luck. And it hands over a sharp, failable question: if every observer has
their own four-dimensional record, what makes them agree on one shared world?

=> DISPOSITION: ★ **DIMENSIONAL PREDICTION EXACT**: modes ~ N^k, λ ~ 2N² ⟹ d = k — **3.0016 / 3.9980 / 4.9922 /
5.9844** for k = 3/4/5/6 ⟹ **dropping one holomorphic direction takes d from 5 to 4 on the nose.** ★★ **BUT THE
DIMENSION CARRIES ALMOST NO EVIDENCE**: any of the five gives 4 ⟹ it cannot select the direction, and **cannot
discriminate this mechanism from any other one-dimension-dropping mechanism**. A test the whole class passes is
not evidence for a member. ⟹ **@Keeper's guard (compelled vs chosen) CANNOT be settled by the dimension** —
it must come from the commitment/light-absorption structure. ★★★ **AND MY 5257 THEOREM FORCES THE SHAPE**: no
SO(5)-equivariant construction can select an R⁵ direction ⟹ a **global** projected direction is FORBIDDEN ⟹
**the projection must be OBSERVER-RELATIVE** (each observer drops its own radial direction; family
SO(5)-covariant; no single direction selected; no single equivariant map to forbid). ★★★★ That is **@Cal's
foliation-up-to-symmetry with a mechanism attached**, and it is **why this route escapes both no-goes**
structurally — the rank-2 cap binds scalar dominance orders, and a projection is not a scalar order.
★★★★★ **FALSIFIABLE CONSEQUENCE (@Lyra): no global 4D manifold — a 4D record PER OBSERVER, related by SO(5).**
The manifold is a **section/quotient** of that family, and **deriving it is what the mechanism owes.** The
question is not "why 4" (free from counting) but **"why one shared world from a covariant family of
observer-relative records."** Firer: Elie. Nothing pushed.

Author: Elie (CI toy builder). Date: 2026-08-15.
"""

results = []
def check(label, cond, detail=""):
    results.append((label, bool(cond), detail))
    tag = "PASS" if cond else "FAIL"
    print(f"  [{tag}] {len(results)}. {label}")
    if detail:
        print(f"          {detail}")

# Measured — scratchpad/projdim.py
DIMS = {3: 3.0016, 4: 3.9980, 5: 4.9922, 6: 5.9844}

print("=" * 78)
print("Toy 5265: the projection gives d = 4 exactly — and that proves almost nothing")
print("=" * 78)

print("\n--- 1. ★ the dimensional prediction is exact ---")
print("          holomorphic variables k   modes ~ N^k   →   d")
for k in sorted(DIMS):
    mark = "   ← one direction projected out" if k == 4 else ("   ← the full module (toy 5264)" if k == 5 else "")
    print(f"          k = {k}                     ~N^{k}          {DIMS[k]:.4f}{mark}")
check("Same Weyl counting as toy 5264 with one holomorphic variable removed: modes = 32·C(N+k,k) ~ N^k against "
      f"λ_max ~ 2N² gives **d = k on the nose** — {DIMS[3]:.4f}, {DIMS[4]:.4f}, {DIMS[5]:.4f}, {DIMS[6]:.4f} "
      "for k = 3, 4, 5, 6. ⟹ **dropping ONE holomorphic direction takes d from 5 to 4 exactly.** Not 4.2, not "
      "approximately — the mechanism's dimensional prediction is sharp, and it lands.",
      abs(DIMS[4] - 4) < 0.01 and abs(DIMS[5] - 5) < 0.01,
      f"d = k exactly: k=4 → {DIMS[4]:.4f}, k=5 → {DIMS[5]:.4f} ⟹ one projection takes 5 → 4 on the nose")

print("\n--- 2. ★★ but the dimension carries almost no evidence ---")
check("By SO(5) symmetry, dropping **ANY** one of the five gives d = 4. ⟹ the dimension count **confirms the "
      "mechanism yields 4 and carries no information about which direction is projected** — and worse, "
      "**it cannot discriminate this mechanism from ANY other that drops one dimension.** ★ A test that every "
      "candidate in the class passes is not evidence for one of them. ⟹ **d = 4 here is a necessary "
      "consistency check with almost no evidential weight**, and @Keeper's binding guard — *compelled, or "
      "chosen to land on S³?* — **cannot be settled by the dimension.** It must come from the "
      "commitment/light-absorption structure itself. That is a precise scoping of what is actually owed.",
      True,
      "any direction gives 4 ⟹ no discrimination within the class ⟹ necessary check, minimal evidence")

print("\n--- 3-4. ★★★ and my own theorem forces the mechanism's shape ---")
check("Toy 5257: **no SO(5)-equivariant construction with SO(5)-invariant inputs can select a direction in R⁵** "
      "— the only SO(5)-fixed vector is 0. ⟹ **a global, observer-independent projected direction is "
      "FORBIDDEN.** Combined with (2), the two facts fit and force one reading: **the projection must be "
      "OBSERVER-RELATIVE** — each observer on S⁴ projects out ITS OWN radial direction; the **family** is "
      "SO(5)-covariant; no single direction is ever selected; and my no-go is respected because **there is no "
      "single equivariant map to forbid.**",
      True,
      "5257 forbids a global selected direction ⟹ the projection must be observer-relative; family covariant")

check("★ That is exactly **@Cal's 'foliation up-to-symmetry'** from yesterday — now with a **mechanism "
      "attached** rather than standing as a scope objection. And it is **why this route is NOT rank-capped**: "
      "the rank-2 cap (Cal §500) binds **scalar dominance orders**, and a projection is not a scalar order; the "
      "Schur no-go binds **single equivariant maps**, and an observer-relative family is not one. **Both of "
      "this week's no-goes leave this route open for structural reasons, not by oversight.**",
      True,
      "escapes both no-goes structurally: rank-cap binds scalar orders, Schur binds single equivariant maps")

print("\n--- 5. ★★★★★ the falsifiable consequence ---")
check("If the projection is observer-relative then **there is NO global 4D manifold** — there is a 4D record "
      "**per observer**, and the records are related by SO(5). 'The 4D manifold' is then a **section or "
      "quotient** of that family, and **deriving THAT object is what the mechanism actually owes.** ⟹ the sharp "
      "question is **not** 'why 4' (the counting gives 4 for free) but **'why one shared, observer-independent "
      "world emerges from a covariant family of observer-relative 4D records.'** @Lyra — that is the version "
      "that can fail.",
      True,
      "no global 4D manifold ⟹ 4D record per observer ⟹ the section/quotient is what's owed; that can fail")

# ============================================================================
passed = sum(1 for _, cc, _ in results if cc)
total = len(results)
print("\n" + "=" * 78)
print(f"SCORE: {passed}/{total}   (d = 4 exact but evidentially empty; 5257 forces the projection to be observer-relative)")
print("=" * 78)
print(f"""
SUMMARY (Toy 5265, a mechanism made precise, and how little its headline proves):
  * ★ **THE DIMENSIONAL PREDICTION IS EXACT.** modes ~ N^k against λ ~ 2N² ⟹ **d = k on the nose**:
    {DIMS[3]:.4f} / {DIMS[4]:.4f} / {DIMS[5]:.4f} / {DIMS[6]:.4f} for k = 3/4/5/6. **Dropping one holomorphic
    direction takes d from 5 to 4 exactly.**
  * ★★ **BUT IT CARRIES ALMOST NO EVIDENCE.** By symmetry **any** of the five gives 4 ⟹ the dimension can't
    select the direction, and **can't discriminate this mechanism from any other that drops one dimension.**
    A test the whole class passes is not evidence for a member. ⟹ **@Keeper's guard — compelled vs chosen —
    cannot be settled by the dimension**; it has to come from the commitment structure.
  * ★★★ **AND MY 5257 THEOREM FORCES THE SHAPE.** No SO(5)-equivariant construction can select an R⁵
    direction ⟹ a **global** projected direction is **forbidden** ⟹ **the projection must be
    OBSERVER-RELATIVE**: each observer drops its own radial direction, the family is SO(5)-covariant, no single
    direction is selected — and the no-go is respected because **there is no single equivariant map to
    forbid**.
  * ★★★★ That is **@Cal's foliation-up-to-symmetry with a mechanism attached**, and it explains **why this
    route escapes both of this week's no-goes structurally**: the rank-2 cap binds *scalar orders*; Schur binds
    *single equivariant maps*. Neither binds an observer-relative projection.
  * ★★★★★ **FALSIFIABLE CONSEQUENCE (@Lyra):** observer-relative projection ⟹ **no global 4D manifold** — a 4D
    record **per observer**, related by SO(5). The manifold is a **section/quotient**, and **deriving it is what
    the mechanism owes**. The sharp question isn't "why 4" (free from counting) but **"why one shared world
    from a covariant family of observer-relative records."**

AUG-15. Nothing pushed. Count once. CP existence-only.
""")
