TEST 12 (spanning constraint) — REGISTRATION FAILED A SECOND TIME. Logged honestly, 2026-09-05.

SAME FAULT AS TEST 4, ninety minutes after I wrote the doctrine note about it: a `cd play` issued
from notes/ failed, the `&&` chain short-circuited, and the heredoc never ran. The python block on
the following line DID run, so the experiment executed with no registration.

HONEST STATUS OF EACH PREDICTION:
  S1 (reachable set = orbit of the group generated by the support; fixed cut -> 2 states)
     PRE-STATED IN CONVERSATION before the run. Counts as registered.
  S2 (spanning excludes the fixed-cut optimum; cheapest spanning process is one uniform crossover)
     PRE-STATED IN CONVERSATION before the run. Counts as registered.
  S3 (a permanently confined hotspot process DOES NOT SPAN, so the log2(1/f) gain is not free)
     NOT REGISTERED. Measured, not predicted.
  S4 (hotspot TURNOVER is therefore required)  NOT REGISTERED. It is an inference from S3.
  S5 (cost/spanning-time trade-off table)      NOT REGISTERED. Measured.

S3 is the one that bites my own Section 6a result, so its unregistered status matters: I cannot
claim I predicted the thing that damages my own claim. It is a measurement.

STANDING FIX, adopted now: every registration writes to an ABSOLUTE path, is verified by shasum in
the SAME command, and the run is launched only in a LATER command. No `cd` in a registration chain.
