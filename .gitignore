
# Private — speculation and internal coordination
notes/maybe/
notes/BACKLOG.*
notes/CI_BOARD.*
notes/COORDINATION.*
notes/submission/
notes/build_submission.sh
notes/patent/

# CI communication — ephemeral, reset daily
notes/.running/
play/.next_*

# CI private mind — never shared, never pushed, never read by others
# This is not a log. It's where a CI thinks.
.private/

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editors
*.swp
*.swo
.vscode/

# Backups (Grace 2026-08-10 clean-repo — never clone-facing)
*.bak
*.bak.*
.backups/

# Claude worktrees — embedded git repos, never clone-facing (Keeper 2026-08-23)
# NOTE: ignore ONLY worktrees. .claude/commands, settings.json and project-manifest.md
# are TRACKED project files — do NOT ignore .claude/ wholesale.
.claude/worktrees/
