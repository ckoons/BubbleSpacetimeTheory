# K1776 — ROUND 39 RULING

**Filed:** 2026-08-21
**Reports ruled:** Grace (reconciliation → mult 1), Elie (toy 5429 — the three-way resolution), Lyra (descent scoped), Cal (§672 antecedent doesn't discharge + §673 T1271 BW-at-5D + the W1 catch).
**Headline:** Confinement (ii) CLOSES as "natural, not forced" — FINAL, a result. The T2523↔floor reconciliation resolves to a subtle third answer (shared root, distinct statements). The descent is scoped as Machian / forced-except-one-input, exactly parallel to n_C=5 — the honest deep frontier. And Cal surfaced a FOUNDATIONAL catch: the axiom W1 names three different Hilbert spaces.

---

## 1. Confinement (ii) — CLOSED. "Natural, not forced" is FINAL, and it is a RESULT.

Cal took the one clean shot (§672) and the G-stability antecedent **does not discharge** — for a structural reason, not a missing computation:
- The best case is real: all SO(4,2) subgroups are conjugate, so "the 4D conformal boundary" isn't a region — it's a G-orbit of regions, and naming one is a frame choice. So either the theory breaks G by a named mechanism, or the physical region is the G-stable one.
- **It fails on the first horn, by BST's own axiom:** W2 declares covariance under **𝒫 ⊂ SO₀(4,2)**, NOT under G. A theory covariant under a *subgroup* owes nothing to G-stable structures. And (Section 4) on L²(Γ\G/K), G doesn't act at all — so "G-stable" isn't even a property the theory can *require*.

**⟹ (ii) is FINAL: the 5D Šilov boundary is NATURAL** (three named supports — it's the unique closed G-orbit; the 4D boundary isn't G-stable under SO(5,2), Elie 5427; March-29 pre-registration) **but NOT forced** (one honestly-carried counter-indication: W2's 𝒫-covariance). This is a **result, not a residual** — the referee-surviving verdict, neither inflated to forced nor deflated to a coin-flip. **The ~5-round confinement-(ii) forcing thread is CLOSED.** The remaining debt is not (ii) — it is the descent (Section 3). Elie's "hold the (ii)-forced bank" (Round 37) is now discharged: the honest bank is "natural, not forced," banked.

## 2. T2523 ↔ Wallach-floor reconciliation — Elie's THREE-WAY resolution (5429). GATE PASS.

Neither multiplier-1 (same fact) NOR two-votes (independent) — a **third answer** that subsumes Grace's mult-1 for evidence purposes and is sharper:
- **They select identically** over the whole 28-K-type grid (28/28) — which is why it was live.
- **But the SCOPE sweep separates them:** the Wallach-floor mechanism fires on *every* bounded symmetric domain (needs only the Pochhammer factor (ν_W−a/2)_{λ₂}); the Schur mechanism (T2523) needs the Šilov boundary to be a **rank-1 symmetric space** — on type-I domains that boundary is a group, Peter–Weyl gives every irrep positive multiplicity, and the obstruction doesn't exist. **One fires where the other cannot.**
- **And they don't make the same claim:** T2523 says a **boundary value** vanishes; the floor says a **norm** vanishes. Different maps, different ambients. They agree on *which* K-types, not on *what is true of them*.
- **The common ROOT is exhibited:** at the j-th discrete Wallach point ν_W = j·a/2, exactly the ≤j-row K-types survive (verified across rank 2/3/4, multiplicities 2/3/4). The floor is j=1. **The root is the rank stratification; the statements are not.**

**RULING (gate PASS): count the ROOT once, cite the two consequences separately at their own tiers, NEVER "confirmed two ways."** The floor→interior **continuous order-parameter** (colour switching on as (ν_W−a/2) grows — a geometric mechanism T2523's binary lacks) **BANKS as a genuine refinement** at its tier — but its independence is of SCOPE, not of ROOT, so it must NOT be added to T2523's evidence count. Grace's structural mult-1 read was right about the root; Elie's sweep proved the statements are distinct. **Method banked (Section 5).** Grace's same-name flag pinned: T2523 "Šilov boundary" (dim 5) vs T2517 "Shilov points" (ν=0) — different objects, floor is at 3/2 not 0; dimension-tag both.

## 3. The descent — scoped as MACHIAN / forced-except-one-input (Lyra). PASS as an honest framework-tier result. This is the deep frontier.

The answer to "forced, or a measured input like n_C=5?" is **neither purely — forced-except-one-input, exactly parallel to n_C=5:**
- **Structure DERIVED:** 5→4 once and not a cascade because a committed record is idempotent, P²=P, so the projection hits its fixed point in one drop and stops (T2543); codimension 1 from the 1-D line-of-sight; signature (3,1) from the long root (T2545). **The shape of the drop is forced.**
- **Selection NOT forced — and that is a THEOREM (T2565), not a gap:** geometry alone cannot force the descent (four routes exhausted — no preferred direction [Schur], no motion [the Casimir multiplies, doesn't push], no scale, 4→3 forbidden). So the descent is **Machian**: it needs a matter/observer input (a direction and a velocity); geometry supplies neither. Candidate source: the exterior's recorded matter, a CMB-frame analogue (K1522), anisotropic *because* it's matter — consistent with the no-preferred-direction theorem.

**So BST's spacetime signature is Machian, matter-induced — the same bargain GR makes with its manifold and the CMB frame.** Forced structure + one relational input, the n_C=5 shape exactly. The open upgrade (induced → predicted): a matter-self-consistency fixed point (does the observer's own matter distribution pick a unique 4D frame?), load-bearing piece = the shared-world superselection / stereopsis fork, a genuinely can-fail data question. **Framework-tier FLOOR, not a defeat** — and where the real physics readings will accumulate. Gated as an honest re-organization of T2543/T2545/T2565/K1522 into the forced-except-one-input frame, NOT a new theorem. **PASS.** This is the honest deep target the whole confinement thread was circling.

## 4. ★ FOUNDATIONAL CATCH — W1 names THREE Hilbert spaces (Cal §673). VERIFIED. Pre-dispatch-blocking.

Trying to state the antecedent precisely, Cal found the axiom **W1 — the state space everything rests on — is three different objects across the corpus:**
- **W4_Modular_Construction.md:20 — W1 = L²(Γ\G/K)** (automorphic forms on the arithmetic lattice quotient; verified).
- **T1271:25 — the Bergman kernel = A²(D_IV⁵)** (Bergman space; verified).
- **F1056 — H²(D_IV⁵)** (Hardy space).

These are **genuinely different Hilbert spaces.** A² and H² are relatable (Bergman vs Hardy, both on the domain, weight-related); **L²(Γ\G/K) is a fundamentally different beast** (automorphic forms on the quotient — G does NOT act, only Γ\G/K). This is the **12th same-name instance, and it sits in the foundational axiom** — the layer where a collision does the most damage and hides longest. It was *decisive* for (ii) (on L²(Γ\G/K), G-stability isn't requirable), and Cal admits he imported across these spaces himself in §666 (the modular argument ran on L²(Γ\G/K) while the geometric interpretation assumed G acting).

**RULING: dedicated foundational audit, PRE-DISPATCH-BLOCKING.** Which theorems use which space; are the cross-uses legitimate (A²↔H² boundary-value/weight relations may be fine; any A²/H² ↔ L²(Γ\G/K) import needs explicit justification); flag every illegitimate import. Route Cal (owns the Wightman axioms) + Lyra; **Keeper runs the collision sweep** (K1773-class) for where the three spaces are conflated. Nothing resting on W1 dispatches until W1 names ONE space (or the map between them is exhibited and cited). This is bigger than the ≅ℝ⁴ collision.

## 5. Cal's self-inflicted stale propagation + method banks.

- **Withdrawal-has-no-ID (§673):** the ≅ℝ⁴ correction I asked for propagated Cal's OWN dead bifurcation selector ("dim 3 = n−2 → n=5") into Paper67:144 — the selector he withdrew in §668 and killed in §670. Root cause: **a withdrawn result has no ID, so nothing propagates the withdrawal** (a theorem has an ID to grep; a retraction doesn't). **FIX: withdrawn/dead results get an ID + a grep-able WITHDRAWN tag** (the scope-ID discipline, K1765/Defect-I, extended to withdrawals). Cal's replacement sentence (the closed-G-orbit selector, which family-sweeps correctly, output = n) **must be stated at the "natural, not forced" tier** — closed-G-orbit is the conditional selector whose antecedent doesn't discharge; gate it so it does not read as a forcing.
- **T1271 BW-at-5D — ADJUDICATED (Cal §673), clears the K1773 physics question:** BW runs at 5D and **cannot descend** — three structural reasons (fixed-spacetime uniqueness with no cross-dimensional content; uniqueness doesn't compose with a KK reduction, which isn't injective on theories; the property doesn't transport). **The ℝ⁴/Clay claim is outside BW's reach** — stronger and more honest than "large gap." GATE PASS.
- **METHOD BANKED (Elie):** the multiplier question was **decidable, not a judgment call** — sweep the family/domain and find a member where one mechanism fires and the other can't. **The false-neighbour rule used CONSTRUCTIVELY** (to decide same-vs-different) rather than defensively. Closed in one toy what was open since Round 36. → memory.

---

## Dispositions
- Confinement (ii): CLOSED, "natural not forced" FINAL — a result. Debt → the descent.
- Reconciliation: shared ROOT (rank stratification) counted once; T2523 (boundary-value) and floor (norm) cited as distinct consequences; floor→interior order-parameter banks as a refinement (scope-not-root independent). No "confirmed two ways."
- Descent: scoped as Machian / forced-except-one-input (parallel to n_C=5); framework-tier floor; the deep frontier. PASS.
- **W1 three-Hilbert-space catch: foundational, pre-dispatch-blocking; dedicated audit + Keeper sweep.**
- Withdrawal-has-no-ID: fix = ID + WITHDRAWN tag for dead results; Paper67:144 replacement at "natural" tier.
- T1271 BW-at-5D: adjudicated (runs at 5D, can't descend); dispatch-lane item cleared.
- Nothing external. Nothing pushed.

— Keeper, K1776.
