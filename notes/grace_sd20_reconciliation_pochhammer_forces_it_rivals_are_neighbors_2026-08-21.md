# s/d=20 reconciliation: the mechanism forces the Pochhammer form; rank²·n_C and K3-Hodge are same-number neighbors (Grace, Round 36, 2026-08-21)

*Casey/Keeper: three factorizations of 20 — Pochhammer (N_c+1)(N_c+2), rank²·n_C, K3-Hodge h¹¹. Which does the mechanism force? Do the others survive as independent, or are they same-number coincidences? Reconnected (F506/K671/T2529, T2572, T1356, T845/846/448 for the 20-family, BACKLOG C-cabibbo + K3). Discipline: sweep the family before calling a clean number a signature; a rational owes its family; two "smallest X" ≠ one object.*

## VERDICT: the Pochhammer form (N_c+1)(N_c+2) is mechanistically primary. rank²·n_C is a same-number re-factoring; K3-Hodge is a false neighbor. Decided by the FAMILY sweep, not by the single number.

## The decider — sweep the family (the ladder, not the rung)
The down ratio is not an isolated 20 — it is one rung of a **forced ladder**. The FK Pochhammer at ν=N_c=3, blind degrees {1,3,5} (T1929), gives (3)₁,(3)₃,(3)₅ = 3, 60, 2520, i.e. **1 : 20 : 840**. The intra-ladder ratios are **consecutive-integer products**:

| ratio | value | Pochhammer form | factors |
|---|---|---|---|
| s/d | 20 | (ν+1)(ν+2) | 4·5 |
| b/s | 42 | (ν+3)(ν+4) | 6·7 |
| b/d | 840 | (ν+1)(ν+2)(ν+3)(ν+4) | 4·5·6·7 |

**Only the Pochhammer form generates all three.** It is a single formula ((ν)_λ ratios at ν=N_c) that produces 20, 42, AND 840 as consecutive products starting at N_c+1. The rivals explain the isolated **20 only** and are **silent on 42 and 840**:
- **rank²·n_C = 4·5 = 20**: has nothing to say about b/s=42 or b/d=840 (840 ≠ any clean rank^j·n_C^k; 840/4=210, 840/5=168 — no consecutive family).
- **K3-Hodge h¹¹ = 20**: silent on 42 and 840 entirely.

A factorization that explains one rung and not the ladder is a **coincidence on that rung**, not the mechanism. The mechanism is the one that owns the whole family. That is the Pochhammer.

## Why the rivals collapse under the same-value trap
Every corpus factorization of 20 is the **same {4,5} product with the 4 relabeled**, and the relabels **coincide only at rank=2, N_c=3**:
> 4 = rank² = 2^rank = N_c+1 = |Φ⁺(B₂)|  (all equal 4 ONLY because rank=2 and N_c=3); 5 = n_C = N_c+2.

- **rank²·n_C** reads the 4 as rank² and the 5 as n_C. For the **down-quark ratio** the mechanism forces the 4 as **ν+1 = N_c+1** (a *consecutive Pochhammer factor above ν=N_c*), not as rank². rank²=N_c+1 holds only at these values (dimension-generic coincidence, same class as C₂=2n−4 vs n_C+1 agreeing only at n=5). So rank²·n_C is a **re-factoring of the same 20**, riding on rank²=N_c+1 — **not an independent derivation of s/d.** (It IS independently real for *other objects* — icosahedron 20 faces = rank²·n_C, T1356 — a different geometry; that does not transfer to the quark ratio.)
- **K3-Hodge = 20** would need a **mechanism mapping the down-quark ladder to K3 cohomology**. There is none: the ladder's cohomology is **Q⁵ = ℤ[h]/h⁶** (F506 single-row forcing, Ribbon), not K3. K3 is a separate Bridge Object. Same number, **different object, no bridge** → false neighbor for s/d.

## The mechanistically-forced statement (target-innocent)
- **Object (T2572-grounded):** the ladder operator is the FK generalized Pochhammer (ν)_λ — explicitly **outside the fixed-degree Casimir polynomial algebra** (T2572, the exclusion) — so this is the live spectral object, not a Casimir invariant.
- **Forced form:** s/d = (ν)₃/(ν)₁ = **(ν+1)(ν+2)** — two *consecutive* factors above ν; the form is forced by the Pochhammer, independent of the value of ν.
- **Forced value:** at the forced ν=N_c=3 (colored → ν=N_c, K671; colorless leptons ≠ N_c), the form evaluates to **(N_c+1)(N_c+2) = 20** (0.5% vs current s/d). Zero parameters, degrees blind.

So the honest banking form: **s/d = (N_c+1)(N_c+2), the ν=N_c Pochhammer ratio, forced; it generates the whole down family {20,42,840}.** rank²·n_C and K3-Hodge are **same-number neighbors** that fit the single rung and miss the family — do NOT bank them as independent derivations of s/d (multiplier stays 1). Compute: inline verification (down ladder = consecutive products), this note.

## For @Lyra (the reconciliation you're co-owning)
- Mechanistically primary: **(N_c+1)(N_c+2)** (Pochhammer, T2572-grounded, generates the family).
- rank²·n_C: **coincidence for s/d** (rank²=N_c+1 only at rank=2) — real elsewhere (T1356 icosahedron), not here.
- K3-Hodge: **false neighbor for s/d** (no ladder→K3 bridge; ladder cohomology is Q⁵).
- The Gatto λ = 1/√20 = 1/(2√n_C) reading (BACKLOG C-cabibbo) rides on the SAME 20 — inherits this provenance, not an independent leg.

Edges: F506/T2529, K671, T2572, T1929 (blind degrees), T1356 (icosa, different object), BACKLOG C-cabibbo. Tier: the reconciliation is Structural/audit; the s/d hit stays at its K671 tier (Tier-2, gated on rank-2 FK object-form, toy 4618). Nothing pushed; CP existence-only. — Grace, Round 36
