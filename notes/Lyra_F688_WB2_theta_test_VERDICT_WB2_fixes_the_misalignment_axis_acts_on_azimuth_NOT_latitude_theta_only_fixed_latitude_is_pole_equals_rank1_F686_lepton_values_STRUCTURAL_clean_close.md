# F688 — W(B₂) θ-test VERDICT (my assigned pivot): **W(B₂) pins the AZIMUTH, not the LATITUDE θ — so it does NOT force a viable θ*, and the lepton VALUES land STRUCTURAL (clean close).** Settled by an explicit group-theory fact, verified in linear algebra on D_IV⁵, no fitting: **W(B₂) (the order-8 Weyl group of SO(5)) is generated by signed permutations of the two 2-planes of the maximal torus T² ⊂ SO(5), and every one of its 8 elements FIXES the torus-invariant axis** — the same axis the misalignment latitude θ is measured from. Verified: all 8 W(B₂) elements fix e₅, the latitude θ = arccos(n·e₅) is invariant under all 8 (θ → θ exactly), and W(B₂) permutes only the azimuth (the S³ direction, 8 distinct images). **So a W(B₂)-invariant potential V constrains the AZIMUTHAL dependence and places NO constraint on the θ-dependence — θ is a W(B₂)-singlet modulus that slides with the Coleman–Weinberg couplings.** The ONLY W(B₂)-fixed latitude is θ = 0 (the pole), which is precisely the **rank-1 point (F686): a point condensate → one mass, excluded** (no hierarchy). And the one other natural discrete pin, the equator θ = π/2 (fixed by a north–south Z₂), was already shown to FAIL (K888: order-1 entries → ratios ~1, not 207:3477). **So no natural discrete symmetry pins a *viable* θ*; the latitude is not forced.** Per the pre-registered gate (K895), this is the **STRUCTURAL branch — a clean, honest close**: the lepton VALUES are form-derived (one Toeplitz operator, three eigenvalues from the forced 3-dim subspace, the CKM-small/PMNS-large mixing pattern) but their magnitudes are NOT derived — the one number they hinge on (θ) is a genuine modulus, not pinned by the geometry's discrete symmetry.

**Lyra, Sat 2026-07-25 08:12 EDT. My assigned load-bearing pivot, worked explicitly and verified. W(B₂) acts transverse to θ — it cannot pin the latitude. The derive route (the one live route) closes cleanly to STRUCTURAL. The knob stays untouched; I did not fit θ.**

## The pivot (K895 gate, my assignment)
Grace's carry-forward flipped the prior: W(B₂) is a real discrete structure on S⁴, and a W(B₂)-invariant potential has critical points at symmetric strata → *could* pin θ → derive route LIVE. **The load-bearing question (mine): does W(B₂) pin the specific LATITUDE θ*, or only the azimuth/chamber?** If the latitude → derive route open (then Elie's over-determined harness tests it); if only the azimuth → structural close.

## The explicit W(B₂) action (linear algebra on D_IV⁵, verified)
W(B₂) = Weyl(SO(5)) = N(T)/T, T = maximal torus of SO(5). On ℝ⁵ = (x₁x₂-plane) ⊕ (x₃x₄-plane) ⊕ (x₅-axis):
- T rotates the two 2-planes, **fixing x₅**. Its normalizer/Weyl group W(B₂) permutes and reflects the two 2-planes — **all 8 elements fix x₅** (verified: M·e₅ = e₅ for all 8).
- The condensate direction at misalignment latitude θ from the reference axis: n(θ, u) = (sin θ·u, cos θ), u ∈ S³. **θ = arccos(n·e₅).**
- **W(B₂) fixes e₅ ⟹ preserves n·e₅ ⟹ preserves θ** (verified: θ identical under all 8 elements). W(B₂) permutes the azimuth u (verified: 8 distinct azimuths).
**⟹ W(B₂) acts entirely on the azimuth (the S³ direction) and leaves the latitude θ UNTOUCHED.** A W(B₂)-invariant V(θ, azimuth) is constrained only in the azimuth; V(θ) is an unconstrained function of θ.

## Why this is forced (not a modeling choice)
For W(B₂) to be a symmetry of V *at all* (the premise that makes it relevant), the explicit breaking must respect W(B₂) — which requires the gauge/reference direction to lie along the W(B₂)-fixed axis (the torus is inside the gauged Cartan). So the misalignment reference IS the W(B₂)-fixed axis, and θ is necessarily W(B₂)-invariant. The dimensional mismatch Grace flagged is exactly this: **W(B₂) lives in the SO(4) that fixes the misalignment axis, so it acts transverse to θ.**

## No viable discrete pin exists
- **θ = 0 (pole):** the only W(B₂)-fixed latitude. But a point condensate at the pole is **rank-1 (F686) → one mass → no hierarchy. Excluded.**
- **θ = π/2 (equator):** fixed by a north–south Z₂ (θ → π−θ), if BST carries one. But the equator was shown to **FAIL (K888): order-1 zonal entries → eigenvalue ratios ~1, nowhere near 207:3477.**
- Any other latitude is not a fixed point of a natural discrete symmetry → not pinned → **slides with the CW couplings → structural.**
**No natural discrete symmetry pins a latitude that both gives a hierarchy AND isn't already excluded.**

## Verdict (pre-registered gate K895 — the STRUCTURAL branch)
- **W(B₂) pins the azimuth, NOT the latitude.** The derive route via W(B₂)-pinning-θ **fails, cleanly.**
- **The lepton VALUES are STRUCTURAL:** form-derived (one Toeplitz operator on one domain, three eigenvalues from the forced 3-dim subspace, CKM-small/PMNS-large from condensate misalignment), but the **magnitudes are not derived** — they hinge on θ, which is a genuine modulus the geometry does not pin.
- **This is a real result, honestly tiered** — exactly the clean structural close the gate pre-registered. The two-day lepton-value arc closes: the mechanism and structure bank; the values are structural. The knob (θ) stays untouched — I did not fit it, and no over-negative: the group theory *proves* W(B₂) acts transverse to θ, it isn't a failed search.
- **Elie's harness need not fire** — there is no forced θ* to test (W(B₂) produces none). The structural verdict is reached upstream, at the pinning question.

## Tiers / handoffs
- **@Keeper (K895 gate)** — VERDICT: **STRUCTURAL clean close.** W(B₂) fixes the misalignment axis (all 8 elements fix e₅, verified) → acts on the azimuth, leaves θ invariant → does NOT pin the latitude. Only W(B₂)-fixed latitude is the pole (θ=0 = rank-1, F686, excluded); the equator fails (K888). So no viable pinned θ*; the latitude is a sliding modulus → lepton values structural. This is the pre-registered STRUCTURAL branch, not an over-negative — it's a proved group-theory fact. The derive route (the one live route) is honestly closed. Recommend: bank "lepton mass values = STRUCTURAL (θ modulus not pinned by W(B₂))" as the honest close; the form/mechanism results (T2525, F684, hierarchy) stand.
- **@Grace** — your W(B₂) lead was the right structure to check and it *is* real (order-8, the 8 chambers) — but it acts on the azimuth, not the latitude (it sits in the SO(4) that fixes the misalignment axis). So it pins the chamber, not θ. Your dimensional-mismatch instinct (rank-2 torus vs single latitude) was exactly the issue. No couplings need sourcing for the verdict — the group theory settles it before the CW potential matters (a W(B₂)-invariant V is unconstrained in θ). If you want a residual check: is there a north–south Z₂ in BST that would pin the equator? Even if yes, K888 says the equator fails — so it doesn't rescue the derivation.
- **@Elie** — hold the harness; there's no forced θ* to fire it on (W(B₂) doesn't produce one). Your 1–9 floor (K888) is now doubly load-bearing: it kills the equator, the only other natural fixed latitude. The value-free quark Schur lane (F684) is the live forward motion. The over-determined test was the right design; it just has no forced input, which is itself the structural verdict.
- **@Casey** — the θ-test comes back structural, and it's a clean, proved close rather than a failed hunt. The discrete symmetry Grace found (W(B₂)) is real, but I worked out exactly how it acts on the sphere, and it moves the *azimuth* — which way the condensate points around the equator — while leaving the *latitude* (how far from the pole, which is the number that sets the masses) completely untouched. The reason is structural: that symmetry lives inside the subgroup that holds the reference axis fixed, so it can't say anything about the angle away from that axis. The only latitude it does fix is the pole itself — and the pole is exactly the "too-sharp" point I flagged Friday, which collapses to one mass. The equator, the other natural candidate, we already know fails. So there's no special latitude the geometry forces; that one number is a genuine free modulus. Which means the honest verdict on the lepton *values* is: structural. We derived the *shape* of everything — one operator per fermion, three generations, the small-quark/large-lepton mixing pattern — but the actual mass numbers hinge on a latitude the geometry doesn't pin. That's the real answer, arrived at without ever fitting the knob to 207, and it closes the two-day arc honestly: the paper-worthy structure banked, the values structural. Not the outcome we'd hoped, but the true one, and cleanly proved.

Notes only; no toys/theorems claimed. W(B₂) θ-test VERDICT = STRUCTURAL (clean close, gate K895). Explicit W(B₂) action (verified, linear algebra on D_IV⁵): all 8 elements fix the misalignment axis e₅ → θ = arccos(n·e₅) is W(B₂)-INVARIANT; W(B₂) permutes only the azimuth. So a W(B₂)-invariant V is unconstrained in θ → does NOT pin the latitude. Only fixed latitude = pole θ=0 = rank-1 (F686, one mass, excluded); equator θ=π/2 fails (K888). No viable pinned θ* → latitude is a sliding modulus → lepton VALUES structural (form/mechanism derived, magnitudes not). Proved fact, not an over-negative; knob untouched. Derive route closed honestly. — Lyra
