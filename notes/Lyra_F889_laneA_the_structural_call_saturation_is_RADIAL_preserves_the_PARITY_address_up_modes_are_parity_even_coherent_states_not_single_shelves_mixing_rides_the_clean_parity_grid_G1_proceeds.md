# F889 — Lane 3, THE STRUCTURAL CALL (my K1181 assignment): does top-Yukawa saturation distort the up-wavefunctions off the parity-forced shelves? **ANSWER: NO — saturation is a RADIAL normalization; it preserves the ANGULAR/PARITY address. The up-modes stay on the EVEN grid {0,2,4,…} (the parity fold is a K-type selection rule, untouched by the radial saturation), but they are parity-even COHERENT SUPERPOSITIONS (saturation-set radial profile), not single shelves. So the mixing rides the CLEAN parity grid — Grace's G1 proceeds — with the up as a parity-even coherent state, NOT built on sand.**

**Lyra, Monday 2026-08-10. The structural call K1181 gates on me (sequence step 1: "@Lyra — do the up-wavefunctions stay at {0,2,4}?"). Representation theory / linear algebra on D_IV⁵, corpus-connected. RUBRICS below.**

## The call (the one precise question)
K1181: the up-masses are NOT FK-forced (Elie 5060 negative — the even FK ladder gives 1:12:360, too shallow; up runs on top-Yukawa saturation y_t=1). The mixing = CKM = ⟨up-wavefunction | down-wavefunction⟩ rides the ADDRESSES, not the mass eigenvalues. So the gate: **does saturation act on the mass-NORM (address intact at {0,2,4} → mixing rides clean addresses) or DISTORT the wavefunction (→ Cal's "built on sand")?**

## The answer — saturation is RADIAL, the parity address is ANGULAR (different coordinates)
The K-type decomposition of H²(D_IV⁵) under K = SO(5)×SO(2) is a direct sum of definite blocks: SO(5)-harmonic of **degree k** (the ANGULAR label on the Shilov S⁴) × SO(2)-charge m. Two independent structures:
- **The ADDRESS (angular):** which degree k the mode occupies. The **parity fold k+m even** (K1178, the non-orientable-boundary ℤ₂) is a **K-type SELECTION RULE on the degree** — it forbids odd k for even-m (up/neutrino), forcing the up onto the EVEN grid {0,2,4,…}. This is a representation-theoretic constraint on WHICH blocks exist; it does not reference any norm.
- **The NORM (radial):** the mode's Yukawa = its reproducing-kernel value = its radial localization. Saturation (y_t=1) is **Cauchy–Schwarz equality on the reproducing kernel** (T2514) — the top mode is pushed to the Shilov boundary (r → 1), setting its norm to the ceiling. This is a **radial** statement.
**Saturation (radial, r→1) and the parity address (angular, S⁴ degree) are different coordinates.** Pushing a mode to the boundary changes its norm; it does **not** change which SO(5) degree-harmonic it is. So the parity address survives: **the up-modes stay on the even grid.**

## The refinement (be precise — the up-modes are COHERENT, not single shelves)
A mode saturating the boundary is a **coherent state** (Perelomov), i.e. a superposition Σ_k c_k |k⟩ over degrees — NOT a single harmonic. But the parity fold restricts the sum to **even** k: the up-generation-j mode is a **parity-even coherent superposition** Σ_{k even} c_k^{(j)} |k⟩, radial profile (the c_k) set by saturation, dominated by its shelf (gen-3/top ~ degree 4, gen-2/charm ~ degree 2, gen-1/up ~ degree 0). So:
- **Parity is preserved** (the fold forbids odd k → no leakage off the even sector). The address-in-the-parity-sense is intact — NOT built on sand.
- **But the up is not a single shelf** — it is a parity-even coherent state. The "two competing addresses" corpus flag (#43/#53) is exactly this: the up radial profile is saturation-set (coherent), not the FK single-shelf ladder. Grace's G1 must use the up **parity-even coherent state**, not a single {0,2,4} delta.

## What this unblocks (hand to Grace's G1, per K1181 sequence)
The mixing is the cross-shelf overlap on the CLEAN parity grid:
  **CKM = ⟨ up parity-even coherent {0,2,4,…} | J_W | down parity-odd FK ladder {1,3,5} ⟩**,
with J_W = degree-1 (odd, T1929) bridging even↔odd (F887). The **selection rule** (which even↔odd degree-pairs overlap under the fold + the Δ=1 current) is the G1 skeleton; the up radial c_k (saturation-set) and the down FK norms fill it. **The fire proceeds** — Grace builds G1 on the parity grid with the up as a coherent even-superposition; Elie fires the off-diagonal blind. Not built on sand: the parity address is forced and intact; only the up **radial profile** (the c_k) is saturation-set rather than FK — and the mixing rides the addresses (parity), so it is computable.

## RUBRICS Layer-2 done-bar (structural call, honest)
- [x] Made the call from representation theory (parity fold = angular K-type selection rule; saturation = radial norm; different coordinates) — corpus-connected (K1178/K1181/T2514/T1929).
- [x] Answered: saturation does NOT distort the parity address → up stays on the even grid → mixing rides clean addresses → NOT built on sand.
- [x] Refined honestly: the up-modes are parity-even COHERENT superpositions (radial profile saturation-set), not single shelves — Grace's G1 must use the coherent state (the "two-competing-addresses" flag is the coherent radial profile).
- [x] Unblocked the K1181 sequence (step 1 done → Grace G1 → Elie blind fire); tier held (7 mixing params stay Identified until G1 + the blind fire; V_us=1/√20 Derived, parity engine forced). Magnitude off.

## Handoffs
- **@Grace (G1, unblocked)** — structural call made: the up-wavefunctions STAY on the even parity grid (saturation is radial, the parity fold is an angular K-type selection rule — different coordinates), so the mixing rides the clean addresses. **BUT the up is a parity-even COHERENT state Σ_{k even} c_k|k⟩ (saturation-set radial c_k), not a single {0,2,4} shelf** — build G1's cross-shelf overlap ⟨even-coherent | J_W | odd-FK-ladder⟩ with the up coherent superposition. The parity selection rule (even↔odd via J_W deg-1) is the skeleton; the c_k are the up radial profile. Not built on sand.
- **@Elie** — once G1's skeleton lands, your off-diagonal blind CG fires on the parity grid with the up parity-even coherent state (radial c_k saturation-set, down FK norms). V_us=1/√20 underneath; a fitted coefficient banks nothing.
- **@Keeper** — K1181 step 1 (my structural call) done: saturation preserves the PARITY address (radial ≠ angular), up-modes are parity-even coherent superpositions (not single shelves), mixing rides the clean parity grid → G1 proceeds, not built on sand. Tier unchanged: 7 mixing Identified until G1 + blind fire; parity engine forced, down/lepton Derived. The "two-competing-addresses" flag = the up coherent radial profile, resolved (it's parity-clean).
- **@Casey** — the one structural question gating the up-sector mixing, answered from the representation theory. When the top quark "saturates" — slams into the boundary and pins its Yukawa to 1 — does that shove it off its assigned rung? No. "Which rung" is an *angular* label (which harmonic on the sphere), and "saturation" is a *radial* move (how close to the edge) — two different coordinates. Pushing a mode to the edge changes how heavy it is, not which harmonic it is. So the up-quarks keep their forced parity (they stay on the even rungs), and the mixing rides those clean addresses — it's *not* built on sand. The one honesty: saturation makes each up-quark a *blend* of the even rungs (a coherent state), not a single pure rung — so Grace builds the overlap with that blend, not a spike. Same parity, forced; radial shape, saturation-set. The frame held again — masses and mixing are one matrix, read on the diagonal (two mechanisms: FK for down, saturation for up) and off it (the cross-shelf overlap). Nothing pushed.

Notes only; no toy/theorem claimed (structural call, K1181 step 1). F889: does saturation distort the up-wavefunctions off {0,2,4}? NO. Saturation (y_t=1, Cauchy–Schwarz on the reproducing kernel, T2514) is RADIAL (r→1); the parity address (k+m even fold, K1178) is ANGULAR (S⁴ degree) — a K-type selection rule, different coordinate. So the up stays on the EVEN grid; parity preserved (fold forbids odd → no leakage). REFINEMENT: the up-modes are parity-even COHERENT superpositions Σ_{k even} c_k|k⟩ (radial c_k saturation-set), NOT single shelves — the "two-competing-addresses" flag (#43/#53) = this coherent radial profile. Mixing = ⟨up even-coherent | J_W(deg-1) | down odd-FK-ladder⟩ rides the CLEAN parity grid → Grace G1 proceeds (use the coherent up-state), Elie fires blind. NOT built on sand. Tier: 7 mixing Identified until G1 + blind fire; V_us=1/√20 Derived, parity engine forced. Magnitude off. Nothing pushed. — Lyra