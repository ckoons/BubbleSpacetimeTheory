# Atlas reading — the gen-2,3 quark addresses are FORCED by H_{ν_W} discreteness (the theory rail under Elie's frame-mismatch V_cb/V_ub)

**Round 52 (forward). The theory rail that keeps Elie's frame mismatch honest: the up and down generation addresses are FORCED by the same Wallach/Harish-Chandra discreteness that forced the leptons to {5/2,3/2,0} (T2517) — there is no continuous knob to tune to V_cb/V_ub.** Declare-before-derive (K1771); target-innocent (G2); no mass-input smuggling (G4). Reconnected: T2517 (lepton template), T2525 (3 = rank+1), T1929 (Q⁵ cohomology ℤ[h]/h⁶, blind), T2513/T2529/T2530 (down ladder + V_us blind), T2515 (up boundary-distance), K1181 (the parity grid + the saturation question), K1343 (Wallach set of H_{ν_W}).

## The claim (target-innocent, the rail)

> **CKM = U_up†·U_down is the overlap of TWO FORCED discrete frames. Both frames' addresses sit at discrete Wallach/cohomology/parity points — un-tunable — so whatever V_cb/V_ub the frame mismatch {1,α,α²}↔{1,3,5} produces is FORCED-not-FIT, exactly as V_us=1/√20 already was (T2530, blind).**

## The discreteness rail (why the addresses can't be fitted)

The weighted-Bergman family H_{ν_W} carries its modes at **discrete** addresses on two axes, both quantized by D_IV⁵'s own harmonic analysis — no continuous parameter:
- **radial ν_W** — the Wallach set (discrete points {0, a/2} ∪ continuum, K1343); the holomorphic discrete series is indexed by **discrete weights** (Harish-Chandra). *This is the axis the leptons sit on: {5/2, 3/2, 0} = the ρ-vector components (T2517), Wallach degenerations — forced, target-innocent (T1829 predates the fit).*
- **angular degree k** — the compact-dual Q⁵ cohomology ring **ℤ[h]/h⁶** (discrete integer degrees {0,…,5}, T1929, Elie's *blind* computation — no mass input); rank-1 in every degree, so each generation class is a single-row K-type.

A fermion mode can only occupy a discrete (ν_W, k) address. **The leptons are ν_W-addresses; the quarks are degree-addresses; both are forced by the same discreteness.** There is no continuous address to slide to reproduce a CKM number — that is the whole target-innocence guarantee, and it is why the mixing is a *reading*, not a fit.

## Down-quark addresses = the odd degrees {1,3,5} (forced)

1. **Winding lives on Q⁵ cohomology** ℤ[h]/h⁶ (T1929, blind) — the generation cycles are the odd-degree classes h¹/h³/h⁵.
2. **Boundary-fold parity** (K1181): charge×N_c = the weight/address (down 1/3→1, e→3); the boundary fold forces degree+weight **same-parity**, so **down + charged-lepton land on the ODD shelves {1,3,5}** — forced, not chosen.
3. At **ν_W = N_c = 3** (the Wallach threshold, forced), the FK Pochhammer on {1,3,5} gives the down ladder 1:20:840 (T2513), and the ℂ³ SVD gives m_s/m_d = 20 → V_us = 1/√20 blind (T2529/T2530). **The down frame {1,3,5} is the forced odd-degree ladder.**

## Up-quark addresses = the even shelves {0,2,4} (forced); frame {1,α,α²} = boundary distance

1. **Parity grid** (K1181, same boundary fold): up (2/3→2) + neutrino (0) land on the **EVEN shelves {0,2,4}** — forced by the same rule that puts down on odd. So the up **addresses** are {0,2,4}, target-innocent.
2. **The frame values {1,α,α²} are the boundary-distance Yukawa ladder** (T2515): y = exp(−geodesic distance to the boundary), α = 1/N_max per shell — the **top** sits AT the boundary (shell 0, saturated y_t = 1), **charm** one shell in (y_c = α, T2515, m_c = α·v/√2 at 0.05%), **up** two shells in (α²).
3. **The load-bearing pin (K1181's open question, ruled here as the theory rail): top-saturation acts on the mass NORM, not on the K-type ADDRESS.** The geodesic distance to the boundary is a *property of the shelf*, so the parity-forced shelf {0,2,4} fixes the *address* and the *distance* fixes the Yukawa magnitude. Saturation is just "the top's shelf is the boundary shell (distance 0, y = 1)" — it sets the *norm*, it does not move the *address*. **⟹ the up wavefunctions stay at {0,2,4}; the frame {1,α,α²} rides the boundary distance on those fixed addresses.** *(This is the rail's one load-bearing assumption — see the honest tier: Elie's V_cb/V_ub compute is its test. If the mixing lands clean, saturation-acts-on-norm is confirmed; if it is off, the up wavefunction distorts off {0,2,4} and the address-clean premise fails. Stated as a can-fail pin, not asserted as closed.)*

## Why the mismatch produces CKM at all (the geometry)

The up frame (even {0,2,4}) and the down frame (odd {1,3,5}) are **two bases of one generation space** (rank+1 = 3, T2525). Their naive overlap has opposite parity → near-diagonal, W-mediated (T2544). The mixing magnitude is the **forced cross-shelf overlap** ⟨even {0,2,4} | odd {1,3,5}⟩ — the off-rank-1 breaking (T2519, the misalignment that also gives J ∝ det[H_u,H_d] ≠ 0). Both frames forced ⟹ the overlap is forced. Elie's compute reads V_cb/V_ub off this overlap; my rail says the *inputs* to that overlap are discrete and un-tunable.

## Honest tier + the G-gates

- **Three generations = rank+1 = 3** — Derived (T2525, KW support-flag).
- **The addresses are discrete/forced** (down odd {1,3,5} via Q⁵ cohomology + parity; up even {0,2,4} via parity) — **Structure-Derived, target-innocent** (Elie's blind cohomology T1929; parity from the boundary fold; ν_W=N_c=3 the Wallach threshold — no mass/mixing input, **G4 clean**).
- **The up frame {1,α,α²}** — Structure-Derived (T2515 boundary distance).
- **The saturation-acts-on-norm-not-address pin** — the rail's one load-bearing assumption; **Elie's V_cb/V_ub compute is its can-fail test** (G2: if forced, it lands without a fit; if it needs a mass input, the pin failed).
- **G2 (target-innocent):** the addresses are forced by discreteness *before* any CKM number is looked at — there is no continuous address to fit. **This is the rail: the frames are pinned, so the overlap is a reading.**

**Lyra, 2026-08-22 (atlas reading, R52 forward — the theory rail under Elie's frame mismatch). CKM = U_up†·U_down overlaps TWO FORCED discrete frames: down = odd shelves {1,3,5} (Q⁵ cohomology ℤ[h]/h⁶ blind + boundary-fold parity + ν_W=N_c=3 Wallach threshold, → 1:20:840, V_us=1/√20 blind); up = even shelves {0,2,4} (same parity fold) with frame values {1,α,α²} = boundary-distance Yukawa (T2515, top-saturated). Both address-sets are DISCRETE (Wallach radial + Q⁵ cohomology degrees + Harish-Chandra discreteness) — the SAME forcing that pins the leptons to {5/2,3/2,0} (T2517) — so there is NO continuous knob to tune to V_cb/V_ub; whatever the mismatch produces is forced-not-fit. Load-bearing pin (ruled as the rail, can-fail): top-saturation acts on the NORM (boundary distance) not the ADDRESS (parity shelf), so the up wavefunctions stay at {0,2,4} — Elie's V_cb/V_ub compute is its test. Target-innocent (G2); no mass-input (G4). Nothing pushed; CP existence-only.**
