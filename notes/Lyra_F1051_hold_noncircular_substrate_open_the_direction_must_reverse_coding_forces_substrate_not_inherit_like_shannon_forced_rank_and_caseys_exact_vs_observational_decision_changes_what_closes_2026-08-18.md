# F1051 — Holding the non-circular-substrate question open (the object is real, unbuilt). The escape is type-valid + homed (progress) but CIRCULAR-AS-BUILT (the corpus code is [g, 2^rank, N_c] = [7,4,3], distance = N_c by construction — Grace's theorem: a geometry can't force its own defining parameter) and EXPONENTIAL-NOT-EXACT (topological suppression is astronomically-long, not τ = ∞). The one unbuilt object: a code whose distance-3 comes from the CODING side and FORCES the substrate's N_c=3 — the direction must reverse (coding → substrate, like Shannon forced rank=2 from *outside*), not substrate → code (inherit). And a load-bearing decision is Casey's: is proton stability ABSOLUTE (τ=∞, the flagship — then exponential fails) or OBSERVATIONAL (astronomically-long-but-finite — then exponential works AND predicts a finite τ_p, a new falsifiable prediction)? It changes what closes.

**Lyra, Tuesday 2026-08-18, Round 24. Holding the non-circular-substrate question open (my call: the object is real; whether it can be built is the frontier). Accepting the honest verdict (circular + exponential; the fork re-asserts one level down). Reconnected: F1050 ([[7,1,3]] home), Grace's boundary theorem (geometry can't force its own defining params), Forcing+Evidence (rank=2 = the one EXTERNAL forcing, Shannon). LA on D_IV⁵. Nothing pushed; CP existence-only.**

## The verdict, accepted — why it doesn't close
1. **Circular-as-built.** The corpus code is **[g, 2^rank, N_c] = [7, 4, 3]** — its distance is *N_c by construction*. The substrate D_IV⁵ carries N_c = 3 as a **defining parameter** (the short-root multiplicity), and any code built on it inherits that. So "distance = N_c" is a **restatement of the substrate**, not a forcing (Grace's theorem: a geometry cannot force its own defining parameters — the same reason n_C = 5 is a datum). The [[7,1,3]] homes the escape and validates the type — real progress — but it does not force N_c.
2. **Exponential, not exact.** A topological code suppresses the logical error rate **exponentially in the distance** — astronomically long, but **finite**. That contradicts the Five-Absence flagship's τ_p = ∞ (exact). So the *exact* stability still comes from the **charge/winding (circular)**, and the *correction* is **exponential (target-innocent but not exact)**. **The fork re-asserts one level down.**

## The one unbuilt object — the direction must reverse
The circularity is entirely about **direction**:
$$\text{circular:}\quad \text{substrate}(N_c{=}3)\ \to\ \text{code}\ \to\ \text{distance}=N_c \quad(\text{inherit})$$
$$\text{non-circular:}\quad \underbrace{\text{correctability}}_{\text{external, target-innocent}}\ \to\ \text{distance}=3\ \to\ \textbf{forces}\ \text{substrate}\ N_c{=}3 \quad(\text{force})$$
**The non-circular substrate is a topological code whose distance-3 is a CODING requirement (single-error-correction) that *constrains* the substrate to have N_c = 3 — not a code read off a substrate already built with N_c = 3.** The precedent exists and is instructive: **rank = 2 was forced from *outside* the geometry** (Shannon: a commitment's binary alphabet = 2 = the Jordan rank) — *not* from the domain's own invariants (which can't force themselves, Grace). So N_c = 3 would need the **same shape of external forcing**: a coding/information requirement that fixes the number, which the geometry then *realizes* rather than *supplies*. That object is **type-valid but unbuilt** (Grace's call, and it's the right one). I flag what it must satisfy: the distance-3 must be **derived from correctability alone** (the coding universal), and the map distance → short-root-multiplicity must run *coding → geometry*, exhibited, not inherited.

## Casey's decision — load-bearing, and it changes what closes
The exact/exponential gap is not just a technicality; it forks on a genuine physics choice only Casey can make:

- **ABSOLUTE (τ_p = ∞, the Five-Absence flagship stands):** exponential ≠ exact, so the coding route **cannot** supply the exact stability — the exactness stays the charge/winding (circular), and **N_c = 3 remains an input.** The flagship is preserved; the prize is not taken.
- **OBSERVATIONAL (τ_p astronomically-long-but-finite, consistent with τ > 10³⁴ yr):** exponential suppression is **fine** — and now the coding route is *viable* (given a non-circular substrate), with a bonus: it **predicts a finite τ_p and a rate bound** — a **new, falsifiable prediction** (a controlled *departure* from "no proton decay," not a contradiction of data). This trades the flagship's "exact stability" for a sharp testable number.

**These are genuinely different theories.** ABSOLUTE keeps the Five-Absence "no proton decay" and takes N_c = 3 as input. OBSERVATIONAL makes proton decay a prediction (huge τ_p, bounded rate) and opens the door to forcing N_c = 3 by coding. **I do not have a basis to choose — it's a foundational commitment, and it's yours.**

## The two wins stand regardless of the fork
- **Type-validity** (Grace): the K1686 barrier is genuinely dodged — a topological code is all-discrete, so winding → check types cleanly. That's banked structural progress independent of forcing.
- **The deconfinement prediction** (Elie): fell out of the code picture and is confirmed at ~155 MeV. A real, data-touching win — *provided* Δ is derived, not borrowed from Λ_QCD (borrowing it is the same circularity, flagged).

## Tier / guard
- **Frontier, held open:** the non-circular substrate is type-valid but **unbuilt**; the exact-vs-exponential gap is real; the fork stands.
- **Guard (absolute):** never claim "N_c = 3 forced" while implying exact stability — the escape needs *either* [exact-not-exponential **and** non-circular substrate] *or* [Casey rules OBSERVATIONAL **and** non-circular substrate]. As of now neither holds, so **n_C = 5 (via N_c = 3) is an input** — one dimensionful ruler + one dimensionless integer, GR-level honest. **Never "commitment forces D_IV⁵" until the non-circular code is exhibited.**

## Handoffs
- **@Grace** — the decider you own: is there a non-circular substrate — a topological code whose distance-3 comes from the coding side (correctability), forcing the geometry rather than inheriting from it? The test is *directional*: does the distance-3 requirement *constrain* the short-root multiplicity to 3 (force), or is it read off the pre-built N_c = 3 (inherit)? If you can only build the inherit-version, the honest answer is "type-valid, unbuilt" — and that's a clean result.
- **@Elie** — the deconfinement win holds *iff* Δ is derived, not Λ_QCD-borrowed (Grace's circularity, same shape). Carry the owed three (finite τ_p, the rate bound, derived Δ) — and note the τ_p bound only *matters* under Casey's OBSERVATIONAL branch; under ABSOLUTE it's moot.
- **@Keeper** — held open at the frontier; the fork stands; N_c = 3 is an input pending [non-circular substrate] and Casey's exact/observational ruling. Two wins (type-validity, deconfinement) bank independent of the fork. Don't let "the escape is type-valid and homed" read as "N_c = 3 forced."
- **@Casey** — the honest state, and there's one question that's genuinely yours, not ours. We found the escape is real in *kind* — the proton as a quantum code, a knot that protects a record — but it's built out of pieces the geometry already had (so it *describes* the three, doesn't *force* it), and a real quantum code protects things *astronomically* well, not *perfectly*. So the last wall is a fork that only you can turn, because it's about what BST *claims*: **is the proton stable forever (exactly), or just for an unimaginably long time?** If *forever*, our flagship stands and the three stays an honest input — clean, GR-level. If *unimaginably long but finite*, then the code picture can work, the three might be forced, and the price is that the proton *does* decay — which becomes a prediction we'd stake, with a number. That's not a math question; it's a decision about what the theory is willing to say the world does. I've laid both branches out honestly and I'm not nudging — but I think it's the decision the whole n_C = 5 question now waits on. Nothing pushed.

Notes only; no theorem/toy claimed (holding the frontier open + the decision surfaced). F1051: escape type-valid + homed (F1050 [[7,1,3]]) but CIRCULAR-AS-BUILT ([g,2^rank,N_c]=[7,4,3], distance=N_c by construction; Grace theorem: geometry can't force own defining param) + EXPONENTIAL-NOT-EXACT (topological suppression finite, contradicts τ=∞). Fork re-asserts. Non-circular object (unbuilt): direction must REVERSE — coding(correctability, external, target-innocent)→distance-3→FORCES substrate N_c=3, like Shannon forced rank=2 from OUTSIDE (not substrate→code inherit). Casey's decision (load-bearing): proton stability ABSOLUTE (τ=∞ flagship → exponential fails → N_c=3 input) vs OBSERVATIONAL (finite-huge → exponential works + PREDICTS finite τ_p, new falsifiable, forces N_c=3 if non-circular substrate). Two wins stand (type-validity, deconfinement-iff-Δ-derived). Guard: never N_c=3-forced-implying-exact-stability; n_C=5 an input until [non-circular substrate] + [exact OR observational-ruling]. — Lyra
