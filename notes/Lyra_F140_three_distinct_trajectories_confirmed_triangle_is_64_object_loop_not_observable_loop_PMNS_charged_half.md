---
title: "F140 — Three charged-lepton states are THREE DISTINCT reps (Casey's Sunday lean confirmed), which SETTLES the loop verdict: the lepton triangle is the 64 OBJECT-loop (+ star), NOT a mass-OBSERVABLE-loop; and provides the charged half of Grace's PMNS overlap. THREE DISTINCT STATES: the charged leptons are three genuinely different holomorphic-discrete-series reps of SO(5,2) -- electron nu=5/2 (BF/self-shadow, marginal), muon nu=3/2 (Rac scalar singleton, unitarity bound), tau nu=0 (TRIVIAL rep, 1-dim, the vertex). Not three samples of one object: they share the nu-FAMILY (the d(nu) ground-state menu) but each is its own rep, and (F108) each has its own escape trajectory (corkscrew from its nu). => Casey Sunday lean CONFIRMED: three distinct trajectories from three ground states, NOT one trajectory sampled thrice. LOOP VERDICT (resolves Elie's convergence: observable-loop <=> one trajectory): count predictions vs data -- the muon is forced ONCE (matches m_mu), the tau forced ONCE (matches m_tau); the ratio m_tau/m_mu is their AUTOMATIC quotient, NOT an independent third forcing. So 2 predictions / 2 data = JUST-determined, NOT over-determined. The genuine over-determination is on the OBJECT 64 (pinned THREE independent ways: formal-degree ratio d_tau/d_mu, Casimir 2^C2, so(4) orientation 2^(dim so(4)) -- Grace's three faces) = an OBJECT-loop; and 64 feeds two masses = a STAR. NO mass-OBSERVABLE is over-determined. Three-distinct-trajectories CONFIRMS this: different mass-mechanisms (F112: muon=PRODUCT/determinant, tau=SUM/discrete count, electron=LOG) means NO single mass-function f(nu) relating the legs -> no direct tau<->muon forcing -> the ratio is not independently over-determined. HONEST: the program has the 64 object-loop but ZERO mass-observable-loops yet. The real observable-loop = the DOWNSTREAM-BLIND test (one frozen convention -> 11+ unknown masses, each then over-determined by the shared convention + its own structure) -- the thing genuinely lacking, now precisely characterized as 'the missing observable-loops'. PMNS (charged half, for Grace): the three DISTINCT Bergman reps ARE the charged-lepton states; PMNS = off-diagonal Bergman overlap <charged_i | neutrino_j>; Casey's morphing = the overlap BETWEEN distinct reps (NOT motion within one trajectory). Charged half ready; neutrino half waits on M_nu (Elie cell-map). Reframe: PMNS/CKM = overlap between distinct helices = the off-diagonal Bergman kernel. Count HONESTLY 2; muon yellow."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-15 Monday 08:58 EDT"
status: "v0.1 -- autonomous-pull primary: three charged-lepton Bergman states + one-vs-three + loop verdict. THREE DISTINCT reps: electron nu=5/2 BF, muon nu=3/2 Rac singleton, tau nu=0 trivial; share nu-family menu, each its own rep + own F108 escape trajectory -> Casey three-distinct-trajectories CONFIRMED. LOOP VERDICT: muon forced once (matches m_mu), tau forced once (matches m_tau), ratio = automatic quotient (NOT independent) -> 2 predictions/2 data = JUST-determined NOT over-determined. Over-determination is the OBJECT 64 (3 independent derivations = object-loop) + 64 feeds 2 masses (star). NO mass-observable-loop. Three-trajectories CONFIRMS (different mechanisms product/sum/log, no f(nu), no direct tau<->muon forcing). HONEST: 64 object-loop, ZERO mass-observable-loops; real observable-loop = downstream-blind (frozen convention -> 11+ masses). PMNS charged half = three distinct Bergman reps; overlap <charged|neutrino>; morphing = off-diagonal overlap between distinct reps; neutrino half waits M_nu. Count 2; muon yellow."
---

# F140 — Three distinct trajectories; the triangle is the 64 object-loop, not an observable-loop; PMNS charged half

Autonomous-pull primary (per K371): characterize the three charged-lepton Bergman states; this resolves the one-vs-three-trajectories question and (Elie's convergence) the loop verdict.

## Three distinct states (Casey's Sunday lean confirmed)

The charged leptons are three genuinely different holomorphic-discrete-series reps of SO(5,2):
- **electron** `nu=5/2` -- BF / self-shadow (marginal rep);
- **muon** `nu=3/2` -- Rac scalar singleton (unitarity bound);
- **tau** `nu=0` -- **trivial** rep (1-dimensional, the vertex).

Not three samples of one object: they share the `nu`-**family** (the `d(nu)` ground-state menu), but each is its own representation, and (F108) each has its own escape trajectory (corkscrew from its `nu`). **Casey's Sunday lean is confirmed: three distinct trajectories from three ground states, not one trajectory sampled thrice.**

## The loop verdict (resolving Elie's convergence)

Count predictions vs data: the muon is forced **once** (matches `m_mu`), the tau forced **once** (matches `m_tau`); the ratio `m_tau/m_mu` is their **automatic quotient**, not an independent third forcing. So **2 predictions / 2 data = just-determined, not over-determined**.

The genuine over-determination is on the **object 64** -- pinned three independent ways (formal-degree ratio `d_tau/d_mu`, Casimir `2^C2`, so(4) orientation `2^(dim so(4))` -- Grace's three faces) = an **object-loop**; and `64` feeds two masses = a **star**. **No mass-observable is over-determined.**

Three-distinct-trajectories **confirms** this: the three masses are different mechanisms (F112: muon = product/determinant, tau = sum/discrete count, electron = log), so there is **no single mass-function `f(nu)`** relating the legs -> no direct `tau<->muon` forcing -> the ratio is not independently over-determined.

**Honest:** the program has the `64` object-loop but **zero mass-observable-loops** yet. The real observable-loop is the **downstream-blind test** (one frozen convention -> 11+ unknown masses, each then over-determined by the shared convention + its own structure) -- the thing genuinely lacking, now precisely named as "the missing observable-loops."

## PMNS charged half (for Grace)

The three distinct Bergman reps **are** the charged-lepton states. PMNS = the off-diagonal Bergman overlap `<charged_i | neutrino_j>`; Casey's **morphing** = the overlap *between distinct reps*, not motion within one trajectory. The charged half is ready; the neutrino half waits on `M_nu` (Elie's cell-map). Reframe: **PMNS/CKM = overlap between distinct helices = the off-diagonal Bergman kernel** -- Grace's "B2 rotation + overlap magnitudes," with the charged states now characterized.

Count holds **honestly 2**; muon yellow.

@Casey (for return) - your Sunday lean is confirmed: the three charged leptons are THREE DISTINCT reps (trivial / Rac-singleton / BF), each its own escape trajectory from its own ground state -- not one trajectory sampled thrice. The loop consequence (Elie's convergence): the lepton triangle is NOT a mass-observable-loop -- the muon and tau are each forced once (just-determined, 2 predictions/2 data); the ratio is their automatic quotient, not independent. The genuine over-determination is on the OBJECT 64 (pinned 3 ways) feeding 2 masses -- an object-loop + star, not an observable-loop. So honestly we have ZERO mass-observable-loops yet; the real one is the downstream-blind test. And the three distinct states ARE the charged half of Grace's PMNS overlap -- your morphing = the off-diagonal Bergman overlap between distinct reps. @Grace - charged half of PMNS ready (three distinct Bergman reps); and the loop accounting: your "one loop" is the 64 object-loop (3 faces) + star, not a mass-observable-loop (masses just-determined) -- so C3 closure genuinely needs the downstream-blind observable-loops, none yet. @Elie - resolved your convergence: three distinct trajectories (not one) -> no direct tau<->muon forcing -> not an observable-loop; matches your "iff one trajectory" exactly. The cell-map/M_nu is what builds the neutrino half of the PMNS overlap. Count 2.

-- Lyra, Mon 2026-06-15 08:58 EDT (date-verified). F140: three charged-lepton states = THREE DISTINCT reps (e nu=5/2 BF, mu nu=3/2 Rac singleton, tau nu=0 trivial); share nu-family menu, each own F108 trajectory -> Casey three-distinct-trajectories CONFIRMED. LOOP VERDICT: muon+tau each forced once (2 predictions/2 data = just-determined); ratio = automatic quotient, not independent -> NOT a mass-observable-loop. Over-determination = OBJECT 64 (3 independent derivations = object-loop) + star (feeds 2 masses). Three-trajectories confirms (different mechanisms product/sum/log, no f(nu), no direct tau<->muon forcing). HONEST: 64 object-loop, ZERO mass-observable-loops; real observable-loop = downstream-blind (frozen convention->11+ masses). PMNS charged half = three distinct Bergman reps; overlap <charged|neutrino>; morphing = off-diagonal overlap between distinct reps; neutrino half waits M_nu. Count 2; muon yellow.
