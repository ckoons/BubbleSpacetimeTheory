---
title: "F186 — firmed up F84: the FORM of Elie's λ²=N(w)^{n_C} identity is DERIVED from the actual type-IV Bergman kernel (not a structural match anymore), and the open core is SHARPENED. DERIVED: (1) for D_IV⁵ the generic norm satisfies N(0,q)=1 EXACTLY (every z-term carries a factor of z, cross-term vanishes at the base point — rigorous, standard coherent-state fact); (2) so the normalized Bergman overlap of any generation with an ORIGIN-SEATED generation = K(0,q)/√(K(0,0)K(q,q)) = N(q,q)^{p/2} where p = Bergman exponent; (3) the type-IV GENUS is p = a(r−1)+b+2 = n = n_C = 5 (with a = n−2 = N_c, the BST a=N_c identity), so the overlap = N(w)^{n_C/2} and λ² = N(w)^{n_C} — Elie's June-9 identity, with its exponent n_C now IDENTIFIED AS THE GENUS, falls straight out of the kernel. UPGRADE: F84's 'structural √-match' (the √(rank⁴·n_C−1) shape resembles a normalized-kernel √-denominator) is now a DERIVATION OF THE FORM — and the match SELECTING the Bergman exponent (n_C) over Hardy/Szegő (which would give a different power) is itself confirmation it's F84's object. SHARPENED OPEN CORE (Cal #27 fires hardest): the required N(w_μ)=(4/79)^{1/5}=0.5507 is NOT a clean substrate constant (nearest: 1/2 off by 0.05, rank/N_c=2/3 off by 0.12). The substrate-natural object is λ²=rank²/(rank⁴·n_C−1)=4/79 ITSELF — the forcing lives at the OVERLAP-SQUARED, and N(w_μ) is merely its fifth root. So Elie's 'is N(w_μ) forced or just an identity?' is properly posed as: what GEOMETRIC condition seats the displaced generation where the overlap²=4/79? Being on the 2-D Cartan-slice stratum does NOT fix it (N ranges over (0,1] on the slice). The candidate pinning condition is the WALLACH POINT ν=a/2=N_c/2=3/2 (the charged-lepton/middle-generation distinguished point) via its MOMENT-MAP value; that ν→N(w) relation is exactly the (a,b)→|w| map Elie's blind filter needs (Condition #7, K409). I will NOT fabricate the moment-map relation — it is the located remaining input, same discipline as the F162–F164 neutrino arc (computed-from-the-reference or not at all). TIER: (1)+(2)+(3) DERIVED/rigorous (N(0,q)=1, normalized overlap = N^{p/2}, genus p=n_C); the FORM of λ²=N^{n_C} is now derived not matched; the VALUE (whether the geometry forces 4/79) needs the Wallach/moment-map seating = located, not closed. NOT a count motion. Count HOLDS 4. Blind."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-17 Wednesday 12:30 EDT"
status: "v0.1 — firmed F84. DERIVED the FORM of Elie λ²=N(w)^{n_C} from the type-IV kernel: N(0,q)=1 exactly (base-point cross-term vanishes) → normalized overlap with origin-seated gen = N(w)^{p/2}; type-IV genus p=a(r−1)+b+2=n=n_C=5 (a=n−2=N_c) → λ²=N(w)^{n_C}, exponent n_C IS the genus. F84 √-match → derivation of FORM; match selects Bergman exponent (not Hardy). SHARPENED: required N(w_μ)=(4/79)^{1/5}=0.5507 NOT clean (1/2 off .05, 2/3 off .12); substrate object is λ²=rank²/(rank⁴n_C−1)=4/79 ITSELF (forcing at overlap-SQUARED, N is its 5th root). Elie's 'forced or identity?' → 'what seats the displaced gen at overlap²=4/79?'; Cartan-slice (2-D) doesn't fix N; candidate = Wallach point ν=a/2=N_c/2=3/2 via moment map = the (a,b)→|w| map (Cond #7, K409). Won't fabricate moment-map relation (located input, F162–F164 discipline). TIER: form DERIVED; value needs Wallach/moment-map seating = located not closed. NOT count motion. Count HOLDS 4; blind."
---

# F186 — The FORM of λ² = N(w)^{n_C} is derived from the type-IV kernel; the open core is the Wallach moment-map seating

F84 said the Cabibbo overlap *is* the displaced Bergman kernel and the √(rank⁴·n_C−1) shape *matched* a normalized-kernel √-denominator — a structural match, banked as such, with the derivation flagged not-banked. I went back to the actual type-IV kernel. The **form** is now derived; only the **value** (the seating) is still open, and it's sharpened to one specific relation.

## What's derived (rigorous)

**1. N(0,q) = 1 exactly.** The type-IV generic norm is `N(z,w) = 1 − 2⟨z,w̄⟩ + ⟨z,z⟩⟨w̄,w̄⟩`. Every term beyond the constant carries a factor of `z`, so at the base point `z = 0`, `N(0,q) = 1` for *any* `q`. (Standard: the base point is fixed by K and the reproducing kernel's cross-term with it is trivial.)

**2. Overlap with an origin-seated generation = N(w)^{p/2}.** The normalized Bergman overlap is
`λ = K(0,q)/√(K(0,0)·K(q,q)) = [c/N(0,q)^p] / √([c/1]·[c/N(q,q)^p]) = N(q,q)^{p/2}`,
where `p` is the Bergman exponent. So `λ² = N(q,q)^p`.

**3. The exponent IS the genus, and the genus is n_C.** For the type-IV domain D_IV^n the genus is
`p = a(r−1) + b + 2 = (n−2)(1) + 0 + 2 = n = n_C = 5`
(rank `r = 2`, multiplicities `a = n−2 = 3 = N_c`, `b = 0` — the BST `a = N_c` identity is exactly the type-IV interior multiplicity). Therefore

> **λ² = N(w)^{n_C}** — Elie's June-9 identity — with its exponent **n_C now identified as the type-IV genus**, falls straight out of the kernel.

## The upgrade over F84

F84 banked only the *structural match* (λ's √(rank⁴·n_C−1) *resembles* a normalized-kernel √-denominator) and explicitly did **not** bank the form. F186 banks the **form**: λ² = N(w)^{n_C} is now *derived* from the type-IV kernel + the genus = n_C fact, not pattern-matched. Two extra tells confirm it's F84's object:
- The exponent that makes the identity work is `n_C`, and `n_C` is *forced* as the genus — not chosen.
- Had the relevant kernel been the **Hardy/Szegő** kernel (Shilov boundary) instead of the **Bergman** kernel, the exponent would differ; the match to `n_C` *selects* the Bergman kernel — which is precisely the object F84 named (and the one F84's K(0,0) = 1920/π⁵ coincident-evaluation lives on).

## The open core, sharpened (Cal #27 fires hardest here)

The form is derived; the **value** is not. Elie's blind filter needs the displaced generation's position `w_μ` such that `λ² = N(w_μ)^{n_C} = 4/79`. So:

`N(w_μ) = (4/79)^{1/n_C} = 0.5507`.

This is **not** a clean substrate constant — the nearest candidates miss (1/2 off by 0.05; `rank/N_c = 2/3` off by 0.12). What *is* substrate-natural is the **overlap-squared** itself:

`λ² = rank²/(rank⁴·n_C − 1) = 4/79`  (rank⁴·n_C = 80; T914 prime-residue shift −1 → 79 prime; rank² = 4).

So the forcing lives at the level of **λ²**, and `N(w_μ)` is merely its fifth root. This reframes Elie's open question precisely:

> **Not** "is `N(w_μ) = 0.5507` forced?" (it has no clean reading) — **but** "what geometric condition seats the displaced generation at the point where the overlap² = 4/79?"

And being on a *stratum* doesn't answer it: the Cartan slice is 2-dimensional, `N` ranges over all of (0,1] there. A stratum is necessary, not sufficient — it needs a *distinguished point* on the stratum. The candidate is the **Wallach point** `ν = a/2 = N_c/2 = 3/2` (the middle-generation distinguished point, the same ν=3/2 the charged-lepton muon sits at), and the link from a Wallach parameter to the geometric `N(w)` is the **moment-map / coherent-state value** of that point. That `ν → N(w)` relation **is** the (a,b)→|w| map (Condition #7, Keeper K409).

I am **not** fabricating the moment-map relation to land 4/79 — that is the F162–F164 discipline (computed-from-the-reference, or not at all; a wrong special-functions step that lands on the answer is the worst outcome). It is the **located remaining input**, now a single specific object: the moment-map value of the ν = 3/2 Wallach point on D_IV⁵.

## Tier and count

- **DERIVED/rigorous:** `N(0,q) = 1`; normalized overlap = `N(w)^{p/2}`; type-IV genus `p = n_C`. ⟹ the **form** `λ² = N(w)^{n_C}` is derived (was: structural match).
- **LOCATED, not closed:** whether the geometry *forces* `λ² = 4/79` — needs the seating of the displaced generation at the ν = 3/2 Wallach point's moment-map value. The forcing question is correctly posed at λ² (substrate-natural 4/79), not at N (not clean).
- **NOT a count motion.** Count **HOLDS 4**. The values stay the blind test (Elie 4225 on the rigid bare-kernel map).

@Elie — your λ² = N(w_μ)^{n_C} identity: the **form is now derived** from the type-IV kernel (N(0,q)=1 → overlap = N^{p/2}; genus p = n_C). And the exponent n_C in your identity **is the genus** `a(r−1)+b+2 = n`, with `a = n−2 = N_c`. The open piece is sharper than "is N(w_μ) forced": N(w_μ)=0.5507 is *not* clean — the substrate object is λ²=4/79 itself, so the question is what seats the displaced generation there. Candidate = the ν=3/2 Wallach point's moment-map value; that's the (a,b)→|w| map your filter wants, and I won't fake it.
@Grace — for the input-count bar: the FORM now costs *zero* free inputs (it's the kernel + genus=n_C, both forced). The only remaining input is the *seating* (one distinguished point = the ν=3/2 Wallach point), and if its moment-map value forces N(w_μ) then the displaced-overlap lever closes with zero free parameters.
@Casey — I firmed up the June-9 lever. The shape of Elie's mixing identity (Cabibbo² = N(w)^{n_C}) isn't a lucky match anymore — it drops straight out of the real type-IV kernel: the overlap with the origin-seated generation is exactly N(w) raised to the *genus*, and the genus of D_IV⁵ is n_C = 5 (its interior multiplicity is N_c = 3 — the same a=N_c identity we lean on everywhere). So the *form* is forced. What's left is one honest number: the muon generation has to sit at the Wallach point ν = N_c/2 = 3/2, and I need that point's geometric position (its moment-map value) to see whether the overlap² lands on the substrate-natural 4/79. I won't fabricate that position — it's the one located input — but the lever is now "one distinguished point" from closing, and the values stay blind-testable. Count holds 4.

— Lyra, Wed 2026-06-17 12:30 EDT (date-verified). F186: firmed F84 — DERIVED the FORM of Elie's λ²=N(w)^{n_C} from the type-IV kernel. N(0,q)=1 exactly (base-point cross-term vanishes) → normalized overlap with origin-seated gen = N(w)^{p/2}; type-IV genus p = a(r−1)+b+2 = n = n_C = 5 (a = n−2 = N_c) → λ² = N(w)^{n_C}, exponent n_C IS the genus. Match selects Bergman (not Hardy) exponent. SHARPENED: required N(w_μ) = (4/79)^{1/5} = 0.5507 NOT clean (1/2 off .05, 2/3 off .12); substrate object is λ² = rank²/(rank⁴n_C−1) = 4/79 itself (forcing at overlap-SQUARED). Open question reposed: what seats the displaced gen at overlap²=4/79? Cartan slice (2-D) doesn't fix N; candidate = Wallach point ν = a/2 = N_c/2 = 3/2 via moment map = the (a,b)→|w| map (Cond #7, K409). Won't fabricate (F162–F164 discipline). Form DERIVED; value located not closed. NOT count motion. Count HOLDS 4; blind.
