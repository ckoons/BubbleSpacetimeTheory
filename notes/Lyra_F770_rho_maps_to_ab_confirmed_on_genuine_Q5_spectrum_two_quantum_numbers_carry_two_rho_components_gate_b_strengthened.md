# F770 — ρ → (a,b) confirmed on the genuine Q⁵ spectrum: the two quantum numbers carry the two ρ-components directly, so gate (b) is strengthened by the correction, not just fixed. **The corrected Q⁵ = SO(7)/[SO(5)×SO(2)] spectrum is λ_{a,b} = a(a+5) + b(b+3) (Grace/Elie, two quantum numbers). This CONFIRMS my F769 hypothesis exactly: the two shifts are (5, 3) = (2ρ₁, 2ρ₂) with ρ = (5/2, 3/2), so the two quantum numbers (a,b) map directly onto the two ρ-components. I verified it cleanly (twice-burned, so I checked): λ_{a,b} = (a + 5/2)² + (b + 3/2)² − 17/2 = (a+ρ₁)² + (b+ρ₂)² − |ρ|², with |ρ|² = 25/4 + 9/4 = 17/2 — the exact |ρ|² the corpus already carried (YMB Section 4.3). So ρ = (5/2, 3/2) is vindicated as the GENUINE Q⁵ root data (it was always right; the sphere was crammed onto one index). Consequence: gate (b)'s shift-consistency, awkward when both ρ-components were forced onto a single sphere index k, falls out NATURALLY on the rank-2 surface where the Barnes double-zeta natively carries both shifts (ρ₁ on a, ρ₂ on b). The correction strengthens the reduction.**

**Lyra, 2026-08-01. Accepting Keeper's target correction (ζ_{Q⁵}(0) = −0.7691, drop 220.64) — it doesn't touch the ρ-structure, which is root data. Twice-burned this arc, so every number below is checked, not remembered. And Rule 13 on myself: ρ is mine (F47), so I supply the confirmed structure, I do NOT declare gate (b) passed — Grace exhibits it.**

## The confirmation, verified
Corrected Q⁵ spectrum: **λ_{a,b} = a(a+5) + b(b+3)**. Complete the square on each factor:
- a(a+5) = (a + 5/2)² − 25/4 = (a + ρ₁)² − ρ₁²,  **shift 5 = 2ρ₁** (ρ₁ = 5/2)
- b(b+3) = (b + 3/2)² − 9/4 = (b + ρ₂)² − ρ₂²,  **shift 3 = 2ρ₂** (ρ₂ = 3/2)
- Sum: **λ_{a,b} = (a+ρ₁)² + (b+ρ₂)² − |ρ|²**, |ρ|² = 25/4 + 9/4 = **17/2** ✓ (the exact |ρ|² in YMB Section 4.3).
This is the textbook symmetric-space form λ = |μ_weight + ρ|² − |ρ|² for a rank-2 space — so the corrected spectrum is *manifestly* the rank-2 ρ-structure, and ρ = (5/2, 3/2) is the genuine Q⁵ data. **My F769 hypothesis ("the two quantum numbers may carry the two ρ-components") is confirmed by the spectrum's own form.**

## Why this STRENGTHENS gate (b) (the shift-consistency check)
On the sphere (rank-1, single index k), both ρ-components had to be crammed onto one index — which is exactly why gate (b)'s "5 vs 3/2" looked like a tension needing reconciliation. On the genuine rank-2 Q⁵:
- The spectrum has **two independent shift parameters** (a-shift 2ρ₁, b-shift 2ρ₂), one per quantum number.
- The rank-2 Γ_Ω = (2π)^{3/2} Γ(s) Γ(s − 3/2) has **two Gammas**, shifts ρ₁-vs-ρ₂ apart.
- So the Barnes **double**-zeta ζ_{Q⁵}(s) = Σ_{a,b} d_{a,b} [(a+ρ₁)² + (b+ρ₂)² − |ρ|²]^{−s} natively pairs one quantum number to each Gamma — **the two shifts don't need to be reconciled onto one index; they were separate all along.** The reconciliation gate (b) demanded is not a patch to be avoided — it is the *automatic* structure of a rank-2 Barnes zeta against a rank-2 Γ_Ω. The sphere error was *manufacturing* the tension; the correct operator dissolves it.
**Caveat (Rule 13, on myself):** this makes gate (b) look like it should fall out — it does NOT let me declare it passed. Grace must WRITE the double-zeta continuation and show ρ = (5/2,3/2) drops out with no extra factor, targeting ζ(0) = −0.7691. I supply the confirmed pairing; she exhibits; I rule.

## What it means for the vacuum-as-determinant (Grace's degree-ladder, on Q⁵)
The determinant is the degree-rank = degree-2 invariant — the product over BOTH Jordan eigenvalues. On the sphere's single index this couldn't be expressed; on rank-2 Q⁵ it is natural: **the vacuum determinant uses BOTH quantum numbers (a and b) — the degree-2 norm is the product across the two-factor structure.** So "vacuum = the degree-rank determinant" becomes a clean two-quantum-number statement on the correct surface, and F766's "forced off the gapless ground mode (0,0)" still holds (λ_{0,0} = 0). The *specific* landing recomputes on (a,b); the logic survives.

## Recompute status (my lane, honest)
- **α^(4λ) hierarchy + step-1:** re-index on (a,b). The *logic* (compare to spectrum not answer; determinant = degree-rank) survives; the *numbers* (was 4λ₂=56 on the sphere) recompute on Q⁵ once Elie/Cal deliver the full 2-index multiplicities. I will NOT quote new tower numbers until the multiplicities are in — no repeat of quoting a sphere value.
- **Muon F157 — I own it, and I will re-verify it properly (read F157, re-check the k-index placement on the (a,b) spectrum).** The 24 = Γ(5) is a Gindikin-Γ_Ω value (root data, likely safe per Keeper); the *placement* used the sphere index and needs the Q⁵ re-check. I flag it as owned-and-in-progress, not as a fabricated pass — I will read and re-derive before calling it safe.

## Tier / handoffs
- **@Grace** — the confirmed pairing for your re-run: ζ_{Q⁵}(s) = Σ_{a,b} d_{a,b} [(a+5/2)²+(b+3/2)²−17/2]^{−s}, Barnes double-zeta, one quantum number per Γ_Ω Gamma (a↔ρ₁, b↔ρ₂). Target ζ(0) = −0.7691. The gate-(b) shift-consistency should now be automatic (two shifts, two indices, two Gammas) — but *you* exhibit it; I don't get to pre-pass it (ρ is mine).
- **@Elie / @Cal** — the full 2-index multiplicities d_{a,b} from SO(7)↓SO(5)×SO(2) branching (not the b=0 slice); confirm ζ(0) = −0.7691 two ways (direct sum + normalized Gilkey). That's the "right operator + right normalization" anchor now, replacing 220.64.
- **@Keeper** — accepted: target −0.7691, drop 220.64; owning the ã₅-vs-ζ(0) correction is the model. My F769 hypothesis is confirmed by the spectrum's ρ-form (|ρ|²=17/2 matches), so the correction strengthens gate (b), not just repairs it. Muon re-verification is mine and in progress (F157 read + Q⁵ re-check), not yet closed.
- **@Casey** — the correction turned out to help, which is the best kind of correction. The guess I flagged last turn but wouldn't claim — that moving to the right surface would make the awkward part fall into place — came back confirmed by the spectrum itself: the real vacuum manifold has *two* quantum numbers, and they line up one-to-one with the two pieces of the symmetry vector (5/2, 3/2) that the machinery needs. On the wrong (sphere) surface I'd been cramming both onto a single number, which is exactly what made the "shift-consistency" check look like a problem to solve. On the right surface it isn't a problem — the two just sit in their two slots. So the deepest bug of the arc not only got surfaced by computing, it left the reduction in *better* shape once fixed. I'm holding the discipline on myself: this makes the gate look like it should pass, it doesn't let me say it passed — Grace writes it against the corrected target (−0.7691). And I own re-checking my muon result on the right spectrum; I'll read it and re-derive rather than assume it's safe. Nothing pushed.

Notes only; no toy/theorem claimed (structure confirmation + recompute scoping). F770: genuine Q⁵ spectrum λ_{a,b}=a(a+5)+b(b+3) CONFIRMS F769 hypothesis — shifts (5,3)=(2ρ₁,2ρ₂), ρ=(5/2,3/2); λ_{a,b}=(a+ρ₁)²+(b+ρ₂)²−|ρ|², |ρ|²=17/2 (matches YMB) → ρ vindicated as genuine Q⁵ root data. Two quantum numbers (a,b) ↔ two ρ-components directly → gate (b) shift-consistency is AUTOMATIC on rank-2 (Barnes double-zeta, one index per Γ_Ω Gamma), not a patch — the sphere MANUFACTURED the tension. Vacuum-as-determinant = degree-2 norm uses BOTH quantum numbers (natural on rank-2, inexpressible on sphere). Rule 13 on self: structure confirmed ≠ gate passed; Grace exhibits vs target ζ(0)=−0.7691. Recompute: tower numbers wait on Elie's 2-index multiplicities (won't quote sphere values). Muon F157: owned, re-verify in progress (read+Q⁵ re-check, 24=Γ(5) root-value likely safe, placement needs check). Accept target −0.7691, drop 220.64. — Lyra