# F681 — The one operator is the **Toeplitz condensate operator Ô = T_φ** (multiply by the VEV profile φ, project back to holomorphic), and reading it as a *multiplication* operator rather than a *rank-1 projector state* dissolves the "single condensate → one mass" problem I'd treated as fundamental in F677. **A Toeplitz operator T_φ restricted to the 3-generation subspace is a full-rank Hermitian 3×3 (verified: one scalar symbol → three distinct eigenvalues, rank 3) — so ONE condensate gives THREE masses directly, as eigenvalues, with no separate seesaw.** F677's rank-1 result was an artifact of modeling the condensate as a state |o⟩⟨o| (a projector); the physical BST object (Keeper K875, my Vol 16 "Engine") is the Toeplitz operator, which is not rank-1. **So the whole lepton-value problem collapses to: write ONE symbol φ (the F583 condensate profile) on the ONE domain H²(D_IV⁵), form Ô = T_φ, diagonalize ONCE — eigenvalues are the masses, eigenvectors are the PMNS mixing, and V_μτ is an off-diagonal element.** The only remaining freedom is the symbol φ, which must be SOURCED (F583, the neutral SU(2)_L singlet VEV), not reconstructed/tuned. Spectral target the diagonalization must hit (Keeper): the log-gaps are **unequal, ratio ~1.9 : 1** (ln(m_μ/m_e)=5.33 vs ln(m_τ/m_μ)=2.82) → genuine 3×3, not a single geometric ladder. **Blind bank criteria O1–O5 pre-registered below** — fixed before φ is written, so a match is forced-not-fit.

**Lyra, Fri 2026-07-24 10:28 EDT. Casey's "one operator, one domain" steer, executed to its endpoint: the object is Ô = T_φ. It supersedes both my partial models (F675 diagonal width, F677 rank-1 seesaw) — those were wrong pictures of the one operator. The values stay gated on the sourced symbol; I set the object + the blind test, I do not claim the spectrum.**

## The resolution (Casey's steer to its endpoint)
The morning walked F675 (diagonal width) → F677 (rank-1 projector → off-diagonal seesaw) → here. Each was a partial model of one object. The object, named by Keeper (K875) and formalized as my Vol 16 Engine, is the **Toeplitz operator on the Bergman space with the condensate as symbol**:
$$ \hat O = T_\varphi = P\, M_\varphi\, P, \qquad M_{ij} = \langle \psi_i \,|\, T_\varphi \,|\, \psi_j\rangle, $$
P = Bergman projection, M_φ = multiply by the condensate profile φ, ψ_i = the three generation states. **Masses = eigenvalues of Ô, mixing = eigenvectors, V_μτ = off-diagonal element — all from ONE diagonalization.**

## ★ The rank crux resolved (Keeper's question; owns F677)
F677 concluded "single condensate = rank-1 = one mass, so light generations need an off-diagonal seesaw." **That was an artifact of the wrong model** — it treated the condensate as a *state* |o⟩⟨o| (a rank-1 projector). The Toeplitz operator T_φ is a *multiplication* operator; its restriction to the 3-generation subspace is a **general full-rank Hermitian 3×3**. Verified: one scalar positive symbol φ, Toeplitz-restricted to three states, gives three distinct eigenvalues (rank 3). **So one condensate gives three masses directly — no seesaw needed, no higher-rank condensate needed.** The "single Higgs" (F583) and "three masses" are reconciled by the operator being multiplication, not projection. (Own it: F677's seesaw was a real step but rested on the rank-1-projector model; the Toeplitz picture supersedes it.)

## The spectral target (Keeper: not a single ladder)
The diagonalization must produce log-eigenvalue-gaps in ratio **~1.9 : 1** (ln(m_μ/m_e) = 5.33, ln(m_τ/m_μ) = 2.82; ratio 1.889), NOT equal. So Ô is genuinely 3×3 (unequal gaps) — a single geometric α-ladder (equal gaps) is excluded by the data itself. The symbol φ must carry this asymmetry.

## ★ Pre-registered BLIND bank criteria O1–O5 (fixed before φ is written)
The lepton spectrum BANKS as derived only if ALL hold on one diagonalization of Ô = T_φ:
- **O1 — one operator, no per-generation freedom:** the 3×3 M is the restriction of a SINGLE Toeplitz Ô; its entries are NOT independently tunable. (Independently-tuned entries = fit → FAIL.)
- **O2 — forced symbol:** φ = the BST condensate (F583 neutral SU(2)_L singlet VEV profile), specified with ZERO free parameters (no adjustable width/shape/normalization). (Tunable φ = fit → FAIL.)
- **O3 — forced subspace:** the three ψ_i are the forced generation states (the Casimir eigenstates / Wallach classification F676), not chosen to fit.
- **O4 — one diagonalization, both observables:** eigenvalues → masses (σ-scored vs 1 : 206.77 : 3477) AND eigenvectors → PMNS, BOTH from the single Ô. (Masses and mixing needing separate inputs = two fits → FAIL.)
- **O5 — rank + gap-structure emerge:** rank(M) = 3 and the log-gap ratio ~1.9:1 come from φ's structure, NOT imposed; and the SAME operator with the neutrino symbol (m₁=0) gives rank 2 (F589) — a cross-sector check. (Rank/gaps put in by hand = FAIL.)

## The one open pin (sourcing, not reconstruction)
**Write the symbol φ** — the condensate profile on D_IV⁵ (F583). That is the entire remaining content: once φ is written, Ô = T_φ is fixed, one diagonalization gives masses + mixing + V_μτ together, and O1–O5 decide forced-vs-fit blind. **φ must be SOURCED from the BST condensate structure (F583/F582), not reconstructed-and-tuned to hit 1:207:3477** — that is the trap, and it is exactly where the discipline holds. This is Grace's/my sourcing lane (the condensate = the neutral SU(2)_L singlet VEV; its Toeplitz symbol on the Shilov boundary).

## Why this is stopping-rule-safe (not a ninth reframe)
It invents no new mechanism. It is the one-domain, one-operator FORM of the question we've circled all day — the Toeplitz operator IS F585 read as an operator (Vol 16 Engine). It supersedes my two partial models (F675, F677) by naming the object they were approximating. The value question is now a single well-posed eigenvalue problem gated on one sourced symbol.

## What survives untouched
- **F676 (three generations = rank+1) STANDS** — it is "the three eigenspaces of Ô = the three Wallach phases," a spectral (basis-independent) fact, Casey's classification in operator language. Coordinate-independent (Elie's spec(M)=spec(QᵀMQ)).
- **F680 (ν=k/2, one ladder)** — the k-labels are Ô's own Casimir index; nothing to reconcile once it's one operator.

## Tiers / handoffs
- **@Grace** — the whole value problem is now: **write the condensate symbol φ (F583) on D_IV⁵**; then Ô = T_φ, diagonalize once. The rank-1 obstruction is gone (Toeplitz is full-rank — one condensate, three masses). Your sourcing lane is φ's profile (the neutral SU(2)_L singlet VEV, its Berezin/Toeplitz symbol on Š), NOT a two-point distance and NOT a coordinate reconciliation — those dissolved. O1–O5 are the blind gate. Please source φ target-innocently; if it hits 1:207:3477 + PMNS on one diagonalization with no tuning, the leptons bank; if it needs a tuned φ, they stay structural.
- **@Elie** — your toy 4829 (basis-independence) IS the proof this is well-posed; the operator is Ô = T_φ. Your W4 harness now reads: diagonalize the ONE Ô once, check O4 (masses AND PMNS together) + O5 (rank 3, log-gap ~1.9:1, neutrino rank 2). It fires the instant Grace writes φ. Verify my Toeplitz-full-rank claim independently (multiplication operator restricted to 3 states = full rank, vs projector = rank 1).
- **@Keeper** — your rank crux is resolved: the condensate is Toeplitz *multiplication* (full-rank), not a rank-1 *projector* — one condensate gives three masses (owns F677's rank-1, which was a modeling artifact). Ô = T_φ, blind criteria O1–O5 pre-registered, log-gap target ~1.9:1. Stopping-rule-safe (the one-operator form, no new mechanism; supersedes F675/F677). The single open pin is the sourced symbol φ (F583). Log: value problem = one eigenvalue problem, gated on φ.
- **@Casey** — your steer, all the way to the object. The lepton masses are the eigenvalues of ONE operator on the ONE domain — the Toeplitz operator that multiplies by the Higgs condensate profile and projects back. And building it that way dissolved the thing I was calling a hard obstruction an hour ago: I'd argued a single condensate can only give one mass (rank-1), so the muon and electron had to come from a separate seesaw. That was wrong — it came from picturing the condensate as a single *state*. As a *multiplication* operator it's full-rank, and one condensate gives all three masses as its three eigenvalues, directly. I verified it. So there's no seesaw, no α-tower, no coordinate dictionary as separate pieces — there's one operator, you diagonalize it once, and the masses, the mixing, and the muon-tau overlap all fall out together. The entire remaining question is a single honest one: what is the condensate's profile φ? Write that one function — sourced from our own Higgs-condensate structure, not tuned to the answer — and the diagonalization decides it, against blind criteria I've fixed in advance. I stopped fragmenting it into lanes and put it on one operator, one domain, like you said — and it turned out two of my own morning models were just partial views of it.

Notes only; no toys/theorems claimed. The one operator = Toeplitz condensate Ô = T_φ (multiplication, NOT rank-1 projector) → full-rank → one condensate gives THREE masses (owns/supersedes F677 rank-1, F675 width). Masses=eigenvalues, mixing=eigenvectors, V_μτ=off-diagonal, all from ONE diagonalization. Spectral target: log-gaps ~1.9:1 (genuine 3×3, not a ladder). Blind O1–O5 pre-registered. Single open pin: SOURCE the symbol φ (F583 condensate), don't reconstruct/tune. Stopping-rule-safe (one-operator form). F676 survives as spectral fact. — Lyra
