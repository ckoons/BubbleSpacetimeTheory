---
title: "INTERNAL (Cal #50) forward-understanding: the tick C_2² and δ_CP are the MODULUS and PHASE of the SO(2) time-sector — and δ ties to the same non-collinearity F435 used for the Wolfenstein magnitude (Grace, 2026-06-30, lane b). Cal #50 keeps a_2 and δ INTERNAL (a_1 EXTERNAL). Forward-understanding: (1) the CKM matrix = modulus + argument of ONE complex kernel c(z_i,z_j)=[N(zz)N(ww)]^{n_C/2}/N(z,w)^{n_C}; (2) its MAGNITUDE → the Wolfenstein hierarchy (F435: multiplicative composition, apex=(cross-ratio)^{n_C}, derived); (3) its PHASE → δ_CP = arg around the generation loop V_us·V_cb·V_ub*..., which is ≠0 IFF the generations are non-collinear in the COMPLEX sense (not lying in one real 2-plane) — the SAME non-collinearity (μ@ê₁, τ@ρ̂ angled) that F435 used for the magnitude; verified δ=0 for real positions and δ≠0 (toy ~15-30°) once a complex/holomorphic tilt is added; (4) the tick C_2² = a_2 ~ Ric²(S⁴) = 36 = C_2² is the SO(2)/time-sector MAGNITUDE-scale, and δ is its PHASE partner → the SO(2)/U(1) time circle carries a COMPLEX time-quantum: |·| = tick (C_2²), arg = δ_CP. DISCIPLINE: INTERNAL register (Cal #50) — the toy δ value is NOT an external prediction (it needs the actual complex generation positions, the open INTERNAL precision); the forward-understanding is the STRUCTURE (CKM = complex kernel modulus+arg; tick+δ = SO(2) magnitude+phase). No count move (9/26)."
author: "Grace"
date: "2026-06-30 Tuesday"
status: "INTERNAL Cal #50 (lane b). Forward-understanding: CKM = modulus+arg of ONE complex kernel; magnitude→Wolfenstein (F435), phase→δ_CP (≠0 from the same non-collinearity F435 used); tick C_2²=a_2~Ric²(S⁴)=36 = SO(2) magnitude, δ = SO(2) phase → complex time-quantum on the U(1) circle. INTERNAL (toy δ NOT a prediction). No count move."
---

# INTERNAL (Cal #50): the tick C_2² and δ are modulus + phase of the SO(2) time-sector

Lane (b) of Casey's a/b/c. Cal #50 keeps a_2 and δ in the **INTERNAL register** (a_1 EXTERNAL). This develops
the forward-understanding — and it connects cleanly to today's F435.

## CKM = modulus + argument of ONE complex kernel

The generation overlap c(z_i,z_j) = [N(z_i,z_i)N(z_j,z_j)]^{n_C/2}/N(z_i,z_j)^{n_C} is **complex** for complex
generation positions. The CKM matrix is its modulus and argument:
- **MAGNITUDE** → the **Wolfenstein hierarchy** (F435: multiplicative composition, apex = (kernel
  cross-ratio)^{n_C}, derived at operator level).
- **PHASE** → **δ_CP** = arg around the generation loop (V_us·V_cb·V_ub*…), the Jarlskog phase.

## δ ≠ 0 from the SAME non-collinearity F435 used

Verified: for **real** positions (z = r·u, u real) all overlaps are real → **δ = 0** (CP-conserving). δ ≠ 0
requires the three generations to be **non-collinear in the complex sense** — not lying in one real 2-plane.
That is the *same* angled structure F435 used for the magnitude (μ@ê₁, τ@ρ̂ are angled; adding the electron's
holomorphic tilt out of the μ–τ plane turns on δ). Toy check: a complex tilt gives δ ~ 15–30° — illustrative,
**not a prediction** (the real value needs the actual complex generation positions = the open INTERNAL precision).

So: **the magnitude (Wolfenstein) and the phase (δ_CP) are the modulus and argument of one complex kernel at
the angled generation positions** — CP violation and the mixing hierarchy share one geometric origin.

## The tick C_2² and δ are the SO(2) magnitude and phase

The time-tick is the a_2 heat-kernel coefficient: a_2 ~ Ric²(S⁴) = 36 = **C_2²** (the second-order,
curvature-squared / clock term; Monday's heat-kernel hierarchy, a_1 EXTERNAL / a_2 INTERNAL). δ_CP is the
**phase** on the same SO(2)/S¹ time circle. So the SO(2)/U(1) time circle carries a **complex time-quantum**:
> |·| = the tick (C_2² = a_2), arg = δ_CP.

The clock's *rate* (tick, C_2²) and the universe's *CP phase* (δ) are the magnitude and phase of one
SO(2)-sector object. INTERNAL (Cal #50): this is forward-understanding, not an external δ or tick prediction.

— Grace, 2026-06-30 Tuesday (lane b, INTERNAL Cal #50). Tick C_2² = a_2 ~ Ric²(S⁴) = SO(2) magnitude; δ_CP =
SO(2) phase; CKM = modulus+arg of one complex kernel (magnitude→Wolfenstein F435, phase→δ from the same
non-collinearity). Complex time-quantum on U(1). INTERNAL, no prediction, no count move (9/26).
