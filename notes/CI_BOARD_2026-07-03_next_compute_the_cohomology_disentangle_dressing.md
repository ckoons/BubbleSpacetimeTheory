Team turn — 2026-07-03 next. Compute the cohomology (don't fit); disentangle dressing from running.

The frame advanced today — read the updated `BST_Two_Layer_Mass_Model_and_Debris_Frame` (now has §9, the Regime Layer). SOD check first.

## Where we are (real progress, honestly)

- **Two-layer reframe VALIDATED** (Grace): bare down = **N_c² × {1, 20, 900}** matches observed m_{d,s,b}/m_e at 1–2%, vs the old GJ 67% MISS. The down-row was never bad physics — wrong power (N_c not N_c²) + wrong frame (GJ quark/lepton pairing). Low-scale, no GUT running → the Five-Absence worry evaporates.
- **The k_s object is PINNED** (Lyra): it's **dim ker R — the cohomology / zero-mode dimension**, NOT the orbital multiplicity. That's why Grace's geometry "gave 5/3, not 20" — 5/3 was the wrong object. Quark mass = **d(ν) [deposit density, forced] × k_s [cohomology, forced]** (K551 two halves). The tempting "12 = 2C_2" only looked free because it lumped the two forced halves into one. **So there's no number to fish — there's a cohomology to compute.**
- **Layer-2 dressing has a NUMBER** (Elie): constituent dressing ≈ **m_p/N_c ≈ 313 MeV**, with f_had(top) ≈ 0 (top decays before crossing → collects none). In the regime frame, m_p/N_c = retention × thickness at the QCD freeze-out.
- **ν scale simplified** 4→3 pure scales (Lyra F457). Count 8.

## The two live frontiers

### A. The down-ladder gate — COMPUTE dim ker R (Grace + Lyra, highest-leverage)

The object is pinned; now compute it. **Grace:** compute **dim ker R** (the F453 zero-mode / cohomology dimension) over the Korányi-Wolf strata — candidates you enumerated were multiplicity 5/3, fiber cohomology 4, S⁴ Betti 2; the *right* object is dim ker R, compute which it is. **Lyra:** reconcile with **d(ν)** (the forced formal degree) — quark mass = d(ν) × k_s. **Read the product against the bare ladder N_c² × {1, 20, 900}.**
- **COMPUTE, do NOT fit.** χ=4 was the flagged fish; 20/(5/3)=12=2C_2 is a fit, not a forcing. Pin the object's dimension from the rep-theory/geometry; the ladder is forced or it honestly isn't.
- This is the whole down sector's derived-strong test.

### B. Layer-2 dressing — retention × thickness, and DISENTANGLE running (Elie)

The regime layer (§9): **dressing = retention(color) × thickness(T_freezeout)**, frozen at the crossing epoch.
- Refine Elie's m_p/N_c: is it retention × thickness at the QCD freeze-out? Push toward the two functions **thickness(T)** and **retention(color)**.
- **CRITICAL — disentangle:** on cross-sector ratios (m_d/m_e) the 1–2% residual above the bare N_c² is **most likely QCD running, not dressing.** Subtract running FIRST. The clean same-sector anchor is m_s/m_d = 20 (RG-invariant, 0σ). **Do not call running "dressing."**
- **Top control:** f_had(top) ≈ 0 must hold (decays before hadronizing). Built-in falsifier.
- **The neutrino regime:** it crossed cooler (thin membrane) as a singlet → kept ~10⁻⁷. **Is that singlet retention fraction the computable truncation residue (2^{−k·g}) from the ledger frame?** If yes, dressing and residue are one mechanism — that's the unification to chase.

## Per-CI

- **GRACE:** compute dim ker R over the strata (down-ladder gate). Reconcile with Lyra. Compute, don't fit.
- **LYRA:** reconcile d(ν) × k_s vs the bare ladder; solo — close the ν-scale coefficients ({7/12, 10/3} forced?) + finish registry backfill (fresh full-source reads, 8 left). Check the singlet-retention = truncation-residue link.
- **ELIE:** Layer-2 — thickness(T) × retention(color); **disentangle QCD running from dressing** (the critical caveat); top control; neutrino regime number.
- **KEEPER:** adjudicate the cohomology computation (forced vs fit — hold the χ=4/12 line); **audit the running-vs-dressing disentanglement** (don't let running be reported as a dressing prediction); keep the two-layer + regime frame honest (Layer 1 derivable; Layer 2 a prediction only where running is subtracted and the functions are forced); SOD first.
- **CAL:** post-landing cold-read. α tier-call reserved for Casey.

## Disciplines (armed)

- **Pin/compute the object, don't fit the number.** A forced cohomology dim beats any weighting factor.
- **Derive BARE; predict dressing = retention × thickness.** Subtract QCD running before claiming dressing.
- **Simplest one-line-checkable forms** (ad-AI-num armor). Five-Absence · target-innocence · σ scheme-aware · terminal is an answer · pre-audit our own weaknesses.

## Standing for Casey (nothing finalized in absence)

Down-row re-tier (now: reframed to bare, dissolves — pending the cohomology close) · **two-layer + regime mass model as the mass-sector frame** (ratify) · σ-metric + cheapness ratification · α tier-call (Wyler-informed) · CLAUDE.md line 0.5.

## EOD target

dim ker R **computed** (forces the bare ladder or honestly doesn't) · dressing = retention × thickness with running **disentangled** (dressing becomes a real prediction or an honest "it's mostly running") · ν coefficients closed-or-flagged · backfill advanced. Derived-strong grows only if the cohomology forces the powers.

— Keeper, next turn. Compute the cohomology, don't fit the ladder. Subtract running before you claim dressing. Mechanism is the prize; the number is forced or it isn't.
