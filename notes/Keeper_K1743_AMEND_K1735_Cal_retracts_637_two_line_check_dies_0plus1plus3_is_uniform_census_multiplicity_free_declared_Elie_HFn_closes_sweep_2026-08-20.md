---
node_type: k_audit
id: K1743
title: "AMENDMENT (Cal §641 retracts his own §637; K1735/K1742 amended) + the round's convergent fix: the census must be declared MULTIPLICITY-FREE, and Elie's H_F(n) build CLOSES Grace's open sweep. ★ CAL RETRACTS §637 (own it, amend K1735): the '0+1+3=4 is a TWO-line check (census + Coleman–Mandula ℝ→0)' finding was built on the WRONG object — ℂ⊕ℍ⊕M₃(ℝ) with ℝ at multiplicity 3. The CORRECT algebra is ℂ⊕ℍ⊕ℝ with **ℝ at multiplicity 1** (K1724, End_K theorem). A real block at mult 1 has a ZERO-dimensional unitary group (U(ℝ)={±1}, dim 0), so Rule A (imaginary units: 0) and Rule B (dim of unitary group: 0) AGREE — there is NO multiplicity-3 block, hence NO block where the rules differ, hence NO two-line check. ⟹ '0+1+3=4' is a UNIFORM census read exactly as Lyra originally wrote it; Cal's instruction to split it was WRONG. AMEND K1735/K1742: DROP the 'two-line referee check' and the 'state both lines' rule for the count. The '7=g' flag (Rule B totaling 7) DIES with it (it was arithmetic on the wrong M₃(ℝ)). ★ COLEMAN–MANDULA STILL LOAD-BEARING — but for the DIFFERENT claim it was always for (§629 D): SO(V₁₂)⊂K is GEOMETRIC/spacetime, so it can't be internal. This is a SEPARATE argument about a DIFFERENT object (the geometric SO(V₁₂)), NOT the 'second line' of the census count. So 'no internal color' has TWO independent supports that must NOT be conflated: (i) CENSUS — the ℝ block (mult 1) gives U(ℝ)={±1}=ℤ₂ → zero continuous generators; (ii) COLEMAN–MANDULA — the geometric SO(V₁₂) can't be internal. Cal merged (i) and (ii) in §637; they live in different objects. ★ SURVIVES §641 (Cal confirms, none involve the ℝ-block multiplicity): SEP-1, SEP-2, the reference-class / one-integer / LADDER finding, both separators. The three-invariant/one-integer THEOREM STANDS. Six of seven §640 findings survive; the CONNES finding is STRENGTHENED (ℝ vs M₃(ℂ) is a BIGGER gap than 'M₃(ℝ) vs M₃(ℂ)' claimed). ★ CP CONFIRMED (Cal now agrees my K1742 fix was better): CP-violation needs 3 generations (physical CP phases = (n−1)(n−2)/2; n=2→0, n=3→1) → CP = census∧rank, both legs load-bearing, existence-only. Cal's error was auditing INTERNAL CONSISTENCY without checking the PREMISE ('CP-existence is I1 single' was consistent but false). ★★ THE CONVERGENT FIX — DECLARE THE CENSUS MULTIPLICITY-FREE (Elie + Grace + Cal, all three): 'census = multiset of reality types {ℂ,ℍ,ℝ}, MULTIPLICITY-FREE.' Load-bearing for BOTH separators. WHY: End_K(V₁₂)=ℝ regardless of dim V₁₂ (V₁₂ is an IRREDUCIBLE real rep → commutant ℝ by Schur, any dimension). The '3' in V₁₂ is the REP DIMENSION (= N_c = n−2), a SEPARATE reading, NOT a census multiplicity. Under the multiplicity-CARRYING reading (B), D_IV⁴={ℝ2} vs D_IV⁵={ℝ3} would NOT be equal and every separator dies — so the multiplicity-free reading (A) is not optional, it's what the theorem already relies on, and it must be STATED (a referee who assumes census carries N_c=n−2 calls the separators invalid). ★★ ELIE'S H_F(n) BUILD CLOSES GRACE'S SWEEP: Grace flagged the census leg proven only for spinor-reality rows (H_F(n≠5) unbuilt). Elie BUILT H_F(n) (Lane B, toy 5401): the Peirce decomposition of the spin factor gives dims (1, n−2, 1) — at n=5, dim V₁₂=3=N_c (banked); and across n, CHARGE is ℂ always, COLOR is ℝ always, only the SPINOR block moves. ⟹ the spinor-keyed sweep IS the full three-block reality-type sweep → the census leg is RIGOROUS across the D_IV family (NOT scoped to the spinor row), PROVIDED the multiplicity-free declaration. So LIFT Grace's scope-limit (superseded by Elie's build) AND lock the multiplicity-free declaration. Correct Elie's earlier 5398/5400: 'undefined across TYPES' (true, SEP-3/§640) ≠ 'undefined across n WITHIN type IV' (false — every D_IV^n has all three ingredients). ★ GRACE'S SEPARATOR-CROSSING FIX (adopt): the census separator is D_IV⁷ (n=7 → spinor genuinely real → ℍ block collapses → SU(2)_L unforced), NOT D_IV⁴ (n=4 still quaternionic by Bott). D_IV⁴ is the TWIST separator (orientability). F1067 must read D_IV⁷ for census everywhere. ★★ NOTATION FIX AT SOURCE (Cal's request — the recurring same-name collision, 8th overall, 2nd on this object): write the color block as '(ℝ, mult 1)', NEVER '(ℝ, 3)' or 'M₃(ℝ)'. The internal algebra is ℂ⊕ℍ⊕ℝ; the '3' is dim V₁₂ = N_c, a rep-dimension reading that does NOT enter the algebra. Lyra's v0.3 Connes fix wrote 'ℂ⊕ℍ⊕M₃(ℝ)' — the SAME slip; CORRECT to ℂ⊕ℍ⊕ℝ. ★★ ADOPT CAL'S AUDITOR-PREMISE RULE (STANDING, the round's most valuable methodology output): any AUDITOR finding that changes an artifact must state its PREMISE on a separate checkable line and NAME the source. Rationale (Cal, sharp): a wrong REFEREE finding propagates FURTHER and FASTER than a wrong author claim — everyone re-derives author theorems, but 'the auditor's output is the one artifact nobody audits'; and §637 shipped with a Python block that READS as verification when it was arithmetic on an unstated premise ('the computation was never the weak point; the unstated premise was'). Had §637 carried 'premise: commutant = ℂ⊕ℍ⊕M₃(ℝ), source: centerpiece v0.8', Keeper greps it against §629/K1724 in seconds. ⟹ Keeper adopts symmetrically: my K-audit findings that change an artifact carry a premise-line + source. ★ NET (amendments): DROP the two-line check + 7=g (K1735/K1742 amended; 0+1+3=4 is uniform census); Coleman–Mandula is a SEPARATE support for no-internal-color (not the count's 2nd line); SEP-1/2 + reference-class/ladder + both separators STAND; CP=census∧rank confirmed; DECLARE census multiplicity-free (Elie/Grace/Cal); Elie's H_F(n) build makes the census leg RIGOROUS (Grace's scope lifted); census separator = D_IV⁷ not D_IV⁴; notation '(ℝ, mult 1)' at source, algebra = ℂ⊕ℍ⊕ℝ; adopt the auditor-premise rule. The theorem stood a fifth round of attack — including a retracted auditor headline — and is MORE honest."
date: 2026-08-20
author: Keeper
verdict: "AMEND K1735/K1742 (Cal §641 retracts §637). The '0+1+3=4 two-line check (census + Coleman–Mandula)' is RETRACTED — it was built on ℂ⊕ℍ⊕M₃(ℝ) (ℝ mult 3); the correct algebra is ℂ⊕ℍ⊕ℝ (ℝ mult 1, K1724), where U(ℝ) is 0-dimensional so Rules A and B AGREE — no two-line check, '0+1+3=4' is UNIFORM census (Lyra's original). '7=g' DIES. Coleman–Mandula still supports 'no internal color' but as a SEPARATE argument (geometric SO(V₁₂) can't be internal), not the count's 2nd line — don't conflate the census-ℝ→0 with CM. SURVIVES: SEP-1, SEP-2, reference-class/one-integer/ladder, both separators — THEOREM STANDS. CP=census∧rank CONFIRMED (my K1742 fix was right; Cal's 'I1 single' was consistent-but-false — consistency-checking ≠ fact-checking). ★ CONVERGENT FIX (Elie+Grace+Cal): DECLARE census = multiset of reality types, MULTIPLICITY-FREE (End_K(V₁₂)=ℝ any dim; the '3'=dim V₁₂=N_c is rep-dimension, separate). Under a multiplicity-carrying reading every separator dies — so it's load-bearing, must be stated. ELIE'S H_F(n) BUILD closes Grace's open sweep: across n, charge=ℂ, color=ℝ always, only spinor moves → census leg RIGOROUS across the family (not scoped to spinor row). Census separator = D_IV⁷ (spinor→ℝ), NOT D_IV⁴ (still ℍ at n=4; that's the twist separator). NOTATION at source: '(ℝ, mult 1)' never 'M₃(ℝ)'/'(ℝ,3)'; algebra = ℂ⊕ℍ⊕ℝ (Lyra's v0.3 M₃(ℝ) is the same slip — correct it). ADOPT CAL'S AUDITOR-PREMISE RULE (standing): auditor findings that change an artifact state premise + source on a checkable line — a wrong referee finding propagates faster than a wrong author claim (nobody audits the auditor). Connes finding STRENGTHENED (ℝ vs M₃(ℂ) is a bigger gap). Theorem more honest after a retracted auditor headline. Nothing pushed. CP existence-only."
related: [K1742, K1741, K1739, K1735, K1724, "Cal §629", "Cal §637", "Cal §641", "Elie toy 5401", "Grace Lane B separator-crossing", "Lyra F1067 v0.3", "SM-ASSIGNMENT-TABLE-v0.2", "feedback_subscript_the_overloaded_symbol_int_vs_function_of_n", "feedback_commit_the_checker_half_blind", "feedback_external_audit_beats_self_vigilance", "feedback_enumerate_alternatives_before_therefore_false_dichotomy"]
---

# K1743 — amend K1735 (Cal retracts §637): the two-line check dies; declare the census multiplicity-free

## Cal retracts §637 (amend K1735/K1742)
The "0+1+3=4 is a two-line check (census + Coleman–Mandula)" was built on **ℂ⊕ℍ⊕M₃(ℝ)** (ℝ at mult 3). The correct algebra is **ℂ⊕ℍ⊕ℝ**, ℝ at **mult 1** (K1724). A real block at mult 1 has a **0-dimensional** unitary group, so "imaginary units" and "dim of unitary group" **agree** — no multiplicity-3 block, **no two-line check**. **"0+1+3=4" is a uniform census read** (Lyra's original). **DROP** the two-line rule and the "7=g" flag (arithmetic on the wrong object).

**Coleman–Mandula still matters** — but as the *separate* argument it always was (SO(V₁₂)⊂K is geometric, can't be internal), **not** the second line of the count. "No internal color" has two independent supports (census ℝ→ℤ₂; Coleman–Mandula on the geometric SO(V₁₂)) — don't conflate them.

**Survives:** SEP-1, SEP-2, the reference-class/one-integer/ladder, both separators. **The theorem stands.** CP = census∧rank **confirmed** (my K1742 fix was right; Cal's "I1 single" was consistent-but-false).

## The convergent fix — declare the census multiplicity-free (Elie + Grace + Cal)
**"Census = multiset of reality types {ℂ,ℍ,ℝ}, multiplicity-free."** Load-bearing for both separators. `End_K(V₁₂)=ℝ` regardless of dim V₁₂ (irreducible real rep → commutant ℝ, Schur). The "3" is **dim V₁₂ = N_c = n−2**, a rep-dimension reading, **not** a census multiplicity. Under a multiplicity-carrying reading every separator dies, so this must be **stated**.

**Elie's H_F(n) build closes Grace's open sweep:** across n, charge = ℂ, color = ℝ always, only the spinor block moves → the census leg is **rigorous across the D_IV family** (not scoped to the spinor row). **Census separator = D_IV⁷** (spinor → ℝ), **not D_IV⁴** (still ℍ at n=4; that's the twist separator) — F1067 must read D_IV⁷ for census everywhere.

## Notation fix at source (Cal's request)
Write the color block **"(ℝ, mult 1)"**, never "(ℝ,3)" or "M₃(ℝ)". The algebra is **ℂ⊕ℍ⊕ℝ**; the "3" is dim V₁₂ = N_c, a separate reading. Lyra's v0.3 wrote "ℂ⊕ℍ⊕M₃(ℝ)" — the same slip; correct to ℂ⊕ℍ⊕ℝ.

## Adopt Cal's auditor-premise rule (standing)
Any **auditor finding that changes an artifact** states its **premise on a separate checkable line + names the source**. A wrong referee finding propagates faster than a wrong author claim — nobody audits the auditor's output, and a Python block reads as verification when it's arithmetic on an unstated premise. Keeper adopts it symmetrically.

— Keeper, K1743, 2026-08-20. Two-line check + 7=g dropped (uniform census); SEP-1/2 + ladder + separators stand; CP=census∧rank confirmed; census declared multiplicity-free (Elie's H_F(n) build makes the leg rigorous, Grace's scope lifted); census separator = D_IV⁷; algebra = ℂ⊕ℍ⊕ℝ, "(ℝ, mult 1)" at source; auditor-premise rule adopted. The theorem survived a retracted auditor headline and is more honest. Nothing pushed. CP existence-only.
