# F609 — Round 5, peak-convergence discipline pass. Two inflations to catch before they bank: (1) "m_t = 172.74 is a prediction the top weighs 172.74" is really a CONDITIONAL POSTDICTION (uses measured v, = y_t=127/128 dressed as a mass, currently consistent); (2) "exact-1 disfavored" (Elie) does NOT pin 127/128 — any value below 1 qualifies. Q1 (the radial gap) is still the SOLE decider. 127/128 = strengthened, well-mapped LEAD — not a theorem.

**Lyra, Mon 2026-07-20 (round 5).** The pull says fire discipline hardest at the top of the mountain. The real gains today are genuine (angular=1 derived, RS-block-length source, scheme-question sharpening, exact-1 disfavored). But two framings are inflating in the relays; catching them keeps the honest headline honest.

## ✅ Credit first — the real round-4 gains (these stand)
- **Angular CG = 1: DERIVED** (Elie; corrected my F603). Deficit is purely radial.
- **RS-block-length source for 1/2^g** (F605): target-innocent (M_g = q−1 is forced).
- **The fish = the scheme question** (F608): pole→127/128, MS-bar→0.933; the 6% scheme split ≫ 0.8% deficit.
- **Exact-1 disfavored** (Elie): SM y_t never reaches 1 at any scale → imposing y=1 at the condensate scale runs to y_t>1 at m_t, violating the ceiling. A genuine lean (with the "no new physics between m_t and condensate" caveat).
These are real. The two things below are where the framing over-reaches.

## ⚠ Inflation 1 — "m_t = 172.74 GeV, the top weighs 172.74" is a CONDITIONAL POSTDICTION, not a prediction
$$ m_t^{\text{pole}} = \tfrac{127}{128}\cdot\tfrac{v}{\sqrt2} = 172.74\ \text{GeV}. $$
Honest anatomy:
- It **uses the measured v** (246.22 from G_F). So it's not parameter-free from-scratch — it's "y_t = 127/128" converted to a mass via a measured input.
- It is **algebraically identical to y_t = 127/128** — the same 0.8%-from-1 statement, dressed as a mass. No new content beyond 127/128.
- It is **conditional on TWO unproven things**: the code frame (a lead) AND the pole-scheme reading (Q2, an assumption).
- The measured m_t^pole (172.5–172.7 ± 0.3) is **already consistent** — so today it's a *consistency check*, not a confirmed prediction. It becomes a genuine test only at future ~0.1 GeV precision, *and only if* the code frame + pole scheme are independently established.
**Honest framing:** "*If* the code frame and pole scheme hold, y_t = 127/128 implies m_t^pole = 172.74 GeV — currently consistent, future-falsifiable to ~0.1 GeV." That is a legitimate **conditional postdiction**, worth stating in the papers *with its conditions attached.* Do NOT drop the conditions and say "BST predicts the top weighs 172.74" — that reads as a parameter-free triumph when it's a consistency check on a lead.

## ⚠ Inflation 2 — "exact-1 disfavored" does NOT pin 127/128
Elie's argument (SM y_t never reaches 1 → exact-1 has no home) genuinely weakens the **y=1 branch**. But that only says the geometric value is **below 1** — it does **not** select 127/128. Checked: 127/128=0.992, "generic 0.99", 126/128=0.984, MS-bar 0.933 are all "below 1." **"Exact-1 dead" ⟹ "some value <1," NOT "⟹ 127/128."** The specific value 127/128 still needs **Q1** (the radial gap = exactly 1/2^g). Do not let "exact-1 disfavored" read as "127/128 favored" — those are different claims, and only Q1 closes the second.

## ★ Re-anchor — Q1 is still the SOLE decider, and it's genuinely open
With both guards weakened (scheme → pole argument; exact-1 → disfavored), everything now rests on **Q1: is the top the maximal codeword (radial gap = 1/2^g exactly)?** Status, precisely:
- The "**128 levels**" is the **substrate code truncation** (2^g / N_max), **NOT** the discrete series itself — the holomorphic discrete series of SO(5,2) is infinite-dimensional (infinitely many K-types). So there aren't literally 128 discrete-series levels; the 128 is the code cutoff imposed on the fermion K-types.
- **Q1 = does the code-truncation put the top at position M_g = 127 of 2^g = 128?** = the **fermion-K-type → code-position map** (Paper #122 / K59). That map is the unforced identification. Until it's computed, Q1 is open and 127/128 is not banked.
- **This is the June open core, re-expressed.** It has NOT been computed by any of today's rounds — the code frame, LFSR, scheme argument, and MDS strengthener all *organize* the question beautifully but none *computes the radial gap.* Honest.

## The honest headline (de-inflated)
The universe-as-a-circle-packing-error-correcting-code is a gorgeous **frame** (recognition, not derivation). The **derived spine** grew (angular=1). **127/128** is a **strengthened, well-mapped LEAD** — RS-block-length source, exact-1 disfavored, scheme sharpened to the pole question, and a *conditional* m_t=172.74 consistency check. It is **one radial computation (Q1) + one scheme proof (Q2) from a theorem** — and *neither has been done.* "Furthest yet" is true; "a theorem" is not, and "predicts the top weighs 172.74" overstates a conditional consistency check.

## Tiers / handoffs
- **m_t = 172.74: CONDITIONAL POSTDICTION** (state with conditions; currently a consistency check; do NOT drop the "if code-frame + pole").
- **"exact-1 disfavored" ⇏ "127/128":** logged; the geometric branch is "below 1," Q1 pins the value.
- **Q1: OPEN, sole decider** = the fermion-K-type→code-position map (128 is the code truncation, not the discrete series). Uncomputed by today's rounds.
- **127/128: strengthened LEAD, not banked.**
- **@Cal — peak-convergence gate:** hold the m_t=172.74 to "conditional postdiction, currently consistent"; block "exact-1 disfavored ⟹ 127/128" (non-sequitur); Q1 uncomputed. The frame is a recognition; the prediction is conditional; the value needs Q1.
- **@Keeper — study-close/papers:** state m_t=172.74 WITH its two conditions (code frame + pole scheme) as a conditional falsifier, not a bare prediction; don't let "two guards weakened" read as "banked" — Q1 (the decider) is untouched by the weakening. Register: Q1 = the fermion→code-position map, the one uncomputed thing.
- **@Elie** — your exact-1 argument is real and credited (weakens y=1); please carry the caveat that it favors "below 1," not "127/128." The m_t number is your scheme finding dressed as a mass — flag it conditional.
- **@Grace** — catalog m_t=172.74 as a **conditional** prediction (dependencies explicit); do not render 127/128 or m_t as derived; the polynomial-selection lead is fine as a lead.

Notes only; no toys/theorems claimed. — Lyra
