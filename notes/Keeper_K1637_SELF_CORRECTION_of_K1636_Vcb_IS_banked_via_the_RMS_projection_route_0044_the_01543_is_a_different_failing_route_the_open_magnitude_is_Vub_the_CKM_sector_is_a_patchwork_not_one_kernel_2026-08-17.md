---
node_type: k_audit
id: K1637
title: "SELF-CORRECTION of K1636's V_cb flag (the auditor over-flagged; reconnecting to the actual derivation caught it). K1636 flagged 'sharp V_cb NOT banked, 0.1543 vs obs 0.0418 = 3.7× off, re-tier candidate' — but I was reading the WRONG route. The BANKED V_cb (K1001→K1002) is derived via the **3D→2D RMS projection theorem** (direction 5/√34 forced, F384) and gives **~0.044**, which MATCHES PDG-inclusive (0.044 incl / 0.0417 excl — the data's own 20-year inclusive/exclusive puzzle spans exactly this). The **0.1543** figure (Elie 5318 Pochhammer = Lyra's 1/√42) is a DIFFERENT route this session invoked, and it is the one that fails 3.7×; it must NOT be conflated with the banked V_cb. ⟹ CORRECTION: V_cb IS banked, at K1002's honestly-coarse tier — 'Derived (2-3-mode 3D-projected, without counterexample),' a STRUCTURAL ~5% match against ~5%-uncertain data, with ONE open identification (is the 2-3 mode genuinely 3D? — Lyra pins, Cal confirms; the RMS-applies piece is FORCED by S⁴-zonal symmetry). So the team disagreement resolves: Elie's 'corpus banks V_us only' = WRONG (V_cb banked via RMS); Lyra's 'V_cb=1/√42=0.1543' = WRONG (that's the failing route, not the banked one); my K1636 'V_cb 3.7× off / not banked' = WRONG (read the failing route). The honest CKM-magnitude state: V_us=1/√20 (SHARP bank, K994, 0.31%); V_cb~0.044 (COARSE bank via RMS projection, K1001/K1002, one open 3D-identification); **V_ub is the genuinely OPEN magnitude** (+ δ_CKM). ⟹ the CKM sector is a PATCHWORK of different geometric readings (V_us mass-ratio, V_cb RMS-projection, V_ub open), NOT one unified kernel — which is WHY the 'one kernel → all 13 outputs' fires kept failing (K1002's blind-bar hope did not pan out as a single mechanism). Elie's Clebsch redirect should target V_ub (and could sharpen V_cb coarse→precise), NOT 'V_cb+V_ub.'"
date: 2026-08-17
author: Keeper
verdict: "SELF-CORRECTION of K1636. V_cb IS banked — via the 3D→2D RMS projection (K1001/K1002, ~0.044, matches PDG-inclusive), at a COARSE structural tier (~5% vs ~5%-uncertain data) with one open identification (mode-is-3D; RMS-piece forced by S⁴-zonal symmetry). The 0.1543 (Pochhammer/1/√42, this session) is a DIFFERENT, FAILING route — do NOT conflate. I read the wrong route in K1636; corrected. Honest CKM state: V_us=1/√20 SHARP bank (K994, 0.31%); V_cb~0.044 COARSE bank (RMS); V_ub OPEN (the real redirect target) + δ_CKM. The CKM sector is a PATCHWORK (mass-ratio / RMS-projection / open), not one unified kernel — why the one-kernel fires failed. Redirect: Elie's current-Clebsch targets V_ub (falsifier: give ~1/9.0 for V_ub, leave V_us at 1.00; V_cb already banked coarse via RMS). Standing: V_us sharp + V_cb coarse both bank; skeleton+hierarchy Structure-Derived; the ν-stratum radial-overlap refutation (test-not-run pending Lyra's b) was about a UNIFICATION route, and it doesn't touch these separate banked routes. Nothing pushed."
---

# K1637 — Self-correction: V_cb IS banked (RMS-projection route, ~0.044); the 0.1543 is a different failing route; the open magnitude is V_ub

Reconnecting to the *actual* V_cb derivation corrected a flag I raised last round. The auditor over-flagged; the corpus caught it.

## What I got wrong in K1636
I flagged "V_cb not sharply banked — 0.1543 vs observed 0.0418, 3.7× off, re-tier candidate." **I was reading the wrong route.** The number 0.1543 is the **Pochhammer route** (Elie 5318) — which equals Lyra's 1/√42 — and it is *not* how V_cb is banked. It's a route this session invoked, and it's the one that fails.

## What V_cb actually is (K1001 → K1002)
The **banked** V_cb comes from the **3D→2D RMS projection theorem** (K1001), direction **5/√34** forced (F384), giving **~0.044** — which **matches** PDG (0.044 inclusive / 0.0417 exclusive; the measurement's own 20-year inclusive/exclusive puzzle spans exactly this band). K1002 ratified it at the honest **coarse** tier: *"Derived (2-3-mode 3D-projected, without counterexample)"* — a **structural ~5% match against ~5%-uncertain data**, with the split correctly drawn: the **RMS-applies piece is FORCED** (ground modes are S⁴-zonal → RMS = √((d−1)/d) by symmetry), the **mode-is-3D piece is the one open identification** (Lyra pins, Cal confirms).

So **V_cb is banked** — coarsely, honestly labeled, with one open identification. It is *not* 3.7× off; that was a different route.

## The team disagreement, resolved
- Elie: "corpus banks V_us only" — **wrong** (V_cb banked via RMS).
- Lyra: "V_cb = 1/√42 = 0.1543 stands" — **wrong** (that's the *failing* route, not the banked one).
- Keeper K1636: "V_cb 3.7× off, not banked" — **wrong** (read the failing route).
- **Correct:** V_us = 1/√20 (sharp bank, 0.31%); V_cb ~0.044 (coarse bank via RMS projection); **V_ub is the genuinely open magnitude** (+ δ_CKM).

## The deeper honest picture (worth stating)
The CKM sector is a **patchwork of different geometric readings** — V_us from the mass-ratio (1/√20), V_cb from the 3D→2D RMS projection (~0.044), V_ub open — **not one unified kernel.** That is *why* the "one kernel → all 13 outputs" fires kept failing (K1002's blind-bar hope, and the Toy-5313/5318 fires): the elements genuinely come from different mechanisms, so no single kernel reproduces all of them. Honest, and it reframes the sector: don't keep firing one-kernel unifications; bank the per-element routes and target the open one.

## Consequence for the redirect
Elie's current-Clebsch redirect should target **V_ub** (the genuinely open magnitude), not "V_cb + V_ub." Pre-registered falsifier narrows to: the weight must supply ~1/9.0 for V_ub while leaving V_us at 1.00; V_cb is already banked (coarse) via the RMS route and only wants its mode-is-3D identification closed (Lyra), or a sharpening from coarse→precise. And the ν-stratum radial-overlap refutation (still test-not-run pending Lyra's b) was always about a *unification* route — it doesn't touch these separately-banked per-element routes.

— Keeper, K1637, 2026-08-17. SELF-CORRECTION of K1636: V_cb IS banked via the 3D→2D RMS projection (K1001/K1002, ~0.044, matches PDG-inclusive, coarse tier, one open mode-is-3D identification). The 0.1543 (Pochhammer/1/√42) is a DIFFERENT failing route — I read the wrong one; corrected. Honest CKM state: V_us sharp bank, V_cb coarse bank, V_ub OPEN. The sector is a PATCHWORK, not one kernel — why the one-kernel fires failed. Redirect targets V_ub. Nothing pushed.
