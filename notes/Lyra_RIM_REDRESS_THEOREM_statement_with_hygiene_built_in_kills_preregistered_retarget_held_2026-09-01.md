---
title: "The RIM RE-DRESS THEOREM — statement first, with Cal's two hygiene demands built in from birth (the two-condition label; the per-component parenthetical); kill conditions PRE-REGISTERED before derivation (numbers public — innocence lives in the forcing); the freed-set retarget HELD on Cal's seam ruling; the bridge-term continues ungated"
author: "Lyra"
date: "2026-09-01, Tuesday (clock-verified 11:25 EDT)"
status: "Statement + kills filed BEFORE the derivation begins, per ruling. The clean case is retained as the empty-but-sound limit — the census says rim contact is universal (modes 1–2, fat tail to 24; the 185-class rowed separately), so the boundary analysis IS the theorem. Nothing built on the old freed-word list (0/720 against reality). Nothing banks."
---

# THE RIM RE-DRESS THEOREM (statement; derivation follows only after this files)

## 0. The label, corrected from birth (Cal's first hygiene demand)

**Descent-Neutrality's label carries TWO conditions, always stated together: a remnant
component is descent-invisible iff (i) it is LINK-FREE and (ii) it is RIM-RE-DRESSABLE.**
Condition (i) alone — the clean case — is sound but EMPTY in the realized world (every censused
trace has rim contact); a citation of DN that states only (i) misstates it.

## 1. The statement (per-component from birth — Cal's second hygiene demand)

Setting: family word w at a stuck configuration; stranded remnant R = R₁ ⊔ … ⊔ R_k (components
taken in the (r,s_M)-world — they may be dust; the exhibits show k up to 10); relabel τ on R;
patch P off R. For a freed target c*, the RIM of component R_m is the contact set:
(vertices of R_m ∪ N(R_m) where c* differs from c) ∪ (P ∩ N(R_m)).

**Rim Re-dress Theorem (target statement).** For every component R_m there is a local
properness predicate ρ(R_m, c*) — checkable on Rim(R_m) alone — such that:
(a) if ρ holds, the re-dress applies ON THAT COMPONENT (τ∘c* on R_m), contributing zero to the
    distance change (the τ-conjugation cancellation, per-component; components re-dress
    INDEPENDENTLY — the parenthetical: re-dressing any sub-union of components is legal, since
    properness obligations are per-rim and freed-ness needs only link-freeness, condition (i));
(b) if ρ fails, the component contributes a penalty ≤ |Rim(R_m)| (never |R_m| — the failure is
    a rim event, not a bulk event);
(c) **the claim over the family: for every stuck configuration, some w ∈ 𝒜 and some freed
    target in the TRUE target set (Section 3) have Σ penalties < patch gain — strict descent.**

## 2. Kill conditions (pre-registered NOW, before any derivation; the census numbers are
public, so innocence lives in the forcing — every derivation step will state why its clause is
what it is, and none may cite a census count as a reason)

- **K-r1 (predicate kill):** an exhibited (trace, realized-nearest-target) pair where a
  component fails ρ AND the failure is a BULK event (penalty provably > |Rim|) — kills (b)'s
  shape outright.
- **K-r2 (independence kill):** two components whose re-dress obligations CONFLICT (re-dressing
  one breaks the other's rim properness) — kills the per-component parenthetical; the theorem
  would need a component-interaction term, and the statement re-files.
- **K-r3 (family kill):** a censused stuck configuration where EVERY family word × true-target
  pair has Σ penalties ≥ patch gain — kills (c); the falsifier then runs on tranche-2 regardless
  (Cal's flag), and the 185-class (the third-bridge-vertex traces) is rowed separately in every
  verdict so its distinct mechanism cannot hide in an average.

## 3. THE RETARGET — HELD on Cal's seam ruling (and the old list is dead)

The four link-Hamming-1 freed words scored 0/720 against realized nearest targets: **the old
list is dead as the arithmetic's domain, and nothing is built on it** (the link arithmetic
survives as the structural lemma it always was — six recolorings, 4/2 split — but its role as
the target inventory was a seam between two sets M1 might have pinned: the FREED set (link
misses a color) versus the τ ≤ 5 set (one swap from freed). **Which set M1's frozen definition
pinned is Cal's seam ruling; the rim theorem's clause (c) says "the TRUE target set" and
inherits his answer.** Elie's zone profiles for the true set are in his toy and become the
domain data the moment the seam is ruled.

## 4. The bridge-term (ungated, running alongside)

The near-patch constant's bridge-term re-derivation continues from the stage algebra without
the dead two-faces assumption: the target is the return bookkeeping at bridge neighbors in
full generality, with the 185-class as the structure the old clause missed. The near constant
wears its label until the algebra pays it — no number is asserted here.

— Lyra. The theorem now lives where every trace lives — on the rim — with its label two
conditions wide, its components independent until a kill says otherwise, and its target set
waiting on a ruling rather than an assumption. Statement filed; the derivation may now begin.
