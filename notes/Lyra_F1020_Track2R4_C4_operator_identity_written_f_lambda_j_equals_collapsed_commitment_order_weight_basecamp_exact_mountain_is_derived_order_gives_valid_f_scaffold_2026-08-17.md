# F1020 — Track 2 R4 (K1630), the C4 summit written as an operator identity, not a slogan. **Connes' spectral action Tr f(D/Λ) = BST's own causal-set action ∫∫_{a≺b} L over the commitment order, made well-posed by the pin (both sides graded by the SO(2)-center level j), with the Hardy reproducing collapse forcing the identity to hold level-by-level iff f(λ_j) = w_j — the spectral cutoff equals the collapsed commitment-order weight.** Base-camp is EXACT (the reproducing property over the future cone). The mountain — scaffold tier — is whether BST's *derived* order produces a *valid* cutoff f. Not leading with α (Wyler retired).

**Lyra, Monday 2026-08-17 R4. Keeper K1630 Lane 2 (my declared deep turn): "write it properly — Tr f(D) = BST's own causal-set action over the commitment order; scaffold tier; the open mountain is whether BST's derived order produces a valid f; the GR-rigor summit, finally on solid ground." Built on F1016 (base-camp) + F1018 (the pin makes it well-posed). Reproducing/Hardy machinery corpus-connected (T754 Born=Bergman, T2562 Kostant Dirac, T1829 K-type tower). Nothing pushed; CP existence-only.**

## The two functionals (stated as operators, not analogies)

**Connes side — spectral, ONE-POINT.** The spectral action of the Kostant cubic Dirac D on D_IV⁵ (T2562: self-adjoint, discrete spectrum climbing to ∞):
$$S_{\text{spec}} = \operatorname{Tr} f(D/\Lambda) = \sum_j f(\lambda_j/\Lambda)\, d_j,$$
with the spectrum organized by the K-type tower level $j$ (T1829): eigenvalue $\lambda_j$, multiplicity $d_j = (2j+N_c)(j+1)(j+\text{rank})/C_2$. A trace of a function of a single operator — local/diagonal.

**BST-causal side — bilocal, TWO-POINT.** BST's own causal action over the commitment order (F989/F990: the derived conformal-Lorentzian order $a\prec b$ on the Shilov boundary $\partial_S = \mathbb{R}\times S^4$):
$$S_{\text{caus}} = \iint_{a\prec b} L(a,b)\, d\mu(a)\,d\mu(b), \qquad L(a,b) = \ell\big(A_{ab}\big),\quad A_{ab} = P(a,b)P(b,a),$$
with $P$ the (Bergman–Dirac) projector kernel and $\ell$ a weight. Bilocal — a sum over ordered pairs.

## The Hardy bridge — the reproducing property IS the two-point→one-point collapse
$P$ is idempotent (the projector; base-camp idempotency, F1016). The reproducing identity on the boundary:
$$\int_{\partial_S} P(a,b)\,P(b,c)\,d\mu(b) = P(a,c), \qquad \text{diagonal: } \int_{\partial_S} P(a,b)P(b,a)\,d\mu(b) = P(a,a).$$
This collapses the bilocal spread $\iint P(a,b)P(b,a)$ (one integration) to the one-point diagonal $P(a,a)$ — **Finster's $\iint P P$ form folding into Connes' diagonal trace.** But the commitment order restricts the inner integral to the **causal future cone** of $a$ (only $b \succ a$), so the collapse is *partial*:
$$\int_{b:\, a\prec b} P(a,b)P(b,a)\,d\mu(b) = \theta(a)\,P(a,a), \qquad \theta(a)\in[0,1] = \text{future-cone fraction.}$$
**This is precisely where the order does work** — the ordered domain, not the full boundary. On the homogeneous boundary $\theta(a)=\theta$ is constant (leading), and the non-trivial order-content lives in the higher moments (below).

## Base-camp — EXACT (unweighted: $\ell=\mathrm{id}$, $f\equiv1$)
$$S_{\text{caus}}\big|_{\text{unwt}} = \iint_{a\prec b} P(a,b)P(b,a)\,d\mu\,d\mu = \int \theta\, P(a,a)\,d\mu(a) = \theta\,\operatorname{Tr}P = \theta\sum_j d_j,$$
$$S_{\text{spec}}\big|_{\text{unwt}} = \operatorname{Tr}\mathbb{1}\big|_{\text{occ}} = \sum_j d_j.$$
So $S_{\text{caus}}|_{\text{unwt}} = \theta\, S_{\text{spec}}|_{\text{unwt}}$ **exactly** — the reproducing property collapses the spread to the dimension $\sum_j d_j$ (= the 16-line stored-outcome capacity, F930), up to the constant causal fraction $\theta$. This is the "trivial idempotent base-camp" Keeper named: *the collapse is the reproducing property; the physics is 100% in the weighting.* (Count-once: $\operatorname{Tr}P$, F930's capacity, and the Bergman dimension are ONE object's triple duty, not three facts.)

## The weighted identity — THE OPERATOR IDENTITY (the mountain, scaffold tier)
Expand the causal weight in powers of the two-point object $A_{ab}=P(a,b)P(b,a)$:
$$\ell(A) = \sum_n c_n A^n.$$
Each $A^n=[P(a,b)P(b,a)]^n$ collapses under the $n$-fold reproducing integral **over the future cone** to a diagonal object graded by the level $j$ — the $n$-th causal reproducing-moment $\rho_n(j)$ (a purely geometric FK-kernel quantity, restricted to the future cone). Hence:
$$S_{\text{caus}} = \sum_j \Big[\underbrace{\sum_n c_n\,\rho_n(j)}_{\displaystyle w_j}\Big]\, d_j, \qquad S_{\text{spec}} = \sum_j f(\lambda_j/\Lambda)\, d_j.$$
**Both sums run over the SAME index $j$** — this is the pin's third payoff (F1018): the spectral eigenlevel and the order's pair-grading are the one SO(2)-center level, so the two functionals are commensurable (they were not, before the pin). The identity $S_{\text{spec}}=S_{\text{caus}}$ then holds level-by-level **iff**
$$\boxed{\,f(\lambda_j/\Lambda) \;=\; w_j \;=\; \sum_n c_n\,\rho_n(j)\,}$$
**The spectral cutoff function, at each Dirac eigenvalue, equals the collapsed commitment-order weight** — the reproducing-moment sum of the order's Lagrangian coefficients. This is C4 as an operator identity: the Hardy collapse turns the bilocal order-integral into a level-diagonal sum, and matching it to the spectral trace *determines* $f$ from the order (or exposes a mismatch).

## What is forced vs. open (honest tiers — SCAFFOLD, not theorem)
**FORCED / exact:**
- The base-camp $\sum_j d_j = \operatorname{Tr}P$ (reproducing property, idempotent $P$).
- The commensurability of the two functionals over $j$ (the pin, F1018) — without it C4 is not even well-posed.
- The FORM $f(\lambda_j)=\sum_n c_n\rho_n(j)$ (the Hardy collapse of a moment-expanded bilocal weight).

**OPEN / the mountain (scaffold, K1390):** two sub-questions, both un-waved:
1. **Are the $c_n$ forced by BST's derived order, or free?** The commitment-order Lagrangian's coefficients must come from F990's *derived* order (the conformal-Lorentzian causal structure on $\mathbb{R}\times S^4$), NOT be fitted. If the $c_n$ are free, C4 is a parametrization of $f$, not a derivation of it. **This is the load-bearing open question** — I do not claim the $c_n$ are forced; exhibiting them from the derived order is the work.
2. **Does $w_j=\sum_n c_n\rho_n(j)$ have the analytic form of a valid spectral cutoff?** — even, positive, decreasing, with a heat-kernel expansion. If yes → $S_{\text{spec}}=S_{\text{caus}}$, spectral = BST-causal (the summit). If a named analytic mismatch → the honest obstruction, localized to a specific moment.

**I am NOT claiming C4 lands.** I have written the identity that must hold and shown its base-camp is exact and its two sides are now commensurable (the pin). The mountain is whether the derived order supplies $c_n$ giving a valid $f$ — scaffold.

## Why this is the GR-rigor summit (Keeper's framing — stated at tier, no α)
In Connes' construction, the heat expansion of $f(D/\Lambda)$ yields the Einstein–Hilbert action + a cosmological term — the gravitational action. **So IF C4 lands** — if BST's derived commitment-order weight $w_j$ IS a valid spectral cutoff — then BST's causal action *reproduces the spectral action, hence the gravitational action functional, from the order commitments happen in.* That is the GR-rigor content: not a constant, but the *action*, derived from the order. (This would connect to the banked gravity results — Einstein equations from Bergman, T1099 — at the action level; I flag that connection as a target, not a claim. **No $\alpha$ anywhere in this: the Wyler integral is retired, K676/K680; $\alpha$ is Identified, not the road here.**)

## RUBRICS Layer-2 done-bar
- [x] Wrote C4 as an operator identity, not a slogan: $S_{\text{spec}}=\operatorname{Tr}f(D/\Lambda)=\sum_j f(\lambda_j)d_j$ (one-point) vs $S_{\text{caus}}=\iint_{a\prec b}\ell(P(a,b)P(b,a))$ (bilocal), bridged by the reproducing property (the Hardy two-point→one-point collapse over the future cone).
- [x] Base-camp EXACT ($\sum_j d_j=\operatorname{Tr}P$, up to the causal fraction $\theta$); named the order's genuine entry point (the future-cone-restricted reproducing integral, not the full boundary).
- [x] Derived the boxed identity $f(\lambda_j)=\sum_n c_n\rho_n(j)$ and showed the pin (F1018) is what makes both sides commensurable over $j$ — the pin's third payoff, load-bearing for well-posedness.
- [x] Held scaffold tier (K1390): forced = base-camp + commensurability + the identity's FORM; open = (1) are the $c_n$ forced by the derived order (the load-bearing question, NOT claimed), (2) does $w_j$ give a valid cutoff. Did NOT claim C4 lands.
- [x] GR-rigor framing at tier (heat expansion → gravitational action IF it lands), no α. Nothing pushed; CP existence-only.

## Handoffs
- **@Keeper** — C4 written as the operator identity: $f(\lambda_j)=\sum_n c_n\rho_n(j)$ (spectral cutoff = collapsed commitment-order weight), well-posed BECAUSE the pin grades both sides by the one SO(2)-center level. Base-camp exact; scaffold above it. The load-bearing open question is sharp and honest: **are the $c_n$ forced by the derived order (F990) or free?** — if free, it's a parametrization, not a derivation. That is the summit's real gate; I'm not claiming it. GR-rigor content is the heat-expansion → gravitational action IF the $c_n$ force a valid $f$.
- **@Elie (if/when Track 1 frees you)** — the base-camp is a clean same-object check on the g=7 kernel: $\iint_{a\prec b}P(a,b)P(b,a)\,d\mu\,d\mu = \theta\operatorname{Tr}P = \theta\sum_j d_j$ — verify the causal fraction $\theta$ is a stable constant on the homogeneous boundary (the leading collapse). The weighted moments $\rho_n(j)$ are the mountain, not asking yet.
- **@Grace** — the two-point object $A_{ab}=P(a,b)P(b,a)$ here is the SAME Bergman-autocorrelation you factorized (Direction-A, $A_{xy}=\|K\|^2(\gamma u)(\gamma v)$); the reproducing collapse is that $\|K\|^2$ prefactor integrating to the diagonal. If you have the future-cone measure on $\mathbb{R}\times S^4$, the causal fraction $\theta$ and the first moment $\rho_1(j)$ are computable.
- **@Cal** — the honest weak point to hostile-check: the boxed identity is only a *derivation* if the $c_n$ come from BST's derived order, not chosen to make $f$ come out valid. I've flagged that as THE open question and refused to claim the $c_n$ are forced. Also check: is the future-cone fraction $\theta$ genuinely constant (homogeneity), or am I hiding position-dependence in a constant? If $\theta(a)$ varies, the base-camp collapse is not as clean as stated.
- **@Casey** — the summit, written out honestly. Connes has one way to package a geometry's physics: add up a function of the vibration-frequencies of its Dirac drum (one number per note). Finster has another: sum a cost over every *pair* of points. The question of C4 is whether these are the same thing for our geometry — and the answer hinges on one clean piece of machinery: the Bergman "reproducing" rule, which says integrating the kernel against itself gives the kernel back. That rule *is* the bridge — it folds Finster's sum-over-pairs into Connes' sum-over-notes. Unweighted, they match exactly (both just count the dimension — the "16 lines" of stored capacity). The physics is entirely in the *weighting*, and there the identity becomes a single clean demand: **the cutoff function on the spectrum must equal the weight the commitment-order puts on pairs, note by note.** And here's the honest edge I won't paper over: that's a real derivation *only if* the pair-weights come from the order we actually derived — not from weights I picked to make it work. I've written the equation that has to hold and shown its easy half is exact and its two halves finally speak the same language (that's what last round's pin bought us). Whether our *derived* order fills in the weights that make it a true equation is the mountain, and it's still a mountain. If it does, the heat-expansion of that cutoff is the action of gravity — which is why this is the summit worth the careful turn. Scaffold, not theorem. Nothing pushed.

Notes only; no toy/theorem claimed (operator-identity scaffold). F1020: C4 written properly. S_spec=Tr f(D/Λ)=Σ_j f(λ_j)d_j (one-point, K-type tower T1829/T2562) = S_caus=∫∫_{a≺b}ℓ(P(a,b)P(b,a)) (bilocal, commitment order F989/F990); reproducing property ∫P(a,b)P(b,a)dμ(b)=P(a,a) = the Hardy two-point→one-point collapse, over the FUTURE CONE (θ fraction = where the order does work). Base-camp EXACT: Σ_j d_j=Tr P (=16-line capacity F930), up to θ. Weighted: ℓ(A)=Σ_n c_n A^n → moments ρ_n(j) → S_caus=Σ_j w_j d_j, w_j=Σ_n c_n ρ_n(j); both sides over the SAME level j (pin F1018, third payoff = commensurability). Identity: f(λ_j/Λ)=w_j=Σ_n c_n ρ_n(j) (spectral cutoff = collapsed commitment-order weight). SCAFFOLD: forced = base-camp + commensurability + identity FORM; OPEN = (1) are c_n forced by derived order F990 or free [load-bearing, NOT claimed], (2) does w_j give a valid cutoff. GR-rigor IF it lands (heat exp → gravitational action). No α (Wyler retired). Nothing pushed. — Lyra
