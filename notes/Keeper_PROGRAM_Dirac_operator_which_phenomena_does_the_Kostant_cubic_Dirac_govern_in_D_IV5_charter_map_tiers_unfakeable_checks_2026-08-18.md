---
node_type: keeper_program_charter
id: PROGRAM-DIRAC
title: "The Dirac Program (Casey's directive 2026-08-18): determine/derive which phenomena the Kostant cubic Dirac operator D (T2562) is responsible for in D_IV⁵. Thesis to test, not assert: in BOTH frameworks D lives in — Kostant's (algebraic) and Connes' (spectral-triple) — the static geometry D_IV⁵ supplies the CONTAINER (Hilbert space H²(D_IV⁵) + the algebra), and the SINGLE operator D supplies everything DYNAMICAL (the MECHANISM): gravity, gauge, masses, generations, causal metric. If that holds, the 'container yes, mechanism open' boundary (K1677) COLLAPSES — the mechanisms are readings of ONE operator, and 'is the mechanism forced?' reduces to 'is D forced by the geometry?' (Kostant: the cubic Dirac is canonical given G/K + the ρ-shift → largely YES). The payoff Casey names: the resulting theory/simplification would be illuminating. METHOD: for each candidate phenomenon, establish the D-linkage at a TIER (Banked / Strong-lead / Lead / Speculative) and name the ONE un-fakeable check that promotes it — this is a MAP OF CHECKS, not a claim. GUARD (Cal #27 at peak convergence): 'one operator does everything' is the maximal seduction; every linkage held at tier; each promotes only on a NUMBER / an INDEX / a FLUCTUATION-computation. The over-promote guard (K1679, the near-4th-over-reach today) is standing on this program specifically."
date: 2026-08-18
author: Keeper
status: "CHARTERED. Casey's directive. 9 candidate phenomena mapped + tiered + each with an un-fakeable check. Endgame question: is D forced by D_IV⁵? (feeds Block C / #79). #108's floor (K1679) is the first illustrative case: gauge = D-fluctuations, not isometry → the isometry floor is EXPECTED."
related: [T2562, K1677, K1679, K927, F1039, "#79", "#108", "#114"]
---

# The Dirac Program — which phenomena does D govern in D_IV⁵?

**Casey, 2026-08-18:** *"We should have a program to determine/derive which phenomena the Dirac operator plays a part [in]. The resulting theory or simplification would be illuminating."*

**The operator.** D = the Kostant cubic Dirac operator on `SO(5,2)/[SO(5)×SO(2)]` (T2562) — the same D that induces gravity and (this week, Lyra F1039) translates tensor↔spinor for the fermion sector. Two facts make it central, not incidental:
- **Kostant:** `D² = Ω_{SO(5,2)} − Ω_{SO(5)×SO(2)} + ‖ρ‖²-shift`. D is the square root of the Casimir difference — and Casimir eigenvalues are what BST reads as observables everywhere.
- **Connes:** in a spectral triple `(A, H, D)`, the geometry, the metric, the gauge fields, and the action are all *functionals of D*. The algebra + Hilbert space are the container; D is the dynamics.

## The map — 9 candidate phenomena, tiered, each with its un-fakeable check

| # | Phenomenon | D-linkage | Tier | The one un-fakeable check | Owner |
|---|---|---|---|---|---|
| **A** | **Spectral side / observables** | `D² = Casimir difference` → C_2=6 ground state, K-type Casimirs = mass/mixing readings, R(k) heat-trace | **Banked** (Kostant, standard) | confirm the exact `‖ρ‖²`-shift = the corpus `ρ=(5/2,3/2)` and `E₀=3/2` | Lyra/Elie |
| **B** | **Gravity** | heat trace `Tr e^{−tD²}`, coeff `a₁` = Einstein–Hilbert (Sakharov induced) | **Banked** (gravity paper) | — (done) | — |
| **C** | **Strong-sector β-function** | the NEXT heat coeff `a₂` (Block B, K927) | **Strong-lead** | compute `a₂` from D's heat trace; is it the QCD β-coefficient? (a NUMBER, blind) | Elie (Block B) |
| **D** | **Half-integers / spin-statistics** | the ρ-shift + D tensors with the spin module; `(−1)^F` = tensor↔spinor swap | **Banked-adjacent** (Elie 5351: `(1,1)→(3/2,1/2)` = shift by B₂ spinor weight `(1/2,−1/2)`) | confirm the shift gives exactly the Time paper `E₀=3/2 = N_c/rank` | Elie |
| **E** | **Fermion masses / mixings** | the translator `K∘D` — the whole charged-fermion sector is one equation | **Banked** (Lyra F1039) | the `(k,m)` rung selection (BLOCKED on partition-vs-Dynkin + muon-ν pin, K1679) | Lyra+Grace→Elie |
| **F** | **Generation count + chirality** | `ind(D)` — the index counts massless/chiral modes | **Lead** | compute `ind(D)`; does it give 3 generations (F86 rank+1) + the chiral asymmetry (γ⁵)? | Lyra/Grace |
| **G** | **The physical Hilbert space** | `ker(∂̄+∂̄*) = H²(D_IV⁵)` (Dolbeault–Dirac on the Hermitian symmetric domain) = holomorphic discrete series | **Strong** (standard for Hermitian symmetric spaces) | confirm the commitment space `ρ_commit` lives on `ker(D)` (Reading A = a statement about D's kernel) | Lyra |
| **H** | **Gauge fields (incl. #108 su(3))** | inner fluctuations `D → D + A` | **Lead** — the mechanism-home | do D_IV⁵'s inner fluctuations produce exactly su(3) on the quadrupole? | Lyra (NCG)+Elie |
| **I** | **Causal / metric structure (#114)** | Connes distance `d(x,y)=sup{|f(x)−f(y)|: ‖[D,f]‖≤1}` | **Lead** | does the D-distance reproduce the derived causal order (T2564)? | Lyra/Grace |

## SCORECARD (as of Round 13, 2026-08-18 — the #108 arc closed)
Honest state of the nine rows after this arc, grouped by Bar-1 dependency line:

- **CREDENTIAL — "D is the right operator" (one line, not four):** A (`D²`=Casimir diff) · D (ρ-shift/half-ints, Elie 5351 confirmed D tensors with the B₂ spinor weights) · G (`ker D`=H²) — structural facts, banked, **not predictions.**
- **GRAVITY (banked):** B (`a₁`=Einstein–Hilbert).
- **RETIRED to the geometry line (K1681):** C (`a₂`) — D's heat trace *receives* `N_c`/`n_f`, never produces them; the strong-sector quantum numbers are geometry-line (short-root / Q⁵ cohomology), not Dirac-line. Off the ledger.
- **GAUGE — FLOOR, closed (K1683):** H — the static geometry forces number-3 + real `M₃(ℝ)→SO(3)` (Weinberg-load-bearing) + baryon-number; the SU(3) gluon *dynamics* is the gauge principle (`M×F` + inner fluctuation), standard input. **Credential: BST fields its own SM spectral triple (T2549–T2552).**
- **THE ONE LIVE PREDICTIVE FRONTIER:** E (`K∘D` masses) — translator banked (F1039), weight fork forced (genus-5/Wallach-3, closes T2513), and the **K-type→mass MAP is the open** (why `(Γ(n_C)/π²)^{C₂}`? — derive, don't reverse-engineer). This is where the program next earns or fails a number.
- **UNTESTED LEADS (could add independent lines):** F (`ind(D)` = 3 generations + chirality) · I (Connes distance = the causal order, #114).

**Bottom line:** the program has a solid credential + gravity (banked), one retired row, one honest floor with a real credential, **one live predictive frontier (the mass map)**, and two untested leads (index, distance). The elegance held its tier throughout — every claim earned or floored on a number, not the thesis.

## Why #108's floor is the program's first case (K1679)
The gluons (`su(3)/so(3)` coset) are **spin-2**, and the forced reductive isometry `so(5) = 3·spin-1 ⊕ spin-0` has **no spin-2** — so the gluons are provably *not* in the static geometry. The Dirac program **predicts exactly this**: gauge fields are inner fluctuations `D → D+A` (row H), *not* isometry generators. So the isometry-floor is **expected**, not a failure — and **D's fluctuation structure is where #108 may finally cross** (row H is the live promote route, replacing the dead isometry route). The floor and the program agree.

## The endgame question (feeds Block C / #79)
If every mechanism we've floored (gauge generators, causal weighting, mass rungs) is a functional of D, then **"is the mechanism forced?" reduces to "is D forced by D_IV⁵?"** Kostant answers largely *yes* — the cubic Dirac is canonically determined by `G/K` and the ρ-shift; you don't choose it. So "does commitment force the geometry" (#79) sharpens to the far more tractable, representation-theoretic **"does the geometry force D?"** That is the program's summit.

## Ordering (proposed)
1. **Now / Block B:** row **C** (`a₂` = β-function) — a blind number, and it's already the Block-B front edge (K927). Best first promote.
2. **Now / unblock E:** the partition-vs-Dynkin + muon-ν pin (K1679) → then the `(k,m)` rung run. Banks the mass-tower readings.
3. **Reframe H:** re-pose #108 as "is the gauge sector an inner fluctuation of D?" — the live cross of the floor.
4. **Then F, G, I:** the index (generations), the kernel (H²), the distance (#114) — each a clean check.
5. **Summit:** is D forced by D_IV⁵? → Block C.

## The two program-level bars (Cal §591, ADOPTED STANDING — before any row lands)
Cal's §591 is the load-bearing governance of this program, and it is right: a "one object explains five sectors" premise **manufactures the gravity-cluster over-count by design.** Two bars, both standing, both prerequisites to banking any row.

### Bar 1 — the row-dependency graph (written BEFORE the rows land, so the burden of proof can't quietly invert)
Five successful rows sharing one operator are **five readings of one operator — magnificent, and NOT five confirmations.** The honest evidence-structure:

- **Cluster 1 — the heat trace of `e^{−tD²}` (ONE object, three readings):** row **B** (`a₁`=gravity) · row **C** (`a₂`=β) · row **F** (graded `a_{n/2}`=index/generations, via McKean–Singer). These are **three coefficients of one expansion** → they count as **ONE line of evidence** ("D's heat trace works"), not three. A program that "predicts gravity AND running AND generations" via the heat trace has done so with **one** heat kernel.
- **Cluster 2 — D's spectrum / spinor structure (ONE object, related readings):** row **A** (`D²`=Casimir diff) · row **D** (ρ-shift/half-ints) · row **E** (`K∘D` masses) · row **G** (`ker D`=H²). One line ("D's spectral/spinor structure works").
- **Genuinely independent structural content (separate functionals):** row **H** (inner fluctuations `D→D+A` — content BEYOND the unfluctuated D) · row **I** (Connes distance `[D,f]` — a different functional, not the heat trace).

⟹ **Honest count: ~3–4 independent lines, not 9 rows.** Any scoreboard/paper/null-model treats Cluster 1 as one, Cluster 2 as one, H and I as their own. This is the T1918/T1955 accident forbidden in advance.

### Bar 2 — a pre-registered falsifier per row (else it's an illustration, not a check)
With one operator and many rows, a failure always has two escapes — *D is wrong*, or *the row's identification was wrong* — and the second always preserves the program. **So every row carries ONE sentence, written before its number: "if this returns X, D is wrong about this sector."** No sentence → illustration, not a check. (This is what made #108's floor a result, not a disappointment.)

| Row | Pre-registered falsifier (blind, before the number) | Status |
|---|---|---|
| **A** | if the constant shift in `D²` ≠ `‖ρ‖²` with `ρ=(5/2,3/2)`, D is not this G/K's Kostant Dirac | open |
| **B** | if `a₁` ≠ the Einstein–Hilbert form, D does not induce gravity | **passed** (banked) |
| **C** | if the **fluctuated** `D_A` heat trace does not yield `+11N_c/3` **with N_c FIXED by the geometry** (not input), D is wrong about the gauge sector. **Sharper informative target (Cal): does N_c or n_f EMERGE from D, or are they received?** | **unfluctuated returned NO** (fermion loop only, `−2n_f/3` screening) → re-aimed to `D_A` |
| **D** | if D's ρ-shift does not give `E₀=3/2=N_c/rank`, D does not carry the half-integer spectrum | open |
| **E** | if the pinned computation (partition coords · muon on the Kostant-D image `(3/2,½)` · lepton Bergman weight from source) does not give `m_μ/m_e` at tier, the `K∘D` translator is wrong about the mass tower | **live** (teeth: muon is a known number) |
| **F** | if `ind(D) ≠ 3` (or no chiral asymmetry), D does not carry the generation structure | open (un-fakeable: a fixed integer) |
| **G** | if `ker D ≠ H²(D_IV⁵)`, the commitment Hilbert space is not D's kernel | open |
| **H** | if the inner fluctuations of `D_A` do not close to **exactly su(3)** on the quadrupole (larger/other algebra, or no spin-2 gluons), D's fluctuations do not carry color | open (the #108 cross-route) |
| **I** | if the Connes distance from D does not reproduce the derived causal order (T2564), D does not set the external metric | open (#114) |

All nine can write the sentence → all nine are checks (none demoted to illustration). Any future row that cannot write one is an illustration and is labeled so.

### The a₂ scoping (Cal, banked into row C)
The β-function emerging from `a₂` is a **general property of gauged-Dirac heat-kernel expansions** — it is precisely how Connes' spectral action "predicts" SM running. A `D_A` match therefore shows **"BST fields a spin-factor-squared Dirac operator with the right gauge content"** (well worth claiming after §571) — it does **not** show "D predicts the strong sector." The only un-fakeable content is the narrow target: **does anything in D fix `N_c` or `n_f` rather than receive them?**

## The guard (standing)
"One operator is responsible for everything" is exactly the shape that trips Cal #27, and me right behind him (K1679: four over-reaches caught today). **Every row stays at its tier; each earns its promotion with a number, an index, or a fluctuation-computation — never with the elegance of the thesis.** The program is a map of checks — now with Bar 1 (dependency graph) and Bar 2 (per-row falsifier) standing in front of every bank.

— Keeper, PROGRAM-DIRAC, 2026-08-18. Charter: D as the single mechanism-operator (container = D_IV⁵ static geometry; mechanism = D). 9 phenomena mapped/tiered, each with an un-fakeable check. #108 floor = first case (gauge = D-fluctuations, not isometry). Endgame: is D forced by D_IV⁵? → #79. Guard: map of checks, not a claim. Nothing pushed.
