# Elie — round 5 PREREG, toys 5796–5799 (written 2026-09-26 10:43 EDT, before any code is run)

Kill line first, direction before numbers. Hash of this file is posted to the board before any toy runs.

## 5796 — K1926 R1–R5 by a second method (+ Addendum 3 checks)
Method (not Keeper's): (i) Schur/commutant — the commutant of all rank-1 records {vv†} in M₃(ℂ); (ii) the stabilizer of the FUNCTION f(ρ)=tr(ρρᵀ) solved as a linear system in u(3) over exact rationals (Gaussian-rational v's), and at group level (which unitary g satisfy gᵀg = c·I).
KILL: if the stabilizer of f inside su(3) has dimension ≠ 3, or the kernel of U(3)→(action on records) is not exactly U(1), K1926 R3–R5 fail.
DIRECTION: commutant = scalars (dim 1) ⇒ faithful algebra su(3), dim 8; stab(f) ∩ su(3) = so(3), dim 3; group-level stab in SU(3) = SO(3)·ℤ₃ (the ℤ₃ acts trivially on records, so on records it is SO(3) exactly); |vᵀv|² = tr(ρρᵀ)|v|⁴ identically; geometry's so(3)⊕u(1) acts on records as so(3).
CONTROL: stab of the real structure (g ḡ⁻¹ = 1, i.e. g real) in SU(3) = SO(3) — must equal the connected part of stab(f).
Addendum 3: u(3) = o(6) ∩ sp(6,ℝ) (dim 9); ω vanishes on ℝ³; stab of ℝ³ in U(3) has dim 3; dim U(3)/O(3) = 6.
Oscillator (structural, with null): isotropic 3D oscillator shells = Sym^N(ℂ³), dims (N+1)(N+2)/2; baryon multiplets from 3⊗3⊗3 = {1, 8, 10}. DIRECTION: only the totally symmetric 10 (and 1) are oscillator shells; 8 is not; null = fraction of integers ≤ 30 that are triangular (7/30); overlap of 2 of 3 is NOT evidence.

## 5797 — handedness, exact
KILL (for Lyra's L3 / K1926 Section 3 lead): if D_IV⁵'s J = 1⊗j on p = ℝ⁵⊗ℝ² commutes with BOTH su(2) factors of the SO(4) stabilizer of a real unit vector in SO(5), J picks no hand in that SO(4) and the lead as worded dies.
DIRECTION: (a) on ℝ⁴, orthogonal complex structures compatible with orientation = unit sphere of Λ²₊ ≅ su(2)₊, and each commutes with all of su(2)₋ and with no nonzero element of su(2)₊ other than its own line — TRUE; (b) J commutes with all of so(5)⊗1, hence with both SU(2)s — the lead DIES in the SO(4)-stabilizer form; (c) on J-invariant real 4-planes of the form ℝ²_plane ⊗ ℝ², which carry a canonical orientation (even⊗even), J lies in one factor and the plane's own rotation in the other, and this does NOT depend on the arrow's sign — so the arrow does not pick the hand; the complex structure and space sit in opposite factors.

## 5798 — atomic shells as K-types, with control (STRUCTURE ONLY, no energies — Casey 19:42)
Different from K1878 (STOP, energies/Rydberg): no Hamiltonian, no 1/n², only representation content.
Compute by SO(4)=SU(2)×SU(2) torus characters: (a) harmonic polynomials H_m(ℝ⁴) = (m/2, m/2), dim (m+1)² — the shells n=m+1; (b) the scalar holomorphic module of D_IV⁴ at the Wallach point ν=1 has K-types {H_m(ℝ⁴) at SO(2) weight m+1}: one shell per level, weight = n; (c) H²(D_IV⁵) at the Hardy point ν=5/2 restricted to SO(4,2) (the unique-up-to-conjugacy embedding, stabilizer of a positive vector): K′-character identity ⇒ ⊕_{k≥0} H_{5/2+k}(D_IV⁴).
KILL (for "hydrogen's representation appears in H²(D_IV⁵)|SO(4,2)"): if no SO(4)-trivial K′-type sits at SO(2) weight 1 in the restriction, the hydrogen ladder is NOT a summand.
DIRECTION: it is NOT a summand (all weights are half-integers ≥ 5/2); n² shells nonetheless appear as the SO(4)-type of every K-type (so the n² STRUCTURE is recapitulated everywhere, the one-shell-per-level PATTERN only at ν=1).
CONTROL: D_IV^n for n = 4..8 — the n² shell content is identical for all n ≥ 4 (not a selection of 5); and the Wallach point of D_IV^n restricted to D_IV^{n−1} never gives ν′=1 except n=4→… (computed, not assumed). 2n² needs a doubling input — reported, not supplied.

## 5799 — the 19 scan, coloured vs colourless, with the hex-shell route, NULL FIRST
Universe: data/bst_constants.json formulas (every entry with a formula string). Hit for target N: the literal integer N appears, OR a sum/difference/product sub-expression built only from {N_c,n_C,g,C_2,N_max,rank} and integer literals evaluates to N.
Colour rule (fixed now, by name/category keywords, before any count): COLOURED if name/category/formula-notes contain quark|CKM|V_u|V_c|V_t|baryon|proton|neutron|nucle|hadron|pion|kaon|meson|QCD|strong|gluon|Cabibbo; COLOURLESS if they contain electron|muon|tau|lepton|neutrino|PMNS|photon|fine structure|alpha|dark energy|Lambda|Hubble|gravit|Planck mass|Higgs|W boson|Z boson|Weinberg; else UNCLASSIFIED (reported, not scored). If both match: COLOURED (a coloured ingredient present).
NULL: for every N in 10..40, the coloured fraction among N-hits; 19's coloured fraction ranks against these; base rate = coloured fraction of the whole catalogue.
KILL (for Casey's "19 appears when colour is added"): a 19-hit in a COLOURLESS quantity.
DIRECTION: 19-hits are few (≤ 6); zero colourless; but the coloured fraction for 19 is NOT significantly above the base rate (small N) — expect p > 0.05 under a hypergeometric null.
Routes per hit: 16+3 (rank⁴+N_c), 27−8 (N_c³−rank³), hex radius 2 (3r²+3r+1 at r=rank) — a literal 19 admits all three (menu); a generated 19 admits the route its tree uses.
