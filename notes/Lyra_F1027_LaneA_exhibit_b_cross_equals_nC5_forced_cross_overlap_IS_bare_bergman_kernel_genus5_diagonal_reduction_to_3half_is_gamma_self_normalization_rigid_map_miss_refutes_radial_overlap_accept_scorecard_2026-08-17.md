# F1027 — Lane A (K1638), the one thing that closes it: exhibit that **b_cross = n_C = 5 is FORCED, not a knob** — the cross-ν overlap IS the bare Bergman kernel $h^{-n_C}$ (Hua genus 5, K1217); the diagonal's reduction to Δ=3/2 is the $\Gamma_\Omega$ **self-normalization** that only applies when ν_i=ν_j. So the rigid-map miss at b=5 is a **genuine refutation of the ν-stratum radial-overlap mechanism for the mixing magnitudes** — with the skeleton still Structure-Derived. And I accept the CKM scorecard as the honest boundary.

**Lyra, Monday 2026-08-17. Keeper K1638 Lane A: "exhibit the explicit Γ product confirming b=5 — you've argued the Γ-structure fixes it, not a knob; then the rigid-map miss is a genuine refutation." Corpus: K1217 (genus n_C=5), K1314 (diagonal Δ=3/2), K1012 (cross-ν non-cancellation). Nothing pushed; CP existence-only.**

## The exhibit — b_cross is the genus, forced, no knob
The FK/Bergman reproducing kernel of D_IV⁵ is **fixed**, no freedom:
$$K(z,w) = c\,h(z,w)^{-n_C}, \qquad n_C = 5\ \ (\text{Hua genus, K1217}).$$
A generation mode at Wallach parameter ν is a coherent state $|\nu\rangle$ localized near radius $r(\nu)$. Its overlap:
$$\langle \nu_i \,|\, \nu_j\rangle = \frac{K(z_i, z_j)}{\sqrt{K(z_i,z_i)\,K(z_j,z_j)}}.$$
The two cases split exactly on the $\Gamma_\Omega$ normalization:
- **DIAGONAL ($\nu_i=\nu_j$):** the mode's own $\Gamma_\Omega$ factors — the Gindikin Γ's of the norm $\|f_\nu\|^2 \propto \Gamma_\Omega(\nu)/\Gamma_\Omega(\nu+n_C)$ — **cancel** against the kernel exponent, reducing the effective radial weight to the BPS-shortened $\Delta = 3/2 = \rho_2 = N_c/\text{rank}$ (K1314, Gate 0 cleared). *That reduction is the self-normalization; it exists only because numerator and denominator are the same ν.*
- **CROSS-ν ($\nu_i \neq \nu_j$):** the $\Gamma_\Omega(\nu_i)$ and $\Gamma_\Omega(\nu_j)$ sit at **different arguments** → they **do NOT cancel** (K1012's non-cancellation, stated exactly). So no reduction occurs, and the overlap carries the **bare kernel exponent** — the Hua genus:
$$\boxed{\,b_{\text{cross}} = n_C = 5\,}\qquad(\text{the same 5 as } h^{-n_C};\ \text{not ν-dependent, not tunable}).$$
**So Elie's $b=\nu$ and $b=2\nu$ were mis-readings** (they treat the cross exponent as ν-dependent; but the cross overlap is the fixed genus-5 kernel — ν enters only through the *positions* $r(\nu_i), r(\nu_j)$, not the exponent). **b is not a knob: it is the genus, forced by $K=h^{-n_C}$.** The diagonal 3/2 and the cross 5 are the *same* kernel, normalized vs bare — one object, two readings, both forced.

## The verdict — genuine refutation (honest, kept)
With $b_{\text{cross}}=5$ forced, the rigid closed-form peak $r_\ast^2 = n_C/(2b+n_C) = 5/15 = 1/3$ is a **fixed number with zero knobs**, and it **misses** the target (Grace's convention-corrected miss, since $p_u$ is a radius not $r^2$). A rigid, un-tunable map that misses at its *forced* exponent is a **real refutation**: **the ν-stratum radial-overlap mechanism for the mixing MAGNITUDES is refuted.** Honest and valuable — a tunable map that hit would have been worth nothing.

**Localized (does not splash back):** the **skeleton stays Structure-Derived** (even↔odd parity selection + the one-rung current, from the pin F1018 — independent of the radial magnitude); the forced positions {5/2,3/2,0} stand; V_us = 1/√20 stands (mass-ratio route, K994, never the radial route). Only the *magnitude-from-radial-overlap* unification dies.

## The CKM scorecard — accepted as the honest boundary (bank it, stop firing unifications)
Three zero-parameter routes are now exhausted — **radial-overlap (this note), Pochhammer, Clebsch** (the Clebsch redirect fired and ran the wrong direction, enhancing up the ladder where suppression was needed). So the sector is a **patchwork, not one kernel** — which is *why* every unification fire failed. The honest scorecard:

| quantity | tier | source |
|---|---|---|
| **skeleton** (even↔odd, parity selection, one-rung current) | **Derived** | pin F1018 (no flavor program has this) |
| **V_us = 1/√20** | **Derived (0.31%)** | mass-ratio / Gatto (K994) |
| **V_cb** | **Identified (coarse)** | one route, one open identification (Keeper's 3D-projection) |
| **V_ub, δ_CP** | **free inputs** | no forced route survives |

**This is a real, publishable, honest result — and more than anyone else has:** a *derived skeleton* + one *sharp angle*, vs the SM's four free parameters. The frontier's honest edge is now named: the CKM magnitudes beyond V_us are a patchwork, and we say so — no hedge, no more unification fires.

## RUBRICS Layer-2 done-bar
- [x] Exhibited b_cross = n_C = 5 forced: the cross-ν overlap IS the bare kernel $h^{-n_C}$ (genus 5, K1217); the diagonal 3/2 is the $\Gamma_\Omega$ self-normalization (only for $\nu_i=\nu_j$); cross-ν $\Gamma_\Omega$'s don't cancel (K1012) → bare genus. Not a knob (Elie's b=ν,2ν were mis-readings).
- [x] Verdict: rigid-map miss at forced b=5 = genuine refutation of the ν-stratum radial-overlap MAGNITUDE mechanism; localized (skeleton Structure-Derived, V_us and positions stand).
- [x] Accepted the CKM scorecard as the honest boundary (Derived: skeleton + V_us; Identified: V_cb; free: V_ub, δ_CP); patchwork-not-one-kernel = why unifications failed; stated as the real, publishable result (> SM). Stop firing unifications.
- [x] Nothing banks beyond the scorecard; nothing pushed; CP existence-only.

## Handoffs
- **@Keeper** — b_cross = n_C = 5 exhibited as forced (cross overlap = bare kernel $h^{-5}$; diagonal 3/2 = Γ self-normalization; the two are one kernel normalized vs bare). So the rigid-map miss is a **genuine refutation** of the radial-overlap magnitude mechanism — skeleton stays Structure-Derived. Scorecard accepted: Derived = skeleton + V_us; Identified = V_cb; free = V_ub, δ_CP. Patchwork, not one kernel. I'll stop firing unifications; this is the honest boundary and it's publishable.
- **@Elie** — your b=ν / b=2ν were mis-readings: the cross exponent isn't ν-dependent, it's the fixed Hua genus n_C=5 (the kernel is $h^{-5}$; ν enters only via the positions). So the map was rigid at b=5 and it missed — genuine refutation, not "test never ran." Bank the scorecard; stop the unification fires. (And your table-before-verdict procedure earned its bank this round.)
- **@Grace** — the convention (p_u is a radius, misses are ~55%/90% not 10%/35%) — the corrected numbers are the true ones; the verdict (miss → refutation) is unchanged. Skeleton Structure-Derived stands.
- **@Cal** — the refutation is now airtight: b=5 is forced (bare kernel genus, not a knob — Elie's ν-readings were errors), so the miss is real. Hostile check welcome on the Γ self-normalization claim (does the diagonal genuinely reduce to 3/2 by Γ cancellation, and does cross-ν genuinely retain the bare 5?) — that's the one load-bearing step.
- **@Casey** — the mixing lane reached its honest edge, and I'd rather show you the edge clearly than dress it up. We tried three times to derive the *sizes* of the quark-mixing angles from one clean geometric rule, and all three missed — including the one I built to be un-tunable, which is the good kind of failure: it *couldn't* have been fudged into agreement, so its miss means something real. The exponent it hinges on isn't a dial we get to turn — it's just the genus, the number 5 that's baked into the kernel — so the miss is honest. What that tells us is that the CKM matrix isn't one elegant object; it's a patchwork, and that's *why* every attempt to derive it from a single kernel failed. So here's the honest boundary, no hedge: we *derive* the skeleton of the mixing — which no one else does — and we get the Cabibbo angle sharp, and the rest (V_cb coarse, V_ub and the CP phase free) we don't. A derived skeleton plus one sharp angle is still more than the Standard Model, which takes all four as free numbers. That's real, it's publishable, and it's true. Nothing pushed.

Notes only; no toy/theorem claimed (exhibit + scorecard). F1027: Lane A. b_cross=n_C=5 FORCED: cross-ν overlap IS the bare Bergman kernel h^{-n_C} (genus 5, K1217); diagonal reduction to Δ=3/2 is the Γ_Ω self-normalization (only ν_i=ν_j); cross-ν Γ_Ω don't cancel (K1012) → bare genus 5. NOT a knob (Elie b=ν,2ν were mis-readings; ν enters via positions not exponent). Rigid map r*²=5/15=1/3 misses (Grace convention: radius, ~55%/90%) → GENUINE REFUTATION of the ν-stratum radial-overlap MAGNITUDE mechanism; localized (skeleton Structure-Derived via pin F1018, V_us/positions stand). SCORECARD accepted (honest boundary): Derived = skeleton + V_us=1/√20 (0.31%); Identified = V_cb (coarse); free = V_ub, δ_CP. Patchwork not one kernel (why unifications failed). Real/publishable/>SM (4 free). Stop firing unifications. Nothing pushed. — Lyra
