# K1276 — THE BLOCK LIFTS: QM 10/10 clears to ship (re-audit round 3 PASS). + Cal's #85 rigor snag resolved: pin the output slot to the RATIO, not sin²θ_W.

**Keeper (player-manager), 2026-08-08.** Two things: the block-lift milestone, and arming the #85 blind test against the phantom.

## 1. BLOCK-LIFT RE-AUDIT ROUND 3 = PASS → QM 10/10 SHIPS
The widened block criterion (Cal: clean on sin²θ_W AND α_s AND all header-rot AND the monotonicity pass) is met:
- **sin²θ_W:** the 4 stragglers (K1271) + the flagship "retired" line (K1273) are fixed; round-3 re-scan finds no live-paper over-claim. The flagship now reads the honest middle ("held at Structural/Identified, neither over-claimed to Proved nor over-demoted to retired") — and it's a *stronger* trust argument. ✓
- **α_s:** the QFT_Foundations line reads "Identified runner-anchor, runs to obs, β₀=7=g" — honest. ✓
- **Monotonicity:** Grace applied m_Z = **PD explicit-split** (Casey ruled); she verified only m_Z genuinely rides sin²θ_W (the other 4 flagged candidates don't — Γ_W pure integers, |V_ud| Cabibbo, Γ_Z already Structural, the PMNS pair rides θ₁₃ not θ_W) — an honest no-over-cap. ✓
- **m_W:** stays Derived via the independent n_C·m_p/8α route (the "8" is task #87 — verify-forced-or-cap, not block-critical for QM).

**VERDICT: the sin²θ_W/α_s block LIFTS. QM 10/10 (Casey-GO since it closed) clears to ship on Casey's venue call.** @Cal's QM package meets the RUBRICS PAPER done-bar and is submission-ready.

**Two housekeeping notes (NOT QM-10/10 blockers):**
1. `notes/maybe/BST_Complexity_Question_or_Method.md:58` — a comparison table; already says "measured, not derived"; speculative dir, excluded from external. Low-priority tier-note for Lyra.
2. **Paper 106's aggregate headline** ("38 observables, zero free parameters") still needs the "zero *fitted* + tier-breakdown" fix (K1272) — but that gates *Paper 106's* external, NOT QM 10/10 (different paper; QM flagship doesn't use sin²θ_W). Fold into Paper 106's pre-external pass.

## 2. #85 rigor snag (Cal) — RESOLVED: pin the output to the RATIO r = g′²/g², apply a fixed map
Cal's catch (the phantom-avoidance discipline): a bare "3/10" out of the blind computation is ambiguous — it's Case A's *ratio* g′²/g² (→ sin²θ_W = 3/13) AND Case B's *sin²θ_W value*. Reading a number into the wrong slot is exactly Thursday's phantom. **Fix — pin the protocol before Elie + Keeper fire:**

- **The blind computation outputs r = g′²/g²** (the hypercharge-to-weak coupling-squared ratio) — NOT sin²θ_W directly.
- **Apply the fixed, deterministic map:** sin²θ_W = r/(1+r).
- **The three cases map to DISTINCT r-values — no overlap, no misread:**

| case | mechanism | **r = g′²/g² (the blind output)** | → sin²θ_W = r/(1+r) |
|---|---|---|---|
| **A** | whole n_C tangent doubles, color a count | **r = N_c/(2n_C) = 3/10** | **3/13** ✓ (the prediction) |
| **B** | SO(2)-component only doubles | **r = 3/7** | 3/10 (Grace's wrong-answer) |
| **C** | uniform / GUT normalization | **r = N_c/n_C = 3/5** | 3/8 (GUT, BST rejects) |

The r-values {3/10, 3/7, 3/5} are distinct and each lands in exactly one case. **Elie and I each compute r blind and independently; post r (not sin²θ_W); apply the map; the case is forced by which r came out.** The bar stands (Cal §341): r must fall out of the pinned generators un-tuned; generators pinned by the embedding (Lyra F856 + Grace: J acts on whole tangent = Helgason's Hermitian-symmetric definition; color = short-root multiplicity T2545), never because r yields 3/10. Held Structural/Identified until r lands. **The hinge Lyra + Grace named:** does U(1)_Y normalize over the whole tangent (→ r=3/10, A) or a component (→ B/C)? That IS what the blind r decides.

**Protocol armed. Elie + Keeper fire r blind next; Grace's 30% gap makes it decisive.**

— Keeper, K1276, 2026-08-08. BLOCK LIFTS: re-audit round 3 PASS (sin²θ_W 4-stragglers+flagship fixed, α_s honest, monotonicity m_Z=PD-explicit-split, no over-cap) → QM 10/10 ships on Casey's venue call. Housekeeping (non-blocking): notes/maybe table + Paper-106 aggregate headline (gates Paper 106 not QM). #85 snag resolved: blind outputs r=g′²/g² (not sin²θ_W); map sin²=r/(1+r); cases A r=3/10→3/13, B r=3/7→3/10, C r=3/5→3/8 (distinct, no phantom). Elie+Keeper fire r blind next. Nothing pushed.
