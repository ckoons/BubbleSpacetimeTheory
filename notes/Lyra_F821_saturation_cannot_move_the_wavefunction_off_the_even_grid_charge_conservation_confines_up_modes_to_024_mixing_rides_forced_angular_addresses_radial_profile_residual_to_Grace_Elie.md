# F821 — the decisive question (K1181), answered from symmetry (complements Grace's G2 corpus-trace): **top-Yukawa saturation CANNOT move the up-wavefunction off the even grid {0,2,4} — because the grid is labeled by a CONSERVED CHARGE, and saturation is a coupling/norm effect that conserves charge. The even grid = the Q=2/3 sector (m = N_c·|Q| = 2, even; F820). Electric charge is exactly conserved, so any up-type state is a Q=2/3 eigenstate, so its harmonic degree obeys k≡m=2 (mod 2) → k even → {0,2,4}, DYNAMICS-INDEPENDENT. The Yukawa/saturation connects left-up ↔ right-up through the NEUTRAL Higgs (Q=0, m=0), preserving Q=2/3 → it cannot bring odd-degree (Q=1/3 or 1) modes into the up-wavefunction (that would violate charge conservation). So saturation RESETS the radial mass-norm (the up masses aren't FK — accepted, Elie 5060) but the ANGULAR address stays parity-forced at {0,2,4}. Therefore the mixing — which rides the addresses (⟨up-shelf|down-shelf⟩), not the mass values — SURVIVES the up-mass failure: the CKM/PMNS overlaps ⟨{0,2,4}|{1,3,5}⟩ are still computable from forced angular addresses. The one genuine residual: saturation may redistribute the up RADIAL profile within the even grid, so the overlap's radial factor may be saturation-set (not FK-forced) — that's the honest open piece, handed to Grace (G2 corpus) + Elie (does the radial redistribution move the overlap, or is the mixing angular-dominated?).**

**Lyra, Wednesday 2026-08-05. A symmetry argument for the decisive question, offered ALONGSIDE Grace's G2 (two independent routes = stronger — isomorphism is nature's proof). I settle the angular/charge part (the wavefunction can't leave the even grid); I hand the radial-profile residual to Grace's corpus trace + Elie's fire. Calibrate both ways: I credit the reframe (mixing rides forced addresses, so the up-mass negative does NOT kill the mixing) AND I hold the honest residual (the up radial profile may be saturation-set, which the overlap could feel).**

## The argument (angular part of the decisive question — settled)
**Claim:** saturation resets the up mass-norm but cannot move the up-wavefunction off {0,2,4}.
1. **The even grid is a charge label.** m = N_c·|Q| (F820, T2470/T2521). Up-type: Q=2/3 → m=2 (even). The F817 parity lock forces k≡m (mod 2) → k even → {0,2,4}. So "up lives at {0,2,4}" is equivalent to "up carries Q=2/3."
2. **Charge is exactly conserved.** An up-type quark is a Q=2/3 eigenstate at all times. So m=2 at all times. So k is even at all times — independent of any dynamics (mass generation, saturation, RG running).
3. **The Yukawa preserves it.** Saturation (y_t=1) is the top-Yukawa hitting the Born-overlap ceiling (K765/K766). The Yukawa couples left-up ↔ right-up through the Higgs, which is NEUTRAL (Q=0, m=0, even). A Q=0 vertex cannot change Q=2/3. So the top wavefunction remains a Q=2/3 eigenstate = a superposition of ONLY even-degree modes. Saturation can redistribute the radial profile WITHIN {0,2,4}; it cannot import odd-degree (Q≠2/3) content.
∴ **the up-wavefunction stays on the even grid.** Saturation moves the NORM (radial/mass), not the SHELF (angular/charge). ∎ (angular part)

## Why this saves the mixing (the reframe, made rigorous)
- CKM/PMNS = ⟨up-wavefunction | down-wavefunction⟩ = overlap of WHICH SHELVES the modes sit on. The up-mass FK-failure (Elie 5060) kills "up masses are FK-forced" — it does NOT touch which shelves the up-modes occupy.
- The up-modes sit at the parity-forced {0,2,4} (angular, charge-conserved, above); the down-modes at {1,3,5} (F820, Derived down ladder). So the mixing overlaps ⟨{0,2,4}|{1,3,5}⟩ are computable from FORCED angular addresses. **The mixing computation is alive.**

## The honest residual (handed off — NOT settled by me)
The overlap ⟨up(k)|down(k')⟩ has an angular factor (the shelves — forced) AND a radial factor (the mode profiles). Saturation resets the up radial mass-norm; it may also redistribute the up radial PROFILE within {0,2,4}. If the mixing is **angular-dominated** (the shelf structure sets the overlap, radial is a subleading rescale) → mixing ~forced from addresses. If the radial profile **materially shifts** the overlap → the mixing picks up a saturation-set (possibly non-forced) radial piece, and the seven params stay Identified until that piece is pinned. **This is the real open question now** — not "does the wavefunction leave the grid" (it doesn't), but "does the overlap feel the saturation-reset radial profile."

## Tier / handoffs
- **@Grace (G2)** — my symmetry route says the answer to "does saturation keep the up-wavefunctions on {0,2,4}" is YES for the angular/charge part (charge conservation confines them; the neutral Higgs can't move the shelf). Your corpus trace is the independent confirmation — check that the corpus's saturation mechanism (y_t=1, top #43/#53, K765/K766) is a radial-norm/coupling effect (charge-preserving), not an angular one. If corpus + symmetry agree, the angular part is doubly settled. Then the residual you + Elie carry: is the up radial PROFILE (within {0,2,4}) FK-forced or saturation-set, and does the overlap feel it?
- **@Elie** — the mixing fire is NOT dead — it rides the forced angular addresses ⟨{0,2,4}|{1,3,5}⟩. When you fire the cross-grid overlap: separate the ANGULAR factor (forced shelves) from the RADIAL factor (up profile possibly saturation-set). If the overlap is angular-dominated, the seven params are forced-from-addresses even though the up masses aren't FK. Report which factor dominates.
- **@Cal** — the symmetry argument (charge conservation confines the up-wavefunction to the even grid; neutral-Higgs Yukawa preserves it) — rule it: is it airtight, or is there a loophole (e.g. a charged component in the saturation mechanism)? If airtight, the angular part of the decisive question is settled by conservation, independent of the corpus trace. Guard the residual: angular-forced ≠ mixing-forced until the radial factor is ruled subleading or pinned.
- **@Keeper** — the decisive question splits: ANGULAR (does the wavefunction leave {0,2,4}?) = NO, settled by charge conservation (F821, symmetry) + Grace's G2 corpus trace = two routes. RADIAL (does the overlap feel the saturation-set up profile?) = the genuine residual → Grace G2 + Elie's fire. So the up-mass negative does NOT kill the mixing path — the mixing rides forced angular addresses; nothing banks until the radial residual is ruled subleading. One-matrix picture holds: diagonal = masses (FK for down/lepton, saturation for up), off-diagonal = cross-shelf overlaps (angular-forced, radial-residual).
- **@Casey** — the up-quark mass test came back negative, straight and accepted — the up-quarks are too top-heavy for the gentle even ladder, exactly as Cal predicted it would fail. But here's why the mixing path lives: the mixing between quarks doesn't care what the quarks WEIGH, only which SHELF each one sits on — it's the overlap of their wavefunctions. And I can show the up-quarks can't leave their shelf even when the top gets its huge mass: the shelf is fixed by electric charge, charge can't change, and the thing that makes the top heavy (its coupling to the Higgs) is electrically neutral — so it can reset how heavy the top is, but it physically cannot move the top to a different-charge shelf. So the masses failed, the addresses held, and the mixing rides the addresses. The one honest thing I can't yet close: making the top heavy might reshape the top's wavefunction WITHIN its shelf (not its location, its spread), and the overlap could feel that. That's the real remaining question — Grace traces it in the corpus, Elie measures it in the fire — and it's a much smaller, sharper question than "is the whole path dead." It isn't. Nothing pushed.

Notes only; no toy/theorem claimed (symmetry argument for the decisive question, supports Grace G2). F821: saturation CANNOT move the up-wavefunction off the even grid {0,2,4} — the grid is a CHARGE label (m=N_c|Q|=2, F820), charge is conserved, and the top-Yukawa saturation acts through the NEUTRAL Higgs (Q=0) → preserves Q=2/3 → can't import odd-degree content. So saturation resets the radial mass-norm (up masses not FK, Elie 5060 accepted) but NOT the angular shelf. Mixing rides addresses ⟨{0,2,4}|{1,3,5}⟩ (overlap of shelves, not masses) → SURVIVES the up-mass negative, computable from forced angular addresses. RESIDUAL (honest, handed off): saturation may redistribute the up RADIAL profile within {0,2,4}; the overlap's radial factor may be saturation-set not FK-forced → mixing forced-from-addresses ONLY if angular-dominated. @Grace G2 corpus-confirm (charge-preserving saturation) + radial residual; @Elie separate angular (forced) vs radial (residual) in the fire; @Cal rule the conservation argument airtight + guard angular-forced≠mixing-forced. Two routes to the angular answer (symmetry + corpus). — Lyra