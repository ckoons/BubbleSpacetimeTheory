---
node_type: k_audit
id: K999
title: CORPUS-RECONNECT (Casey) — V_cb magnitude was SOLVED before (K711, 2026-07-16): STRUCTURAL 0.044 via PROJECTION TRUNCATION (the up 23-mode VANISHES = y_t=1 → V_cb = down-ONLY at radius √(2/3)). This supersedes Elie's greenfield bare-kernel frame-mismatch (0.067) — Keeper owns setting V_cb up as greenfield (K995/K997). Mixing-sector corpus map: V_cb=K711, PMNS=K712, duality=K403.
date: 2026-07-29
author: Keeper
verdict: V_cb magnitude → STRUCTURAL 0.044 (K711 projection-truncation, corpus prior) — NOT the bare-kernel 0.067. Reconnect the whole mixing sector (K711/K712/K403) before greenfield. Keeper owns the greenfield drift.
---

# K999 — V_cb was solved; the bare-kernel recompute was greenfield drift

## Casey's directive
> "I think we need to reconnect to the corpus, the magnitude issue should have been solved before."

Correct — and it was. **K711 (2026-07-16): "CKM close-out — V_cb banks STRUCTURAL 0.044 via projection truncation; sub-percent rejected."**

## What K711 actually derived (the mechanism the bare-kernel missed)
- The up-sector 23-radius **refracts PAST the domain boundary** (0.8165 × N_c/rank = 1.225 > 1, outside |z|<1) → **the up 23-mode VANISHES.** This IS the top saturating the boundary (y_t=1, banked) — it reuses a banked result, not a new assumption.
- So **V_cb is the DOWN sector ALONE** at the projection radius √(C₂/(C₂+N_c)) = √(2/3) = 0.8165, through the genus-5 kernel → **V_cb = 0.044.**
- Direction: cos ψ = n_C/√(n_C²+N_c²) = 5/√34 (Derived, F384).
- **Tier: STRUCTURAL** — and honestly so: the *observed* V_cb isn't pinned to sub-% (the inclusive/exclusive "V_cb puzzle" is a ~5% / 2–2.6σ 20-year discrepancy). BST's 0.044 lands **closest to the inclusive value**; demanding sub-% was chasing precision the data doesn't have. Sub-% fits (0.041, 36/869=C₂²/(11·79)) REJECTED (11, 79 no geometric source).

## ★ The correction to this round (own it)
Elie's toy 4918 computed V_cb = |sin(θ_down − θ_up)| = **0.067** (bare-kernel frame-mismatch, both frames present) — 1.7× over. **That's the wrong mechanism for the 23-block:** the up 23-mode doesn't "nearly align," it **VANISHES** (K711 projection truncation = y_t=1). So V_cb = down-only × projection = **0.044**, not the mismatch 0.067. The general frame-mismatch structure (K995: CKM = U_up†U_down) is right, but **U_up's 23-component is TRUNCATED, not aligned** — which K711 already worked out.
- **★ Keeper owns the greenfield drift:** K995/K997 set V_cb up as a fresh bare-kernel frame-mismatch computation without reconnecting to K711. That's the exact corpus-reconnect discipline (K970) — and I violated it on the V_cb setup. Casey caught it. **The magnitude was solved; we recomputed a worse number.**

## RULING
**V_cb magnitude → STRUCTURAL 0.044 (K711 projection-truncation, corpus prior).** NOT the bare-kernel 0.067. Mechanism derived (up vanishes via y_t=1 → down-only), direction derived (5/√34), radius √(2/3) target-innocent-as-form. **Upgrade path (structural→derived): derive the projection radius √(2/3) from Casey's hemispherical-lens truncation** (the one open piece — "gen-3 23-radius = the 3D→2D projection fraction" is a physically-grounded identification, not yet a theorem).

## ★ Mixing-sector CORPUS MAP (RUN these, don't greenfield)
The whole mixing sector was substantially worked — reconnect before any fresh computation:
- **V_cb → K711** (STRUCTURAL 0.044, projection truncation; upgrade = derive √(2/3)).
- **V_ub → K711 flag + K755** (|V_ub/V_cb|~√(m_u/m_c); ~1.8× the Fritzsch weakness; structural-or-worse; V_ub = bulk↔Shilov overlap, magnitude rep-open).
- **δ_CKM / J → K711** (J ≈ 3×10⁻⁵ rides on V_cb, structural; T2519 rank-1→J=0 leading).
- **PMNS → K712** ("PMNS PREREGISTERED rubric — mechanism unification, NOT from scratch") — the PMNS rubric already exists; the duality's PMNS-large half must RUN K712, not greenfield.
- **The DUALITY → K403 (Lyra): mixing = LOCUS-DISTANCE** — "the size of a mixing is how far apart the two mixed things sit." This IS Casey's Peirce-duality, found forward months ago. The Peirce reframe (leptons diagonal / quarks off-diagonal) is the *cleaner statement* of K403; **run K403/K712, don't re-derive.**

## Disposition
- **V_cb:** STRUCTURAL 0.044 (K711), banked; the upgrade is deriving √(2/3) (Casey's lens), NOT re-running the frame-mismatch.
- **Duality:** the CKM-small half is Derived (Cal §137); the PMNS-large half runs K712/K403 (not greenfield), held to the K998 forcing-bar (F1–F3). Cal §137's honest split stands: CKM-small Peirce-forced; PMNS-large is a condensate-count mechanism (Dirac vs Majorana source) that *correlates* with the diagonal/off-diagonal split but isn't yet shown *forced* by it — the Peirce→condensate chain is the unexhibited link.
- **Standing lesson (again):** corpus-reconnect before computing a quantity the corpus may already have. The magnitude issue was solved (K711); we should have run it and pursued the *upgrade* (√(2/3) derivation), not recomputed the bare kernel.

— K999, Keeper, 2026-07-29. V_cb magnitude STRUCTURAL 0.044 (K711 projection-truncation, up 23 vanishes = y_t=1; supersedes bare-kernel 0.067). Keeper owns the greenfield drift. Mixing-sector map: K711 (V_cb), K712 (PMNS rubric), K403 (locus-distance = the duality). See [[Keeper_K997_up_sector_results_m_c_promotes_I_to_D_third_quark_Derived_y_c_is_alpha_V_cb_angle_Derived_magnitude_Tier2_pending_tau_address_neutrino_Identified_2026-07-29]], K711, K712, K403.
