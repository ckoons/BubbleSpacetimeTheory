# F759 — The cc-magnitude FORENSIC BAR, committed BLIND before any a₀-magnitude derivation. **This is the guardrail, not the derivation — committed now so that whenever the forward computation happens, it is pinned and cannot be retrofitted to 10⁻¹²². The forensic heart (Keeper's spine, sharpened): the three banked forms disagree by 2.38 dex and the target is convention-dependent by ~1 dex, so the "match manifold" is ~3.4 dex wide — which means ANY claimed match tighter than that spread is form × convention SELECTION, not forcing. With that much freedom you can hit ~122 orders within a dex almost surely; so a sub-dex "match" proves essentially nothing. The bar sharpens K1065's seven criteria (Keeper's committed bar) into the specific tests below. The cc-magnitude stays IDENTIFIED at best until a mechanism clears all seven — and I expect that verdict to hold for a good while.**

**Lyra, 2026-08-01. The cc-magnitude is the real remaining cosmology prize AND the single most fit-tempting number in the program. So the bar goes down blind, before Elie's forward derivation and before I've seen any attempt — the F756/K1047 move, one number over, on the number that most wants to fool us. This does not presuppose Casey's timing (derive today / externalize first / rest); it's the prerequisite that sits ready under all three.**

## The null model this bar must beat (the forensic core)
- **Form spread:** the three banked forms for the cc-magnitude disagree by **2.38 dex** (orders of magnitude).
- **Convention spread:** the target itself is **convention-dependent by ~1 dex** — "the vacuum energy is ~10⁻¹²²" depends on ρ_Λ vs Λ, M_Pl vs reduced M_Pl, energy-density vs the 4th-root scale, etc.
- **Therefore the match manifold is ~3.4 dex wide.** A form × convention pair that lands within ~1 dex of the target is available almost surely by *selection*, with zero mechanism. **Consequence, stated hard: any cc-magnitude "match" tighter than the form×convention spread is SELECTION, not forcing, and is fit-suspect by construction.** Hitting 122 to sub-dex precision is the *expected* outcome of a fishing expedition, not evidence against one.

## The one question that decides Derived vs Identified (Keeper's framing, made the test)
**Does the suppression exponent fall out of a regularized heat-kernel computation done BLIND to the ~122 orders (target-innocent, forward) — OR is it the integer-combo chosen because it hits 122?**
- **Route (a) — forward, blind:** compute the regularized a₀-coefficient magnitude on D_IV⁵ *without knowing the answer is ~122*; the suppression exponent emerges from the regularization + the geometry. → clears toward **Derived** (if it also clears the other criteria).
- **Route (b) — the integer-combo that hits 122:** any post-hoc BST-integer expression selected because it lands near 122. → **Identified at best; fit-suspect.** This is the default and the trap.
The distinction is provenance, not value (Rule 11): the exponent must be distinguished by *which regularized computation produced it*, never by *that it equals ~122*.

## The forensic tests (what a Derived claim must pass — sharpening K1065's seven)
1. **Exponent forward & blind:** the suppression exponent emerges from the regularized heat-kernel computation *before* comparison to 10⁻¹²². If the computation is run knowing the target, it is Route (b) — Identified.
2. **ONE form, forced:** a single form, forced by a single mechanism. **Form-multiplicity is the fishing flag, not evidence** — "3 forms hitting 122" is a WARNING; the more forms that hit, the more selection freedom, the WORSE. Fewer-forms-forced is strictly better.
3. **Convention pinned BLIND to a primary source:** commit the exact target statement (which ρ_Λ / Λ / scale, which M_Pl) to a primary source *before* any comparison — do not relabel the target from memory (the "pin conventions to primary sources" discipline). A match that only works under one hand-picked convention is convention-selection.
4. **Provenance reported, not the number:** which operator / regularization scheme produces the suppression, stated with its provenance — the number 122 is worthless without it.
5. **Beat the null:** the claimed match must be tighter than the ~3.4-dex form×convention manifold *by a forced mechanism* — otherwise it is inside the selection floor and cannot be banked.
6. **Five-Absence filter first:** passes the forbidden list before banking.
7. **Score σ, scheme-aware, not dex-eyeball:** agreement scored against the (convention-fixed) uncertainty, carried per scheme.
**Verdict discipline:** IDENTIFIED at best until a mechanism clears all seven. Do NOT bank Derived on a match. Expected default outcome: Identified — and saying so is the honest result, not a failure.

## The linear-algebra cast (Casey's directive — how to compute it forward)
Cast the a₀-coefficient magnitude as a **regularized spectral trace of one operator on D_IV⁵**: Λ (or ρ_Λ) ∝ the regularized vacuum trace of the a₀ heat-kernel rung, normalized by the domain volume — Λ ~ Reg-Tr(e^{−tΔ})|_{a₀} / Vol, with the suppression = the ratio of that regularized trace to the naive Planck-scale estimate. The question in linear-algebra terms: does the regularization (zeta / heat-kernel cutoff on the Bergman spectrum k(k+n_C)) produce a suppression *exponent* as a spectral invariant of Δ — forced by the eigenvalue structure — or must the exponent be supplied by hand to hit 122? Compute the regularized trace forward; read the exponent off the spectrum; do NOT reverse-engineer from 122.

## Tier / handoffs
- **@Elie** — when the forward derivation runs: compute the regularized a₀-magnitude on D_IV⁵ **blind to the ~122** (Route a). **Report the exponent's provenance (which regularization/operator), not the number.** Commit your derivation half blind; I audit against this bar, committed here before I've seen your attempt. If the exponent must be supplied to hit 122, that is Route (b) → Identified, and we say so.
- **@Keeper / @Cal** — this sharpens your K1065 bar into forensic tests; the load-bearing one is the ~3.4-dex null (2.38 form + ~1 convention) — any sub-dex match is inside the selection floor unless a mechanism forces one form + one convention. Audit provenance-not-value the instant it lands; hold Identified until all seven clear.
- **@Casey** — the bar's down, blind, on the number that most wants to fool us — that part's the same discipline every day this week, one number over. Here's the honest forecast so nobody's surprised: the cc-magnitude has *so much* freedom (three forms spanning 2.38 orders of magnitude, and the target itself fuzzy by a full order depending on convention) that hitting ~122 to within a factor of a few is what a *fishing expedition* produces — so a "match" is the expected null, not a result. The only thing that counts is if the suppression exponent falls out of a regularized computation run *blind* to the answer. I expect the honest verdict to be Identified, possibly for a long time, and I want that on the record before we start so a pretty match can't move it. **The bar is the prerequisite under all three of your options** — derive now, externalize first, or rest at the summit — so it's ready whichever you pick; the timing is your call, not presupposed here. Separately, the small K1066 cleanup assigned to me (JCAP Paper #15 DE section → w=−1) is mine and independent of the direction — I'll take it as parallel hygiene. Nothing derived yet, nothing pushed; four GO'd + YM v0.3 shippable on your GO.

Notes only; no toy/theorem claimed (forensic bar, committed blind). F759: cc-magnitude bar. NULL MODEL: 3 forms span 2.38 dex + target convention-dependent ~1 dex → ~3.4-dex match manifold → any sub-dex match is form×convention SELECTION not forcing (hitting 122 is the expected fishing null). DECIDER: does the suppression exponent fall out of a regularized heat-kernel computation BLIND to ~122 (Route a → Derived) or is it the integer-combo chosen to hit 122 (Route b → Identified)? Provenance not value (Rule 11). Tests: exponent forward+blind; ONE form forced (form-multiplicity = fishing flag); convention pinned blind to primary source; provenance reported; beat the ~3.4-dex null by mechanism; Five-Absence; σ scheme-aware. VERDICT: Identified at best until all 7 clear; expected default Identified. Linear-algebra cast: Λ ~ Reg-Tr(a₀ rung)/Vol, exponent as spectral invariant of Δ, computed forward not reverse-engineered. @Elie derive blind, report provenance; @Keeper/@Cal hold Identified. Bar is prerequisite under all 3 of Casey's options (derive/externalize/rest) — timing is his call, not presupposed. — Lyra