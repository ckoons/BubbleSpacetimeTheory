# Round 147 — L1: the null bin, in the operator note's terms (one paragraph, hashed). A note on a note; nothing registers.
**Lyra, Monday 2026-09-14, 11:1x EDT.** Inputs: v1.1 §3.3–3.4 (the per-bin kinematic coefficient f_i = 2 + x̃_i(1+α̃_i) + B_i), §4.5 (the redshift channel, g_i; Landing A′/C′), K1899 §5 (the null bin), my R146 L1 (Theorem B's two factors; the dipole as the ℓ = 1 K-type; a frame as the kernel of the ℓ = 1 moment).

In bin *i* the count channel's kinematic ℓ = 1 moment is f_i β û — the boost's image on the k = 1 level of the Rac factor's density, with f_i = 2 + x̃_i(1+α̃_i) + B_i the bin's kinematic coefficient (aberration, flux boosting, and the observed-redshift boundary term of von Hausegger–Dalang Eq. (20)/(21)). **A null bin is a bin in which f_i = 0: the boost's image on the Rac factor's ℓ = 1 level vanishes — the kinematic moment lies in the kernel of the count channel's moment map — so whatever ℓ = 1 moment the counts carry there is the Rac factor's *own* anisotropy (the intrinsic term A w_i ŵ of §4.2) or a systematic, with no boost admixture by construction.** The clock factor has no null: its per-bin coefficient g_i is set by (1 + z̄_i) and dN/dz and does not vanish, so in the same bin the redshift channel's ℓ = 1 moment g_i β û still reads the boost. The null bin is therefore the one place where Theorem B's two factors are read *separately* in one sample — the counts read the Rac's own dipole, the redshifts read the clock's boost — which is what makes it a calibrator (K1899 §5: it fixes the intrinsic profile's amplitude from the data with no knob) rather than a defect. **Landing A′ (§4.5) is, in these terms, the statement that the Rac factor's kinematic ℓ = 1 moment — read from the non-null bins after the intrinsic term is subtracted — and the clock factor's ℓ = 1 moment are parallel and carry one β: one vector (K1898 §5), the operator form of "one boost, not two"; Landing C′ is the two moments failing to be parallel within their errors.** Three caveats on the note's face: f_i ≈ 0 in Elie's top bin [2.4, ∞) is a property of the *frozen* dN/dz (a steeply falling n(z) cancelling aberration-plus-flux against the redshift boost), not a theorem — control S5 decides whether it is a null in practice, and if S5 fails the bin is not null and the calibrator dies with it, as the round says; the null bin's intrinsic moment is not assumed zero (that is the point of measuring it there); and none of this moves P1's tier or adds a claim — it is the same identification (Rac density = counts, clock charge = observed redshift) read in the bin where the two are cleanest.

— Lyra.
