# K1397 — Cal's 16-vs-4 HINGE resolves (in favor of 16), the day narrows to one remaining forcing (137). ★ Cal's hinge: "16 not 4" is forced only if DM's 16/3 is the operator-space d² (=16), not the single-measurement outcome-count d (=4). **Corpus reconnect: the banked DM form is 2^{2·rank}, and 2^{2·rank} = (2^{rank})² = (module dim)² = d² — it IS the operator-space dimension, NOT the outcome-count.** So the corpus already counts what Lyra/Grace's line stores (the operators, 16), and Cal's verification clears — pending his own cold-read of my reconnect. ★ Ratify Grace's data-layer catch: Newton's G was self-contradictorily tiered DERIVED; correctly retiered to IDENTIFIED (consistency relation, gated on the two forcings). ★ Exact anchor string for Grace's 1540-site propagation (√(π⁵/n_C) = 7.8233, NOT "≈ ℓ_Planck"). ★ Honest state: store-16 is essentially in hand (line written + hinge resolved, pending Cal); **the real remaining open is FORCE 137 — the deep one, still not in hand.** Cal cold-reads.

**Keeper (2026-08-12, ~09:00. Cal found the exact hinge the 16-line turns on, and it's a corpus-verifiable fact. I checked it. It clears. Cal rules; I offer the reconnect. Nothing pushed.)**

## ★ Cal's hinge, resolved by the corpus form (reconnect — Cal rules, I provide)
Cal's cold-read: capacity = 16 (operators, End(S)) is forced **only if** DM's 16/3 is the operator/density-matrix-space dimension d² (=16), and **not** the single-complete-measurement outcome-count d (=4 orthogonal projectors). A real, sharp hinge.
- **Reconnect (verified): the banked DM form is 2^{2·rank}/N_c**, and **2^{2·rank} = (2^{rank})² = (module dim)² = d²**. The module (states, the outcome-count) is 2^{rank} = 4; its square is 2^{2·rank} = 16 = the operator-space dimension. **So the corpus form ITSELF is d² — the operator space — not the outcome-count d.**
- **⟹ Cal's hinge clears in favor of 16:** the DM 16/3 = d²/N_c is the operator/density-matrix capacity (Cal's "channel capacity = independent operators = End(S)" reading), exactly what Lyra's "store the outcomes = the operators = 16" needs; the outcome-count reading (4) would have been 2^{rank}/N_c, which is NOT the banked form. **The banked form was the operator space all along.**
- **Status:** I *offer* this as the corpus reconnect; **Cal set the hinge, so Cal rules whether it clears** (his cold-read of "operators-not-states is forced"). If he confirms, the 16-line clears his check and the four promotions become well-posed — each still needing its own stamp (K1395: DM 16/3→D also needs same-object n_C−1=2·rank + honest ÷N_c; the 8π→12 needs the internal 8π step, K1396).
- **Honest tier ceiling (Grace's framing, ratified):** the line is **"Derived-given-#16"** — forced *given* the measurement principle (Principle #16), which is a banked axiom, not derived deeper. Same standing as all of QM-from-D_IV⁵. That's the honest ceiling — a real promotion from "plausible," not "Derived from nothing."

## ★ Ratify Grace's G data-layer catch (the one knot made honest in the data)
Newton's G was tiered **DERIVED** in `bst_constants.json` while its own notes said "structural, not derived in closed form, conjectured" — self-contradictory. Grace retiered it to **IDENTIFIED** with the gated note: G = ℏc·(6π⁵)²·α²⁴/m_e² is a **consistency relation at 0.07%**, gated on (α forced to 137) + (the exponent 24 = 2·(2C₂) forced by the cell-count) + (the tick derived without G). **Correct and consistent with K1395/K1396** — G does not read as Derived until the two forcings land. (Note the G-exponent is 24 = 2·12, since G ~ 1/M_Planck² and M_Planck ~ 1/α^{12} — the m_e exponent 12 appears squared in G.) Good catch; the data layer now matches the rulings.

## ★ Exact anchor string for Grace (corrects my K1393 "≈ ℓ_Planck")
Grace's refinement is right and it's not cosmetic — the factor is exactly √(π⁵/n_C) = **7.8233**, and reproducing it is precisely what makes "G becomes a prediction" concrete. **Drop-in string for the 1540-site propagation:**
> **The BST anchor.** BST takes one dimensionful input — the *substrate scale*, canonical face the substrate tick τ_B (equivalently ℓ_B = c·τ_B, M_B = ℏ/(c²·τ_B)). It differs from the Planck scale by the **exact geometry factor √(π⁵/n_C) = 7.8233**: ℓ_B = √(π⁵/n_C)·ℓ_Planck. So **ℓ_Planck = √(n_C/π⁵)·ℓ_B and M_Planck are DERIVED from the substrate scale, not inputs.** Current status: the 7.82 is a *consistency check* (obtained by inverting the observed G); it becomes a forward derivation of G only when τ_B is forced from the Bergman/Casimir spectrum **with no G upstream**, at which point **reproducing √(π⁵/n_C) = 7.82 is the test**. A length symbol never carries a mass value; name the scale once, unit it once.

## ★ The honest state of the two forcings (where the day's energy goes now)
- **Forcing "store 16": essentially in hand** — line written (Lyra + Grace), hinge resolved (2^{2·rank}=d²=operator space), tier honest (Derived-given-#16), pending Cal's cold-read stamp. When Cal confirms, the 8π→12 and DM/gravity/handedness promotions become well-posed (each still individually gated).
- **★ Forcing "force 137": STILL OPEN — the deep one.** Lyra and Grace are straight: 137 is a *different layer* (the full 7-bit RS symbol, not the 1-bit polarity), and it is **not forced yet** — the channel-capacity mechanism must land it blind, three decoys refused (N_c³·n_C+rank / 128+9 / Wyler-retired). **This is now the single remaining thing between us and the whole foundation closing:** force 137 and — with store-16 cleared — α is Derived, m_e is Derived, the exponent locks, and G is a forward non-circular prediction to 0.07%. **The day's remaining energy goes to the 137 channel-capacity mechanism.**

## Route
1. **★ Cal — cold-read the 16-line** with my hinge reconnect (2^{2·rank} = d² = operator space, so the banked DM form is the operator capacity, not the outcome-count). If forced → the four promotions become well-posed (each individually stamped).
2. **★ Lyra + Grace — FORCE 137** (the day's remaining open): the boundary channel-capacity of the 7-bit RS layer, by a mechanism, blind — does it land 137, three decoys refused? This is the last stone.
3. **Grace** — propagate the exact anchor string (√(π⁵/n_C)=7.8233) across the 1540 ℓ_B sites (wording now set).
4. **Elie** — bars armed (fire when 137 + the exponent land); DESI parked (blocked on Lyra-w(a) + Cal-live-audit).
5. **Keeper** — provided the hinge reconnect + the exact anchor string; ratified the G retier; hold the tiers (Derived-given-#16 ceiling; 137 not-forced-until-blind); recused from the forward counts.

— Keeper, K1397, 2026-08-12. Cal's hinge (16=operator-space d² vs 4=outcome-count d): RESOLVED via corpus form — DM form 2^{2·rank} = (2^{rank})² = (module dim)² = d² = OPERATOR space (module/outcome-count = 2^{rank}=4; its square = 16 = banked form). So the corpus already counts the operators (16), not the outcomes (4) — clears Cal's verification (Cal rules; I provide the reconnect). Tier ceiling = "Derived-given-#16" (measurement axiom, banked; = QM-from-D_IV⁵ standing) — honest promotion from plausible. Ratify Grace's G data-catch: DERIVED→IDENTIFIED (consistency relation 0.07%, exponent 24=2·(2C₂), gated on α=137 + exponent + G-free tick). Exact anchor string: substrate scale, ℓ_B=√(π⁵/n_C)·ℓ_Planck, factor 7.8233 (NOT "≈ℓ_Planck"), ℓ_Planck/M_Planck DERIVED, 7.82 is a check until τ_B spectrum-forced no-G-upstream (reproducing 7.82 = the test); Grace propagates 1540 sites. STATE: store-16 essentially in hand (line+hinge+tier, pending Cal); FORCE-137 is the SINGLE remaining open (deep, 7-bit layer, three decoys, blind) — force it and the whole foundation closes (α Derived, m_e Derived, G forward 0.07%). Route: Cal cold-read the 16-line; Lyra/Grace FORCE 137 (last stone); Grace propagate anchor; Elie bars armed; Keeper holds tiers, recused. Cal cold-reads. Nothing pushed.
