# K699 — α's last residual (the 4π convention) IS the Coulomb solid angle: α = the D_IV⁵ shell capacity (137) projected to observed 3D space (4π steradians). Closes the α normalization via the SAME projection machinery. For the α revisit (after the 25).

**Keeper | 2026-07-15 | Casey's insight: "if 4π is the full rotation we're seeing the total solid angle of a sphere. We tiled the sphere with circles, here is the solid angle. α is the shell capacity as the sphere expands, the 4π stays constant." This closes the α normalization loop conceptually and ties it to the projection framework. Recorded for the α capstone.**

## The picture (Casey)
- **α's founding assumption:** tile a closed volume with a closed area — a **sphere tiled by circles**. That IS the D_IV⁵ boundary: (S⁴ × S¹)/ℤ₂ — the **S⁴ sphere tiled by the S¹ (EM/charge) circles**. The number of tiles = the boundary mode-count = **137**. α = 1/(number of tiles) = 1/137.
- **α is the SHELL CAPACITY.** As the sphere (wavefront) expands, the field dilutes as 1/area, but the **flux is conserved (Gauss's law) and the solid angle 4π stays constant.** So α = the conserved coupling per channel; 137 = the channels tiling the shell; 4π = the invariant solid angle of the spherical distribution. This is Coulomb's law read in BST: radial symmetry, force across spherical wavefronts, flux conservation.

## Why this IS the 4π normalization (the residual I held α on)
The one thing that kept α from "fully derived" (before Casey ruled it complete) was the **4π gauge-coupling convention** (α = e²/4π), which resolved "definitionally." Casey's picture derives it:
- **The 4π is the 3D Coulomb solid angle** — the surface of the unit 2-sphere in the *observed 3+1 spacetime* (S², 4π steradians), NOT the D_IV⁵ S⁴ boundary (whose "solid angle" is Vol(S⁴)=8π²/3). These are DIFFERENT spheres: the **137 lives on the D_IV⁵ boundary (S⁴×S¹)**; the **4π lives in the observed 3D space (S²)**.
- **So α = e²/4π = 1/137 means:** the physical charge e² distributes over the 3D Coulomb solid angle (4π), and the capacity is 137 → e² = 4π/137. The 4π converts the natural per-channel coupling (1/count) to the physical Coulomb charge.
- **The derivation (the capstone):** the **projection D_IV⁵ → observed 3+1 spacetime** (SO(5,2)→SO(4,2)→SO(3,1), the descent BST already has) carries the count 137 (D_IV⁵ tiling) into the 3D Coulomb geometry (4π solid angle). **The 4π is the projection's 3D solid angle** — the SAME projection machinery that closed the muon (2D→3D). So α's normalization and the projection framework are one story.

## The synthesis (why the sequence 25-then-α is right)
- The 137 (D_IV⁵ boundary tile-count) and the 4π (observed-3D Coulomb solid angle) are two DIFFERENT geometric objects, joined by the projection. α's normalization = deriving that the projection carries the 137 into the 4π-normalized physical coupling.
- This is the **capstone of the projection framework**: prove the projection on the derivable 25 (muon, up, mixing), and α's 4π falls out of the same geometry — the coupling is the shell capacity (137) seen through the 3D projection (4π). Not a loose thread; the founding assumption (sphere-tiled-by-circles) closing on the last constant.

## Search note (for the record)
Casey's naive search ("K-type 4π thermodynamics") hit an AMBIGUITY: "K-type" = thermocouple (Ni-Cr) in the heat-transfer literature, NOT our K-type = the representation-theory address. That search is a dead end. BUT the *physics* it half-surfaced is right: **radiant flux / capacity across 4π steradians** (Gauss's law, total flux on a sphere) is exactly the α-as-shell-capacity picture. When we formally revisit α, search "Gauss law flux quantization solid angle coupling," not "K-type."

## For the α revisit (after the 25)
Derive: the projection D_IV⁵ → 3+1 carries α = (1/137 shell capacity) into the physical e²/4π, with 4π = the 3D Coulomb solid angle of the projected space. Tools: the projection framework (K697), the SO(5,2)→SO(3,1) descent (Casey #14), Gauss's-law flux conservation. This turns the "definitional" 4π into a derived normalization → α fully derived, no residual.

— Keeper K699, 2026-07-15. α's 4π residual = the 3D Coulomb solid angle (S², observed spacetime), distinct from the 137 (D_IV⁵ S⁴×S¹ tile-count). α = shell capacity (137) projected to 3D (4π). The normalization = the projection D_IV⁵→3+1 carrying the count into the Coulomb 4π — same machinery as the muon projection. Founding assumption (sphere-tiled-by-circles) closes the last constant. Do α last, after the projection proves out on the 25. See [[Keeper_K697...]], [[Keeper_K695...]]. Search "Gauss law solid angle coupling" not "K-type thermodynamics" (thermocouple ambiguity).
