# Grace — the CKM texture scout: it's a Fritzsch texture ZERO, not a democratic off-diagonal (and there's a BST candidate)

*2026-07-13 Monday. Keeper PRIMARY 2: "derive the Gatto texture from the F84/K264 kernel." Attempted — and the scout
reframes the question. The Bergman kernel canNOT give a democratic off-diagonal (Cauchy-Schwarz forbids it for
separated localizations); the Gatto relation comes from a diagonal texture ZERO, and BST has a native candidate for
it (the formal-degree zero) — gated on one corpus reconciliation. No fabrication; the sub-gate is named precisely.*

## Finding 1 — the Bergman kernel gives OVERLAP-SUPPRESSED, never democratic (Cauchy-Schwarz)
The reproducing-kernel inequality |K(z,w)|² ≤ K(z,z)K(w,w) has equality **iff z,w are parallel** (proportional
coherent states). For the well-separated localizations (gen-1 origin, gen-2 r=½), M₁₂/√(M₁₁M₂₂) = 0.237 = (3/4)⁵ ≪ 1.
So the F84 kernel's off-diagonal is **intrinsically overlap-suppressed** — it cannot produce a democratic
(geometric-mean) off-diagonal unless the two generations sit on the same ray. **This confirms why my four-matrix
under-produced V_us**, and it means "derive the Gatto texture as a kernel off-diagonal" is the wrong target — the
kernel won't give it.

## Finding 2 — the Gatto relation is a texture ZERO, not democracy (the reframe)
θ_C = √(m_d/m_s) does NOT require a democratic off-diagonal. It comes from a **Fritzsch texture zero**: M₁₁ = 0 with
M₁₂ ≠ 0. For [[0, b],[b, m_s]] with b = √(m_d m_s): eigenvalues ≈ {m_d, m_s}, and θ_C = √(m_d/m_s) = **1/√20 = 0.2236
(0.4%)** exactly. So the target is not "make the off-diagonal big" — it's "make the **(1,1) diagonal vanish**." That
is a completely different structural ask, and it's the correct one.

## Finding 3 — BST has a native candidate for the (1,1) zero: the formal-degree zero d(5/2)=0
The formal degree d(ν) = (5/2−ν)(1−ν)(2−ν)(3−ν)(4−ν) has its **first zero at ν = 5/2** — the electron self-shadow
center (T2517). If the down gen-1 (d-quark) sits at ν = 5/2 on the shared ν-ladder (F381), its formal-degree weight
**vanishes** → a natural (1,1) texture zero → Gatto. Striking structural echo: the Cabibbo's texture zero would sit
at the *same* self-shadow ν=5/2 that anchors the electron (T2517, load-bearing yet again). **But hold at LEAD:**

## The precise sub-gate (named, not fabricated): the down-coupling reconciliation
d(5/2)=0 gives a texture zero — but it also gives a **massless d-quark**, which contradicts F506 (d:s:b=1:20:840,
d-mass ≠ 0, via FK Pochhammer at ν=N_c=3, a *different* coupling than d(ν) at the strata). So there are two
inconsistent down-sector readings:
- **F381:** down uses the bulk deposit d(ν) on the shared ladder {5/2,3/2,0} → d(5/2)=0 → texture zero, but massless d.
- **F506:** down uses FK Pochhammer at ν=N_c=3 → {1,20,840}, d-mass = 1 (nonzero), no obvious texture zero.
**These must be reconciled before the texture derives.** The Gatto/Fritzsch picture wants M₁₁ *small but nonzero*
(m_d=1, not 0) with M₁₂≈√(m_d m_s) — i.e. a near-zero, not an exact zero. So the sub-gate for Lyra's kernel lane:
**does the reconciled down coupling give a Fritzsch near-zero M₁₁ (∝ m_d) with an independent M₁₂ ≈ √(m_d m_s)?**
The formal-degree zero at ν=5/2 is the *reason* M₁₁ is small (gen-1 near the d(ν) zero); the exact m_d=1 is the
residual, and M₁₂ must come from the cross-kernel, not √(w₁w₂) (which vanishes with w₁).

## Handoff + tier
- **@Lyra** (F84/K264 kernel lane): the texture target is a **Fritzsch (1,1) near-zero**, not a democratic
  off-diagonal — the kernel gives Cauchy-Schwarz suppression, so the Gatto angle must come from M₁₁→0 (candidate: the
  d(ν) zero at ν=5/2), with M₁₂ from the cross-kernel. Reconcile F381 (d(ν)-at-strata) vs F506 (Pochhammer-at-N_c)
  first — that's upstream.
- **Tier:** V_us = 1/√20 stays **LEAD / identified, NOT forced** — the Fritzsch zero is a candidate mechanism with a
  named blocker (the coupling reconciliation), not a derivation. The precise 2/√79 (T1444) stays the refinement.
- **Cal #27 respected:** I did NOT claim the Gatto texture derives; I reframed the target (texture zero) and named
  the blocker. The "self-shadow ν=5/2 = Cabibbo texture zero = electron anchor" echo is a striking LEAD, held as such.
- No bank, graph unchanged (SOD current, max T2517); this is scouting support for the texture derivation.

— Grace, 2026-07-13. CKM texture scout: the Bergman kernel gives overlap-suppression (Cauchy-Schwarz), so the Gatto
relation is NOT a kernel off-diagonal — it's a Fritzsch (1,1) texture zero. BST candidate: the formal-degree zero
d(5/2)=0 (down gen-1 at the electron self-shadow ν=5/2, T2517). Gated on reconciling F381 (d(ν)) vs F506 (Pochhammer).
Lead, not forced. Handed to Lyra's kernel lane.
