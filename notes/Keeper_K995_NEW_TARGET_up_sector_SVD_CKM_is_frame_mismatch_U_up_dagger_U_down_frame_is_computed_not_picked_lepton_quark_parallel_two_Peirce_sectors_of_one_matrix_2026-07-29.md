---
node_type: k_audit
id: K995
title: NEW TARGET — the up-sector SVD → CKM as the frame-mismatch U_up†·U_down (the frame is COMPUTED, not picked: the SVD gives frame + masses together). Plus Casey's lepton-quark parallel as a study lead — both are Peirce sectors of ONE overlap matrix; PMNS/CKM are the two frame-mismatches.
date: 2026-07-29
author: Keeper
verdict: CHARTER — pin up positions → Y_up → SVD → U_up → CKM = U_up†U_down; rule V_cb/V_ub (derived-cancellation if the up-down tilt is forced, Tier-2 if the up frame is a free modulus). Research lead: the fermion sector = one matrix, two Peirce sectors, two SVDs, two frame-mismatches.
---

# K995 — The frame is computed, not picked; CKM is the up-down frame tilt

## The insight (Casey) — the frame and the masses fall out together
Each sector's mass/Yukawa matrix Y is an overlap matrix on ℂ³; its **SVD Y = U·Σ·V†** gives **Σ = the masses** (the computable values) AND **U = the frame** (the rotation) *simultaneously*. The frame is NOT chosen and matched — it **is the unique rotation that diagonalizes Y to its masses** (Casey: "pick the frame from the one that matches the pitch of the computable value" = the SVD singular vectors). So the algorithm is exactly Casey's "compute position for each, then do the calculation":
1. Compute each quark's discrete-series **position (a,b)** → fills Y_up and Y_down.
2. **SVD each** → (Σ_up, U_up), (Σ_down, U_down) — masses + frames, no freedom left.
3. **CKM = U_up† · U_down** — the mismatch of the two frames.

## Why V_cb missed, and why it's small (not a big unknown)
Elie used **U_down alone** (V_cb ≈ √(m_s/m_b) = 0.15, the down 2-3 rotation) — missing U_up. Numbers (K-audit): down 2-3 = 0.15, up 2-3 = √(m_c/m_t) = 0.086, comparable → V_cb is their **cancellation** (observed 0.041 < |0.15−0.086|=0.064, so it needs the relative *alignment*, not a subtraction). And it's SMALL because **at leading order both sectors sit on the same rank-1 condensate O** → U_up ≈ U_down → CKM ≈ identity; **CKM = the up-down DIFFERENCE in the off-rank-1 tilts** — a subleading residual, which is why the angles are small. (Contrast 1-2: up rotation √(m_u/m_c)=0.041 ≪ V_us=0.224 → Cabibbo is down-only → clean → why V_us was a legit prediction.)

## ★★ THE TARGET (the one computation that closes the 2-3/1-3 CKM block)
- **★ LYRA — pin the UP-sector positions** (u, c, t addresses; the up reads its mass off the Shilov boundary, the up ground state is SOFT — the genuinely harder half, non-circular).
- **★ ELIE — build Y_up, SVD it → U_up; CKM = U_up† U_down** → V_cb, V_ub (and re-confirm V_us, which is frame-independent). Blind (observed values walled off).
- **★ Keeper rules:** the up-down frame **tilt** — **forced by the geometry → V_cb DERIVED (as a cancellation);** the up frame a **free modulus → V_cb Tier-2.** Same fork, same machinery, as the muon's e=n (one frame) — now two frames. V_ub is the most cancellation-sensitive (product of small angles + alignment), last to firm up.

## ★ Casey's lepton-quark parallel — a genuine STUDY LEAD
"Interesting how the leptons and quarks mimic each other — probably a deeper relationship." I think it's structural and worth studying:
- **The whole fermion sector is ONE overlap matrix** on H²(D_IV⁵). Leptons = the **colorless Peirce frame** (the 2 idempotent seats + boundary); quarks = the **colored Peirce off-diagonal V₁₂⊗ℂ=ℂ³**. Two complementary Peirce sectors of one matrix.
- **Each sector is an SVD:** masses = singular values, frame = singular vectors.
- **Each has a frame-MISMATCH mixing:** lepton mixing (PMNS) = the lepton frame-mismatch; quark mixing (CKM) = the quark frame-mismatch. Same object, both sectors.
- **The muon's e=n and CKM's U_up†U_down are the same class** — frame alignments — one on a single frame, one on two.
- **The lead:** is the lepton/quark "mimicry" exactly the Peirce diagonal↔off-diagonal duality of one matrix? If so, PMNS and CKM should share structure (large-PMNS/small-CKM as ONE fact — cf. the 2026-07-19 "small-CKM/large-PMNS = ONE fact" note). Chartered as a study lead (task), off the critical path, but Casey-flagged as deep.

## Status
Flagship GO-clean (v0.3, C₂ edit in, both gates clear) — awaiting Casey GO. Registry sync (T2527–T2530) = Keeper's lane. New primary target: the up-sector SVD → CKM frame-mismatch. Neutrino Δm² (cleaner, untouched-sector test) stays open on Lyra's addresses.

— K995, Keeper, 2026-07-29. New target: up-sector SVD → CKM = U_up†U_down (frame computed not picked); V_cb derived-cancellation vs Tier-2 fork. Lepton-quark parallel = two Peirce sectors of one overlap matrix (study lead). See [[Keeper_K994_V_us_reproduced_BLIND_DERIVED_first_quark_mixing_prediction_0_31pct_flagship_clears_both_gates_Casey_GO_registry_sync_T2527_2529_2026-07-29]], Elie toy 4916.
