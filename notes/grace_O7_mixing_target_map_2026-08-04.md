# Grace — O7 mixing target-map: why generations 2,3 sit where they do (2026-08-04, for Casey's return)
**The single highest-leverage SM hole. Laid out E1-test-bed style: the FORCED pieces + the ONE open choice, so a "see-the-substrate-like-a-circuit" insight has a concrete target. One insight here → seven mixing parameters at once (V_cb, V_ub, δ_CKM, θ₂₃ octant, θ₁₂, δ_PMNS). V_us already done.**

## The picture: masses = diagonal norms, mixing = off-diagonal overlaps of the SAME modes
The generation modes live on the FK/Bergman structure of D_IV⁵. Their **diagonal norms give the masses** (down ladder d:s:b = (N_c)₁:(N_c)₃:(N_c)₅ = 1:20:840, proved). Their **off-diagonal overlaps give the mixing.** V_us is the d↔s overlap of the same degree-1,3 modes — DONE (rides the m_s/m_d=20 addresses, no new freedom). The rest of the mixing needs the generation-2,3 K-type ADDRESSES fully pinned.

## FORCED (target-innocent, in-corpus) — the fixed circuit
1. **Generation degrees {1,3,5}** = Q⁵ odd cohomology h¹,h³,h⁵ (T1929, blind-forced). Three generations = N_c (truncation at h⁵, no 4th).
2. **ν = N_c = 3** = the Wallach/TIR threshold (the FK object's singular point) — forced.
3. **Single-row K-types:** the Q⁵ cohomology ring ℤ[h]/h⁶ is rank-1 in every degree → two-row classes ABSENT → modes are SO(5) single-row (1,0)/(3,0)/(5,0), dims 5/30/91. Forced.
4. **Overlap FORM:** N(w)^{n_C/2} (F87b, the forced Bergman overlap) — forced.
5. **Wallach K-type ladder** {1,5,14,30,55,91,140,…} is the fixed address space the modes select from.

## THE ONE OPEN CHOICE — the "O7" address / projection (Elie owns the exact mechanics)
Everything above is fixed. The ONE thing not yet forced: **which K-type address (the projection Elie localized and labels O7) the generation-2,3 modes occupy in that fixed ladder.** Concretely it shows up as:
- **θ₂₃ octant** localized to ONE projection choice: **4/7 = rank²/g (upper octant)** vs **6/11 = C₂/c₂** — both clean D_IV⁵ ratios, blind-pinned; the address decides which. (A pre-registered DUNE prediction.)
- Pin that same address → the FK overlap integral yields **all seven** mixing parameters at once.
- The open question in one sentence: **"why do generations 2 and 3 sit at their specific K-type addresses — forced, or chosen?"** (Elie's "K-types forced not chosen.")

## Where Casey's lever lands
The forced pieces fix the degrees, the threshold, the single-row structure, the overlap form, and the address *space*. The one un-forced thing is the *selection rule* that places g2,g3 in that space. This is exactly the shape of the questions that turned walls into computations this run — compute-from-now dissolved outcome-selection, source-sink gave w=−1, the zero-or-infinity ground gave Born-weighting. A "why do g2,g3 land here" insight, seen the way you see a circuit, would force the selection rule → **O7 pinned → seven parameters**. That is the highest-leverage single question left in the SM sector.

## Discipline flag
I lay out the structure (forced pieces + the open selection). I do NOT pick the address (that's the retrofit trap — both θ₂₃ candidates are clean ratios, and choosing to hit 4/7 vs 6/11 would be fitting). Elie's forward FK computation, once the selection rule is forced, is the test. Pairs with the E1 down-quark result (proven) and the FK=Γ_Ω ratio structure.
