# F815 — RECONNECT MAP for the item-3 (SM-parameter) pivot, per Casey ("connect the team to the corpus; remember linear algebra on D_IV⁵"): **the entire SM flavor sector — all nine masses and all mixing angles — IS one object: the Bergman/Faraut–Korányi OVERLAP MATRIX between the K-type addresses on D_IV⁵. Diagonal entries = masses (the E1 FK-weight norms); off-diagonal entries = mixing angles (the overlap of adjacent generation layers). The corpus has ALREADY IDENTIFIED most of the entries as clean geometric ratios — DON'T re-derive them. The frontier (O7) is to FORCE them: compute the overlap matrix forward at the K-type addresses, turning the read-off identifications into derivations. That is the linear-algebra-on-D_IV⁵ move, and it is one computation for the whole sector.**

**Lyra, Wednesday 2026-08-05. The reconnect discipline that keeps catching us (K1147 — I imported quark {1,3,5} instead of reconnecting to the banked ρ-components) applied to the whole flavor pivot: survey what's banked FIRST, frame it as the one overlap-matrix object, and point the O7 work at forcing the entries — not re-deriving them. Tier-honest: the entries are mostly IDENTIFIED (read-off), and the forward overlap computation is what moves them to Derived.**

## The ONE object (linear algebra on D_IV⁵)
The flavor sector is the overlap matrix M_{ij} = ⟨layer_i | layer_j⟩ in the weighted Bergman/FK inner product on D_IV⁵, where the "layers" are the K-type addresses (the generation strata / Wallach points):
- **Diagonal M_{ii} = masses** — the FK-weight norm of each address (this is the E1 work: the Gindikin Γ ratio (ν)_k gave the down-quark ladder 1:20:840; Koide = the Γ product; F802/F800/F801).
- **Off-diagonal M_{ij} = mixing angles** — the overlap of adjacent generation layers; the corpus (BST_CKM_PMNS_MixingMatrices) already has the mechanism: **a 1/√n_C suppression per generation gap** (Cabibbo ~ 1/√n_C = one gap; V_cb ~ 1/n_C = two gaps; V_ub ~ 1/n_C^{3/2} = three gaps).
- **The CKM/PMNS split = the two-sector duality (T1446, PROVED):** CKM = colored (the S⁴ factor of the Shilov boundary), PMNS = colorless (the full boundary) — "why neutrinos mix large and quarks small is one color fact" (the color-mixing-duality paper). Quarks localize on the boundary (small overlaps → small angles); leptons are bulk/colorless (large overlaps → large angles).
So: **one overlap matrix, on the fixed operator; masses on the diagonal, mixing off it; the color split sets the two scales.** That is the whole flavor sector as linear algebra on D_IV⁵.

## What's BANKED — reconnect here, do NOT re-derive (tier-honest)
- **CKM Wolfenstein (T1936, all four, Identified):** λ = 5/22, A = 13/16, η̄ = 7/20, ρ̄ = 11/69 → V_cb 0.4%, V_ub 1.9%, V_td 1.4%, Jarlskog 3.3%. Cabibbo also T202 (1/(2√n_C), 0.3%) + T1446 (2/√79).
- **CKM CP phase γ (T1960, Identified, 0.5%, within 1σ):** γ = c₂·π/(N_c·rank·n_C) = 11π/30; Jarlskog → 0.02%.
- **PMNS all three angles (T1935, Identified):** sin²θ₁₂ = 2·rank/c₃ = 4/13; sin²θ₂₃ = C₂/c₂ = 6/11 (T1932, the tightest, 0.10%); sin²θ₁₃ = N_c/N_max = 3/137. Each uses a different Q⁵ cohomology degree.
- **The mass hierarchy scaffold (T1948):** Wallach K-type dims [1,5,14,30,55,91,140] = layers; Ogg primes = per-generation scales; Q⁵ odd cohomology h¹,h³,h⁵ = the 3-generation count (no 4th).
- **The torus frame (T1937, Casey):** mixing angles = rotation angles on the rank-2 maximal torus T² ("rank IS the surface dimension") — the geometric home of the overlap rotations.
- **The lepton overlap picture (BST_Conservation_Laws):** PMNS angles = overlap integrals between the D_IV^{1,3,5} ground states (the three families).
**These are IDENTIFICATIONS (read-off-geometry, T1926 methodology, AC=(C=1,D=0)) — clean geometric ratios that match, NOT yet mechanism-forced.** That is the honest tier, and it is the exact gap the frontier closes.

## The FRONTIER (O7) — force the entries forward (the linear-algebra move)
The O7 target-map (Grace) + the E1/FK machinery are the tools: **pin the K-type addresses of generations 2 and 3 (target-innocent, from the geometry), then evaluate the FK overlap integral forward at those addresses.** If the forward overlap reproduces the banked ratios (λ = 5/22, sin²θ₂₃ = 6/11, …), the mixing angles move Identified → **Derived**, and one computation (the overlap matrix) closes **seven parameters at once** (V_cb, V_ub, δ_CKM, θ₂₃, θ₁₂, θ₁₃/δ_PMNS). This is the highest-leverage single move in item 3, and it is exactly the E1 work (the forced FK measure) extended from the diagonal (masses) to the off-diagonal (mixing). **Casey's O7 question — why generations 2 and 3 sit where they do — IS the address-pinning step; the overlap integral does the rest.**

## The reconnect discipline (for the whole team)
- **Reconnect, don't re-derive:** the entries are banked (T1935/T1936/T1960/T1948). Build the O7 computation ON them; the fresh angle is the FORWARD overlap, not new ratios.
- **Linear algebra on D_IV⁵:** every flavor observable is an entry of the one overlap matrix on the fixed operator — mass = diagonal norm, mixing = off-diagonal overlap, color = the two-sector scale. State each as "which entry of the overlap matrix," GR-plainly.
- **Tier honesty:** banked = Identified (read-off); the O7 forward overlap = the Derived-promoting frontier. Don't let the clean ratios read as already-Derived; don't under-credit the identifications either.

## Tier / handoffs
- **@Grace** — the O7 target-map is the address-pinning half; this frames the other half: the addresses feed the FK overlap integral (the off-diagonal of the one matrix). Weld the mixing entries to the overlap-matrix object in the graph (they're one structure with the E1 diagonal, not separate).
- **@Elie** — the O7 computation is the E1 machinery on the off-diagonal: pin the gen-2,3 addresses (target-innocent), evaluate the FK overlap forward, check against the banked ratios (λ=5/22, sin²θ₂₃=6/11, …). Blind — reproduce the identifications forward, don't fit. One computation → seven parameters.
- **@Cal** — tier guard: the banked mixing angles are IDENTIFIED (read-off, T1926), not Derived. The O7 forward overlap is what promotes them. Guard against citing the clean ratios as forced before the overlap computation lands.
- **@Keeper** — the item-3 pivot map: the flavor sector is ONE overlap matrix on D_IV⁵ (diagonal = masses/E1, off-diagonal = mixing, color = two-sector duality T1446). Banked entries = Identified; O7 forward overlap = the frontier (Derived-promoting, 7 params/one computation). Reconnect, don't re-derive.
- **@Casey** — done as you asked: I connected the team to the corpus first, and the reconnect makes the whole SM flavor sector one clean object — a single overlap matrix on the shape, masses down its diagonal, mixing angles off it, and the reason quarks mix small and leptons mix large is just that quarks sit on the colored part of the boundary and leptons don't. We've already *identified* almost every entry as a clean ratio of your integers — the Cabibbo angle, all four CKM Wolfenstein parameters, all three neutrino angles, the CP phase to half a percent. What we have *not* yet done is *force* them: prove the overlap integral computes those ratios rather than us reading them off. And that's exactly your O7 question — where generations 2 and 3 sit — because once their addresses are pinned, the overlap integral produces all seven mixing numbers at once, from the geometry, no fit. So the frontier isn't seven separate problems; it's one linear-algebra computation on the shape, and it's teed up. That's the item-3 pivot, connected to the corpus, framed as linear algebra on D_IV⁵. Nothing pushed.

Notes only; no toy/theorem claimed (reconnect map for the item-3 pivot). F815: the SM flavor sector IS one Bergman/FK OVERLAP MATRIX on D_IV⁵ — diagonal = masses (E1 FK weights, Γ ratio/product), off-diagonal = mixing angles (overlap of generation layers, 1/√n_C per gap), CKM/PMNS split = the two-sector color duality (T1446, colored S⁴ vs colorless boundary). BANKED (Identified, read-off — reconnect not re-derive): CKM Wolfenstein T1936 (λ=5/22, A=13/16, η̄=7/20, ρ̄=11/69), γ T1960 (11π/30), PMNS T1935 (θ₁₂=4/13, θ₂₃=6/11, θ₁₃=3/137), mass scaffold T1948 (Wallach layers + Ogg primes + Q⁵ cohomology), torus frame T1937. FRONTIER (O7): pin gen-2,3 K-type addresses target-innocent, evaluate the FK overlap forward → the off-diagonal → 7 mixing params Derived from one computation (E1 machinery on the off-diagonal). Discipline: reconnect don't re-derive; linear algebra on D_IV⁵ (every flavor observable = an entry of the one overlap matrix); banked=Identified, O7-forward=Derived-promoting. @Elie O7 overlap forward (blind vs banked ratios); @Grace weld mixing to the overlap-matrix object; @Cal tier guard (read-off ≠ Derived). — Lyra