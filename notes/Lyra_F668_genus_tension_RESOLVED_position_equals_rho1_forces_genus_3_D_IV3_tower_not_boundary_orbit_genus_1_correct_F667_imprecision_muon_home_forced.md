# F668 — The genus pin RESOLVED (and it corrects an imprecision in my own F667). Elie's 2-vs-3 flag, pushed, revealed a real tension I'd glossed: **the muon home has two incompatible readings — genus 3 (D_IV³, tower embedding) vs genus 1 (the disk, the actual rank-1 Korányi–Wolf boundary ORBIT of D_IV⁵, since the Peirce-0 space of a minimal idempotent in the spin factor is dim 1).** F667 said "generations = boundary sub-domains" but *used* genus 3 — I conflated the tower embeddings with the boundary orbits, and they give **different genera** (3 vs 1) and **different c-function ratios** (c₅/c₃ vs c₅/c₁). **It resolves — target-innocently — via the derived position:** ρ₁(D_IV^n) = n/2 (Grace's sourced c-function theorem), and the muon position 3/2 (T2517, derived) **equals ρ₁(D_IV³) = 3/2**, NOT ρ₁(disk) = 1/2. **So the derived position FORCES the muon home to be genus 3 = D_IV³ (the tower reading), and RULES OUT the genus-1 boundary orbit** (which would require position 1/2). Elie's flag resolves toward genus 3, c₅/c₃ is the right ratio, and **F667 is corrected: the generations are the TOWER-EMBEDDING sub-domains {D_IV⁵ ⊃ D_IV³ ⊃ point}, NOT the Korányi–Wolf boundary orbits** (whose middle is a genus-1 disk, inconsistent with the position). The structural picture (nested sub-domains, mass = genus) stands — but on the tower reading, and the exact reading is now forced, not assumed.

**Lyra, Thu 2026-07-23. Pushed Elie's flag, found a real tension I'd glossed in F667, resolved it via the derived position. Correcting my own conflation. No faking.**

## The tension (Elie's flag, deepened — and it was real)
The muon's "home" has two readings that give DIFFERENT genera:
- **(B) Tower embedding** (Grace's reading): muon on **D_IV³** via the standard inclusion D_IV³ ↪ D_IV⁵. **Genus 3 = N_c.** → ratio c₅/c₃.
- **(i) Korányi–Wolf boundary ORBIT** (F86 strata, my F667 wording): the rank-1 boundary orbit of D_IV⁵. For the spin factor, the Peirce-0 space J₀(e) of a minimal idempotent is **dim 1** (the complementary idempotent) → the rank-1 orbit is a **DISK, genus 1.** → ratio c₅/c₁.
**These differ.** And **F667 conflated them** — I wrote "generations = boundary sub-domains" (orbit, i) but used genus 3 (tower, B). Own it: the two are not the same object, and the c-function ratio depends on which.

## The resolution (target-innocent — the position forces it)
The derived position decides, with no assumption:
$$ \rho_1(D_{IV}^n) = n/2 \quad\text{(Grace's c-function theorem, sourced)}. $$
| domain | ρ₁ = n/2 | matches muon position 3/2? |
|---|---|---|
| **D_IV³ (tower, genus 3)** | **3/2** | **YES** ✓ |
| disk D_IV¹ (boundary orbit, genus 1) | 1/2 | no ✗ |
**The muon position is 3/2 (T2517, derived) = ρ₁(D_IV³). So the muon home is FORCED to be genus 3 = D_IV³ (the tower reading). The genus-1 boundary orbit would require position 1/2 — inconsistent with the derived position, RULED OUT.** Target-innocent: the position (T2517) and the ρ₁ formula (Grace's sourced theorem) are both derived; their agreement forces genus 3. **Elie's 2-vs-3 flag (and my 3-vs-1 concern) resolve: genus 3, and c₅/c₃ is the right ratio.**

## Correction to F667
F667: "the three strata are the boundary sub-domains of D_IV⁵" — **imprecise.** The correct statement: **the three generations are the TOWER-EMBEDDING sub-domains {D_IV⁵ ⊃ D_IV³ ⊃ Shilov-point}** (nested by the standard inclusion, genera {n_C, N_c, 0}), NOT the Korányi–Wolf boundary orbits (whose rank-1 middle is a genus-1 disk). The distinction matters because it fixes the c-function ratio (c₅/c₃, not c₅/c₁). **The structural picture — generations as nested sub-domains, mass = genus, 3 = tower depth = rank+1 — stands, on the tower reading.** (And it dodges the F86-boundary-orbit dim-2/genus-1 confusion Elie flagged: the generations are the *tower levels*, whose genera are {5, 3, 0}, not the boundary orbits.)

## What this settles / what's left
- **SETTLED (target-innocent):** muon home = genus 3 = D_IV³ (tower), forced by position 3/2 = ρ₁(D_IV³). c₅/c₃ is the right ratio. F667 conflation corrected.
- **LEFT (unchanged):** my evaluation **c₅/c₃ = Γ(n_C)/π²?** (Grace's ratio, my eval) — the one numerical closure. And the book confirmation of ρ₁(D_IV^n) = n/2 (Grace's theorem already gives it, so this is nearly closed too).
- **The τ** is still the Shilov collapse (genus 0), residue not embedding — unaffected.

## Tiers / handoffs
- **@Elie** — your 2-vs-3 flag was load-bearing and I pushed it: there was a real genus 3 (tower) vs genus 1 (boundary orbit) fork, and my F667 conflated them. It **resolves toward genus 3**, target-innocently: position 3/2 = ρ₁(D_IV³) forces the tower reading; the genus-1 boundary orbit needs position 1/2, ruled out. So the muon home is D_IV³ (genus N_c=3), c₅/c₃ confirmed as the right ratio — but via the *tower embedding*, not the boundary orbit. Keep the invariants straight: tower genus 3 (c-function), fiber-dim 2 (F446 D̂), boundary-orbit genus 1 (NOT the muon home). Three distinct invariants; the muon uses the tower genus 3.
- **@Grace** — the pin resolves via YOUR sourced result: ρ₁(D_IV^n) = n/2, and the muon position 3/2 = ρ₁(D_IV³) forces genus 3 (rules out the genus-1 disk boundary orbit). So the generations are the tower-embedding sub-domains (reading B), and c₅/c₃ is the right ratio — target-innocent, no book-pin needed beyond your ρ₁ formula. My F667 "boundary sub-domains" corrected to "tower-embedding sub-domains." Hand me c₅/c₃ → I evaluate = Γ(n_C)/π².
- **@Keeper** — the generations-as-nested-sub-domains result is corrected and STRENGTHENED: it's the TOWER embeddings {D_IV⁵ ⊃ D_IV³ ⊃ point} (genera {n_C, N_c, 0}), forced by position = ρ₁ (target-innocent), NOT the boundary orbits (which give a genus-1 middle, inconsistent). My F667 conflated tower/orbit; corrected. Elie's genus flag resolves toward genus 3. The muon home is now *forced* (not "confirm with book") — one c₅/c₃ evaluation from derived. Log the correction: tower reading, genus 3 forced by ρ₁.
- **@Casey** — Elie flagged a subtlety and, pushing it, I found I'd been sloppy in my own consolidation: there were actually *two* ways to read "the muon lives on a smaller sub-domain," and they give different answers — genus 3 if it's the standard nested copy of D_IV³, but genus 1 (a disk) if it's the literal boundary face of D_IV⁵. I'd blurred them. It resolves the right way, and target-innocently: the muon's position is 3/2, and that number is exactly the leading weight of D_IV³ (each domain D_IV^n has leading weight n/2), whereas the disk's would be 1/2. So the derived position *forces* the genus-3 reading and rules out the disk — the geometry picks the answer, I don't. So the muon home is D_IV³, genus N_c = 3, and the c-function ratio we need is the right one — now forced rather than "to be confirmed with the book." The nested-sub-domains picture stands; I just had to be precise that it's the tower of copies, not the boundary faces. That's the audit catching my own imprecision at peak convergence — which is exactly when it should.

Notes only; no toys/theorems claimed. Genus pin RESOLVED: position 3/2 = ρ₁(D_IV³) forces muon home = genus 3 (tower), rules out genus-1 boundary orbit; F667 "boundary sub-domains" corrected to "tower-embedding sub-domains"; c₅/c₃ confirmed; one evaluation from derived. — Lyra
