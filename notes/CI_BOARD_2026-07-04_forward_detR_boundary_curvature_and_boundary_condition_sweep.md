Team turn — 2026-07-04. Forward det′(R) at the three strata + a boundary-condition sweep of the mass sector.

SOD check first. Casey's non-linear-boundary-curvature insight REOPENED the down-ladder and named a general principle: every eigenvalue problem terminates on a non-linear boundary, and that boundary term must be included. Read K658.

## Where we are (honest)

- **Down-ladder REOPENED — correctly.** F462's "definitively not forced" was an overreach; it killed only the color *charge* (blind) and the flat *count*. Casey named the third channel F460/F462 discarded: the **non-linear boundary curvature**. det(R+sI) = s^k·**det′(R)**·(1+O(s)); F460 used s^k and **dropped det′(R)**. det′(R) is read at each quark's **Korányi–Wolf stratum** → **generation-dependent → survives the ratio** (exactly the per-generation channel K655 required). Right mechanism, right place.
- **BUT 12 and 27 are REVERSE-READ.** They came from dividing the targets by 5/3 (20/(5/3)=12=rank·C₂; 45/(5/3)=27=N_c³). Target-aware, fit-suspect. Lyra flagged it herself and did NOT bank. **Count stays 8.**
- Partial forward support: 12=rank·C₂ has a Kähler-Einstein Ricci rationale; 27=N_c³ is F459 — but **why does 1→2 use rank·C₂ and 2→3 use N_c³? Unexplained.** The mechanism must *give* the difference.

## Primary — the forward det′(R) computation (Grace + Elie + Lyra)

> **Compute det′(R) — the product of nonzero boundary-curvature eigenvalues — at the three Korányi–Wolf strata (the three generations). Do 12 (step 1→2) and 27 (step 2→3) come out on their own?** Forward only — never insert 12/27 to hit 20/45. The "20/6.7 ≈ N_c" shortcut is the fish (Elie flagged); the det′(R) term must fall out of the full overlap.

> **THE sharp fish-detector (Keeper): ONE tensor, two strata — not two insertions.** det′(R) must be the SAME D_IV⁵ curvature tensor, *restricted* to each stratum (bulk dim n_C / Cartan slice dim rank / Shilov dim 0, F86) — one geometric object, evaluated at three shelves, yielding *whatever* it yields. The red flag: 12 = rank·C₂ and 27 = N_c³ are written as **two different algebraic forms**, which is what reverse-reading looks like (pick any integer that hits the target). A genuine derivation is **one det′ formula → 12 at the s-stratum and 27 at the b-stratum by restriction alone.** If the mechanism needs rank·C₂ *chosen* for one step and N_c³ *chosen* for the other, that is two insertions wearing a derivation's clothes — NOT forward. Demand the single restricted-determinant that produces both.

- **GRACE:** compute det′(R) at each stratum (bulk / Cartan slice / Shilov, F86). Does the s-stratum give 12, the b-stratum give 27? Is the different-factors structure geometric (which stratum forces rank·C₂ vs which forces N_c³)?
- **ELIE:** run the ζ-truncation / regularized-determinant numerics; forward-check that 12 and 27 emerge from det′(R), not from the target. Fish-detector armed on exactly the O(s)/det′(R) term.
- **LYRA:** the full overlap with the boundary term restored — F460 = bulk×linear-boundary was the *incomplete* reading; add the (1+O(s)) non-linear boundary curvature. Does the complete overlap produce 12 and 27 at the strata? Colorless-limit still must reduce to the muon (K657) on the *bulk* piece.

## Secondary — the boundary-condition SWEEP (Casey's broader directive: "improve our values by making sure any boundary conditions are properly included")

The general principle: **every eigenvalue problem terminates on a non-linear boundary, and dropping the boundary term undercounts.** F460 dropped it and came up short by exactly the boundary curvature. So **sweep the mass sector and every derived value for a dropped boundary term:**
- Which current MISS/APPROX/coarse rows (down-row rows 8,9; leptons; others) used a bulk-only or linear-boundary-only reading that **dropped the non-linear boundary contribution**?
- Re-derive each *with* the boundary term (det′(R) / the O(s) residue) properly included. Report which values improve and by how much — forward, not fitted.
- The "next-integer termination" (each eigenvalue terminates at an integer level; the fractional residue is discarded on the discrete side — proton k=6) is the quantization; check it's applied consistently.

## Per-CI (beyond the above)

- **KEEPER:** adjudicate every det′(R) result forward-vs-fit (K658 criteria); enforce the different-factors explanation; hold the sweep to forward-only; count 8 until 12 and 27 fall out on their own. SOD.
- **CAL:** post-landing cold-read — especially the reverse-read → forward transition (is the "forward" derivation genuinely target-innocent, or the reverse-read relabeled?).

## Disciplines armed

- **Forward-only: 12 and 27 are currently reverse-read; they must emerge from det′(R), never be inserted.** Target-innocence is the gate.
- **The different-factors question (rank·C₂ vs N_c³) is load-bearing** — a mechanism that fits both but explains neither's *selection* is not a derivation.
- Always linear (energy); single manifold D_IV⁵; the boundary term is the non-linear curvature, generation-dependent.
- Simplest one-line-checkable forms; cite-don't-claim; Five-Absence; pre-audit.

## EOD target

det′(R) computed forward at the three strata → 12 and 27 emerge on their own (bank the down-ladder) or honestly don't (stays a strong lead). The boundary-condition sweep identifies which other values dropped a boundary term and re-derives them with it included. **Then re-score the 26.** Count 8 until the forward det′(R) lands the numbers.

— Keeper, 2026-07-04. Casey's boundary curvature reopened the ladder correctly (det′(R), generation-dependent, right place); 12 and 27 reverse-read; the forward det′(R) at the three strata is the test, and the different-factors must be explained. Plus: sweep every value for a dropped boundary term. Forward-only, count 8.
