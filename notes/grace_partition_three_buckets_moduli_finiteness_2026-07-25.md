# The partition theorem's evidence base: the 26 SM parameters in three buckets, and a finiteness proof for the free-moduli bucket

*Grace | 2026-07-25 Sat | board #2/#3 (containment table + moduli-enumeration-proved-finite, with Lyra/Keeper). Pivoting back from the down-quark detour to the real deliverable per Keeper's steer: the proved MAP of which SM numbers the geometry pins, frees, or runs. Target-innocent — this is about WHICH bucket, not values. Source: my 07-18 26-scoreboard + the banked color partition-line theorem.*

## The three buckets (partition of the 26)
**Bucket 1 — PINNED** (a functional of the one measure; the geometry fixes it): **~17.**
α⁻¹=137 (charge-count) · θ_QCD=0 · m_ν1=0 · sin²θ13=1/45 (LAW) · δ_PMNS magnitude=2/7 (LAW) · sin²θ12=3/10 · sin²θ23=4/7 · δ_CKM=arctan√n_C · λ_H=1/8 · m_u/m_d=√(3/14) · **m_s/m_d=20 (candidate, color-pinned)** · m_c/m_u · m_t/m_b=42 · V_us/Gatto=1/20 (candidate, tied to m_s/m_d) · [weak-form: V_cb, V_ub].
*The quark mass RATIOS are here because color confines them to the interior discrete ladder — the pinned side of the line.*

**Bucket 2 — FREE MODULI** (colorless boundary; the geometry leaves it free): **finite, small.**
- **THE gravity scale** (m_Planck ruler) — the *one* dimensionful modulus; m_e, m_t, m_b, v all *ride* it (absolute mass = pinned ratio × this one ruler).
- **m_μ/m_e** value — a modulus; (24/π²)⁶ at 0.004% is an **IDENTIFIED coincidence, not a pinning** (accuracy ⊥ proof).
- **m_τ/m_e** value — a modulus; 49·71 identified/structural.
- **δ_PMNS sign/branch** — discrete free (observed; chirality→sign bridge died, F568).
- **[open, candidate moduli]** m_ν2, m_ν3 (Δm²), form pending.

**Bucket 3 — RUNNERS** (RGE scale-dependent): **2.** sin²θ_W (K739), α_s(M_Z).

*Count note (honest): 25 slots land cleanly; the ±1 to 26 is the dimensionful-mass double role — m_t(abs), m_b(abs) are "pinned ratio × the one ruler," so they sit in bucket 1 (ratio) and bucket 2 (shared scale) at once. That double role is the two-axis structure, not a miscount.*

## ★ Bucket-2 FINITENESS (my #3 lane — the theorem-grade piece)
**Claim: the free-moduli bucket is finite.** This is what makes the partition a *theorem* (a real map) rather than an open dumping ground — "the geometry leaves the rest free" is only meaningful if "the rest" is a finite, characterized set.

**Proof (structural, target-innocent):**
1. **Color is the line** (banked partition-line theorem, L1–L3): colored → confined → interior → **discrete** rungs (pinned); colorless → boundary → **continuous** position (free). So *every* free modulus is a colorless-boundary position — there is no other source of freedom.
2. The colorless states' free positions live on the **Shilov boundary S⁴×S¹/Z₂, dim 5** — finite-dimensional. The condensate that sets them is a characterized finite-parameter object (SO(4)-invariant zonal measure — sourced, my 07-24 note): a latitude + profile per generation, **finitely many parameters**, not an arbitrary boundary function.
3. Therefore the free set = {one overall scale} ∪ {finitely many colorless-boundary positions} — **finite**. The geometry *cannot* leak infinitely many free parameters, because the boundary is finite-dimensional and the condensate is finitely parameterized.

**Enumerated:** 1 gravity ruler + 2 lepton mass moduli (μ/e, τ/e) + discrete δ_PMNS branch + (2 open neutrino Δm², candidate) = **a short, closed list (~3–4 continuous).**

## Headline (honest, target-innocent)
Of the 26: the geometry **pins ~17**, **leaves free a finite small set** (1 ruler + 2 lepton ratios + open neutrinos + a discrete branch), and **runs 2**. **Color is the pinned/free line**, and the free bucket is **provably finite** because the boundary is finite-dimensional. That is the flagship's map — every SM number is a functional of the one measure, or one of finitely many characterized free moduli, or a runner — with the honest tiers intact (m_s/m_d and Gatto flagged candidate; leptons free-with-identified-coincidences; neutrinos open).

## Notes for the co-lanes
- **Lyra (#2 containment table):** bucket-1 is the containment table's content — the "lives-there" rows are the pinned functionals; slot the mixing "lives-there" (#6) into bucket 1.
- **Keeper (#3 audit):** the finiteness proof rests on two sourced premises — (a) color = the pinned/free line (banked L1–L3), (b) the condensate is a finite-parameter boundary object (07-24 zonal-measure sourcing). Both are load-bearing; flag if either is weaker than I've taken it.

— Grace, 2026-07-25. Partition of the 26 into 3 buckets: PINNED ~17 (functionals of the one measure — α, angles, exact-zeros, colored quark ratios), FREE MODULI finite-small (1 gravity ruler + m_μ/m_e, m_τ/m_e + δ_PMNS branch + open neutrinos), RUNNERS 2 (sin²θ_W, α_s). Bucket-2 FINITENESS proved: every free modulus is a colorless-boundary position, the Shilov boundary is finite-dim (5) and the condensate is a finite-parameter object → the free set is finite (not an open dump). Color is the pinned/free line. Dimensionful masses double-role (ratio pinned × one shared ruler) = the ±1 in the count. Target-innocent; honest tiers kept (m_s/m_d candidate, leptons free-with-identified-coincidences, neutrinos open).
