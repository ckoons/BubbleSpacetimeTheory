---
node_type: k_audit
id: K1181
title: (Elie's up-tower blind fire came back NEGATIVE (5060) — the up-masses are NOT FK-forced (they run on top-Yukawa SATURATION, y_t=1, corpus-confirmed); accepted straight as data. BUT the KEY REFRAME: the MIXING rides the wavefunction ADDRESSES, not the mass eigenVALUES — so the up-mass FK-failure does NOT automatically kill the mixing; it raises ONE precise structural question (are the up-WAVEFUNCTIONS still at the parity-forced even shelves {0,2,4}, or distorted by saturation?). + per Casey, GRACE GETS REAL MATH: the cross-shelf overlap MATRIX (linear algebra on D_IV⁵), the saturation-vs-address corpus trace, and the neutrino k=0/m₁=0 reconciliation. ★ THE NEGATIVE (Elie 5060, Cal §286 pre-registered it blind via a robust convexity argument — credit both): the up-tower FK ladder at the even shelves {0,2,4} gives 1:12:360 (or 1:30:1680 excl. k=0) — far too SHALLOW vs the observed top-heavy up sector ~1:588:79981 (off ~50× at charm/up). Cal's robustness: for ANY convex accelerating norm, a lower k-interval [0,4] grows LESS than a higher [1,5], so the even grid is NECESSARILY shallower — but up is observed STEEPER, so the even-grid FK assignment predicts the up hierarchy in the WRONG DIRECTION, independent of the exact norm. ⟹ THE UP-MASSES ARE NOT FK-FORCED. This SQUARES with the corpus: the up-sector is derived through TOP-YUKAWA SATURATION (y_t=1, the top hits the boundary ceiling), a DIFFERENT mechanism, precisely BECAUSE it's top-heavy (#43/#53, the up "cold anomaly"). So only the DOWN + CHARGED-LEPTON ladders are FK-forced (odd shelf {1,3,5} → 1:20:840); the hoped-for "both ladders forced" did NOT happen. Accepted straight — a real negative, on the board as data, not buried. Four turns running the discipline mattered more than the result; this turn it caught a real one. ★ THE PARITY GRID STILL STANDS as a FORCED prediction (Lyra's normalization, ratified): charge×3 = the weight/address (down 1/3→1, up 2/3→2, e→3, ν→0); the boundary fold forces degree+weight same-parity → DOWN+ELECTRON on ODD {1,3,5}, UP+NEUTRINO on EVEN {0,2,4}. That down/lepton-odd, up/neutrino-even SPLIT is real and forced; only the claim "the even FK ladder gives the up-masses" failed. ★ THE KEY REFRAME (the load-bearing insight — linear algebra on D_IV⁵): CKM = U_up†·U_down = the OVERLAP of the up mass-eigenvectors with the down mass-eigenvectors = ⟨up-wavefunction | down-wavefunction⟩. The mixing depends on the WAVEFUNCTIONS (which shelves the modes live on), NOT on the mass eigenVALUES. The mass VALUES set the DIAGONAL (FK norms for down/lepton; saturation for up); the MIXING = the OFF-DIAGONAL cross-shelf overlaps. So the up-mass FK-failure kills "up-masses = FK-forced," NOT necessarily "the mixing is computable." IF the up-WAVEFUNCTIONS still live at the parity-forced even shelves {0,2,4} (saturation acting on the mass-NORM, leaving the address intact), THEN the mixing = the cross-shelf overlap ⟨{0,2,4}_up | {1,3,5}_down⟩, computable from the FORCED addresses, INDEPENDENT of how the up-masses are set. ★ THE ONE PRECISE OPEN QUESTION (resolves Cal's "built on sand" dilemma): does top-Yukawa saturation act on the mass-NORM (→ up-wavefunctions stay at {0,2,4} → mixing rides the clean addresses → the fire proceeds) or DISTORT the wavefunction (→ up-modes pushed off the shelf → the mixing needs the distorted wavefunctions)? This is the structural call before the mixing fire. ★ GRACE'S MATH (Casey's directive — real work beyond the ledger, all linear algebra on D_IV⁵ / corpus-connected): (G1) THE CROSS-SHELF OVERLAP MATRIX — characterize the overlap structure ⟨odd {1,3,5} | even {0,2,4}⟩ on D_IV⁵: the parity SELECTION RULES (which degree-pairs have nonzero overlap under the boundary fold), the resulting CKM/PMNS matrix SKELETON as a cross-shelf overlap. Build the linear-algebra structure of the mixing (the selection rules + the nonzero-entry pattern) so Elie's numerical fire drops into a known skeleton. This is the mixing's linear algebra on D_IV⁵. (G2) THE SATURATION-VS-ADDRESS TRACE — trace through the corpus up-sector work (#43/#53, the y_t=1 saturation, the "cold anomaly," the two-competing-addresses flags): does saturation act on the mass-NORM (address intact at {0,2,4}) or DISTORT the wavefunction? This decides whether G1's overlap rides the clean addresses. Corpus-connected. (G3) THE NEUTRINO k=0/m₁=0 RECONCILIATION — F93 had neutrinos ν-varying {5/2,3/2,0}; the parity grid has them even-degree {0,2,4}; both put the massless one at "zero" (k=0 / ν=0 / F619). Are these ONE structure re-read or TWO parametrizations? Reconcile (Grace + Lyra). ★ THE CORPUS + LINEAR-ALGEBRA FRAME (Casey's standing directive): the whole SM flavor sector = ONE overlap matrix on D_IV⁵ — the DIAGONAL is the masses (FK norms for the odd/down-lepton ladder [Derived]; SATURATION for the up-tower [corpus mechanism, not FK]), the OFF-DIAGONAL is the mixing (the cross-shelf overlaps, G1). The up-mass negative REFINES this (up-diagonal = saturation, not FK) but does NOT break the one-matrix frame — it's still one matrix, read on the diagonal (masses, two mechanisms) and off it (mixing, cross-shelf overlaps). ★ NOTHING BANKS: down+charged-lepton masses Derived (FK odd ladder); the PARITY GRID is a forced prediction (down/lepton odd, up/neutrino even); up-masses NOT FK (saturation); the 7 MIXING params stay IDENTIFIED until (a) the saturation-vs-address question (G2) resolves the up-wavefunction shelves, (b) Grace's cross-shelf overlap skeleton (G1) is built, (c) Elie's off-diagonal blind fire reproduces the 7 under the K1177 count. ★ SEQUENCE (updated): (1) @GRACE G2 (saturation-vs-address, corpus) + @LYRA the structural call — do the up-wavefunctions stay at {0,2,4}?; (2) @GRACE G1 (the cross-shelf overlap matrix skeleton, linear algebra on D_IV⁵); (3) IF up-wavefunctions confirmed at {0,2,4} → @ELIE the off-diagonal cross-shelf blind fire → 7 params; (4) @CAL independent, @KEEPER counts. Plus @GRACE G3 (neutrino reconciliation) in parallel. ★ CALIBRATION: NOT over-braking — the parity grid is a genuine forced prediction, the down/lepton ladder is Derived, and the mixing may well still proceed (it rides addresses, not the failed up-masses); NOT waving through — the up-masses are NOT FK-forced (real negative), and whether the up-wavefunctions survive saturation at {0,2,4} is an open structural call. Nothing pushed; externals at Casey's trigger.
date: 2026-08-05
author: Keeper
verdict: Elie's up-tower blind fire NEGATIVE (5060): even-grid FK {0,2,4} = 1:12:360, far too shallow vs observed top-heavy up ~1:588:79981; Cal §286 pre-registered it via robust convexity (lower k-interval necessarily shallower, up is steeper → wrong direction for any convex norm). Up-masses NOT FK-forced — they run on top-Yukawa SATURATION (y_t=1, corpus #43/#53), a different mechanism because top-heavy. Only down+charged-lepton FK-forced (odd {1,3,5}→1:20:840). Accepted straight as data. KEY REFRAME (linear algebra on D_IV⁵): CKM = ⟨up-wavefunction|down-wavefunction⟩ overlap — rides the ADDRESSES (shelves), NOT the mass eigenvalues; masses set the diagonal, mixing = off-diagonal cross-shelf overlaps. So the up-mass FK-failure does NOT auto-kill the mixing; the one open question = does saturation act on the mass-NORM (up-wavefunctions stay at {0,2,4} → mixing rides clean addresses) or DISTORT the wavefunction (Cal's "built on sand"). Parity grid STANDS as a forced prediction (down/e odd {1,3,5}, up/ν even {0,2,4}). GRACE'S MATH (Casey's directive): G1 the cross-shelf overlap MATRIX skeleton ⟨{1,3,5}|{0,2,4}⟩ (parity selection rules, CKM/PMNS structure — linear algebra on D_IV⁵); G2 the saturation-vs-address corpus trace (#43/#53, does y_t=1 change norm or wavefunction?); G3 the neutrino k=0/m₁=0 reconciliation (F93 {5/2,3/2,0} vs even {0,2,4}, F619). One-matrix frame holds (diagonal = masses [FK for down/lepton, saturation for up]; off-diagonal = mixing cross-shelf overlaps). NOTHING BANKS: down+lepton Derived, parity grid forced, up-masses not FK, 7 mixing Identified until G2 resolves addresses + G1 skeleton built + off-diagonal fire reproduces. Sequence: Grace G2 + Lyra structural call → Grace G1 → (if addresses confirmed) Elie off-diagonal fire → Cal independent, Keeper counts; Grace G3 parallel. Calibrated: not over-braking (parity forced, down Derived, mixing rides addresses), not waving through (up-masses not FK, wavefunction-survival open). Nothing pushed.
---

# K1181 — up-masses not FK (saturation); the mixing rides addresses not masses; Grace gets the cross-shelf overlap math

## The negative (accepted straight — Elie 5060, Cal §286 pre-registered it)
The up-tower FK ladder at the even shelves {0,2,4} gives 1:12:360 — far too shallow vs the observed top-heavy up sector (~1:588:79981). Cal's robustness: for *any* convex norm, the lower interval [0,4] grows less than [1,5], so the even grid is necessarily shallower — but up is *steeper*, so the even-grid FK assignment predicts the up hierarchy in the **wrong direction**. ⟹ **The up-masses are not FK-forced** — they run on **top-Yukawa saturation** (y_t=1, corpus #43/#53), precisely *because* the up-sector is top-heavy. Only down + charged-lepton ladders are FK-forced. A real negative, on the board as data.

## The parity grid still stands (forced prediction)
charge×3 = the address; the boundary fold forces degree+weight same-parity → **down+electron on odd {1,3,5}, up+neutrino on even {0,2,4}.** That split is forced; only "the even FK ladder gives the up-masses" failed.

## The key reframe (linear algebra on D_IV⁵)
**CKM = U_up†·U_down = ⟨up-wavefunction | down-wavefunction⟩** — the mixing depends on the **wavefunctions (which shelves the modes live on), not the mass eigenvalues.** The masses set the diagonal; the mixing = the off-diagonal **cross-shelf overlaps.** So the up-mass FK-failure kills "up-masses = FK-forced," **not** necessarily "the mixing is computable." **If the up-wavefunctions still live at the parity-forced {0,2,4}** (saturation acting on the norm, address intact), **the mixing = ⟨{0,2,4}_up | {1,3,5}_down⟩, computable from the forced addresses.**

## The one precise open question
Does saturation act on the mass-**norm** (→ up-wavefunctions stay at {0,2,4} → mixing rides clean addresses → fire proceeds) or **distort the wavefunction** (→ Cal's "built on sand")? The structural call before the mixing fire.

## ★ Grace's math (Casey's directive — real work, linear algebra on D_IV⁵ / corpus)
- **G1 — the cross-shelf overlap matrix.** Characterize ⟨odd {1,3,5} | even {0,2,4}⟩ on D_IV⁵: the parity **selection rules** (which degree-pairs overlap under the boundary fold), the CKM/PMNS **skeleton** as a cross-shelf overlap. Build the mixing's linear-algebra structure so Elie's numerical fire drops into a known skeleton.
- **G2 — the saturation-vs-address trace.** Trace the corpus up-sector (#43/#53, y_t=1, the "cold anomaly," two-competing-addresses): does saturation change the mass-**norm** (address intact) or **distort the wavefunction**? Decides whether G1 rides the clean addresses.
- **G3 — the neutrino k=0/m₁=0 reconciliation.** F93 (ν-varying {5/2,3/2,0}) vs the parity grid (even {0,2,4}); both put the massless one at "zero" (F619). One structure re-read or two parametrizations? (Grace + Lyra.)

## The frame holds (corpus + linear algebra)
The whole SM flavor sector = **one overlap matrix on D_IV⁵** — diagonal = masses (FK norms for the odd down/lepton ladder [Derived]; **saturation** for the up-tower [corpus mechanism]); off-diagonal = mixing (cross-shelf overlaps, G1). The up-mass negative **refines** this (up-diagonal = saturation, not FK) — it doesn't break the one-matrix frame.

## Nothing banks
Down + charged-lepton masses **Derived**; the parity grid is a **forced prediction**; up-masses **not FK** (saturation); the 7 mixing params stay **Identified** until G2 resolves the up-wavefunction shelves, G1's skeleton is built, and Elie's off-diagonal fire reproduces the 7 under the K1177 count.

## Sequence
(1) @Grace G2 + @Lyra the structural call (up-wavefunctions at {0,2,4}?). (2) @Grace G1 (the cross-shelf overlap skeleton). (3) If confirmed → @Elie the off-diagonal cross-shelf blind fire → 7 params. (4) @Cal independent, @Keeper counts. @Grace G3 in parallel.

## Calibration
Not over-braking — parity grid forced, down/lepton ladder Derived, the mixing may still proceed (rides addresses). Not waving through — up-masses not FK (real negative), wavefunction-survival open.

— K1181, Keeper, 2026-08-05. Up-tower FK negative (5060: {0,2,4}=1:12:360 vs top-heavy up; Cal §286 robust convexity). Up-masses not FK — saturation (y_t=1, corpus #43/#53). Parity grid stands (down/e odd, up/ν even). REFRAME: mixing = ⟨up-wf|down-wf⟩ overlap, rides addresses not masses → FK-mass-failure doesn't auto-kill mixing; open question = does saturation keep up-wavefunctions at {0,2,4} or distort them. Grace's math: G1 cross-shelf overlap matrix (linear algebra on D_IV⁵), G2 saturation-vs-address corpus trace, G3 neutrino k=0/m₁=0 reconciliation. One-matrix frame holds. Nothing banks. See K1180, Elie 5060, Cal §286, Lyra normalization, #43/#53, F93/F619.
