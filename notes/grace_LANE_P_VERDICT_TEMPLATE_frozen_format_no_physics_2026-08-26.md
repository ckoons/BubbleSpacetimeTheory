---
node_type: verdict_template
title: "LANE P VERDICT TEMPLATE — format frozen BEFORE the gate opens, so nothing drifts at fire time. Contains NO physics; every numbered slot is empty until the shot."
author: "Grace (merge owner)"
date: 2026-08-26
status: "Template. The fire fills it top-to-bottom in order; no section may be written out of order; the verdict line is written LAST."
---

# LANE P — THE SHOT (to be filled in this order, timestamps per line)
1. **CONTROLS (printed first, before any live value):** W2a must-catch (hand-built exp family) PASS/FAIL · W2a must-reject (the 5454 flat-norm family) FIRES/SILENT — *a silent control = no verdict.*
2. **W2a capability** on the unconstrained family: CAPABLE/NOT (if NOT: the lane FLOORS here, remaining sections struck through, not deleted).
3. **W3 — the forcing condition, quoted verbatim from Lyra's co-sign**, + the COUNT: [n_forced independent quantities] vs [2 targets]. *Any fitted τ ⟹ the SUGGESTIVE cap engages here and prints in the verdict.*
4. **THE CONSTRAINED MECHANISM'S VALUES (before any comparison):** m_μ/m_e = [ ] · m_τ/m_μ = [ ] · R = [ ] — exact arithmetic, then decimal.
5. **W4 under the frozen bands:** R vs [WIN 1.8796–1.8985 · SUGG (1.7946,1.8796)∪(1.8985,1.9835) · FAIL outside] · per-ratio dev% vs the 0.5%/5% ladder · the m_τ band (±0.09 MeV) carried.
6. **THE MUST-DIFFER PAIR:** (i) single-T Boltzmann limit claimed physical? YES = cap · (ii) R > 1.7946? (the bend direction).
7. **5487 MUST-FAIL RUN (Elie):** the machinery pointed at the down sector — reproduces 1:20:840? YES = cap, printed.
8. **FAMILY-SWEEP PRICING:** where does n_C = 5 enter the form, or the absence priced.
9. **CAN-FAIL ACCOUNTING:** k-of-N-of-which-m-can-fail, both directions.
10. **VERDICT (LAST LINE, one word + caps engaged):** WIN / SUGGESTIVE(caps named) / FLOOR(obstacle named) / FAIL.

*No section reordered, none written before its predecessors, the template itself immutable from gate-open. — the merge owner*
