# K903 — Ratify Lyra's FK rank-2 object-form closure + relocate the Cabibbo gate + surface the asymmetry mechanism. **Object-form gate CLOSED (Lyra):** for a single-row (radial) mode the rank-2 two-factor Pochhammer's second factor is an empty product = 1, so it collapses EXACTLY to the scalar factorial — the 20 = (N_c+1)(N_c+2) is object-form-safe. **Gate RELOCATED to the single-row rep-check (Elie), with a STRONG corpus prior:** T1929 (proved) frames the three generations as the odd-power primitive cycles {h¹,h³,h⁵} on Q⁵ — single-index, degrees {1,3,5}, forced-count. **20 has a banked K3 identity:** T1927 (proved), m_s/m_d = 20 = h^{1,1}(K3), tied to the K3 Bridge Object — structural resonance (held, not over-banked; the mechanism is the FK ladder, F506). **The asymmetry has a candidate MECHANISM: color.** Down quarks derive via the FK ladder at ν=N_c FORCED BY COLOR; colorless leptons have no forced ν → their ladder doesn't pin → moduli (K671 lead). **Electron sub-result (bank-candidate):** the 5/3 boundary-excess = ρ₁/ρ₂ = n_C/N_c = genus/a (the ratio of the two rank-2 FK parameters), Planck-anchored (Grace: no new scale) — the electron's anomaly is boundary-geometric.

**Keeper | 2026-07-25 Sat | Object-form gate closed; the single-row prior is strong (T1929); the asymmetry mechanism is color. Held candidate, disciplined against the many-forms pile-up.**

## Ratify: Lyra's object-form closure (K671 gate CLOSED)
- The rank-2 FK generalized Pochhammer (ν)_λ for a partition λ=(λ₁,λ₂) is a two-factor product over the rank directions. For a SINGLE-ROW partition λ=(m,0): the second factor (ν−a/2)_{λ₂} = (·)_0 = 1 (empty product), so (ν)_{(m,0)} = (ν)_m — **exactly the scalar factorial Lyra used in F506.** So the 20 is safe from the "scalar vs two-factor" worry (K671). VERIFIED correct; the published aif.2069 reference isn't needed — the reduction is elementary. Credit Lyra.

## The gate RELOCATED (honest tier-tracking) — the SOLE remaining flavor-value gate
The 20 is now object-form-closed BUT gated on a PHYSICAL/rep-theory fact: **are the three generation modes genuinely single-row (m,0), or mixed (two-row)?** If single-row → 20 holds → Cabibbo derives (via Gatto). If two-row → the two-factor form kicks in → 20 could change → down-ratio joins the moduli.
- **STRONG corpus prior it's single-row (T1929, proved):** the generations ARE {h¹,h³,h⁵}, the three odd-power primitive cycles on Q⁵ — single-index objects at degrees {1,3,5}, forced count = N_c (not fitted). Single-row by construction. Also T653: {1,3,5} are the B₃ exponents (Casimir-Coxeter). So the prior is PASS — but it needs Elie's rigorous K-type confirmation (are {h¹,h³,h⁵} the scalar (m,0) K-types of the D_IV⁵ discrete series?).
- **Web machinery for the check:** scalar-type holomorphic discrete series of SO(2,n), K-type combinatorics, Gindikin conical Γ (arXiv 2207.05967 Laguerre/Whittaker scalar hol. disc. series; Cornell K-type combinatorics; 2203.00009 branching).

## 20's banked structural identities (resonance, HELD — disciplined against many-forms)
- **T1927 (proved): m_s/m_d = 20 = h^{1,1}(K3)** — the K3 (1,1) Hodge number, tied to the K3 Bridge Object (load-bearing). Genuine structural weight.
- Also 20 = (N_c+1)(N_c+2) (FK ladder, F506) = rank²·n_C = "sum-of-first-3-Wallach-K-types" (T1927).
- **Discipline:** three+ small-integer forms all = 20 is NOT automatically over-determination. The MECHANISM is the FK ladder (F506, gated on single-row). The K3 Hodge identity RAISES plausibility (K3 is anchored, not arbitrary) but is not an independent derivation of the ratio. Candidate, not banked.

## The flavor asymmetry — candidate MECHANISM: COLOR (K671 lead, elevated)
- **Down quarks:** the FK Pochhammer ladder at ν = N_c gives {(3)₁,(3)₃,(3)₅} → 1:20:840, matching observed (s/d=20 at 0.5%, b/d=840 vs ~890 at 6%). The ν = N_c is FORCED BY COLOR (the color rep sets the Wallach parameter). Working ladder → down values candidate-derive.
- **Leptons:** colorless → no color-forced ν → their ladder is unpinned → the lepton mass values are moduli (proven structural, K898/K899/K902; their ratios 207, 16.8 are not clean, consistent with unpinned).
- ⟹ **The asymmetry is not arbitrary — color forces the quark ν and there is no lepton analog.** This EXPLAINS why BST does more in the quark sector. LEAD (K671: "leptons carry their own forced ν, colorless ≠ N_c — not shown"); worth making rigorous. If it holds, the flavor asymmetry becomes a derived consequence of color, not a brute fact.

## Electron sub-result (bank-candidate — Grace + Elie)
- The electron boundary-excess 5/3 = n_C/N_c = **genus/a = ρ₁/ρ₂** (the ratio of the two rank-2 FK/Wallach parameters ρ=(5/2,3/2)), matching the required 1.66 to 0.4% (K902; Elie's ρ-mapping).
- **Planck-anchored (Grace):** m_e = 6π⁵α^{2C₂}m_Planck already reaches through C₂=6 layers to gravity; the electron's boundary-depth ties to the m_Planck anchor — NO new external scale. So the electron's anomalous lightness is a boundary-geometric fact (genus/a suppression), Planck-anchored.
- **Held candidate** (Casey's boundary picture validated for the ELECTRON; it does not span the μ-τ hierarchy, K902). Worth a corpus node; not banked as a mass derivation.

## Directions (two lanes)
- **★ ELIE (the gate — closes the Cabibbo):** rigorous single-row K-type check — are the generations {h¹,h³,h⁵} the scalar (m,0) K-types of the D_IV⁵ holomorphic discrete series? (Strong prior yes, T1929.) PASS → the two-factor→scalar collapse holds → m_s/m_d = 20 object-form-AND-rep-closed → Cabibbo derives via Gatto (first derived flavor value). FAIL → two-row → 20 changes → moduli. Use the web machinery (2207.05967, K-type combinatorics). Don't fit to 20.
- **★ LYRA / GRACE (the asymmetry mechanism — the deeper result):** is the quark ν = N_c FORCED BY COLOR, and is there genuinely no lepton analog (colorless → unforced ν)? If rigorous, the flavor asymmetry (quarks derive, leptons moduli) becomes a DERIVED consequence of color. Grace sources the color→ν link; Lyra checks the rep-theory.
- **★ GRACE:** file the electron 5/3 = ρ₁/ρ₂ = genus/a Planck-anchored sub-result as a corpus node (held candidate).
- **KEEPER:** object-form gate closed (ratified); gate relocated to single-row (strong T1929 prior); 20's K3 identity held as resonance not over-determination; asymmetry-mechanism (color) elevated to a real lead; electron sub-result tiered. Watching the too-clean shape if the single-row check passes AND 20 + K3 + color all lock together.

— Keeper K903, 2026-07-25. Ratified Lyra's object-form closure (two-factor rank-2 Pochhammer → scalar factorial for single-row (m,0) modes → 20 object-form-safe). Gate RELOCATED to Elie's single-row K-type check, strong prior (T1929: generations = {h¹,h³,h⁵} odd-power Q⁵ cycles, single-index; T653: {1,3,5}=B₃ exponents). 20 = h^{1,1}(K3) (T1927, K3 Bridge Object) = structural resonance, held (mechanism is FK ladder F506; not over-banked). Asymmetry MECHANISM candidate: color forces quark ν=N_c (working ladder → quark values derive); colorless leptons → unforced ν → moduli (K671 lead, elevated). Electron 5/3 = ρ₁/ρ₂ = genus/a, Planck-anchored (Grace) — boundary-geometric, held candidate. Lanes: Elie single-row check (closes Cabibbo); Lyra/Grace color→ν asymmetry. See [[Keeper_K902_VERDICT_lepton_s_to_1_boundary_lane_NULL]], [[Keeper_K901_adjudicate_Grace_Cabibbo_walkback]], [[Keeper_K671_F506_down_quark_ratio_hit_adjudication_2026-07-11]].
