# K1511 — BLIND pre-registration: does BST's DERIVED matter content select the 3+1 descent (a V₅ direction)? (committed before the density matrix is computed)

**Keeper, 2026-08-14, before the number.** Elie 5257 (K1510) is a theorem: no SO(5)-equivariant construction from SO(5)-invariant inputs can select a direction in V₅ (the only fixed vector in the vector rep is 0). So the 3+1 descent is not derivable from the geometry alone — it needs an ingredient from **outside** the isotropy K = SO(5)×SO(2). The leading candidate is the committed **matter/record**, which is the one non-equivariant thing BST has. This pre-registration sets the test for that candidate — target-innocent, can-fail, with the circularity guard committed **before** anyone computes, because the last two frontier mechanisms (K1507, §492) were circular and I want this one to be honest by construction.

## The object under test
- **State:** the **one-particle occupied density matrix ρ** of BST's **derived** matter content — the actual *filled* fermion modes (SM fermions at their K-type addresses). **NOT the bare vacuum** (trivial K-type → SO(5)-invariant → V₅-component = 0 by construction; testing the vacuum is empty, it's isotropic by definition — that is Elie's sanity-null, not the test).
- **The direction operator:** the SO(5)-**equivariant** projection of ρ onto the **V₅-isotypic component** of End(H) — i.e. the "dipole moment" of the occupied configuration in the SO(5) vector rep. Call its norm ‖ρ_{V₅}‖ and its axis n̂_ρ.
- **The named axis (blind, from the descent):** V₅ = the n_C-axis fixed by SO(5)→SO(4) (Casey #14 chirality axis), named in the corpus **before** ρ is computed (K1504/K1507 named-axis rule).

## ★ The circularity guard (committed FIRST, because it is the whole point)
The occupied configuration ρ **must be DERIVED independently** — the filled modes come from the fermion spectrum / the K-type addresses / the neutrino spine / the ground-state filling **as the corpus already fixes them**, NOT chosen to produce V₅. **If any mode is occupied "to make the direction come out," the test is void** — that is the K1507/§492 insertion trap. The occupation list must be citable from prior corpus artifacts, dated before this note, with no free choice exercised at test time.

## The candidate mechanism the test EVALUATES (does not assume)
BST already carries a **derived** non-equivariant feature for independent reasons: the **chiral asymmetry** — the Five-Absence forbids ν_R (no right-handed neutrino). A chiral asymmetry distinguishes handedness, which is a direction-selecting structure. **IF** ρ_{V₅} is nonzero and traces to this chiral asymmetry, that is a genuine *non-circular* selection — the chirality was forced by Five-Absence long before this question, not inserted for it. **The test computes whether this happens; it is a candidate to be measured, not a conclusion.** (Honest scope check to run: is the ν_R-absence chirality an *outside-K* ingredient, or is it SO(5)-equivariant after all? If equivariant, the theorem forbids it selecting V₅ too, and the answer is U.)

## Outcomes, committed blind
- **P — content selects the descent.** ‖ρ_{V₅}‖ nonzero and robust, axis n̂_ρ **aligned with V₅** (overlap above an isotropic-filling null; borrow the K1504 alignment discipline — the null is chance-overlap 1/n_C, not zero). **Ruling:** the 3+1 split is **content-derived (Machian)** — "the breaking is the content" literal. Tier **Identified**; **Derived only if the occupation itself is forced** (the filled modes are compelled by the corpus, not merely consistent) AND the V₅-selection traces to a named derived asymmetry (e.g. the chiral ν_R-absence). No promotion past Identified from the direction alone.
- **U — content is isotropic.** ‖ρ_{V₅}‖ = 0 (within an N-matched null): the derived occupied config fills complete SO(5) reps (a full generation → isotropic), selects nothing. **Ruling:** the descent **stays a structural posit** (GR-like), honestly labeled. A real, pre-committed answer — not a failure. This is a live and plausible outcome (a complete generation may well fill a rep).
- **P' — selects a different axis.** ‖ρ_{V₅}‖ nonzero but n̂_ρ ≠ V₅. **Ruling:** matter picks *a* frame but not the corpus-predicted one — investigate, do not relabel V₅ to match.
- **N — no clean readout** (occupation ambiguous, ρ ill-defined). Bigger/sharper spectrum, not a bigger claim.

## Confounds committed (per feedback_preregistration_protects_interpretation_not_instrument)
1. **Sanity null:** an SO(5)-complete filled shell MUST give ‖ρ_{V₅}‖ = 0. Run FIRST, post blind. If a complete shell reads nonzero, the projector is mis-built (the equivariance-trap analog) and every P is void.
2. **Occupation is derived, not tuned** (the circularity guard above) — the filled-mode list is citable and pre-dated.
3. **Alignment null is chance-overlap 1/n_C, not zero** (K1504/K1510) — "aligned" must beat the isotropic-filling percentile, N-matched.
4. **Vary the should-not-matter knobs:** basis/gauge of H, ordering of modes — ‖ρ_{V₅}‖ and n̂_ρ must be invariant to them (they are physical if the projector is genuinely SO(5)-equivariant).

## Tier ceiling
- Elie 5257 (the no-go): **theorem, settled** — the descent is not geometry-forced.
- This test on P: **Identified**; **Derived** needs (forced occupation) AND (V₅-selection traced to a named derived asymmetry). 
- Nothing external; nothing pushed. This sets the frontier's pass/fail before Lyra/Elie compute ρ.

— Keeper, K1511, 2026-08-14 12:04 EDT. Blind pre-registration. ρ uncomputed; occupation list owed by Lyra (citable, pre-dated) before Elie projects onto V₅.
