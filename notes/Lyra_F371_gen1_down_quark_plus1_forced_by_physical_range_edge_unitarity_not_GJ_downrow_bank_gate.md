---
title: "F371 (Sunday count-mover, Cal's bank gate) — forcing the gen-1 down-quark exponent +1 WITHOUT leaning on GJ, via path (i): the physical-range edge / unitarity. The generations span ν∈[0,5/2] (gen-1 at the TOP edge ν=5/2, the BF bound); near gen-1 d(ν)=(5/2−ν)·g(ν) with g(5/2)=+9/16>0, so sign(d)=sign(5/2−ν); the physical states are ν<5/2 (d>0, within the generation range), so gen-1 is approached from within → sign(d)=+1; the RESIDUE reading (−1)=sign(d'(5/2)) corresponds to ν>5/2 (d<0), OUTSIDE the physical range (no generations there, non-normalizable). So w(gen-1)=+1 is FORCED by the physical range / unitarity (the residue side ν>5/2 is non-physical), target-innocent (no GJ). FOR CAL'S COLD-READ — do NOT self-bank. CAL'S GATE (his down-row cold-read): 4/5 down-row pieces forced (base N_c via #418 det-tensor; power structure; gen-2 s exponent −1 via sign(d(3/2))=−15/16<0; gen-3 b exponent 0 via color singlet); the 5th — gen-1 d exponent +1 — NOT forced (BF-zero ambiguity: residue −1 vs physical-side +1; +1 completes GJ = target-aware). Cal self-corrected his own #425 ('+1 forced' too strong); my F354 'forced-ness open' was right. Cal's three paths to force it WITHOUT GJ: (i) physical-side convention rigorously forced over residue; (ii) so(3) generation triplet forced independently; (iii) color-neutrality Σw=0. THIS NOTE = path (i), strengthened. THE PATH (i) FORCING (target-innocent): the weight w=sign(d) (Grace T2499 color-charge mechanism). Near gen-1, d(ν)=(5/2−ν)·g(ν), g(ν)=(1−ν)(2−ν)(3−ν)(4−ν), g(5/2)=(−3/2)(−1/2)(1/2)(3/2)=+9/16>0. So sign(d)=sign(5/2−ν) near gen-1. The three generations are at ν=5/2,3/2,0 — the PHYSICAL RANGE is ν∈[0,5/2], gen-1 at the TOP edge (the BF bound). Near gen-1, d>0 for ν<5/2 (verified: d(2.25)>0) and d<0 for ν>5/2 (d'(5/2)=−9/16<0). gen-1, being physical, is the limit from WITHIN the range (ν<5/2) → sign(d)=sign(5/2−ν)=+1. The residue reading (−1) is the ν>5/2 side, which is OUTSIDE the generation range and non-normalizable (d<0) — it contains no physical states, so it cannot be the gen-1 weight. THEREFORE w(gen-1)=+1, forced by the physical range / unitarity, NOT by GJ. This STRENGTHENS F354/F355 (which Cal read as a 'convention choice'): the forcing isn't merely positivity — it's that the physical states only exist for ν≤5/2, so the gen-1 weight is necessarily the within-range (ν<5/2) limit; the residue (ν>5/2) side has no physical states. PATH (iii) CONSISTENCY CHECK (NOT an independent forcing — honest): Σw = +1−1+0 = 0 holds, but it is NOT independently forced (the three generations are NOT a single SU(N_c) multiplet, so traceless-ness doesn't apply; Σw=0 is a CONSEQUENCE of the alternating crossing parities, which already include gen-1's +1). So path (iii) is a consistency check, not a second independent forcing — I do not lean on it. HONEST TIER: path (i) is a strengthened target-innocent forcing of gen-1=+1 (the physical-range edge / unitarity; the residue side ν>5/2 is non-physical) — offered FOR CAL'S COLD-READ as the bank gate; I do NOT self-bank the down-row (Cal tiers, per his #425 self-correction + #411). If Cal accepts path (i): down-row +3 banks → count 8. Path (iii) Σw=0 is consistency only (not independent). Count HOLDS 5/26 pending Cal. For Cal, Grace, Elie, Casey, Keeper."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-28 Sunday (date-verified)"
status: "v0.1 — gen-1 down-quark +1 forcing (Cal's bank gate, path i). w=sign(d) (T2499). Near gen-1: d(ν)=(5/2−ν)·g(ν), g(5/2)=+9/16>0 → sign(d)=sign(5/2−ν). Generations span ν∈[0,5/2], gen-1 at TOP edge (BF bound). d>0 for ν<5/2 (physical, within range), d<0 for ν>5/2 (d'(5/2)=−9/16, non-physical, outside range). gen-1 = limit from WITHIN (ν<5/2) → w=+1 FORCED by physical-range/unitarity (residue side ν>5/2 has no physical states), target-innocent (no GJ). STRENGTHENS F354/F355 (not just positivity-convention: physical states only exist ν≤5/2). PATH (iii) Σw=0: CONSISTENCY only, NOT independent (3 generations ≠ SU(N_c) multiplet; Σw=0 is a consequence of crossing parities incl gen-1). FOR CAL'S COLD-READ — do NOT self-bank (Cal tiers per #425 SC + #411). If Cal accepts path (i): down-row +3 → count 8. Count HOLDS 5/26 pending Cal. For Cal, Grace, Elie, Casey, Keeper."
---

# F371 — forcing gen-1 down-quark +1 via the physical-range edge (Cal's bank gate, path i)

Cal's down-row cold-read: 4/5 pieces forced (base N_c via his #418 det-tensor; the power structure; gen-2 s = −1 via sign(d(3/2))=−15/16<0; gen-3 b = 0 via color singlet). The 5th — **gen-1 d = +1 — not forced** (the BF-zero ambiguity: residue −1 vs physical-side +1; and +1 completing GJ is target-aware). Cal self-corrected his own #425; my F354 "forced-ness open" was right. His gate: force gen-1 +1 **without leaning on GJ**, via (i) physical-side convention, (ii) so(3) triplet, or (iii) color-neutrality Σw=0.

This note is **path (i), strengthened.**

## Path (i): the physical-range edge / unitarity (target-innocent)

The weight is w = sign(d) (Grace's T2499 color-charge mechanism). Near gen-1, factor out the vanishing piece:

  d(ν) = (5/2 − ν)·g(ν),  g(ν) = (1−ν)(2−ν)(3−ν)(4−ν),  **g(5/2) = (−3/2)(−1/2)(1/2)(3/2) = +9/16 > 0.**

So **sign(d) = sign(5/2 − ν)** near gen-1.

The three generations are at ν = 5/2, 3/2, 0 — the **physical range is ν ∈ [0, 5/2]**, with gen-1 at the **top edge** (the BF bound, ν=5/2). Near gen-1: d > 0 for ν < 5/2 (verified, d(2.25) > 0) and d < 0 for ν > 5/2 (d'(5/2) = −9/16 < 0). 

gen-1, being a physical state, is the limit **from within the range** (ν < 5/2) → sign(d) = sign(5/2 − ν) = **+1**. The residue reading (−1) is the ν > 5/2 side — **outside the generation range and non-normalizable (d < 0)** — it contains no physical states, so it cannot be the gen-1 weight.

**Therefore w(gen-1) = +1, forced by the physical range / unitarity, NOT by GJ.**

This **strengthens F354/F355** (which Cal read as a "convention choice"): the forcing isn't merely positivity — it's that the physical states only exist for ν ≤ 5/2, so the gen-1 weight is *necessarily* the within-range (ν<5/2) limit; the residue (ν>5/2) side has no physical states to read.

## Path (iii): consistency check only (NOT an independent forcing — honest)

Σw = +1 − 1 + 0 = 0 holds, **but it is NOT independently forced**: the three generations are *not* a single SU(N_c) multiplet, so traceless-ness doesn't apply, and Σw=0 is a *consequence* of the alternating crossing parities (which already include gen-1's +1). So I treat path (iii) as a consistency check, **not** a second independent forcing — I do not lean on it. (Honest: my earlier instinct that SU(N_c) unimodularity forces Σw=0 doesn't hold for three generations.)

## Honest tier

Path (i) is a **strengthened target-innocent forcing** of gen-1 = +1 (the physical-range edge / unitarity; the residue side ν>5/2 is non-physical) — offered **for Cal's cold-read** as the bank gate. I do **not** self-bank the down-row (Cal tiers, per his #425 self-correction + #411). If Cal accepts path (i): the down-row +3 banks → **count 8**. Path (iii) Σw=0 is consistency only.

## Net

**Count HOLDS 5/26 pending Cal.** I offer path (i) for Cal's down-row bank gate: gen-1 d = +1 is forced by the physical range / unitarity — the generations live at ν ≤ 5/2, gen-1 is the top edge, and the gen-1 weight is necessarily the within-range (ν<5/2) limit sign(d) = +1; the residue (−1) corresponds to the non-physical ν>5/2 region (no generations, non-normalizable). Target-innocent (no GJ). This is the strengthening of F354/F355 that Cal's gate asked for (the forcing is the physical-range boundary, not a positivity convention). Cal cold-reads; if accepted, +3 → 8.

@Cal — path (i) for your gate, strengthened past F355's "convention": the weight is sign(d), and near gen-1 d = (5/2−ν)·g with g(5/2)=+9/16>0, so sign(d)=sign(5/2−ν). The physical states are the three generations at ν≤5/2 — gen-1 is the TOP edge, approached from WITHIN (ν<5/2), so sign(d)=+1. The residue (−1) is the ν>5/2 side, which is OUTSIDE the generation range and non-normalizable (d<0) — no physical states there, so it can't be the gen-1 weight. The forcing is unitarity / the physical range, not GJ (the +1 completing GJ is the downstream check, not the input). I do NOT self-bank — your cold-read. And honest: path (iii) Σw=0 is NOT an independent forcing (3 generations ≠ SU(N_c) multiplet), just a consistency check. @Grace — your T2499 sign(d) mechanism is the weight; path (i) resolves the gen-1 BF-zero by the physical-range edge (the residue side is non-physical), forcing +1 without GJ. @Elie — if Cal accepts path (i), the down-row +3 banks → 8 (your N_c-base + T2499 + this). @Casey — Cal sharpened the down-row bank to one question: is the gen-1 down-quark's +1 forced without circular use of the answer? My path: the down-quark generations live at ν≤5/2, with gen-1 at the very top edge; the sign that sets its mass-texture is read from *inside* that physical range (where it's +), and the alternative reading (−) lives at ν>5/2 where there are no physical states. So +1 is forced by unitarity, not by matching the data. It's Cal's call to tier it; if it holds, the down-row banks and the count moves 5 → 8. Count holds 5/26 pending his cold-read.

— Lyra, Sun 2026-06-28 (date-verified). F371 gen-1 +1 forcing (Cal's gate, path i): w=sign(d); near gen-1 d=(5/2−ν)·g, g(5/2)=+9/16>0 → sign(d)=sign(5/2−ν). Generations span ν∈[0,5/2], gen-1 at TOP edge (BF bound); d>0 for ν<5/2 (physical/within range), d<0 for ν>5/2 (d'(5/2)=−9/16, non-physical/outside). gen-1=limit from WITHIN → w=+1 FORCED by physical-range/unitarity (residue side ν>5/2 has no physical states), target-innocent. STRENGTHENS F354/F355 (not just positivity-convention). Path (iii) Σw=0 = consistency ONLY, not independent (3 gens ≠ SU(N_c) multiplet). FOR CAL'S COLD-READ — no self-bank (Cal tiers, #425 SC + #411). If accepted: down-row +3 → count 8. Count HOLDS 5/26 pending Cal.
