# K783 — Casey's refinement: interior discrete, exterior continuous, the CODE lives on the SURFACE. This grounds the discrete/continuous split in SO(5,2) rep theory AND relocates Lyra's Γ-ratio — the tiling test is now the boundary integration (continuous vs 128-cell). Testable, not a rescue.

**Keeper | 2026-07-20 | Casey: the Big Bang was so hot it was continuous, covering the exterior of all manifolds; the interior is discrete; the exterior cools and TILES the surface, coupling the (discrete) interior to the (continuous) exterior. Genuine sharpening — held to the test, post-de-inflation.**

---

## 1. The rep-theory grounding (this is real, not a picture)
SO(5,2) has two representation families:
- **Discrete series** — square-integrable, a *discrete* family, in the **BULK** (L²(D_IV⁵)) → the **MASSIVE fermions** (interior discrete).
- **Principal/continuous series** — tempered, *continuously* parametrized, on the **BOUNDARY/exterior** → the **MASSLESS modes** (light, neutrinos) (exterior continuous).

So Casey's **"interior discrete, exterior continuous"** = **discrete-series (bulk, massive) vs continuous-series (boundary, massless)** — the actual harmonic analysis. And "the Big Bang so hot it was continuous" = the early universe in the *continuous/principal-series* (massless) phase; cooling condenses the discrete (massive) structure — consistent with the thermal history (massless above the EW transition, massive below).

## 2. The reconciliation — the code is a SURFACE tiling, so Lyra's Γ-ratio is the UN-TILED answer
Lyra computed the **bulk radial** overlap → a continuous Γ-ratio. But **the code lives on the SURFACE** — the Shilov boundary, where the condensate O sits (F603). Two different objects:
- **bulk × continuous-surface** → the Γ-ratio (Lyra's computation);
- **bulk × TILED-surface** (128 discrete cells) → the boundary integration is discrete → potentially **127/128**.
So the physical Yukawa is a bulk-to-**surface** overlap, and **if the Shilov boundary is tiled into 2^g cells, that overlap discretizes** — Lyra's Γ-ratio is then the *un-tiled* (continuous-surface) value, not a refutation of the tiled-surface code. **This turns the decider into a concrete computation:** redo the Yukawa's boundary integral over a **tiled (128-cell) Shilov boundary** vs a continuum — does it give 127/128?

## 3. The cosmogenesis (Casey)
Big Bang: hot, continuous (principal-series/massless), covering the exterior of all manifolds. **Cooling** crystallizes/**tiles** the surfaces → a discrete code appears on the Shilov boundary → couples the **discrete interior** (massive fermions, discrete series) to the **continuous exterior** (the cooled continuum). **Light + neutrinos emitted at the boundary = the coupling across the interface** (light tangent/conformal to the exterior; neutrino radial/dead-state). The tiled surface IS the holographic interface.

## 4. The decidable test (the tiling test, now LOCATED on the surface)
- **★ Redo Lyra's overlap with a TILED Shilov boundary** (2^g = 128 cells) instead of a continuum. Result:
  - **127/128 (maximal codeword)** → the tiling is confirmed and 127/128 is rescued as a *surface-tiled* value;
  - **~126/128 (nearest cell)** → naive discretization; 127/128 does not follow;
  - **back to the Γ-ratio** → the tiling doesn't discretize the overlap; 127/128 dies.
- **★ Does cooling actually tile the Shilov boundary into 2^g cells?** (Elie's 3π/128 = the 128 in the Bergman norm is the geometric hint.) The crystallization must SELECT 2^g cells and the extremal one for the top.

## 5. Discipline (post-de-inflation — held hard)
- **This is the move that could re-inflate 127/128** ("the surface tiling fixes whatever the bulk gave"). It counts ONLY IF the tiled-surface overlap comes out the **maximal** codeword (not the nearest), and ONLY IF cooling is shown to tile the boundary into 2^g cells. Both unverified.
- **127/128 stays a conditional prediction.** The refinement *sharpens and locates* the test (the boundary integration, tiled vs continuous); it does not bank the number.
- **The genuine gains:** (a) the discrete/continuous split is now rep-theory-grounded (discrete series vs continuous series = massive vs massless = interior vs exterior); (b) Lyra's Γ-ratio is relocated as the un-tiled answer, not a refutation; (c) the test is a concrete integral (tiled boundary), not a frame.
- **Derived spine untouched.**

— Keeper K783, 2026-07-20. Casey's refinement: interior discrete (discrete series = massive) / exterior continuous (principal series = massless light+ν) / the CODE = a TILING of the Shilov SURFACE (formed by the exterior cooling). Grounds the split in SO(5,2) rep theory. Reconciles Lyra's bulk Γ-ratio: it's the un-tiled (continuous-surface) answer; the physical Yukawa is bulk×surface, and a TILED surface discretizes it → possibly 127/128. Decidable test: redo the overlap over a 128-cell (tiled) boundary — 127/128 (max, confirmed) vs 126/128 (nearest, no) vs Γ-ratio (no tiling). Testable, NOT a rescue: needs the maximal codeword + cooling shown to tile into 2^g cells. 127/128 stays a conditional prediction. See [[Keeper_K782_Q1_computed_geometry_gives_gamma_ratio_not_127over128_caseys_tiling_inverts_the_mainstream_2026-07-20]].
