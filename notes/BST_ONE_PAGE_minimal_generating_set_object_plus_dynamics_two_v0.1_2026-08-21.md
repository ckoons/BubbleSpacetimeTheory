# Physics from a Minimal Generating Set: One Object, One Dynamics
*BST one-pager v0.1 (Grace + Elie, 2026-08-21). SHELF-GATED — external requires Keeper-PASS + Cal-vet + both voices + Casey GO. Nothing pushed.*

## The claim
All of BST's physical content is generated by a set of size **TWO**: one geometric **OBJECT** and one **DYNAMICS**. Every observable — every Standard-Model constant, the cosmological constant, the spacetime signature — is a **reading of the one operator on the one object**, at one address. Zero free parameters; the single dimensionful input is the ruler (one Planck unit).

## Generator 1 — the OBJECT: D_IV⁵ = SO₀(5,2)/[SO(5)×SO(2)]
The object carries *all* its structure intrinsically — nothing is added by hand:
- **The Bergman measure** = the object's canonical automorphism-invariant measure = the Born measure. It is the Jordan-norm-power kernel K = c·N^{−p} (D_IV⁵ is tube-type; the Jordan norm N is the domain's defining equation). *Ambient, not an operation.*
- **Γ_Ω (Gindikin Gamma / FK norm)** = the integration of that same Jordan determinant over the cone — it collapses **through the measure** (a two-step chain Γ_Ω ⊂ measure ⊂ object), a re-read of the object's own kernel. *Not a new operation.*
- **Several ℤ₂ classes — subscript, do NOT merge** (their inter-relationships are OPEN): the **twist channel is the non-orientability, a ℤ₂ orientation datum (kind pinned per reading — H¹ w₁ **or H⁰ involution**; Elie 5411, they don't all live in H¹)**. It is carried in the corpus by more than one object — **ℤ₂_Šilov** (w₁ of the Šilov boundary), **ℤ₂_Möbius** (the T1949 Möbius locus, Pin(2)/SO(2)=ℤ/rank), **ℤ₂_orient** (the orientation quotient) — and, algebraically separate, **ℤ₂_Peirce** (the Jordan frame vs off-diagonal grading → the (1,n−2,1) split). *All definitional, not applied.* The **carrier relationship (which ℤ₂'s coincide, and any Pin/Spin consequence) is OPEN** — the solid claim is only that the twist is a ℤ₂ orientation datum (H¹ w₁ or H⁰ involution, per reading).
- **Five integers** read off the structure: rank=2, N_c=3, n_C=5, C₂=6, g=7; N_max = N_c³·n_C + rank = 137.

## Generator 2 — the DYNAMICS: the commit / reading operation
One operation, the AC(0) triad: **restrict to an address** (definition) · **count** · **identify**. Every observable is *one operation at one address*. (Basis-independent to 3.3×10⁻¹⁶ — the reading is intrinsic to the object, not to a chart.)

## What the two generate — a sample of readings
- **The internal Standard Model** (the cleanest single result): read the reality-type census of End_K(H_F) and the boundary non-orientability. Within the D_IV family the whole internal skeleton is a function of the **one measured integer n_C = 5** — content **1.58 bits**: *is the spinor real, complex, or quaternionic?* Quaternionic (the n≡3,4,5 mod 8 class, selected to n=5 by N_c=3) **is the weak force**. Generated by **two** invariants (census + non-orientability); the third channel (rank → three generations) is *derived* from the census (a quaternionic block caps rank at 2). Gauge group, chirality, Majorana neutrino, Higgs, CP — all readings.
- **Spacetime & the cosmological constant**: the commit **order** is a causal set (Order+Number = Geometry, derived not posited); the commit **count** is volume; the count's **variance** is Pauli-frozen (fermionic area law) → **Λ is stable, not fluctuating** — a pre-registered DESI discriminator (BST forbids a phantom crossing from both the mean and the variance).
- **Constants**: α, the mass ratios (m_p/m_e = 6π⁵), mixing angles — each a reading at its address.

## The honest close
One object, one dynamics, every observable a reading. One dimensionful input (the ruler); everything else forced. **The math is on GitHub.**

---
*Provenance: the reading-calculus arc (T2566–T2569), the H_F(n) construction (T2568), the census/algebra (T2567), the causal-set + Λ results (T2564/T2570/T2571), the operation-collapse (this round). Every claim is a reading of one operator on D_IV⁵ — never "commitment forces D_IV⁵."*
