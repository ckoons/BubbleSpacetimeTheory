# K924 — PRE-REGISTERED audit criteria for the FK Wyler 3×3 (the muon-value gate), committed **BLIND** before Grace sources / Elie turns it. Same discipline that made K922→K923 clean: the ceiling is set before the result so the verdict can't be retrofitted. **The one criterion that decides everything: the DIRECTION of the exponent-6 argument.** A target-INNOCENT derivation runs SO(4)-structure → 6 copies → *predicts* π¹² (matches data). A target-AWARE fit runs observed-π¹² → N=6 (F111's "π-counting forces N=6"). If the only reason we know it's 6 copies is that the answer carries π¹², the argument is circular — CANDIDATE, not derived, no matter how clean the integral.

**Keeper | 2026-07-26 Sun | Blind pre-registration of the FK-3×3 muon-value gate. Committed before the computation. The forward/backward direction of the "6" is the whole audit.**

## What the computation is (K879, set-up-not-turned)
The muon value m_μ/m_e = (24/π²)⁶ is gated on the fully-determined 3×3 **M_ij = ⟨ρ-stratum_i | Szegő-φ | ρ-stratum_j⟩** on the three forced strata {ψ_{5/2}(e), ψ_{3/2}(μ), ψ_0(τ)}. States forced, symbol forced (F582/F682), integral banked (Wyler=α). Only the electron residue d'(5/2)=9/16 has ever been computed (F114); the muon/tau residues at the BF-degeneracy points are the open work. The diagonal already proved the plain √π cancels (K923) — so the π² must come from the off-diagonal / BF-zero residue structure, or not at all.

## The three anchors (from K922/K923/F671, carried forward)
- **Rank bound:** a single residue's ORDER ≤ rank = 2. Exponent 6 CANNOT be a residue order. So a PASS requires 6 to be a **PRODUCT over copies**, not a high-order pole. (Any claim of exponent-6-from-a-single-residue-order = WRONG on its face.)
- **F671 (prior attempt, honest-negative):** the accessible Γ_Ω residue ratio = 3/2 = O(1), not 207. The ~140× "IS the exponent 6" and did NOT emerge from the Γ_Ω residue. The one thing anyone computed leans NO.
- **FF-20 / K847:** 6 = dim SO(4) = C(4,2) = C₂ = n_C+1 = C(n_C−1,2) — five readings of one number. Rich vocabulary ≠ a forcing.

## PASS criteria (muon value DERIVED) — ALL required
- **D1 — π² emerges PER COPY from the BF-zero residue**, computed the way d'(5/2)=9/16 was computed at the electron — NOT inserted, NOT read off (24/π²). The residue at the muon/tau degeneracy points must be actually computed and must carry a π².
- **D2 — THE DIRECTION TEST (load-bearing): the copy-count 6 is produced by the SO(5)→SO(4) measurement-little-group / integration structure FORWARD, and only THEN predicts π¹² — which then matches data.** The mechanism must give "6 copies" from the geometry (6 = the unbroken SO(4) generators = 6 independent boundary-integration 2-planes) **without using the observed π¹² to fix the count.** ❌ FIT if the count is set by "π¹²=(π²)^N ⟹ N=6" (F111's phrasing) — that is the answer fixing the count, backwards.
- **D3 — the per-copy amplitude 64 = d_τ/d_μ falls out of the same residues** (the open piece from F111), not fitted; and the base 24 emerges (24 = Γ(5)=4! at the quark side is clean, but the LEPTON 24 must be shown to be the same object, target-innocently).
- **D4 — the Wyler integral is BOOK-SOURCED, not reconstructed** (Grace's standing discipline; do NOT fabricate to hit 24/π²).

## FIT / FAIL red flags (any one → not derived)
- (24/π²)⁶ or its π-power used anywhere as input (its own evidence).
- The count 6 fixed by π-counting to match π¹² (D2 backward — target-aware).
- 6 justified only as "= dim SO(4)" without the integration structure producing exactly 6 (relabel, FF-20).
- Exponent 6 claimed as a residue ORDER (rank-bound violation).
- Any tuned free parameter; any reconstructed integral.

## PRE-COMMITTED VERDICT CEILING (committed now)
- **DERIVED** (bank the muon value) ⟺ D1∧D2∧D3∧D4, with **D2 in the FORWARD direction** (SO(4)→6→predicts π¹², not π¹²→6). Even then: the value moves from "free modulus" to "derived-from-boundary-residue" — still bucket-2 / free-boundary side, **NO partition-theorem change** (color line is support, orthogonal).
- **CANDIDATE** (not derived) ⟺ the residue gives a π² per copy BUT the count 6 is only relabeled (D2 fails forward) — the honest "warm lead," FF-20 not cleared.
- **CONFIRMED COINCIDENCE** ⟺ the residue gives O(1) / no per-copy π² (F671's lean confirmed) — muon value = identified coincidence, closed honestly; the STRUCTURE (K923) still stands.
- **I commit: I will NOT sign "derived" on a backward (π¹²→6) argument, however clean the integral.** The direction is the audit.

## My honest prior (stated so it can't bias the read)
F671 leaned negative on the one accessible piece, and the "6" is over-readable (FF-20). So my prior is this returns **CANDIDATE or COINCIDENCE more likely than DERIVED.** I am recording that prior HERE so that if it comes back DERIVED I check my skepticism didn't make me miss a real forward-direction forcing, and if it comes back COINCIDENCE I check my prior didn't make me wave it through. Either way, D1–D4 decide it, not the prior.

— Keeper K924, 2026-07-26. BLIND pre-registration, FK Wyler 3×3 (K879) muon-value gate. PASS(derived) = D1 π² per-copy from BF-zero residue (computed, not inserted) + **D2 the copy-count 6 produced FORWARD by SO(5)→SO(4) structure → predicts π¹² (NOT π¹²→6, which is target-aware/circular)** + D3 per-copy 64=d_τ/d_μ falls out + D4 integral book-sourced. Red flags: (24/π²)⁶ as input, 6 fixed by π-counting, 6-as-relabel (FF-20), exponent-6-as-residue-order (rank bound), tuning, fabrication. Ceiling: DERIVED only if D2 forward; else CANDIDATE (residue π² but 6 relabeled) or CONFIRMED COINCIDENCE (residue O(1), F671 confirmed) — structure stands either way, no partition-theorem impact. Prior: CANDIDATE/COINCIDENCE > DERIVED (F671 negative + FF-20 over-read). See [[Keeper_K923_GRAM_DIAGONAL_AUDIT_PASS_on_structure]], [[Keeper_K922_PRE_REGISTERED_audit_criteria_lepton_Gram_diagonal]], [[Keeper_K879_compute_the_banked_Wyler_crank]], [[Lyra_F111_pi_counting_pins_exponent_six]], [[Keeper_K847_24_and_exponent_6_are_RICH_VOCABULARY]], [[feedback_commit_the_checker_half_blind]].