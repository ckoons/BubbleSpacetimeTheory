---
node_type: k_audit
id: K1133
title: (Nucleus LANDING — the forcing-chain audit returns NOT-forced; magic-number VALUE = Partially Derived, explicit-split) Keeper AFFIRMS Grace's NOT-certified verdict against Cal §244/§245. This is the audited-and-hardened landing of the nucleus frontier. ★ WHAT'S BANKED, CREDITED FULLY (not strict-pessimism'd): (1) SKELETON = DERIVED. Block = the tetrahedron (C(4,2)=6=C₂ → A=4 unique, marble); undeformed set {2,8,20,40,70,112} = the 3D-HO packing 2·T_n, confirmed by TWO independent builds (Elie 5019/5022 + Grace's own κ=0 scan). This is a genuine PROMOTION over K601 (which credited the magic numbers only Structural / post-hoc): the light-shell SKELETON now has a forced geometric origin. (2) MODE-COUNT = forced/target-innocent for the "6 edges = C₂" part: 6 tetrahedral edges = C₂. ★ WHY IT CANNOT EARN FULL DERIVED — the forcing-chain audit (Cal §245: full blindness is gone, so the test is per-step forcing, not blind derivation) returns NOT-forced on the load-bearing step, for FOUR reasons (Grace, affirmed): (1) ★ THE DEEP ONE — EQUIPARTITION IS ASSERTED, NOT DERIVED. Even granting the full mode-count 12, "κ = 1/(mode-count) = 1/12" is an equipartition ASSERTION (unit total twist spread equally over 12 modes → 1/12 each). The unit-total normalization AND the equal spread are asserted from first principles, not shown. This is PD BY CONSTRUCTION and is fatal on its own — independent of the count. It is structurally IDENTICAL to n_s: mode-count forced, per-mode magnitude asserted → Partially Derived. (2) THE ×2 IN THE COUNT is the weakest link (Cal §245): "6 edges × 2 spin/tensor components = 2C₂ = 12" — the ×2 must FALL OUT of the tensor structure, not be inserted to double 6 into 12; plausible (2 spin states) but not rigorously forced in a target-aware setting. (3) FORM-MULTIPLICITY: 12 = 2C₂ = C₂·rank = n_C+g = N_c²+N_c — the denominator is not a unique reading of the empirical 0.08 (and 4% off; a closer simple fraction 2/25 exists; ≥4 BST fractions land within 10% — "a BST fraction near 0.08" is nearly free). (4) §244 ITSELF: in-band ≠ certification. 1/12=0.0833≈0.08 opens the four locks, BUT the measured (Mayer-Jensen) κ opens them by construction — opening the locks was never the test; it is HOW the 1949 strength was found. (5) CONTAMINATION: the whole team has now seen 0.08 and 1/12; a form-search run now is reverse-engineering in a derivation's clothes — Elie AND Grace both recused from being the blind deriver; the honest instrument available now (forcing-chain audit) already ran and returned NOT-forced. ★ RULING: magic-number VALUE = PARTIALLY DERIVED, explicit-split — [Derived: the {2,8,20,40,70,112} skeleton (two builds) + the "6 edges = C₂" count] | [asserted/open: the per-mode STRENGTH — equipartition κ=1/(mode-count) — which sets the 1/12≈0.08 magnitude; this is measured, not forced]. K601's Structural FLOOR holds for the value; PD is an UPGRADE of the state (forced skeleton + forced partial-count now exist, which K601 did not credit). ★ THE ONE OPEN GEOMETRIC PROBLEM (promotion path, genuinely open — not a formatting fix): derive the per-mode STRENGTH (why equipartition, why unit total twist) from first principles, by someone UNCONTAMINATED by 0.08. Clears that → magic numbers earn Derived, assembly principle reaches the nucleus. Until then, skeleton stands alone, honest and bounded. ★ NO WAVE-THROUGH on a pretty coincidence; NO over-brake (1/12 is not ruled dead — it's an open problem). Smallness/structure/operator/fermion/strong/anchor + referee-ready corpus + hook package untouched. Nothing pushed.
date: 2026-08-03
author: Keeper
verdict: Keeper AFFIRMS Grace's NOT-certified. The forcing-chain audit (the honest instrument once blindness is gone, Cal §245) ran and returned NOT-forced on the load-bearing step. Magic-number VALUE = PARTIALLY DERIVED in explicit-split form: DERIVED = the {2,8,20,40,70,112} skeleton (block via C₂, two independent builds — a real promotion over K601) + the "6 edges = C₂" count; ASSERTED/OPEN = the per-mode STRENGTH (equipartition κ=1/mode-count → 1/12≈0.08), which is measured (Mayer-Jensen), not derived. Four blocks: (1) equipartition asserted not derived — fatal alone, structurally identical to n_s (forced count + asserted per-mode magnitude → PD); (2) the ×2 is inserted-not-forced-risk; (3) 12 has ≥4 integer forms, 4% off, 2/25 closer — "a BST fraction near 0.08" nearly free; (4) §244: opening the locks is how the 1949 strength was FOUND, not a test. Team contaminated (all have seen 0.08/1/12) → the clean forward derivation needs an uncontaminated deriver or a first-principles equipartition proof; that is the genuinely-open geometric problem and the sole promotion path. K601 Structural floor holds for the value; PD is a state-upgrade. No wave-through, no over-brake. Nothing pushed.
---

# K1133 — the nucleus landing: forcing-chain audit returns NOT-forced; magic numbers = Partially Derived

## The ruling
**Keeper AFFIRMS Grace's NOT-certified verdict.** I do not send it back for another round — the forcing-chain audit (Cal §245: once full blindness is gone, the honest test is per-step forcing, not a "blind derivation") has already run, and it returns a clear answer. Re-running it would be over-processing a settled result.

## What's Derived — credited fully (calibrate both directions)
- **The skeleton is Derived.** Block = the tetrahedron (C(4,2)=6=C₂ → A=4 unique, marble); the undeformed set **{2,8,20,40,70,112} = the 3D-HO tetrahedral packing 2·T_n**, confirmed by **two independent builds** (Elie 5019/5022 + Grace's own κ=0 scan). This is a **genuine promotion over K601**, which credited the magic numbers only Structural / post-hoc — the light-shell skeleton now has a *forced geometric origin*. Don't diminish it.
- **The "6 edges = C₂" count is forced**, target-innocent.

## Why it cannot earn full Derived — the four blocks
1. **★ Equipartition is asserted, not derived (fatal alone).** Even granting the full mode-count 12, "**κ = 1/(mode-count) = 1/12**" is an *equipartition assertion* — a unit total twist spread equally over 12 modes. Both the unit-total normalization and the equal spread are asserted, not shown from first principles. **This is Partially-Derived by construction, and it's fatal independent of the count.** It is structurally *identical to n_s*: forced mode-count, asserted per-mode magnitude → PD.
2. **The ×2 in the count is the weakest link (Cal §245).** "6 edges × 2 spin/tensor components = 2C₂ = 12" — the ×2 must *fall out* of the tensor structure, not be inserted to double 6 into 12. Plausible (two spin states), not rigorously forced in a target-aware setting.
3. **Form-multiplicity.** 12 = 2C₂ = C₂·rank = n_C+g = N_c²+N_c. The denominator is not a unique reading of 0.08 — and it's 4% off, a closer simple fraction (2/25) exists, and ≥4 BST fractions land within 10%. "A BST fraction near 0.08" is nearly free.
4. **§244: in-band ≠ certification.** 1/12 opens the four locks — but the *measured* (Mayer-Jensen) κ opens them by construction. **Opening the locks was never the test; it is how the 1949 strength was found.**

Plus **contamination**: the whole team has seen 0.08 and 1/12, so a form-search now is reverse-engineering in a derivation's clothes. Elie and Grace both correctly recused from being the blind deriver.

## The landing
- **Magic-number VALUE = Partially Derived (explicit-split):**
  - **Derived:** the {2,8,20,40,70,112} skeleton (two builds) + the "6 edges = C₂" count.
  - **Asserted/open:** the per-mode **strength** (equipartition κ = 1/mode-count → 1/12 ≈ 0.08), which is **measured** (Mayer-Jensen), not forced.
- **K601's Structural floor holds** for the value; **PD is a state-upgrade** (a forced skeleton + forced partial-count now exist, which K601 did not credit).

## The one open geometric problem (sole promotion path)
Derive the per-mode **strength** — *why equipartition, why unit total twist* — from first principles, produced by someone **uncontaminated by 0.08**. That is a genuinely open geometric problem, not a formatting fix. Clear it → the magic numbers earn Derived and the assembly principle reaches into the nucleus. Until then the skeleton stands alone, honest and bounded.

**No wave-through** on a pretty coincidence. **No over-brake** — 1/12 is not ruled dead; it's an open problem.

— K1133, Keeper, 2026-08-03. Nucleus frontier audited-and-hardened at PARTIALLY DERIVED. Skeleton Derived (two builds, promotion over K601); per-mode strength asserted (equipartition), the sole open problem. Structurally identical to n_s. See K1132, K601, K1131, toy 5022, Grace's blind-cert result, Cal §244/§245, feedback_derived_with_caveat_over_demotion, reference_tier_system_PD.
