# F818 — O7 forcing criterion, PRE-REGISTERED BLIND before Elie's overlap posts (commit-the-checker's-half discipline): **what result counts as Derived vs Identified vs nothing-banks, committed NOW so a match cannot be retrofitted as "forced." The test is a count: A = independent DISCRETE address choices remaining after the ℤ₂ parity-lock (F817) + the forced pieces; B = independent mixing observables reproduced sub-percent. Verdict rule, pre-committed: A=0 → the reproduced observables are FORCED (Derived); A=1 forced-by-rule bit predicting all B → over-determined B:1 → Derived IF the bit is fixed by a stated geometric rule (ground/lowest mode per stratum), Identified if the bit is chosen; A≥B → a re-coordinatization of CKM/PMNS → NOTHING banks (S-tier). Bank Derived only if B/A ≥ 2 AND every address input is forced by a pre-stated rule, not selected. The θ₂₃ octant (4/7 upper vs 6/11) is the canary: whatever the blind fire produces is our PREDICTION (DUNE-testable), never slid to match the observed octant.**

**Lyra, Wednesday 2026-08-05. My last non-contaminating move before Elie fires: pin the forcing bar BLIND. I will not pre-compute the overlap (that would poison Elie's blind fire), but I CAN commit what "forced" means so the verdict is honest whichever way it lands. This is the B5/pre-registration principle + commit-the-checker's-half: the checker posts the criterion before the number, so a match can't be retrofitted. Calibrate both ways — the bar must be high enough that a lucky match doesn't bank, and fair enough that a genuine over-determination gets its due.**

## The count (target-innocent — no data touched)
**Inputs already FORCED (A-reducers, from the corpus + F817):**
- Generation degrees {1,3,5} = Q⁵ odd cohomology (T1929) — forced.
- ν = N_c = 3 (Wallach threshold) — forced.
- Single-row K-types (1,0)/(3,0)/(5,0), dims 5/30/91 — forced.
- Overlap form N(w)^{n_C/2} (F87b) — forced.
- **ℤ₂ parity-lock k≡m (mod 2) (F817)** — forced; removes the independent-weight freedom.

**A = the discrete address DOF STILL free after all of the above.** (Elie's blind fire measures A: does the ℤ₂-surviving set + a ground-mode rule fix g2,g3 with 0, 1, or ≥B free choices?)

**B = independent mixing observables reproduced sub-percent.** CKM: 3 angles + 1 phase = 4. PMNS: 3 angles + 1 Dirac phase = 4 (Majorana phases not oscillation-observable). Grace's seven (V_cb, V_ub, δ_CKM, θ₂₃, θ₁₂, θ₁₃, δ_PMNS) + V_us (done). So B up to 8.

## The verdict rule (COMMITTED — apply exactly, no post-hoc adjustment)
| A (free address DOF) | Verdict | Condition |
|---|---|---|
| **A = 0** | **Derived** (B params) | ℤ₂-lock + ground-mode rule fully fix g2,g3; every sub-percent observable is forced. |
| **A = 1**, one bit → all B | **Derived** if the bit is fixed by a *pre-stated* geometric rule (ground/lowest surviving mode per stratum); **Identified** if the bit is *chosen* | over-determination B:1; the bit itself must be forced, not tuned. |
| **2 ≤ A < B** | **Identified / partial** | some forcing, but residual freedom; bank only the over-determined sub-block, flag the chosen part. |
| **A ≥ B** | **NOTHING banks** (S-tier) | it's a re-parametrization of the mixing matrices, not a derivation. |

**Global gate:** bank **Derived** only if **B/A ≥ 2** AND every address input traces to a pre-stated geometric rule. B/A ≥ 2 with a *chosen* input → **Identified**, and the chosen bit is named explicitly.

## The octant canary (pre-registered, hardest discipline)
θ₂₃ has two clean D_IV⁵ candidates: **4/7 = rank²/g (upper octant)** vs **6/11 = C₂/c₂**. They differ by which ℤ₂-surviving address g3 occupies. **Pre-commitment:** whatever octant Elie's blind ground-mode fire produces IS the prediction (a pre-registered DUNE test) — we report it and stand behind it. We do NOT slide the address to hit the observed octant. If the blind fire is ambiguous between them, the verdict is A≥1 (a choice remains), NOT "pick the one that matches." Forced-address-or-nothing.

## Why pre-register (the discipline, stated)
Once Elie's numbers are visible, every clean ratio looks "forced" in hindsight (Cal #27 fires hardest at peak convergence; the nearest-anchor reflex grabs the match). The only defense is to fix the bar BEFORE the number. This note is that bar, committed blind. When Elie posts, we read A off his construction and apply the table — no renegotiation.

## Tier / handoffs
- **@Elie** — fire the overlap blind, and in your post report **A explicitly**: after the ℤ₂-surviving (3,0)/(5,0) modes + your ground-mode rule, how many independent discrete address choices remain to place g2,g3? 0, 1, or more? Report A and the raw mixing outputs BEFORE comparing to PDG. I've committed the verdict table above; A decides it.
- **@Cal** — the criterion is pre-registered blind (this note, before your independent fire). Hold me and Keeper to it: apply the table as written when the numbers land; guard the B/A ≥ 2 + forced-input gate and the octant-is-a-prediction commitment. If anyone renegotiates the bar after seeing the match, that's the retrofit — flag it.
- **@Grace** — your seven-parameter ledger is the B-counter. Tag each of the seven as forced/chosen when Elie's A lands, per the table. Your no-fit discipline (don't pick 4/7 vs 6/11) is now the pre-registered octant rule.
- **@Keeper** — LINK 1 (F817) done; this (F818) is the pre-registered forcing bar for LINK 2, committed blind. The make-or-break verdict is now mechanical: Elie reports A, we apply the table. No post-hoc "it's forced." sin²θ_W stays parked (S-tier, scale-assignment open) — O7 is the live lever.
- **@Casey** — before Elie pulls the trigger, I wrote down — without looking at any answer — exactly what would count as a real derivation versus a lucky fit. It's a counting rule: how many free choices are left after the fold locks the addresses, versus how many mixing numbers come out. If the fold leaves *zero* free choices and seven numbers fall out, they're derived. If it leaves *one* forced bit that predicts all seven, that's seven-for-one — over-determined, still a win, as long as that bit is set by a rule and not by us peeking at the answer. If it takes seven separate choices to get seven numbers, we've just renamed the Standard Model's knobs and nothing is gained — and I've committed to saying so. The sharpest line is the neutrino octant: two clean values, 4/7 and 6/11, and the honest move is that whichever one the blind fire lands on is our *prediction* for the DUNE experiment — we don't slide it to match what's measured. I fixed the bar now so that when the numbers appear we can't move it. That's the whole game with a forced result: decide what forced means before you see the number. Nothing pushed.

Notes only; no toy/theorem claimed (pre-registered forcing criterion for O7 LINK 2). F818: committed BLIND before Elie's overlap. A = free discrete address DOF after ℤ₂-lock (F817) + forced pieces; B = independent mixing observables reproduced sub-percent (CKM 4 + PMNS 4). Verdict table: A=0 → Derived; A=1 forced-by-rule bit → Derived (B:1 over-determined), chosen bit → Identified; A≥B → nothing banks (re-parametrization). Gate: Derived only if B/A≥2 AND inputs forced by pre-stated rule. Octant canary: blind-fire octant (4/7 vs 6/11) IS the DUNE prediction, never slid to match. Discipline: fix the bar before the number (commit-checker's-half + B5). @Elie report A explicitly + raw outputs pre-comparison; @Cal enforce the table, flag any post-hoc renegotiation; @Grace ledger = B-counter, tag forced/chosen. — Lyra