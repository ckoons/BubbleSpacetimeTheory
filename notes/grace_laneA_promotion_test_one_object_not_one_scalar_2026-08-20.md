# Lane A — the promotion test: ONE object, ONE computation, NOT one scalar invariant (Grace + Cal, 2026-08-20)
*Casey's test: are the five reality-type readings literally one computation walking K→U(1)_Y→U(1)_em→spinor→vector-branching, or five cousins? Verdict calibrated BOTH directions (my over-reach guard is live): it PROMOTES to "one object / one computation" — a real theorem — but does NOT promote to "exactly one scalar LA invariant," because K=SO(5)×SO(2) has two commuting factors and the walk BRANCHES.*

## The computation (what "one" means, precisely)
**ONE object:** H_F as a K-module with real structure J, K = SO(5)×SO(2).
**ONE computation:** the K-equivariant real-structure (isotypic + Frobenius–Schur) decomposition of H_F.
Every internal-SM structure is an OUTPUT of that single decomposition — not a separate computation. In that exact sense it IS "one computation," and the LA-on-D_IV⁵ program lands: one linear-algebra object, one operation, every internal observable a reading.

## Why it does NOT collapse to one scalar invariant (the honest ceiling)
K = SO(5) × SO(2) is a **product** — two commuting factors — so "decompose H_F under K" inherently reads BOTH factor-actions and their cross. The five readings distribute over the two factors; the proposed chain is NOT linearly descending:

| Reading | Lives on | Invariant channel |
|---|---|---|
| gauge SIZE (census 0+1+3=4) | reality form of the whole | **FS → division algebra → its unitary group:** dim{O(1),U(1),Sp(1)} = {0,1,3} — SIZE *is* the FS reading |
| gauge PROVENANCE (SU(2)_L forced / color imported / U(1)) | reality form | same FS channel (collapses with SIZE) |
| CHIRALITY (Y≠0 → complex) | **SO(2) factor** | hypercharge weight Y |
| MAJORANA (ν the lone Q=0) | **SO(2) factor** | Q = T₃+Y/2 at the U(1)_em node |
| CP-EXISTENCE (the √−1) | **SO(5) factor** | quaternionic Spin(5)=Sp(2) spinor (FS=−1) |
| HIGGS (doublet) | **the CROSS** | ℍ↔ℂ intertwiner (off-diagonal D_F) |

**The spinor (SO(5)) and the charge (SO(2)) live in DIFFERENT commuting factors** — so "K → U(1)_Y → U(1)_em → spinor → ..." is not one descending chain; the resolution BRANCHES at K into an SO(2)-branch (charge → chirality+Majorana) and an SO(5)-branch (spinor → CP+SU(2)_L), joined by the cross (Higgs).

## The count-once structure (what's genuinely independent)
- **SIZE + PROVENANCE = ONE reading** (FS → division algebra → unitary group; the 0+1+3 IS the reality types).
- **SU(2)_L + CP-existence ride the SAME ℍ block** → count-once (T2547 already flags the shared quaternionic parent).
- **CHIRALITY + MAJORANA ride the SAME SO(2)-charge branch** → count-once.
- **HIGGS** = the factor-cross intertwiner (its own channel).
⟹ Minimal generating set of the ONE object = **three invariant channels**: {reality form J, SO(2)-weight, factor-cross}. Five readings, three independent channels, one object.

## The theorem worth stating (calibrated)
**"The internal Standard Model is the invariant data of a single object — H_F as a K=SO(5)×SO(2) module with real structure J on D_IV⁵ — recovered by one computation, its K-equivariant real-structure decomposition."** This is the sharpest TRUE landing of LA-on-D_IV⁵: one object, one operation, every internal observable a reading. It does NOT reduce to one scalar invariant (the two K-factors + cross are irreducible) — claiming that would over-fit "minimal" to "one number." Over-claim rejected; under-claim ("five cousins") rejected; the object is genuinely one.

**Tier:** structural theorem (one-object generation of the internal SM). **@Cal:** vet the calibration — is "one object, three invariant-channels, one computation" the right ceiling, or does the factor-cross reduce further? **Edges:** the census-instrument note, A_F=ℂ⊕ℍ⊕ℝ, T2547, T1985/T2524, Cal #36. **@Keeper** for a T-number if Cal passes the calibration.
