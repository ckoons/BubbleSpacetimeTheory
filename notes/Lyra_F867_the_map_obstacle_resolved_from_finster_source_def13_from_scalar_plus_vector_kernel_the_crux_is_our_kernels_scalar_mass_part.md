# F867 — the map make-or-break (Lane 1, solo), obstacle RESOLVED from Finster's source: Def 1.3 follows from a scalar+vector kernel P = α·γξ + β·1 (verified), and the whole map now reduces to ONE sharp, physical question — does our Bergman kernel's boundary limit carry a nonzero SCALAR (β, mass-like) part? **Worked from the source (arXiv:1102.2585, Finster Lemma 1.1) as instructed, and it resolved F866's obstacle exactly. THE EXACT STRUCTURE (source, verbatim): the Minkowski Dirac-sea kernel is P(x,y) = α·(y−x)_j γ^j + β·1 — VECTOR (α·γξ) AND SCALAR (β·1), α,β complex. The closed chain A_xy = P(x,y)P(y,x) = a·(y−x)_j γ^j + b·1 with REAL coefficients a = αβ̄+βᾱ, b = |α|²(y−x)²+|β|², and eigenvalues λ = b ± √(a²(y−x)²). For timelike ((y−x)²>0): √ real → all-real → TIMELIKE. For spacelike ((y−x)²<0): √ imaginary → b ± i·(real) = complex conjugate pair, EQUAL magnitude → SPACELIKE. This IS Def 1.3, and I verified it numerically (a·γξ+b·1, a,b real: timelike eigenvalues real, spacelike complex-equal-|·|). F866's failures are now fully explained: my naive kernels had β = 0 (no scalar part) → A = |α|²ξ²·1, real for BOTH → no causal distinction. The complex-spacelike REQUIRES β ≠ 0. And a = αβ̄+βᾱ vanishes if β=0, confirming: NO scalar part → no causal structure from the closed chain. THE MAP, now reduced to one crux: Def 1.3 is reproduced by ANY kernel of the form α·γξ + β·1 with β ≠ 0. So "Finster's projector P(x,y) = our Bergman kernel" holds IFF our (2,2)-spinor Bergman kernel's boundary limit has this scalar+vector form with a nonzero β. THE PHYSICS (why this is plausible AND what the crux means): (i) the correspondence is grounded — Finster's P is the OCCUPIED (negative-energy, Θ(−k⁰)) two-point function; our Bergman kernel is the reproducing kernel of the OCCUPIED HOLOMORPHIC (positive-frequency) committed modes; Hardy/holomorphic = positive-frequency = the same occupied-state structure. (ii) the SCALAR β part IS the fermion MASS (Finster's β ∝ m, the (k̸+m) mass term); in BST the committed fermions are MASSIVE (mass = the radial norm, item-10) → β ≠ 0 is expected. A MASSLESS conformal boundary field would have β = 0 and FAIL — so the crux is precisely whether our massive committed modes give the boundary kernel a scalar/mass part, which physically they should. HONEST TIER: the mechanism is EXACT and VERIFIED (Finster Lemma 1.1 + numerics); the correspondence is WELL-FOUNDED (occupied-state/positive-frequency identity); the credential is EARNABLE — but NOT yet earned: the concrete remaining step is computing our Bergman kernel's boundary Lorentz-decomposition and confirming β ≠ 0 with real a,b. I do NOT claim the map holds; the β≠0 confirmation is the crux, and I state it as the sharp open question, not a result. This is a MAJOR advance over F866 (exact structure + precise physical crux) with the discipline intact. Cal's ceiling holds: even a full match buys the continuum-limit leading-order route, never "forces Einstein."**

**Lyra, Saturday 2026-08-08 (Lane 1, solo). The map's obstacle resolved from the source, the mechanism verified, the map reduced to one sharp physical crux (the scalar/mass β part). Decided on the math, blind. No map claim; the β≠0 confirmation is named as the remaining step. RUBRICS done-bar below.**

## The exact mechanism (source-verified + numerically confirmed)
- **Kernel (Finster L1.1):** P(x,y) = α·γξ + β·1 (vector + scalar, α,β complex).
- **Closed chain:** A_xy = a·γξ + b·1, **a = αβ̄+βᾱ, b = |α|²ξ²+|β|² — both REAL.**
- **Eigenvalues:** λ = b ± √(a²ξ²). **Timelike (ξ²>0) → real → TIMELIKE; spacelike (ξ²<0) → complex-equal-|·| → SPACELIKE.** = Def 1.3. ✓ verified.
- **F866 explained:** β=0 (pure vector) → a=0 → A=|α|²ξ²·1 → real for both → no causal structure. **The scalar β is load-bearing.**

## The map reduced to ONE crux
> **Does our (2,2)-spinor Bergman kernel's boundary limit decompose as α·γξ + β·1 with β ≠ 0 (and real a,b)?**
- **Vector part (α):** generic (any covariant Dirac two-point function has it).
- **Scalar part (β) = the fermion MASS:** the crux. A massless boundary field → β=0 → FAILS. Our committed fermions are MASSIVE (mass = radial norm, item-10) → β≠0 EXPECTED — but must be CONFIRMED by the explicit boundary decomposition.

## Why the correspondence is grounded (not a coincidence)
Finster's P = occupied (Θ(−k⁰)) two-point function; our Bergman kernel = occupied HOLOMORPHIC (positive-frequency) committed-mode reproducing kernel; Hardy = positive-frequency = the same occupied-state object. Both are occupied-state two-point functions of a Dirac field → both carry α·γξ + β·1. The map is well-founded; the β≠0 (mass) confirmation is the concrete step.

## RUBRICS Layer-2 done-bar (make-or-break, honest)
- [x] Mechanism concrete + source-verified: A = a·γξ+b·1 (a,b real) → λ=b±√(a²ξ²) → Def 1.3 (Finster L1.1 + numerics).
- [x] Open edge named precisely: compute our Bergman-kernel boundary Lorentz-decomposition; confirm β≠0 (the fermion mass) with real a,b.
- [x] Tier = well-founded, credential EARNABLE, NOT claimed; "if β≠0, then the map holds"; Einstein NOT promoted (Cal ceiling).
- [x] Ran the number; F866's failure explained (missing scalar part); the crux is physical (mass), target-innocent.

## Handoffs
- **@Keeper** — F866's obstacle is RESOLVED from Finster's source: Def 1.3 follows from a scalar+vector kernel α·γξ+β·1 (verified), and the map now reduces to ONE crux — **does our Bergman kernel's boundary limit carry a nonzero scalar (β = fermion mass) part?** Physically it should (our committed fermions are massive), but it's NOT yet confirmed, so the credential is earnable-not-earned. Hold the Finster note until the β≠0 boundary computation lands. Major advance over F866; discipline intact.
- **@Elie** — the load-bearing check for your CFS calculator: the closed chain needs the kernel's SCALAR (β) part, not just γ·ξ. If your Minkowski form-match used a pure-γξ kernel it would give real-for-both (my F866 error); with β≠0 it gives Def 1.3. Confirm your kernel carries the mass/scalar term.
- **@Cal** — guard held: mechanism exact and source-verified, but NO map claim — the β≠0 confirmation is named as the open crux, and even a full match stays continuum-limit-leading-order (never Einstein). The correspondence is well-founded (occupied-state identity), the credential earnable.
- **@Casey** — the source cracked it open. Finster's "arrow at each point" has two parts, not one: a *direction* part and a *size* part — and it's the *size* part that decides space-vs-time separation. My earlier tries only had the direction part, which is why they gave the same answer for space and time (that bug is now fully explained). With both parts, the math reproduces his rule exactly — I checked it cold. And the beautiful bit: that "size" part is literally the *mass* of the particle. Finster's example is a massive field; ours are massive too (mass is the radial size in our geometry). So the whole credential — "your projector is our Bergman kernel" — now comes down to one clean, physical question: does our kernel, read at the boundary, carry that mass term? It should, because our records are massive — but "should" isn't "does," so I'm not claiming it until I compute the boundary form and see the mass term appear with the right (real) coefficients. No claim, no Finster note yet — but we went from a wall to a single, physical, checkable question, and it's the mass. Nothing pushed.

Notes only; no toy/theorem claimed (make-or-break, obstacle resolved, crux named). F867: from Finster source (L1.1) — Minkowski kernel P=α·γξ+β·1 (vector+scalar); closed chain A=a·γξ+b·1, a=αβ̄+βᾱ, b=|α|²ξ²+|β|² REAL; λ=b±√(a²ξ²) → real-timelike/complex-eq-|·|-spacelike = Def 1.3 (VERIFIED numerically). F866 failed ∵ β=0 (pure vector) → a=0 → real-for-both. MAP reduces to: does our (2,2)-spinor Bergman kernel boundary limit have β≠0 (β = the fermion MASS; massless→β=0→fails; our committed modes massive→β≠0 expected). Correspondence grounded (Finster P = occupied Θ(−k⁰) kernel = Hardy/Bergman positive-frequency occupied-mode kernel). TIER: mechanism exact/verified; credential EARNABLE not earned; β≠0 confirmation = the crux (open); NO claim; Cal ceiling (continuum-limit, never Einstein). @Keeper hold Finster note; @Elie kernel needs scalar β part; @Cal well-founded, not claimed. — Lyra