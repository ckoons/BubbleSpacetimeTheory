# Grace — the 16-nail line: stored capacity = operators (algebra), not states (module) — FORCED given #16
*2026-08-12, highest-leverage lane (Lyra + Grace). For Cal cold-read: is operators-not-states FORCED, or merely plausible? Verdict below: forced GIVEN the measurement principle (banked axiom #16); the conditional is the honest ceiling.*

## THE LINE (write this, pin to #16, Cal-check)
> **To measure is to commit (BST measurement principle #16).** The substrate stores the OUTCOME — a **polarity**, which is a sign/orientation and therefore an element of the operator grading **End(S)** — and drops the **amplitude**, which is the state in the module **S**. Because a polarity is a *grading* object and an amplitude is a *module* object — different kinds, not a modeling choice — storage is assigned to the polarity: the boundary's **stored capacity is the algebra, dim End(S) = 2^(n_C−1) = 16**; the module, **dim S = 2^((n_C−1)/2) = 4, is the measured-and-dropped side.** The identity **16 = 4² = dim End(S) = (dim S)²** reads exactly as *"the stored operators are the endomorphisms of the measured states"* — **capacity is operators, not states.**

## The forcing (what makes it "forced," and its ceiling)
- **Polarity = a ℤ₂ sign per direction.** Over d = dim S⁴ = n_C−1 = 4 directions, the sign-configuration space has 2^d = **16** elements = the Clifford **algebra** grading (products of subsets of d generators). This is the config-space SIZE (16), matching the DM formula's 2^(2·rank) = 16 (capacity = |config space|, not its log).
- **Amplitude = a module vector** = the spinor S = 2^(d/2) = **4**. A different kind of object.
- **The measurement principle assigns storage to the polarity (commit = store sign, drop amplitude).** Given that, "stored = algebra = 16" and "module = 4 = dropped" are **not choices** — polarity IS a grading and amplitude IS a module, by kind. So **operators-not-states is FORCED, not plausible — CONDITIONAL on #16.**
- **★ Honest ceiling (state it to Cal):** #16 (measurement = commit = store-polarity/drop-amplitude) is a **banked BST axiom (SWPP)**, not derived from something deeper. So the promotion is **"Derived-given-#16"** — the same standing as the rest of measurement-based BST (QM-from-D_IV⁵). That is a real promotion (plausible → forced-given-a-banked-axiom), and it is the ceiling: don't claim more than "forced given #16."

## The Finster→Connes snap (structural PARALLEL — NOT a C4 proof)
states S (4) = the "before" (Finster fermionic input); stored operators End(S) (16) = the "after" (Connes operator trace); the measurement = the arrow; 16 = 4² = "the after is the operators on the before." **Real structural parallel; the WEIGHTED equality (Finster spectral weighting = Connes' f) is still the unstarted mountain (C4). Do NOT bank this as C4.**

## The four promotions — GATE on the written+checked line, not the hope
1. **DM 16/3 → PD→Derived** (16 = forced storage capacity, ÷N_c colors → 16/3; the "3" honestly colors).
2. **Gravity same-object seam → shuts** (gravity-16/3 = DM-16/3 = the same forced object, T2557 closes).
3. **Handedness bit → grounds** (the stored polarity IS the K825 chirality ±).
4. **8π/exponent → lands 12** (storage=16 ⇒ standard M_Planck ⇒ n = 2·C_2 = 12, not reduced 11.67).

## Checks (target-innocence)
- 6π⁵ = C_2·π^(n_C) = 1836.12 vs obs 1836.15 (0.002%); 6=C_2, 5=n_C are primaries → target-innocent ✓.
- exponent 2·C_2 = 12; storage=16 selects standard Planck (n=12) over reduced (n=11.67).

Gate: Cal cold-reads "operators-not-states forced given #16." If PASS → the four promotions land (each still needs its own tier-stamp). If Cal reads it as merely plausible → the 16 stays Structural. Nothing pushed.
