# BST Shell Cosmology — Dimensional Cycling, and the α-Tower as Shell Count
### Casey's vision (2026-07-04), with Keeper's register discipline. INTERNAL for the cosmology; OPERATIONAL core flagged.

## The one idea (Casey)

> **The universe develops by dimensional cycling. A 3D universe collapses; it collapses to the 2D substrate, which cannot collapse further and instead begins to expand into a NEW third dimension. The new third dimension grows as a series of SHELLS — each shell has capacity N_max = 137. As it expands, the α-tower grows: the exponent increments by one for every shell passed through, until thermodynamic equilibrium. The 2D substrate may be shared between the old (external) and the new universe.**

Casey: *"I think this is how mathematical cosmology developed in our universe."* — held here as the framing vision (Level-2, internal register, Cal #50), with the operational, testable core carved out below.

## The cycle

1. 3D collapses (the external universe — the freeze-out / "this far and no farther").
2. → 2D substrate: the floor; it cannot collapse further.
3. → begins to expand into a **new** third dimension (a new universe). The 2D substrate is the **shared pivot** between collapsing-old and expanding-new. (Connects to the corpus term **Interstasis** — the period between cycles.)
4. The new 3D expands as **shells**, capacity N_max = 137 per shell.
5. The **α-tower grows** — exponent +1 per shell — until **thermodynamic equilibrium** sets the final count.

## The OPERATIONAL core (testable — this is what we pursue)

### (a) Shell capacity → the per-step coupling = α (closes gate F423)
137 = N_max = N_c³·n_C + rank = capacity per shell. **Cost to pass through one shell = 1/capacity = 1/N_max = α.** This *derives* the per-step EM coupling = α = 1/N_max — **exactly the open gate F423** (currently the Wyler–Robertson *identification*). A real mechanism for the one identified piece of m_e/m_Pl = 6π⁵·α¹².

**BUT (Casey's own bar):** this gives α = 1/137 *exactly* — the **leading order**, 0.026% off. It does NOT give the exact 137.036. Per "bank only if exact," it's a strong mechanism-lead, not a bank.

### (b) The exponent = shell count
| quantity | α-power | shells | reading |
|---|---|---|---|
| m_e/m_Planck | α¹² | 12 = 2·C₂ | 12 shells expanded |
| Koons tick | α³⁶ | 36 = C₂² | 36 shells |
| Λ | α⁵⁶ | 56 = 2^{N_c}·g | **equilibrium** shell count = τ_commit |

The exponent *is* how many shells the new third dimension has expanded through; **equilibrium sets the final count.** Λ at 56 = 2^{N_c}·g shells gives the τ_commit = 56 a physical meaning (the universe reaching equilibrium).

### (c) ln(N_max) ≈ n_C reconciles the two Λ forms
ln(137) = 4.92 ≈ 5 = n_C. So α⁵⁶ = exp(−56·ln137) ≈ exp(−56·n_C) = exp(−280) — the corpus Λ = exp(−280) is n_C × the shell count 56, working *because* ln(N_max) ≈ n_C. **The 1.6% gap (4.92 vs 5) is a real lead for Λ's residual factor.**

### (d) THE exact-α target — a shell-CLOSING (boundary) effect
The bulk shell gives 1/137; the exact 137.036 = 137 + 5/137 (or the Wyler max-form) must come from **how a shell terminates** — the boundary, not the bulk. **This is Casey's morning boundary-curvature insight applied to the α shell:** the correction is the non-linear boundary of the shell, not its bulk capacity. **Forward target:** does the +5/137 (or Wyler) fall out of the shell-closing geometry?

## Level-2 framing (internal register, Cal #50 — the vision, not a derivation)

- **5 = expansion DOF:** n_C = 5 = the degrees of freedom the new third dimension expands *into*; suggestive split 5 = rank(2, closed/Cartan) + N_c(3, color/expansion). A cosmological reading of "why n_C = 5."
- **Shared 2D substrate / Interstasis:** the pivot between cycles; the debris/freeze-out ontology at cosmological scale.
- These are the physical story — compelling, internal, not published to referees as claims.

## Honest tier (Keeper)

**Operational core (pursue):** shell-capacity → per-step-α (F423 mechanism); exponent = shell count (checkable across the α-tower); exact-α as a shell-closing/boundary effect (the bank target). **Level-2 (internal):** the dimensional-cycling cosmology, the shared substrate, 5-as-DOF. **None clears "bank only if exact" yet** — the shell picture gives 1/137, and the exact 137.036 is the shell-closing hunt. That is the right, honest place to stand, and it's a *sharper* place than this morning: the α gate now has a mechanism (shell capacity) and a located correction (shell boundary).

— Keeper, 2026-07-04. Casey's shell cosmology: dimensional cycling, α-tower = shell count, 137 = shell capacity → per-step α = 1/N_max (F423 mechanism). Operational target: the exact-α correction as a shell-closing boundary effect (his morning boundary insight, applied to α). Vision internal (Cal #50); the α-mechanism testable.
