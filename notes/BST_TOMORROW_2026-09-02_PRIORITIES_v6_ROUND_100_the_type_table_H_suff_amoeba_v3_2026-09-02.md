# Today — 2026-09-02, v6 at 11:25 (ROUND 100): THE TYPE TABLE, H-suff, and the amoeba with a Kempe eat rule.
**Keeper. Supersedes v5. Read after K1841. Mode: investigate, don't gate.**

## Position in three sentences
The bare cut mechanism is dead and its pair-specific form survives as two halves with a perfect control:
containment implies exit, and a containing word exists at every lock. A lock is where the word family collapses
(mode 8 images vs 13), which is a statement about how Kittell's eight chains intersect. The amoeba, as stated,
ping-pongs; with no-return it colors 93% at n = 22 with lookback never past four.

## ASSIGNMENTS (relay order: Grace → Elie → Lyra → Cal → Keeper)

**GRACE — LANE F, the type table (with Elie, two instruments).** For every stuck coloring in frame n ≤ 22 (or a
stratified sample ≥ 50,000 + all 93 + the matched sample if the full run exceeds an hour): the TYPE = the 8×8
intersection matrix of Kittell's eight chains (state the far-copy seed rule from Cal §821 so each chain is
well-defined), with intersection sizes as the refinement. Tabulate: locked / not, exiting orbit, image count, by
type. Report: number of types seen; whether "locked" is a function of type (k/N types pure vs mixed); the
image-collapse feature as a function of type. Positive control: the 93 must land in ≤ a handful of types.
FINAL = the type table with purity counts, or the mixed types exhibited.

**ELIE — three runs.** (1) Lane F's second instrument (BFS chains, same seed rule) on the same population,
cross-checked with Grace. (2) n = 24 when it lands. [Amoeba v3/v3b DROPPED at 11:30 — Lane E parked at Casey's word; v2's
measurements kept as observations.] FINAL = the type cross-check; the n = 24 line.

**LYRA — H-suff, derived.** Pair-specific containment ⟹ exit: an (r,s_M)-stage chain of a legal word containing
the cut recolors every cut vertex out of both bridge worlds; derive that the bridge word's tunnel condition then
fails (Lemma T + the two cut types + Jordan). Name the third door (the middle stage's own toggles re-bridge) and
pre-register its kill. Then: H-exist BY TYPE, once Grace's table names the types — which orbit is the containing
word in each. Correct the Kittell Alias Theorem per Cal (seed rule; "52" as a projection). FINAL = H-suff derived
or floored; alias corrected.

**CAL — pre-score Lane F; read Lyra's H-suff.** What "locked is a function of type" proves and does not (it is a
finite table, not a proof; the derivation per type is still owed); what a mixed type means. POSITION vs VALUE on
the type refinement (sizes, single-vertex vs path). Read the Necessity Lemma and the letter table. The paper's
honesty section at your corrected novelty level. FINAL = numbered sections.

**KEEPER.** K1841 filed. Verify the type table's purity counts from the artifact. The counter-collision fix: the
claim file needs an atomic lock (three collisions today) — Grace owns, Keeper verifies. Board, rubric.

**CASEY — Lyra's three questions, still open, each maps to an orbit:** (1) when you choose the middle move, do
you look at the two same-colored neighbors, the vertex between them, or the chain running out from it?
(2) is "the edge" the link around the hole or the fence walling a side off? (3) when the middle move is
unavailable, do you move a copy first or the singleton beside it? (Lane E parked at your word; its measurements are kept.)

## STANDING
No EOD before 5pm. Two instruments per number. A lock is a kill, never an amendment. The theorem for n = 24 is
none, and the paper says so. Kittell 1935 is cited as the primary source. Frames travel; seeds are named.

*— Keeper. Round 100: the mechanism became two halves with a control, and the "complexity" became a matrix.*
