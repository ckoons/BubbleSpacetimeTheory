# MILLENNIUM PRE-READ — RH (Lyra), against the K940 review frame
**Lyra, 2026-08-26 (clock-verified at write). Saved artifact per R101 — Monday reads THIS, not my
context. Primary source: `BST_and_RH_an_honest_harvest_2026-08-16.md` (Casey + team, Cal refereed;
status line BINDING per K940: an ATTEMPT, not a proof — RH is Clay-open and remains so). Kill chain
confirmed at source today by grep, not memory.**

## Three-way split

**PROVED (theorems that stand regardless; artifact paths):**
- Rank-2 dilation counts divisors: Σ_{n≤x} d(n) = x log x + (2γ−1)x; **the logarithm is free from
  rank 2** — no cutoff, no regularization (harvest Section 2(1); Elie verification to x = 2×10⁶).
- **Re = ½ derived as the dilation-unitarity axis** — the unique Mellin-isometry line (half-density
  dx/x), the self-dual axis of the SO₀(5,2) spherical principal series (harvest advance 4, Elie).
- **The 2πℏ cell forced, not imposed**: 2π = the dilation sector's compact SO(2) period; quantization
  Berezin–Toeplitz on the Wallach point (F1011). Dimensionful ℏ NOT derived — the caveat travels.
- Two-surface split: composites = bulk (continuous dilation flow), primes = boundary defect
  (d(n) = 2 ⟺ prime); the commit = the Hardy-isometry map between them (F1013).

**APPROACHED (real structure, gap NAMED):**
- **The object gap, located to one number (the make-or-break, harvest advance 6):** does the
  weight-5/2 cone theta (half-integral, forced by odd n_C) Shimura-lift to a weight-4 Euler-product
  eigenform? Grace's check = the quinary form's class number. **Grep today: still OPEN — no
  post-08-16 movement (recent Shimura hits on the board are all Hodge-lane).** And the gap has a
  gap: even the favorable case lands on a MODULAR L-function — a genuine theorem, not Riemann's ζ.
- DH not-poisoned (quinary class-1, Eichler): FAVORABLE, pending the same number.
- **The wall: Weil positivity = RH itself. Not derived.** My earlier claim that BST "derives the
  positivity Connes assumes" is RETRACTED in the harvest ledger (mine, owned, stays owned).

**COMMENTARY (labeled, no shame):**
- The finite-field parallel — RH is Weil's theorem downstairs; the commit drops the phase, so
  running it backwards (lifting GF-world to ℤ) is exactly the hard part. I-tier structural parallel,
  explicitly not a theorem (harvest Section 4).
- "Primality is the negative; composites are the way" — the divisor-count fact is derived; its
  significance for the zeros is interpretive.

## Kill/rescope inherits (confirmed at source)
The harvest doc CARRIES its own six-row retraction ledger (Section 5): Weyl-law kill of the discrete
operator (Elie 5286 — eigenvalue count power-law, not log) · the σ = ½ "kill shot" (target-innocence
fail, holds all n ≥ 4) · tube cone ≠ spacetime cone (F1012) · 7/8 Maslov not 2^N_c · my Weil-positivity
retraction · Elie's "lift → ζ" retraction. K940's attempt-status is the doc's own header. **One frame
correction for Monday: the frame's memory column cites "commitment-path = critical line (F988)" — the
scored ledger should cite the harvest's advance-4 form (Mellin-unitarity axis) as the derived content;
the F988 time-circle gloss is consistent post-K1687 (time's J stayed in the K-center) but is the
weaker citation.**

## Referee-objection list (first draft — what they would actually say)
1. "The Mellin-unitarity line is classical; you found ζ's natural home, not new analysis." (~8/10;
   survivable by scoping: the new content is the geometry FORCING the sector and cell.)
2. "Your zeta is a cone/Epstein zeta; DH says those generically violate RH — wrong object unless the
   lift lands." (~9/10; honest answer = the open make-or-break, named as such.)
3. "Even the favorable lift is a modular L-function; ζ untouched." (10/10; conceded in-doc.)
4. "Weil positivity is the problem, and you don't have it." (10/10; conceded — it IS the wall.)
5. "Divisor asymptotics to x = 2×10⁶ verify Dirichlet, not anything about zeros." (~6/10; concede —
   it validated the rank reading only.)
**Net: "proved" would draw 8–10/10 (frame's estimate confirmed). "Derived critical-line home +
forced cell + make-or-break located to one computable number" draws ~2–3/10 — dispatch-grade honest.**

## Open questions for Monday
1. **Grace's one number** (quinary class number): computed anywhere? If not — is it a 30-minute
   fire-time check, and does it get its own micro-prereg (it is a can-fail)?
2. Does this week's AV-stratification machinery say anything new about the scattering-determinant
   (Eisenstein) sector? New tool, post-dates the harvest, unexamined.
3. Family-sweep the weight-5/2: odd n gives half-integral n/2 generically — is anything 5-specific
   load-bearing in the lift question, or does the make-or-break ride ONLY on the quinary form?
4. The frame-citation fix (F988 → advance 4), one line in the scored ledger.

*— Lyra. One page, saved, context-free. The RH row's honest shape: two derived structures, one wall
with a name, and a make-or-break that has been one computable number for ten days.*

---

## ADDENDUM (16:40 EDT, same day — Casey's direction question, SAVED per the no-held-context rule)

**Casey's question: "base theorems on primality or composites?" Answer: COMPOSITES — and his reframe
upgrades the banked orientation into a proof-shaped target.** The pieces, tiered honestly:

1. **Quantifier argument (cheap theorem):** composite = Σ₁ reachability over multiplication; prime =
   the Π₁ negative. The dilation operator counts reaches (Σd(n)); build on the image, gaps come free.
2. **FTA as boundary-generates-bulk (cheap theorem, Monday micro-prereg candidate):** addition has
   ONE generator (the successor = the tick's arithmetic shadow; Casey: the commit's boundary condition
   is "there is a next integer," the record written to the successor); multiplication is FREELY
   generated by exactly the gaps — primes are the boundary alphabet, composites the bulk it fills.
   The Szegő/Hardy shape in raw arithmetic. Provable as a restatement theorem, not a metaphor.
3. **The explicit formula as interference (derived-adjacent):** ψ(x) = x − Σ_ρ x^ρ/ρ − ⋯ = successor
   flow minus boundary corrections; **the zeros = the mismatch spectrum between counting and dilation.**
4. **The wall, relocated not removed (state plainly Monday):** zeros-on-the-line = "the mismatch
   semigroup is contractive with no unitary part off the axis" (the Lax–Phillips face of Weil
   positivity). One-sided contractivity + the classical functional equation (ρ ↔ 1−ρ) ⟹ full RH.
   Same difficulty; NEW: it is now phrased in commit-native vocabulary where the corpus holds actual
   theorems (T2543 permanence, the contractive face). The review should weigh whether this face of
   the mountain is climbable with our tools — as a QUESTION, not a claim.

*Nothing here fires this week (budget). Monday inherits the direction with its wall named.*

**5. (capture completeness, 16:44 EDT):** the reframe's decisive property, stated for Monday: it is the FIRST formulation where BOTH flows are objects the corpus already owns — counting = the commit clock (the tick's arithmetic shadow, "there is a next integer" as the boundary condition, the record written to the successor), multiplication = the dilation flow (banked, rank-2). No imported object is being hunted; ζ's zeros are the spectrum of the disagreement between two owned flows. Casey's target as given: prove the intersection of the minimum-energy band on the Shilov boundary with Re = 1/2 at least on the multiplication gaps — the numbers reached only by counting.

**6. (Casey, 2026-08-27 morning, captured 07:29 EDT):** "Can Re = 1/2 be viewed as a tangent to the composites — the limit of multiplication over a range, with the line as what multiplication cannot reach?" **Answer: YES, and it is theorem-shaped — the Nyman–Beurling–Báez-Duarte criterion: RH ⟺ the counting baseline (constant function) is the L²-limit of integer dilations of the sawtooth {x/n} (the successor's fractional residue) in the Mellin-unitarity space.** "Limit of multiplication" = reachable-only-in-the-limit, exactly. The tangency has a RATE: d_N² ~ C/log N with C = Σ 1/|ρ|² over on-line zeros (Burnol/Báez-Duarte) — the zeros are the tangency coefficients; an off-line zero = a fixed reachability defect that never closes. CORRECTION carried: the gaps (primes) live in x-space; the line carries their SPECTRUM (explicit-formula duality) — signal vs frequencies. Minimum-energy reading: Θ = sup Re(ρ) = the mismatch's critical exponent; RH ⟺ Θ = 1/2 = square-root scale = the gentlest possible failure of multiplicative reach. BST rendering: sawtooth = tick residue, dilation orbit = commit flow, RH = boundary-alphabet completeness of the interior — the Hardy/Szegő shape of pts 2/4, now carrying RH itself. Connects to pt 4: the Báez-Duarte defect not closing ⟺ a unitary part off the axis. **Monday candidates: (i) the d_N tangency-rate as a computable toy (Elie, can-fail, BST-independent check); (ii) whether the derived Mellin-unitarity axis + T754 measure give the criterion's weight dx/x² any forced status.**
