---
node_type: k_audit
id: K1707
title: "HOLD on the Yang–Mills flagship number (λ₁=C₂=6 'derived gap') — Cal (§614) and Elie both threw a stop-sign, and the corpus reconnect DECISIVELY confirms them. This is a productive walk-back on the paper's core claim, caught BEFORE it shipped. Also: Cal's baryon-ledger hash INDEPENDENTLY VERIFIED (§603). ★ HASH VERIFY: sha256(notes/Cal_BARYON_SECTOR_LEDGER_authoritative_vetted_2026-08-19.md) = f02728a882b3c35bc7ea3cd7674e0399e5a9d71e8c5edc49b47950ff9cdce64f = Cal's posted hash. MATCH. I am the second verifier, not the locker (§603 satisfied). Ledger accepted. ★ THE HOLD — 'λ₁ = C₂ = 6 is a derived gap' is NOT safe, two independent reasons, both correct: (A) CAL'S IDENTITY POINT: the corpus (BST_1920_WeylGroup_Theorem:273) gives the holomorphic-discrete-series Casimir C₂(π_k) = k(k−n) for SO₀(n,2); the Bergman space is π_{n+1} (weight k=n+1), so C₂(π_{n+1}) = (n+1)(n+1−n) = (n+1)·1 = n+1 = 6. But C₂ IS DEFINED as n_C+1 in the corpus. So 'the gap equals C₂' = the eigenvalue formula at the Bergman weight set equal to a quantity DEFINED to be that same expression — it CANNOT fail (same class as P²=P, #Rac, reductive-by-definition). Not a convergence, an identity. (B) ELIE'S OPERATOR-UNPINNED POINT: the value is not even robust across the corpus's OWN formulas — FOUR different eigenvalue expressions are on the record: k(k−5) [BST_1920 discrete-series Casimir → n+1=6 at weight n+1, but that's a single rep-Casimir=n+1, i.e. (A)]; k(k+5) [T1452 positive tower → λ₁=6, but this is the round S⁶ Laplacian ℓ(ℓ+n−1) with n=6, where λ₁=n=DIMENSION identically → 'gap=6' just restates 'space is 6-dim,' DIMENSION-GENERIC, the 4th instance after 25/4, √(π^n/n), Condition-5]; k(k+6) [T1301, DEMOTED → 7]; k(k+3) [so(5) Casimir on K-type (k,0), ⟨(k,0),(k,0)+2ρ⟩ = k(k+3) → λ₁=4]. Four formulas → {6,6,7,4}. The value '6' is NOT a robustly-derived spectral gap; it depends entirely on which operator you pick, and two of the four don't give 6. I verified Elie's arithmetic: S^n Laplacian ℓ(ℓ+n−1) at ℓ=1 = n (dimension) ✓; so(5)=B₂ Casimir of (k,0) with ρ=(3/2,1/2) = k(k+3), at k=1 = 4 ✓. ★ WHAT SURVIVES (real, bankable — the HOLD is on the VALUE-CLAIM, not the physics): (1) the gap is NONZERO because the domain is BOUNDED (bounded ⟹ discrete spectrum ⟹ nonzero lowest excitation; decompactify R→∞ ⟹ gap→0). THIS is the genuine content — why the strong force has a gap and EM does not — and it does NOT depend on the value. (2) the value, whatever it is, is GEOMETRY-FIXED with NO FREE PARAMETER. Cal's honest claim stands: 'the gap is fixed by the domain's dimension, nothing tuned' — a derivation. '= C₂, one of BST's five integers' is dressing that makes it look like a discovery. (3) THE GLUEBALL NUMBER (1720 MeV @ 0.6%) is SEPARATE and UNTOUCHED — it comes from the adjoint ratio (11/6)·938 (T1788), not from this λ₁ construction; Elie flagged this explicitly. The Clay-relevant prediction is intact. ★ GRACE'S PIN RE-SCOPED (the discriminator, load-bearing): pin the ONE physical mass-gap operator from the primary source and answer TWO things — (i) is its value DEFINITIONALLY forced (the discrete-series Casimir of the Bergman rep = n+1 by construction → honest claim is 'geometry-fixed, no free parameter,' NOT '=C₂ discovery') or a genuine tower-gap that could have come out otherwise? (ii) does the physical operator give k(k+3)→4 or k(k+5)→6? Grace's Round-12 'confirmation' evaluated C₂ at weight n+1 and got n+1 — that RE-DERIVES the identity (A), it does NOT settle (B). ★ LYRA — STAND DOWN Section 2's 'one winding at C₂=6' to 'a nonzero geometry-fixed gap; its value is set by the domain with no free parameter; the specific integer awaits the operator pin (k(k+3)→4 vs k(k+5)→6)'. KEEP Section 0's guards (they're good). ADD Cal's THIRD same-name guard: C₂ carries ≥3 readings all =6 at n_C=5 (the integer n_C+1; the commitment-flow Casimir 2n−4; the Bergman-rep Casimir) — say WHICH. HOLD the two-paper package intro until the operator pin resolves + Cal's explanatory-framing vet. ★ ADOPTED STANDING DISCIPLINE (Cal's structural fix, §614 — it would have caught all THREE C₂ incidents: Condition-5-as-forcing-lever, the θ_W confusion, and this): any symbol that is SOMETIMES AN INTEGER and SOMETIMES A FUNCTION OF n gets SUBSCRIPTED — C₂^int (the integer 6) vs C₂(n)=2n−4 or n+1 (the function) — so the collision cannot survive a grep. This is the most dangerous same-name class (both readings always true, never the same object). ★ NET: HOLD the 'gap=C₂=6 derived' claim (identity + operator-unpinned). BANK: gap EXISTS (boundedness, the real physics), value geometry-fixed/no-free-parameter, glueball 1720@0.6% untouched. Grace pins the operator. Paper stands down the value-claim, keeps existence + guards. This is the reconnect-before-claim discipline firing on the flagship — the 10th same-name of the week, caught before ship."
date: 2026-08-19
author: Keeper
verdict: "HOLD on 'λ₁=C₂=6 derived gap' — Cal+Elie right, corpus-confirmed. (A) IDENTITY (Cal): C₂(π_{n+1})=(n+1)·1=n+1, and C₂:=n_C+1, so 'gap=C₂' can't fail (P²=P class). (B) OPERATOR-UNPINNED (Elie, verified): corpus has 4 formulas — k(k−5)→6[=identity], k(k+5)→6[S⁶ Laplacian, 6=dim, dimension-generic], k(k+6)→7[demoted], k(k+3)→4[so(5) Casimir]. Value not robust. SURVIVES: (1) gap NONZERO because domain BOUNDED (real physics, decompactify→0); (2) value geometry-fixed/no-free-parameter (Cal's honest claim); (3) GLUEBALL 1720@0.6% SEPARATE/untouched (11/6·938, not this λ₁). GRACE re-scoped: pin the ONE physical operator — definitionally-forced (n+1) or genuine tower-gap? k(k+3)→4 or k(k+5)→6? (her Round-12 eval re-derived the identity, didn't settle it). LYRA: stand down Section 2 to 'nonzero geometry-fixed gap, value pending operator pin'; keep Section 0 guards; add the 3rd C₂ same-name guard; HOLD package intro. ADOPTED: subscript sometimes-int/sometimes-f(n) symbols (C₂^int vs C₂(n)) — would've caught all 3 C₂ incidents. HASH VERIFIED: Cal's baryon ledger sha256=f02728…ce64f matches (§603, I'm 2nd verifier). Nothing pushed. CP existence-only."
related: [K1706, K1705, K1704, "BST_1920_WeylGroup_Theorem", T1452, T1301, T1788, "Cal §614", "feedback_sweep_the_family_before_calling_a_clean_number_a_signature", "feedback_same_name_different_object", "feedback_keeper_25_productive_walkback_boundary", "feedback_commit_the_checker_half_blind"]
---

# K1707 — HOLD the YM gap value (Cal + Elie right); Cal's hash verified

## Hash verify (§603)
`sha256(Cal_BARYON_SECTOR_LEDGER_authoritative_vetted_2026-08-19.md) = f02728a882b3c35bc7ea3cd7674e0399e5a9d71e8c5edc49b47950ff9cdce64f` = Cal's posted hash. **Match.** I'm the second verifier, not the locker. Ledger accepted.

## The HOLD — "λ₁ = C₂ = 6 is a derived gap" is not safe (two independent reasons)
**(A) Cal's identity.** BST_1920 gives the discrete-series Casimir `C₂(π_k)=k(k−n)`; the Bergman space is `π_{n+1}`, so `C₂(π_{n+1})=(n+1)·1=n+1=6`. But `C₂` **is defined** as `n_C+1`. So "gap = C₂" is the formula at the Bergman weight set equal to a quantity defined to be that expression — it **cannot fail** (P²=P class). An identity, not a convergence.

**(B) Elie's operator-unpinned (verified).** The corpus has **four** eigenvalue formulas: `k(k−5)`→6 [=(A)]; `k(k+5)`→6 [but this is the round S⁶ Laplacian `ℓ(ℓ+n−1)`, where `λ₁=n=dimension` — "gap=6" restates "6-dimensional," dimension-generic]; `k(k+6)`→7 [T1301, demoted]; `k(k+3)`→4 [so(5) Casimir of `(k,0)`, `⟨(k,0),(k,0)+2ρ⟩=k(k+3)`]. Four formulas → {6, 6, 7, 4}. The value is **not robust** — it depends entirely on which operator you pick, and two of four don't give 6.

## What survives (the HOLD is on the value-claim, not the physics)
1. **The gap is nonzero because the domain is bounded** (discrete spectrum; decompactify → 0). This is the genuine content — why the strong force is gapped and EM isn't — independent of the value.
2. **The value is geometry-fixed, no free parameter.** Cal's honest claim stands: "fixed by the domain, nothing tuned" — a derivation. "= C₂" is dressing.
3. **The glueball (1720 MeV @ 0.6%) is separate and untouched** — it comes from `(11/6)·938` (T1788), not this λ₁. The Clay-relevant prediction is intact.

## Grace's pin re-scoped (the discriminator)
Pin the ONE physical mass-gap operator from the primary source: (i) is its value **definitionally forced** (Bergman-rep Casimir = n+1 → honest claim "geometry-fixed," not "= C₂ discovery") or a genuine tower-gap? (ii) `k(k+3)→4` or `k(k+5)→6`? Her Round-12 eval re-derived the identity (A); it did not settle (B).

## Lyra
Stand down Section 2's "one winding at C₂=6" → "a nonzero geometry-fixed gap; value set by the domain, no free parameter; the specific integer awaits the operator pin (`k(k+3)→4` vs `k(k+5)→6`)." Keep Section 0's guards. Add the **third** C₂ same-name guard (integer `n_C+1` vs commitment-flow `2n−4` vs Bergman-rep Casimir — say which). **Hold** the package intro until the pin resolves + Cal's vet.

## Adopted standing discipline (Cal §614)
Any symbol that is **sometimes an integer and sometimes a function of n** gets subscripted — `C₂^int` (the integer 6) vs `C₂(n)` (the function) — so the collision can't survive a grep. Would have caught all three C₂ incidents (Condition-5, θ_W, this). The most dangerous same-name class: both readings always true, never the same object.

— Keeper, K1707, 2026-08-19. HOLD the "gap=C₂=6 derived" claim (identity + operator-unpinned); bank gap-existence (boundedness), geometry-fixed value, glueball 1720@0.6% untouched. Grace pins the operator; Lyra stands down the value-claim, keeps existence+guards; package on hold. Cal's hash verified. Subscript-the-overloaded-symbol adopted. Nothing pushed. CP existence-only.
