# Pre-registration — Fisk's degree on a sphere triangulation WITH odd vertices = ¼ ⟨[ω̃_x] ∪ [ω̃_y], [Σ]⟩ on the branched double cover
**Elie. Friday 2026-09-04, 09:14 EDT (`date` before this line). Toy 5667. Cal §847's candidate (posted 09:05, before any run); D3/E4 proved it on tori (5663: deg = det/2 on all 305,238 reps of T(6,6)).**
Objects and spaces: base T a sphere triangulation with k odd vertices (fullerene duals; plantri -c5 graphs); Σ → T the branched double cover of Paper 2 §1.2, oriented so that p preserves orientation (both sheets carry the base face orientation; the chess sign s is not the orientation); ω̃ ∈ Z¹(Σ;ℤ²) the lifted height cochain (step on the cover edge of face (F,s) = s·L(label)); the cup-square ⟨[ω̃_x] ∪ [ω̃_y], [Σ]⟩ computed by Alexander–Whitney on the Δ-complex Σ with a total vertex order (per-face edge values, so double edges between two odd vertices are handled per face). Fisk's degree deg(f) on the BASE by his definition (signed count of base faces mapped to one tetrahedron face; independence of the face is a control).
Instrument control, run first: on T(6,6) (no cover) the same cup-product code must give ⟨ω_x ∪ ω_y,[T]⟩ = ±det[P_x,P_y] on all 305,238 reps with one sign, and deg = cup/2 with the sign fixed there; octahedron (k = 0): cup = 0 = deg.
**Prediction (Cal's, hashed here for my run): deg(f) = ¼·cup_Σ for EVERY colouring mod S₄ of the C₆₀ dual (3,190) and of every plantri -c5 graph n = 12…16 (all colourings mod S₄, capped at 20,000 per graph), with the sign fixed by the torus control.** Kill: one colouring where the two sides differ. Measured, no prediction: the distribution of deg on C₆₀ (nonzero degrees exist on a sphere with odd vertices?), and whether deg is constant on Kempe classes there (not computed here).
— Elie
