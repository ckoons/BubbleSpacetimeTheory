Team turn — 2026-07-03 next. The mass sector reframed: derive BARE masses; pin the object, don't fit the number.

Two new frame docs to read first (they change the mass-sector target):
- `BST_Two_Layer_Mass_Model_and_Debris_Frame_2026-07-03.md` — mass = bare (substrate, clean) + dressing (glueball/membrane, variable). We derive the BARE mass; the dressing is a separate Layer-2 prediction.
- `BST_Why_We_Derive_The_Method_Is_The_Armor_2026-07-03.md` — reproducible, computed, pre-audited derivations are the defense against *ad AI-num*. Reach for the simplest, one-line-checkable forms.

**SOD check first** (registry drift still open → Lyra's backfill).

## The reframe that changes the work

We stop chasing observed (dressed, fuzzy) masses and derive **bare** masses against the clean electron:
- leptons → π-forms (have them) · top → v/√2 (undressed, have it) · **down → N_c² × {1, 20, 900} = {9, 180, 8100}** (the target)
- m_s/m_d = rank²·n_C = 20 is already a **bare-mass 0σ match**. The old "down-row" failed because it used the wrong power (N_c not N_c²) and a GUT frame (quark-to-lepton pairing). Bare-vs-electron dissolves both.

## The gate — Grace's insight, sharpened

The down-ladder does NOT close by finding a weighting number. Grace showed the geometry forces the multiplicity ratio **5/3**, not 20, and 20 has ≥3 fit-routes (form-cheap) — and Elie flagged that 20/(5/3)=12=2C_2 is just another fit. **So there is no factor to fish.** The real gate:

> **What IS the F453 k_s object?** — orbital multiplicity (→5/3), fiber cohomology (→4), or a forced composite. Pin what k_s actually is, and the bare ladder N_c²×{1,20,900} is forced-or-not, no fiat.

This is a **joint Grace (geometry) + Lyra (rep-theory)** reconciliation of the F453 regularized-determinant object. Not a solo forcing.

## Per-CI

**GRACE + LYRA (joint, highest-leverage):** pin the F453 k_s object. Grace: what the geometry/strata actually give (multiplicity, cohomology). Lyra: what the rep-theory zero-mode object is. Reconcile to ONE definition, then read off whether it forces the bare down powers {N_c², rank²·n_C, N_c²·n_C}. **Pin the object; do NOT fit 20** (that's the value-reaching the pipeline forbids).

**LYRA (solo):** close the ν-scale coefficient gaps — is m_ν/m_e = f·α²/(6π⁵) with {7/12, 10/3} *forced* or still value-forms? (The scale simplification 4→3 pure scales is real structure; make it derived-strong.) Finish the registry backfill — 8 remaining, **fresh full-source reads, not grep-guesses**.

**ELIE:** checker on the joint k_s object (structure-forcing vs value-reaching — you already flagged the 12=2C_2 trap; hold that line). Then open the **Layer-2 thread**: what sets the membrane-dressing per quark (the three-commitment collection during escape)? **Top is the built-in control — it must come out ≈ zero dressing** (it decays before hadronizing). If you can put a number on the dressing, Layer 2 becomes a prediction, not a name.

**KEEPER:** score/adjudicate; keep the two-layer frame honest — **Layer 1 (bare) is derivable now; Layer 2 (dressing) is a name until it's computed.** Hold the bar; the k_s object is derived-strong only when forced, not fitted. Report the delta.

**CAL:** post-landing cold-read. α tier-call reserved for Casey.

## Disciplines (the armor)

- **Derive BARE, not dressed.** The clean value is the minimum/floor; the spread is Layer 2.
- **Pin the object, don't fit the number.** A forced object-definition beats any weighting factor (12=2C_2 is form-cheap).
- **Simplest, one-line-checkable forms** — reproducible from the five integers (ad-AI-num armor).
- **Terminal is a finished answer** (muon/τ floors, sin²θ_W runner, δ_PMNS forward) — don't reopen.
- Five-Absence · target-innocence · σ scheme-aware · cheapness soft-flag · pre-audit our own weaknesses.

## Honest state / EOD target

Count 8. Down-row repair OPEN, gated on the k_s object (joint). ν-scale simplified 4→3 (structure derived, coefficients open). Registry 8/16. **EOD win = the k_s object pinned to ONE forced definition** (which either forces the bare ladder or honestly doesn't), + ν-scale coefficients closed-or-flagged, + backfill advanced. Derived-strong grows only if the object forces the powers.

## Standing for Casey

Down-row ratification (structural-MISS, repairable via the bare frame) · σ-metric + cheapness ratification · α tier-call (Wyler-informed) · CLAUDE.md line 0.5 · and now: the **two-layer mass model** as the mass-sector frame (ratify the reframe).

— Keeper, next turn. Derive the bare masses; pin the F453 object; the number is forced or it isn't. Mechanism is the prize; simplicity is the armor.
