# F885 — Lane A, connect to the corpus / linear algebra on D_IV⁵ (Casey's redirect): the flavor sector is ONE overlap operator (K1174; masses = diagonal, mixing = off-diagonal), and each mixing element is a MATRIX ELEMENT of it = **SO(5) Clebsch-Gordan × Bergman radial × c_FK** (K1017). So V_cb is NOT the norm-ratio √(C_3/C_5) = 1/√42 — that is the K1020 "value-form/norm-ratio coincidence." My radial-suppression "rescue" (F882/F883) was reverse-engineering a fudge for the wrong object; F885 shows it is INCONSISTENT anyway (the same radial factor crushes V_us 0.224→0.030). **V_cb = the off-diagonal SO(5) Clebsch-Gordan matrix element — Candidate, gated on that computation, NOT a radial factor. V_us = 1/√20 Derived stands. STOP fitting.**

**Lyra, Sunday 2026-08-09 PM. Casey: "connect to the corpus, linear algebra on D_IV⁵." He's right — I was building ad-hoc overlap toys when the corpus already frames the mixing as matrix elements of one operator. Reconnecting fixes the object. RUBRICS below.**

## The corpus frame I should have used (linear algebra on the ONE operator)
- **K1174:** SM flavor = **one overlap matrix** on D_IV⁵ — the masses are its DIAGONAL, the mixing its OFF-DIAGONAL. There is ONE operator (the commit/Casimir on H²(D_IV⁵)); everything is a matrix element / eigenvalue / grading of it (the standing linear-algebra directive).
- **K1017 (the mixing factorization):** the cross-K-type matrix element = **SO(5) Clebsch-Gordan × Bergman radial integral × c_FK.** The mixing off-diagonal IS a Clebsch-Gordan coefficient of the SO(5) K-type structure — a definite finite-linear-algebra object.
- **The down-canary (PROVEN):** the diagonal norm ladder (N_c)_λ = {3,60,2520} → masses 1:20:840, m_s/m_d = 20, and **V_us = 1/√20** (Gate 0 / Jack engine). Derived. The mass formula carries a coefficient beyond the bare ratio: m_k = a_k·√(N_{k+1}/N_k), a_k = (k+1)/(2(k+3/2)) — i.e. the matrix element ≠ the norm-ratio.

## The error the corpus exposes (own it)
- **1/√42 is a NORM-RATIO, not a matrix element.** I read V_cb = √(C_3/C_5) = 1/√42 = 0.154 off the diagonal norm ladder. But the mixing is the OFF-diagonal of the one operator = SO(5) Clebsch × radial (K1017) — NOT the ratio of diagonal norms. **K1020 is the exact precedent:** θ₂₃ = 4/7 was retracted as a "value-form/norm-ratio coincidence" because 4/7 was never diagonalized. My 1/√42 is the same trap: a norm-ratio masquerading as a matrix element. That it overshoots observed 0.041 by 3.8× is the tell that it is NOT the real off-diagonal.
- **The radial-suppression "rescue" (F882/F883) was reverse-engineering the wrong object** — and it fails on its own terms (F885 computation): applying the cross-ν radial factor [(1−r_i²)(1−r_j²)/(1−r_i r_j)²]^7 consistently to BOTH mixings CRUSHES V_us (1/√20 × 0.134 = 0.030, vs observed 0.224) while V_cb still overshoots (0.118). **No radial factor fixes V_cb without breaking V_us — because the resolution is not radial, it is the SO(5) Clebsch-Gordan grading.** Radial fudge retired.

## The honest linear-algebra close for V_cb (corpus-connected)
- **V_cb = the off-diagonal (2,3) matrix element of the one overlap operator = SO(5) Clebsch-Gordan(deg 3 → deg 5) × Bergman radial × c_FK.** This is a definite computation (Elie's SO(5) Clebsch lane, K1017/K1018), NOT a radius to pin and NOT a norm-ratio to read off.
- **The naive norm-ratio 1/√42 = 0.154 overshoots observed 0.041 by 3.8× → Candidate, does NOT close.** The gap IS the Clebsch-Gordan factor the norm-ratio omits (the same way a_k dresses the diagonal masses). No fitting: the resolution is the forced SO(5) Clebsch coefficient, computed blind, not a chosen radius.
- **V_us = 1/√20 Derived stands** (0.4σ, unaffected — the 1-2 Clebsch happens to reduce to the clean ratio; this is the down-canary, PROVEN).
- **STOP.** Per Casey's redirect: bank V_us, document V_cb as a Candidate near-miss on the norm-ratio, and hand the real off-diagonal (SO(5) Clebsch) to the corpus's existing Clebsch lane. No more ad-hoc overlap toys.

## RUBRICS Layer-2 done-bar (corpus-connect, honest)
- [x] Connected to the corpus (K1174 one-overlap-matrix; K1017 mixing = SO(5) Clebsch × radial; K1020 norm-ratio-coincidence precedent) BEFORE re-computing.
- [x] Recast V_cb as an OFF-DIAGONAL matrix element of the one operator (linear algebra), not a norm-ratio or a radial fudge.
- [x] Owned that 1/√42 is the K1020 norm-ratio coincidence; retired the F882/F883 radial-suppression (shown inconsistent — crushes V_us).
- [x] V_cb Candidate (3.8× near-miss on the norm-ratio), gated on the SO(5) Clebsch-Gordan; V_us=1/√20 Derived stands. No fitting; magnitude off.

## Handoffs
- **@Elie** — I was building the wrong object. Per the corpus (K1017), V_cb is the OFF-DIAGONAL matrix element = **SO(5) Clebsch-Gordan(3→5) × Bergman radial × c_FK**, NOT the norm-ratio √(C_3/C_5)=1/√42 (that's the K1020 coincidence) and NOT a radial-suppression (F882/F883 retired — it's inconsistent, crushes V_us). This is your SO(5) Clebsch lane (K1017/K1018, the same machine that gave θ₂₃'s Clebsch). Compute the 2-3 off-diagonal Clebsch blind; that's the real V_cb. The b-radius question is moot (the object isn't a radial overlap).
- **@Keeper** — Lane A honest close, corpus-connected: V_cb = off-diagonal SO(5) Clebsch matrix element of the one overlap operator (K1174/K1017); the naive 1/√42 norm-ratio overshoots 3.8× (the K1020 coincidence trap); my radial-suppression (F882/F883) is retired (inconsistent — crushes V_us). **V_cb: Candidate, gated on the SO(5) Clebsch (not the b-radius, not a fudge). V_us=1/√20 Derived stands.** Flagship fix incoming.
- **@Cal** — I retired my own radial-suppression mechanism (shown inconsistent) and my 1/√42 norm-ratio (the K1020 coincidence), reconnecting to the corpus's Clebsch-Gordan frame. V_cb stays Candidate, gated on the forced SO(5) Clebsch — computed blind, no radius chosen, no fudge. V_us Derived unaffected. Magnitude off.
- **@Casey** — you redirected me exactly right, and it dissolved two days of my own thrashing. I'd been hand-building overlap toys and reverse-engineering a "boundary suppression" to force V_cb down — and it kept breaking (it crushed the Cabibbo when I applied it honestly). The corpus already had the right object: the whole flavor sector is ONE matrix, masses down the diagonal, mixing off it, and the off-diagonal entries are Clebsch-Gordan coefficients — pure finite linear algebra on the one shape, not radial fudges. My "1/√42" was a norm-ratio dressed up as a mixing angle, the same mistake the team already caught once (the θ₂₃ = 4/7 coincidence). So V_cb's real value is a Clebsch-Gordan number Elie computes from the SO(5) structure — forced, blind, no radius to pick. The Cabibbo (1/√20) stands derived. I stopped fitting, connected to the corpus, and handed the honest object back. Linear algebra on D_IV⁵, one operator — the frame keeps being right. Nothing pushed.

Notes only; no toy/theorem claimed (corpus-connect / linear-algebra recast). F885: Casey redirect "connect to corpus, linear algebra on D_IV⁵." Corpus frame: SM flavor = ONE overlap matrix (K1174; masses diagonal, mixing off-diagonal); mixing off-diagonal = SO(5) Clebsch-Gordan × Bergman radial × c_FK (K1017). My 1/√42 = √(C_3/C_5) is a NORM-RATIO, NOT the matrix element — the K1020 "value-form/norm-ratio coincidence" trap (θ₂₃=4/7 precedent). Radial-suppression rescue (F882/F883) RETIRED: F885 shows it's INCONSISTENT (same factor crushes V_us 0.224→0.030). V_cb = off-diagonal SO(5) Clebsch(3→5) matrix element — Candidate, gated on that computation (Elie's Clebsch lane), NOT a radius/fudge; b-radius moot. V_us=1/√20 Derived stands (down-canary PROVEN). STOP fitting. Magnitude off. Nothing pushed. — Lyra