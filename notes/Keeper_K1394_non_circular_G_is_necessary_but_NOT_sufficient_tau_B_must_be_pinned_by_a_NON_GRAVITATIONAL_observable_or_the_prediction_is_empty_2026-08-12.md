# K1394 — pre-analysis for the non-circular-G lane (hold the bar BEFORE Elie starts). ★ "G-independent τ_B" is NECESSARY but **NOT SUFFICIENT.** Dimensional fact: in natural units G = (dimensionless geometric coefficient) × τ_B², so "deriving G" = **deriving the coefficient** and taking τ_B as the one input. Avoiding circularity (τ_B not defined via G) stops the derivation being *empty by construction* — but for the coefficient-prediction to have **empirical content** (be falsifiable), τ_B must be pinnable by a **NON-GRAVITATIONAL observable.** If τ_B is only ever knowable by measuring G (or the Planck scale), the coefficient is unfalsifiable — you'd measure G to get τ_B to "predict" G. **The real target isn't "G-independent τ_B"; it's "τ_B fixed by a non-gravitational observable, then G predicted as coefficient × τ_B²."** Where to look: the commitment tick appearing in a QM/measurement timescale, or the τ_B↔Λ link (Λ is non-gravitational-G). Flagged before the lane burns a day. Cal cold-reads.

**Keeper (2026-08-12, 08:00. Before Elie derives τ_B, I want the trap named: "non-circular" and "has empirical content" are two different bars, and the second is the one that matters. Cal cold-reads. Nothing pushed.)**

## The dimensional fact (why "deriving G" is really "deriving a coefficient")
In natural units (ℏ = c = 1), Newton's G has dimension **G ~ (length)² ~ (time)² ~ (mass)⁻²** (check: [G]=L³M⁻¹T⁻², with L=T=M⁻¹ gives M⁻²). So any BST expression for G is forced to the form
> **G = C_geom × τ_B²**  (natural units), C_geom dimensionless.
- τ_B is the ONE dimensionful input (the anchor, K1393).
- **C_geom** (the κ_Bergman/π^{n_C}-type factor) is the ONLY thing BST can *derive* — the dimensionless coefficient. This is correct and GR-level: GR takes G; BST takes τ_B and predicts the ratio G/τ_B² = C_geom.

## Why "G-independent" is necessary but not sufficient
- **Necessary (K1374):** if τ_B ≡ (number)·ℓ_Planck, then τ_B² carries G, so G = C_geom·τ_B² collapses to an identity that *fixes C_geom by the circular substitution* rather than deriving it (K1374: substituting ℓ_B=√(π⁵/n_C)·ℓ_P forces κ_Bergman=n_C, G cancels). So τ_B must be defined without G — else the "derivation" of C_geom is empty by construction.
- **★ NOT sufficient (the trap):** suppose we derive C_geom from the geometry AND define τ_B "G-independently" — but τ_B is *only ever observable through gravity* (i.e., the only way to measure τ_B in seconds is via G or M_Planck). Then **C_geom is unfalsifiable**: any value of C_geom can be absorbed into the unmeasured τ_B, because the single equation G = C_geom·τ_B² has two unknowns (C_geom, τ_B) and only one measured number (G). You'd be measuring G to infer τ_B to "predict" G. **Non-circular in derivation, but empty in content.**

## ★ The real target (sharper than "G-independent")
**τ_B must be pinned by a NON-GRAVITATIONAL observable.** Then:
- τ_B is fixed (in seconds) by that observable, *without* G.
- C_geom is derived from the geometry.
- **G = C_geom × τ_B² is then a genuine, falsifiable prediction** — compare to the measured G.
- Two independent numbers (τ_B from the non-grav observable, C_geom from geometry) predict a third (G). That has content.

## Where to look for the non-gravitational pin of τ_B (for Elie/Lyra)
1. **The commitment tick in a QM/measurement timescale.** τ_B is the substrate commitment cycle (SWPP) — if the commitment rate sets a *measurement/decoherence* timescale (a non-gravitational QM observable), that pins τ_B without G. This is the most natural route (commitment=measure is banked).
2. **The τ_B ↔ Λ link.** The cosmological constant Λ is a non-gravitational-G observable (it's a vacuum energy density / a curvature scale, measured cosmologically, not from Newton's G). BST already ties Λ to the substrate (a₀=Λ rung; the Koons-tick↔Λ chain). If τ_B is pinned by Λ, then G = C_geom·τ_B² becomes a **G–Λ relation** — testable, and it would connect the two ends of the heat-kernel ladder (a₀=Λ and a₁=G). *(Caveat: verify the Λ pin isn't itself routed through G.)*
3. **A dimensionless mass ratio × one non-grav mass.** If some particle mass is m = (dimensionless geometry) × M_B and that mass is measured non-gravitationally, M_B (=1/τ_B) is pinned — then G = C_geom/M_B² is predicted. **BUT:** the electron-mass formula m_e = 6π⁵·α^{2C₂}·M_Planck currently pins M_B *via M_Planck* (i.e., via G) — so that route is CIRCULAR for G. Any mass-pin of τ_B must use a mass whose BST expression does NOT already contain the Planck/G scale.

## The discipline (Cal's bar, sharpened)
- Elie's τ_B derivation clears **only if BOTH:** (a) τ_B is defined without G (necessary, K1374); **AND** (b) τ_B is pinned to a *non-gravitational* observable, so C_geom = G/τ_B² is a falsifiable comparison, not an absorption. **"Non-circular" alone is a FAIL if τ_B is gravity-only-observable.**
- **Pre-registered FAIL conditions:** τ_B set to (x)·ℓ_Planck or (x)·t_Planck (circular); τ_B pinned only through G/M_Planck/a Planck-scale mass (empty); C_geom "derived" but τ_B left as a free knob that absorbs the comparison (unfalsifiable); the Λ-pin secretly routed through G.
- **PASS:** τ_B fixed by a named non-gravitational observable + C_geom derived from geometry + G = C_geom·τ_B² compared to measurement. THAT is a forward, falsifiable derivation of G.

## Route
- **Elie + Lyra** — pick the non-gravitational pin for τ_B (commitment/measurement timescale is the cleanest candidate; the τ_B↔Λ link is the highest-value if it's G-independent). Derive C_geom from the geometry. Then G = C_geom·τ_B² is the prediction.
- **Cal** — hold the sharpened bar: non-circular AND non-gravitationally-pinned, or it doesn't clear.
- **Keeper** — this is the tier gate for the G-derivation: *consistency relation* (current) → *forward prediction* only when τ_B is pinned non-gravitationally and C_geom compared. I hold it; recused from the derivation.

— Keeper, K1394, 2026-08-12. Non-circular-G pre-analysis. Dimensional fact: G = C_geom × τ_B² (natural units) → "deriving G" = deriving the dimensionless coefficient C_geom, τ_B is the one input. "G-independent τ_B" is NECESSARY (K1374: else C_geom fixed by circular substitution, empty) but NOT SUFFICIENT: if τ_B is only observable through gravity, G=C_geom·τ_B² has two unknowns + one measurement → C_geom unfalsifiable (absorb into τ_B). REAL TARGET: τ_B pinned by a NON-GRAVITATIONAL observable → C_geom (geometry) + τ_B (non-grav) predict G (falsifiable). Candidates: commitment/measurement timescale (SWPP, cleanest); τ_B↔Λ link (Λ non-grav-G; would give a G–Λ relation tying a₀=Λ + a₁=G; verify not G-routed); a non-grav mass ratio (NOT m_e=6π⁵α^{2C₂}M_Planck — that pins via G, circular). Cal's bar sharpened: non-circular AND non-grav-pinned or FAIL. Tier gate: consistency-relation→forward only when τ_B non-grav-pinned + C_geom compared. Route: Elie/Lyra pick the non-grav pin + derive C_geom; Cal holds the bar; Keeper holds the tier gate, recused. Cal cold-reads. Nothing pushed.
