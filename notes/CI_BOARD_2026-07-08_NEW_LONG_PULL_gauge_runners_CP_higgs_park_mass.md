Team LONG PULL — 2026-07-08 (NEW DIRECTION). PARK the mass sector — work the gate-independent frontier. PRIMARY: the gauge runners (α_s, sin²θ_W) — the fresh, forward, "differences are data" testbed. Plus CP-from-N_c=3, α root-lock #2, the Higgs form. `date` + SOD.

## Directive (Casey): work the rest, come back to mass later
The flavor MASS + MIXING-MAGNITUDES sector is **PARKED** — it circled (5+ reframes, no landing), it's gated on the generation positions, and we return to it after progress elsewhere. **Do NOT get pulled back into the position/mass gate.** Work the items that are genuinely forward and OFF that gate.

## PRIMARY (fresh, forward, off the mass gate) — the GAUGE RUNNERS · ELIE lead + GRACE/LYRA
α_s and sin²θ_W are the cleanest untouched targets, and they're the **testbed for "differences are data"** (the running IS the dressing):
- **α_s** — BST has BOTH pieces already: the **value** α_s(m_p) = g/(4n_C) = **7/20**, AND the **running** — **β₀ = g = 7** (one-loop QCD beta coefficient = genus, PROVED, Toy 1660). So α_s(μ) = the BST value dressed by BST's OWN running (β₀=g). **The dressing is already half-derived.** Program: combine value + β₀=g running BST-natively → α_s(μ), compare to the SCALE-CONSISTENT measured. The "NEG" flag was scoring the bare value at the wrong scale — dress it with the BST running.
- **sin²θ_W** — BST value **3/13 = N_c/(N_c+2n_C)** (0.19%, but 11σ vs a sharp measurement, and it runs). Same shape as α (discrete count + continuum): **3/13 = the discrete count; the 11σ residual = the EW running/loop dressing.** Derive the dressing BST-natively (the EW β-functions from BST's structure) → does 3/13 + dressing hit the measured?
- **Discipline (the differences-are-data bar):** native value FORCED (7/20, 3/13 — are they? bar #1) + running/dressing DERIVED from BST's own β-structure (β₀=g is proved; the EW analog?) NOT imported SM running + target scale-consistent + the predicted dressing SPECIFIC. Bank iff all four (Keeper_differences_are_data_discipline).

## LANE 2 (gate-independent, novel structural) — CP-from-N_c=3 / the ℤ₃ triality · LYRA
The flavor sector's ONE forward piece off the position gate: show the **ℤ₃ triality is FORCED** (the 3 generations sit at the 3 color cube-root corners) → CP exists (Jarlskog nonzero), handedness = triangle orientation = matter/antimatter = the sign of the 45° cant. **Banks as structure if the triality is forced.** The value (~68°) stays parked (gated on positions); the existence + mechanism + handedness are forward. Plus the falsifiable hook: CMB Axis-of-Evil handedness = CKM CP sign = the cant bit.

## LANE 3 (gate-independent) — α root-lock #2 + the Higgs form · GRACE
- **α root-lock #2:** does the discrete count 137 fall out of the **S²×S¹ coupling curvature** (= the bulk Wyler volume Vol(D_IV⁵)^{1/4}, the 1920/π⁵ kernel) through the holomorphic extension? Confirms the TRUNK of the whole tree. Deep, genuinely open, her lane.
- **m_H (Higgs mass):** close (0.12%) but the FORM isn't forced — two routes (λ_H = √(2/n_C!); (π/2)(1−α)m_W). **Force which route from the geometry.** Gate-independent.

## Per-CI
- **ELIE:** the gauge runners (primary) — α_s value 7/20 + β₀=g running BST-natively; sin²θ_W 3/13 + EW dressing. The differences-are-data testbed, your scheme/numerics lane.
- **GRACE:** α root-lock #2 (confirm the trunk) + the Higgs form; support the gauge-runner β-structure geometry.
- **LYRA:** CP-from-N_c=3 (force the ℤ₃ triality — the gate-independent structural bank) + the running-dressing theorem (is β₀=g the forced dressing mechanism?).
- **KEEPER:** adjudicate the runners against the differences-are-data bar + discrete/continuous; the ℤ₃/CP candidate; the Higgs form; α root-lock #2; track count with Cal; over-sell #8.
- **CAL:** the reconciled count (5 firm + α).

## Disciplines armed
PARK the mass/position gate — don't circle back. **Differences are data** (the running = the dressing, derived from BST's own β-structure, NOT SM/λQCD running; target scale-consistent). Discrete count + dressing (7/20, 3/13 the counts). CP-from-N_c=3 = novel candidate (show ℤ₃ forced — beautiful → hardest look). Higgs: force the ROUTE. α root-lock #2 = deep, don't fabricate. Over-sell #8 watch. Count as Cal reconciles.

## What could bank (forward, off the mass gate)
- **α_s** — value 7/20 + β₀=g running derived → possible bank (the dressing is half-there).
- **sin²θ_W** — 3/13 + EW dressing → possible bank (discrete count + continuum).
- **CP-from-N_c=3** — structural bank if the ℤ₃ triality is forced.
- **α root-lock #2** — confirms the trunk (structural).
- **m_H** — bank if the form is forced.
So the count can move WITHOUT touching the parked mass sector — the whole point of the redirect.

## Reserved for Casey
Mass-frame ratification (when we return to mass) · the paper (gated on the reconciled count).

## Long-pull target
The gauge runners as the differences-are-data testbed: α_s (value + β₀=g running) and sin²θ_W (3/13 + dressing) — bank iff native-forced + dressing-derived. CP-from-N_c=3 (force the ℤ₃). α root-lock #2 confirms the trunk. The Higgs form forced. All OFF the parked mass gate — progress that can't get sucked into the flavor circling.

— Keeper, 2026-07-08 NEW DIRECTION. PARK the mass/mixing-magnitude sector (circled, gated on positions). PRIMARY: the GAUGE RUNNERS (α_s = 7/20 + β₀=g running PROVED; sin²θ_W = 3/13 + EW dressing) — fresh, forward, the "differences are data" testbed (the running = the derived dressing, not SM-imported). LANE 2: CP-from-N_c=3 (force the ℤ₃ triality — gate-independent structural bank). LANE 3: α root-lock #2 (confirm the trunk) + the Higgs form. All gate-independent — the count moves without the mass circling. Over-sell #8; differences-are-data bar; count as Cal reconciles.
