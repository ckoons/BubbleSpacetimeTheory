# K786 — Casey's "inside the gaps" + Elie's GF(128) candidate collapse the two premises to ONE. 127/128 is a PARAMETER-FREE consequence of a single decidable hypothesis (fundamental discreteness inside the gaps), not two imposed premises. It sharpens the end-state; it does not prove it.

**Keeper | 2026-07-20 | Casey: "127/128 may still be true inside the gaps." Checked carefully (post-de-inflation, so scrutinized as re-inflation risk). It's a genuine logical sharpening — the second premise is not independent. `inside_the_gaps.py`.**

---

## The team's stable end-state (K785): TWO premises
127/128 rested on two premises the geometry doesn't supply:
- **(a)** the surface/interior is a fundamentally **discrete** 128-cell field (Casey's inside-the-gaps / inversion);
- **(b)** the top covers exactly **127 of 128** cells — Elie's open questions: *why the top? why all nonzero? why miss the zero?*

## The sharpening: (b) is NOT independent — it follows from (a) + already-earned results
Given (a) [the substrate inside the gaps is the discrete RS/GF(128) code], each of Elie's questions is answered without a new premise:
- **why the TOP** → **boundary-reach ordering (DERIVED spine):** heaviest = most boundary-localized = most-covering = the **maximal codeword.** So the top *is* the maximal codeword — forced, not chosen.
- **why ALL NONZERO** → a **primitive-polynomial LFSR** cycles through all **127 nonzero elements** (the multiplicative group GF(128)*) = the maximal RS codeword (MDS, exactly q−1). Forced by the code structure.
- **why MISS THE ZERO** → **0 = the additive identity = the LFSR dead state** (unreachable from the cycle) = the **neutrino cell.**

So **given (a), y_t = |GF(128)*| / |GF(128)| = 127/128, PARAMETER-FREE** — a *group count* (the multiplicative group), not a tuned weight, missing exactly the identity (= the neutrino). Elie's own distinction makes the point sharp: a *continuous* edge-weight reaches 0.992 only by tuning (crosses at ×45, a one-knob fit); the *group-count* 127 is not tuned — it's forced.

## What this changes (and what it doesn't)
- **The two premises collapse to ONE:** *is the substrate the discrete RS/GF(128) code inside the gaps?* That single hypothesis — Casey's "inside the gaps" — plus the **derived** boundary-reach ordering and the **established** RS/GF(128) structure (Paper #122), **forces 127/128 parameter-free.** The "top covers 127" is not a second imposition; it is a consequence.
- **Honest tier — this does NOT prove 127/128.** It sharpens it from *"a value resting on two imposed premises"* to *"a PARAMETER-FREE consequence of one decidable hypothesis (fundamental discreteness inside the gaps)."* That is a **more defensible** conditional — the value isn't fit, it's the multiplicative-group count — but the contingency remains: **is the substrate fundamentally discrete inside the gaps?** That is the it-from-qubit question, decidable in principle, and it IS Casey's intuition.
- **The neutrino gets tied in cleanly:** the deficit 1/128 = the additive identity = the one cell the LFSR cycle never reaches = the neutrino. So *the top covering the multiplicative group and the neutrino being the identity are one statement.* Parameter-free.

## For the papers (the consolidation)
State 127/128 as: **"a parameter-free consequence of the discrete-code hypothesis (the substrate is the RS/GF(128) code inside the gaps) — the top covers the 127 nonzero field elements, the neutrino is the missing identity — conditional on that hypothesis (the it-from-qubit question), decidable in principle."** This is stronger and more honest than "two imposed premises," and it keeps the "if." The derived spine + angular=1 remain the banked content; the inversion is the framed hypothesis; 127/128 is now a *parameter-free conditional* of that hypothesis.

## Discipline
- **Not a proof, and not re-inflation:** it's a logical reduction (the second premise follows from the first + derived boundary-reach + the established code structure). Checked. 127/128 stays **conditional** on the one hypothesis.
- The one hypothesis (fundamental discreteness inside the gaps) is exactly Casey's instinct — coherent (the discrete series is genuinely discrete; the 128 sits in the Bergman norm), decidable in principle (Γ-ratio vs 1/2^g), but the fundamental geometry we compute is continuous and the discreteness is a cooling/tiling hypothesis. So the contingency is real.

— Keeper K786, 2026-07-20. Casey "inside the gaps" + Elie GF(128): the two premises collapse to ONE. (b) [top covers 127] is NOT independent — it follows from (a) [discreteness] + DERIVED boundary-reach (heaviest=maximal codeword) + the LFSR cycle (127 nonzero = multiplicative group) + 0=dead state=neutrino. So given the ONE hypothesis (substrate is the discrete RS/GF(128) code inside the gaps), 127/128 = |GF(128)*|/|GF(128)| PARAMETER-FREE (group count, not tuned; neutrino = the missing identity). Sharpens the end-state (two imposed premises → one decidable hypothesis + parameter-free consequence); does NOT prove it. Contingency = the it-from-qubit question = Casey's intuition. See [[Keeper_K785_code_arc_study_close_the_stable_end_state_what_banked_2026-07-20]].
