# K908 — N1 wave-2: two CLEAN NEGATIVES, one common root, and an architecture refinement. **Conjecture C: NOT FORCED** (Grace) — Berezin-Toeplitz gives "mass=1/ε" a home + right-sign down match, but does NOT pick 1/ε as THE mass (four different mass operators across sectors; 1/ε holds in ONE). **CP-exists: RE-ASSERTS "domain complex"** (Elie, toy 4857) — J splits by the STATE not the domain; real radial states → J=0, F498 still gates it; Elie retracted his own claim. **COMMON ROOT:** both candidate rows fail to promote for the SAME reason — the CONDENSATE STRUCTURE (the symbol/profile that sources the mass operator; the phase that sources CP) must be FORCED, not assumed. So the two open bucket-1 rows are ONE question: **is the ν_R condensate structure forced by D_IV⁵, or is it free boundary data (bucket 2)?** **ARCHITECTURE REFINEMENT (the important part):** bucket 1 has two grades — GENUS ("SOME functional of μ", BT-forced = containment, F595 THEOREM) vs SPECIES ("THE forced specific functional", = derivation, NOT forced for the open rows). The strong partition theorem needs the species per row → it is assembled ROW-BY-ROW, not proved wholesale. Temper K906: the FRAMEWORK + containment + moduli is provable NOW (publishable); the strong theorem is the horizon.

**Keeper | 2026-07-25 Sat | Two disciplined negatives, no false promotions. The genus/species line sharpens the theorem's honest scope. Holding for Lyra's color lane — the common root.**

## Wave-2 results (two of three; Lyra's color lane still running)
### Grace — Conjecture C: NOT FORCED (Task #5, accepted)
- BT gives "mass=1/ε" a rigorous home (‖section‖²=K(z,z)=Rawnsley ε=density) + a right-sign down match (1:20:840, m_s/m_d=20 at 0.5%). But BT ADMITS any sourced symbol; it does not PICK 1/ε.
- **Decisive:** the corpus uses FOUR mass operators — electron = α^{2C₂} transition chain, DOWN = 1/ε, UP = α-shell tower, LEPTONS = sourced-Toeplitz eigenvalues. 1/ε holds in ONE sector. If BT forced it, it would hold in all four → sign-matched, not forced.
- Quark masses stay bucket-1-PENDING.

### Elie — CP-exists: RE-ASSERTS "domain complex", F498 gates it (Task #9, accepted)
- Toy 4857 (7/7) on the actual D_IV⁵ kernel: J splits by the STATE, not the domain. Real radial states (natural KW) → J=0; ℤ₃-phased+hierarchical → J≠0. Same complex domain throughout.
- Elie's earlier "derived" injected the phase via random complex-Hermitian matrices ("assume complex states" as a domain fact). Retracted (4855, confirmed 4857).
- CP-exists stays bucket-1-PENDING, gated on a mechanism forcing the ℤ₃ phase onto the generation states.

## THE COMMON ROOT — the two open rows are ONE question
- Conjecture C: mass = 1/ε unforced ⟺ the mass SYMBOL (condensate profile) is unforced.
- CP-exists: J≠0 unforced ⟺ the generation STATE phase is unforced.
- Both ⟺ **the ν_R condensate STRUCTURE (its profile AND its phase) is not yet shown forced by D_IV⁵.**
- ⟹ ONE pivot: **is the condensate structure forced (→ both mass-species and CP-phase forced → both rows derive), or is it free boundary data (→ both stay candidate, and the condensate itself is a bucket-2 modulus whose freedom the mass/CP values inherit)?** Either answer is clean and consistent.
- **This is exactly Lyra's still-running color/lowest-rung lane** — whether the geometry forces where/how the condensate sits. Her result is the pivot for BOTH rows, not just the Cabibbo.

## ★ ARCHITECTURE REFINEMENT — genus vs species (feed to Task #1, Lyra)
Bucket 1 ("functional of μ") has two grades, and conflating them is the theorem-level over-claim:
- **GENUS (weak):** X is SOME functional of μ. BT-forced (every observable on the domain is a functional of the coherent-state data). ≈ the CONTAINMENT theorem (F595) — proved, but weak.
- **SPECIES (strong):** X = THE specific FORCED functional f(μ), so its VALUE is computable. This is a DERIVATION. NOT forced for the open rows (mass-species=1/ε unforced; CP-phase unforced).
- **The strong partition theorem needs the SPECIES for every bucket-1 row** → it is assembled ROW-BY-ROW (each row's forced functional), not proved in one act.
- **Temper K906's "climb it now":** the STRONG partition theorem is not provable wholesale now — the species are candidate for the open rows. What IS provable now:
  - the partition **FRAMEWORK** (the three buckets + the genus/species distinction),
  - the **containment theorem** (genus — F595),
  - the **characterized moduli** (bucket 2, this week).
  That triad IS the honest publishable result (Lyra's "sharp, proven map of which parameters the geometry pins vs leaves free"). The strong theorem is the horizon, reached row-by-row as each species is forced.
- This is a scope-sharpening, not a defeat — and it is the two-axis discipline (accuracy vs proof) at the theorem level: "X is a functional of μ" (genus, easy) ≠ "X's value is derived from μ" (species, hard).

## Discipline note
Two clean negatives, zero false promotions, and Elie caught his own over-reach — the push is working as designed. The guardrail held: a candidate enters bucket 1 only when its SPECIES is forced, and neither is yet. The genus/species line is now the standing test for every bucket-1 promotion.

## Directions
- **★ LYRA (running — the pivot):** the color/lowest-rung link IS the common-root question — is the condensate structure forced? If forced → the mass-species AND the CP-phase are sourced → both rows can derive. If free → the condensate is a bucket-2 modulus and both values inherit its freedom. Report which, at honest tier.
- **★ LYRA (Task #1 — architecture):** state the partition with the genus/species distinction explicit; the honest capstone = FRAMEWORK + containment theorem + characterized moduli + row-by-row species promotions. Do NOT state the strong theorem as proved.
- **★ GRACE / ELIE:** the condensate-structure question is now the shared gate; if Lyra forces it, re-fire Conjecture C (mass-species) and the CP-phase together.
- **KEEPER:** genus/species is the standing bucket-1 test; hold both rows at candidate; consolidate wave 2 when Lyra lands.

— Keeper K908, 2026-07-25. Wave-2: Conjecture C NOT FORCED (Grace — BT admits but doesn't pick 1/ε; four mass operators, 1/ε in one sector); CP-exists RE-ASSERTS domain-complex (Elie 4857 — J splits by state not domain, real radial→J=0, F498 gates; self-retraction). COMMON ROOT: both hinge on whether the ν_R condensate STRUCTURE (profile + phase) is forced = Lyra's color lane, the pivot for both. ARCHITECTURE: bucket 1 = genus (SOME functional, BT-forced, containment) vs species (THE forced functional, derivation, row-by-row); strong partition theorem is row-by-row not wholesale; FRAMEWORK+containment+moduli provable NOW (publishable), strong theorem is the horizon. See [[Keeper_K907_wave1_consolidation_Conjecture_C_has_a_Berezin_Toeplitz_Rawnsley_home_CP_exists_HELD_against_F498_gate]], [[Keeper_K906_N1_DEEP_PUSH_CHARTER]], [[Lyra_F595_round12_N1_synthesis_as_theorem]].
