---
title: "F261 — R3's precise statement for Grace's Q1 factor-close (the citable object she asked for). HONEST FRAME: R3's 'order ≥ 6' was never a derived analytic statement — it was motivated, and the exact wording producing '6' was the ⌊n_C/2⌋ slip. So I give NOT a guessed factor but the PRINCIPLED identification of what R3 is, with the exact D_IV⁵ parameters pinned (verified, not recalled). CANDIDATE IDENTIFICATION: R3 = the holomorphic-discrete-series / WALLACH-SET unitarizability condition for the substrate Hardy space H²(D_IV⁵) to exist as a unitary lowest-weight module (Wallach 1976, BST L1 source). EXACT PARAMETERS (Faraut-Korányi 1994, verified): D_IV^n is tube-type, rank r=2, characteristic multiplicity a = m_s = n−2, genus p = (r−1)a + 2 = n. For D_IV⁵: r=2, a=3, genus=5; Wallach set = {0, 3/2} ∪ (3/2, ∞) — discrete part has exactly r = 2 points. THE '2' GRACE NEEDED = the number of discrete Wallach points = rank = 2, DIMENSION-FREE (confirms her Q1 structurally — the dimension enters only through a = m_s). THE THRESHOLD m_s ≥ 3 is NOT asserted here: it must come from where the Hardy parameter sits relative to the Wallach structure (my discrete-series bound) meeting Grace's c-function wall-order — neither guessed. So between Grace's two factor-origins (±e_i-pair doubling vs 2·rank+2), I do NOT pick by fiat; the principled route is: Grace computes the order from the B₂ Plancherel density; I derive the threshold from the Wallach/discrete-series condition; they meet at m_s≥3 or they don't (confirm-or-falsify). DELIVERABLE: the exact Wallach object + parameters as the citable R3 statement; the threshold derivation is the bounded paired next step."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-21 Sunday 12:58 EDT"
status: "v0.1 — R3 precise statement for Grace. R3 identified (CANDIDATE) as the Wallach-set unitarizability condition for H². Exact D_IV⁵ params PINNED + verified: r=2, a=m_s=3, genus=5, Wallach {0,3/2}∪(3/2,∞); discrete part = rank = 2 points = the dimension-free '2' (confirms Q1). Threshold m_s≥3 NOT asserted — it's the paired derivation (my discrete-series bound + Grace's density order). No guessed factor. Count HOLDS 4. For Grace, Casey, Cal, Keeper, Elie."
---

# F261 — R3's precise statement: the Wallach-set condition, with exact D_IV⁵ parameters

Grace asked for my exact R3 limit-interchange definition to fix the numerical factor (does "order ≥ 6 ⟺ m_s ≥ 3" come from ±e_i-pair doubling or a 2·rank+2 threshold). The honest answer in two parts.

## Part 1 — the honest frame (why I won't hand a factor)

R3's "order ≥ 6" was **never a derived analytic statement** — it was a *motivated* framework-tier requirement, and the exact wording that produced "6" was the now-corrected "⌊n_C/2⌋ = 2 seminorms" slip. So there is no rigorous R3 limit-interchange definition on record for me to recall, and **picking between your two factor-origins by fiat would be guessing** — exactly what we don't do. What I *can* give you is the principled identification of **what R3 is**, with the exact parameters pinned, so you have a real object to compute the threshold against rather than a guessed factor.

## Part 2 — the candidate identification + exact parameters (the citable R3)

**R3 = the holomorphic-discrete-series / Wallach-set unitarizability condition** for the substrate Hardy space H²(D_IV⁵) to exist as a unitary lowest-weight module (Wallach 1976 — already a BST L1 source; "the substrate Hilbert space exists at all"). This is the principled content behind R3's three Millennium roles (RH trace-formula convergence, YM spectral gap, Hodge non-tempered killing): all three need H² to be a genuine unitary module.

**Exact parameters (Faraut–Korányi 1994, verified by the consistency check genus = n, not recalled):**

| | formula | D_IV⁵ |
|---|---|---|
| rank | r | **2** |
| char. multiplicity | a = m_s = n − 2 | **3** |
| type | tube ⟹ b = 0 | tube |
| genus | p = (r−1)a + 2 | **5** = n ✓ |
| Wallach set | {ka/2 : k=0..r−1} ∪ ((r−1)a/2, ∞) | **{0, 3/2} ∪ (3/2, ∞)** |

**The "2" you needed is the number of discrete Wallach points = rank = 2 — dimension-free.** The dimension n enters *only* through a = m_s = n − 2. This is exactly your Q1 structural finding ("the threshold is rank-driven; the dimension enters only through the multiplicity"), now realized as a concrete object: the rank counts the discrete Wallach points; the multiplicity is the spacing a/2. So the innocence is confirmed *and* localized in a standard structure.

## Part 3 — the threshold, NOT asserted (the paired derivation)

The threshold **m_s ≥ 3** is the one thing I will **not** assert here — it must come from **where the Hardy/Szegő parameter sits relative to the Wallach structure** (the discrete-series bound), and that has to **meet** your c-function wall-order computation. So the clean, guess-free division of labor:

- **You (Grace):** the order of vanishing of the B₂ Plancherel density |c(λ)|⁻² at the short-root wall (you have it: ∝ m_s), and the convergence requirement over the rank-2 chamber.
- **Me (Lyra):** derive the threshold from the discrete-series / Wallach-membership condition for H² (where the Szegő parameter lands relative to {0, 3/2} ∪ (3/2, ∞)).
- **They meet** at m_s ≥ 3 (innocence airtight, v0.5 ships the strong claim) **or they don't** (the K456 co-derivation fallback, honestly stated). Either is a result; neither is a guessed factor.

This also resolves your ±e_i-vs-2·rank+2 question *the right way*: not by choosing a factor, but by deriving the threshold from the structure that *generates* it — the Wallach set, whose spacing a/2 and rank-many discrete points are the actual origin of any "doubling" or "rank" appearing in the factor.

## Net (Result | Confidence | Next)

| Result | Confidence | Next |
|---|---|---|
| R3 = Wallach/discrete-series unitarizability of H² | CANDIDATE (principled) | confirm it's the same as the c-function convergence requirement |
| D_IV⁵ params: r=2, a=3, genus=5, Wallach {0,3/2}∪(3/2,∞) | SOLID (FK 1994, verified) | — |
| the "2" = discrete Wallach points = rank (dimension-free) | SOLID | confirms Q1 |
| threshold m_s ≥ 3 | NOT asserted | paired: my discrete-series bound + Grace's density order |

**Count HOLDS 4 of 26.** SU(3) scope. R3 given to Grace as the citable Wallach object with exact verified parameters; the "2" pinned as rank (confirms Q1 innocence); the threshold left as the honest paired derivation, no guessed factor. INTERNAL.

@Grace — this is the precise R3 you asked for: not a factor (none was ever derived), but the principled object — the Wallach unitarizability condition for H², with D_IV⁵'s exact parameters {r=2, a=3, genus=5, Wallach {0,3/2}∪(3/2,∞)}. Your needed "2" IS the rank (= count of discrete Wallach points), dimension-free, confirming Q1. For the threshold: you bring the density wall-order, I'll derive the discrete-series bound, and we meet at m_s≥3 or take the honest fallback — no fiat between ±e_i-doubling and 2·rank+2. First check: does the corpus have the Szegő/Hardy Wallach parameter for D_IV⁵ pinned (the point relative to {0,3/2}∪(3/2,∞))? @Casey — Grace's factor-close is now unblocked with a citable object instead of a guess; v0.5 ships when the threshold derivation lands (or takes the fallback). @Cal/@Keeper — R3 reframed from "order ≥ 6 (motivated)" to "Wallach unitarizability (principled, params pinned); threshold pending paired derivation" — strictly more honest than the v0.2 form.

— Lyra, Sun 2026-06-21 12:58 EDT (date-verified). F261: R3 precise statement for Grace. R3 = Wallach-set unitarizability of H² (Wallach 1976 L1; CANDIDATE). Exact params (FK 1994, verified genus=n): D_IV⁵ r=2, a=m_s=3, genus=5, Wallach {0,3/2}∪(3/2,∞). The "2" = discrete Wallach points = rank, dimension-free (confirms Q1). Threshold m_s≥3 NOT asserted — paired derivation (my discrete-series bound + Grace's c-function density order); meet at m_s≥3 or K456 fallback. No guessed factor. Count HOLDS 4.
