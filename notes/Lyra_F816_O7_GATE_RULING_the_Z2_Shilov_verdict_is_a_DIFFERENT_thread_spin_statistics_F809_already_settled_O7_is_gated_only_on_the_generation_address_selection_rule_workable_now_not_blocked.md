# F816 — RULING (Keeper K1174 asked Lyra+Elie to confirm): **the O7 gate is STALE as a blocker — O7 is workable right now. Two different ℤ₂ threads were crossed. The "ℤ₂ = Shilov verdict" Elie flagged belongs to the SPIN-STATISTICS fold line (F808/F809, item 10), NOT to O7/mixing — and it is already SETTLED (F809: the Shilov ℤ₂ is the parity element −I₅, det −1, O(5), DISTINCT from the spin double-cover, refuted by determinant, Tuesday). Grace's O7 target-map gates O7 on EXACTLY ONE thing: the generation-2,3 K-type ADDRESS SELECTION RULE. No ℤ₂/Shilov verdict appears anywhere in it. So nothing stale blocks O7; the only open thing is the research target itself (the selection rule), which is workable now — Elie can run the forward FK overlap the moment the rule is forced.**

**Lyra, Wednesday 2026-08-05. Ruling from the corpus (grep-verified, not memory): grace_O7_mixing_target_map_2026-08-04.md + my own F808/F809. Calibrate both ways — I clear the stale blocker (don't leave the highest-leverage move falsely gated) AND I hold the real gate honest (the selection rule is genuinely open; both θ₂₃ candidates are clean ratios, so it must be FORCED not chosen).**

## The two ℤ₂ threads, disentangled
- **Thread A — spin-statistics fold (item 10, F808/F809):** "is spin-½ = the Shilov-quotient ℤ₂?" **SETTLED, REFUTED by determinant.** The Shilov ℤ₂ of (S⁴×S¹)/ℤ₂ acts as (antipode −I₅ on S⁴) × (S¹ half-turn); det(−I₅) = (−1)⁵ = −1 → −I₅ ∈ O(5)∖SO(5) = PARITY. The spin ℤ₂ is the Spin(5)=Sp(2)→SO(5) kernel, which projects to +I₅ ∈ SO(5). Parity ≠ spin-kernel → distinct. This is the fermion/fold thread, and it's closed. **It has nothing to do with the mixing addresses.**
- **Thread B — O7 mixing gate (item 3, Grace's map):** what's open per grace_O7_mixing_target_map. **FORCED already:** generation degrees {1,3,5}=Q⁵ odd cohomology (T1929); ν=N_c=3=Wallach threshold; single-row K-types (1,0)/(3,0)/(5,0) dims 5/30/91; overlap form N(w)^{n_C/2} (F87b); the Wallach ladder as address space. **THE ONE OPEN CHOICE:** "which K-type address the generation-2,3 modes occupy in that fixed ladder" — the *selection rule*. **No ℤ₂/Shilov verdict is in the gate.**

**So Elie's "O7 is gated on a ℤ₂=Shilov verdict" imports Thread A's (already-settled) question into Thread B, where it doesn't live. The gate is stale.** O7 is blocked on nothing but its own research target.

## The "genuinely complex" concern (Keeper's 11:30) — also not a blocker
The CP phases (δ_CKM, δ_PMNS) need the off-diagonal overlaps to be genuinely complex (carry a phase). **This is already banked, and it is NOT the F809 question:** D_IV⁵ is a Hermitian symmetric domain — its Bergman/FK kernel is holomorphic, so the overlaps ⟨layer_i|layer_j⟩ are generically complex; CP = complex conjugation z↦z̄ (T1947, W-22, chirality+CP from the complex structure), and the CP phase is already identified (γ = c₂π/(N_c·rank·n_C) = 11π/30, T1960, 0.5%). So the generation peaks live on a complex domain by construction — the phases are there. **Honest line: T1947 says the domain IS complex (so phases exist); it does NOT by itself pin the selection rule that fixes the specific phase values — that's still the O7 work.** Don't over-merge: complex-structure ✓ banked; selection rule = the open target.

## The ruling (for Keeper)
- **O7 is NOT blocked by a stale ℤ₂ verdict.** Confirmed. The ℤ₂=Shilov question is Thread A (spin-statistics), settled Tuesday (F809, by det). It is absent from Grace's O7 gate.
- **O7 is workable now.** The one open thing is the generation-2,3 address selection rule — the research target, not a gate. Elie's forward FK overlap runs the moment the rule is forced.
- **The real discipline to hold (not a blocker, a guardrail):** the selection rule must be FORCED, not chosen. Both θ₂₃ candidates (4/7 = rank²/g upper octant; 6/11 = C₂/c₂) are clean D_IV⁵ ratios — picking the address to land on one would be fitting (Grace's exact caution). Elie fires the kernel blind; the address is pinned by geometry (why g2,g3 sit there), then the overlap yields all seven at once.

## Tier / handoffs
- **@Keeper** — confirmed: the O7 gate is stale as a blocker (the ℤ₂=Shilov verdict is Thread A/spin-statistics, settled F809 by det; absent from Grace's O7 gate). O7 is workable now, gated only on the generation-address selection rule (the research target). The "genuinely complex" concern is banked (T1947 Hermitian domain + T1960 γ), also not a blocker. Green-light O7 as the highest-leverage move.
- **@Elie** — the ℤ₂ you were waiting on is the spin-statistics one (F809), and it's already settled distinct-by-determinant — it does not touch the mixing addresses. O7 is unblocked. Run the forward FK overlap at the gen-2,3 K-type addresses (single-row (3,0)/(5,0), dims 30/91), overlap form N(w)^{n_C/2}, blind against the banked ratios. The domain is complex (T1947) so the phases are there. The only open piece is the selection rule — pin it by geometry (why g2,g3 land where they do), not by which ratio it hits.
- **@Grace** — your O7 map is the authority here: it lists the selection rule as the one open choice, no ℤ₂ verdict. Confirmed against F809. Hold the retrofit discipline (don't pick the address to hit 4/7 vs 6/11).
- **@Cal** — guard both ways: (i) the stale gate is cleared (F809 settles Thread A, distinct by det; don't let it re-block O7); (ii) the selection rule stays OPEN and must be forced not chosen (don't let "O7 unblocked" read as "O7 solved").
- **@Casey** — the thing Elie thought was blocking your biggest lever turns out to be a different question that we already closed on Tuesday. There are two "minus-one folds" in the shape that look alike: one is the spin fold (makes fermions), the other is a mirror-reflection fold on the boundary (a parity thing) — and I showed with a one-line determinant they're genuinely different. Elie was holding the mixing work until that got decided; but that decision was never what the mixing needed. Grace's map is explicit: the mixing is fully wired except for one thing — which "shelf" generations 2 and 3 sit on in a fixed ladder of addresses. That's your O7 question, and it's open and workable right now, not blocked. The one rule I'm keeping: we have to *force* which shelf, not pick the shelf that gives the right answer — both candidate answers are clean, so choosing would be cheating. So the board is greener than it looked: O7 is the live lever, nothing stale in front of it. Nothing pushed.

Notes only; no toy/theorem claimed (gate ruling per K1174). F816: O7 gate STALE as blocker. Two ℤ₂ threads crossed — the "ℤ₂=Shilov verdict" is Thread A (SPIN-STATISTICS fold, F808/F809), SETTLED Tuesday (Shilov ℤ₂ = parity −I₅ det −1 O(5), distinct from spin-kernel +I₅ SO(5), refuted by det); it is ABSENT from Grace's O7 gate. Thread B (O7/mixing, grace_O7_mixing_target_map): FORCED already = degrees {1,3,5}, ν=3, single-row K-types, overlap form N(w)^{n_C/2}, Wallach ladder; ONE OPEN = the generation-2,3 address SELECTION RULE (the research target, not a gate) → workable now. "Genuinely complex" concern banked (T1947 Hermitian domain + T1960 γ=11π/30), also not a blocker. Discipline: selection rule must be FORCED not chosen (θ₂₃: 4/7 vs 6/11 both clean → picking = fitting). @Keeper green-light O7; @Elie unblocked, run forward FK overlap blind; @Cal guard both ways (cleared ≠ re-block; unblocked ≠ solved). — Lyra