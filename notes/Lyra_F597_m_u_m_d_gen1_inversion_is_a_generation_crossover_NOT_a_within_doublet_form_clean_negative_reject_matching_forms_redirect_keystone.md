# F597 — m_u/m_d and the gen-1 inversion: a CLEAN NEGATIVE with teeth. The inversion is a *generation crossover* (up/down ratio crosses 1 between gens 1 and 2), so m_u/m_d is NOT a within-doublet geometric form — and any clean form matching its loose range (3/7, 1/2, 2/5 all fit) is the coincidence trap. Reject them. Redirect: anchor m_u via the up-type ladder, not a doublet ratio.

**Lyra, Sun 2026-07-19. Lead item.** Casey: derive m_u/m_d, DON'T match (the range 0.38–0.58 admits several forms). Honest result: it's not derivable from the doublet geometry *because it isn't a within-doublet quantity* — it's the gen-1 value of a generation-crossing coefficient. Clean negative, and it redirects the keystone.

## Two-loci refuted first (K758, ties off F596)
The two-mass = two-idempotent candidate is dead, killed by *our own* confinement result: a colored quark has **zero Shilov support** (F588 Schur), so it cannot sit on the c₂ (Shilov) idempotent. The two masses are **current + gluon dressing on ONE locus**, not two loci. F596's Break 2 (frame-dependent labels) is now a full refutation. Good — consistent.

## The inversion, quantified (linear algebra)
Model the up-down split on the Sp(1)_L doublet as **M = M₀·𝟙 + Λ·T₃** (T₃ = ±½):
$$ m_u = M_0 + \tfrac{\Lambda}{2}, \qquad m_d = M_0 - \tfrac{\Lambda}{2}. $$
The observed up-type/down-type ratio across generations:
| gen | up/down ratio | ordering |
|---|---|---|
| 1 (u/d) | **0.46** | up lighter (inverted) |
| 2 (c/s) | 13.6 | up heavier |
| 3 (t/b) | 41.3 | up heavier |
Monotonic: **0.46 → 13.6 → 41.3, crossing 1 between gen 1 and gen 2.** The gen-1 inversion *is* this crossing.

## ★ The load-bearing logical result (clean, and it decides the strategy)
**A generation-INDEPENDENT doublet splitting Λ gives the SAME ordering for all three generations.** The observed inversion — ordering *flips* between gen 1 and gen 2 — therefore **cannot come from within-doublet geometry alone.** It requires the splitting coefficient **Λ(gen) to change sign** — a zero-crossing between generations 1 and 2.

Consequence: **m_u/m_d is not a within-doublet geometric ratio.** It's the gen-1 value of a generation-crossing coefficient. So "derive m_u/m_d from the doublet geometry" is **structurally the wrong request** — the doublet supplies at most the T₃-splitting *form*, never the generation-dependence that *is* the inversion.

## ✗ REJECT the matching forms (the discipline, explicit)
N_c/g = 3/7 = 0.43, 1/rank = 0.5, rank/n_C = 2/5 = 0.4 — **all three fit the loose range**, none is derived. And the deeper point makes the rejection principled, not just cautious: since m_u/m_d is a **generation-crossover output** (not a clean geometric ratio), *any* clean form landing in [0.38, 0.58] is a coincidence **by construction** — the quantity doesn't *have* a clean closed form. This is exactly why the range admits several forms: it's not a forced ratio. (Same trap family as 3/13, D=10, Λ≈m_p/N_c — killed all week.) @Cal: reject all three.

## The redirect (what the negative buys — and it's the useful part)
- **The keystone is NOT m_u/m_d; it's the up-type ladder anchor m_u.** Since m_u/m_d is a crossover *output*, the right move is to anchor **m_u independently via the up-type ladder (u:c:t)** — Elie's lane — exactly as m_d is anchored by the down-type ladder (s/d = 20). Then m_u/m_d falls out as a *derived consequence* of two independently-anchored ladders, not as an input form. **m_u becomes as solid as m_d when the up-type ladder is anchored — not when a ratio is matched.**
- **The real open physics is the crossover mechanism:** why does the up-type/down-type ratio grow monotonically and cross 1 (i.e. why does Λ(gen) change sign)? Equivalently: why is the up-type ladder *steeper* than the down-type (y_t≫y_b but y_u<y_d)? That differential steepness IS the inversion. Deriving *that* is the deep target — and it's a statement about the two ladders' slopes, not about the gen-1 doublet.

## Tier / verdict
- **CLEAN NEGATIVE** on "derive m_u/m_d from doublet geometry" — complete, and it's a *structural* negative (the quantity isn't a within-doublet ratio), not a failed search.
- **STRUCTURAL RESULT (genuine):** the gen-1 inversion = a sign-change (zero-crossing) of the T₃-splitting coefficient between gens 1 and 2; equivalently the up-type ladder is steeper than the down-type. This is the honest content and it's target-innocent (just the mass data + the doublet algebra).
- **STRATEGIC REDIRECT:** anchor m_u via the up-type ladder (Elie), not a doublet ratio; m_u/m_d is an *output*. This is how m_u gets solid.

## Handoffs
- **@Elie — you now hold the keystone.** Anchor the **up-type ladder u:c:t** as radial moments (down-type s/d=20, b/s≈42 is the template). If u:c:t anchors independently, m_u is solid and m_u/m_d is a derived output. Also report the up-type ladder *slope* vs the down-type slope — the ratio of slopes IS the inversion; a clean form for "why up-ladder steeper" is the deep prize.
- **@Cal — discriminator:** reject 3/7, 1/2, 2/5 for m_u/m_d (all fit, none derived; the quantity is a crossover output with no clean form). Guard against any "gen-1 inversion form." The derivation must come through the up-type ladder anchor, not a doublet ratio.
- **@Keeper** — update the ledger: m_u/m_d is NOT a target for a closed form (structural crossover); the m_u fix routes through the up-type ladder anchor. Bank the structural result (inversion = ladder-slope difference / T₃-coefficient sign change). Two-loci refuted (K758) folded in.
- **@Grace** — render the up/down ratio across generations (0.46→13.6→41.3, crossing 1) — the inversion made visual; don't render a clean m_u/m_d form.

Notes only; no toys/theorems claimed. — Lyra
