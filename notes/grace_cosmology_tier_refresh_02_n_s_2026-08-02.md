---
id: grace_cosmology_tier_refresh_02_n_s_2026-08-02
date: 2026-08-02
program: TEGMARK
status: current
topic_tags: [cosmology-tier-refresh, n_s, spectral-index, target-innocence, mode-counting, multi-role-recurrence, tier-inconsistency, re-tier]
claims:
  - id: this-a
    topic: cosmology tier-refresh #2 — n_s = 1−n_C/N_max audited; STRONGER than Ω_Λ (has a counting mechanism + multi-role recurrence) but the per-mode tilt-magnitude 1/N_max is asserted not shown; registry "Proved" over-claims; provisional PARTIALLY DERIVED
    status: current
    superseded_by: null
    date: 2026-08-02
---

# [TEGMARK] Cosmology tier-refresh #2 — n_s = 1 − n_C/N_max

*Grace | 2026-08-02 | the queued next item after Ω_Λ (K666 flagged n_s "strong candidate PENDING CAL"). Same target-innocence lens. Verdict: n_s is a STRONGER candidate than Ω_Λ — but it is NOT "Proved" (as the registry marks it), and the load-bearing step is still asserted. Calibrated both directions.*

## What n_s has (the good — don't under-claim)
n_s = 1 − n_C/N_max = 1 − 5/137 = 0.9635 vs Planck 0.9649 (**0.3σ**, 0.145%). Three things put it ABOVE Ω_Λ:
1. **Integers target-innocent:** n_C=5 (compact complex dim) and N_max=137 are BST primaries — forced, not chosen.
2. **★ A COUNTING mechanism, not just a clean ratio (T1962):** the red tilt = n_C running modes × (1/N_max per mode). Ω_Λ had a bare topological ratio c₃/(c₃+χ) with no mechanism; n_s has a *counting* argument (n_C modes each contributing 1/N_max to the tilt) — an AC(0)-legitimate operation, and n_C=compact-dim gives a genuine reason for the numerator.
3. **Multi-role recurrence (suggestive):** the SAME ratio n_C/N_max=5/137 anchors BOTH the CMB tilt (T1962) AND the tritium β-decay endpoint Q_β=(n_C/N_max)·m_e (T2050) — a ratio recurring across independent domains is structural evidence, not a CMB-specific fit. (Suggestive, not conclusive — two appearances of a small ratio.)

## What n_s lacks (the honest gap — don't over-claim)
- **The load-bearing step is ASSERTED: "tilt per mode = 1/N_max."** Why exactly 1/N_max per running mode? That is the Derived-vs-fit hinge, and the entry states it ("each contributing 1/N_max"), it does not *derive* it from the slow-roll/cascade. Until a slow-roll computation on D_IV⁵ forces the per-mode contribution = 1/N_max, this is a grounded heuristic, not a forcing.
- **Mild form-selection flag:** n_C/N_max IS the closest clean X/N_max to the observed tilt (checked: n_C/N_max=0.0365 vs obs 0.0351; next is C_2/N_max=0.0438). n_C=compact-dim mitigates it, but the numerator being the best-fitting integer is worth noting.

## ★ TIER INCONSISTENCY (a real currency finding)
The tier is recorded THREE different ways: **registry T1962/T1421 = "Proved"**, **data const_016 = [D]**, **K666 = "pending Cal."** "Proved" is NOT supported — the per-mode 1/N_max forcing is unshown; a mode-counting heuristic with an asserted per-mode magnitude is not a closed proof. The registry over-claims.

## Verdict (provisional — flag for Casey/Cal, do NOT unilaterally re-tier)
**n_s → PARTIALLY DERIVED** (stronger than Ω_Λ): **DERIVED part** — the integers (n_C, N_max) and the mode-*count* n_C are forced, and the ratio recurs cross-domain; **OPEN part** — the per-mode tilt-magnitude 1/N_max is asserted, not derived from slow-roll on D_IV⁵. Re-promotes to Derived if a slow-roll/cascade computation forces tilt-per-mode = 1/N_max target-blind; stays PD otherwise. **Definitely NOT "Proved"** — the registry should come down from Proved to PD. This mirrors the Ω_Λ ruling but from a higher floor (n_s has the counting mechanism Ω_Λ lacked).

## Handoff
- **@Casey/@Cal:** rule the demotion of the registry "Proved" → PD, and confirm the PD split. K666's "pending Cal" is exactly this.
- **@Elie/@Lyra (if useful):** does the slow-roll/cascade (Toy 1401/1962) genuinely force tilt-per-mode = 1/N_max, target-blind? That's the re-promotion test.
- **Me:** data entry const_016 annotated tier_review (flag, [D] held pending). Next in refresh: A_s, H₀, Ω_b.

— Grace, 2026-08-02 [TEGMARK]. Cosmology tier-refresh #2: n_s=1−n_C/N_max. STRONGER than Ω_Λ (counting mechanism n_C modes×1/N_max + multi-role recurrence with tritium T2050, integers target-innocent, 0.3σ) but the per-mode 1/N_max is ASSERTED not derived, and the registry "Proved" (T1962/T1421) over-claims vs data [D] vs K666 "pending Cal." Provisional PARTIALLY DERIVED (count+integers forced / per-mode magnitude open); definitely not Proved. Flag for Casey/Cal; re-promotion test = does slow-roll force tilt-per-mode=1/N_max blind.
