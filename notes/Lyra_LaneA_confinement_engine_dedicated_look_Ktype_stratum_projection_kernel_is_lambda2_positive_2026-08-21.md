# LANE A — the confinement engine: the K-type↔stratum projection and its kernel (dedicated look)

**The one transition that closes as a *projection with a kernel* — and the kernel is the physics.** The map stays Derived; the two physical readings it *induces* are tiered, and the two forward leads are **pre-registered, not claimed** (C6). Address named before value throughout. Reconnected: the round-4 Šilov theorem (λ₂>0 ⇒ zero boundary value), the m₁=0 engine, the B₂ = SO(5) root system, T2517 (strata addresses).

## The map (Derived structure)

- **Object:** an SO(5)×SO(2) K-type, highest weight **(λ₁, λ₂)** [entry 5], and the Šilov boundary Š [entry 3]. **Verb (v):** boundary restriction (the Poisson–Szegő boundary-value map).
- **The projection Π_Š : A²(D_IV⁵) → {functions on Š}.** By the **round-4 Šilov theorem**, a K-type's boundary value **vanishes iff λ₂ > 0** (non-spherical in the short-root e₂ direction). So:
  - **ker Π_Š = { K-types with λ₂ > 0 }** — the modes that never reach the boundary (**confined**);
  - **im Π_Š = { K-types with λ₂ = 0 }** — spherical in the short root, **reach the boundary (emitted)**.
- **Tier: DERIVED-structure.** The projection and the λ₂>0 kernel are the round-4 theorem (banked); this is "closes as a projection with a kernel," not a full bijection — and the kernel is where the content is.

## What it reads (two physical readings, tiered)

1. **Confinement (the master mechanism).** A mode confined ⇔ it carries short-root weight (λ₂ > 0) ⇔ zero Šilov support. *Reading:* the bulk/boundary split of the domain **is** the confined/emitted split of the spectrum — a geometric confinement criterion, read by verb (v). **Tier: DERIVED as a criterion** (round-4); the *identification* with color confinement is a lead (below).
2. **m₁ = 0 (the lightest mode is massless at leading order).** The leading mass operator is supported on the boundary (emitted, λ₂=0); the confined sector contributes no leading mass. *Reading:* the rank-1 leading mass matrix (only the heaviest generation massive at leading order) is the boundary-supported part. **Tier: STRUCTURAL** (the link λ₂↔generation-mass is not yet the down-sector Gram computation; do NOT re-run the quark/lepton mass machinery here — mass gate closed).

## Two forward leads — PRE-REGISTERED (C6), not claimed

*Logged to the reading ledger with expected form + tolerance before any value is read; a null result is reported beside a hit.*

| lead | pre-registered form | what would confirm | what would refute | tier now |
|---|---|---|---|---|
| **L1 — color confinement is λ₂>0** | the color block **V₁₂** (the real SO(3) triplet, entry 4) carries short-root weight **λ₂ > 0**, so color modes are exactly the confined sector | V₁₂'s K-type has λ₂ > 0 *and* the λ₂=0 sector is color-singlet | V₁₂ sits at λ₂ = 0 (would reach the boundary — no confinement) | **LEAD (open)** — a clean geometric statement of color confinement *if* it lands; else a null |
| **L2 — m₁=0 ⇔ the confined generation** | the lightest generation (address ν=0, Šilov point, T2517) coincides with the λ₂>0 confined kernel at leading order | the ν=0 stratum mode is in ker Π_Š | ν=0 is λ₂=0 (emitted) | **LEAD (open)** — do NOT compute masses; test only the λ₂-membership property (the new rule: test the property, not existence) |

**Guardrail:** these are *property tests* (is V₁₂ at λ₂>0? is ν=0 in the kernel?), not mass computations. The mass gate stays closed; no lepton re-run, no Koide. If a lead lands it is a *structural* statement (which modes are confined), not a spectrum claim.

## Why this matters for the atlas

The confinement projection is the atlas's first transition whose **kernel is load-bearing** — the "private information that never reaches the boundary" (the Bergman↔Szegő ker Π, made concrete). It is the linear-algebra picture of the master mechanism: **bulk = confined = λ₂>0; boundary = emitted = λ₂=0.** Every downstream confinement reading is a reading of *this one projection* on D_IV⁵.

**Lyra, 2026-08-21 (Lane A, confinement engine). The K-type↔Šilov-stratum transition closes as a projection Π_Š with ker Π_Š = {λ₂>0 K-types} (round-4 theorem, DERIVED-structure): confined = short-root-carrying = zero Šilov value; emitted = λ₂=0 = spherical, reaches the boundary. Induced readings: confinement criterion (DERIVED), m₁=0 leading-mass (STRUCTURAL). Two forward leads PRE-REGISTERED, not claimed: L1 color-confinement = V₁₂ at λ₂>0; L2 m₁=0 ⇔ ν=0 in the kernel — both PROPERTY-tests (not mass computations; mass gate closed). Address before value; the kernel is the physics. Nothing pushed; CP existence-only.**
