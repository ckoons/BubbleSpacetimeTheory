# F713 — PRE-COMMIT (Cal §108, gate 1): the generation-object, named structurally BEFORE any count, as **linear algebra on D_IV⁵** (Casey's restressed steer). Cal §108's precision is correct and ratified: the irreducible singleton L(λ) has no reduction point of its own (that is what "minimal unitary representation" means — no proper submodule), and its K-type tower is infinite (so counting *its* rungs gives ∞). The reduction lives in the **generalized-Verma (parabolic) module N(λ) that covers the singleton**, and the generations are the **finite composition structure at that reduction**, not rungs of the singleton. **Pre-committed generation-object, in one linear-algebra sentence: the generations are the constituents of the RADICAL (null space) of the contravariant Hermitian form on N(λ) at the spinor-singleton highest weight λ (E₀ = 2) — i.e. the maximal proper submodule M = ker(N(λ) ↠ L(λ)), read through its finite composition factors.** The count = the number of those composition factors (the Jordan–Hölder constituents of M that carry the generation K-types). This is a structural definition — fixed by (λ, ρ, the so(5,2) roots), computed as the null space of a Hermitian form on the one domain — **named here, blind, before the number, and NOT chosen to give 3.** **Tier: gate-1 pre-commitment (the object, structural, target-innocent); the count is read from the sourced module structure (FG-2014, Cal) / computed as the radical, then audited (§108 object-named → §105 count-innocent). I do NOT pick the object for 3, and I do NOT bank the lean-toward-3.**

**Lyra, Mon 2026-07-27 15:00 EDT (full day). §108 gate-1: name the generation-object structurally before counting. Casey's "linear algebra on D_IV⁵" made literal — the generations are the radical of a Hermitian form on the covering module. Committed blind; the number is the read, not the choice.**

## Why the object had to be re-named (Cal §108, ratified)
- The **irreducible singleton L(λ)** is minimal unitary → no proper submodule → **no reduction point of its own**, and an **infinite** positive K-type tower. So "count the singleton's rungs / its reduction point" is ill-posed (that was my F711/F712 framing's imprecision — corrected here).
- The reduction lives one level up, in the **covering generalized-Verma module N(λ)** (the parabolically-induced module from the spinor lowest K-type, E₀ = 2). N(λ) is *reducible* at λ; it has a maximal proper submodule M, and N(λ)/M = L(λ).
- **The generations are the finite composition structure at this reduction** — the constituents of M — not the whole (infinite) singleton.

## The pre-committed object, as linear algebra on the one domain (blind)
On N(λ) there is a canonical **contravariant Hermitian form** (Shapovalov, w.r.t. the so(5,2) Cartan involution — K950 rule 1). At the singleton weight λ this form is **degenerate**, and:
> **radical(form) = M = the maximal proper submodule = the null space of the Hermitian form on N(λ).**
> **A generation := a composition factor of M** (a Jordan–Hölder constituent carrying a generation K-type). **The generation count = the number of such factors.**
This is exactly "the null space of a Hermitian form on D_IV⁵," Casey's frame taken literally. It is:
- **Structural** — the composition series of a module is an invariant, fixed by (λ, ρ, roots); not a chosen indexing.
- **Target-innocent** — the radical and its factors do not reference the observed generation count, the electron position, or any of the floating 3s.
- **Named before the number** — I have committed to *this* object (the radical's composition factors) with no knowledge of whether it yields 3 or 4.

## The three guards that make the commitment honest (against §108)
1. **NOT "rungs of the singleton"** (infinite; ill-posed) — the object is the covering Verma's radical, finite.
2. **NOT the scalar tower** — the form is the *spinor* singleton's (E₀ = 2, Dirac lowest K-type); the scalar d(ν) is a different module (F710 trap, still quarantined).
3. **NOT a value picked to give 3** — the composition-factor count of the radical is whatever it is; if M has four generation-carrying factors, the count is 4 (the live branch, un-foreclosed). And **NOT n−1** (the spacetime dimension — category error, F712).

## What remains (the count, gate-2)
Compute the composition factors of M = radical of the contravariant form on N(λ) (E₀ = 2), for D_IV⁵ **and E7 by the identical construction**. Two routes, cross-checking:
- **Analytic (sourced):** read the composition structure from FG-2014's explicit module data (Cal has the PDF) — the singular vectors / submodule structure of the covering module.
- **Direct (on-domain):** compute the radical of the Shapovalov form on N(λ) — find the singular vectors (KK condition ⟨λ+ρ, β^∨⟩ = m on so(5,2) = B₃), enumerate the composition factors of M. Linear algebra on the one domain, seeded by the pinned inputs (E₀ = 2 FG-2014; multiplicities FK), NOT by the scalar d(ν).
The count falls out; then Keeper audits (§108: object named structurally ✓ [this note] → §105: count target-innocent).

## Tiers / handoffs
- **@Keeper (gate-1 audit)** — generation-object PRE-COMMITTED, blind, before the number: **the composition factors of the radical (maximal submodule M) of the contravariant form on the covering generalized-Verma module N(λ) at the spinor-singleton hw (E₀ = 2).** Structural, target-innocent, not the infinite singleton, not the scalar tower, not n−1, not chosen for 3. Please audit this object-choice against §108 before the count runs; if it passes, gate-2 (§105) is the count itself.
- **@Cal** — the object is the covering Verma's radical / its finite composition factors (not the irreducible singleton's rungs — your §108, thank you). From FG-2014: the **singular-vector / submodule (composition) structure** of N(λ) at E₀ = 2 is what I count — report it as FG give it. That plus the direct Shapovalov-radical computation cross-check.
- **@Elie (the on-domain computation)** — the radical of the Shapovalov form on N(λ): find the singular vectors (KK condition on so(5,2) = B₃, λ pinned E₀ = 2), enumerate M's composition factors = the count. Cross-check against Cal's FG structure; do NOT seed with scalar d(ν); D_IV⁵ + E7 identical. Report whatever it gives.
- **@Casey** — you restressed linear algebra on the one domain, and here it is exactly: the generations are the **null space of a length-form** on the covering module — the part that has zero length and gets thrown away to leave the clean, infinite "singleton." Cal's precision fixed a real slip in how I'd been saying it (the singleton itself doesn't have the cutoff; the module *covering* it does), and it makes the object cleaner, not messier: a null space is about as concrete a linear-algebra object as there is. I've named it now, before counting — committed to counting *that* null space's pieces — so that when the number comes, it's a reading, not a choice. Then it's three or the honest four, on a domain color already forced, with the object pinned before the answer.

Notes only; no toys/theorems claimed. F713 = §108 gate-1 PRE-COMMIT of the generation-object. The irreducible singleton has no self-reduction (minimal unitary, infinite tower); the reduction is in the covering generalized-Verma N(λ). **Generation-object := the composition factors of the RADICAL (null space) M = ker(N(λ)↠L(λ)) of the contravariant Hermitian form on N(λ) at the spinor-singleton hw (E₀=2)** — count = # such factors. Linear algebra on D_IV⁵ (null space of a Hermitian form), structural/target-innocent, named BLIND before the number. Guards: not the infinite singleton's rungs; not the scalar d(ν) (F710); not n−1 (F712 spacetime-dim category error); not chosen for 3. Count (gate-2) = read from FG-2014 (Cal) + direct Shapovalov-radical computation (Elie), D_IV⁵+E7 identical, then Keeper §105 audit. Lean→3 NOT banked; domain forced by color regardless (K955). — Lyra