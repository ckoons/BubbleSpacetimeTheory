# THE α LANE — M-DEFINITION v0.1, against PREREG v2. **The photon re-cited to the corrected bank (the v1 ingredient carried K1687's falsified identification — found before one integral was written). Normalization Born-fixed. The family skeleton α(n) stated with its one remaining computation named.**

**Lyra, 2026-08-24. Read v2 first, then v1's carried-forward D1–D6, then the banks. This is the DEFINITION artifact — no evaluation anywhere below, per the one-shot budget; @Cal cold-reads the frozen form BEFORE any number exists; @Elie evaluates LAST. Nothing banked. Cell: External 3 (α).**

## 0. ⚠ ROUTED TO @Keeper FIRST — a stale citation inside v1's ingredient sketch
v1 §0 names *"the photon = the intrinsic J/SO(2) charge direction (K732, derived)."* **K732 (07-17) cites the ORIGINAL T2470 — the identification K1687 (08-18) explicitly CORRECTED:** *"the substrate SO(2) ≡ U(1)_em line was FALSE and had caught two CIs… that circle carries time's J… Q is sign-indefinite, one circle cannot be both. Q = T₃ + Y/2, the electroweak Cartan INSIDE the SO(5) factor."*
**My reading: correcting the citation is COMPLIANCE with D1 (every ingredient cited to its bank — the bank moved), not a deviation requiring v3** — the D1–D6 gate itself is untouched; only v1's §0 prose carried the stale label. **But the gate is yours: rule compliance-or-v3 before Cal's cold-read.** *(Had the lane computed on the v1 label, the "vertex" would have been a TIME coupling. The definition phase caught it at zero cost — this is what the phase is for, and it is the fourth CI this identification has caught.)*

## 1. THE STATES (grep-cited)
- **The electron:** the lightest charged fermion mode — an **asymptotic boundary state** (A1: asymptotic states live on the Shilov boundary, K744/K745), spinor-valued (the Δ sector, F638/K1822), charge eigenvalue **N_c·Q = −3 in bulk quanta** (K806 I-tier; m_Q chain, derived 08-24), **unit-normalized in the forced boundary (Hardy/Szegő) inner product** — the norm forced by the same automorphism-invariance that forces the Born measure (T754).
- **The photon (soft):** the **q² → 0 mode of the gauge direction Q̂ = T₃ + Y/2 ∈ so(5)** — the em Cartan **per the CORRECTED bank (K1687/T2470-corrected)**. Its mode function at the Thomson point: the constant function on Σ along Q̂, normalized in the SAME forced measure. **No second normalization convention exists anywhere in the definition — one measure, both objects (D1a's requirement).**

## 2. THE VERTEX AND THE AMPLITUDE
> **M(n) = ⟨ e | Γ_Q | e ⟩**, where **Γ_Q = the soft-photon vertex operator = Q̂ ⊗ (the normalized soft mode)**, all three factors in the one forced measure.
**The two load-bearing DERIVATION steps (named as steps, where a cheat would hide — D4's discipline):**
- **(i) Soft vertex = charge operator.** The geometric analog of the Low theorem: the q² → 0 vertex couples to TOTAL CHARGE and nothing else. **Must be derived on the domain, not imported from QED.** If this step requires a choice among vertex candidates, **D1 fails and the lane is not ready** — pre-declared.
- **(ii) Thomson = Shilov.** The scale statement (α at q²=0 is the boundary-value of the vertex) derived via A1, per D4 — and per v2 it is what selects honestly between landings A and B.

## 3. THE NORMALIZATION, BORN-FIXED (D1a — no floating power, no floating scale)
With |e⟩ unit-normalized and the soft mode unit-normalized in the same forced measure, **M is a normalized Born amplitude and α = |M|², exponent 2 by the Born rule (T754/T2401), full stop.** The only place a number can enter is the **geometric norm of the soft gauge mode against the charge lattice** — and that is not a choice, it is the computation:
> ### **THE FAMILY SKELETON: α(n)⁻¹ = 𝒩(n) / q² — where 𝒩(n) is the forced norm² of the soft gauge mode on D_IV^n's boundary in the FK/Hua-normalized measure** (the family of banked constants whose n = 5 members are c_FK·π^{9/2} = 225 and K(0,0) = 1920/π⁵ — family-varying by construction, D3's requirement), **and q is the charge in bulk quanta (integer, K806).**
**THE ONE REMAINING COMPUTATION, named:** evaluate 𝒩(n) from the banked Hua integrals at n = 3, 5, 7, 9 — closed-form Γ/π expressions, no numerics needed. **That evaluation is @Elie's, LAST, after Cal's cold-read of the completed frozen form.** *(This skeleton is v0.1: the derivation steps (i)/(ii) must be completed to fix 𝒩's exact integrand before the form is "frozen" in v2's sense. The skeleton names where every ingredient lives; it does not yet claim the integrand.)*

## 4. THE TRIPWIRES, restated against my own definition
- **D2:** 𝒩(n) is computed from Hua integrals that never reference N_max, 137, or any measured value. **If 137 appears, it must EMERGE at n = 5 — anyone recognizing it en route stops and reports.** The pre-named backdoor (re-deriving the capacity count inside the integral) is the thing step (i)'s derivation must be checked against.
- **D3:** if 𝒩(n) comes out n-independent, the computation read the rank — dead by K1774, pre-agreed.
- **D5:** the count as defined — free parameters: **0**; targets: **1**. Every normalization above is forced or the lane aborts at (i).
- **The meaning slot stays outside the gate:** Casey's write-efficiency/channel-capacity pairing is the epigraph, not an input.

**Lyra, M-definition v0.1. The photon re-cited to the corrected bank (routed: compliance-or-v3, Keeper's call). One measure, both objects, Born-fixed power. Two derivation steps named where cheats hide. The family skeleton locates every number in 𝒩(n), a banked-family Hua constant — and the integrand's completion via steps (i)/(ii) is the remaining definition work before the freeze. No evaluation has occurred. Nothing banked.**
