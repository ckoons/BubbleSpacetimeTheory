---
title: "F388 — the charm disk-fiber Szegő-over-fiber GEOMETRY SETUP (the f(ν) PRIMARY; unblocks the Grace+Elie numerical pairing; target-innocent, NO fishing of λ/q′). The up-type boundary concentration f(ν) = the deposit's spread over the rank-k Korányi-Wolf fiber (F385/F386 mechanism). For the charm (k=1 stratum): (1) FIBER = the rank-1 KW boundary component of D_IV⁵ = the disk D_IV¹ (= unit disk, the rank-1 Lie ball) [target-innocent: the k=1 boundary component IS a rank-1 disk]; (2) FK root multiplicity on the fiber a = n_C−2 = N_c = 3 (Grace's G4, forced by the C₂ root system, not a coincident 3); (3) charm position ν_charm = 3/2 = ρ₂ = N_c/2 (F377 ν=ρ-components); (4) up-type concentration law on the disk (Szegő/Poisson, max at boundary = right direction): P(r) = [(1+r)/(1−r)]^{q′}, growing toward r→1 (top/heaviest); the Yukawa = the normalized disk Szegő overlap of the ν=3/2 charm state with the disk boundary mode. TWO inputs to pin target-innocently (Grace's FK machinery, do NOT fish): (i) the inherited Szegő weight q′ on the disk fiber (F382: up uses q=n_C/2=5/2 globally; the fiber-restricted q′ from the D_IV¹ embedding + a=N_c); (ii) the ν↔r map on the fiber (N(z,z)=(1−r²)², charm at ν=3/2). ELIE'S scheme-robust target = the steepness RATIO ln(y_u)/ln(y_c) = fiber-dim ratio = n_C/rank = 2.5 (dims from F86, target-innocent) vs observed 2.296 (8%, scale-dep) — robust-er than the absolute m_c/m_t (scheme swings 0.0034–0.0073). HONEST landing target (Cal #27, Keeper #25): structure derived + bounded computation; if exact m_c/m_t lands ~8% (scheme-trap) that IS the honest tier, steepness ratio n_C/rank may land scheme-clean. NO fishing of q′ or λ; Grace pins geometry/weight from FK, Elie computes, Cal tiers at landing. ALSO: F384 CLOSED at full rigor (Grace confirmed u_μ=ê₁ target-innocent via t₂/t₁=N_c/n_C fixed by V_us before V_cb angle) → V_cb angle watertight end-to-end (cos ψ=n_C/√(n_C²+N_c²)=5/√34); angle DERIVED, magnitude STRUCTURAL ~8%. Count 8/26."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-28 Sunday (date-verified)"
status: "v0.1 — charm disk-fiber Szegő geometry setup (f(ν) PRIMARY, trio-unblock). Fiber = rank-1 KW component = disk D_IV¹; a=n_C−2=N_c=3 (Grace G4); charm ν=3/2=ρ₂; concentration P(r)=[(1+r)/(1−r)]^{q′} max at boundary. Pin target-innocently (Grace FK, no fish): (i) inherited Szegő weight q′ (D_IV¹ embedding + a=N_c; global q=n_C/2=5/2 per F382); (ii) ν↔r map (N=(1−r²)², charm ν=3/2). Elie scheme-robust target = steepness ratio n_C/rank=2.5 vs 2.296 (8%, scale-dep), robust-er than absolute m_c/m_t (0.0034–0.0073). Honest tier: structure + bounded computation; absolute may stay ~8%, steepness scheme-clean. No fishing. F384 CLOSED (Grace u_μ=ê₁ confirmation): V_cb angle watertight end-to-end (5/√34), angle DERIVED magnitude STRUCTURAL. Count 8/26. For Grace, Elie, Cal, Casey, Keeper."
---

# F388 — charm disk-fiber Szegő-over-fiber geometry setup (the f(ν) PRIMARY)

F387 (top uniqueness) is with Cal; the live concrete forward landing is the f(ν) charm computation, which the Grace+Elie pairing is waiting on. This pins the target-innocent geometry so they can fire — no fishing of λ/q′.

## The setup (charm, k=1 stratum)

The up-type boundary concentration f(ν) = the deposit's spread over the rank-k Korányi-Wolf fiber (F385/F386 mechanism). For the charm:
- **Fiber = the rank-1 KW boundary component of D_IV⁵ = the disk D_IV¹** (= unit disk, the rank-1 Lie ball). [target-innocent: the k=1 boundary component IS a rank-1 disk]
- **FK root multiplicity on the fiber: a = n_C−2 = N_c = 3** (Grace's G4, forced by the C₂ root system, not a coincident 3).
- **Charm position: ν_charm = 3/2 = ρ₂ = N_c/2** (F377 ν=ρ-components).
- **Up-type concentration law on the disk** (Szegő/Poisson, max at boundary — the right direction): P(r) = [(1+r)/(1−r)]^{q′}, growing toward r→1 (top/heaviest). The Yukawa = the normalized disk Szegő overlap of the ν=3/2 charm state with the disk boundary mode.

## Two inputs to pin target-innocently (Grace's FK machinery — do NOT fish)

(i) the **inherited Szegő weight q′** on the disk fiber (F382: up uses q = n_C/2 = 5/2 globally; the fiber-restricted q′ comes from the D_IV¹ embedding + a = N_c).
(ii) the **ν↔r map** on the fiber (N(z,z) = (1−r²)², Grace's convention; charm at ν=3/2).

## Elie's lane: the scheme-robust target

The steepness RATIO ln(y_u)/ln(y_c) = fiber-dim ratio = n_C/rank = 2.5 (dims from F86, target-innocent) vs observed 2.296 (8%, scale-dep) — robust-er than the absolute m_c/m_t (which Elie flagged swings 0.0034–0.0073). Once Grace pins q′ and the ν↔r map, compute the disk Szegő integral numerically; report the steepness ratio (scheme-clean candidate) and flag the absolute as scheme-dependent.

## Honest landing target (Cal #27, Keeper #25)

Structure derived + bounded computation. If the exact m_c/m_t lands ~8% (scheme-trap), that IS the honest tier; the steepness ratio n_C/rank may land scheme-clean. No fishing of q′ or λ — Grace pins the geometry/weight from FK, Elie computes, Cal tiers at landing.

## Also: F384 closed at full rigor

Grace confirmed u_μ=ê₁ is target-innocent (the muon's split t₂/t₁ = N_c/n_C = 3/5 is fixed by V_us, before any V_cb angle enters — the pending F361/F362 condition, met). So the V_cb angle is watertight end-to-end: u_τ=ρ̂ (ν_τ=0 floor, rigorous) + u_μ=ê₁ (V_us-fixed) → cos ψ = ê₁·ρ̂ = n_C/√(n_C²+N_c²) = 5/√34 in bare integers. V_cb: angle DERIVED end-to-end (target-innocent), magnitude STRUCTURAL ~8% (two honest tiers).

## Net

The charm-mass computation is set up as a concrete (not free-function) problem: the geometry is the disk D_IV¹ (the rank-1 boundary component), the parameters are the substrate integers (a=N_c=3, ν=3/2), and the only inputs left to pin (q′, ν↔r map) come from the established FK kernel machinery, not from fitting to the charm mass. Grace pins those, Elie integrates, the result is reported at the honest tier (steepness ratio scheme-clean; absolute may stay ~8%). No fishing. Count 8/26.

— Lyra, Sun 2026-06-28 (date-verified). F388: charm disk-fiber Szegő geometry setup (f(ν) PRIMARY, trio-unblock). Fiber=rank-1 KW component=disk D_IV¹; a=n_C−2=N_c=3 (Grace G4); charm ν=3/2=ρ₂; concentration P(r)=[(1+r)/(1−r)]^{q′} max at boundary. Pin (Grace FK, no fish): (i) inherited Szegő weight q′ (D_IV¹ embedding + a=N_c; global q=5/2 F382); (ii) ν↔r map (N=(1−r²)², charm ν=3/2). Elie scheme-robust target = steepness ratio n_C/rank=2.5 vs 2.296. Honest tier: structure + bounded computation; absolute may stay ~8%. F384 CLOSED (Grace u_μ=ê₁): V_cb angle watertight (5/√34), angle DERIVED magnitude STRUCTURAL. Count 8/26.
