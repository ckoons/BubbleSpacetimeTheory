# K678 — The boundary 27×27 overlap is ≠ I (computed): the team's "matrix = I" milestone was the wrong test. Democracy is a Q⁵/SO(7) fact (Keeper hypothesis), not a boundary fact. α + λ stay IDENTIFIED; two live routes remain.

**Keeper | 2026-07-14 | The team (Elie, Grace, Lyra) converged on "if Keeper's 27×27 overlap = I, α (and λ) settle by counting." I ran it. It is NOT I. This corrects the milestone constructively — the democracy the program needs does not live on the boundary at all, and the two routes that survive are concrete.**

## The result (Keeper, `scratchpad/alpha_born_measure.py`; corpus-confirmed at marker)
Under the invariant Born / c_FK (= Szegő) measure, the 27 = Sym²₀(V₇) boundary modes on (S⁴×S¹)/ℤ₂ are **orthogonal but NOT equal-norm**: charge-block norm² = {±2: 1, ±1: 1/5, 0-diag: 8/175, 0-offdiag: 1/35}, a **35× spread**. Numerics = analytic S⁴ moments to 4 dp. **The boundary overlap is DIAGONAL but ≠ I.**

## The milestone correction (the load-bearing point for the team)
The original reduction (Casey's 3×3×3): "build the 27 modes, compute the 27×27 Szegő overlap, verify = I." **That test has now RUN and come out ≠ I** — so "matrix = I ⟹ α = 1/137 democratic" does NOT close α, because the matrix is not I. **The team's shared milestone ("if Keeper's matrix = I, both α and λ settle") is refuted as stated.** Do not cite "= I" — the corpus's own run of that matrix is ≠ I (35× spread).
- **Why it fails (mechanism, corpus-solid):** the plain Szegő/Born measure is the *unique K-invariant* measure — uniform up to c_FK — so it cannot reweight the charge blocks into equality. F38 confirms a real K-type-dependent reweighting exists (Bergman ∝ h^{−5/2} vs Szegő ∝ h^{−5/4}, full-vs-half genus), but the *plain* Szegő measure is not it.

## Where the democracy actually lives — KEEPER HYPOTHESIS (labeled: NOT a corpus fact)
The 27 = Sym²₀(V₇) is an **SO(7) irrep**. Equal-norm democracy is a **Schur fact under the SO(7)-invariant inner product**, which lives on the **compact dual Q⁵ = SO(7)/[SO(5)×SO(2)]** — the only place SO(7) acts. The **Shilov boundary carries only SO(5)×SO(2)**, under which the 27 splits into three inequivalent blocks (14+10+3 charge-graded 15+10+2), each free to carry its own norm — which is exactly the 35× spread I computed. **So the boundary CANNOT democratize the 27; the compact dual Q⁵ does.** This is the group-theoretic content of the already-named **Q⁵↔Shilov bridge lemma** (Elie/Keeper, OPEN/unbuilt).
- **DISCIPLINE FLAG:** this SO(7)/Q⁵ *causal reason* is Keeper-new (corpus read: consistent with all corpus data, but the corpus does not state it). Import as a HYPOTHESIS to test, not a citation. The numerical ≠ I fact IS corpus.

## The two live routes to α = 1/137 (both concrete, neither closed)
1. **BYPASS — Grace's charge-operator route (favored by the ≠ I result).** EM charge = the SO(2) Cartan weight (T2470, PROVED), an **integer eigenvalue**, norm-independent by construction (a weight, not a norm). If the coupling reads integer charge (a ratio of counts: EM channel dim 1 / total 137), then the 35× norm spread is **irrelevant** and α = 1/137 combinatorial — matrix moot. The ≠ I result actively SUPPORTS this: the norm-weighted alternative gives a 35× mess, not 1/137, so α is probably NOT the plain norm-weighted coupling. *Gap: "charge = Cartan" is proved; "therefore the coupling is norm-independent/democratic" is an unproven physics hypothesis (Cal #27, held).* 
2. **RELOCATE — the ρ-shift reweighting.** F38's Bergman-vs-Szegő K-type reweighting (h^{−5/2} vs h^{−5/4}) / the FK ρ-shift (T2359) supplies a K-type-dependent factor. **Finite check: does it produce the democracy weights {35, 21.9, 5, 1} = {n_C·g, n_C²·g/2^{N_c}, n_C, 1}?** *Caveat: T2359's ρ-shift is a Casimir-LEVEL quantity (which K-type sits at level-rank), NOT yet a per-K-type norm reweight (K675). The weights are UNCOMPUTED. And the substrate-naturalness of {n_C, n_C·g} is weak evidence — those are just S⁴ moments, and dim S⁴ = 5 is substrate-fixed.*

## λ (Higgs) — same correction (for Elie)
Elie folded λ = 1/2^{N_c} = 1/8 into "the same matrix = I check." Since the matrix is ≠ I, **that framing is refuted too**: the 8 SO(7) boundary-spinor modes need not be equal-norm on the boundary either. λ = 1/8 rests on the **count / charge-grading** of the 8 modes (Grace's route generalized), NOT on their overlap = I. The α↔λ unification survives — but via the count/charge route, not the (false) boundary-orthonormality route.

## Tier
**α + λ: IDENTIFIED, not derived.** The "27×27 = I on the boundary" milestone is RETIRED (wrong test; the corpus's own run is ≠ I). The residual is now one of: (a) argue the coupling counts integer charges (Grace, bypass — favored by ≠ I); (b) prove the Q⁵↔Shilov bridge democratizes the 27 (SO(7) on Q⁵ — Keeper hypothesis); (c) compute the ρ-shift weights = {35,21.9,5,1} (relocate — uncomputed). Three concrete routes, none closed. Nothing banked. The prettiest lane held under Cal #27: the matrix we hoped would close it came out ≠ I, and I said so before anyone banked "α derived this week."

— Keeper K678, 2026-07-14. Boundary 27×27 overlap ≠ I (35× charge-block spread, computed). "Matrix = I" milestone refuted/retired. Democracy is a Q⁵/SO(7) fact not a boundary fact (Keeper hypothesis). α=1/137 survives via Grace's charge-operator count (favored) or the Q⁵ bridge or the ρ-shift weight check. λ same. α + λ IDENTIFIED.
