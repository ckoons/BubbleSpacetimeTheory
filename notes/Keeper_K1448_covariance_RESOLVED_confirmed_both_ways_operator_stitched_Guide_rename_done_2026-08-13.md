# K1448 — (1) ★ COVARIANCE FORMAT RESOLVED and confirmed two independent ways — Grace's block clears. (2) ★ FRONTIER: the curved operator is STITCHED (Lyra) — Elie measures next, after the sign/object protocol. (3) Working_Paper → **Guide** rename DONE (`git mv`, history preserved). Cal cold-reads.

**Keeper (2026-08-13, ~11:15. Grace correctly refused to ship a garbage full-covariance number and flagged the format for me. My earlier format-crack (from the DES likelihood code) IS the resolution — now cross-checked under her working python3.11+numpy: it matches to the digit and ΛCDM validates at χ²/dof=0.90, her own criterion. And the operator is finally stitched. Cal cold-reads. Nothing pushed.)**

## 1. ★ Covariance RESOLVED — it's the INVERSE covariance (Grace's block clears)
Grace hit the format wall honestly (χ²/dof = 0.002 = impossible; "unphysical" diagonals) and refused to bank it — **exactly right.** The resolution was already in hand (K1447, from the DES likelihood code) and is now **confirmed under her working environment:**
- **The stored `cov.npy` is the INVERSE covariance** (Covtot_inv), upper-triangle (row-major, i≤j), n=1820, SN order = HD.csv order. **You do NOT invert it — you use it directly in χ².**
- **Grace's "unphysical, >1" observation is actually CORRECT and confirms it:** the inverse-covariance diagonal runs **min 0.0 / median 36.7 / max 253.6** — inverse-*variance* scale, so **>1 is right, not unphysical.** Her χ²/dof=0.002 came from treating it as the covariance (inverting → the true covariance's tiny diagonal → tiny χ²). Same wall, opposite side.
- **Positive-definite:** smallest eigenvalue = 4.6×10⁻⁶ > 0 → a valid inverse covariance.
- **The contaminant SNe Grace found (MUERR up to 468, down-weighted non-Ia) are handled automatically** by the inv_cov (buried) — no manual exclusion needed.
- **★ ΛCDM validates at χ²/dof = 0.897** (Om=0.315; 1632.6 over 1820 SNe) — **which IS Grace's validation criterion (ΛCDM near 1), MET.** Confirmed two ways: my pure-Python helper (`des_sn5yr_chi2_helper.py`, numpy-free) = 1632.6, and a numpy/python3.11 reconstruction (exact DES-code convention) = 1632.6.
- **The "format spec" Grace asked me for: it's the DES likelihood code itself** (`DES-Dovekie-SN_Likelihood.py`, pulled to `notes/data_external/`), lines 79–88 — upper-triangle fill + symmetrize + "Covtot_inv is the inverse."
- **⟹ Grace is unblocked for the definitive fit:** use the validated helper (or numpy under **python3.11** — default python3's numpy is broken, python3.11's works: 2.2.4). Run the three model μ(z) through `marg_chi2` → the full-covariance verdict → upgrades T2560 from diagonal to definitive. **A clean convergence: Grace's refusal-to-ship-garbage + Keeper's format-crack = the resolution, and both did their job.**

## 2. ★ FRONTIER — the curved operator is STITCHED (Lyra); the sign protocol gates the measurement
**Lyra stitched the operator:** the missing piece was one **connection term** (how the space bends as a particle moves), computed from the geometry, **zero at the center** (the flat spot) and **nonzero elsewhere** (genuine curvature), dropped into the operator. The four verified pieces — curved kernel (F947) + grading (F954) + causal structure (F952) + curvature — are now **one operator, runnable in the reference file.** B1's frontier is unblocked; **Elie measures next.** But first, the sign/object protocol (Elie 5222 + Cal §458), which is load-bearing:
- **★ Elie's c is a SQUARED gap — non-negative BY CONSTRUCTION, not convention:** D is exactly Hermitian (‖D−D†‖ = 0), so D² ⪰ 0 and the smallest eigenvalue **cannot** be negative. He literally **cannot measure −8.75.** And |ρ|² = +35/4 is a squared norm, positive by definition. So **Lyra's −8.75 is a curvature-side quantity** (legitimately negative for the noncompact domain), and the map to the gap carries a **relative minus sign that must be written down.**
- **★ Cal's two blind gates (both committed before the stitch, §458):** (1) **Independence** — Lyra's ρ→8.75 formula exhibited without Elie's number, Elie's measurement without peeking. (2) **Sign convention** — both state explicit conventions (sign of R, Laplacian, signature) first; re-express in one common convention; a match needs magnitude AND sign; the sign flip must **trace to a documented source** (the likely one: **Lichnerowicz R/4** — scalar curvature R < 0 for the negatively-curved domain → −8.75; the D²/mass²-type gap constant > 0 → +8.75). Same-object first (curvature vs metric-coefficient vs D²-constant — the −10-vs-metric point). **The number (8.50 / 8.75 / neither) counts only when both gates pass.** "Neither" stays the informative outcome.
- **⟹ Route: Lyra states the sign bridge (−8.75 curvature → +8.75 squared gap, via Lichnerowicz R/4) and the object identity; Cal certifies both gates; THEN Elie measures blind.** This is the one-sentence fix that makes the test falsifiable — and it's the moment the ρ debate becomes a number.

## 3. Working_Paper → Guide — DONE
Per Casey's decision: `git mv Working_Paper Guide` (history preserved, no delete+add churn); the PDF builder's curated list re-pointed to `Guide`; internal navigation refs inside `Guide/` fixed (0 remaining). The Curriculum scoping cross-ref + the memory/CLAUDE.md pointers are left for the accuracy sweep. **`Guide/` is now the narrative-canon front door.** (Also: the RETIRE sweep's front-matter Millennium fixes from K1446 stand; PDFs re-sync via the script.)

## Route
1. **★ Grace — run the definitive full-covariance fit** (three models via the validated helper / numpy-under-python3.11) → T2560 diagonal → definitive; ΛCDM already validates at 0.90. Fold the tiered falsifier board into the Falsifiable paper.
2. **★ Lyra — state the sign bridge + object identity (−8.75 curvature → +8.75 gap, Lichnerowicz R/4), for Cal to certify;** then hand the operator to Elie. + narrative pilot when exemplars land.
3. **★ Elie — measure blind the instant the sign gate certifies** (±0.05, four branches, "neither" reserved). The ρ debate becomes a number.
4. **Cal — certify both gates (independence + sign) before the number counts.**
5. **Keeper — covariance resolved for Grace (+validated both ways); Guide rename done; RETIRE sweep continuing; PDF script owns Guide; accuracy sweep as background.**

— Keeper, K1448, 2026-08-13. (1) COVARIANCE RESOLVED (Grace's block clears): stored cov.npy = INVERSE covariance (Covtot_inv), upper-triangle i≤j, n=1820, SN order=HD.csv; DON'T invert, use directly in χ². Grace's "unphysical >1" CONFIRMS it (inv-cov diagonal min0/med36.7/max253.6 = inverse-variance scale, >1 correct); her χ²/dof=0.002 = treated-as-covariance-and-inverted. Positive-definite (min eig 4.6e-6). Contaminants (MUERR≤468 non-Ia) auto-buried by inv_cov. ΛCDM validates χ²/dof=0.897 (=1632.6/1820) = GRACE'S OWN criterion MET; confirmed BOTH ways (pure-Python helper 1632.6 + numpy/python3.11 1632.6). Format spec = DES likelihood code (DES-Dovekie-SN_Likelihood.py lines 79-88, pulled). python3.11 numpy WORKS (2.2.4); default python3 broken. ⟹ Grace unblocked → definitive T2560 via validated helper. Convergence: Grace's refuse-garbage + Keeper's format-crack = resolution. (2) FRONTIER: Lyra STITCHED the operator (added connection term = curvature, zero at center nonzero elsewhere; curved kernel+grading+causal+curvature = one runnable operator); Elie measures next. SIGN PROTOCOL (Elie 5222 + Cal §458): Elie's c = squared gap, NON-NEGATIVE by construction (D Hermitian ‖D−D†‖=0 → D²⪰0), cannot measure −8.75; |ρ|²=+35/4 positive by def; Lyra's −8.75 = curvature-side (negative for noncompact), map to gap carries relative minus. Cal 2 blind gates: independence + sign-convention (state conventions, one common frame, match needs magnitude AND sign, flip traces to Lichnerowicz R/4: R<0→−8.75 vs gap-constant>0→+8.75; same-object-first curvature-vs-metric-vs-D²). Number counts only when both gates pass; "neither" informative. Route: Lyra state sign bridge + object → Cal certify → Elie measure blind. (3) Working_Paper→Guide DONE (git mv history-preserved, PDF script re-pointed, Guide internal refs fixed 0 remaining; Curriculum-scoping + memory refs for accuracy sweep). Route: Grace definitive fit (validated helper/numpy-3.11) + fold board; Lyra sign bridge + hand operator + narrative pilot; Elie measure blind on sign-gate; Cal certify both gates; Keeper covariance-resolved + Guide-rename + RETIRE-continuing + PDF-owns-Guide. Cal cold-reads. Nothing pushed.
