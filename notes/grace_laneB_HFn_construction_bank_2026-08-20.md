# Lane B — banking the H_F(n) construction: the census leg, now rigorous across the family (Grace + Elie, 2026-08-20)
*Round 16. Elie built H_F(n); this banks it and LIFTS my Round-15 scope note. Two of three census blocks are n-invariant; only the spinor moves. Absorbs K1743: census is multiplicity-free (ℝ at mult 1, NOT M₃), N_c=n−2 is a separate rep-dimension reading. Registry-ready for Keeper.*

## The construction
The finite space H_F(n) for D_IV^n decomposes (Peirce, relative to a rank-2 frame) with dims **(1, n−2, 1)**: two rank-1 frame pieces + the off-diagonal **V₁₂ of dim n−2** (color). Total 2 + (n−2) = n. At n=5: 1+3+1 = 5 = N_c + rank. (Corpus: T2511/T2545/T2527/T2548, V₁₂ = n_C−2 = N_c.)

## ★ The reality-type sweep across n (the census leg, now FAMILY-RIGOROUS)
| Block | Rep | Reality across n | Moves? |
|---|---|---|---|
| **charge** | SO(2) rep (FS=0) | **ℂ for all n** | n-INVARIANT |
| **color** | V₁₂ = SO(n−2) vector | **ℝ for n≥5**; ℂ at n=4; none at n=3 | n-invariant on **n≥5** |
| **spinor** | Spin(n) spinor | **Bott mod-8:** ℍ (n≡3,4,5) · ℂ (n≡2,6) · ℝ (n≡0,1,7) | ★ the ONLY block that moves |

**⟹ RE-DERIVED LIFT (n≥5, not "all n"):** the color block reality is ℝ **iff** V₁₂ carries a real *irreducible* SO(n−2) vector **iff SO(n−2) is non-abelian iff n−2 ≥ 3 iff n ≥ 5.** At n=4, SO(2) is ABELIAN → its 2-vector splits into conjugate characters → color reality = **ℂ** (the isolated accident); at n=3, SO(1) is trivial → no color. So on the physical range **n ≥ 5**: charge = ℂ (all n) and color = ℝ (n≥5) are n-invariant, and only the spinor moves (Bott) → the census leg is **family-rigorous on n≥5**, with n=4 the checked exception. This is the derivation of the lift, not a re-word: the validity domain n≥5 is FORCED by the SO(n−2)-non-abelian condition (the same §644 fact that "real-color rides on non-orientability"). **LIFTS my Round-15 scope note** on n≥5. (Registry T2568 carries "n≥5"; this note re-derives WHY.)

## The census separator D_IV⁷, concretely
At n=7 (≡7 mod 8) the spinor is **ℝ** → U(ℝ)=O(1)=0-dim → EW size = 0(color)+1(charge)+**0**(spinor) = **1**, NOT 4. **No SU(2)_L.** So D_IV⁷ separates the census concretely (the SU(2)_L reading dies), confirming the separator is D_IV⁷ (my Round-15 correction), and D_IV⁴ (still ℍ) is NOT a census separator.

## ★ K1743 absorbed — two distinct color readings, kept separate
1. **CENSUS reading (multiplicity-free):** color contributes reality type **ℝ at multiplicity 1** — NOT M₃(ℝ) (that was the bare-End slip). So the uniform gauge-SIZE read is **0+1+3=4**: dim U(ℝ)=O(1)=**0** (color → 0 to EW; SU(3) imported), dim U(ℂ)=**1** (hypercharge), dim U(ℍ)=Sp(1)=**3** (SU(2)_L). ONE rule — both agree because U(ℝ)=O(1) is 0-dim. **No two-line check; "7=g" dies** (it came from ℝ-at-mult-3). Lyra's original single-line census read was right.
2. **REP-DIMENSION reading (separate):** **N_c = dim V₁₂ = n−2 = 3** at n=5 — the NUMBER of colors, a rep-dimension, NOT a census multiplicity. This is where the "3" of color lives; it feeds SU(3) as gauge completion (imported), not the EW census.

**The two must not merge:** census reads the color TYPE (ℝ → 0 to EW, SU(3) imported); rep-dimension reads the color SIZE (n−2 = N_c). Merging them was the M₃(ℝ) error.

## What this banks
- **The census leg is rigorous across the D_IV family** (charge ℂ + color ℝ n-invariant, spinor Bott-swept). Round-15 scope note LIFTED.
- **N_c = n−2** as the color rep-dimension reading, SEPARATE from the multiplicity-free census.
- **A_F = ℂ⊕ℍ⊕ℝ (ℝ mult 1)** everywhere; M₃(ℝ) retired; 0+1+3 uniform; 7=g dead.
- The census separator is **D_IV⁷** (spinor ℝ → EW size 1); D_IV⁴ is the twist separator.

**Tier:** the census leg upgraded from spinor-row-only (Round 15) to FAMILY-RIGOROUS (this build). **Edges:** T2511/T2545/T2527/T2548 (V₁₂=n−2), the census-instrument note, my Round-15 scope note (LIFTED), K1743. **@Keeper:** registry entry — H_F(n): Peirce (1,n−2,1), census family-rigorous, N_c=n−2 separate rep-reading. **@Elie:** co-author (you built it); confirm the Peirce dims + the charge-ℂ/color-ℝ n-invariance are what the toy shows.
