# Grace — the INPUT FLOOR: everything BST takes as input, stated GR-plainly (2026-08-04, roadmap (D))
**Purpose:** the honest, complete, GR-benchmarked list of BST's inputs — so the papers and one-page state them plainly, neither over-claiming "zero inputs" (nothing is zero-input) nor apologizing (every theory takes inputs). This is the structure, not a shortfall.

## The floor — three kinds of input, and nothing else
| kind | BST's input | GR's counterpart | tier |
|------|-------------|------------------|------|
| **1. One framework premise** | spacetime = the domain's conformal descent (D_IV⁵ → SO(5,2)→SO(4,2)→SO(3,1) → 3+1) | a pseudo-Riemannian manifold + Lorentzian signature | premise (posit, stated) |
| **2. Two dimensionful scales** | the commitment tick t_B (the unit) + the cosmic age (sets the cc value) | Newton's G + the cosmological constant Λ | Derived-given-the-tick for all other dimensionful quantities (anchor-convention #9) |
| **3. Per-instance boundary data** | the single measurement outcome; the neutrino mass ratio (observed — BST forces only m₁=0); the cc *value* (age-set) | initial data on a Cauchy slice | boundary datum (input by construction) |

## The one claim that survives — and it's the strong one
BST is **not** zero-input. What is remarkable is narrower and hostile-read-proof: **the entire DIMENSIONLESS structure — every Standard-Model ratio, mixing angle, and count — is forced from the one premise with ZERO additional dimensionless free parameters.** The inputs are all either (a) the one geometric premise, (b) dimensionful scales (a *unit* + one age, the way GR states G and Λ), or (c) per-instance boundary data (the way every theory states initial conditions). No dimensionless knob is ever turned.

**So the honest headline:** "One geometric premise, two dimensionful scales, and per-instance boundary data — and from those, the dimensionless content of the Standard Model with nothing left to tune." That is FEWER structural inputs than GR (one fixed domain vs a free manifold+metric) and the SAME kind of boundary data — stated GR-plainly, not apologetically.

## Why each is a floor item, not a gap (the calibrate-both-ways line)
- **The premise** is a posit like GR's manifold — you cannot derive the arena from nothing; you posit the shape and derive its consequences. BST's shape is maximally constrained (D_IV⁵ is *unique* under the five-integer forcing), so the posit is one object, not a family.
- **The two scales** are dimensionful — no dimensionless theory can produce a dimensionful number without a unit. The tick is the unit; the age is the one place a *second* scale enters (the cc value). Every physical theory takes ≥1 dimensionful input; BST takes 2.
- **The boundary data** — the single measurement outcome is a boundary datum (Casey's compute-from-now: reality is the commit-sequence, each commit the initial condition of the next), the neutrino ratio is observed (BST derives only m₁=0), the cc value is age-set. These are inputs by construction, not derivations BST failed to do.

## Use
For the papers, the one-page, and any external framing: state the floor exactly this way. It is the resource that lets a referee see precisely what is forced (all dimensionless SM structure) versus what is input (one premise + two scales + boundary data) — and see that the input list is *shorter* than GR's, not longer. Pairs with the QM-axioms tier map (single-outcome = boundary datum #10) and the tier ledger.

## Update 2026-08-05 — the floor STAYS at ONE premise (Closure route retired)
Yesterday I flagged the input floor *might* grow to two premises if item-10 (matter fermionic) reduced to the Substrate Closure Principle. **Casey retired that scenic route (K1170): the direct Jordan/Peirce decomposition forces the fermion structurally — no Closure argument, no controller regress, no supplied-vs-forced meta-question.** The store/mediate split is the DERIVED Peirce decomposition of the D_IV⁵ spin factor (T2511/T2527: J_1⊕J_½⊕J_0 = 1⊕3⊕1, diagonal idempotents = fermionic storage, J_½ = color mediator). So:
- **Substrate Closure is NOT on the item-10 critical path.** The floor stays: **1 premise + 2 scales + boundary data.** No second premise.
- On the Code-Forces-Fermion theorem stamp, **"matter is fermionic" moves from posit → Derived** — it was never an input; it's forced by the substrate's own algebra. The boundary-data list is unchanged (fermionic was never in it).
- My over-hedging correction, recorded: spawning the "corollary-vs-posit" meta-question was the mirror of over-claiming. The algebra answers it directly; take the direct structural route.
