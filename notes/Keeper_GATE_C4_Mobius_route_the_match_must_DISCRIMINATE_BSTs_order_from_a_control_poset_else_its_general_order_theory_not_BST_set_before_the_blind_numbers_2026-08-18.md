---
node_type: keeper_gate
id: GATE-C4-Mobius
title: "The hard gate on the C4 Möbius route (Elie's live candidate, K1673), set BEFORE the blind numbers land — because I over-ratified two closures today and will not over-ratify a third. Extends GATE-C4. THE ROUTE: Elie found the Möbius function μ=ζ⁻¹ of BST's derived causal order (T2564) is the ONE graded object that passes the necessary criteria (magnitudes>1, fence-admissible/order-only, order-indexed); the pending test is whether μ's magnitudes reproduce the Benincasa–Dowker layer coefficients (4D: 1,−9,16,−8). THE TRAP (same shape as 'the signs are the free half', one level up): BOTH μ (=ζ⁻¹, inverts the order relation) AND the BD operator are ORDER-THEORETIC INVERSIONS. If the BD magnitudes are a known function of ANY locally-finite order's Möbius/interval/layer structure (a theorem of causal-set combinatorics, not BST physics), then EVERY poset's μ relates to its BD operator the same way → a CAN'T-FAIL match carrying ZERO BST-specific information — exactly the empty-confirmation failure ([[feedback_empty_confirmation_cant_fail_test_and_circular_hunt_mechanism]]). A magnitude match alone would then be the 'free half' at the next level, not evidence."
date: 2026-08-18
author: Keeper
status: "OPEN — set before Elie's blind μ-vs-BD numbers. PASS requires a DISCRIMINATING match (BST's order succeeds where a control poset fails); a match that holds for any order is the FLOOR (general order theory, C4 stays scaffold K1390)."
---

# GATE-C4-Möbius — the match must discriminate BST's order, or it's general order theory

Set before the numbers, deliberately. I've ratified two over-broad closures today (the color SO(3) swing; the C4 'closed structurally'); the corrective is to fix the bar *before* Elie's blind result can pull me.

## The four bars (all required for PASS)
1. **Blind (protocol, already committed):** μ-weights written and committed before BD is opened — as the 5345/5346 runs did.
2. **No continuum □ imposed:** μ is defined by the order alone (fence-admissible — Elie established this; heat-kernel coefficients fail here, μ passes). Any step that inserts "require ⟨Bφ⟩→□φ" is empty.
3. **★ DISCRIMINATION — the load-bearing new bar:** the magnitude match must be SPECIFIC to BST's derived order. Run a **control**: a *different* locally-finite order (a generic/Kleitman–Rothschild poset, and/or a wrong-dimension order) computed the same way. If the control's μ *also* reproduces the BD magnitudes (or the same continuum operator), the match is a **general order-theory identity** (μ ↔ BD holds for any poset) and carries no BST-specific content — FLOOR. PASS requires: **BST's order reproduces BD, the control does NOT** (or reproduces a *demonstrably wrong* operator). Only a match that separates BST's order from a control is BST physics.
4. **Zero free choices in the layer→index map:** BD is indexed by causal-LAYER (order-distance k); μ is indexed by PAIRS (x,y). The map from μ's pair-structure to BD's layer-coefficients must be FORCED by the order (e.g., μ binned by interval-cardinality/height), not chosen to fit. Count the choices before the numbers; >0 tunable choices = not forced.

## PASS vs FLOOR
- **PASS (C4 promotes: spectral action = causal action via the Möbius weighting, #4 crown jewel closes):** all four bars — a blind, □-free, FORCED-map magnitude match that DISCRIMINATES BST's order from a control.
- **FLOOR (C4 stays scaffold, K1390):** the match is construction-guaranteed (holds for any order), or needs □ imposed, or needs a tuned layer→index map. Named honestly: the derived causal ORDER stands (the real result, #122); spectral=causal via Möbius is not established.

## The honest expectation (stated so it can be wrong)
Rota's theory makes μ = the alternating chain-count, a *general* order invariant; BD's operator is *also* a general causal-set construction. So my prior is that the μ↔BD relation is likely **general order theory** (bar 3 fails) — which would be the FLOOR. I state this so the blind test can prove me wrong: if BST's order matches BD and a control poset does NOT, that is a real, discriminating result and C4 promotes. I will ratify PASS only on the discrimination, not on a bare magnitude agreement.

## Route
- **Elie — include the CONTROL in the blind run:** compute μ→(candidate BD magnitudes) for BST's derived order AND for a generic/KR control poset (and ideally a wrong-dimension order), weights committed first. Report both; the discrimination (BST passes, control fails) is the whole result.
- **Keeper — will PASS only on bars 1–4, discrimination included; FLOOR otherwise.** Set before the numbers, per the sweep-before-closure lesson (K1673).

— Keeper, GATE-C4-Möbius, 2026-08-18. The μ-vs-BD match must DISCRIMINATE BST's order from a control poset — because both μ and BD are order-theoretic inversions, a match that holds for any order is general order theory (the free half, one level up), not BST physics. Four bars: blind · no-□ · discrimination · forced layer-map. PASS only on discrimination; else FLOOR (scaffold K1390, derived order stands). Set before Elie's blind numbers. Nothing pushed.
