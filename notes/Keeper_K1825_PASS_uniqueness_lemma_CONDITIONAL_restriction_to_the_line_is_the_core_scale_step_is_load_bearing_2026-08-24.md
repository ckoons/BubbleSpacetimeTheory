# K1825 — Keeper pass on the Uniqueness Lemma: CONDITIONAL PASS. The conclusion stands; the architecture misplaces the load — and both fixes SHORTEN the lemma.
**Keeper, 2026-08-24, ~13:30. In sequence (Grace's marks → winding ruling → lemma → THIS PASS → Cal →
Elie). Read at source. Verdict: CONDITIONAL PASS — two MODERATE findings, one MINOR, no CRITICAL.
The corrected claim ("the RESTRICTION to the soft mode is one-dimensional") is true and its proof is
simpler than the lemma's.**

## What is right, first
The §0 correction owned up front (the invariant-quadratic space is infinite-dimensional; its restriction
is not) — exactly the honest form. The tower's death certificate is the right KIND: m(0)=0 dies on the
object — degeneracy, not preference. The θ = 0 / total-derivative demotion to consistency-notes is
correct discipline. The citation direction (the gate encodes the physics, never the reverse) is fixed as
demanded. Nothing evaluated, nothing banked, scope held.

## M1 (MODERATE) — Step 1 claims more than the proof needs, and the surplus is where a referee lands
"Every Aut-invariant gauge-invariant quadratic = ∫F·m(Δ)·F + topological" over-claims three ways:
(a) **rank 2 ⟹ the invariant algebra has TWO generators** — the multiplier must live in the full
commuting algebra, not m(Δ) alone; (b) F is a 2-form — multipliers act per K-type of the form bundle,
not as one scalar function; (c) **boundary-supported quadratics** (∫_Σ A·(…)·A) are not visibly in the
bulk-multiplier + topological decomposition.
**THE FIX, which deletes the exhaustiveness burden entirely: the load-bearing core is
RESTRICTION-TO-THE-LINE.** The soft mode spans a one-dimensional subspace; **on a one-dimensional
subspace, EVERY quadratic form is a scalar multiple of the norm² — linear algebra, no Schur, no
enumeration needed.** N4 forces the scalar positive. The multiplier picture then survives as the
explicit REALIZATION, and the death certificates (tower, topological) become corollaries answering the
referee's named families — kept, but no longer carrying the theorem.

## M2 (MODERATE) — Step 3 is the actual load-bearing step and is one sentence short
With M1, the lemma reduces to: Q|_soft = c·‖·‖²_{L²(Σ,µ_forced)}, c > 0. **The entire content is
c = 1 — and the lemma must say WHY in its own text, not only in the reconciliation:** a kinetic
coefficient c rescales the physical amplitude by 1/√c, so α⁻¹ = c·𝒩/q² — "c absorbs into the coupling"
IS the KK statement. c = 1 is the canonical-amplitude convention **in the T754-forced measure**: the
measure is forced (T754/T2401), the amplitude normalization is the Born rule's, so the convention is
pinned by banked physics, not chosen. One added sentence stating this — with the reconciliation cited
as the mechanism — closes the one hole a hostile referee will actually aim at ("you chose units").
Also cite Elie's 4715 fact-report here (the arc's own proportionality class, no number — provenance
clean).

## m1 (MINOR) — the topological family's degree bookkeeping
"Parity-odd and indefinite" is asserted; state in one line which form-degree pairing makes an
F∧F-type term exist on this space at all. (The N4 kill is independent and already sufficient; this is
presentation.)

## Verdict
**CONDITIONAL PASS.** Conditions: M1 (restructure around restriction-to-the-line; demote the multiplier
form to realization) + M2 (the c = 1 sentence with T754 citation and the KK-absorption mechanism).
m1 at author's discretion. Both conditions SHORTEN the lemma. On their landing the freeze packet
(lemma + M-definition v0.1 + steps (i)/(ii) + reconciliation) goes to Cal — still pending Grace's marks
and the winding ruling, per the frozen sequence. The D2 quarantine and the landing table are untouched.

*— Keeper, K1825. The rival list was never the hard part — on a line there are no rivals. The hard part
is the scale, and the scale is where the banked physics does its one job. Say so, and the lemma is done.*
