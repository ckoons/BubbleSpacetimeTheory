# R99 — Keeper's verification criteria, PRE-FILED BLIND before any shot posts
**Keeper, 2026-08-26, 08:5x (clock-verified). The §770 move applied to my own step: these are the
checks I will run on each lane's posted shot, written while zero results exist. A verification that
invents its criteria after seeing the numbers is a rubber stamp with extra steps.**

## Common to all three lanes (checked from the artifact, never the message)
C1 — SEQUENCE: values/exponents posted BEFORE the verdict, timestamps in-file; the verdict cites the
frozen limb by name, no reinterpretation language anywhere.
C2 — QUARANTINE GREP: the lane's named quarantined tokens (280 · Λ_obs · 2/α · Beck/Zel'dovich for Λ;
target masses pre-band for P; "closure" language for I's V₁₂ case) absent from the derivation chain,
present only in the landing comparison where the table allows.
C3 — CONTROLS IN THE LOG: must-catch AND must-reject outputs shown in the shot's own artifact, run
this session, not cited from history.
C4 — COUNT-PRINTING: free-vs-forced counts beside the verdict where the prereg requires them; any
fitted parameter ⟹ the cap applied visibly (SUGGESTIVE-max), not footnoted.
C5 — BUDGET: one shot; no second form anywhere in the file; the exhaustive-landings table used as
frozen, every limb accounted.
C6 — ARITHMETIC RE-DERIVATION: at least one load-bearing number per lane re-derived independently by
me before certification (the convention of any ratio metric checked FIRST — the 5.90-vs-6.26 lesson).

## Lane-specific
P — the W4 bands applied at the FROZEN values (WIN 1.8796–1.8985; the m_τ ±0.09 band traveling); the
5487 must-fail's down-sector output shown with its non-pass; W2a's capability result stated with the
W2b honesty clause intact (no capability language in the verdict).
I — the non-equivariance theorem cited to its bank (#67 §2a / the Peirce-stabilizer argument) with
the citation resolving; the bracket exact (no numerics standing in for algebra); if spin-3 = 0, the
V₁₂-compression reading applied FIRST (known accident ⟹ zero new content) before any other reading;
the DERIVED-CONDITIONAL cap visible in the verdict line itself.
Λ — the two-post separation real (form post and number post distinct, ordered, both on disk); the
equation's structural demand (response vs step) exhibited BEFORE a₁ enters; every object in the
assembled balance carrying its cite; the K1057 CONDITIONAL cap visible in any landing; the
ladder_heat subscript throughout.

## The refusal, pre-stated
A shot failing any C-line gets returned for the missing exhibit — not failed, not passed, RETURNED —
exactly once; a second return converts to a FAIL of the shot's own making. Certification language
("Keeper certified") attaches only after every line above passes.

*— Keeper. Filed blind. The checker's criteria are now as frozen as the shots they will check.*

## Amendment (08:5x, pre-shot, per Cal §771 PIN I-1)
Lane I verification adds: **the composition order of W = commit_op ∘ S_Δ must be exhibited as
TYPE-FORCED by the banked objects** — the shot artifact shows the typing argument, or the I-1(b)
choice-demand fires NOT-READY. (Added before any shot exists; still blind.)
