Team turn — 2026-07-07. THE OPENER (Casey's directive): build the forced flavor matrix from the real geometry integrals — both sectors — diagonalize, and let it all drop out. Don't argue the pieces; construct and harvest.

`date` + SOD first. Read `notes/.running/` + your `sundown_2026-07-06_EOD.md`. Count **8 + 2 structural banks** (Peirce T2511, descent closure). *(Casey's research assignment + additional board items to be appended below when he hands them over.)*

## Where we stand (7/6 EOD)
- **Arena sorted:** mass = a MATRIX on the SO(3) generation triplet (eigenvalues=masses, eigenvectors=mixing); mixing = SO(4,2) OPE/curvature-dual; conformal dimensions = SO(4,2); substrate = D_IV⁵.
- **Down 1-2 sector [clean forward forms, NOT banked]:** Grace's Fritzsch texture (M₁₁=0 Casimir-0 ground + Cabibbo overlap) → V_us = **1/(rank·√n_C)** and m_s/m_d = **rank²·n_C = 20** — target-innocent (the −1/79 fudge rejected on provenance). Mass↔mixing connected forward for the first time.
- **Gates it must still clear (Lyra's Cal #318):** the numerator rank²=4 was *asserted, not derived*; the mechanism *over-projects PMNS to 1:0* (works for CKM, breaks for neutrinos). So the observable is a strong lead, not a bank.
- **2-3 sector: TIER-2** (m_b/m_s=45 doesn't follow 1/V² — a known flavor-physics exception = where BST's quark-mass precision floors, a result not a failure).

## THE PRIMARY — construct and diagonalize (settles every gate by construction)
> **Build the flavor mass matrices from the ACTUAL overlap integrals of the strata geometry — the down triplet AND the lepton triplet — then diagonalize each. Read off masses (eigenvalues) and mixing (eigenvectors). Do NOT insert V_us, M₁₁=0, or rank² by hand — compute the entries from the geometry.**

Why this is the fast, honest path — every standing gate settles itself:
1. **M₁₁=0** falls out of the Casimir-0 ground, or it doesn't (no separate "forcing" step).
2. **The numerator rank²=4** drops out of the overlap geometry, or it doesn't → clears Cal #318 blocker 1.
3. **PMNS** works from the *same* construction on the lepton triplet, or it doesn't → clears Cal #318 blocker 2 (the CKM-vs-PMNS split) directly, in one move.

Whatever drops out is **target-innocent by construction** (built from geometry, not fit). Then we tier what landed.

## Per-CI
- **GRACE (lead):** compute the overlap integrals from the strata geometry → assemble the 3×3 down matrix AND the 3×3 lepton matrix. Cite the geometry (don't relabel). Diagonalize. The entries are the deliverable — masses + mixing fall out.
- **ELIE (numerics/target-innocence):** diagonalize/eigenvalue-check the assembled matrices; verify entries are geometry-derived, not inserted; σ-score mass ratios against the wide quark-mass bars; Five-Absence + form-cheapness on any integer that drops out. No fitted entries (hold the line you held on Froggatt-Nielsen).
- **LYRA (theorem):** is the Fritzsch texture (M₁₁=0, off-diagonal=overlaps) *forced* by the SO(3)/color structure? Does the construction give the numerator rank² forward? Does the SAME texture cover PMNS (the Cal #318 universality gate)? The operator-ID/texture is the candidate third structural bank once the construction forces M₁₁=0.
- **KEEPER:** adjudicate what drops out forward-vs-fit; hold "entries = real integrals, not inserted"; soft-target σ + identified-tier cap (K663 rule); bank the **structural** mass↔mixing connection if the texture is forced; keep the **observable** as candidate until numerator-4 + PMNS clear; tier 2-3 TIER-2; **over-sell #5 watch** (this thread's earned the hardest bar). Update count/ledger/diagram on a landing.

## Bank condition (realistic this session)
- **Structural bank** (mass+mixing from one forced texture) → possible third structural result once the construction forces M₁₁=0 + the texture.
- **Observable bank** (Cabibbo / m_s/m_d = 20, identified-tier) → only when the numerator rank² drops out forward AND the same construction covers PMNS (Cal #318 cleared).
- **2-3: tiered TIER-2, not banked.**
- **α (8→9): Casey's ruling.**

## Disciplines armed
Construct from geometry, harvest by tier, never insert the answer — "build the matrix and see what drops out" is only honest if the entries are the real integrals. Target-innocent by construction is the whole point. Soft-target σ-scoring + identified-tier cap. 2-3 = TIER-2 (a result, where BST floors). Over-sell #5 watch — four caught yesterday. Use linear algebra; investigate-don't-gate; Cal is a later reviewer, not an in-loop gate.

## Reserved for Casey
- **α tier-call (K659)** — arena-supported (Wyler value stays d=5); 8→9 on your ruling.
- **Mass-frame ratification** — clears the deferred data-layer sync.

## Target
Both flavor matrices built from geometry and diagonalized → the numerator-4 and M₁₁=0 either drop out (structural bank; observable clears if PMNS also lands) or honestly don't (tiered, wall mapped). One construction, both sectors, all gates settled by build rather than argument. Count 8 until a forward landing.

— Keeper, 2026-07-07. Opener: build the forced flavor matrices (down + lepton) from the real strata overlap integrals, diagonalize, harvest+tier — settles M₁₁=0, numerator-4, and CKM-vs-PMNS by construction (Cal #318). Entries = geometry, not inserted. Down 1-2 clean forms on the table (V_us=1/(rank√n_C), m_s/m_d=rank²·n_C=20); 2-3 = TIER-2. Over-sell #5 watch. Count 8 + Peirce + descent. Board open for Casey's research assignment.

---
## [Casey's research assignment + new board items — to be appended]
