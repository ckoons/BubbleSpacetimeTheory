# F623 — The (A)/(B) fork resolves to (B), on the math. Wigner–Eckart separates the ANGULAR selection rule (yes/no) from the RADIAL overlap (a magnitude). If the 3 generations are RADIAL COPIES of one angular K-type (the natural reading of "3 strata = 3 generations of one field"), the angular selection rule is IDENTICAL for all three — so if the top couples, charm couples too, just with a smaller radial overlap. **No texture zero is possible; the hierarchy is continuous radial suppression → (B), Tier-2.** An exact texture zero (A) would require the generations to differ *angularly*, which contradicts the one-field-three-strata structure. The data confirms it (non-integer powers 3.8, 4.2, 2.6, 2.0 — not clean {4,4,2,2}). The up/down asymmetry is real and *located*: saturation sets the anchor (top at the boundary vs bottom in the interior) → the radial-falloff rate → λ⁴ vs λ², as a **continuous** effect, not a derived integer. Honest-negative on the integers; a genuine result on the structure.

**Lyra, Tue 2026-07-21. Casey: think hard, contribute to the math.** Here's the resolution of the fork Keeper set up (K796), and it's decidable from the rep theory + the structure — not just the data.

## The decisive rep-theory argument
The Yukawa is an SO(5)-invariant with the condensate in the vector (1,0):
$$ y \sim \langle f_L \,|\, O_{(1,0)} \,|\, f_R\rangle \;=\; \underbrace{(\text{does } f_L\otimes f_R^* \supset (1,0)?)}_{\text{ANGULAR — Clebsch, yes/no}} \times \underbrace{(\text{reduced matrix element})}_{\text{RADIAL — a magnitude}}. $$
**Wigner–Eckart separates the angular selection rule (a yes/no) from the radial overlap (a number).** Now the key structural fact:

**If the 3 generations are RADIAL COPIES of ONE angular K-type** — the natural reading of "3 Korányi–Wolf strata = 3 generations of *one* fermion field" (they differ by *where they localize*, |z|, not by their angular rep) — then:
- The **angular selection rule is IDENTICAL for all three** (same K-type ⟹ same Clebsch).
- The top couples (allowed: top = O, CG = 1). ⟹ **charm couples too** (same angular rule), just with a **smaller radial reduced matrix element**.
- **⟹ NO texture zero is possible.** You cannot have the *same* angular rule be "allowed" for the top and "forbidden" for charm — that's a contradiction. The kernel-block is nonzero (radially small, but nonzero) → σ₂ ~ ε¹ → **continuous suppression → (B).**

**(A) — an exact texture zero — REQUIRES the generations to differ ANGULARLY** (different K-types so the rule flips between top and charm). That contradicts the one-field-three-radial-strata structure. So **(A) is only available if generations are genuinely different angular K-types**, which the "3 strata of one field" picture says they are not.

## The data confirms (B) (independent check)
Effective powers (ratio = λ^p): m_c/m_t → **3.8**, m_u/m_c → **4.2**, m_s/m_b → **2.6**, m_d/m_s → **2.0**. **Scattered non-integers**, not the clean {4,4,2,2} that an exact grading (A) would give. Non-integer, running-dependent powers are the *signature of continuous overlap suppression* — (B). So the data leans (B) regardless of the structural argument, and the two agree.

## The up/down asymmetry is real and LOCATED (the positive result under B)
(B) does not mean "nothing is explained." The hierarchy and the up/down asymmetry both have a geometric origin — just a *continuous* one:
- **Up-type anchors at the SATURATED top** (top = O, *at* the boundary) → the radial overlap falls **steeply** away from the boundary → up-type steps ~ λ⁴.
- **Down-type anchors at the UNSATURATED bottom** (y_b ≈ 0.02, interior-ish) → **gentle** radial falloff → down-type steps ~ λ².
So **saturation MATTERS** — it sets *where the ladder anchors* (boundary vs interior), hence the *falloff rate* — but as a **continuous** radial suppression, not a derived integer. This is the honest home of F621/F598's intuition: saturation sets the *rate*, not a clean power.

## Verdict — sharper honest-negative, and a real result
- **The fork resolves to (B).** The up-type λ⁴ is a **continuous radial-overlap suppression, NOT a derived integer.** No texture zero (generations = radial copies → shared angular selection rule). **Row = Tier-2** (Elie's flag, now with a *reason*, not just a data-scatter).
- **What IS derived / located (publishable):** the hierarchy is **radial localization on the F86 strata**; the **up/down steepness asymmetry is located in the saturation difference** (boundary-anchored top vs interior-anchored bottom); the **exact powers are localization-dependent (Tier-2)**. That's a genuine, referee-defensible statement: *BST locates the flavor hierarchy in radial wavefunction localization and locates the up/down asymmetry in the top's ceiling-saturation, while being honest that the precise powers are continuous/running-dependent, not clean integers.*
- **The one thing that could still give (A):** if the generations turn out to be genuinely *different angular K-types* (a discrete-series ladder, not radial copies), a grading could forbid the up-type coupling. But (i) that's not the natural "one field, three strata" reading, and (ii) the data's non-integer powers already lean against it. **So (A) is disfavored on both structure and data.** If anyone pins the (a,b) addresses and finds different angular K-types with a genuine grading, revisit — but don't round 4.2 down to 4 to manufacture (A) (Keeper's bar).

## Tiers / handoffs
- **Fork → (B): the hierarchy is continuous radial suppression, NOT a derived integer.** Rep-theory reason (radial copies share the angular selection rule → no texture zero) + data (non-integer powers). Row Tier-2.
- **Located (publishable):** hierarchy = radial localization; up/down asymmetry = saturation-set anchor (boundary vs interior); exact powers Tier-2/localization-dependent.
- **(A) disfavored** (needs different angular K-types, against the one-field structure + non-integer data). Don't manufacture integers.
- **@Cal** — (B) resolves on the math: radial copies of one angular K-type share the Wigner–Eckart selection rule, so no texture zero is possible; the coupling is allowed for all generations, hierarchy = continuous radial overlap. Data (non-integer p) confirms. λ⁴ = NOT a derived integer; row Tier-2. Gate (A) OUT unless someone exhibits genuinely different angular K-types with a grading (and even then the data leans against). This is the honest close.
- **@Elie** — your invariance harness: under (B) the σ₂-power is a *continuous* running-dependent number (matches your ε¹-generic + weak-basis-invariance); no exact ε² texture zero because the angular rule is shared. The honest-negative branch is the right one. Confirm: radial copies (same K-type, different radial overlap) → σ₂ ~ (continuous overlap), non-integer.
- **@Keeper** — fork resolved to (B): hierarchy = continuous radial suppression (radial copies share the angular selection rule → no texture zero); up/down asymmetry = saturation-set anchor (continuous, not integer). Bank the *located* result (radial localization + saturation-anchor asymmetry, Tier-2 powers) as the honest-negative-that's-a-result. (A) disfavored on structure + data; don't bank integers. This closes the quark-hierarchy row as the sharper honest-negative you predicted — with the mechanism located.
- **@Grace** — your ⊥⊥-block reduction was right; the resolution is that radial copies force the block nonzero (shared angular rule) → (B). Render: hierarchy = radial localization; up/down asymmetry = saturation anchor (boundary vs interior); powers Tier-2 continuous, NOT clean integers. Your sphericity idea pointed at the right mechanism-family (λ₂/boundary) but the correct criterion is the shared angular selection rule under radial copies.

Notes only; no toys/theorems claimed. — Lyra
