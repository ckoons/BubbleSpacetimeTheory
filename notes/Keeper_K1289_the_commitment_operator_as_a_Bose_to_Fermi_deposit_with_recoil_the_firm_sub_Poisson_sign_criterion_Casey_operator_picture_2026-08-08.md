# K1289 — Casey's full operator: commitment = a Bose→Fermi *deposit-with-recoil* map. + The FIRM sub-Poisson criterion (the correlator SIGN, forced by T2543). + boilerplate=readability=sub-Poisson is ONE fact.

**Keeper, 2026-08-08 PM. Casey drew the full "stores-information / generates-fermions" operator and asked how we FIRMLY know sub (not super). Capturing both — his operator picture (a synthesis target) and the firm criterion (a rigor sharpening). Candidate/Framework tier except where noted. Cal audits; Lyra owns the synthesis. Λ stays Structural.**

## The firm sub-Poisson criterion (rigor — this is the forced part)
"Not Poisson" is the curvature floor. **Sub-vs-super is decided by the SIGN of the connected density–density correlator ⟨nᵢnⱼ⟩−⟨nᵢ⟩⟨nⱼ⟩, fixed by the exchange statistics of what is counted:** bosons bunch (positive → super), fermions anti-bunch (negative → sub). No regime-dependence, no free choice. For Λ we count committed **records = fermions (T2543, derived)** → negative correlator → **sub-Poisson, FORCED.** Only the Fano *magnitude* (0.73→1) drifts with occupancy; the *sign* is locked by T2543.
**Operational test (firmer than the Fano value):** compute the connected correlator of committed records on D_IV⁵ and check it is **negative** — a blind yes/no. @Grace/@Elie: verify the SIGN, not just the number.

## Casey's full operator: commitment = Bose→Fermi deposit-with-recoil
One chain: **exterior bosonic field → [commitment: deposit + push] → interior fermionic record.**
- **Exterior/continuum:** the bosonic Bergman/holomorphic field amplitude — super-Poisson (Elie's Perelomov object). "The field before it commits."
- **The dip + deposit:** the commitment operator (item-10 idempotent) PROJECTS the field amplitude onto a record on the **Shilov boundary**. This projection IS the **Bose→Fermi flip** — a coherent bosonic amplitude → a Pauli-exclusive fermionic record. *This is why Elie's super-Poisson (input) and Grace's sub-Poisson (output) are the two ends of ONE operator; commitment is the map between them.*
- **The "slight push" (Casey):** depositing a record has a recoil = an emission impulse. Candidate: **the recoil IS the a₁ (Einstein–Hilbert) rung — gravity as the back-reaction of the deposit.** Ties a₀ (fluctuation) to a₁ (curvature) through one act. CANDIDATE, right shape, not banked.
- **Output:** a fermion (the record), on the boundary, carrying its Pauli boilerplate.
**Synthesis target (Lyra + Keeper):** assemble these into ONE operator whose input statistics are super-Poisson and output statistics are sub-Poisson, with the commitment as the flip. Pieces all exist (Bergman field / item-10 projection / Shilov site / T2543 fermion / a₁ push); the assembly is the work.

## Boilerplate = readability = sub-Poisson (ONE fact, three languages)
Random source = maximum entropy = Poisson. A structured codebook with **boilerplate** carries less entropy → fluctuates less → sub-Poisson. **Pauli exclusion IS the boilerplate** (F846: Pauli = the grammar). The same boilerplate makes records **distinguishable/readable** (Elie's redundancy→objectivity plateau; Lyra's error-correction). So *records are readable BECAUSE they carry structure, and that same structure makes their count sub-Poisson* — readability and sub-Poisson are one coin. **Held as ONE fact, not stacked as independent votes** (Pauli / anti-bunching / boilerplate / low-entropy = the same statement).

## Discipline flag (Λ-territory): 0.27 is OCCUPANCY, not a codebook size
p ≈ 0.27 (Fano ≈ 0.73) is the **current filling fraction** of the record-sea — regime-dependent (→ Poisson when dilute, Grace), **cosmological, not geometric.** Do NOT weld it to a literal "27 codewords." The **codebook SIZE** (number of distinguishable records the ladder supports; alphabet = Pauli {0,1}) is a *different, possibly-geometric* number worth computing on its own (candidate: a K-type/2^g count). Compute the codebook size independently; let the occupancy be the occupancy. (This is the seam where a beautiful number wants to become an unearned claim — Grace refused 10^137.8=N_max for the same reason.)

## Tier
- **Forced/Structural:** the sub-Poisson SIGN (T2543 + exchange statistics; the correlator-sign criterion).
- **Candidate mechanism:** the deposit-with-recoil operator; the "push = a₁" identification; the codeword/boilerplate picture (F846-tier).
- **Synthesis target:** the full Bose→Fermi commitment operator (Lyra+Keeper).
- **Open:** #79 (is the descent/geometry FORCED). Λ value stays Structural.

— Keeper, K1289, 2026-08-08. Firm sub-Poisson = the correlator SIGN (negative, forced by T2543 records-are-fermions); test the SIGN not the Fano value. Casey's operator: commitment = Bose→Fermi deposit-with-recoil (exterior Bergman field → item-10 projection onto Shilov boundary → fermionic record), input super-Poisson / output sub-Poisson with commitment as the flip; the "slight push" = candidate a₁/gravity-as-recoil (ties a₀ to a₁). Boilerplate=Pauli=readability=sub-Poisson = ONE fact. 0.27 is cosmological OCCUPANCY not a geometric codebook size (compute codebook size separately). Synthesis target: the full commitment operator (Lyra+Keeper). Cal audits. Nothing pushed.
