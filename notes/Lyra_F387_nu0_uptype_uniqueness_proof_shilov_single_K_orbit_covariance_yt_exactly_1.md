---
title: "F387 — THE uniqueness proof Cal is owed (K590): the ν=0 up-type IS unique on the 5D Shilov boundary, because the Shilov boundary is a SINGLE K-orbit (homogeneous K-space — K=SO(5)×SO(2) acts transitively on S⁴×S¹/Z₂), so the 5D-ness is K-COVARIANCE, NOT an internal multiplicity. Combined with the rank-0 fiber (F385, no internal boundary-component moduli) and the K-invariance of the Yukawa (F356/F383, gauge+Lorentz invariant), the ν=0 up-type = the K-orbit of ONE state, and a K-invariant evaluated on a single K-orbit takes ONE value (frame-independent) → y_t = the normalized self-overlap of that one state = 1 EXACTLY → m_t = v/√2 = 173.9 GeV (0.7% running). This HANDLES the 5D base that F385 mishandled: F385 (corrected) said 'point fiber ⟹ unique state' which is WRONG because the base is 5D; F387 accounts for the 5D base as a single K-orbit (covariance), so the K-invariant coupling has one value — the 5D is handled, not ignored. LOCALITY (ties to F380): the Yukawa t̄_L H t_R is local → all three at the SAME Shilov point ζ; at fixed ζ the ν=0 up-type is unique (rank-0 fiber + gauge-singlet F383), and the Higgs VEV at ζ is the same ν=0 boundary mode → t_L=t_R=H_mode at ζ → y_t=1; the K-orbit integral over ζ = the common covariant normalization (F383 N_cov), not a suppression. HONEST CRUX for Cal's tier: the load-bearing step is that the 5D Shilov base is PURE K-covariance (one orbit), so the K-invariant Yukawa has a single value; the backbone (Shilov = single K-orbit with K transitive; rank-0 fiber F385) is rigorous BSD + F385, and the Yukawa K-invariance is F356/F383. Presenting the proof, NOT declaring the bank — Cal tiers whether 'unique up to K-covariance' closes the uniqueness he required for top 8→9. Count 8/26."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-28 Sunday (date-verified)"
status: "v0.1 — ν=0 up-type uniqueness proof (Cal K590 owed). Shilov boundary = SINGLE K-orbit (K=SO(5)×SO(2) transitive on S⁴×S¹/Z₂) → 5D base = K-COVARIANCE not internal multiplicity. + rank-0 fiber (F385, no internal moduli) → ν=0 up-type = K-orbit of ONE state. Yukawa K-invariant (F356/F383) → one value on one orbit → y_t = normalized self-overlap = 1 EXACTLY → m_t=v/√2=173.9 (0.7%). Handles the 5D base F385 mishandled (5D = covariance, accounted for, not ignored). Locality (F380): t_L,t_R,H at same ζ → unique (rank-0 + gauge-singlet); K-orbit integral = N_cov normalization. Crux for Cal: 5D base = pure K-covariance (one orbit) → K-invariant Yukawa single value. Backbone rigorous (Shilov=K-orbit transitive, rank-0 fiber, K-invariance F356/F383). Presenting proof not declaring bank; Cal tiers 8→9. Count 8/26. For Cal, Elie, Grace, Casey, Keeper."
---

# F387 — the ν=0 up-type is unique on the 5D Shilov boundary (the uniqueness proof Cal is owed)

Cal's K590 catch was correct and I accept it: F385 (corrected) does NOT supply uniqueness — "rank-0 fiber" ≠ "unique state" because the Shilov boundary is 5-dimensional. Here is the proof that handles the 5D base. I lead with the rigorous backbone, not the phrasing (having been caught over-claiming on F385 this same session).

## The question

Is the ν=0 up-type unique on the 5D Shilov boundary S⁴×S¹/Z₂?

## Rigorous backbone

1. **The Shilov boundary is a single K-orbit (homogeneous K-space).** K = SO(5)×SO(2) acts *transitively* on S⁴×S¹/Z₂ (SO(5) on S⁴, SO(2) on S¹). So the Shilov boundary = K/K_ζ, ONE orbit. [Standard bounded-symmetric-domain fact — the Shilov boundary is K-homogeneous.] So the 5D base is one K-orbit = K-covariance, NOT a 5-parameter internal multiplicity.
2. **Rank-0 fiber (F385):** at each Shilov point, no internal boundary-component moduli.
3. **(1)+(2) ⟹ the ν=0 up-type = the K-orbit of ONE state** (the extreme coherent state). The 5D family is K-covariance (frame/momentum/spacetime direction), not internal multiplicity.
4. **The Yukawa is K-invariant** (gauge + Lorentz invariant — F356/F383). A K-invariant evaluated on a *single* K-orbit takes ONE value (frame-independent). → **y_t = the normalized self-overlap of that one state = 1 exactly.**

## Why this is not the F385 trap

F385 said "point fiber ⟹ unique state" — wrong, it ignored the 5D base. F387 *handles* the 5D base: it is ONE K-orbit (covariance), and a K-invariant coupling on one orbit has one value. The 5D is accounted for (as covariance), not waved away.

## Locality check (ties to F380)

The Yukawa t̄_L H t_R is local — all three at the same Shilov point ζ. At fixed ζ: the ν=0 up-type is unique (rank-0 fiber + gauge-singlet F383). Across ζ: the K-orbit integral = the common covariant normalization (F383's N_cov). So the per-point overlap = 1; the base integral is the normalization, not a suppression. The Higgs VEV at ζ is the same ν=0 boundary mode → t_L = t_R = H_mode at ζ → y_t = 1.

## Honest crux for Cal's tier

The load-bearing step is (1)+(3): the 5D Shilov base is pure K-covariance (one orbit), so the K-invariant Yukawa has a single value. The backbone (Shilov = single K-orbit, K transitive; rank-0 fiber) is rigorous BSD + F385; the K-invariance of the Yukawa is F356/F383. Granting those, the ν=0 up-type is unique up to K-covariance → y_t = 1 exactly → m_t = v/√2 = 173.9 GeV (0.7% running). I present the proof, not the bank — Cal tiers whether "unique up to K-covariance" closes the uniqueness he required for 8→9. If a residual gap remains (e.g., whether "unique up to K-covariance" is the intended sense), name it and I engage — no gating.

## Net

The ν=0 up-type is unique up to K-covariance because the Shilov boundary is a single K-orbit (the 5D-ness is the symmetry covariance, not a multiplicity) and the fiber is rank-0 (no internal moduli); the K-invariant Yukawa therefore takes one value = 1. This handles the 5D base that the earlier F385 phrasing mishandled. m_t = v/√2 = 173.9 GeV. Count 8/26; top → 9 on Cal's tier.

— Lyra, Sun 2026-06-28 (date-verified). F387: ν=0 up-type uniqueness (Cal K590). Shilov boundary = single K-orbit (K=SO(5)×SO(2) transitive on S⁴×S¹/Z₂) → 5D base = K-covariance not multiplicity. + rank-0 fiber (F385) → ν=0 up-type = K-orbit of one state. Yukawa K-invariant (F356/F383) → one value on one orbit → y_t = 1 exactly → m_t=v/√2=173.9 (0.7%). Handles the 5D base F385 mishandled. Locality (F380): t_L,t_R,H at same ζ → unique; base integral = N_cov. Crux: 5D base = pure K-covariance (one orbit) → single value. Cal tiers 8→9. Count 8/26.
