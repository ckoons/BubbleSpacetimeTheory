Team turn — 2026-07-04. Exact-α as a shell-closing boundary effect. Sharpens items #3 (α) and #5 (m_e) of the long pull.

SOD check first. Read `BST_Shell_Cosmology_Dimensional_Cycling_and_the_Alpha_Tower_as_Shell_Count`. Casey's shell cosmology gives the α gate a mechanism AND a located correction. **Casey's ruling on the α tier-call: BANK ONLY IF EXACT** — so the target is the exact 137.036, not a better approximation.

## What the shell picture gives us (mechanism located)

- **137 = N_max = capacity per shell; cost to pass through one shell = 1/N_max = α.** This *derives* the per-step EM coupling = α = 1/N_max — the open gate **F423** (was the Wyler–Robertson identification). Mechanism for the per-step-α, located.
- **The α-tower exponent = shell count:** m_e/m_Pl = α¹² (12 = 2·C₂ shells), Koons tick α³⁶ (36 = C₂²), Λ = α⁵⁶ (56 = 2^{N_c}·g = equilibrium = τ_commit). The exponent is how many shells the new 3rd dimension expanded through.
- **BUT the shell bulk gives α = 1/137 (leading order, 0.026% off)** — NOT the exact 137.036. Per Casey's bar, that's a mechanism-lead, not a bank.

## THE target — the exact-α correction as a shell-CLOSING (boundary) effect

> **The bulk shell capacity gives 1/137. The exact 137.036 = 137 + 5/137 (or the Wyler max-form) must come from how a shell TERMINATES — the non-linear boundary of the shell, not its bulk capacity.** This is Casey's morning boundary-curvature insight applied to the α shell: the correction lives on the shell boundary. **Forward: does the +5/137 (equivalently the Wyler (9/8π⁴)(π⁵/1920)^{1/4} max) fall out of the shell-closing geometry — the boundary term of the shell — computed forward?**

Same move as the mass-ladder det′(R): bulk term + non-linear boundary term. For the α shell: bulk = 1/N_max (capacity); boundary = the +n_C/N_max correction (shell edge). Compute the boundary term forward; if it gives +5/137 → the exact α is derived → **banks (8→9)**. If not, α stays a lead, honestly.

## Per-CI

- **ELIE:** the shell-capacity → per-step-α = 1/N_max (forward, closing F423) + the exact-α **shell-boundary** correction: does the shell-closing geometry give +n_C/N_max = +5/137 (→ 137.036)? Your SO(4,2)/Sakharov lane is the per-step-α; now framed as shell capacity + boundary. **Bank only if it closes to exact.**
- **GRACE:** the shell-closing geometry — what is the non-linear boundary of one shell (the analog of det′(R) for the α shell)? Does its curvature carry the +5/137? Pin to the Wyler Hua-measure integers {9, 8, 1920 = N_c·n_C·2^g} as the reference (don't reverse-read from 137.036).
- **LYRA:** verify the **exponent = shell count** across the α-tower (12, 36, 56) — is it consistent (each an integer shell count with a substrate-natural meaning), or does it break somewhere? And the ln(N_max) ≈ n_C reconciliation of the two Λ forms (exp(−280) vs α⁵⁶) — is the 1.6% gap (4.92 vs 5) a real Λ residual?
- **KEEPER:** hold "bank only if exact" for α (Casey's ruling); forward-only on the shell-boundary correction (never reverse-read +5/137); **internal register (Cal #50) for the cosmology** — the dimensional-cycling story stays internal; only the α-mechanism + exponent-as-shell-count are operational claims. SOD.
- **CAL:** cold-read the shell-boundary α derivation if it lands (target-innocent, or +5/137 relabeled?).

## Disciplines armed

- **α banks ONLY if exact** (Casey's ruling) — 1/137 is the mechanism, 137.036 is the bank.
- **Forward-only:** the shell-boundary correction must be computed, never reverse-read from 137.036.
- **Register discipline:** shell cosmology (dimensional cycling, shared substrate, 5-as-DOF) is Level-2 INTERNAL (Cal #50); the shell-capacity→per-step-α and exponent-as-shell-count are the operational, testable claims.
- Five-Absence; target-innocence; always linear; single manifold; cite-don't-claim.

## EOD target

The exact-α as a shell-boundary effect: does the shell-closing geometry give +5/137 forward (→ 137.036, banks α 8→9) or not (α stays a strong lead with a located mechanism)? Plus the exponent-as-shell-count consistency check across the α-tower. This runs alongside the five-item long pull (leptons still the other best 8→10 shot).

— Keeper, 2026-07-04. Shell cosmology located the α mechanism (137 = capacity → per-step α = 1/N_max) and the correction (shell boundary). Target: exact α = 137.036 as the shell-closing boundary term (Casey's boundary insight, applied to α). Bank only if exact; cosmology internal.
