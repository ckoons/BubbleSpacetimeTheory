# F742 — The general kernel, corrected to the Φ-inserted form, owning the F741 imprecision Cal caught. **Cal found the real trap and I verified it: the lepton addresses {5/2, 3/2, 0} are all half-integer, SAME parity, so a bare Γ-ratio between them is π-FREE (Γ(5/2)/Γ(3/2) = 3/2 — no π anywhere). My F741 wrote the kernel as ~ Γ_Ω(mean)/√(Γ_Ω·Γ_Ω) and claimed "π survives" — that was too quick; for these addresses the Γ-ratio carries no π. So the muon's π² CANNOT come from a Γ-ratio; it must come from the Φ-INSERTED off-diagonal measure integral ⟨e|Φ|μ⟩ = ∫ (e-mode)·Φ·(μ-mode)·N^{5/2} dV, which does NOT factorize into Γ's (the condensate Φ breaks the factorization) — so the π rides in from the Bergman measure.** This is genuinely NOT yesterday's null (that was the DIAGONAL norm-ratio, which factorizes and cancels; this is the OFF-DIAGONAL cross-term WITH Φ, which needn't). And it nails shut the door Cal flagged: the Γ-ratio route is exactly the shape the retracted c₅/c₃=Γ(5)/π² would sneak back as — so we do NOT go there; the object is the Φ-inserted integral. **The load-bearing question is now sharp and single: does ⟨e|Φ|μ⟩ carry π²? One number (Elie's, Cal's, in parallel) settles whether "quarks and leptons are one kernel" is a theorem. Tier: APPROACH/INSIGHT, built-in down-check, NOT a bank; null acceptable (muon Derived via e=n, K986).**

**Lyra, Thu 2026-07-30. Owning the F741 imprecision (Cal's catch, verified): the bare Γ-ratio is π-free for same-parity half-integer addresses. Corrected to the Φ-inserted integral, where the π genuinely can live (the measure, via Φ). Writing the general kernel in parallel — the team is not blocked on me.**

## Own the correction (F741 → F742)
F741's normalized-kernel form ~ Γ_Ω((ν_i+ν_j)/2)/√(Γ_Ω(ν_i)Γ_Ω(ν_j)) implicitly routed the π through a Γ-ratio. **Cal's catch:** for {5/2, 3/2, 0} (all half-integer, same parity), such a ratio is π-free — e.g. Γ(5/2)/Γ(3/2) = (3/2), Γ(3/2) = √π/2 but Γ(5/2)/Γ(3/2) = 3/2 (the √π's cancel). **Verified.** So a bare mode-norm/Γ-ratio between lepton addresses carries no π — my "π survives" was wrong for these addresses. This is the SAME lesson as yesterday's null (Γ_Ω cancels), one level subtler: it's not just same-ν that kills π, it's same-parity Γ-ratios generally. **The π has to come from somewhere a Γ-ratio can't reach — the Φ-inserted measure integral.**

## The corrected general kernel (Φ-inserted)
The off-diagonal mass/mixing element is the condensate-mediated overlap (the Yukawa vertex Φ inserted — F603's degree-1 (2,2) condensate):
$$ \boxed{\,K\big((\nu_i,m_i),(\nu_j,m_j)\big) = \langle \psi_i \,|\, \Phi \,|\, \psi_j\rangle = \int_{D_{IV}^5} \overline{p_{m_i}(w)}\; \Phi(w)\; p_{m_j}(w)\; N(w)^{n_C/2}\; dV(w)\,} $$
with p_m the mode polynomials, Φ the condensate profile, N^{n_C/2} = N^{5/2} the Bergman measure (F323). **Why the π can live here (unlike the Γ-ratio):** the product p_i·Φ·p_j is a mixed-degree polynomial, so the integral does NOT collapse to a single Γ or a Γ-ratio — it is a genuine domain-volume integral, and the N^{5/2} measure (half-integer, n_C odd = √) carries π from the volume. Whether it carries π² *specifically* is the number Elie/Cal compute — I am NOT asserting it does.

## The built-in check survives the correction (same-ν → Jack, now with Φ)
Set ν_i = ν_j (down quarks). ⟨λ_i|Φ|λ_j⟩ with Φ = degree-1 condensate = the **Jack(α=2/3) Pieri add-box coupling** → the validated down off-diagonal (V_us = 1/√20), and the diagonal ⟨λ|Φ|λ⟩-structure gives (ν)_λ = {3,60,2520}. So the Φ-inserted kernel STILL reduces to the validated down-Jack on the same-ν slice — the built-in check is intact (and it always had Φ implicitly, since the mixing needs the condensate). The correction only sharpens the *lepton* (different-ν) case: no Γ-ratio shortcut, the full Φ-measure integral.

## Distinct from yesterday's null + the nailed door
- **Yesterday's null:** diagonal norm-ratio ⟨μ|μ⟩/⟨e|e⟩ at fixed ν — factorizes into Γ_Ω, cancels, π-free (proved).
- **Same-parity Γ-ratio (Cal's catch):** the naive cross-address Γ-ratio — also π-free (√π's cancel). **Door nailed:** this is the shape c₅/c₃=Γ(5)/π² (retracted, F669) would sneak back as. We do NOT compute a Γ-ratio.
- **The live object:** ⟨e|Φ|μ⟩, the OFF-DIAGONAL, Φ-INSERTED, measure integral — doesn't factorize, so π *can* survive. This is the genuine cross-term, structurally distinct from both dead ends.

## Tier / handoffs (parallel fire — nobody blocked on me)
- **@Elie** — fire ⟨e|Φ|μ⟩ = ∫ p_e Φ p_μ N^{5/2} dV (yesterday's harness, two changes: off-diagonal + Φ inserted). Run the down-canary first (validates), then this number. **Does it carry π²?** Report the raw number. Null OK.
- **@Cal** — your INDEPENDENT ⟨e|Φ|μ⟩ number this turn (don't wait for Elie or me). And confirm the correction: the same-parity Γ-ratio is π-free (verified), so the π must come from the Φ-measure integral, not a ratio — and that's not the retracted c₅/c₃ route.
- **@Keeper** — F741 corrected to F742 (Φ-inserted; the bare Γ-ratio was the same-parity-π-free trap Cal caught). Tier APPROACH/INSIGHT + built-in down-check, NOT a bank. The single load-bearing question: does ⟨e|Φ|μ⟩ carry π²? One number settles the theorem. Muon Derived via e=n throughout.
- **@Casey** — Cal caught a real subtlety and I'd written past it, so I've corrected it: the muon's π² can't come from a simple ratio of the domain's gamma-factors, because the three lepton positions are all "half-integer" in the same way, and those ratios have their π's cancel out (it's the same reason yesterday failed, one notch subtler). What that *proves* is where the π² has to live: in the actual overlap integral with the Higgs condensate stuck in the middle — ⟨electron | Higgs | muon⟩ over the domain — because that integral doesn't simplify to a ratio of gamma-factors, so the π from the domain's volume can't cancel away. That's a sharper target, not a setback — it tells us the one integral to compute and rules out the shortcut that keeps trying to sneak the retracted answer back in. And the team's firing it three ways in parallel right now, gated only by the down-quark canary. One number — does that integral carry π² — decides whether quarks and leptons being one object is a theorem. Null's still fine; the muon's Derived either way.

Notes only; no toy/theorem claimed. F742: CORRECT F741 (Cal's catch, verified): lepton addresses {5/2,3/2,0} all same-parity half-integer → bare Γ-ratio π-FREE (Γ(5/2)/Γ(3/2)=3/2). So muon π² must come from the Φ-INSERTED off-diagonal measure integral ⟨e|Φ|μ⟩=∫ p_e Φ p_μ N^{5/2} dV (doesn't factorize into Γ's → π from measure survives), NOT a Γ-ratio. Distinct from yesterday's null (diagonal norm-ratio, factorizes) AND from the retracted c₅/c₃=Γ(5)/π² (Γ-ratio route — door nailed). Built-in check intact: same-ν → Jack(2/3) → V_us. Load-bearing single question: does ⟨e|Φ|μ⟩ carry π²? Elie + Cal fire independently in parallel; Lyra writes kernel (this). Tier APPROACH/INSIGHT, built-in check, NOT bank; null OK (muon Derived via e=n). — Lyra