---
title: "Wallach→gauge: rank and N_c each do DOUBLE DUTY (generation + gauge) (Grace, 2026-06-29). The two SM non-abelian gauge groups' dual-Coxeter numbers ARE the generation-structure substrate invariants: h^v(SU(3)_color)=3=N_c (= the Wallach/FK multiplicity a=n_C-2, T2500, the #418 color su(3)⊂g₂⊂so(7)) and h^v(SU(2)_weak)=2=rank (= the domain rank that gives rank+1=3 generations, T2504/T2505). So rank=2 → {3 generations (rank+1) AND SU(2)_weak} and N_c=3 → {Wallach generation-multiplicity AND SU(3)_color AND FK measure}. Each internal primary ties the generation sector to the gauge sector. Cal #35: shared-primary CROSS-ROLES (one N_c seen in several places), NOT N independent forcings — do NOT over-count. Five-Absence clean: NO GUT relation (gauge-GROUP structure, not a unified coupling; does NOT produce sin²θ_W=3/8). Elie lead (flagged): one-loop QCD b_0=(7/3)N_c=7=g (n_f=2N_c). Tier STRUCTURAL, target-innocent; NOT a count bank (gauge couplings running-limited, separate)."
author: "Grace"
date: "2026-06-29 Monday"
status: "STRUCTURAL spine for the gauge frontier (Lyra QFT lane). Gauge dual-Coxeter = generation-structure substrate invariants; rank & N_c double-duty. Cal #35 cross-roles (not over-counted). Five-Absence clean. Coupling values open/running-limited. Count 9/26 (no bank)."
---

# Wallach→gauge: rank and N_c each do double duty

The weekend closed the fermion sector through the rank-2 substrate (3 generations = rank+1 = Wallach set =
Korányi-Wolf support flag; T2504/T2505). This note opens the gauge frontier (Lyra flagged it; Elie's
dual-Coxeter β lead) by noting the same rank-2 + multiplicity-N_c structure constrains the gauge GROUP.

## The double duty

| substrate primary | generation role | gauge role |
|---|---|---|
| **rank = 2** | rank+1 = **3 generations** (Wallach / KW support flag) | **h^∨(SU(2)_weak) = rank** |
| **N_c = 3** | Wallach/FK **multiplicity a = n_C−2** (T2500; the radial Vandermonde) | **h^∨(SU(3)_color) = N_c** (su(3)⊂g₂⊂so(7), #418, T2496/T2497) |

So the **non-abelian gauge dual-Coxeter numbers ARE the generation-structure invariants**: the same `N_c`
that is the Wallach generation-multiplicity is `h^∨(SU(3))`; the same `rank` that gives `rank+1`
generations is `h^∨(SU(2))`. The generation sector and the gauge sector are tied through `rank` and `N_c`.

## Discipline (held)

- **Cal #35:** one `N_c` appearing in the FK measure + the gauge dual-Coxeter + the Wallach multiplicity is
  **cross-roles of one primary**, NOT N independent forcings. Do not over-count it as a multi-confirmation.
- **Five-Absence (ran it):** NO GUT relation — this is the gauge-GROUP structure (which groups, their
  `h^∨`), not a unified coupling. It does **not** produce `sin²θ_W = 3/8` (the GUT value I got burned on
  earlier). Clean.
- **Coupling VALUES are NOT claimed** — they run (boundary-condition / scale-limited), Lyra's QFT lane.
  This note is the group-structure spine, not the couplings.

## Elie's β lead (flagged, his to develop)

One-loop QCD: `b_0 = (11/3)C_A − (4/3)T_F·n_f = (11/3)N_c − (4/3)N_c = (7/3)N_c = 7 = g` (with `n_f = 2N_c =
6`). Suggestive — though it is `(7/3)·N_c` landing on `g` via `N_c=3`, so possibly arithmetic; flagged as a
lead, not a bank.

## Tier

**STRUCTURAL, target-innocent, Five-Absence clean.** The gauge group structure is constrained by the same
rank-2 + multiplicity-N_c substrate as the generations. **NOT a count bank** (gauge couplings running-
limited; group structure is not a free-parameter count item). The spine for Lyra's gauge lane.

— Grace, 2026-06-29 Monday. Gauge dual-Coxeter (N_c, rank) = the generation-structure substrate invariants;
rank & N_c double-duty (generation + gauge). Cross-roles not over-counted; Five-Absence clean; couplings
open. Count 9/26.
