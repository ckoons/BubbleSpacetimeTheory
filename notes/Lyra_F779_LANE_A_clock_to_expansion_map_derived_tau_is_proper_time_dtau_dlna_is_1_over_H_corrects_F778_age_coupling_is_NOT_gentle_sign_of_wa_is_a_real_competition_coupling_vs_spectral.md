# F779 — LANE A: the clock-to-expansion map, DERIVED from the geometry (not assumed), and it CORRECTS my F778 "age-coupling is gentle." **The substrate commits once per Koons tick (a fixed proper-time interval, corpus Tier-0), so the commitment time τ ∝ cosmic proper time t — DERIVED from the commitment structure, not chosen. That gives dτ/d ln a = κ/H (κ = 1/τ_tick), and d²τ/d ln a² = −κḢ/H³ which is POSITIVE and O(H) in a decelerating era — NOT small. So my F778 "age-coupling → gentle → wₐ>0" was WRONG: the proper-time map DOES accelerate (via 1/H growing as H falls). The sign of wₐ is therefore a genuine competition, computed below: coupling term (r|Ḣ|/H) vs spectral term (κ·Var(λ)). wₐ < 0 (DESI, value Derived) iff the coupling wins: r|Ḣ|/H > κ·Var. Elie/Grace plug in the real K-Casimir spectrum and the expansion history and read the sign vs DESI. I do NOT declare it — the scales (κ Planck-ish vs H cosmic) make it a genuine numerical contest.**

**Lyra, Mon 2026-08-03. Compute, not re-frame (per the wake). The map is the geometry's to give; I derived it from the commitment-per-tick structure and set up the sign-competition. Correcting my own F778 over-quick "gentle" — the proper-time map accelerates, so the age-coupling was never trivially wₐ>0.**

## The map, derived (τ ∝ proper time)
Corpus Tier-0: the substrate commits **once per Koons tick** τ_tick = t_Planck·α^(C₂²), a fixed *proper-time* interval. So the accumulated commitment time is
$$ \tau = \kappa\, t, \qquad \kappa = 1/\tau_{\text{tick}}\ (\text{const}), \qquad t = \text{cosmic proper time}. $$
This is **derived** from the commitment structure (per-proper-time), not an assumption — and it's the honest reading; if the substrate clock were conformal or e-fold time the map would differ, but commitment-per-tick is proper-time. Then, with dt/d ln a = 1/H:
$$ \boxed{\ \frac{d\tau}{d\ln a} = \frac{\kappa}{H}, \qquad \frac{d^2\tau}{d(\ln a)^2} = -\frac{\kappa\,\dot H}{H^3}\ }. $$
In a **decelerating** era (matter/radiation, Ḣ < 0) the second derivative is **positive and O(κH)** — the map genuinely accelerates. **This corrects F778:** the age/proper-time coupling is NOT gentle; 1/H grows as H falls, so d²τ/d ln a² is substantial. The sign of wₐ was never trivially wₐ>0 for age-coupling.

## The sign of wₐ — the genuine competition (computed)
With ρ_Λ(τ) = Σ c_k e^{−λ_k τ}, r(τ) = ⟨λ⟩_τ (effective rate), dr/dτ = −Var_τ(λ):
$$ w+1 = \tfrac{1}{3}\,r(\tau)\,\frac{\kappa}{H}, \qquad \frac{d(w+1)}{d\ln a} = \tfrac{1}{3}\frac{\kappa}{H^2}\Big[\,\underbrace{r\,\frac{|\dot H|}{H}}_{\text{coupling, }>0}\ -\ \underbrace{\kappa\,\mathrm{Var}(\lambda)}_{\text{spectral, }>0}\,\Big]. $$
Since d(w+1)/d ln a > 0 ⟺ wₐ < 0 (as w(a)=w₀+wₐ(1−a) ⟹ d(w+1)/d ln a = −wₐ·a):
- **wₐ < 0 (DESI phantom-crossing → value DERIVED)** ⟺ **r·|Ḣ|/H > κ·Var(λ)** — the coupling-acceleration beats the spectral-variance.
- **wₐ > 0 (near-miss, K1040 fires)** ⟺ **κ·Var(λ) > r·|Ḣ|/H** — the spectral deceleration wins.
With |Ḣ|/H = (3/2)H(1+w_bg) (~ H in matter domination), the contest is **r·H vs κ·Var** at the transition epoch (z ≈ 0.45, where DESI's crossing sits).

## Honest scale note (why I do NOT declare the sign)
κ = 1/τ_tick is **Planck-ish** (huge); H is **cosmic** (tiny); r, Var are **spectral** (O(1) in K-Casimir units). So the contest r·H vs κ·Var pits a cosmic-scale coupling against a Planck-scale spectral term — the outcome depends entirely on the *normalization* of τ relative to the physical Λ evolution (how the dimensionless heat-flow parameter maps to physical time), which is exactly the piece Elie/Grace pin numerically. Naively κ·Var (Planck) dwarfs r·H (cosmic) → wₐ>0 — but that naive reading ignores that the physical bleed is normalized to the *observed* Λ evolution, which re-scales the comparison. **So the sign is a genuine numerical contest, not a structural default either way. I set up the map and the competition; I do not declare the winner.**

## Tier / handoffs
- **@Elie / @Grace** — the map is derived: **dτ/d ln a = κ/H, d²τ/d ln a² = −κḢ/H³** (τ ∝ proper time, commitment-per-tick). The sign of wₐ = sign of [r·|Ḣ|/H − κ·Var(λ)] at z ≈ 0.45. Plug in: (1) the real K-Casimir spectrum {λ_k} and overlap weights c_k → r(τ), Var(λ); (2) the physical normalization of τ (κ, or equivalently how the heat-flow parameter is fixed to the observed Λ scale — this is the load-bearing normalization); (3) the expansion history H(z), Ḣ(z). Read the sign, overlay the DESI (w₀~−0.75, wₐ~−0.8, crossing z≈0.45) contour. **The normalization of τ vs physical Λ is where the answer lives — pin it from the geometry, blind.**
- **@Cal** — the exciting answer is wₐ<0 (coupling wins → Derived → DESI). Guard: "it's a real computation now" licenses the numerical sign, NOT expecting wₐ<0. And guard my τ ∝ proper-time map — is commitment-per-tick genuinely proper-time (my reading), or could the substrate clock be conformal/e-fold (which would change dτ/d ln a and could flip the sign)? That's the one assumption in the map; audit it.
- **@Keeper** — LANE A compute deliverable: the map is dτ/d ln a = κ/H (proper-time, derived from commitment-per-tick), correcting my F778 "gentle." The sign of wₐ is a genuine competition (coupling r|Ḣ|/H vs spectral κVar), decided by the τ-normalization Elie/Grace pin. Not declared. **DE Proxy Register (#1):** smallness Derived, structure Proven, value conditional on the wₐ sign (this competition), w=−1 conditional.
- **@Casey** — I derived how the substrate's clock maps to the expanding universe, from the one fact that it ticks once per Koons tick (a fixed slice of ordinary time). That means the substrate clock *is* ordinary cosmic time — and here's the correction I owe: I claimed Friday that this "age" coupling was too gentle to matter, and that was wrong. As the universe expands, ordinary time stretches relative to expansion (1/H grows), so the clock-map *does* accelerate — which means the whole thing really is a live tug-of-war, exactly as your instinct said. On one side, the fading modes slow the vacuum's decay toward constant (pushing to the un-DESI answer); on the other, the accelerating clock-map speeds it up (pushing to the DESI-matching answer). Which wins is now one honest number — the strength of the clock-map's acceleration against the fading's variance — and it hinges on how the substrate's tick-rate is normalized against the actual dark-energy scale. I set that up precisely and handed it to Elie and Grace to compute blind; I refused to call the winner because the two scales involved are wildly different and the answer lives entirely in the normalization. Compute, not re-frame — the map's derived, the contest's set, the number decides. Nothing pushed.

Notes only; no toy/theorem claimed (LANE A map derivation, compute). F779: clock-to-expansion map DERIVED — τ ∝ proper time (commitment-per-Koons-tick, corpus Tier-0), so **dτ/d ln a = κ/H, d²τ/d ln a² = −κḢ/H³** (POSITIVE, O(κH) in decelerating era). CORRECTS F778 "age-coupling gentle" — the proper-time map DOES accelerate (1/H grows as H falls). Sign of wₐ = genuine competition: wₐ<0 (DESI, Derived) ⟺ r·|Ḣ|/H > κ·Var(λ) [coupling wins]; wₐ>0 (near-miss, K1040) ⟺ κ·Var > r·|Ḣ|/H [spectral wins]. Contest = r·H vs κ·Var at z≈0.45. NOT declared — hinges on the τ-normalization (κ Planck-ish vs H cosmic; physical bleed normalized to observed Λ). @Elie/@Grace: K-Casimir {λ_k,c_k}→r,Var; the τ-normalization (load-bearing); H(z),Ḣ(z); read sign vs DESI. @Cal guard: exciting=wₐ<0, don't expect it + audit the proper-time map assumption (vs conformal/e-fold). Smallness Derived, structure Proven, value conditional on this sign. — Lyra