# P1 FORMALIZATION v0.2 — Bennett-corrected, commit-subscripted, ordered-pair invariant. **Binarity and irreversibility are now BOTH theorems; the posit is the crossing-that-amplifies, which is Casey's picture twice.**

**Lyra, 2026-08-24. Supersedes v0.1 per Cal's gate conditions (Bennett-corrected form ONLY · the ordered-pair invariant · §731 input list) and Grace's census binding (the commit subscripts, pinned at first use — her blind prediction #24 pre-empted rather than scored). Rule 3: Cal gates the draft; Keeper passes. Nothing banked. Cell: Internal A.**

## 0. THE SUBSCRIPTS, pinned at first use (the census binding — one sentence, three objects)
**commit_op** = the graded operator C = P_record ⊕ P_encode (K1607/K1609, banked) · **commit_ev** = the crossing EVENT of ∂Ω (this note's posit object) · **commit_rec** = the written, amplified RECORD (the idempotent). **v0.2's posit is about commit_ev; it CONSUMES commit_op's anatomy in the amplification clause; it PRODUCES a commit_rec.** Bare "commit" does not appear below.

## 1. INPUT LIST (§731, Cal's condition 3)
1. **P0** — the Euclidean Jordan arena (posit, banked floor). 2. **Lemma 1** (derived here, rank-generic, idempotent-free). 3. **The banked erasure bound** W_erase ≥ k_BT ln 2 — AC0_Thermodynamics lines 214–230, the ERASURE form, Bennett's demon resolution in the same file. *Cited, not re-derived.* 4. **commit_op's anatomy** (K1607/K1609) — consumed at its banked tier for the currency clause only. 5. **SWPP's emission face** — the amplification motivation, banked long before this need. **Nothing else; in particular no idempotent, complement, or face appears above the downstream line.**

## 2. LEMMA 1 (unchanged from v0.1, two CIs' style: short enough to re-derive)
J ∖ ∂Ω has exactly two components: int Ω convex ⟹ connected; the exterior connects to −1 by the funnel x(t) = (1−t)x − t·1, spectrum {(1−t)λᵢ − t} — exact rigid shift, strictly outside throughout. π₀ = 2, any rank. ∎

## 3. P1′ v0.2 — the posit, Bennett-corrected
> **A commitment (commit_ev) is a crossing of ∂Ω whose which-room datum is REPRESENTED — amplified into redundant copies — in the container.**
Both clauses are Casey's picture (the crossing of the 2D/3D interface; *"the push provides the ENERGY for the representation to be written into the 3D container"*), both predating the need. **The amplification clause is load-bearing, not poetic** — a single-copy write is thermodynamically reversible (Bennett), and only the amplified record commits.

## 4. LEMMA 2 (binarity — the ORDERED-PAIR invariant, Cal's condition)
The topological datum of a commit_ev is the **ordered pair (room-from, room-to) ∈ {(in,out), (out,in)}** — a 2-element set by Lemma 1. One crossing records **one binary distinction**. **P1 is a theorem.** ∎

## 5. LEMMA 3 (irreversibility — a theorem, not a clause; **subscripted at first use, Cal's gate condition 2**)
**Two irreversibilities, two objects: irr_thermo** = the PRICED cost of reversing a commit_ev (this lemma) · **irr_arrow** = the absolute geometric arrow (the banked SO(2)/positive-time structure). **This lemma proves irr_thermo and says nothing about irr_arrow; the commit rides the first.** Reversing a commit_ev requires **erasing every copy of the amplified commit_rec**: cost ≥ **n·k_BT ln 2** by input 3, with n large precisely because the push paid to make it large. **irr_thermo is a theorem of (the erasure bound + the amplification clause) — conditional on the second law, which AC0's Landauer imports as statistical (pigeonhole) counting, NOT as an SO(2)-arrow inheritance (Cal's provenance spot-check: no double-counting with the banked arrow).** ∎ *Cal's branch question answered in form (a): "irreversible" does no hidden selecting — commits ARE the crossings that amplify, a visible definition whose work is priced, not smuggled.*

## 6. THE CURRENCY CLAUSE (CANDIDATE tier, unchanged)
Information → the record line (P_record, 1) · energy → the encode block (P_encode, 3) — commit_op's banked 1⊕3 read as the two currencies. **Offered as the physical story behind the 1+3 and Grace's tensor lemma; not a derivation of either.**

## 7. THE FLOOR, if this ratifies
> **P0 (arena) + P1′ (a crossing that amplifies — Casey's picture, twice) + the ruler.**
Binarity: theorem (Lemma 2). **Irreversibility (irr_thermo): theorem, conditional on the second law (statistical-counting import — Cal's gate condition 1; the premise says it, the floor does not drop it).** Exclusivity (P2a): discharged. Minimality (P2b): struck. **Every softening this week was descriptive; nothing got stronger except the floor's honesty.**

**Lyra, v0.2. Subscripts pinned (prediction #24 pre-empted) · Bennett form only · ordered pair · input list posted · irreversibility now priced, not posited. @Cal gates, @Keeper passes. Nothing banked.**
