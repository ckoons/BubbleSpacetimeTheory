# K1310 — The F85 ANGULAR gate is NOT a new gate: it IS the already-specified K1012 cross-address two-point kernel (the OFF-DIAGONAL / "interfering" object). The diagonal (masses) closes via F85-radial; the off-diagonal (mixing) is the K1012 kernel. The sub-Fritzsch up-1-2 suppression is a GEOMETRIC PREDICTION — radial-address separation, the same family as the already-derived 23-block vanishing. Reduction + a third anchor for the team; not banked.

**Keeper (research lane, timeboxed while the team runs Lane A/B), 2026-08-09. Pulled on the F85 angular gate. It is not a new object — it is the cross-address two-point kernel K1012 already specified in July, with a built-in down-slice check. This note (a) identifies the angular gate WITH K1012, (b) shows it is exactly Casey's "interfering / off-diagonal" object (K1308) — cleanly complementary to F85-radial's "separate / diagonal" masses, and (c) adds a THIRD anchor: the sub-Fritzsch suppression Elie handed over is a geometric prediction (radial separation), the cousin of the 23-block boundary-refraction vanishing. Insight/reduction tier, NOT banked. Cal cold-reads.**

## The angular gate = K1012, precisely
K1309 named the remaining F85 gate "the angular alignment — where the condensate modes point on the boundary." Reading the corpus, that gate is already a *defined computation* (K1012, 2026-07-30): the up-frame U_up has only ONE open block, the **12-block (up–charm)**, which is a **cross-address overlap** between modes at *different* radial/ν addresses. And crucially:
- The **23-block (charm–top) is DONE and ≈ 0** — the up 23-mode refracts *past* the Shilov boundary (effective radius √(2/3)·N_c/rank = 1.225 > 1) → vanishes (K711/K1001). So the top decouples; U_up ≈ (up–charm 2×2) ⊕ (top singlet). This is *why* V_cb is down-only.
- The up 12-block AND the lepton off-diagonal are the SAME object: the two-point kernel **K((ν_i,m_i),(ν_j,m_j))** on D_IV⁵, with a **built-in consistency check** — on the same-ν slice it must reduce to the validated down-Jack(α=2/3) binomial (V_us=1/√20, {3,60,2520}).
**So the F85 angular gate is not open-ended: it is "exhibit the K1012 cross-address two-point kernel, verify the down-slice, read off the up-12 block and the lepton off-diagonal."** The team's Lane A/B are already pointed here; this note confirms the target is the July-specified object and adds the anchor below.

## It is EXACTLY Casey's "interfering / off-diagonal" object (ties K1308 → K1309 → K1012)
Casey's separate-vs-interfering framing (K1308 addendum) now organizes the whole flavor sector cleanly:
- **DIAGONAL ⟨i|Φ|i⟩ = mass-depth = the "separate / non-interfering" object.** Positive-weight, monotone — **this is F85-radial, and it is FORCED (K1309).** The masses close here.
- **OFF-DIAGONAL ⟨i|Φ|j⟩, i≠j = the mixing = the "interfering" object.** The two modes sit at *different* addresses, so the two Γ_Ω normalizations do NOT cancel (K1012 "sharpening") — this is genuinely a two-point kernel, not a diagonal norm. **This is the remaining gate.**
So "F85 radial closes the masses; the K1012 cross-address kernel closes the mixing" is the same statement as "the separate/diagonal part is forced; the interfering/off-diagonal part is the last computation." The F85-radial (K1309) and the K1012 kernel are the two halves Casey's question already separated. That is why F85's complete-monotonicity (a diagonal, positive-weight property) never reached the mixing: the mixing lives in the off-diagonal, by construction.

## ★ THE THIRD ANCHOR (new, from Elie's round) — sub-Fritzsch is a geometric prediction
Elie's Lane-A bonus (K1309): the naïve Fritzsch-√ up-block over-corrects, so **the up 1-2 mixing must be SUPPRESSED (sub-Fritzsch).** This is not just a loose constraint — it has a candidate geometric origin in the SAME mechanism that kills the 23-block:
- **23-block:** up and top modes are separated so far radially that the mode refracts past the boundary (radius > 1) → overlap **vanishes** (derived, K711/K1001).
- **12-block:** up (deep interior, soft) and charm (α-shell) sit at a large — but sub-boundary — radial separation. The cross-address two-point kernel **decays with address separation** (the FK/Bergman reproducing kernel (1−z·w̄)^{−p} weakens as the two localizations pull apart). So the 12-block should be **suppressed but non-zero** — i.e. **sub-Fritzsch** — for the same reason the 23-block vanishes, just less extreme.
**⟹ The sub-Fritzsch suppression is a PREDICTION of the cross-address kernel (radial separation), the cousin of the 23-vanishing — not a fit.** This gives the kernel a THIRD anchor beyond the down-slice check and V_us: it must reproduce a *suppressed* up-12 block from the up/charm radial separation. If it does, the sub-leading up correction is forced (not fitted), and the angular gate closes. (I deliberately quote NO number for the 12-block — that is Elie's validated-machine output; crudely reconstructing it here would risk diverging from the validated overlap, per Grace's discipline. The claim is the mechanism + the anchor, tiered as insight.)

## What this buys the team (concrete)
1. **The angular gate is the K1012 object** — resume that exact target (two-point kernel + down-slice check), don't treat "angular alignment" as a fresh search.
2. **Three anchors now pin the kernel** (over-constrained, which is the honest test): (i) reduces to the down-Jack on the same-ν slice; (ii) reproduces V_us=1/√20 leading; (iii) yields a *suppressed* (sub-Fritzsch) up-12 block from up/charm radial separation. A kernel hitting all three is forced, not fit.
3. **The conceptual close:** masses = diagonal/separate (F85-radial, forced); mixing = off-diagonal/interfering (K1012 kernel, the gate). One operator, two matrix-element types — Casey's framing is the organizing principle.

## Tier / discipline
- **NOT banked.** This is a reduction (angular gate = K1012) + a connection (diagonal/off-diagonal = Casey's split = masses/mixing) + a new anchor (sub-Fritzsch = radial-separation prediction). The kernel must still be exhibited and pass all three anchors — the team's computation.
- **No competing number introduced** (Grace's discipline: don't crudely reconstruct Elie's validated overlap).
- **Non-load-bearing CM parked** (K1309) — consistent: CM is a diagonal property, the mixing is off-diagonal, so CM was never going to give the mixing; that is now explained, not just set aside.

— Keeper, K1310, 2026-08-09. The F85 ANGULAR gate = the already-specified K1012 cross-address two-point kernel (off-diagonal object); NOT a new gate. Diagonal/separate = masses (F85-radial, FORCED K1309); off-diagonal/interfering = mixing (K1012 kernel, the gate) — exactly Casey's separate-vs-interfering split. NEW third anchor: sub-Fritzsch up-1-2 suppression is a geometric prediction (up/charm radial separation, cousin of the derived 23-block boundary-refraction vanishing), NOT a fit — over-constrains the kernel (down-slice + V_us + sub-Fritzsch). Insight/reduction tier, no number quoted (Grace discipline). Cal cold-reads. Nothing pushed.
