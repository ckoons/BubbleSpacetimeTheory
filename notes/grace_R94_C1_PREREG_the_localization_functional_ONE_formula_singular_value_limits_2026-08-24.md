---
node_type: prereg_definition
title: "C1 PRE-REGISTERED — the localization functional, ONE explicit formula: stratum_KW assignment by Born-averaged singular-value limits. Filed BEFORE any exponent. C2/C3 controls stated with expected outcomes. @Cal audits for outcome-steering before anything computes."
author: Grace
date: 2026-08-24
status: "Per K1824. ZERO exponents in this file. The blind cross runs only after C1–C3 pass and Cal's audit."
cell: "External 3 (generations)"
---

# C1 — THE FORMULA (one, explicit, no envelope language)
Every z ∈ D̄_IV⁵ has Jordan singular values **λ₁(z) ≥ λ₂(z) ≥ 0** (rank-2 domain), and the KW strata are exactly their level structure: **interior λ₁ < 1 · rank-1 boundary λ₁ = 1 > λ₂ · Shilov λ₁ = λ₂ = 1.**

For a mode f (normalizable or not), define the **cutoff-normalized Born expectations**
> **⟨λᵢ⟩_f(t) = ∫_{D_t} λᵢ(z) |f(z)|² dμ_ν(z) / ∫_{D_t} |f(z)|² dμ_ν(z)**, D_t = t·D̄, i = 1, 2,
and the assignment
> ### **S(f) = j, where j = #{ i : lim_{t→1⁻} ⟨λᵢ⟩_f(t) = 1 }.**
**j = 0 → stratum_KW bulk · j = 1 → rank-1 boundary · j = 2 → Shilov.** One formula, exact/symbolic for K-type modes (singular-value moments of |f|²dμ_ν are computable integrals — Elie's arithmetic). The measure weight ν and any convention entering dμ_ν must be **the pinned scalar calibration measure, stated in the computation header** — no per-mode measure choices.

# THE MODE→SUBSPACE ASSEMBLY (required header sentence, Cal audit-2)
**The exponent's subspace for stratum j is the CLOSED SPAN, in the K-type basis, of the modes with S(f) = j; E_j = inf spec of H_B on that span.** One sentence, no alternative assembly available to a later reader.

# C3b — exhaustion-independence (required, Cal audit-2)
The t·D̄ cutoff is identically the λ₁-sublevel exhaustion (λᵢ(tz) = t·λᵢ(z) ⟹ t·D̄ = {λ₁ ≤ t}). **C3b: at least one mode is run under a genuinely different exhaustion — Euclidean balls {|z| ≤ R} — and the limits must AGREE.** Disagreement ⟹ the functional is exhaustion-dependent and dies at the control.

# C2 — must reproduce banked assignments it was NOT built from (expected outcomes, fixed now)
- **T2514:** the top's boundary-saturating mode (the reproducing-kernel direction at a Shilov point) must land **j = 2**. *Expected mechanism: the kernel at a Shilov point concentrates there; both ⟨λᵢ⟩ → 1.*
- **F375 direction:** among the banked heavy/light placements, heavier must land at ≥ the stratum of lighter — the control is the DIRECTION, scored on the banked pairs only.

# C3 — must-reject (expected outcome, fixed now)
- **The constant mode f ≡ 1** (Born density = the Bergman measure itself): a bounded domain with finite measure gives ⟨λᵢ⟩ strictly < 1 with **no boundary concentration ⟹ j = 0.** If the formula sends the constant mode anywhere but bulk, the formula dies, not the mode.

# GREEN-LIST ITEMS 1–2 (R95): Elie's exact halves + the corrected mechanism sentence
**C2 CONFIRMED AT EXACT LEVEL (Elie toy 5493): j = 2.** The 0.575 plateau is EXPLAINED, not dismissed — the global MC was integrating the broad rank-1 shoulder (singularity order −(ν+5)) and missing the point spike (order −(2ν+10)). **Corrected mechanism sentence, binding: the kernel mode is SINGULAR ON A VARIETY, RATE-DOMINATED AT THE POINT** — the singular set has two exact families (Shilov + rank-1), and the verdict stands because the rates decide, by (ν+5) orders at every ν. **C3b: AGREE at resolved level** (Elie's cutoff-shape-independent rate argument replaces my withdrawn plateau-level exhibit).

# ⚠⚠ GREEN-LIST ITEM 3 EXECUTED — AND THE ASSEMBLY SENTENCE IS PRESENT AND **DEFECTIVE**. THE CROSS MUST NOT FIRE.
Checking item 3 meant *executing* the frozen assembly ("the closed span, in the K-type basis, of the modes with S(f) = j"), and **both readings of it degenerate**:
- **Reading 1 (span all S(f)=j modes, close):** the S=2 modes include the boundary kernels K(·,ζ), ζ ∈ Shilov — and **span{K(·,ζ): ζ ∈ Shilov} is DENSE in H², by the DEFINING property of the Shilov boundary** (a holomorphic function vanishing on the Shilov is zero). Closure = everything ⟹ **E₂ = global min = E₀. Degenerate.**
- **Reading 2 (S on K-type basis modes only):** every K-finite mode on a bounded domain with finite measure is L² ⟹ interior Born limits ⟹ **S = 0 for every basis mode ⟹ the S = 1, 2 spans are EMPTY. Degenerate.**
> ### **The controls validated the FUNCTIONAL; the assembly kills the STRATIFICATION. The C2/C3 control modes (boundary kernels, the constant) are precisely the modes the assembly then collapses or excludes. An L² space cannot see boundary strata from inside — and the Shilov's defining property (it determines everything) is exactly what makes its span collapse to everything.**
**Consequence, per the frozen discipline ("the lane reports the failure, not a patched formula"): the one-shot cross MUST NOT fire on a procedure known to degenerate.** The fix — a mode space with the non-L² boundary modes as first-class objects (the m_e file's electron is already one) and a stratum assembly that does not close-collapse (quotient or graded structure, not spans) — **is a NEW assembly requiring its own prereg and Cal audit. Reported before the shot, with the shot preserved.**
I benefit from definition B; K1824 neutralizes that **by controls, not trust**: the formula above is pre-registered before any exponent exists, C2 forces it to reproduce assignments it was not built from, C3 forces it to fail on a wrong input, and **Cal audits the formula itself for outcome-steering before anything runs.** If Cal finds a steering step, C1 is re-drafted by someone else; if C2 or C3 fails, the lane reports the failure, not a patched formula.

# Sequence from here (frozen)
Cal's audit → C2/C3 runs (Elie + me, these controls are NOT blind-split — they are joint instrument validation) → **only then** the blind cross on the three exponents: independent computation, post together, exponents first, verdict last, one shot.

*— Grace, R94/C1. The cartoon became a procedure; the procedure can fail three separate ways before it is allowed to say anything about generations.*
