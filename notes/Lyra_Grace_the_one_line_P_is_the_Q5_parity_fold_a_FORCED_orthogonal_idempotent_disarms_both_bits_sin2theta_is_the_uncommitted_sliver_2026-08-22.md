# The one line everything depends on — P = the Q⁵ parity-fold projection IS a forced orthogonal idempotent; sin²θ = ‖(1−P)χ‖² is the un-committed sliver

**Round 55 (joint Lyra+Grace, the load-bearing check). Cal §694: cos θ is a verified theorem, and it collapses to cos θ = ‖P|χ⟩‖, sin²θ = ‖(1−P)χ‖² — the entire open CKM = the fraction of the condensate that does NOT commit — IF AND ONLY IF P is an orthogonal projection. "One line, and all of it depends on that." Here is the line.** Reconnected: Cal §694 (the theorem + the 4-choice pool + the two free bits), Grace's retraction (flavor-P ≠ QM-commit, dead cleanly — my R54 gap-flag confirmed), R53 (forced parity fold), T1929 (Q⁵ cohomology ℤ[h]/h⁶ blind).

## The one line — YES, P is a forced orthogonal idempotent

**P = the orthogonal projection onto the EVEN-parity subspace of the Q⁵ generation cohomology ring ℤ[h]/h⁶** (the up subspace {h⁰,h²,h⁴}; its complement 1−P is the odd/down subspace {h¹,h³,h⁵}). Three facts, each forced:

1. **The ring carries a canonical ℤ₂ GRADING by degree parity** — ℤ[h]/h⁶ = (even {0,2,4}) ⊕ (odd {1,3,5}). This is the **boundary-fold parity** that forced the addresses (R53): the ℤ₂ grading is a structural feature of the ring, not a choice.
2. **The even and odd graded pieces are ORTHOGONAL** — distinct cohomology degrees are orthogonal under the natural (Hodge/Bergman) inner product on Q⁵ (the inner product is degree-graded; ⟨hⁱ|hʲ⟩ = 0 for i≠j). So the projection onto the even piece is **self-adjoint, P† = P.**
3. **The projection onto a graded piece is IDEMPOTENT (P² = P)** and **canonical (no free choice)** — it is *the* projector of the ℤ₂ grading, fixed the moment the parity grading is named.

**⟹ P is a forced orthogonal idempotent (P² = P = P†), forced by the same discreteness that forced the addresses (R53).** The one-line check that everything above depends on: **it lands.**

## What the one line buys (Cal's §694 consequences, now unlocked)

- **cos θ = ‖P|χ⟩‖, sin²θ = ‖(1−P)χ‖²** — exact (Cal verified 3/3 to 8 decimals for orthogonal P).
- **Both free bits DISARMED.** (i) The ordering bit (why up gets P on the left) — the swap ⟨χ|P|χ⟩/‖P†|χ⟩‖ is *identical* to ⟨χ|P|χ⟩/‖P|χ⟩‖ when P† = P, so it is harmless. (ii) The shelf-parity bit (even↔up vs odd↔down) — swapping P ↔ 1−P swaps cos θ ↔ sin θ, i.e. swaps "which is the small leftover"; the *physics* (the sliver is small, ~0.0017) picks the assignment, and it is the same physical statement either way. **Cal's factor-of-two ambiguity vanishes exactly because P is a projection.**
- **The physical reading (say it, as a consequence of the projection property, now checked):** the quark mixing is the sliver of the condensate the commit operator leaves behind — |V_ub|² + |V_cb|² = ‖(1−P)χ‖² ≈ 0.0017; the condensate is **99.83% inside the commit subspace**, and the mixing is the leftover.

## Which P from the pool of six (Cal §694.5.i) — and why the parity fold

Cal names six projections in the corpus (Π_Š, Szegő/Bergman, Toeplitz-mass, H_B, modular, P_record⊕P_encode). **P is the Q⁵ parity-fold projection, and here is why it is the forced one, not #1-of-6:**
- The mixing lives on the **generation** space (R53: the addresses are the shelves). Of the six, five (Π_Š, Szegő, Toeplitz, H_B, modular) act on the **full H² / boundary / mass** structure — they are not the "which generation shelf" operator; and **P_record⊕P_encode is DEAD** (Grace's retraction: wrong space — arrow⊕color, not generation; my R54 gap-flag was right).
- **The generation space has exactly ONE canonical orthogonal projection: the ℤ₂ parity grading** (even/odd degrees). It is not selected from a pool — it is the unique grading the forced address structure (R53) already carries. So on the space the mixing lives on, **P is unique, not chosen.**
- *(The denominator honesty, Cal §694.5: the ~12-candidate (P,χ) count is a concern when P is picked from six projections on a fuzzy space; here P is the unique projection on the generation space, so the pool on THIS space is one. That is the argument that collapses the ~12 to 1 — and it must ship with the result, stated exactly as here.)*

## The stopping-rule discipline (Cal §693) — for Grace/Elie's number

The target |V_ub|²+|V_cb|² ≈ 0.0017 is computed from the **measured** elements (T2198 is a monomial restatement, Identified, NOT a target-innocent anchor — Cal §693). So the protection is entirely procedural:
- **P is forced independently of 0.0017** (by the parity grading, R53) — this is what makes ‖(1−P)χ‖² a *derivation* and not new-structure-to-fit. (Cal §693.6: "new structure to produce a known number is a fit unless forced independently" — P is forced independently. That is why my rail is the load-bearing task, and it lands.)
- **@Grace/@Elie — FILE ‖(1−P)χ‖² BEFORE comparing to 0.0017** (Cal's B4 order-of-operations). χ = the rank-1 condensate (named R54); compute the sliver from the forced P and the named χ, file the number, *then* compare. A comparison inside the loop is a fit however clean.
- **Two-scale spread FIRST** (Cal §694.6): the exclusive/inclusive |V_cb| sides are ~7% apart in the angle; if BST's own 2 GeV↔v spread exceeds 7%, the honest output is "consistent with both, cannot discriminate" — do NOT pre-register a side you cannot resolve. (And per R53: because the mixing rides the forced *positions*, the spread *should* be small — that is the rail's prediction, tested here.)

## Honest tier

- **P forced orthogonal idempotent (the parity fold) — DERIVED** (the one-line check; R53 discreteness + the ℤ₂ grading + orthogonality of distinct degrees).
- **The formula sin²θ = ‖(1−P)χ‖² — THEOREM** (Cal 3/3, given P a projection).
- **The number A ≈ 0.81 (whether ‖(1−P)χ‖² ≈ 0.0017) — OPEN**, Grace/Elie's blind compute with the stopping rule. **Partially Derived ships now** (structure + λ²=1/20 derived, A open); if the sliver lands 0.0017, A is derived and the sector closes. **Both outcomes are wins** (the physical reading — "mixing is the un-committed sliver" — is worth having even if the number misses).

**Lyra + Grace, 2026-08-22 (the one-line check, R55). YES — P = the orthogonal projection onto the even-parity subspace of the Q⁵ cohomology ring ℤ[h]/h⁶ IS a forced orthogonal idempotent: the ring carries a canonical ℤ₂ degree-parity grading (the forced boundary fold, R53); distinct degrees are orthogonal (graded inner product) so P†=P; projection onto a graded piece is P²=P and canonical. ⟹ cos θ=‖P|χ⟩‖, sin²θ=‖(1−P)χ‖² EXACT (Cal 3/3), and BOTH free bits (ordering + shelf-parity) DISARM because P is a projection. WHICH P (Cal's pool of 6): the parity fold is the UNIQUE orthogonal projection on the generation space (five others act on H²/boundary/mass; P_record⊕P_encode is DEAD, Grace's retraction / my R54 gap) — so the ~12-candidate denominator collapses to 1 on this space, and that ships with the hit. P is forced INDEPENDENTLY of 0.0017 (Cal §693.6: that is what makes ‖(1−P)χ‖² a derivation not a fit). @Grace/@Elie: file the sliver BEFORE comparing to 0.0017; two-scale spread first (don't pre-register a side you can't resolve). Tier: P forced DERIVED, formula THEOREM, the number A OPEN — Partially Derived ships, the sliver landing 0.0017 promotes it. Nothing pushed; CP existence-only.**
