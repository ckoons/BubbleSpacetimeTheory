---
title: "Chain↔positive-root cross-check — does {rank,N_c,n_C,g} correspond to the 4 positive roots of B₂? COUNT yes (forced), VALUE-bijection NO. Honest negative that pins the cyclotomic route's true strength."
author: "Lyra (Claude Opus 4.7)"
date: "2026-05-29 Fri 10:48 EDT"
status: "CROSS-CHECK v0.1 (Lyra lane; Keeper's 'strengthen the cyclotomic carrier' ask). RESULT (honest negative): the chain↔positive-root correspondence holds at the COUNT level (4=|Φ⁺(B₂)|, forced) but FAILS at the VALUE level (chain={2,3,5,7} are primes; root heights={1,1,2,3}, no bijection). So the cyclotomic generation route rests on the forced COUNT |Φ⁺|=4 + base/seed (Gate 1) — NOT on a deeper chain↔root structure. Prevents an overclaim; generation-forcing stays OPEN."
---

# Chain↔positive-root cross-check

## 0. The ask (Keeper)

Keeper: keep the cyclotomic generation route alive; if the tube number pins ≠3, it carries the generation count via generations = |Φ⁺(B₂)| − 1 = 3. He asked me to nail the **chain↔positive-root correspondence**: is the substrate commitment chain {rank,N_c,n_C,g} = {2,3,5,7} in structural bijection with the 4 positive roots of B₂? If yes, generations = |Φ⁺|−1 rests on a forced structure, not a count-coincidence.

## 1. The test (computed)

- **B₂ positive roots** (convex order from w_0 = s_1s_2s_1s_2): α_1 (ht 1, short), 2α_1+α_2 (ht 3, long), α_1+α_2 (ht 2, short), α_2 (ht 1, long). |Φ⁺(B₂)| = 4 — a FORCED fixed root-system fact.
- **Chain values** {2,3,5,7} = the first four primes (each an independently-derived substrate primary).

| level | COUNT | VALUE |
|---|---|---|
| chain | 4 | {2,3,5,7} (primes) |
| Φ⁺(B₂) | 4 | heights {1,1,2,3}; lengths {short,long,short,long} |

## 2. Result — honest negative

- **COUNT: MATCH, and forced.** |chain| = 4 = |Φ⁺(B₂)|, and |Φ⁺(B₂)| = 4 is a fixed fact (no theorem-scope issue, unlike the tube number). This is the robust ingredient.
- **VALUE-bijection: FAILS.** Root heights {1,1,2,3} ≠ {2,3,5,7}; the length split (2 short, 2 long) is not 4 distinct primes. No natural root invariant reproduces the chain values. The chain values are primes (each derived as a substrate primary), NOT functions of the root vectors.
- **Aside (flagged as coincidence-risk, NOT claimed)**: B₂'s exponents are {1,3}, degrees {2,4}, h=4, h^∨=3 — so N_c=3 appears as an exponent and rank=2 as a degree. Suggestive, but it does NOT assemble into a clean chain↔root map, and pattern-matching here is exactly where Cal #27 fires. I flag it and do not claim it.

## 3. What this means for the cyclotomic route

The cyclotomic generation route — generations = |Φ⁺(B₂)| − 1 = 3 — rests on exactly two ingredients:
1. **|Φ⁺(B₂)| = 4** — FORCED (fixed root-system fact). ✓
2. **base/seed = rank** (Gate 1, the X=rank argument). ✓

It does **NOT** rest on a chain↔positive-root value-bijection — because there isn't one. The "chain has 4 levels" matches "|Φ⁺|=4" as a COUNT, and a candidate structural reason exists (the 4 commitments ↔ the 4 generators of the maximal nilpotent n⁺ / the Hall algebra's root-vector degrees of freedom) — but that reason rides on the same A_sub↔H dictionary bet (Phase 2) as everything else. So:

> The cyclotomic route's forced strength = the forced COUNT |Φ⁺|=4 (cleaner than the tube number, which is NOT forced). It does NOT gain extra independent force from a chain↔root structure. The "why exactly |Φ⁺| commitments" link is the Phase-2 dictionary bet.

This is a useful NEGATIVE: it prevents dressing up the cyclotomic route as stronger than it is. Generation-forcing stays OPEN; the cyclotomic |Φ⁺|−1 route is the better-footed CARRIER (forced count) but is not itself a closed forcing.

## 4. Honest scope + tier

**RIGOROUS**: |Φ⁺(B₂)| = 4 (forced); the value-bijection failure (computed).

**NEGATIVE RESULT (this doc)**: no chain↔positive-root value-bijection; the cyclotomic route rests on |Φ⁺|=4 (forced) + base/seed (Gate 1), not on a chain↔root structure.

**OPEN**: generation-forcing (the count = 3 is matched/over-determined; the cyclotomic carrier is forced at the COUNT but the "why |Φ⁺| commitments" mechanism rides on the Phase-2 dictionary). Neither tube nor cyclotomic route is a closed forcing.

**Cal #27 / honesty**: this is a case of the discipline producing a clean negative — I went looking for a chain↔root structure to strengthen a favored route, did not find one at the value level, and am reporting that rather than pattern-matching the exponent/degree coincidence into a claim. The route is exactly as strong as |Φ⁺|=4 + Gate 1, no stronger.

**Next (my lane)**: the Phase-2 A_sub↔H(Q_B2) dictionary — which is also where the "commitments ↔ n⁺ generators" structural reason would be established (the same gate). The dictionary is the common bottleneck for the physical identifications AND the generation-count mechanism.

— Lyra, chain↔positive-root cross-check v0.1. HONEST NEGATIVE: the correspondence holds at the COUNT level (4 = |Φ⁺(B₂)|, FORCED) but FAILS at the VALUE level (chain {2,3,5,7} are primes; root heights {1,1,2,3} — no bijection; exponent/degree near-matches flagged as coincidence-risk, not claimed). So the cyclotomic generation route rests on |Φ⁺|=4 (forced) + base/seed (Gate 1), NOT on a deeper chain↔root structure; the "why |Φ⁺| commitments" link rides on the Phase-2 dictionary. Prevents an overclaim. Generation-forcing stays OPEN; cyclotomic is the better-footed CARRIER (forced count) but not a closed forcing.
