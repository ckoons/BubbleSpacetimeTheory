# K1796 — PRE-REGISTERED GATE: naming χ and P (theorem-vs-definition; is P forced?)

**Filed:** 2026-08-22
**Status:** PRE-REGISTRATION — written BLIND, before Grace/Lyra name χ and P.
**Why:** Grace's ordered-product result — cos θ = |⟨χ|P|χ⟩|/‖P|χ⟩‖ from M_up=P·C, M_down=C·P — is target-innocent BY CONSTRUCTION *only if χ and P are forced independently of θ*. The entire physical content now sits in what fixes P (Cal §693's relocation). This gate is the difference between a theorem and a definition.

---

## The primary objects (corpus reconnect, handed to the team)
- **χ = O, the SO(5)-vector condensate** — banked (F603; T2518 "the 127/128 condensate arc"; T2520 O branches 5→(2,2)⊕(1,1)). This is a NAMED, banked direction, not a free choice. Good.
- **P = the commit operator — TWO candidate identities in the corpus:**
  1. the **order-totalizer** exp(−τ H_B), H_B = Casimir of K=SO(5)×SO(2), C(a,b)=a(a+3)+b(b+1) (T2564, the derived commit ORDER); and
  2. the **measurement operator** C = **P_record ⊕ P_encode** (K1607/K1609, criterion C — irreversible binary write ⊕ confined 3-encode, rank=2 forced externally). **Grace's unification lead: P = (2).**

## ★ SYMBOL COLLISION — subscript BEFORE computing (this has bitten us before).
Grace's theorem writes the **condensate** as "C" (C = |χ⟩⟨χ|). Criterion C writes the **commit operator** as "C" (C = P_record⊕P_encode). **Same letter, two different objects.** Grace's lead is: her **P** (commit) = criterion-C's **C operator** (P_record⊕P_encode); her **C** (condensate |χ⟩⟨χ|) is a SEPARATE object = O. Before any computation, rename: **C_cond = |χ⟩⟨χ| = O** and **P_commit = P_record⊕P_encode**. A run that conflates them is void. (Consistency check that must hold: M_up = P_commit·C_cond is rank-1 because C_cond is rank-1, regardless of P_commit's rank-2 — matches T2519. Good.)

## THE GATE — six conditions. G1, G2 are immovable.

**G1 — P forced INDEPENDENTLY of θ (immovable; this is the whole content).** The identity P_commit = P_record⊕P_encode must be justified on its OWN terms (the criterion-C dig, K1607/K1609), NOT because it lands θ in the band. Count the P candidates: order-totalizer vs measurement-operator vs others. If P is selected because it produces 2.4°, it is a fit — the ordered-product form is then a definition, not a theorem.

**G2 — TARGET-INNOCENT against the BAND (immovable).** There is no innocent anchor (T2198 is a fit, K1795). So the protection is procedural: name χ, name P, compute cos θ — and STOP when the geometry runs out, not when the value matches. **Pre-register WHICH SIDE of the |V_cb| controversy (2.26° exclusive vs 2.43° inclusive) BEFORE computing** — a side, not a percentage. File it hashed.

**G3 — theorem-vs-definition, stated explicitly (Cal §693).** cos θ = |⟨χ|P|χ⟩|/‖P|χ⟩‖ is a mathematical identity given M_up=P·C, M_down=C·P. The CONTENT is (a) the ordering identification (commit→emit forces M_up=P·C vs M_down=C·P — corpus-grounded, Cal §291c) and (b) the forced P (G1). Declare both as the load-bearing claims; do not let the clean identity read as if it were the derivation.

**G4 — the unification is a SEPARATE, strong claim (tier it apart).** "The flavor commit operator = the QM measurement operator (P_record⊕P_encode)" is a major unification. If it holds, it is the headline — but it needs its own justification (why the SAME operator), not "assumed because both are called commit." Tier the mixing-angle result and the unification claim separately; the angle can land without the unification, and the unification must not be smuggled in on the angle's success.

**G5 — Lyra's scale-stability can-fail.** Addresses don't run under RG; Yukawa values do. So if the mixing rides positions (Lyra's rail), cos θ is SCALE-STABLE across ~2 GeV and v. Report both; stable confirms "mixing rides positions," scale-dependent refutes it. A genuine can-fail limb — keep it.

**G6 — symbol hygiene + selection-honesty (C6).** C_cond vs P_commit subscripted throughout; every ν_W subscripted. If P was chosen from a candidate pool, report the full pool (which P's close vs don't), not the survivor alone.

---

## Disposition
When Grace/Lyra name χ and P, I measure against G1–G6, **G1/G2 immovable**. Order of check: **G1 first** (is P forced independently, or picked to land θ?), then G2 (band-side pre-registered before compute), then the rest. A forced χ (=O) + a forced P (justified by the criterion-C dig, not by θ) + cos θ landing on the pre-registered side of the |V_cb| band = the mixing sector closes at Derived AND (if P=P_record⊕P_encode holds on its own) unifies flavor with measurement. A P selected to land θ = definition, CONDITIONAL, back to the naming.

**Keeper, K1796, pre-registered blind 2026-08-22. χ is banked (O). The theorem lives or dies on whether P is forced — everything else is linear algebra.**
