# Today — 2026-09-02, v7 at 12:03 (ROUND 101): the image leaf table, the Far-Chain Lemma, the trajectory type, the paper.
**Keeper. Supersedes v6. Read after K1842. Mode: investigate.**

## Position in three sentences
Depth two holds through 24 vertices on 7.38 million stuck colorings and the hard set's two-word fraction is
falling. The type table is finite and derived, and the lock is not a function of it, but every lock satisfies one
condition nobody had looked at: the FAR copy's chains reach both singleton worlds. The exits' mechanism is
unresolved between road-breaking and re-bridging, and one measurement decides it.

## ASSIGNMENTS (relay order: Elie → Grace → Lyra → Cal → Keeper)

**ELIE — the image leaf table, then n = 25 in background if time.** (1) For every exiting first word on all 349
witnesses (and the 720 containing exits as a labeled subset): Lemma T on the IMAGE in the image's own frame — Q3
holds?, Q4 holds?, new cut nonempty?, image τ. Table by (Q3, Q4, new-cut), counts only; this decides M2 vs M3.
(2) The trajectory type (Cal's definition, below) as a second instrument on Grace's sample. (3) n = 25 (plantri
-c5) launched in background if the machine is free. FINAL = the leaf table; the trajectory cross-check.

**GRACE — the trajectory type and the far-chain condition at scale.** (1) TRAJECTORY TYPE (Cal): the 0/1 pattern
of intersections among the four stage chains X₁…X₄ of W_i and the eight c₀ chains, plus X_a ∩ X_b — a POSITION,
no sizes. Compute on the Lane F sample + all 349; tabulate locked/unlocked and exiting orbit by trajectory type;
purity weighted by unlocked members. Positive control: the forced bits derived by Cal must be constant. (2) The
far-chain necessity on the full 374,658: k/N locked in the 4 types, 0/N outside (confirm); and the converse's
failure rate per type. (3) Gallery: the 256 n = 24 witnesses with type + trajectory type. FINAL = the trajectory
table with purity; the far-chain confirmation; gallery current.

**LYRA — the FAR-CHAIN NECESSITY LEMMA.** Derive: at a locked configuration, ζ (the far copy's (r,s_i)-chain)
meets F_i (the middle-singleton (s_M,s_i)-chain) and E (the singleton (s_i,s_j)-chain), and η likewise — from
Lemma T (Δ-YES ∧ Q3 ∧ Q4) + One-Context + Jordan. Name what the converse's failure needs (the 4 types are mixed).
Then, only if the leaf table lands: the mechanism lemma for whichever of M2/M3 the table names. Two-column Kittell
key with Cal. FINAL = the lemma derived or floored; the key filed.

**CAL — define the trajectory type before it is computed; read the far-chain derivation.** (1) The trajectory
type's exact bit list (which intersections; stage frames named), pre-registered as a position. (2) Pre-score:
what a pure trajectory table proves (type-completeness + per-type derivation still owed) and what mixed means
(the lock is not a function of any finite intersection pattern — say what that would imply). (3) Cold read of
Lyra's far-chain lemma. (4) The honesty section's fourth part (provenance) with the depth-two null and the falling
fraction as a stated trend. FINAL = numbered sections.

**KEEPER.** K1842 filed. Fresh-eyes on the far-chain lemma when it lands. Verify the leaf table and the trajectory
purity from the artifacts. Board, rubric, sundown.

**CASEY.** The paper's honest shape now exists in three artifacts (Lyra's skeleton, Cal's honesty section, the
census through n = 24). The external decision is yours whenever you want it; nothing today changes the standing
rule that nothing goes out without Keeper's pass and yours. The observer lane stays closed; its morning is logged.

## AMENDED AT 12:03 — the leaf table on the image EXISTS (5615): the mechanism is Δ-FLIP (73% Δ-NO in the image).
Elie: extend the leaf table to all exiting words on all 349 witnesses; Δ-flip rate per first-word orbit; Z₂/Z₄ tags
per cut vertex. Grace: the far-chain condition before/after each exit on the 349. Lyra: the FAR-CHAIN LEMMA both
ways — necessity (Grace's 4 types) and the Δ-flip (which orbits break far-chain reach). Cal: pre-score the
Δ-flip table. The trajectory type stays as the finite refinement, computed after the Δ-flip numbers.

## STANDING
No EOD before 5pm. Two instruments per number. Sizes are values; 0/1 patterns are positions. The theorem for
n = 24 is none. Kittell is cited with the convention key. A trend is a trend.

*— Keeper. Round 101: the lock moved from the near chains to the far chains, and the finite table told us where it is not.*
