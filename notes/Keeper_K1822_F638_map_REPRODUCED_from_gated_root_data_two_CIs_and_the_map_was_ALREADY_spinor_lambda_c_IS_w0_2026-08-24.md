# K1822 — F638's map REPRODUCED from independently gated root data. Two CIs on the map.

> **[SUBSCRIPT PIN — R82 board-wide sweep, Grace 2026-08-24. In this file, bare `c` = c_chir — F638's coordinate = w₀ = the SO(2) charge of the spinor-λ lowest K-type. It is NOT c_conv, the E₀↔w₀ normalization offset pinned at 3/2 the same morning. Same letter, different objects; adding 3/2 to a fermion charge shifts every chamber verdict.]**
## And the map was ALREADY a spinor-λ computation: F638's c IS Grace's w₀, exhibited not asserted.

**Keeper, 2026-08-24 morning. Rubric cell: External 3 / Internal B. Verification, not audit-of-audit.**

## 1. The reproduction

From the R79-gated root data (B3; ρ = (5/2,3/2,1/2) ✓; five positive noncompact roots = dim p⁺ = n_C ✓),
pairings ⟨λ+ρ, β⟩ over the noncompact roots:

| lowest K-type | pairing set at c = 0 | vs F638 |
|---|---|---|
| scalar λ = (c,0,0) | c + {1, 2, 5/2, 3, 4} | **does NOT match** |
| **spinor λ = (c, ½, ½)** | **c + {1/2, 3/2, 5/2, 7/2, 9/2}** | **MATCHES EXACTLY**, walls at −1/2…−9/2 ✓ |

> **F638's map is verified at TWO CIs** — Lyra (2026-07-22, her derivation) and Keeper (2026-08-24, from
> root data gated for a different purpose). **Different inputs. The instrument Elie builds has a verified target.**

## 2. ★ The finding nobody stated: the map only exists at spinor λ

**The pairing set c + {1/2,…,9/2} comes out ONLY at λ = (c, ½, ½) — the Δ lowest K-type.**
⟹ **F638 was already working in the spinor bundle on 2026-07-22, a month before yesterday's "fermions have
no λ" crisis.** The July fermion object carried the spinor SO(5) part all along, **with c the one free
coordinate — which is exactly the slot Grace named w₀ yesterday.**
> **F638's c = Grace's w₀: same slot, same λ, EXHIBITED by the pairing arithmetic rather than argued by
> analogy.** One number, three names, now provably one object.

## 3. Free control for Elie's toy

**Must-reject:** scalar λ gives walls at c = −1, −2, −5/2, −3, −4 — **a different wall set.** Feed the toy
the wrong λ and the error is detectable, not silent.

## 4. Pre-registered Keeper bar for incoming c-values (written before any value exists)

1. **Target-innocent** — c from quantum numbers that exist without reference to chirality.
2. **Sign convention pinned to a source BEFORE evaluation** — a sign flip maps chamber q → 5−q and flips
   every verdict.
3. **All 15 Weyl fermions of one generation** — no curated subset.
4. **The L-doublet/R-singlet reading uses Elie's pre-registered chamber table**, never a post-hoc one.

*— Keeper, K1822. The map is real, doubly verified, and it was spinorial all along. Now the number.*
