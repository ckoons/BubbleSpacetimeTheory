# BOOK-DAY LEDGER — ITEM 5: which coordinate do the corpus's own formulas use?
**Reader: Elie · 2026-08-24 · pending Keeper verification (R91). Nothing re-tiers on this file.**

## PRE-REGISTERED EXPECTATIONS, cited from the board BEFORE the quotes
- The two-family frame (R82/R88): **A: c = ±ν − 5/2** (a half-ρ shift between the E₀-unit and the
  c-unit) **vs B: c = −ν / ν − 5** (label-level, no hidden shift; the two related by Lyra's conjugation).
- My 5485 instrument, mtime BEFORE this read, froze both families' consequences:
  **A ⟹ (a) = SILENCE (walls) · wall finding REAL. B ⟹ (a) = NAMED LOSS · wall finding DIES.**
- The 5478 definitional identity (verified symbolically): d(ν)'s zero set = the scalar pairing zero
  set under **c = −ν**, term by term, coefficient 1.
- Lyra's Item 2 (landed, verified): **FG quotes E₀ in the PHYSICAL label.** The family verdict is the
  CONJUNCTION of her read and this one.

## THE QUOTES, verbatim, with locations
**Q1 — the formal degree (K1820 amendment text, registry):**
> "d(ν)=(5/2−ν)(1−ν)(2−ν)(3−ν)(4−ν) are {1, 2, 5/2, 3, 4} — 5/2 is THIRD; its true descriptor is the
> UNIQUE HALF-INTEGER zero"
**Reading: d is a polynomial in ν directly; its zeros sit at the PHYSICAL values {1,2,5/2,3,4}. Under
c = −ν these are EXACTLY the scalar ⟨λ+ρ,β⟩ zeros (5478). The ρ-shift lives inside the pairing, never
inside the label.**

**Q2 — the quark ladder (T2513, registry line 11162):**
> "Down-Quark Current-Mass Ratios from the FK Pochhammer at the Wallach Threshold — d:s:b =
> (N_c)₁:(N_c)₃:(N_c)₅ = 1:20:840"
**Reading: the Pochhammer argument is ν = N_c = 3 — the PHYSICAL Wallach parameter ("at the Wallach
Threshold" names the physical-ν landmark a/2). No shifted argument appears.**

**Q3 — the Wallach set (T1438, registry line 2259):**
> "OS Axioms PROVED: Wallach Set {0,3/2}∪(3/2,∞), Bergman"
**Reading: the set is stated at the physical values {0, a/2}. An HC-shifted statement would read
{5/2, 4}∪(4,∞). The corpus writes the physical one.**

**Q4 — F638's own construction (read in full this session):**
> "Fermion K-type = SO(5) spinor ... with SO(2)-charge c: λ = (c, 1/2, 1/2). Then λ+ρ = (c+5/2, 2, 1)"
**Reading: c is DEFINED as the λ-label; ρ is added EXPLICITLY inside the formula. F638's c is the
physical label by its own construction — the 5/2 appears only where λ+ρ is formed, never as a hidden
offset on c.**

**Q5 — the truncation mechanism (Grace R74, gated this session):**
> "(ν)_m = (ν)_{m₁} · (ν − 3/2)_{m₂}, and a K-type survives iff that factor is nonzero"
**Reading: truncation fires at the PHYSICAL Wallach points ν ∈ {0, 3/2} — her must-catch passed there.
The formula's ν is the physical parameter.**

## DETERMINATION (pending verification)
**Every checked corpus formula writes its ν/c in the PHYSICAL WEIGHT LABEL; every ρ-shift appears
EXPLICITLY inside a formula (λ+ρ formed in Q4; the pairing zeros of Q1), never as a hidden offset on a
label.** Conjoined with Lyra's Item 2 (FG's E₀ = physical label): **both units are physical, so no
half-ρ shift exists between E₀ and corpus-ν.**
> ### ⟹ **FAMILY B: c = −E₀ (conjugation partner c = E₀ − 5). Family A's ±ν−5/2 requires a hidden
> half-ρ offset that BOTH reads exclude.**
**Pre-registered consequences (5485, frozen before this read; NOT fired — awaits R91):** (a)'s final
failure mode = **NAMED LOSS (P3.i)** · my 5477-era wall finding = **DIES as artefact** · the c_conv
cross-check slot routes to Grace.

**One residual, named honestly: this read covered FIVE formulas — the ones load-bearing for the family
question (addresses, ladder, Wallach set, F638, truncation). A corpus-wide sweep for any OTHER formula
using a shifted convention was NOT done and would be the Z3-style follow-up. — Elie**
