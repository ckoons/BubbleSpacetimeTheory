# F863 — the CFS map (central play), first pull: SOURCE CORRECTION to the causal classification — my earlier F843/F848/F850 had it WRONG, and the corrected Def 1.3 changes what the map must reproduce. **Working from the verified anchor (arXiv:1102.2585, Finster) as instructed, NOT from memory — and the instruction paid for itself immediately: my memory of the CFS causal classification was wrong. VERBATIM (Finster Def 1.3): the closed chain is A_xy = P(x,y)P(y,x) (the fermionic-projector kernel times its reverse), with non-trivial eigenvalues λ^{xy}_1,…,λ^{xy}_{2n} (2n = twice the spin dimension); "The points x and y are called TIMELIKE separated if the λ^{xy}_j are all REAL. They are said to be SPACELIKE separated if all the λ^{xy}_j are COMPLEX and have the same absolute value." My earlier F843/F848/F850 stated "spacelike = eigenvalues equal magnitude, timelike = they differ" — that is WRONG (it's about REAL-vs-COMPLEX, not just magnitude; spacelike is complex-AND-equal-magnitude, timelike is all-real). So any mapping built on the old classification (F843 ties=spacelike, the F848 G2-structure "verbatim match") must be REDONE against the corrected Def 1.3. I OWN this — it's exactly why "work from the source, not memory" was the instruction, and the phantom lesson (run the source, don't assert) caught it before we built the Einstein–Dirac inheritance on a wrong pier. And I ran a first toy of the closed chain BEFORE claiming any map: a generic (2,2)-signature F(o) does NOT automatically realize Def 1.3 (my quick toy gave mixed/boundary spectra, and its F(o) was not even the fermionic-projector structure, so it is NOT a valid test) — the honest lesson is the map is NOT automatic; it needs OUR specific committed-mode F(o) (F849/F850) evaluated at genuinely separated points via the explicit g_x action (the F855 point-separation machinery, still owed). NO map claim, NO Einstein promotion.**

**Lyra, Saturday 2026-08-08. CFS map central play, first pull, from the verified anchor. The deliverable is a CORRECTION (the causal classification I and the corpus had wrong) + the corrected object + the honest state (map not established, needs the real F(o) computation). Phrased "if the map holds, then…" throughout; nothing about Einstein promoted. RUBRICS Layer-2 done-bar below.**

## The source correction (banked — from arXiv:1102.2585, Finster Def 1.3)
| object | verbatim (source) | my earlier (WRONG, memory) |
|---|---|---|
| closed chain | **A_xy = P(x,y)P(y,x)** : S_x → S_x | (loosely F(x)F(y)) |
| eigenvalues | λ^{xy}_1,…,λ^{xy}_{2n} (2n = 2·spin dim) | — |
| **TIMELIKE** | **all λ^{xy}_j are REAL** | "eigenvalues differ" ✗ |
| **SPACELIKE** | **all λ^{xy}_j COMPLEX, same \|·\|** | "eigenvalues equal magnitude" ✗ (missed the COMPLEX requirement) |

**Consequence:** F843 (ties=spacelike/simultaneous) and F848 (G2-structure "verbatim match to Finster") were mapped to the WRONG classification. The real test is real-vs-complex, not degenerate-vs-split magnitude. The map must be REDONE on Def 1.3.

## What I ran (the number, before any claim — phantom lesson)
- Formed the closed-chain toy on a (2,2) spin space with a generic S-self-adjoint F(o), under a rotation (candidate spacelike) and a boost (candidate timelike). **Result: neither gave the Def-1.3 signature cleanly (mixed/boundary spectra).**
- **Honest read:** this is NOT a valid object test — my F(o) was a generic operator, not the fermionic-projector structure. What it DOES show: the map is **not automatic** — you cannot wave "(2,2) signature ⟹ CFS classification." The real test needs OUR committed-mode F(o) (F849/F850) at separated points via the g_x action (F855 machinery, owed).

## The map, stated honestly (the "if–then", not asserted)
- **IF** our closed chain A_xy = F(x)F(y) (from the committed-mode F(o) at separated x,y) reproduces Def 1.3 — timelike ⟺ all-real, spacelike ⟺ complex-equal-|·| — **THEN** our two-point commitment object IS the CFS fermionic-projector kernel structure, and we inherit the CFS Einstein–Dirac forcing route ("if the map holds, then…").
- **It is NOT shown.** The generic toy fails; the object-match needs the explicit committed-mode F(o) at separated points. **NO Einstein promotion** (Cal's standing guard); sin²θ_W's PD ceiling (which rides this same #79 forcing) is unaffected.

## RUBRICS Layer-2 done-bar (FRAMEWORK / map, honest)
- [x] Mechanism concrete: closed chain A_xy = P(x,y)P(y,x); Def 1.3 classification (real=timelike, complex-equal-|·|=spacelike).
- [x] Open edges named precisely: (i) redo F843/F848 map on Def 1.3; (ii) compute OUR F(o) at separated points (g_x action, F855) and test Def 1.3.
- [x] Tier = Framework/open: map NOT established; "if the map holds, then…"; Einstein NOT promoted.
- [x] Worked from the verified anchor (not memory); ran the number before claiming; owned the prior error.

## Handoffs
- **@Keeper** — first pull on the map delivered a CORRECTION, not a map: the CFS causal classification is timelike=all-REAL / spacelike=COMPLEX-equal-|·| (Finster Def 1.3), and my earlier F843/F848/F850 had it wrong (magnitude-only). The map must be redone on Def 1.3; the object-match needs the committed-mode F(o) at separated points (F855). No Einstein promotion; the "if the map holds" phrasing holds. This is the source-not-memory instruction and the phantom lesson both paying off.
- **@Elie** — your earlier "200/200 form-match vs Minkowski" (F848 era) should be re-checked against Def 1.3 (real-vs-complex), not the magnitude-only version — if it was coded on the wrong classification, the 200/200 may not survive. Please confirm which classification your calculator used.
- **@Cal** — guard held: no map claim, no Einstein promotion, "if the map holds, then…" throughout; and I ran a toy that FAILED to confirm the easy map, reported honestly. The corrected Def 1.3 is the anchor to audit future map claims against.
- **@Casey** — the first thing building the map did was catch a mistake — mine, and it was in the corpus. Finster's rule for when two points are "space-separated" versus "time-separated" is sharper than I'd remembered: time-separated means a certain set of numbers come out purely real; space-separated means they come out as *complex* numbers that happen to all have the same size. I'd been carrying a fuzzier version ("same size vs different size") and had mapped our structure onto it — so that mapping has to be redone against the real rule. And I checked before claiming: a generic version of our object does NOT automatically obey Finster's rule, so the map is real work, not a freebie — it needs our actual commitment object computed at two genuinely different points, which is the piece I parked last session as needing a fresh head. So: no claim that we've inherited "gravity is forced," the honest rule is now on the table to test against, and the wrong version is off it. That's the map-building doing its job on turn one. Nothing pushed.

Notes only; no toy/theorem claimed (source correction + honest map state). F863: CFS causal classification SOURCE-CORRECTED (Finster Def 1.3, arXiv:1102.2585): closed chain A_xy=P(x,y)P(y,x), eigenvalues λ^{xy}_j; TIMELIKE=all REAL, SPACELIKE=all COMPLEX with equal |·|. My earlier F843/F848/F850 ("equal magnitude vs differ") WRONG — missed the real-vs-complex distinction; map must be redone on Def 1.3. Ran a closed-chain toy BEFORE claiming: generic (2,2) F(o) does NOT auto-realize Def 1.3 (not a valid object test; F(o) wasn't the fermionic-projector structure) → map is NOT automatic; object-match needs committed-mode F(o) at separated points (g_x action, F855). "IF the map holds THEN inherit Einstein–Dirac forcing" — NOT shown, NO Einstein promotion. @Elie re-check 200/200 form-match vs Def 1.3 (real-vs-complex). @Keeper map redone on Def 1.3; @Cal guard held. — Lyra