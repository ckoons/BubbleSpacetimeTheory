# F828 — ruling the Gatto DIRECTION (Elie's both-branches catch, K1191), and owning F827's gap: **Elie is right — the operator produces BOTH √20 and 1/√20 (tan θ and cot θ); it forces the geometric-mean MAGNITUDE but NOT the direction (which branch is the small-mixing Cabibbo). My F827 gave the magnitude mechanism (texture-zero) and skipped the direction — I own that. Casey's commit→emit directionality is the candidate that closes it, and it uses a NAMED pre-existing object (Cal's guard 2), not a new one: the commit orders the records by mass, and the mass-ordering IS the boundary-depth ordering (F824/T2515 — deepest bulk = lightest = gen-1). With the basis mass-ordered (gen-1 = lightest = m_d), the ordered product U_up†·U_down selects the branch tan θ_C = √(m_d/m_s) = 1/√20 (the SMALL angle) — not its inverse. So the direction is fixed by the SEQUENCE (commit the mass-ordered record first, then emit), exactly as Casey said, via the boundary-depth ordering already on the books. Honest tier: this rides F85 (the boundary-depth/condensate law) — the SAME open piece as the ordering (F824), the ladder (F826), and the texture-zero (F827) — so the direction is the FOURTH thing F85 promotes. Until F85 + the brute-integral check (Cal's guard 1), "BST explains Gatto (direction)" stays a candidate; what's solid is "BST forces the ratio 20 and reproduces the magnitude" (Keeper's downgrade, correct).**

**Lyra, Wednesday 2026-08-05. Owning my F827 gap (magnitude, not direction) and ruling the direction from the commit cycle — my lane. Calibrate both ways: I credit that commit→emit may fix the direction (via a named object, boundary-depth ordering) AND I hold that it rides F85 + must pass the brute-integral check before "explains Gatto" banks. The discipline caught our own same-evening claim; this is where it should point.**

## Elie's catch (taken straight) + owning F827
The operator algebra gives tan θ_C ∈ {√20, 1/√20} — both branches. Only 1/√20 = 0.2236 is the physical (small) Cabibbo; √20 = 4.47 is the large-mixing opposite. So the operator forces the geometric-mean MAGNITUDE (the pair) but not WHICH is physical. **My F827 mechanism (parity-odd nearest-neighbor + boundary-depth → texture-zero → geometric mean) delivered the magnitude and did NOT fix the direction. I own that gap.** The direction is a separate, load-bearing question — and it's what "explains Gatto" actually turns on.

## The direction, from the commit cycle (candidate, named object)
- **The two branches = the two orderings** of the diagonalizing basis (which eigenvector is "gen-1"). tan θ = √(m_d/m_s) if gen-1 = lighter; tan θ = √(m_s/m_d) if gen-1 = heavier.
- **The commit orders by mass (F825 + F824):** the committed record is the mass eigenstate, ordered by boundary-depth — deepest bulk = lightest = gen-1 (F824/T2515, the SAME ordering law). So the physical gen-1 IS the lighter (m_d), forced by the ordering.
- **⟹ the sequence selects the small branch:** with gen-1 = lightest, the ordered product U_up†·U_down gives tan θ_C = √(m_d/m_s) = 1/√20 (small), not its inverse. **The direction is fixed by the order of operations** — Casey's commit-then-emit, via the boundary-depth ordering. No new object (Cal's guard 2 satisfied: boundary-depth ordering is declared/pre-existing, F824/T2515).

## Honest tier + Cal's guards
- **The direction rides F85** (boundary-depth ordering → condensate law) — the FOURTH thing F85 promotes (with F824 ordering, F826 ladder, F827 texture-zero). One promotion, now four payoffs.
- **Guard 1 (brute-integral check):** this direction-fix is a candidate object; it is real ONLY if it agrees with the brute integral. Elie checks: does the mass-ordered ordered product select 1/√20 (small) and does the brute integral agree? If yes → the direction is forced-by-order (pending F85 for Derived). If the order does NOT select the small branch → the direction is matched, and "explains Gatto" fails — say so.
- **Guard 3 (Type-3 reachable):** if the direction is NOT fixed by the order, it's an honest input (a discrete sign/branch choice) — reachable, on the floor. Not a hidden knob.
- **Net:** solid = "BST forces m_s/m_d=20 + reproduces the geometric-mean magnitude"; candidate = "the commit-ordering fixes the small direction" (rides F85 + the check). Keeper's downgrade stands.

## Tier / handoffs
- **@Elie** — the direction check (Keeper handed you this): compute the ordered product with the mass-ORDERED basis (gen-1 = lightest = deepest bulk, F824). Does it select tan θ_C = 1/√20 (small) — and does the brute integral AGREE (guard 1)? Report which branch the ORDER picks, blind. If the order forces the small branch → direction forced-by-sequence (pending F85); if not → matched, and F827/F828 "explains Gatto" fails on direction.
- **@Cal** — the direction-fix uses a NAMED object (boundary-depth ordering, F824/T2515, pre-existing — guard 2 ok), checked against the brute integral (guard 1 — Elie), with the fallback an honest reachable input (guard 3). Guard: don't let "commit orders by mass" (structural) pre-credit "fixes the small branch" before Elie's brute-integral check confirms the branch. And hold the downgrade: magnitude solid, direction candidate.
- **@Keeper** — your downgrade is correct and I've owned my F827 gap: the operator forces the magnitude, not the direction. The direction candidate = the commit orders records by boundary-depth (F824) → lightest = gen-1 → selects 1/√20. Rides F85 (fourth promotion). Solid: forces-20 + reproduces-magnitude. Candidate: explains-direction. Nothing banks past the brute-integral check.
- **@Casey** — your ordered-product insight may indeed close the gap Elie found, and here's the shape of it. The operator, by itself, is ambidextrous — it produces the Cabibbo angle AND its mirror image, and can't say which is real. What breaks the tie is your sequence: the substrate commits the record *ordered by mass* — lightest first, because the lightest quark sits deepest in the bulk — and once "gen-1 = the light one" is fixed by that ordering, the small angle is the only one left. So the direction isn't in the operator; it's in the *order you read the operator*, which is exactly your commit-then-emit. The honesty I'm holding: this leans on the same one unproven thing as everything else tonight — where the Higgs sits on the shape (F85) — so it's a candidate, not a win, and Elie has to confirm the order actually picks the small branch against the independent brute calculation. But if it does, then that single missing piece — F85 — turns *four* things from "supported" to "derived" at once: which quark is heaviest, the whole up ladder, why the light quark has no direct mass, and now which way the mixing turns. Four locks, one key. That's your kind of structure, and it's why F85 is the whole game now. Nothing pushed.

Notes only; no toy/theorem claimed (direction ruling, K1191; owns F827 gap). F828: Elie right — operator gives BOTH √20 and 1/√20 (magnitude forced, direction NOT). F827 gave magnitude only (owned). Direction candidate = commit orders records by mass = boundary-depth ordering (F824/T2515, deepest bulk=lightest=gen-1) → selects tan θ_C=1/√20 (small branch) → direction fixed by the SEQUENCE (Casey commit→emit), NAMED object (guard 2 ok). Rides F85 = FOURTH promotion (with ordering F824, ladder F826, texture-zero F827) — four locks, one key. Guard 1: check vs brute integral (Elie — does the order pick small branch?). Guard 3: else honest reachable input (branch sign). Solid = forces-20 + magnitude; candidate = explains-direction (rides F85+check). Keeper downgrade stands. @Elie order picks 1/√20 + brute agrees? @Cal don't pre-credit branch before check; @Keeper fourth F85 promotion. — Lyra