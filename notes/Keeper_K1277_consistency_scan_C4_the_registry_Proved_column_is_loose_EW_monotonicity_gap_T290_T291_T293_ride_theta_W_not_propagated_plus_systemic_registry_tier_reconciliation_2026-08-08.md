# K1277 — Consistency scan (C4, our weakest axis): the registry "Proved" column ≠ the rigorous tier. Specific EW monotonicity gap (T290/T291/T293) + the systemic reconciliation task.

**Keeper (player-manager), 2026-08-08.** Timeboxed consistency scan for the NEXT hidden over-claim of the sin²θ_W type (Cal flagged C4 as our weakest axis). Found one specific gap + one systemic issue. **Neither retracts the QM-10/10 block-lift** (QM flagship uses none of these) — but both are real C4 debt for the endgame.

## Specific — the EW monotonicity gap (the m_Z ruling didn't propagate to the registry)
Three registry theorems are tagged **"Proved"** but ride the Weinberg angle (T197 = now Structural/Identified):
- **T290 (m_W = 80.38 GeV):** registry says "from θ_W + v. Parents: T197 (Weinberg Angle)." **Proved** — but its stated route is *through θ_W* (Structural). Yesterday we kept m_W Derived only via the **independent** n_C·m_p/8α route (task #87, verify the "8"). **Fix:** T290 must cite the independent route, and mark the θ_W-route as a lower-tier cross-check — not a bare "Proved from θ_W."
- **T291 (m_Z = m_W/cosθ_W):** **Proved.** But Casey RULED m_Z = **PD explicit-split** yesterday (K1275) — Grace applied it to the ledger; **the registry theorem was never updated.** Fix: T291 → PD explicit-split.
- **T293 (W/Z ratio = √(10/13)):** cos²θ_W = 10/13 rides θ_W → caps at Structural/Identified. **Proved** → fix.

**Root cause:** the monotonicity pass (Cal's rule) hit the LEDGER (constants.json, Grace) but not the REGISTRY theorem tags. The ledger and registry disagree on the EW sector. **Not QM-blocking; IS a pre-external gate for the EW-sector papers, and a C4 consistency defect now.** Route: Grace propagates the monotonicity caps to T290/T291/T293 in the registry; Keeper verifies.

## Systemic — the registry "Proved" column is used LOOSELY, ≠ the rigorous D/PD/I/C/S tier
The scan surfaced dozens of "Proved" registry entries that are plainly NOT rigorous-Derived: T318 (CI coupling ≤19.1%), T344 (multicellularity ~2-3 Gyr), T410 (Dunbar hierarchy), T411 (intelligence-loss taxonomy), T444 (forced cooperation), T472 (cosmic hierarchy, Ω_Λ=13/19), and many biology/social ones. The registry has been using **"Proved (depth d)"** to mean *"registered / derivation-depth d in the AC graph,"* NOT *"rigorous-tier Derived."* Those are two different axes, and they've collided in one column.

**This is the C4 weakness at scale — and exactly what a hostile appraiser (or Tegmark) would exploit:** "your registry says 600+ theorems Proved" reads as the crank-tell Cal warned about (RUBRICS C1). The honest position needs the registry's "Proved/depth" column DISENTANGLED from the rigorous tier column. **Endgame task (View-1 accuracy, C4):** add/repair a rigorous-tier field (D/PD/I/C/S) distinct from "registered-depth," so the registry can be MINED honestly and no "Proved" is read as rigorous-Derived when it's Identified/Structural. This is the biggest single View-1 accuracy item and it's the C4 axis Cal named.

## Scope discipline (calibrate both directions)
Do NOT bulk-demote the registry — that's the over-correction (K1261). Many "Proved (depth 0)" entries ARE rigorous (α, the QM axioms, T2545). The task is to ADD the honest rigorous-tier field and reconcile, entry by entry through the coverage/tier audit, NOT to sweep. Broad registration is fine; the fix is making the tier legible, not deleting the breadth.

## Actions
- **@Grace:** propagate the monotonicity caps to the registry (T290 cite-independent-route, T291→PD explicit-split, T293→Structural/Identified) — the specific EW fix; part of View-1 accuracy. Keeper verifies.
- **Endgame (new task):** disentangle the registry "Proved/depth" column from the rigorous D/PD/I/C/S tier — the systemic C4 reconciliation, run through the coverage/tier audit, no bulk sweep.
- **QM 10/10:** unaffected — ships on Casey's venue call.

— Keeper, K1277, 2026-08-08. Consistency scan (C4): registry "Proved" column is loose (= registered-depth, NOT rigorous tier). Specific: T290/T291/T293 (m_W/m_Z/W-Z-ratio) tagged Proved but ride θ_W (now Structural) — the m_Z=PD ruling hit the ledger not the registry; monotonicity gap → Grace propagates, Keeper verifies (EW pre-external, not QM-blocking). Systemic: dozens of "Proved" entries are Identified/Structural (Dunbar, cooperation, Ω_Λ) — the registry conflates registered-depth with rigorous tier; endgame task = add an honest rigorous-tier field, reconcile entry-by-entry, NO bulk demote (calibrate both directions). The single biggest View-1 accuracy item; the C4 axis Cal named. QM 10/10 unaffected. Nothing pushed.
