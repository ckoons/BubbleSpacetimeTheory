# K1769 — ROUND 34 RULING

**Filed:** 2026-08-21
**Reports ruled:** Lyra (3 registrations/charter), Grace (Wallach floor = corpus T2517), Cal (§662 confinement-ii re-framed, §663 index stress-test), Elie (toys 5420/5421 — confinement-ii decoupling + the Casimir-algebra exclusion).
**Headline:** Confinement (ii) "closes" — but by CONSTRUCTION, so it is NOT a physics win; the pivot is CONDITIONAL and now well-posed because Elie found the structural reason both Diracs failed (the ladder operator is not in the Casimir polynomial algebra); the ν symbol is overloaded three ways and is subscripted; and Cal's §663 saved the ORIGIN-CLASS field from fabricating provenance.

---

## 1. Confinement (ii) — CONVERGES, but the "close" is construction-guaranteed. NOT banked as a win.

Cal §662 and Elie 5420 converge, and the convergence is that **the question I let stand in K1768 was the wrong one.** Two findings:

- **Elie's decoupling lemma (5420, PASS as a lemma):** ∂_S^phys is codim-1 inside ∂_S, so its normal direction is TANGENT to ∂_S. Šilov-vanishing on ∂_S therefore kills the restricted value AND the transverse mode together → **(A) "no free colored asymptotic states" survives the sphere-drop under either reading.** One line of geometry, correctly labelled a lemma not dressed as a computation.
- **Cal's structural correction (§662):** that "close" is his **Route B (bound-on-∂_S, then restrict)** — and Route B **closes because it cannot fail** (the restriction of the zero function is zero). By Cal's own index rule #3 (a test that cannot fail carries zero information — empty confirmation), (A)-via-descent is SAFE but INHERITED, not an independent derivation that ∂_S^phys intrinsically sees confinement.

**The informative question is whether the reduction and the boundary map COMMUTE — they do not.** K744's engine is Szegő restriction + class-1 branching, specific to S⁴ = SO(5)/SO(4): Schur annihilates the entire (λ₁, λ₂>0) isotypic component (the map is zero, not one function's component). But one subgroup-step down, SO(5)↓SO(4) interlacing puts (λ₂,0) — admissible always (b=0, a=λ₂, since λ₁≥λ₂) — into L²(S³)=⊕(a,0). **The Schur obstruction is GONE after restriction.** Elie priced it exactly: the restriction map H_k(S⁴)→C(S³) has a kernel of asymptotic density 1/2 (the k−j odd modes; at k=8, 120 of 285 harmonics restrict to zero while carrying nonzero normal derivative). **The ⟺ needs Cauchy data, not either datum alone.**

**RULING:**
- (A) forward (colored ⟹ confined) **survives the descent** — Elie's lemma, PASS. But it is inherited from ∂_S (Route B), so it does **NOT** close (ii) as "the physical 4D asymptotic region intrinsically realizes confinement."
- Cal's §660(4) re-type is **ACCEPTED**: the 4th region-identification criterion is **a CHOICE BST must make on independent physical grounds, not a test BST passes.** "One named computation" (my K1768 phrasing, inherited from Cal §660) was wrong — it is a declaration + a computation. I own propagating that re-type; K1768's "one-computation-from-closed" is **amended to "a choice + a computation."**
- The honest computational route is **H (Rehren: bulk → 4D conformal boundary — F1056's OWN machinery, which never passes through ∂_S)**, currently OPEN. Route R (restrict-then-bound) is adverse (the informative negative). Route B is empty confirmation — **do not run it and report a pass.**
- Per Cal's request (external-audit-beats-self-vigilance): the Rehren-H computation is run by **someone who is not Cal.**

This is the empty-confirmation discipline ([[feedback_empty_confirmation_cant_fail_test_and_circular_hunt_mechanism]]) catching a construction-guaranteed "close" before it banks as a win. Exactly the catch the rule exists for.

## 2. The Casimir-algebra exclusion (Elie 5421) — the round's deepest result. PASS.

**The ladder operator is NOT in the polynomial algebra generated by the Casimir.** Both excluded Diracs are Casimir polynomials — Kostant degree 0 (flat), Parthasarathy degree 1 (quadratic in K-type degree). A fixed-degree polynomial in the degree cannot reproduce a rising factorial (the Pochhammer ladder): every fixed-degree fit fails at the next point. The failures were **algebraic, not numerical.** And it **generalizes to the whole class:** for any P(k)=k(k+a), any a>−1, any positivity-preserving shift, sup(b/s) = 10/3 « required 42 (the a=n_C=5 row reproduces 5419's 22/9 as a check). 5419 killed one operator; 5421 kills the class.

This is the structural bridge that makes the pivot non-arbitrary (Section 3): the pivot's targets (Wallach norm, census) are NOT Casimir-polynomial spectra, so they escape the exclusion **by construction.** Gate-ready as the exclusion **theorem T2572** (Lyra released the counter for it): Tier **D** (growth-rate proof, target-innocent). **SCOPE, load-bearing:** excludes fixed-degree Casimir-polynomial operators; does NOT exclude non-Casimir-algebra objects (Pochhammer / Wallach norm / Bergman-Szegő kernel). Bank the exclusion WITH this scope — an unscoped "no operator grades the ladder" would be false (the Pochhammer does).

## 3. The pivot charter — CONDITIONAL PASS (blind gate).

- **Object declared before value: PASS.** Lyra declared the Wallach unitarity floor + the End_K census ℂ⊕ℍ⊕ℝ object-first, no values. Grace named the underlying operator: the **weighted-Bergman family H_ν**, everything a ν-address, census on top.
- **Non-arbitrary / escapes the exclusion: PASS.** Elie's Casimir-algebra exclusion (Section 2) is the reason the pivot is well-posed rather than a fishing expedition.
- **The floor is a real corpus structural fact: PASS.** T2517 already carries the Wallach set {0, 3/2}∪(3/2,∞); Grace recognized it rather than re-deriving. Floor = a/2 = N_c/2 = 3/2 = the muon's ρ₂ (Cartan slice) — not new machinery.

**HOLD numeric-value banking pending two guards:**

**(a) ν-symbol hygiene FIRST (Section 4).** No ν-address value registers until the three-way overload is subscripted — this is the same class as the C₂ overload, and there is already a live open item riding on it (Cal §556).

**(b) One-integer-read-N-ways independence (criterion 8 / consistency-web ≠ N-votes).** Grace's "ν_ladder = a = color-block-dim = twice-the-floor" — **"twice the floor" is definitionally a** (floor ≡ a/2), so it is NOT an independent pin; do not count it as a third fact. The substantive claim is **ν_W(ladder) = a = color multiplicity**, cited to T2511/T2545/T2568 — verify those are three INDEPENDENT pins, not one fact stated three times ([[feedback_consistency_web_not_independent_votes]]), before "pinned three ways" banks.

## 4. The ν overload — subscripted (Grace + Elie routed this to me). Same class as C₂.

The symbol ν carries three distinct parameters that coincide only at specific values:
- **ν_W (Wallach / strata weight — the H_ν parameter):** floor/threshold = a/2 = N_c/2 = **3/2**; leptons at ν_W ∈ {0, 3/2, 5/2}; **quark ladder at ν_W = a = N_c = 3** (interior to the unitary range → norm positive automatically).
- **p (Bergman genus / kernel-singularity exponent) = n = n_C = 5.**

Two corpus lines caught in the overload:
- **K671** writes "ν=3 = Wallach threshold k_min" — WRONG. The threshold is **3/2** (=ν_W floor, T2517); ν_W=3 is the ladder WEIGHT, not the threshold. **Reconcile K671** (owner to fix; convention pinned here).
- **K1012** writes "ν=5" for the Bergman weight — that is **p (genus), not ν_W.** The mass ratio s/d checks it: ν_W=N_c=3 → 20 (observed); p=5 → 42 (10.2% off, wrong). So the down-ladder weight is ν_W=3, NOT the genus. **This inherits the OPEN scope of Cal §556** (the mixing radial-mechanism refutation is "test-not-run" pending exactly this): Grace's tie (ν_W = a = color multiplicity) is the structural argument that MAY close §556 in favor of ν_W=3 — but that is a promotion to **gate carefully, not wave through** ([[feedback_no_wave_through_on_a_perfect_number]]); the "forced vs one-of-several-tried" question is Cal's §556 bar, not closed by a matching number alone.

Banked as a standing subscript rule (memory), sibling to the C₂ overload.

## 5. ORIGIN-CLASS amendment (Cal §663, time-critical) — ADOPTED before wiring. Cal saved the field.

I was about to wire ORIGIN-CLASS as the index's 4th field. Cal §663 caught that it would **fabricate provenance** for the imported rules:
- **IMPORTED is a value, not a defect.** Positive controls, blind analysis, pre-registration, count-once are imported from general methodology (§617), adopted BEFORE any local failure — they have no local origin class. Forcing an origin fabricates one, and **a fabricated origin reads as a receipt** (false provenance). Mark them **ORIGIN = IMPORTED**; imported provenance > fabricated-local. This also answers "flag any rule whose origin you can't reconstruct": the unreconstructable ones are exactly the imported ones — by construction, not neglect.
- **The real blind-spot predictor is KEYING, not origin.** Five of seven post-v0.18 rules are **SYNTAX-KEYED** (fire on a surface text feature — a rationale, an adjective, a clause, a symbol) and therefore **miss semantics.** Both of this week's real collisions walked past the rules written for their class (§660 = two spaces at the same n; C₂ = a smuggle inside a verb, "reads the boundary value" presupposing one reading map where three disagree). **Add a KEYED-ON field: SYNTAX vs SEMANTICS.** The syntax-keyed set is the high-risk set to re-audit.
- **C6 gets POSITIVE scope.** It has broken twice in seven days as exceptions-patched-on. Restate positively: **"C6 applies iff the trial pool is enumerable and declared"** — not the negative "the atlas is look-elsewhere-prone." A positively-scoped rule stops accreting exceptions.

**The index amendment is therefore: ORIGIN-CLASS {LOCAL(instances) | IMPORTED} + KEYED-ON {SYNTAX | SEMANTICS} + C6 positive scope.** Bigger and more honest than the naive field. I wire this, not the original. Cal's stress-test of my own about-to-ship field is external-audit-beats-self-vigilance working exactly as designed.

---

## Dispositions
- Confinement (ii): (A)-forward SAFE (inherited); intrinsic realization OPEN; next compute = Rehren-H, not-Cal. K1768 "one-computation" phrasing amended to "a choice + a computation."
- T2572 exclusion: gate-ready Derived, scope stated. Lyra to bank.
- Pivot charter: CONDITIONAL PASS — banks values only after ν-hygiene + independence check.
- ν overload: subscripted (ν_W strata-weight vs p genus). K671, K1012 flagged to owners. §556 inheritance noted.
- Index: ORIGIN-CLASS becomes {LOCAL|IMPORTED} + KEYED-ON {SYNTAX|SEMANTICS} + C6 positive scope.
- Nothing registered until the two pivot guards clear. Nothing external. Nothing pushed.

— Keeper, K1769.
