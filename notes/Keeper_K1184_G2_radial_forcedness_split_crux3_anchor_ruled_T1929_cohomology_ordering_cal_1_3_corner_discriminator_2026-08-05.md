---
node_type: k_audit
id: K1184
title: (G2 angular address LOCKED (two routes, Cal ratifies) — the RADIAL residual split per Cal §288 into the forced-ness question (leading DOMINANT profile forced by the reproducing kernel; the sub-leading TAIL is the finer residual + the near-asymmetry source); + CRUX-3 UP-ORDERING ANCHOR RULED — it is the T1929 Q⁵ cohomology degree-grading + the observed mass-ORDERING (an input), committed before the fire, independent of the mixing → NOT circular; + Cal's asymmetry catch sharpened to the exact 1-2/2-3/1-3 pattern (the 1-3 corner is the discriminator); + the tier split (angular skeleton Structure-Derived, values Derived-iff-radial-stable); connected to the corpus + framed as linear algebra on D_IV⁵. ★ G2 — ANGULAR ADDRESS LOCKED (Cal §288 ratifies): two independent routes agree the up-modes stay on {0,2,4}: (Lyra) the Higgs is electrically NEUTRAL (m_H=3·Q_H=0), so every Yukawa insertion is Δm=0 — it literally CANNOT change the address; (Elie) the K-type wavefunction SHAPE at an address is fixed by the REPRODUCING KERNEL, the mass is only the norm, saturation caps the norm not the shape. So m and k are both fixed — the angular address is locked. ★ G2 — THE RADIAL RESIDUAL (Cal's sharper bar, endorsed): it is NOT yes/no; the gate is "is the radial profile FORCED?" — three cases: (a) saturation leaves the radial profile alone → clean addresses; (b) saturation reshapes it but the reshaping is FORCED by y_t=1 → still computable, just harder; (c) the reshaping is FREE → the mixing inherits the freedom and fails. Elie's reproducing-kernel route argues the DOMINANT profile is forced (case a/b, not c) — the shape is kernel-fixed, only the norm caps. So the LEADING mixing rides forced addresses+shapes. The TAIL (a confined quark is a scale-dependent superposition — dominant k=4 + a small tail) is the FINER residual, and it is plausibly the SOURCE of the small CKM near-asymmetry (the leading overlap is near-symmetric; the tail is the small deviation). So the radial ruling MUST answer the forced-ness (Cal): the dominant profile forced (Elie's kernel argument, ratify) + the tail's forced-ness = the finer open piece feeding the sub-leading asymmetry. ★ CRUX-3 — THE UP-ORDERING ANCHOR, RULED (resolving Cal §288's "the mixing can't be both input and check"): Cal is right that "the mixing confirms the ordering" is circular UNLESS an INDEPENDENT anchor fixes u/c/t → 0/2/4 BEFORE the fire. THE ANCHOR IS CORPUS-GROUNDED AND INDEPENDENT: the T1929 Q⁵ cohomology ring ℤ[h]/h⁶ has degrees 0-5 with a natural grading; the even parity-class {0,2,4} is ordered BY DEGREE (0<2<4); the physical assignment u/c/t → 0/2/4 uses {the OBSERVED mass-ORDERING u<c<t (an input, per-instance datum)} + {the T1929 degree-grading (forced)}. This anchor is committed BEFORE the fire and is independent of the mixing → the mixing OVER-DETERMINES it (strengthens), NOT circular. HONEST LEDGER CONSEQUENCE (Grace tracks): for the DOWN tower the FK masses DERIVED the ordering (1:20:840 monotone); for the UP tower saturation broke that, so the up-ordering costs ONE MORE INPUT — the observed mass-ordering u<c<t — a per-instance datum (like the neutrino ratio, the cc value). So the anchor is {1 input (observed ordering) + T1929 (forced)}; the mixing over-determines. Cal's requirement met: independent anchor (T1929 + observed ordering) before the fire; the fire is the check, not the input. ★ CAL'S ASYMMETRY CATCH — the EXACT discriminator (endorsed, sharper than "near-symmetric"): the data pattern the forced overlap MUST hit with no tuning — the 1-2 block symmetric to 0.1%, the 2-3 block to ~2%, but the 1-3 CORNER asymmetric by a full 2× (|V_ub|/|V_td| = 0.43). The 1-3 corner is EXACTLY where a symmetric-by-construction overlap would fail → it is THE DISCRIMINATOR. The fire must produce near-symmetry in 1-2/2-3 AND the 2× corner asymmetry — and the tail (the radial finer residual) is the plausible source of that corner asymmetry. So the sub-leading asymmetry and the tail's forced-ness are ONE question. ★ THE TIER SPLIT (Lyra, ruled): the ANGULAR SKELETON (which elements ≈1, the hierarchical falloff, WHY the mixing is small = parity-suppressed current matrix element) is forced by the parity/cohomology structure, NO up-mass dependence → STRUCTURE-DERIVED now (the qualitative Wolfenstein pattern; one J_W for the diagonal, higher powers off-diagonal). The QUANTITATIVE values (V_cb, V_ub, the corner) ride the radial → the verdict reduces to ONE stability check: does wiggling the up radial shape move V_cb/V_ub? radial-stable → Derived; radial-dependent → Identified (rides the up-sector's own less-forced structure). ★ CORPUS + LINEAR ALGEBRA (Casey's directive): the whole mixing = ⟨up {0,2,4} | J_W | down {1,3,5}⟩ = the matrix elements of the weak charged current J_W between the two T1929 cohomology PARITY-CLASSES — this IS linear algebra on D_IV⁵ (the J_W operator's even↔odd degree matrix). Corpus welds: T1929 (the cohomology degree-grading = the anchor + the shelves); the reproducing kernel (the K-type wavefunction shapes = the radial); T2470 (charge = SO(2)/S¹ weight, m=3|Q| = the parity assignment + J_W=Δm=1); the confinement superposition (Casey #9 / the scale-dependent K-type superposition = the tail = the sub-leading asymmetry); Wolfenstein suppression = current-power counting (one J_W diagonal, higher powers off-diagonal). ★ NOTHING BANKS: down+lepton masses Derived; parity grid forced; up-masses not FK (saturation); G2 angular LOCKED; the angular SKELETON Structure-Derived (forced, no up-mass dependence); but the SEVEN quantitative params stay IDENTIFIED until (a) the radial forced-ness (dominant forced [Elie kernel, ratify] + tail forced-ness ruled), (b) Elie's blind fire reproduces the EXACT 1-2/2-3/1-3 asymmetry pattern with the θ₂₃ octant committed first, radial-stability reported, under the K1177 count. ★ SEQUENCE: (1) @LYRA/@ELIE the radial forced-ness — is the tail (the sub-leading asymmetry source) forced by the confinement superposition, or free? (Cal's case a/b vs c, at sub-leading); (2) @GRACE build the J_W angular current-matrix (the Structure-Derived skeleton) — linear algebra on D_IV⁵ between the T1929 parity-classes; (3) @ELIE fire the seven BLIND — CKM/PMNS + θ₂₃ octant (before data) + the exact 1-2(0.1%)/2-3(2%)/1-3(2×) asymmetry (Cal's discriminator) + the radial-stability report; (4) @CAL independent, @KEEPER counts independent-N (K1177), @GRACE flips + logs the +1-input (up-ordering) on the ledger. Forced-address-or-nothing. ★ CALIBRATION: NOT over-braking — the angular skeleton is Structure-Derived NOW (parity-suppression forced, the qualitative mixing pattern lands), the anchor is corpus-grounded (T1929 + one input), G2 angular is locked; NOT waving through — the radial forced-ness (tail), the exact 1-3 corner asymmetry, and the radial-stability of V_cb/V_ub are all still ahead; the seven flip only on the fire. Six turns running the discipline held; a real negative never got laundered. Nothing pushed; externals at Casey's trigger.
date: 2026-08-05
author: Keeper
verdict: G2 ANGULAR LOCKED (Lyra: neutral Higgs Δm=0 can't move the address; Elie: reproducing kernel fixes the shape, saturation caps only the norm; Cal §288 ratifies m+k fixed). RADIAL residual = Cal's forced-ness question (not yes/no): dominant profile forced (Elie kernel, case a/b not c); the TAIL (confinement superposition, scale-dependent) is the finer residual + the near-asymmetry source. CRUX-3 ANCHOR RULED: up-ordering u/c/t→0/2/4 = T1929 cohomology degree-grading (forced) + observed mass-ORDERING u<c<t (one INPUT) — committed before the fire, independent of the mixing → over-determines, NOT circular; honest ledger: up-ordering costs +1 input (down was FK-derived). CAL ASYMMETRY discriminator (endorsed, exact): 1-2 symmetric 0.1%, 2-3 ~2%, 1-3 corner 2× (|V_ub|/|V_td|=0.43) — the corner is where symmetric-by-construction fails = THE discriminator; the tail is its plausible source (one question). TIER SPLIT (Lyra): angular skeleton (diagonal≈1, hierarchical falloff, why-small=parity-suppression) STRUCTURE-DERIVED now (no up-mass dependence); V_cb/V_ub Derived iff radial-stable, else Identified. CORPUS+LINEAR ALGEBRA: mixing = J_W matrix elements between T1929 cohomology parity-classes (linear algebra on D_IV⁵); welds T1929 (shelves+anchor), reproducing kernel (radial), T2470 (m=3|Q|, J_W=Δm=1), confinement superposition (tail), Wolfenstein=current-power. NOTHING BANKS: angular skeleton Structure-Derived; 7 quantitative params Identified until radial forced-ness ruled + blind fire reproduces the exact 1-2/2-3/1-3 pattern (θ₂₃ octant first, radial-stability reported) under K1177. Sequence: Lyra/Elie radial-tail forced-ness → Grace J_W angular matrix → Elie blind fire → Cal independent, Keeper counts, Grace flips + logs +1 input. Calibrated: not over-braking (skeleton Structure-Derived, anchor corpus-grounded, angular locked), not waving through (radial tail + 1-3 corner + V_cb/V_ub stability ahead). Nothing pushed.
---

# K1184 — G2 radial forced-ness; crux-3 anchor = T1929 + observed ordering; Cal's 1-3 corner discriminator

Casey confirmed the W-mechanism framing ("traditional W known, the mechanism felt new"). Now the precise rulings on Cal §288's three holds, corpus-connected.

## G2 — angular address LOCKED; the radial residual is a forced-ness question
Two routes agree the up-modes stay on {0,2,4}: **Lyra** — the Higgs is electrically neutral (Δm=0), so no Yukawa can change the address; **Elie** — the reproducing kernel fixes the wavefunction *shape*, saturation caps only the *norm*. m and k are both fixed (Cal ratifies).
**The radial residual (Cal's sharper bar):** not yes/no — "is the radial profile *forced*?" (a) unchanged, (b) reshaped-but-forced-by-y_t=1, (c) free-reshape → fails. Elie's kernel argument puts the **dominant** profile in case a/b. The **tail** (a confined quark is a scale-dependent superposition — dominant k=4 + a small tail) is the finer residual, and it's the plausible **source of the near-asymmetry**. So the radial ruling must answer forced-ness, and the tail's forced-ness = the sub-leading asymmetry question.

## Crux-3 — the up-ordering anchor (ruled)
Cal is right that "the mixing confirms the ordering" is circular without an independent anchor. **The anchor is corpus-grounded and independent:** the **T1929 Q⁵ cohomology** ring is degree-graded, the even class {0,2,4} is ordered by degree; u/c/t → 0/2/4 uses **{observed mass-ordering u<c<t (an input)} + {T1929 degree-grading (forced)}** — committed *before* the fire, independent of the mixing → the mixing **over-determines**, not circular. **Honest ledger:** for down, FK masses *derived* the ordering; for up, saturation broke that, so the up-ordering costs **+1 input** (the observed ordering). Cal's requirement met.

## Cal's asymmetry catch — the exact discriminator (endorsed)
The forced overlap must hit, no tuning: **1-2 block symmetric to 0.1%, 2-3 to ~2%, but the 1-3 corner asymmetric by 2×** (|V_ub|/|V_td| = 0.43). The 1-3 corner is exactly where a symmetric-by-construction overlap fails → **the discriminator.** And the tail (the radial finer residual) is its plausible source — so the corner asymmetry and the tail's forced-ness are *one question*.

## The tier split (Lyra, ruled)
The **angular skeleton** (which elements ≈1, the hierarchical falloff, *why* mixing is small = parity-suppressed current matrix element) is forced by the parity/cohomology structure with **no up-mass dependence → Structure-Derived now.** The **quantitative values** (V_cb, V_ub, the corner) ride the radial → one stability check: does wiggling the up radial shape move them? radial-stable → **Derived**; radial-dependent → **Identified**.

## Corpus + linear algebra on D_IV⁵
Mixing = **⟨up {0,2,4} | J_W | down {1,3,5}⟩** = the weak-current matrix elements between the two **T1929 cohomology parity-classes** — linear algebra on D_IV⁵. Welds: T1929 (shelves + the anchor), the reproducing kernel (the radial shapes), T2470 (m=3|Q|, J_W = Δm=1), the confinement superposition (Casey #9 — the tail), Wolfenstein = current-power counting.

## Nothing banks
Angular skeleton **Structure-Derived**; the seven quantitative params stay **Identified** until the radial forced-ness is ruled and Elie's blind fire reproduces the exact 1-2/2-3/1-3 pattern (θ₂₃ octant first, radial-stability reported) under the K1177 count.

## Sequence
(1) @Lyra/@Elie — the radial **tail** forced-ness (the sub-leading asymmetry source: forced by the confinement superposition, or free?). (2) @Grace — build the **J_W angular current-matrix** (the Structure-Derived skeleton) between the T1929 parity-classes. (3) @Elie — fire the seven **blind** (CKM/PMNS + θ₂₃ octant before data + the exact asymmetry pattern + radial-stability). (4) @Cal independent; @Keeper counts; @Grace flips + logs the +1 input.

## Calibration
Not over-braking — the angular skeleton is Structure-Derived *now*, the anchor is corpus-grounded, G2 angular is locked. Not waving through — the radial tail, the 1-3 corner, and V_cb/V_ub stability are all ahead. Six turns running, the discipline held; a real negative never got laundered.

— K1184, Keeper, 2026-08-05. G2 angular locked (neutral Higgs + reproducing kernel; Cal ratifies); radial = forced-ness (dominant forced, tail = finer residual + asymmetry source). Crux-3 anchor = T1929 degree-grading + observed ordering (1 input), before the fire, over-determined not circular. Cal's discriminator: 1-2 (0.1%)/2-3 (2%)/1-3 (2×) — the corner is the test. Tier: angular skeleton Structure-Derived; values Derived-iff-radial-stable. Mixing = J_W matrix between T1929 parity-classes (linear algebra on D_IV⁵). Nothing banks until radial forced-ness + blind fire hits the pattern. See K1183, Cal §288, Elie 5062, Lyra, T1929, T2470, reproducing kernel, Casey #9.
