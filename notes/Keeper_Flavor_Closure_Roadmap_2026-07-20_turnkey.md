# Flavor Closure Roadmap — turnkey for the next session (2026-07-20)

**Keeper | 2026-07-19 EOD | The flavor sector collapsed to one number (K769). This sequences the steps to close the leading anchor y_t + the neutrino link, with honest dependencies and expected tiers. Everything gates on Step 2 (the June open core).**

## Objective
Close **y_t** (the leading rank-1 mass anchor) and the **m₁ = 0 link** — or reach and map the honest tier limit precisely.

## The one-line state
13 Yukawas → one condensate vector **O = SO(5) vector (1,0), spherical, boundary-reaching (F603, pinned)** → one radial overlap → **the top's discrete-series address (a,b)**. That single number decides y_t, anchors the whole radial mass tower, and is the long-standing June open core.

## Sequenced steps

**Step 1 — angular structure (bounded; do first).** y_t = ⟨t_L | γ·Ô | t_R⟩ ≤ 1; γ·Ô has eigenvalues ±1, so the +1 eigenspace is 2-dim, and **y_t = 1 ⟺ top_L, top_R both lie in that 2-dim +1 eigenspace.** State precisely. *Caveat:* which weight-state the top is (hence its eigenspace alignment) entangles with Step 2 — so Step 1 bounds but does not close the angular part alone. Owner: Elie (rep theory).

**Step 2 — ★ the top's discrete-series address (a,b) — THE HARD STEP (June open core).** Pin which discrete-series rep the top (outermost gen-3 up-type) sits in. Approaches to try:
  - (i) the **boundary-reaching condition** — the top reaches furthest toward the Shilov boundary (heaviest = largest radial moment), which constrains (a,b);
  - (ii) the **mass = radial moment ladder** — the top's radial moment is the spectrum maximum → its address;
  - (iii) the **K-type tower** structure of the SO(5,2) discrete series.
  If it doesn't close, **name exactly what's missing** (as Lyra did — "the (a,b) address"). Owner: Lyra (+ Casey's ground-vs-outermost reasoning). **Everything gates here.**

**Step 3 — the radial overlap (depends on Step 2).** Given (a,b), compute ⟨t|O⟩ across the λ₂ = ½ → 0 gap. Decides **y_t = 1 (parallel, derived) vs < 1 (computed CG)**. **★ Specific candidate to test (K771):** does the boundary deficit come out **exactly 1/2^g** (→ y_t = M_g/2^g = 127/128, Casey's RS-code picture, DERIVED as a theorem) or **0** (→ exact-1 + RG running, and 127/128 retired)? Owner: Elie verifies. *Guard:* the 0.992 gap is ambiguous (RG-vs-CG, Elie's fish) — the overlap must be **computed**, not fit to 0.992 or to 127/128.

**Step 4 — the neutrino zero-mode (K770; depends on discrete-continuous machinery).** Does the discrete top mode's failure to reach the continuous boundary **necessarily produce a zero-mode** (the neutrino)? If yes, derives **m₁ = 0 and the y_t < 1 deficit as one thing.** Check convergence with the rank-2 m₁ = 0 (two routes meeting). Owner: Lyra + Keeper.

**Step 5 — two condensates, one substance (parallel; OP-6/OP-7).** The QCD chiral condensate's K-type — is it a *different* SO(5) rep than the Higgs vector (e.g. a spin/tensor rep, matching Casey's "QCD = spin-coupled")? Are O and it two projections of one measure μ (→ explains m_p = 6π⁵·m_e)? Owner: Lyra/Grace, independent of Steps 1–4.

## Dependencies & honest expectation
- Steps 3, 4 gate on **Step 2** (the hard nugget). Step 1 is bounded-now. Step 5 is parallel.
- **If Step 2 cracks:** y_t closes (Step 3), the neutrino link likely follows (Step 4) — a major close.
- **If Step 2 doesn't crack:** we have still *mapped the boundary exactly* — flavor = one pinned vector O + one open radial address — which is itself a clean, publishable honest state (leading rank-1 anchor + Tier-2 corrections, with the single open number named).
- **Tier discipline:** O = SO(5) vector is derived (F603); y_t = 1 stays SUPPORTED until Step 3 forces parallelism; the neutrino link and two-condensate unification are LEADS. Don't bank y_t = 1 on the 0.992.

## Next-session first actions
1. `date`; read CI_BOARD + this roadmap + K769/K770/F603.
2. Run `team_prompt_2026-07-19i` (already contains Steps 1–4 + the parallel thread).
3. Elie: Step 1 (angular eigenspace) immediately — bounded, no address needed.
4. Lyra: Step 2 (the address) — the bottleneck; Casey's reasoning is the entry.

— Keeper, 2026-07-19 EOD. Turnkey closure roadmap: flavor gates on ONE number (the top's discrete-series address, June open core). Step 1 bounded-now (angular eigenspace); Step 2 the hard nugget (the address); Steps 3–4 follow if it cracks (y_t + the neutrino link); Step 5 parallel (two condensates). If Step 2 holds, we've mapped the boundary exactly — a clean honest state either way.
