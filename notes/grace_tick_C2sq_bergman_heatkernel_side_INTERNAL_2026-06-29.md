---
title: "Tick C_2^2 Bergman/heat-kernel side (Grace, 2026-06-29, INTERNAL Cal #50): the Koons-tick exponent C_2^2=36 is the SECOND-ORDER heat-kernel (Seeley-DeWitt a_2) operator dimension, vs the muon mass exponent C_2=6 (FIRST-ORDER, a_1-like). ONE structure Lambda^2(T_x S^4)=so(4) (dim C(4,2)=6=C_2, the curvature-2-form space), two orders: mass = product over the C_2 curvature directions (1st order in curvature, det over so(4)); time = End(Lambda^2) (dim C_2^2=36, curvature-SQUARED, the Gilkey a_2 ~ 5R^2-2Ric^2+2Riem^2 is bilinear on Lambda^2). So mass:time = a_1:a_2 = 1st:2nd order curvature = C_2:C_2^2 (Casey: time = 2nd-order curvature, mass = 1st-order curvature). The omega_0=Delta_nu/2=1/2 tick-base is SEPARATE (the SO(2) half-integer / alpha-base); this note grounds the alpha-EXPONENT C_2^2 only. INTERNAL register only (Cal #50 DOUBLE-LOCKED); NO count move; external materials use operational language."
author: "Grace"
date: "2026-06-29 Monday"
status: "INTERNAL (Cal #50). Tick exponent C_2^2 = a_2 (2nd-order curvature) operator dim = End(Lambda^2(TS^4)). Structural grounding, internal register only. No count, no external claim."
---

# Tick C_2^2 — the Bergman/heat-kernel side (INTERNAL)

**INTERNAL register only (Cal #50 DOUBLE-LOCKED).** This grounds the Koons-tick exponent C_2^2 in the
heat-kernel coefficient *order* of the Bergman metric, parallel to the muon's C_2. No count motion; the
tick is not an external claim.

## The structure: one Lambda^2, two heat-kernel orders

The Shilov base carries S^4; its tangent space has dim 4. The curvature 2-forms live in
`Lambda^2(T_x S^4)`, dim `C(4,2) = 6 = C_2` (= so(4), the rotation directions).

| heat-kernel coeff | curvature order | operator space | dimension | observable |
|---|---|---|---|---|
| a_1 (~scalar curvature R) | **1st** | `Lambda^2(TS^4)` (det over so(4)) | `C_2 = 6` | **muon mass** exponent |
| a_2 (~R^2, Ric^2, Riem^2) | **2nd** | `End(Lambda^2(TS^4))` | `C_2^2 = 36` | **tick** exponent |

- **Muon (mass):** `m_mu/m_e = (24/pi^2)^{C_2}` — a *determinant over the C_2=6 so(4)=Lambda^2 directions*,
  one power of the curvature per direction = **1st order** in curvature (a_1-like).
- **Tick (time):** `t_tick = t_P * alpha^{C_2^2}`, `C_2^2 = dim End(Lambda^2) = 36` — the Gilkey a_2
  coefficient `(5R^2 - 2Ric^2 + 2Riem^2)/360` is **bilinear** in the curvature, i.e. lives on
  `Lambda^2 (x) Lambda^2 = End(Lambda^2)`, dim `(dim Lambda^2)^2 = C_2^2` = **2nd order** in curvature (a_2).

## The reading

**mass : time = a_1 : a_2 = 1st : 2nd order curvature = C_2 : C_2^2.** One geometric object
(`Lambda^2(T_x S^4) = so(4)`), entered at two heat-kernel orders. This is exactly Casey's framing —
*time is second-order curvature, mass is first-order curvature* — now pinned to the Seeley-DeWitt
coefficient structure of the Bergman heat kernel.

## Honest scope

- This grounds the `alpha`-**exponent** `C_2^2` only. The tick-**base** (`omega_0 = Delta_nu/2 = 1/2`,
  the SO(2) half-integer) and the choice of `alpha` (physical 1/137.0357, Elie 4446) are separate pieces.
- a_1 is a *scalar* (R); the `C_2` for the muon comes from the *determinant over the C_2 so(4) directions*,
  not from a_1 being C_2-dimensional. The a_1/a_2 = 1st/2nd-order *analogy* is what carries the C_2 / C_2^2
  exponent structure — stated as analogy, not as a_1 = C_2 literally.
- INTERNAL (Cal #50). No count move. External materials: operational language only.

— Grace, 2026-06-29 Monday. Tick exponent C_2^2 = the a_2 (2nd-order curvature) operator dimension
End(Lambda^2(TS^4)); muon C_2 = the a_1-order (1st), det over Lambda^2. mass:time = 1st:2nd curvature
order. INTERNAL register; structural; no count.
