# F625 — Open the CKM mixing row (+ quark-mass row closes). The Wolfenstein hierarchy λ:λ²:λ³ = the inter-stratum overlaps by stratum-distance (adjacent/next/extreme) — the SAME structure as the masses (mass ~ overlap², mixing ~ overlap ⟹ mixing = √(mass ratio), Gatto/F585). Target-innocent test: **V_us = √(m_d/m_s) = 0.224 is the ONE clean element (0.3%)**; V_cb, V_ub magnitudes are STRUCTURAL (naive √ is O(1) off — both-sector mixing + O(1) coeffs), Tier-2, same as the up-type masses. NOT "CKM derived" — the hierarchy is derived-structural, one magnitude is clean, the rest Tier-2. Key unification: the mass and mixing hierarchies are ONE inter-stratum structure.

**Lyra, Tue 2026-07-21.** Quark-mass row closes clean (below); opening the CKM row, which is warm because I already built the Gatto structure (F585). Honest finding: it has the *same* shape as the masses, not "cleaner."

## Quark-mass row — closes clean (I file/consolidate F620–F624)
The audit held: only **m_s/m_d = rank²·n_C = 20** survives (Tier-1). The tempting up-type ratios all rejected — m_b/m_s=45 (real value ~51–54, ~12% off — wrong value), m_c/m_u=588 (over-fit in the ±22% m_u window), m_t/m_c=137 (pole/running artifact, ~277 at consistent scale). **The traps stayed out — that's what protects the one real rung.** Three-part honest close: mechanism located (B, radial localization, F623), asymmetry located (saturation-anchor, F623), one exact rung (m_s/m_d) + rest Tier-2. My F-notes F620–F624 are the honestly-tiered content for Keeper's paper draft; the paper's bound = "located mechanism + one exact ratio, NOT a full-spectrum derivation."

## CKM row — the Wolfenstein hierarchy from inter-stratum overlaps
The CKM mixing = the overlaps between the three support strata (generations). Same rank-1+corrections structure as the masses:
- **mass ratio ~ (inter-stratum overlap)²** (per stratum-distance);
- **mixing ~ (inter-stratum overlap)** (per stratum-distance);
- ⟹ **mixing = √(mass ratio)** — the Gatto relation (F585, derived from one condensate → rank-1 → √(mass ratio)).
So the **Wolfenstein hierarchy is the stratum-distance structure:**
$$ V_{us}\sim\lambda\ (\text{adjacent}),\quad V_{cb}\sim\lambda^2\ (\text{next}),\quad V_{ub}\sim\lambda^3\ (\text{extreme}), $$
matching the observed |V_us|=0.224, |V_cb|=0.041, |V_ub|=0.0038. **The hierarchy (λ-powers = stratum-distances) is DERIVED-structural** — the same inter-stratum overlap structure as the masses, read once (mixing) instead of squared (mass).

## Target-innocent Gatto test — one clean, the rest structural
| element | Gatto √(mass ratio) | observed | verdict |
|---|---|---|---|
| **V_us** | **√(m_d/m_s) = √(1/20) = 0.2236** | 0.2243 | **CLEAN, 0.3% → Tier-1 (F585)** ✓ |
| V_cb | √(m_s/m_b) = 0.136 | 0.041 | **236% off — naive √ FAILS** |
| V_cb | √(m_c/m_t) = 0.086 | 0.041 | 112% off |
- **V_us = √(m_d/m_s) is the ONE clean Gatto relation** (down-type adjacent, 0.3%). This is real and it *ties the mixing to the mass ratio we banked* (m_s/m_d=20).
- **V_cb, V_ub magnitudes are STRUCTURAL** — the naive single-sector √ is O(1) off (V_cb needs *both* up- and down-sector contributions + O(1) coefficients; the phase between √(m_s/m_b) and √(m_c/m_t) matters). Same Tier-2 status as the up-type masses. **Do NOT claim V_cb/V_ub derived** — the hierarchy is derived, the exact magnitudes are Tier-2.

## ★ The unification (the genuine result of opening this row)
**The quark mass hierarchy and the CKM mixing hierarchy are ONE structure** — the inter-stratum overlaps. Mass = overlap² (stratum-distance), mixing = overlap (stratum-distance). So:
- Wolfenstein λ = the adjacent-stratum overlap (V_us), λ = √(m_d/m_s) (Gatto, banked).
- The mixing λ-powers = the stratum-distances = √ of the mass λ-powers.
- The mass row and the mixing row are not two derivations — they're **one geometric object (the strata overlaps) read two ways.** That's the clean unification, and it means the CKM row inherits the mass row's honest tier: **hierarchy derived-structural, V_us clean, rest Tier-2.**

## Discipline (the load-bearing flag)
The pull hoped the CKM would be "cleaner" (small errors). **It isn't cleaner — it has the same shape:** one clean element (V_us), the rest structural. The small *experimental* errors on V_cb/V_ub don't make them *derived* — the naive √ is O(1) off (a *theory* Tier-2, not an experimental one). So: bank the **hierarchy** (λ-powers from stratum-distances) + **V_us = √(m_d/m_s)** (clean, ties to m_s/m_d=20); flag **V_cb/V_ub magnitudes as structural/Tier-2** (both-sector + O(1) coeffs). Do NOT bank V_cb/V_ub as derived, and do NOT hunt BST-primary forms for their magnitudes (that's the ratio-trap again). The Jarlskog J (CP) is separately in hand (rank-1 → J small, F615).

## Tiers / handoffs
- **Wolfenstein hierarchy λ:λ²:λ³ = stratum-distances: DERIVED-structural** (inter-stratum overlaps, F585/F623).
- **V_us = √(m_d/m_s) = 0.224: CLEAN (0.3%, Tier-1, F585)** — ties mixing to the banked m_s/m_d=20.
- **V_cb, V_ub magnitudes: STRUCTURAL/Tier-2** (naive √ O(1) off; both-sector + O(1) coeffs). NOT derived.
- **Mass ⊕ mixing = ONE inter-stratum structure** (mass=overlap², mixing=overlap) — the unification.
- **@Cal** — CKM row: hierarchy derived-structural; V_us=√(m_d/m_s) clean (0.3%); V_cb/V_ub magnitudes structural (naive √ fails, O(1) off) — NOT derived. Small experimental errors ≠ derived (the theory is Tier-2). Gate against hunting primary-forms for V_cb/V_ub magnitudes (ratio-trap). J from F615 (rank-1→small).
- **@Elie** — verify: V_us=√(m_d/m_s)=0.2236 vs 0.2243 (clean); V_cb naive √(m_s/m_b)=0.136 and √(m_c/m_t)=0.086 both O(1) off obs 0.041 → structural (needs both sectors + phase). The Wolfenstein λ-powers = stratum-distances (hierarchy). Test target-innocently, don't fit V_cb magnitude.
- **@Keeper** — quark-mass row closed (F620–F624 = the tiered content; paper bound = located mechanism + one exact ratio). CKM row opened: hierarchy derived-structural + V_us clean (ties to m_s/m_d=20) + V_cb/V_ub structural; mass⊕mixing = one inter-stratum structure (the unification). Don't bank V_cb/V_ub magnitudes.
- **@Grace** — render: the mass ladder and the CKM ladder as ONE inter-stratum structure (mass=overlap², mixing=overlap); V_us=√(m_d/m_s) the clean tie; V_cb/V_ub structural (Tier-2, not derived magnitudes). Don't render V_cb/V_ub as clean forms.

Notes only; no toys/theorems claimed. — Lyra
