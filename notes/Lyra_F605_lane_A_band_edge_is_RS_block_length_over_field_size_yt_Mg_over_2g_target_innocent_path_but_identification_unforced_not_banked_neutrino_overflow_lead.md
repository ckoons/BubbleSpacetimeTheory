# F605 — Lane A (CORE): the band-edge deficit = the Reed–Solomon block-length gap. y_t = M_g/2^g = (RS block length)/(field size) = 127/128 = 1−1/2^g, now with a TARGET-INNOCENT derivation path (RS length = q−1 is a standard coding fact, not fit). But the fermion=codeword identification is unforced and the number stays scheme/RG-degenerate → CANDIDATE-LEAD strengthened, NOT banked. Lane A precisely mapped, not closed. + Lane B neutrino-overflow lead, Lane C Casimir pass.

**Lyra, Mon 2026-07-20. Lead: Lane A — the band-edge computation.** Peak-convergence; discipline fires hardest, including on Casey's 127/128 and on my own new derivation. Result: a genuine target-innocent derivation *path* appeared, but the guards hold — it's a strengthened lead, not a close.

## ★ Lane A — the last band gap = the RS block-length deficit (the advance)
The substrate codes on **GF(2^g) = GF(128)** via Reed–Solomon (Paper #122, K59 cyclotomic mechanism) — established, not new. The **standard coding fact**: a primitive Reed–Solomon code over GF(q) has **block length n = q − 1**. For q = 2^g = 128:
$$ n = 2^g - 1 = 127 = M_g \quad(\text{Mersenne prime}). $$
Read the band-edge geometry through this: a **discrete** (fermion) mode is an RS **codeword**, occupying **n = M_g = 127** field positions; the **continuous** (boson) condensate **O spans the full field = 2^g = 128** positions. The normalized overlap is the fraction of shared positions:
$$ \boxed{\; y_t = \langle t|O\rangle = \frac{\text{RS block length}}{\text{field size}} = \frac{M_g}{2^g} = \frac{127}{128} = 1 - \frac{1}{2^g}. \;} $$
The deficit **1/2^g = the ONE field position the discrete codeword cannot occupy** — the parity/overflow symbol. That is Casey's "loses a neutrino's worth of its tail," made precise: the one code symbol beyond the M_g-length codeword.

**Why this is a real advance:** the deficit's value 1/2^g now has a **target-innocent source** — **M_g = q − 1 is the RS block length, a standard coding fact, NOT chosen to hit 127/128.** This addresses the "unit not a-priori unique" guard from F604: the code-unit is forced by the RS structure the substrate already runs on. The number stopped being a bare coincidence and became a candidate *derivation*.

## ⚠ But it does NOT close — the guards that survive (Cal #27, hardest here)
1. **The load-bearing identification is unforced.** "A fermion mode = an RS codeword of length M_g; the condensate O = the full field of size 2^g." This is *motivated* (the substrate RS framing, discrete-fermion vs continuous-boson) but **not proven.** If a fermion is instead the full field, or a codeword of a different (extended, length 2^g or 2^g+1) RS code, the deficit changes. The primitive-RS choice (length q−1 = M_g) is the natural one and it's a Mersenne prime (K59-favored), but "why primitive, why the top is the maximal codeword" must be forced.
2. **Scheme-dependent (survives).** y_t = √2 m_t/v = 127/128 holds for the **pole** mass (m_t=172.74, inside the bar); MS-bar (m_t≈163) gives 0.94, no match. Needs a reason the geometric y_t is the pole value.
3. **RG-degenerate — the number can't decide (Elie's fish, decisive).** 0.992 is degenerate between geometric 1−1/2^g and exact-1-plus-RG-running. **Only the computed overlap decides, not the value.** The RS reading is a *mechanism* for the geometric branch; it does not by itself rule out the RG branch.

**Verdict: Lane A is PRECISELY MAPPED, not closed.** The derivation path is RS-block-length/field-size (target-innocent); the single open step is **forcing "the top is the maximal RS codeword (length M_g) and O the full field"**; the number stays degenerate until that identification is forced independently. **127/128 stays CANDIDATE-LEAD — strengthened (now with a forced-code-unit path), still NOT banked.**

## Lane B — the shed symbol ↔ neutrino zero-mode (LEAD, and it converges with rank-2)
The deficit is **one RS field position** (the parity/overflow symbol beyond the M_g codeword). A parity/erasure position **carries no information → no mass → a zero-mode.** Casey's hypothesis: the top's shed tail = the massless neutrino, linking **y_t < 1 ↔ m₁ = 0.**
- **Two-route convergence (the real test):** m₁ = 0 was *independently* derived from **rank-2 counting** (F589: n(ν_R) = rank = 2 → one exactly-zero eigenvalue). Now a *second* route: the RS-overflow symbol is a zero-mode. **Two independent geometric routes to the same one massless neutrino** — if both are forced, that convergence is itself strong evidence (a Schur-generator meeting).
- **Structural support:** the top and ν are the two T₃=+½ upper partners (up-type quark ↔ neutrino across quark/lepton). The one the top sheds is the neutrino direction.
- **Tier: LEAD.** Casey's earlier "a neutrino worth" as a *number* fails (F604); as a **MODE statement** (the shed symbol IS a zero-mode = the neutrino) it's coherent and convergent with F589. Needs: is the overflow symbol *forced* to be a zero-mode, and does it *identify* with the ν_R of the rank-2 count?

## Lane C — which Poincaré Casimir on which K-factor (LEAD, first pass, flagged reach)
Casey's frame: one substance, two currents = the two Poincaré Casimirs, **P²** (linear → mass/inertia, spin-blind → Higgs) and **W²** (Pauli–Lubanski, angular → spin → strong sector). Map onto **K = SO(5)×SO(2):**
- **SO(2)** (1-dim, the conformal weight/dilation) ↔ **the scale/mass direction (P²-like)** — it sets the mass dimension; O's SO(2) weight is the hypercharge/scale. Candidate: SO(2) carries the P²/mass current.
- **SO(5)** (the "internal/spatial" rotations) ↔ **the angular/spin current (W²-like)** — carries spin/color/tensor structure.
- O = the SO(5) **vector (1,0)** (spin-blind, spherical) = the mass/Higgs projection; a **QCD chiral condensate** would be a *different* SO(5) rep — a higher tensor/spin rep (candidate: the adjoint (1,1) or symmetric (2,0)), carrying the W²/spin structure.
- **If O and the QCD condensate are two projections of one measure μ**, that would ground m_p = 6π⁵·m_e (mass from one measure, two rep-projections).
**Tier: LEAD, flagged reach** (per the prompt: ⟨q̄q⟩ is a scalar; "QCD = W² angular" needs the spin-dependence to be dynamical/hyperfine, not kinematic). First-pass mapping only; the clean question for Grace/me: **does the QCD condensate sit in a non-vector SO(5) rep, and is it the W²-carrying projection of the same μ as the Higgs vector?**

## Tiers / handoffs
- **Lane A: y_t = M_g/2^g via RS block-length — CANDIDATE-LEAD strengthened** (target-innocent code-unit path); **NOT banked** (fermion=codeword unforced; scheme-dep; RG-degenerate). **Precisely mapped, not closed.**
- **Lane B: neutrino-overflow zero-mode — LEAD**, converges with rank-2 m₁=0 (F589) — the two-route convergence is the test.
- **Lane C: Casimir↔K-factor — LEAD**, first-pass map (SO(2)↔P²/mass, SO(5)↔W²/spin), flagged reach.
- **@Cal — the gate, hardest:** the RS-block-length path makes the *unit* (1/2^g) target-innocent, but does NOT bank y_t — the fermion=maximal-codeword identification is unforced and the number is RG-degenerate. Do NOT let Lane A read as closed/derived. The one thing that would close it: force "the top is the maximal RS codeword" from the discrete-series structure, independent of the value.
- **@Elie — Lane A angular CG** (5-branch of 4⊗4, sub-maximal — bounds the angular factor); and: is the RS-overflow symbol *forced* to be a zero-mode (Lane B numeric)? The value 0.992 can't decide A — confirm the degeneracy stands.
- **@Keeper — the study-close:** Lane A precisely-mapped (RS-block-length path, one open identification, degenerate number); B + C leads with deciding computations named. Derived core unchanged (O=vector, rank-1→one mass, N_c quark, ceiling). 127/128 = strengthened candidate, not banked.
- **@Grace — Lane C table** (Casimir↔K-factor↔rep); Lane D render (band structure, the last gap = the overflow symbol). Don't render 127/128 as derived.

Notes only; no toys/theorems claimed. — Lyra
