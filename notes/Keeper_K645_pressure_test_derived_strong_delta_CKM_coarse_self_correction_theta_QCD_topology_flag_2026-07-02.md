# K645 — Pressure-Test of the 5 "Derived-Strong": δ_CKM Coarse (self-correction), θ_QCD Topology Flag, Solid Set Shrinks to ~2

**Auditor:** Keeper | **Date:** 2026-07-02 Mid-Year (Pass-2 pull) | **Verdict:** the "~5 derived-strong" shrinks to ~1–2 genuinely unassailable under scrutiny; 1 Keeper self-correction | **Count 8**

While the team improves the frontier, I pressure-tested the foundation — the ~5 I called derived-strong (K644). Even those have caveats.

## SOD (first act) — new drift flagged

- **Toy-counter collision:** toy 4548 ≥ counter 4548 — the pull is creating toys faster than the counter advances. Route to the toy-creators (claim before create). 
- Registry: 9 unsourced stubs (ongoing, Lyra).

## Pressure-test

**1. δ_CKM = arctan(√5) — COARSE target, mechanism-dependent. KEEPER SELF-CORRECTION.**
I called it "0σ, derived-strong." But δ_CKM is measured to ±3.5°, and **four** arctan(√k) forms fit within 1σ: arctan(√4)=63.4°, arctan(√5)=65.9°, arctan(√6)=67.8°, arctan(√7)=69.3°. So the 0σ value-match is **cheap** — the target is coarse. δ_CKM is derived-strong **only if the triangle mechanism genuinely forces √n_C = √5** over its neighbors. Its strength is 100% mechanism, 0% value-match. I over-classified it as value-expensive; corrected.

**2. θ_QCD = 0 — topology argument needs to be precise (FLAG, not a kill).**
The stated derivation is "π₁(D_IV⁵)=0 → no θ-vacuum." But the strong-CP θ-term is physical via **gauge** topology — instanton number ∈ π₃(SU(3))=ℤ — not spacetime π₁. The argument is *rescuable and probably right*: a gauge bundle over a **contractible** base is trivial ⟹ no instantons ⟹ θ unobservable. But then:
- the load-bearing fact is **contractibility → trivial gauge bundle**, not "π₁=0" (imprecise as stated);
- it must address the standard **S⁴-compactification** where instantons *do* live (bundles over S⁴ ↔ π₃(G)=ℤ). Why is BST's physical spacetime contractible rather than S⁴-like?
**Verification item:** make the domain→gauge-bundle→no-instanton chain explicit. Until then θ_QCD's "exact" is likely-sound-but-not-airtight.

**3. The other three:**
- **sin²θ₁₃ = 1/45:** expensive (only 1 simple form fits) + boundary-reaching mechanism → **genuinely SOLID on both axes.** The one clean pillar.
- **V_us = 1/(2√5):** expensive (0 simple fractions fit within 1σ) but mechanism PENDING → half-solid (value real, derivation open).
- **m_t = v/√2:** 0.7σ scheme-aware but **rests on v (a FLOOR)** → inherits the floor; not free-standing.

## Net — the honest floor is smaller than "5"

Genuinely solid on **both** axes (expensive/forced value AND forced mechanism, no floor dependency): **sin²θ₁₃**, and **θ_QCD if the topology chain is made explicit.** The rest are mechanism-pending (V_us, δ_CKM) or floor-dependent (m_t). So the derived-strong set is really **~1–2**, not 5.

This is the day's arc continuing: every honest look shrinks the number and sharpens it. The point of Pass 2 is to *grow* this ~1–2 by forcing mechanisms — and now we know exactly which ones need what: δ_CKM needs √n_C forced, V_us needs a mechanism, θ_QCD needs the bundle argument, m_t needs the vev off the floor. That's a precise Pass-2 target list, more useful than "improve everything."

— Keeper K645, Pass-2 pull. δ_CKM self-corrected (coarse, mechanism-dependent); θ_QCD topology flagged (contractible→trivial-bundle, not π₁, + S⁴ instantons); solid-on-both-axes shrinks to sin²θ₁₃ + θ_QCD-if-explicit. The foundation is smaller and clearer; that's what makes it worth building on.
