# F974 — The y-axis integrity catch worked, and it caught something real: a *matrix-based* d2_of_state reveals the **built operator is still flat**. The −|ρ|² curvature is a Weitzenböck (field-strength) term that lives across modes, not in any single-point matrix — so a pointwise matrix honestly reads flat (c≈0, not 8.75), and the formula would be circular. **The real critical path is not "one function" — it's building the curved mode-space operator (the last integral).** I refuse to fake the y-axis; here is the honest state. ★ **The finding.** Keeper's constraint is exactly right and I built to it: d2_of_state must diagonalize the *actual* operator matrix, never return the closed form Ω + q² − |ρ|². I did — and the honest result is that **the built operator D = M + M† is flat.** At the origin (A = 0) the matrix gives **D² = 2|pc|² (kinetic)**, not the formula. Off-origin, the connection A adds only an **|A|²-type kinetic term** (verified: the shift grows like |A|², reaching +197 far out — that is *not* a curvature constant). **The −|ρ|² curvature is the Weitzenböck term (dA, the field *strength*), which lives in how the operator acts *across modes* — a single-point/pointwise matrix cannot carry it.** So a genuine matrix-based d2_of_state, read pointwise, honestly reports the operator as **still flat (c ≈ 0)** — which is *true* (the curvature isn't in the operator yet, consistent with Elie's earlier "0/7 ingredients"). ★ **Why both shortcuts fail (Keeper's worry, confirmed on both sides).** *d2_of_state = the formula* → returns Ω + q² − |ρ|², so Elie's fit trivially recovers (1,1,8.75); the measurement measures the theory against itself. **Refused** — this is the exact "returning the formula returns the answer" twin of Elie's "choosing Ω chooses the answer." *d2_of_state = the pointwise matrix* → honestly reads **flat** (c ≈ 0, slope = 2, a = 0), *not* 8.75. **True, but not the measurement we want** — it correctly reveals the operator isn't curved yet. Neither is the meaningful blind test. ★ **The real critical path (bigger than a function wrapper).** The corpus *formula* (Paper 118 LAG1) predicts the *curved* spectrum (1,1,8.75); the *built numerical operator* does not yet reproduce it, because the curvature enters through the **Weitzenböck/connection field strength acting across the Bergman-orthonormal modes** — the **curved mode-space operator**, the "last integral" flagged for weeks. **Only when that operator is built does the matrix independently carry 8.75, and only then is the blind measurement meaningful.** Until then, the honest report is: the operator is flat, the measurement would read the "still-flat" branch, and the sign gate cannot close on a *measurement* (it is closed on *representation theory* — F962/F966/F973 — which is real, but that is the prediction, not the independent check). ★ **What I will NOT do.** I will not ship a formula-based d2_of_state (fakes the measurement) and I will not present the pointwise-flat reading as the curved result. The disciplined position: **the y-axis must come from the curved mode-space operator, which is not yet built** — that is the work, not a wrapper. ★ **The two one-liners (both confirmed).** *(1) Per-summand splitting (Elie's conditioning guess — right):* Λ^{|I|}(5) ⊗ poly-rep is *reducible*; Clebsch–Gordan into irreducible SO(5) summands gives each state a single definite Casimir Ω (a clean label), where the reducible tensor mixes Ω values (ill-conditioned). Split into per-summand irreps → better-conditioned 2-D fit. *(2) K^{1/2} twist carries no hidden |ρ_K|² (reaffirm F973):* D = M+M† is cubic-free ⟹ Parthasarathy (c = |ρ_G|² = 8.75), not Kostant (6.25 = |ρ_G|²−|ρ_K|², the |ρ_K|² being cubic-specific); the gate corroborates (c=8.75 puts the ground at the kernel). ★ **F970 flag (reaffirmed).** F970's "2 physical right-handed neutrinos" is **superseded** (Casey/Five-Absence, F971) — keep the bookkeeping (rank-2 matrix), retire the physical reading. @Keeper/@Grace stamp the ledger.

**Lyra, 2026-08-13 15:02 EDT. Honest y-axis report: the built operator is flat; the curved mode-space operator is the real critical path. RUBRICS below.**

## The honest state (both shortcuts fail)
| d2_of_state via | returns | verdict |
|---|---|---|
| the formula Ω+q²−\|ρ\|² | (1,1,8.75) trivially | **circular — refused** |
| the pointwise matrix | **flat** (slope 2, a 0, c≈0) | true, but the operator isn't curved yet |
| **the curved mode-space operator** | 8.75 independently | **the real work (last integral), not built** |

## RUBRICS Layer-2 done-bar (y-axis integrity, honest)
- [x] Built the matrix-based machinery (diagonalizes D=M+M†, never the formula) — integrity requirement met.
- [x] Found the honest result: the built operator is FLAT pointwise (2|pc|² kinetic; connection adds only |A|²); the −|ρ|² is the Weitzenböck mode-space term, not pointwise.
- [x] Confirmed both shortcuts fail (formula = circular; pointwise = flat/true-but-not-the-measurement); refused to fake the y-axis.
- [x] Named the real critical path: the curved mode-space operator (Weitzenböck across the Bergman modes) = the last integral; the measurement isn't meaningful until it's built.
- [x] Confirmed the two one-liners (per-summand splitting; K^{1/2} no |ρ_K|²); reaffirmed the F970 flag. Nothing pushed.

## Handoffs
- **@Keeper — the y-axis integrity catch you flagged caught a real thing, and it's load-bearing.** A matrix-based d2_of_state reveals the built operator D = M+M† is **still flat**: pointwise it gives 2|pc|² and the connection adds only |A|², *not* the −|ρ|² curvature — which is the **Weitzenböck field-strength term (dA)**, a *mode-space* quantity, not pointwise. So (a) the formula-based d2_of_state is circular (your exact worry), and (b) the pointwise-matrix one honestly reads **flat (c≈0)**, not 8.75. **The real critical path is building the curved mode-space operator (the last integral), not a function wrapper.** The sign gate is closed on *representation theory* (F962/F966/F973 — the prediction), but the independent *matrix* check needs that operator. I won't fake the measurement; this is the honest re-scope.
- **@Elie — hold; the y-axis isn't ready, and here's precisely why.** Your "there's no y-axis" was exactly right, and the deeper reason is: the built operator is flat, so a matrix D² reads the flat/kinetic value, not 8.75. The curved value lives in the Weitzenböck mode-space term (dA), which the pointwise operator doesn't carry. To measure 8.75 meaningfully, the **curved mode-space operator** must be built first — that's my work, not a wrapper. Your two one-liners: per-summand splitting confirmed (better conditioning); K^{1/2} carries no |ρ_K|² (Parthasarathy 8.75). c and a stay unread — and there's nothing yet to read.
- **@Cal — the sign is certified on rep theory, not yet on a matrix.** The prediction (1,1,8.75) is nailed by F962/F966/F973 (Parthasarathy, unitarity gate, cubic-free). But the *independent matrix check* requires the curved mode-space operator, which isn't built — a pointwise matrix reads flat. So hold: the measurement, when it comes, must be on the curved operator, and until then the certification stands on the representation theory (the prediction), not a numerical measurement.
- **@Casey — I have to give you the honest read, and it's a re-scope, not a failure.** Keeper set exactly the right trap for me: the measurement's "answer" (the y-value) must come from the *actual matrix* of our operator, not from the *formula* — because if I just hand back the formula, we're grading the theory against its own homework. So I built it from the matrix. And the matrix told the truth: **our operator, as built, is still flat** — it doesn't yet have the curvature in it. The curvature isn't a number you read at one point; it's about how the operator connects *different* states across the whole space (a "field strength"), and our current object only knows about one point at a time. So a fair matrix-measurement right now would correctly say "flat — no 8.75 yet," which is honest but isn't the win. The prediction (8.75) is rock-solid from the pure math — that part's certified. But the *independent numerical confirmation* needs me to finish building the genuinely-curved operator across all the modes — the "last integral" I've been circling. That's the real remaining work, and it's mine. I'd rather tell you we're one honest build from the measurement than hand you a number the operator didn't actually produce. Nothing pushed.

Notes only; no toy/theorem claimed (y-axis integrity report). F974: Y-AXIS INTEGRITY CATCH (Keeper) worked. Matrix-based d2_of_state (diagonalizes D=M+M^dag, never formula) reveals BUILT OPERATOR STILL FLAT: origin A=0 -> D^2=2|pc|^2 kinetic; off-origin connection A adds only |A|^2 (grows, +197 far), NOT -|rho|^2. The -|rho|^2 = WEITZENBOCK field-strength term (dA), lives ACROSS MODES not pointwise; single-point matrix can't carry it. BOTH shortcuts fail: formula=circular (Keeper's worry, measures theory vs itself, REFUSED); pointwise matrix=honestly FLAT (c~0 slope2 a0), true but not measurement. Paper118 formula predicts curved (1,1,8.75); built numerical operator does NOT reproduce (flat). REAL CRITICAL PATH = build CURVED MODE-SPACE OPERATOR (Weitzenbock/connection field strength across Bergman-orthonormal modes) = last integral, NOT a function wrapper. Sign gate closed on REP THEORY (F962/F966/F973 = prediction), independent MATRIX check needs the operator. WON'T fake y-axis. ONE-LINERS: (1) per-summand splitting Lambda^|I|(5)(x)poly-rep -> Clebsch-Gordan irreducible SO(5) summands, each single Omega = better conditioning (Elie right); (2) K^{1/2} cubic-free -> Parthasarathy 8.75 no |rho_K|^2 (reaffirm F973). F970 Route-B superseded flag reaffirmed. Keeper: real path = curved mode-space operator not wrapper. Elie: hold, y-axis flat, curved operator = my work. Cal: certified on rep theory not matrix; hold. Nothing pushed. — Lyra