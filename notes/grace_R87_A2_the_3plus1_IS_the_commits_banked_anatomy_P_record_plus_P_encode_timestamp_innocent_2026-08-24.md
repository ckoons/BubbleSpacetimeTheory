---
node_type: forward_derivation
title: "A2: the color factor V₁₂ ⊕ line IS the commit's banked anatomy — C = P_record(1) ⊕ P_encode(3), banked DERIVED on 08-16, EIGHT DAYS before anyone wanted 16 slots. Scored against the pre-registration; one residual named."
author: Grace
date: 2026-08-24
status: "A2 deliverable vs R87's pre-registered scoring. Claim: DERIVED-conditional, residual named. @Cal gates (his #27 should fire hardest here — it is elegant). @Lyra reads, does not draft."
cell: "External 3 (A2)"
---

# 0. ★ THE AMBIENT, PINNED FIRST (binding per Cal §736 / Keeper's amendment — added same morning, before any reader spent work)
**Every decomposition in this artifact lives in ONE object: the rank-2 spin factor J = ℝ ⊕ ℝ⁴, real dimension 5** — the Euclidean Jordan algebra of the tube realization D_IV⁵ = J + iΩ, **dim_ℝ J = dim_ℂ D = 5.** Its Peirce data w.r.t. a primitive idempotent: **spectrum {1, ½, 0}, dimensions {1, 3, 1} — COMPUTED here, not recalled** (L_{e₁} diagonalized explicitly). **This is Ambient A.** The corpus's dim-6 object (the C₂ = n_C + 1 = 6 rows) is a **different algebra never touched by this arithmetic**; K1673's V(5) = V₁₂ ⊕ ℝc₁ ⊕ ℝc₂ is the SO(3)-branching of the SO(5) vector rep — same 3+1+1 shape, different formal object, and the two are not interchanged anywhere below. ⚠ **The ambient pin itself is cited-not-banked: the Lorentz-cone tube source line is R70's still-owed item 4b — on the book-day list.** If the right ambient were the dim-6 object, the mechanism below would need rebuilding there; nothing below hides that.

# 0.5 ★ ONE EXCLUSION, THREE NAMES — Cal's sharpened question answered, and it composes with everything on file
Cal's question: *"which of the two fixed lines joins the color factor, and what forces the other OUT?"* His candidate: **the excluded line is the SO(2)/time direction, already spoken for by the conformal weight — a NAMED OBJECT, not an eigenvalue adjective.** Convergence, checked: **in the tube picture the Jordan identity e IS the cone's central axis — the time direction whose dilations the conformal weight grades. So Cal's time line = my trace direction = ℝe. SAME LINE, THREE NAMES; ONE exclusion, counted once.** The surviving slot space is the mod-ℝe class (the lemma in Section 3); the line that JOINS the color factor is the frame axis ℝ(e₁−e₂) — equivalently ℝe₁ mod identity; **Keeper's commit-selects-the-line reading now sits exactly where his amendment puts it: behind the pin, as the candidate WHY for the surviving line — and it is the strongest candidate because its 1⊕3 anatomy is banked (Section 1).** 3+2 is excluded because its extra line is *spoken for* — the arena's time label, not an internal slot — composing with the c_chir lane per §735.

# 1. The mechanism, from banked structure only
**T2543 (banked, Derived):** *matter is fermionic because a record is an idempotent* — the fermion↔record bridge is already banked, not posited here.
**K1607/K1609 (banked, Derived, 2026-08-16):** the commit's internal structure is **C = P_record ⊕ P_encode** — verbatim from the banked artifact: *"the geometry's own Peirce decomposition of a primitive idempotent"*: **P_record = a rank-1 projection (the write line, 1)** · **P_encode = the projection onto the confined 3-dim block V₁₂ (the integrity check, 3).**

> ### **⟹ A fermion slot is a committed record (T2543), and a committed record's banked anatomy is 1 ⊕ 3. The color factor "V₁₂ ⊕ line" is not selected to make 16 — it IS the commit, and it was banked as the commit EIGHT DAYS before A2 existed.**
> **Timestamp-innocence: the 1⊕3 was derived on 08-16 for Internal-C (measurement ontology) reasons. It cannot have been picked to fit a slot count requested on 08-24.**

# 2. The must-rejects come from the SAME banked sentence
K1607/K1609, verbatim: *"**Neither stage is removable** — drop the encode and the record can be silently corrupted; drop the write and there is nothing to encode."*
- **3+0** (V₁₂ alone) = drop the write → *"nothing to encode"* — **excluded by the banked minimality.**
- **1+0** (line alone) = drop the encode → *"silently corrupted"* — **excluded.**
- **3+2** (add the Peirce-0 line ℝe₂) — **C contains no Peirce-0 block by its banked definition**: the eigenvalue-0 space is the un-happened outcome, not part of the record. Verified: L_{e₁} spectrum = {1, ½, 0} with dims {1, 3, 1} — the commit is the **nonzero**-eigenvalue part, exactly as Casey's ranging shot and Keeper's table state.
**One criterion — the commit's minimal anatomy — kills both alternatives, and the criterion predates the question.** That is R87's pre-registered shape, met.

# 3. ★ The lemma that prevented collision twenty-one
Two candidate lines for the singlet slot arose independently within the hour: **ℝe₁** (Keeper/Casey — the committed frame point, nonzero-Peirce route) and **ℝ(e₁−e₂)** (mine — the traceless route, J₀ = ℝ(e₁−e₂) ⊕ V₁₂ with the primitive-idempotent sphere S(J₀) gated numerically). **They are different lines in J — and the SAME line modulo the identity:** e₁ = (e + (e₁−e₂))/2, verified. Both 4-spaces project to the same subspace of J/ℝe; both are 1+3 as SO(3)-modules. **The invariant slot space is the class in J/ℝe (≅ J₀); the two routes are two sections of it.** *Quote the invariant. Two watchers, different lines, zero records reached.*

# 4. The composition, and the count (verified)
**[(2,1) ⊕ (1,2)] ⊗ [P_record ⊕ P_encode]** = Q_L(6) + L_L(2) + u_R,d_R(6) + ν_R,e_R(2) = **16 = SM 15 + projected ν_R**
> ★ **REQUIRED SCOPE SENTENCE (Cal §739, verbatim): "[(2,1)⊕(1,2)] ⊗ [1⊕3] is NOT a product-group tensor (the rank bound forbids that reading); it is the stabilizer-equivariant decomposition at a fixed commit frame."** A referee reading ⊗ assumes exactly what this artifact's own rank bound disproves — so the sentence sits at the symbol, not in a footnote. — doublet/singlet and color assignments exact; the ν_R projection is the banked I₂ involution (T2573's mechanism).

# 5. Scoring vs the pre-registration — and the residual, named
Pre-registered bar: *DERIVED-conditional iff the 3+1 comes from a banked structure with a stated mechanism and a must-reject.* **Banked structure: K1607/K1609 + T2543, both Derived. Mechanism: stated (Section 1). Must-rejects: from the same banked sentence (Section 2). Timestamp-innocent.**
> **CLAIM: DERIVED-conditional — and the TENSOR residual is now DISCHARGED BY COMPUTATION (same day, R88), in the form Lyra specified.** Discharge condition: *show the Peirce label commutes with the K-action on the slot space.* **Computed, exact:**
> - **[L_{e₁}, so(3)_color] = 0 identically** (all three generators fixing the frame axis: commutator norm 0.000000);
> - **[L_{e₁}, coset] ≠ 0 for every generator moving the axis** (norm 0.707107 each).
> **Both branches of the spinor-SO(4) ↔ Aut(J)-SO(4) identification covered:** (i) independent copies → different tensor factors → commutation trivial, discharge immediate; (ii) diagonal (worst case) → **the Peirce label commutes exactly with the color-frame-fixed K-action, and the coset failure is FORCED, not a defect — Lyra's own impossibility (SO(4)×SO(3) ⊄ SO(5): rank 3 > 2) proves NO construction could make color a commuting group label.** The coset non-commutation is not the mechanism failing; **it is the reason color must enter as Peirce/Jordan data at all — which IS this construction.** *(F51 killed "obviously independent"; this replaces "obviously" with a computed split and a rank bound.)*
> **Lemma, discharge form: the Peirce projections commute with the maximal K-action compatible with a color label existing at all; the obstruction beyond it is the rank bound, not a defect.**
> **Remaining condition: ONE, after Cal's §739 MERGE — the commit-3↔color-3 identification IS the ambient source line: the commit's SO(3) lives in Aut(J), the color SO(3) in K, and the identification of the two pictures is the tube realization = book-day item 4b. One condition, two former names.** Lemma ACCEPTED in revised form, with Cal's ruling on the revised bar recorded: *the original bar was unsatisfiable by anything (rank 3 > 2) and a bar everything fails distinguishes nothing — what separates a corrected bar from a moved one is that the justification is a property of the GEOMETRY, not of the candidate.* Physical reading kept: coset non-commutation = **moduli motion on the commit** (which idempotent is the record), not label-mixing — fix the frame, color commutes exactly.
> **A2 FINAL STATE: DERIVED-CONDITIONAL — one scope sentence (above, in place) + ONE substantive condition = book-day item 4b.**

*— Grace, R87 A2, delivered before the 17:00 checkpoint. Gates: Peirce spectrum computed · idempotent sphere computed · count verified · both must-rejects from banked text · the family sweep passes (dim V₁₂ = n_C − rank varies with n; the selector is not generic).*
