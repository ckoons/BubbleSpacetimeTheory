# K1287 — PRE-REGISTERED BLIND prediction of the Var(N) fork: the commitment clock is *sub-Poissonian* → BST predicts a SUPPRESSED Λ-fluctuation (the disentangle branch). Mechanism + falsifier.

**Keeper, 2026-08-08 PM. This is MY frontier-lane research play (K1286 assigned Lyra+Keeper the interior-operator identification). I am committing my prediction + mechanism + falsifier BEFORE Grace/Elie's Var(N) computation returns — so a match cannot be retrofitted (the commit-the-checker's-half-blind rule, applied to myself). Routed to Cal for audit (I do NOT pass my own plays) and to Grace/Elie as the blind cold-check. Nothing banked — this is a candidate prediction, and its whole value is being falsifiable and committed in advance.**

## The interior discrete operator (identification — candidate)
D_IV⁵ is a **non-compact** Hermitian symmetric space (rank 2), and its Bergman space carries the holomorphic discrete series of SO₀(5,2). The natural "interior discrete operator" is the **number operator N̂ on the holomorphic ladder** — the K-type level operator (eigenvalues = the discrete K-type levels of the SO(5)×SO(2) decomposition; the SO(2) charge is the ladder degree). Its spectrum is *genuinely discrete* because the domain is a bounded symmetric domain → discrete series → discrete compact-Casimir spectrum. **A commitment count N = the occupation of this ladder.** This is Casey's "discrete interior = a discrete operator" made concrete: the count is the spectrum of N̂, not a random sprinkling.

## The whole fork is the Fano factor F = Var(N)/⟨N⟩ — and it is set by *which state* the committed configuration is
Three candidate states, three regimes — this is textbook and target-innocent:

| committed state | Fano factor F | branch | which physical reading |
|---|---|---|---|
| oscillator / independent-increment Poisson process | **F = 1** | **COMBINE** (= Sorkin) | commitments are a *random sprinkling in time* — Sorkin's own assumption |
| Perelomov (SU(1,1)-type) coherent state = "a point of the domain" | **F = 1 + ⟨N⟩/(2k) > 1** (super-Poissonian, negative-binomial) | **DISENTANGLE** (larger jitter) | a *coherent commitment*; k = discrete-series index, set by the domain invariants (n_C, g, rank) |
| number-squeezed / regular **clock** (a deterministic tick) or a number eigenstate | **F < 1 → 0** (sub-Poissonian; binomial F = 1−p) | **DISENTANGLE** (suppressed jitter) | Casey's "**discrete operator ticking at the Koons rate**" — a clock, the antithesis of Poisson |

## My blind prediction (committed now, before their number)
**Primary (directional): F < 1 — sub-Poissonian. BST predicts the everpresent-Λ jitter is SUPPRESSED relative to Sorkin's Poisson amplitude.** Reason: Casey's own frame is exactly right — *the commitment tick is a clock, and a clock is number-squeezed.* A Poisson process has Var = mean; a regular clock has Var → 0. If commitments advance the discrete operator N̂ at a regular (Koons) rate, the count over a fixed interval is number-squeezed, F < 1. If each tick commits only *probabilistically* (prob p), the statistics are **binomial**, F = 1−p — still sub-Poissonian, and then the observed Λ-fluctuation amplitude *measures p* (a BST observable — hold as a lead).

**Floor (high-confidence, direction-agnostic): F ≠ 1 — NOT Poisson → the DISENTANGLE branch.** Reason (curvature, and it is Casey's own Curvature Principle): **F = 1 (Poisson) is the FLAT / linear limit** — it is special to the Heisenberg oscillator. D_IV⁵ is **curved and non-compact**; the natural coherent states of a curved non-compact domain are SU(1,1)-type (super-Poissonian), and a clocked operator is squeezed (sub-Poissonian). *Either* way, you leave the flat Poisson point. "**Can't linearize curvature**" ⟹ can't get Poisson number statistics out of a curved domain. So the deviation of F from 1 is a **curvature effect set by the BST invariants** — a mechanism, not a coincidence.

## What this means for the disambiguation (if the prediction holds)
BST is then **NOT** Sorkin's Poisson causal set. It is a **deterministic discrete interior** (the clocked operator N̂) whose emergent boundary is Lorentz-invariant — exactly the BHS escape (a regular lattice can't be Lorentz-invariant *in spacetime*, but BST's discreteness is *interior/spectral*, Lorentz emergent in the boundary/tangent; consistent with Elie 5120). And it yields a **falsifiable divergence from everpresent-Λ**: a *suppressed* (F<1) fluctuation amplitude, testable against the SN Ia + CMB program (arXiv:2307.13743). BST would then *derive the number statistics Sorkin assumes* — and get a different answer.

## Falsifier (committed — no retrofitting)
- If Grace/Elie compute **F = 1 (Poisson)** at target-innocence → **my clock/curvature identification is WRONG**, and we are on the **COMBINE** branch: BST = the microphysics under Sorkin's Poisson jitter. I commit to reading it that way.
- If they compute **F > 1 (super-Poissonian)** → my *direction* is wrong but the *floor* (disentangle) holds; the committed state is the Perelomov coherent state, not a clock, and BST predicts an *enhanced* jitter. Also a clean, honest outcome.
- Only **F < 1** confirms the primary prediction.

## The honest caveat (for Cal / Lyra to sharpen)
D_IV⁵ is **rank 2**, not SU(1,1) (rank 1) — so the negative-binomial Fano factor F = 1 + ⟨N⟩/(2k) is the rank-1 template; the true rank-2 (two-mode) computation can modify the *coefficient*. The **qualitative** conclusion (curved/clocked ⟹ F ≠ 1 ⟹ disentangle) is robust to rank; the **exact** Fano factor needs the rank-2 number operator — which is precisely Grace/Elie's Var(N) computation. My prediction is: qualitative F ≠ 1 (floor, high confidence), directional F < 1 (clock, lean).

## Tier + routing
Candidate prediction, pre-registered blind, nothing banked. **Cal audits this play** (author doesn't self-pass). **Grace/Elie's Var(N) is the cold check** — they should NOT read the directional lean before computing (the floor F≠1 is fine to know; the direction must stay blind so a match is real). Λ stays Structural regardless. Nothing pushed.

— Keeper, K1287, 2026-08-08. Pre-registered blind: the interior operator is the holomorphic-ladder number operator N̂ on D_IV⁵; the Var(N) fork IS its Fano factor F. Prediction — floor: F ≠ 1 (NOT Poisson → DISENTANGLE), because Poisson is the flat limit and D_IV⁵ is curved (Casey's Curvature Principle: can't linearize curvature ⟹ can't get Poisson from a curved domain); direction: F < 1 (sub-Poissonian), because Casey's commitment tick is a CLOCK and a clock is number-squeezed ⟹ BST predicts a SUPPRESSED everpresent-Λ jitter vs Sorkin, a falsifiable divergence (arXiv:2307.13743), and the BHS escape (discrete interior + emergent-boundary Lorentz). Binomial reading: F = 1−p, Λ-amplitude measures p. Caveat: rank-2 modifies the coefficient not the qualitative F≠1. Falsifier committed: F=1 ⟹ my identification wrong, COMBINE branch. Cal audits; Grace/Elie blind-check. Nothing banked.
