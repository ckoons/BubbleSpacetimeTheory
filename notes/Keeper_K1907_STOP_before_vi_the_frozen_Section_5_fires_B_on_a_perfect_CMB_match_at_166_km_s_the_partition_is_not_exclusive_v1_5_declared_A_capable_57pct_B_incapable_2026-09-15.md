# K1907 — STOP before (vi): the frozen Section 5 fires Landing B on a *perfect* CMB match at σ_β = 166 km/s. The partition is not exclusive at this power. v1.5 declared; Quaia is A-capable (57 %), not B-capable (12 %)
**Keeper, 2026-09-15 09:11 EDT (clock, substituted). Found by the selftest of the step-(vii) instrument I wrote this morning — before any dipole exists. This is the second time today's instruments have caught the frozen text; it is what they are for.**

## 1. The finding, with the numbers from the instrument
Section 5 (v1.3, carried into v1.4): *Landing B iff χ²₃(b, b_CMB) ≥ 14.156 OR (b consistent with ZERO — χ²₃(b, 0) ≤ 7.815 while σ_β < β_CMB/2).* At Quaia's joint σ_β = 166 km/s (Elie 5762), the CMB vector itself has **χ²₃(b_CMB, 0) = (369.82/166)² = 4.96 < 7.815** — so a fitted vector *exactly equal to the CMB boost* satisfies the zero clause and **lands B: A9 fires on a perfect confirmation.** Monte Carlo (200,000 draws, isotropic Σ, `play/keeper_partB_vii_compare.py` and the script in this note's commit): **P(B | P1 true, σ = 166) = 0.56** under the frozen rule. The 1-D reasoning that made Quaia "A/B-capable" (σ < β/2 = 185) does not carry to three degrees of freedom: separating zero from the CMB at 95 % in χ²₃ needs |b_CMB|²/σ² > 7.815 ⟹ **σ_β < 132 km/s**; killing (χ²₃ ≥ 14.156) needs **σ_β < 98 km/s**. Two absolute 95 % regions cannot partition the outcomes when both hypotheses sit inside each other's region. *Over-claiming falsifiability is the same error as over-claiming derivation (K1810).* The frozen text would have committed it on the first sky vector.

## 2. The fix — v1.5, Section 5 only, declared before any dipole
- **Landing A (P1 HOLDS):** NOT Landing C, AND χ²₃(b, b_CMB) ≤ 7.815, AND **the CMB is preferred over no boost by a likelihood ratio: Δχ² ≡ χ²₃(b, 0) − χ²₃(b, b_CMB) ≥ 3.84** (odds ≥ 6.8 : 1 for the CMB over zero — two simple hypotheses, one threshold, frozen now), AND the per-bin residual test, AND A′. A is reported **"decisive"** when additionally χ²₃(b, 0) ≥ 7.815 (zero excluded on its own), otherwise "A, not decisive against zero alone" — both are A.
- **Landing B (P1 FIRES):** NOT C, AND χ²₃(b, b_CMB) ≥ 14.156, AND every hatch check passes. **The separate "consistent with zero" clause is removed:** "no boost is not one boost" is a *B* only when zero is *distinguishable* from the CMB — which is the same χ²₃(b, b_CMB) ≥ 14.156 with b ≈ 0, i.e. σ_β ≤ 98 km/s. At Quaia's depth a null result is **C**, not a fire.
- **Landing C:** everything else, including "consistent with the CMB but not preferred over zero."
- §4.4 clause 1 gains its 3-d.o.f. form beside the 1-D bar: the run is C by construction if σ_β ≥ β_CMB/√7.815 = 132 km/s *for the decisive-A reading*; Quaia at 166 is not C by construction for A (57 %) — it is for B.
**Power, from the instrument (P1 true / no boost), to be confirmed by Elie on synthetic Quaia before (vi):** σ = 166: A 0.57 (decisive 0.39), B 0.003, C 0.42 / no boost: A 0.02, B 0.12, C 0.86. σ = 132: A 0.73 / B-on-null 0.26. σ = 100: A 0.87 / B-on-null 0.58. **The honest sentence for §1: Quaia is A-capable at 57 % if P1 is true, and cannot kill P1 (B 12 % even if there is no boost at all); DESI DR1's ±50 can do both.** Calibrate both directions: "A/B-capable at 2.5σ" was over-optimistic and is retired here.

## 3. Instruments
`play/keeper_partB_landing.py --selftest-v15` PASS (eight cases: the exact CMB vector → A; the zero vector at 166 → C; 3σ-equivalent → B; hatch fail → C; consistent-but-not-preferred → C; the decisive flag). `play/keeper_partB_vii_compare.py --selftest` PASS (builds χ²₃ from Elie's vector + covariance and Grace's held target; the 1.2× norm bias does not fire the vector test). The v1.3 mode is retained and inert.

## 4. Order
(vi) does NOT run on v1.4. Cal freezes v1.5 (Section 5 as above; §1's capability sentence; §4.4 clause 1's 3-d.o.f. form; nothing else) — I audit under a re-keyed scope and verify from the file; Elie posts the synthetic landing fractions under v1.5 (P1 true; no boost) — the confirmation of §2's numbers by an independent instrument — then closes (v) and runs (vi) with the target withheld. Casey: nothing owed; you should know the capability claim moved from "A/B" to "A only" before the sky, not after.

— Keeper. K1907. Counter next: K1908.
