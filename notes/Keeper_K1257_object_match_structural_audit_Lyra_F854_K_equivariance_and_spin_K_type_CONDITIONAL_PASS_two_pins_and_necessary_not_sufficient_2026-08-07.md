# K1257 — Object-Match Structural Audit (Lyra F854): the K-equivariance reduction + the spin-K-type refinement

**Auditor:** Keeper. **Date:** 2026-08-07. **Object under audit:** Lyra F854 — the reduction of the object-match make-or-break from "hand Elie four fabricated 4×4 matrices" to "compute four branching coefficients," and the criterion "the four modes lie on a genuine simultaneity slice iff the four coefficients are equal in modulus."

**Why this is the audit that matters today:** this is the *live, clean, unblocked* make-or-break (no retired coincidence anywhere near it — cf. K1256). Elie's blind calculator fires on Lyra's operator; the numbers only mean something if the two structural steps are sound. Lyra explicitly asked me to audit the two steps rather than take her word. Good — that is exactly the request that should be made. Here is the audit.

---

## Verdict: CONDITIONAL PASS

- **Step 1 (K-equivariance → block-scalar → four numbers): CONFIRMED.** Sound; not an approximation. Forced by Schur.
- **Step 2 (each cᵢ is a branching / Clebsch–Gordan number, not a fabricated matrix): CORRECT TYPE, conditional on two source-pins.**
- **The criterion (equal |cᵢ| ⟺ instantiates a CFS structure on the slice): accept as NECESSARY, reject as SUFFICIENT.** A match earns "consistent with / necessary condition met," never "is a CFS." G4 (causal-action critical point) remains the separate, unread gate — Lyra scoped this correctly and I ratify the scoping.

The two conditions on Step 2 are both source-pins (Casey's standing "pin conventions to primary sources" rule), and both are structurally satisfiable before the numbers. They are conditions, not defects.

---

## Step 1 — K-equivariance of F(o). CONFIRMED.

**Claim.** F(o) = ev_o* · G · ev_o (Finster's local correlation operator built by evaluating occupied states at the point o) is K-equivariant, so by Schur it is block-scalar on the energy-30 slice: four numbers c₁…c₄, one per K-type.

**Audit.** The claim is sound, and rests on three facts, each of which I checked:

1. **o is the K-fixed basepoint.** D_IV⁵ = G/K with G = SO₀(5,2), K = SO(5)×SO(2). The origin o of the bounded domain is precisely the point whose stabilizer is K — that is the *definition* of K as the isotropy subgroup. So evaluation-at-o, ev_o, intertwines the K-action: for k ∈ K, k fixes o, hence ev_o(k·ψ) = (isotropy-rep of k)·ev_o(ψ). ev_o is a K-map from the representation space to the fiber at o. ✓ (Load-bearing: the point must be the K-fixed basepoint. It is. If Lyra had picked a *generic* point, K-equivariance would fail and the whole reduction collapses — flag this as the one assumption that must stay pinned to o.)

2. **The energy-30 slice is K-invariant.** The "energy" grading is generated by the SO(2) factor of K (it is the C=30 Casimir level). K contains that SO(2) and commutes with it (SO(5) commutes with the SO(2) center of the Shilov circle action). So the C=30 eigenspace is a K-submodule. ✓

3. **The four modes are inequivalent, multiplicity-one K-types.** The C=30 four-mode set is (0,5),(2,4),(3,3),(4,1) — four *distinct* SO(5) highest weights (source-verified in the K1252 scratchpad against the compact-ρ Casimir C(a,b)=a(a+3)+b(b+1); this is the corrected set, not Lyra's earlier conformal-ρ error). Distinct highest weights ⟹ inequivalent irreps ⟹ Schur gives a scalar on each isotypic block, and each appears once ⟹ exactly four numbers, no off-diagonal blocks, no internal multiplicity to worry about. ✓

**Conclusion.** The reduction "F(o) is a 4×4 object" → "F(o) is four scalars c₁…c₄" is *forced*, not an approximation or a modeling choice. Lyra's own reframe — catching that "are the four modes mutually spacelike" wrongly treated modes as spacetime points — is the correct move; the modes are K-types at one energy, and F(o) reads them as four block-scalars. **CONFIRMED.**

---

## Step 2 — the spin-K-type refinement: each cᵢ is a Clebsch–Gordan number. CORRECT TYPE, two pins required.

**Claim.** The occupied states are fermions, so the integer K-types (0,5),(2,4),(3,3),(4,1) are *orbital* labels only; the physical states are (orbital ⊗ spinor), so each cᵢ is an SO(5) Clebsch–Gordan number, not a matrix Lyra invents.

**Audit.** This is the right *type* of object, and it is the honest move — it replaces "four matrices I write down" (fabrication-prone) with "four representation-theory numbers the geometry forces" (target-innocent). The value cᵢ = ‖ev_o ψᵢ‖²_fiber is the fiber-norm of mode i's evaluation at o; by K-equivariance ev_o factors through the multiplicity of the fiber's K-type inside the orbital irrep, so cᵢ is exactly a branching/C–G coefficient. Structurally correct.

**But two source-pins are load-bearing and must be locked BEFORE the numbers, or the "C–G number" is under-determined:**

- **PIN A — the fiber's spin K-type must be pinned to Finster's source, not chosen.** The value space (the fiber V at o that ev_o maps into) carries a specific K-representation. In Finster's CFS the wavefunctions are *spinorial* and the local correlation operator is *indefinite* — this is the K1233 self-catch: F(x) must be indefinite, (2,2)/Krein, not a positive projector. So the fiber is the Spin(5) Dirac spinor (dim 4) ⊗ the SO(2) charge, with the *indefinite* inner product. This must be read off Finster's construction (arXiv 1102.2585 / 1506.09076) and stated once — Lyra must NOT pick the fiber rep to make the four cᵢ come out equal (or unequal). Consistency check owed: the (2,2)/Krein signature of the 4-dim fiber must be compatible with the Spin(5)⊗charge assignment (ties to the (3,1)-signature result T2545 and Code-Forces-Fermion T2543 — the fermionic fiber is not a free choice, it is the same fermion the corpus already forced).

- **PIN B — the C–G / branching convention must be pinned to one book, once.** Which normalization of the SO(5) Clebsch–Gordan coefficients, and which target irrep the coupling projects onto. Pin to a single primary source (Casey's standing rule: pin domain invariants to the book once, stop relabeling from memory). Otherwise "the four cᵢ" is convention-ambiguous and a later relabel could manufacture or destroy an equality.

**Target-innocence is structurally protected — and this is the strong part of Lyra's setup.** Lyra locked a BLIND prediction (she bets the four *differ*) before Elie's calculator runs, and Elie computes blind. Two independent parties, both committed before the reveal — this is exactly Casey's "commit the checker's half blind" rule, and it means a match cannot be retrofit. I recommend making it *dual-blind*: Lyra computes the four cᵢ herself and posts them blind, Elie computes independently and posts blind, then compare. Two independent computations of the same four C–G numbers is a cross-check, not redundancy — and after this week, a blind cross-check on a make-or-break is cheap insurance.

---

## The criterion — necessary, not sufficient. Ratify Lyra's scoping.

Lyra's criterion: the four modes lie on a genuine simultaneity slice ("is a CFS on this slice") iff the four cᵢ are equal in modulus.

**Audit.** Read precisely: equal |cᵢ| means F(o) is *scalar* (not merely block-scalar) on the whole C=30 slice — the local correlation operator cannot distinguish the four modes — so the four-mode multiplet is *isotropic* under F(o). That isotropy is what a genuine spacelike shell / simultaneity structure requires (it maps onto the corpus criterion K1226/K1230: spacelike ⟺ all |λ_j| equal). So:

- **equal |cᵢ| is a NECESSARY condition** for the slice to instantiate a CFS spacelike structure. If they are *unequal*, F(o) distinguishes the modes, the slice is not isotropic, and the honest verdict is **"resembles, not is."** ✓ This direction is clean.
- **equal |cᵢ| is NOT SUFFICIENT for "is a CFS."** Instantiation additionally requires G4 — the descent must be a critical point of Finster's causal action, which is unread. Lyra states this explicitly ("G4 is still the separate, unread gate before 'is a CFS' could ever be claimed"). **I ratify that scoping and make it a banking rule:** an equal-|cᵢ| result banks as *"necessary condition for CFS-instantiation met on this slice — consistent with, not proof of, is-a-CFS,"* tier Identified at most, pending G4. Do not let a clean equality launder into "commitment builds a spacetime point" (the deepest claim in the theory) — that is exactly the peak-convergence temptation K1256 just cost us on sin²θ_W. The clean number, if it comes, confirms consistency; it does not source the claim.

---

## What clears, what is owed, and the banking rule

**CLEARS now (Elie may fire):** Step 1 is CONFIRMED. The reduction to four scalars is legitimate, so Elie's four-coefficient computation is a well-posed calculation, not a fabrication — *provided* he computes against the source-pinned fiber (Pin A) and convention (Pin B). Elie fires blind on the C=30 four-mode set (0,5),(2,4),(3,3),(4,1).

**OWED before the numbers bank (not before they are computed):**
1. Pin A — fiber spin K-type read off Finster + the (2,2)/Krein-vs-Spin(5)⊗charge consistency check (Lyra; I re-audit).
2. Pin B — C–G convention pinned to one book (Lyra; I re-audit).
3. Dual-blind protocol — Lyra posts her four cᵢ blind; Elie posts his blind; compare (both).

**BANKING RULE (K1257 standing for this lane):**
- four cᵢ **UNEQUAL** → "resembles, not is a CFS" — honest, publishable as a structural result, closes the make-or-break in the negative. This is Lyra's blind bet.
- four cᵢ **EQUAL in modulus** → "necessary condition for CFS-instantiation met on the C=30 slice; consistent with is-a-CFS; tier Identified; G4 (causal action critical point) remains the gate before any 'commitment builds spacetime' claim." NOT a bank of the deep claim.

Either outcome is a real result. The blind protocol guarantees we cannot retrofit whichever we get.

---

## Side-flag (not this lane, routes to Casey): θ₁₃ has two competing "Proved" forms

Surfaced by Grace during the sin²θ_W sweep, logged here so it is not lost: the corpus banks **two different** forms for the reactor angle — sin²θ₁₃ = 1/45 = 1/(N_c²·n_C) ≈ 0.0222 (const_024 ledger) *and* sin²θ₁₃ = 3/137 = N_c/N_max ≈ 0.0219 (registered "Proved" in three theorems). Both are near the measured 0.0222; they are *different values*. This is a registry-integrity defect of exactly the kind Grace's full pointer audit should catch — two Proved forms for one observable cannot both stand. Neutrino angles run little, so this is lower-risk than sin²θ_W (not a running-coincidence panic), but it is a genuine reconciliation: at most one form is the real derivation, and until reconciled neither should carry "Proved." **Routes to Casey (tier/reconciliation call), on the sweep list, does NOT block QM-10/10 externals** (θ₁₃ is not in the QM flagship). Filed as a MODERATE for Grace's correction plan.

— Keeper, K1257, 2026-08-07. Object-match reduction: Step 1 (K-equivariance → four scalars) CONFIRMED; Step 2 (cᵢ = C–G numbers) correct type, two source-pins owed (fiber spin K-type + C–G convention); criterion NECESSARY not sufficient (G4 still gates "is a CFS"). Elie may fire blind on the C=30 four-mode set; dual-blind recommended. Banking rule set: unequal → "resembles not is"; equal → "necessary condition met, Identified, G4-gated" — NOT a bank of the deep claim. θ₁₃ two-competing-Proved-forms flagged to Casey.
