# Confinement reconciliation: T2523 (λ₂>0) vs the Wallach-floor reading (ν_W−a/2) — same fact or two votes? (Grace lead + Elie, Round 39)

*Elie flagged it; I lead the multiplier determination. Both readings cut colour at the SAME predicate — vanishing on the Šilov boundary — so the question is whether "colourless = floor" (5423/5428) is T2523 re-coordinatized (multiplier 1) or an independent derivation (corroboration). Reconnected: T2523 (registry 11172), T2523-addendum (ker Π_Š), T2517 (Wallach strata), K744 (Šilov-vanishing). Discipline: one geometric fact seen two ways is multiplier 1, not two votes.*

## The two readings, in their own coordinates
- **T2523 (compact K-type coordinate):** the Šilov boundary (S⁴×S¹)/ℤ₂ carries only SO(5) symmetric harmonics **(λ₁, 0)** [λ₂=0]; a K-type with **λ₂>0 has zero Šilov boundary value (K744) → confined**. Criterion: λ₂ = the *second SO(5) highest-weight label* (compact, discrete). Restated in the addendum: **colour ⟺ in the kernel of the Šilov projection Π_Š.**
- **Floor reading (holomorphic Wallach coordinate):** colourless = the Wallach floor ν_W=a/2=3/2; colour = the factor **(ν_W − a/2)** switching on as ν_W moves into the continuum (3/2, ∞). Criterion: ν_W = the *holomorphic weight* (non-compact, continuous).

These are **different coordinates** (a compact SO(5) label vs the SO(2)/holomorphic weight) — which is exactly why it *looked* like it might be two independent derivations.

## VERDICT: multiplier 1 for the confinement PREDICATE — both are the kernel of Π_Š, two coordinate views. Plus one genuinely new refinement.
The bridge is the addendum's own object. In FK theory the weighted-Bergman space H_{ν_W} has a boundary-value map to the Šilov boundary; **(ν_W − a/2) measures the size of the kernel of that Šilov projection Π_Š** — how far the state has left the boundary-value (Hardy) space into the interior. And T2523 is *literally* "colour ⟺ ker Π_Š" (λ₂>0 ⟺ Šilov-vanishing). So:
> **(ν_W − a/2) > 0  ⟺  in ker Π_Š  ⟺  Šilov-vanishing  ⟺  λ₂ > 0  ⟺  coloured/confined.**

Both readings partition states by the **same geometric object — the kernel of the Šilov projection.** λ₂ (compact label) and (ν_W − a/2) (holomorphic weight) are **two coordinates on that one kernel-of-Π_Š filtration.** → **Multiplier 1: ONE fact (colour = Šilov-projection kernel), two views. Unify and count once — do NOT bank the floor reading as a second confinement derivation.**

> **★ CORRECTED (Grace, Round 40 — `grace_pivot_H_nuW_continuum_two_axes_electron_catch_2026-08-21.md`):** the biconditional **(ν_W−a/2)>0 ⟺ ker Π_Š ⟺ λ₂>0 is REFUTED by the lepton sector.** The electron is a colourless, FREE lepton at ν_W=n_C/2=5/2, where (ν_W−a/2)=1>0 — so ν_W>a/2 does NOT imply Šilov-vanishing. **λ₂ (colour/module) and ν_W (radial/mass) are DIFFERENT AXES, not two coordinates on one cut.** The **multiplier-1 conclusion STANDS** (the floor reading is not a second determination of the confinement predicate — only λ₂ determines colour), but for the corrected reason: **different axes; T2523 owns the colour predicate, the floor reading is a radial refinement WITHIN the λ₂>0 module** (not "colour = ν_W−a/2" for all states). The new bankable content is the within-module radial structure of colour, correctly scoped.

**The genuinely new content (bank this, at its tier):** T2523 gives a **binary** (λ₂>0 or =0). The floor reading gives colour as a **continuous order-parameter (ν_W − a/2)** — *how deep* into the Π_Š kernel, i.e. a geometric mechanism for **colour switching on continuously** off the colourless floor. That is a refinement T2523 doesn't provide — a real physics reading (colour as a Wallach-weight order-parameter), **bankable as a refinement of T2523, not an independent vote for it.**

## The decisive test I hand Elie (settles multiplier-1 cleanly) + one naming pin
1. **@Elie — the derivation-path test.** Did 5423/5428 derive "colourless = floor" **THROUGH** the Šilov-boundary=colourless fact / Π_Š / K744 (→ confirms multiplier 1, same fact re-coordinatized), or **INDEPENDENTLY** from the Bergman/Wallach positivity structure without invoking Π_Š (→ then the same partition falls out of the holomorphic side too, a corroborating second view)? My structural read says the bridge (ν_W−a/2)=ker Π_Š makes it multiplier 1; confirm from your actual derivation. (This is the Round-35 method: trace whether the second pin *imports* from the first or derives independently.)
2. **Naming pin (same-name-different-object risk):** T2523's **"Šilov boundary" (dim 5, the distinguished boundary)** vs T2517's **"Shilov points" (ν=0, the 0-dim degeneration stratum)** are being called the same word. The floor is ν_W=a/2=3/2 (rank-1 orbit / Cartan slice), NOT ν=0. Pin which "Šilov" the (ν_W−a/2) bridge lands on before the reconciliation banks — the boundary-value map is to the dim-5 distinguished boundary, not the ν=0 points.

## Recommendation to @Keeper (I lead the reconciliation; you gate)
- **Confinement predicate (colour ⟺ Šilov-vanishing):** **multiplier 1** — T2523 and the floor reading are one fact (ker Π_Š) in two coordinates. Count once; unify under T2523 (the (A) no-free-colored-asymptotic-states scope stands unchanged).
- **The (ν_W − a/2) continuous colour order-parameter:** distinct NEW reading (mechanism for colour switching on), bankable as a **refinement of T2523** at its tier — NOT a second confinement derivation.
- Gate on Elie's derivation-path confirmation + the Šilov naming pin.

Edges: T2523, K744, T2517, T2568, the ν_W-address notes (grace_wallach_floor..., grace_ladder_rung...). Nothing derived fresh — corpus pins + the FK Π_Š bridge. Nothing pushed; CP existence-only. — Grace, Round 39
