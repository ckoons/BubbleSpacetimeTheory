# K1308 — F85-via-Bernstein: the route REDUCES to ONE checkable condition. F85's monotonicity is FORCED **iff** the boundary-condensate coupling is a positive-weight diagonal self-overlap ⟨ψ_d|e^{−(geodesic)·H_B}|ψ_d⟩ (same structure as F778), NOT an off-diagonal cross-shelf matrix element (signed weights). Corpus favors the diagonal reading (F826, K768) → route likely real. Verify on the real operator; DO NOT bank F85.

**Keeper (research probe, timeboxed, de-risking the F85 route before the team invests), 2026-08-09. I set out to test whether the DE-sign completely-monotone machinery (F778) transfers to F85's "coupling monotonic in depth." Result: the transfer reduces to a single, sharp, verifiable condition — the positivity of the spectral weights — and the corpus makes that condition likely true. I state the reduction and the gate; I do NOT declare F85 promoted. Cal cold-reads. This is a reduction + assessment, not a ruling.**

## The exact fact I'm trying to transfer (from F778)
The DE bleed is completely monotone for ONE reason, and it's precise:
> ρ_Λ(τ) = ⟨ψ₀| e^{−τ H_B} |ψ₀⟩ = Σ_k c_k e^{−λ_k τ}, with **c_k = |⟨k|ψ₀⟩|² ≥ 0**. Positive weights ⟹ Laplace transform of a positive discrete measure ⟹ completely monotone (Bernstein) ⟹ the effective rate r(τ)=⟨λ⟩_τ decreases (dr/dτ = −Var_τ(λ) ≤ 0) ⟹ **monotone, no reversals.**

The load-bearing step is NOT the exponential form (any matrix element has that) — it is **c_k ≥ 0**, which holds because ψ₀ appears on BOTH sides (a diagonal self-overlap → weights are squared magnitudes).

## The reduction — F85 monotonicity ⟺ positive weights ⟺ diagonal coupling
F85's promotion needs: the boundary-condensate coupling C is **monotone in geodesic depth** (no reversals). Write the coupling spectrally in the same H_B eigenbasis:
> C(s) = Σ_k w_k e^{−λ_k s}, s = geodesic distance (F826: up-ladder weight = exp(−geodesic distance)).
- **IF C is a DIAGONAL self-overlap** C(s) = ⟨ψ_c| e^{−s H_B} |ψ_c⟩, then **w_k = |⟨k|ψ_c⟩|² ≥ 0** → C(s) completely monotone → strictly monotone in s → **monotone in depth (F85's requirement) is FORCED**, by the identical Bernstein argument as F778. (Direction: C(s) decreases in distance-from-boundary s; reparametrized as outward depth d, C increases — monotone either way, which is all F85 needs. Concavity is not required, only monotonicity, so no inflection worry.)
- **IF C is an OFF-DIAGONAL cross-shelf matrix element** C(s) = ⟨ψ_a| e^{−s H_B} |ψ_b⟩ with a≠b, then **w_k = ⟨a|k⟩⟨k|b⟩ can be SIGNED** → NOT completely monotone → monotonicity NOT forced (can oscillate/reverse). The route FAILS here and F85 needs a direct attack.

**So the entire F85-via-Bernstein route reduces to one binary condition: is the condensate coupling a diagonal self-overlap (positive weights) or an off-diagonal cross-shelf element (signed weights)?**

## Assessment — the corpus favors the diagonal (positive-weight) reading
F85 is about the up-tower's OWN depth-ordering (each generation's condensate coupling setting its own seat/mass), which is a **diagonal** object — a shelf's coupling to its own depth-shifted self — not a cross-shelf mixing element (the off-diagonal objects are the MIXING, handled separately in Lane A). Supporting corpus:
- **F826:** the up-ladder weight is exp(−geodesic distance) — a single positive exponential, the extreme (rank-1) case of a completely monotone function.
- **K768:** "flavor closes as **rank-1 condensate** + tier-2 corrections" — a rank-1 condensate is ONE positive weight (trivially CM); if the tier-2 corrections are additional squared-overlap modes, the sum stays positive-weight → CM.
- **F778 precedent:** the boundary zero-point ψ₀ decomposed in the SAME H_B eigenbasis already gives positive weights for the diagonal object; the condensate is the same boundary object one shelf out.
**⟹ The diagonal/positive-weight reading is strongly favored, so the route very likely closes F85 with the tool already in hand — no new machinery, the same Bernstein theorem as the DE sign.**

## The gate (what Lyra/Elie must verify on the REAL operator — do not skip)
Confirm, on the actual FK/discrete-series operator, that the boundary-condensate coupling is the **diagonal self-overlap** ⟨ψ_c|e^{−sH_B}|ψ_c⟩ (weights = squared overlaps, manifestly ≥ 0), and NOT an off-diagonal cross-shelf element with signed weights. Concretely: exhibit the w_k and check w_k ≥ 0 for all k. If yes → F85 monotonicity is a theorem (Bernstein), promote per the K1307 condition. If any w_k < 0 → the route fails cleanly and F85 stays the direct open gate.

## Discipline (I am watching myself here — Cal #27, shared-structure ≠ shared-mechanism)
- **NOT banked:** I have NOT promoted F85. I have reduced the route to one condition and assessed it likely-positive from the corpus. The operator-level check is required and un-skipped.
- **The honest failure mode is named** (off-diagonal signed weights) — I did not paper over it to force the isomorphism. If the coupling is cross-shelf, the transfer genuinely fails.
- **Not a false isomorphism:** the transfer is not "both are monotone-ish" hand-waving — it is the *same* Bernstein theorem applied to the *same* operator H_B, contingent on the *same* positivity fact (squared-overlap weights). That is shared MECHANISM, not just shared structure — IF the diagonal condition holds. The "if" is the whole content, and it's checkable.
- **Value of the probe:** it de-risks the F85 route before the team invests a session, and hands them the exact scalar check (w_k ≥ 0?) that decides it.

## ADDENDUM (2026-08-09, Casey's framing folded in — now load-bearing after Elie's test)
Casey read the squared-overlap structure and asked whether "made a fixed value from combined pieces" means each direction is taken *separately*. It does — and it is the cleaner physical statement of this gate. The positivity c_k = |⟨k|ψ⟩|² ≥ 0 holds **exactly when the eigen-directions are orthogonal / non-interfering** (diagonalized H_B, Parseval sum of independent contributions). Cross-terms — directions that are NOT taken separately — are precisely the signed off-diagonal weights that break complete monotonicity. So the gate is, physically: **is each mode taken separately (diagonal → CM → forced), or do the modes interfere (off-diagonal → signed → can reverse)?** Elie's round confirmed this is the real fork: his natural coupling-vs-depth model is monotone but not CM — i.e., a case where the directions do NOT cleanly separate. Two refinements Casey's framing needs: (i) the "directions" are the *eigenmodes* (SO(5) harmonics {1,5,14,30}), not the raw spatial axes one-for-one; on the Shilov product S⁴×S¹ the decomposition factorizes across the two factors (Casimir eigenvalues add, overlaps multiply), while inside S⁴ the four angles bundle into SO(5) harmonics — so it separates into the geometry's irreducible factors + harmonic modes, not naive coordinate axes. (ii) The positivity IS the Born rule (|⟨k|ψ⟩|² = probability; T2401 Born=Bergman) — so the same positivity that makes the theory quantum-mechanical is what would force the monotonicity. **Per K1309: F85 does not need CM (only monotonicity, forced directly by Elie's kernel route), so this gate is now a non-load-bearing structural question — but Casey's "separate vs interfering" is the right way to state it if we revisit CM.**

— Keeper, K1308, 2026-08-09. F85-via-Bernstein transfer REDUCED to one condition: F85 monotonicity is FORCED (same Bernstein theorem as F778's DE sign) **iff** the condensate coupling is a positive-weight diagonal self-overlap ⟨ψ_c|e^{−sH_B}|ψ_c⟩; it FAILS iff off-diagonal cross-shelf (signed weights). Corpus (F826 exp(−geodesic), K768 rank-1 condensate) favors the diagonal/positive reading → route likely real, closes F85 with tool in hand. GATE: Lyra/Elie exhibit the w_k on the real operator, check w_k≥0. NOT banked — reduction + assessment only. Cal cold-reads. Nothing pushed.
