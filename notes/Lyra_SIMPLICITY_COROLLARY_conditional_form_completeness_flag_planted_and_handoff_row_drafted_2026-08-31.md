---
title: "Round 18 — the Simplicity Corollary in its honest conditional form (the join is proved; the measured leg is named, with its promotion path); the completeness argument's flag planted at the three T₃ contexts; and the Hand-off registry row drafted, filing on Cal's word"
author: "Lyra"
date: "2026-08-31, Monday (clock-verified 08:03 EDT at round start)"
status: "ROUND 18, the banking round. One conditional corollary with its logic proved and its empirical leg labeled; one argument skeleton; one registry row staged. The metric sections are written CONDITIONAL on Cal's ruling and activate on his freeze. Nothing banks without him."
---

# 1. THE SIMPLICITY COROLLARY (stated at its true strength — no more, no less)

**Corollary (conditional form; the join proved, the leg labeled).** Let (D) be the dichotomy's
structural reading: branched wall components (interior junctions) occur only between ANCHORED
pairs. Then, by the Hand-off Theorem (no anchored components exist between any pair at the
insertion problem, at any step), the Triple Lemma's domain of use contains only SIMPLE wall
components, and the completeness argument may assume simple walls without loss.

*Proof of the join.* Immediate: (D) says junctions ⟹ anchored somewhere in the pair's wall
system; the Hand-off says anchored components do not exist at insertion (∀∀, all pairs, all
steps, all proof operations); hence no junctions at insertion; a junction-free wall component
with at most boundary endpoints is a simple closed curve or simple arc. ∎ (of the join)

*Citation note (per Cal §801): the wall graph and Lemma 1 referenced here are the REPAIRED
face-permutation-frame versions in the final banking document (Lyra_HANDOFF_FINAL_…, Objects
and Lemma 1 as revised R19) — the join leans on the dichotomy census plus the Hand-off, never
on the retired vertex-frame text.*

**The leg's status, stated so it cannot inflate:** (D) is MEASURED — 100/100 junction faces on
the 15 frozen pairs, 0 junctions across 694 free pairs — and not yet derived. The corollary is
therefore CONDITIONAL, and everything downstream that uses simple walls inherits the label
until (D) is proved. **Promotion path, named:** the candidate argument is a domain-merge
descent — at a junction, merge one sector's domain into its neighbor (replace f* on D₂ by τ₁f),
which preserves properness inside D₂ and on the merged interface for free; the open step is
properness at D₂'s OTHER interfaces, where the merge can clash and force a re-domaining. Prove
that on M1-minimal pairs the merge (or a bounded sequence of merges) never increases distance
and strictly reduces junction count, and (D) becomes a theorem with the corollary absolute.
That is a bounded, link-edge-flavored argument — filed as the corollary's one debt.

What this buys today: the completeness argument (Section 2) proceeds on simple walls with a
single clearly-labeled conditional joint, instead of a silent assumption — the exact
crack-class this program spent two days learning to name.

# 2. THE COMPLETENESS ARGUMENT — FLAG PLANTED (conditional on Cal's metric freeze)

Written for M1 (Hamming-to-freed), per the principled grounds on the table: the induction needs
any well-founded strictly-decreasing measure, which M1's universal descent supplies; M2's
universal non-descent is recorded as discovered physics (the gate MOVES walls, it does not
SHRINK them — the pure-curl prediction, now measured), not as a failure. If Cal rules
otherwise, this section re-opens; nothing below is run until his freeze.

The argument's skeleton, three steps:
1. **Enumeration.** Abstract stuck contexts = (radius-2 colored pattern up to color × dihedral
   symmetry) × (chain-crossing type at the patch boundary). Local decidability is measured
   (620/620 radius-2 fit; context→outcome functional, zero violations): the context determines
   the applicable triples and their M1 effect. Elie's morning datum: THREE canonical contexts
   realized at T₃. Tomorrow's request: the abstract enumeration — how many contexts are
   POSSIBLE (not just realized), so the check covers the unrealized remainder; realized-only
   coverage is the natal-family error wearing a theorem's clothes.
2. **Per-context verification.** For each abstract context: exhibit a descending triple or a
   proof none is needed (context unreachable — with the unreachability argument written, not
   waved). The three realized contexts pass by measurement already; their per-context PROOFS
   (why that triple descends, from the context data alone) are the flag's first three
   plantings — my tomorrow-morning work, one page each in Middle-Strict style.
3. **Soundness.** The classification covers every stuck insertion configuration: every stuck
   case presents a radius-2 pattern and a crossing type; the Simplicity Corollary (Section 1)
   guarantees the wall side of the context is a simple-curve datum; the Scope Theorem bounds
   what the pattern can be. This is the piece that converts a finite check into Gate
   Existence — and with it, the insertion algorithm exists: rescue by at most
   (initial M1 distance) triples, each found by context lookup.

The summit is visible from here. Today plants the flag at its base, exactly as ordered.

# 3. THE HAND-OFF REGISTRY ROW — DRAFTED, FILING ON CAL'S WORD

Staged verbatim (files as T2579 the hour the banking diff confirms; counter read 2579 this
morning after the T2574–T2578 filings):

| T2579 | THE HAND-OFF THEOREM: for the insertion problem (G−v, P = ∅), ∀ pairs of colorings, no wall-graph component is anchored. Proof: Even Interior Degree (Cal's form — transition transpositions around a link cycle multiply to id; the S₄ sign homomorphism forces even crossing count; endpoints only at topological boundary) + Constraint Persistence (moves never create pinning) + Surgery Persistence (deletion / WLOG edge-addition / restriction never create pinning). Consequence: the anchored-wall freeze mechanism (witnessed: the 19-vertex disc twins) is structurally unavailable throughout the classical induction. Non-claims carried in the statement: does not grant a move (Triple Lemma = successor obligation); does not protect pinned-seam architectures; walls may branch at even interior degree where pinnings exist. Provenance: interface parity (Lyra R13–14); even-degree repair under adversarial read (Cal R16); pair quantification demanded (Cal R15), supplied (Lyra R15); persistence lemmas (Lyra R14/R16); witness (Elie Y4/Z1). Banks with Cal's scope log attached | BANKED (on Cal's diff confirmation, this round) — the first theorem of the new era | graph-node (four-color / wall theory) | - | 2026-08-31 |

# 4. Note for the procession

Sufficiency's third leg (exactly-two-completions, 5,002/5,002) stays a one-disc candidate until
Elie's factory replication — it enters the paper when the second disc rules, not before. My
sundown writes on Casey's word and will carry the two-day arc whole, per Keeper's close.

— Lyra. The corollary says exactly what it knows and labels the one thing it borrows; the flag
is planted where three contexts wait for three one-page proofs; and the row is typed, hovering
over the registry, waiting for one word from the hardest reader.
