# F708 — Deliverable A (Lyra+Elie, occupancy fulcrum): the **interior identification** — does the Toeplitz mass operator spectral-decompose on the Jordan frame, one generation per primitive idempotent? **The mechanism is clean and it reduces Deliverable A to ONE bounded, target-innocent condition: the mass operator T_φ commutes with the Peirce frame projections IFF its symbol φ is a Jordan-spectral function (a function of the generic norm / trace only). If φ is spectral, T_φ's interior spectral decomposition IS the r = 2 primitive idempotents {c₁, c₂}, one generation-mode each — the interior identification "generation = idempotent mode" DERIVED target-innocently, with the masses falling out as φ evaluated at the two spectral positions and the electron never placed by hand.** The interior COUNT (r = 2) was already rigorous (Cal/K947, EJA rank cap); this closes the IDENTIFICATION *modulo* the symbol condition. **Not claimed closed:** whether the sourced condensate symbol φ (F583) is in fact Jordan-spectral is the one remaining check (Grace's sourcing lane). **Tier: interior identification DERIVED-IF-φ-spectral; the "φ spectral" check is the bounded open piece. Operator-anchored, K880 guard held (electron falls out, not fed in).**

**Lyra, Mon 2026-07-27 12:12 EDT (full day). Deliverable A on the sourced structure (F681 the Toeplitz operator, T2511 the Peirce frame, Bergman K∝N^{−(n+1)} the generic norm) — NOT reconstructed from memory. The count tension resolves: 2 interior idempotent-modes + 1 boundary (Deliverable B) = rank+1 = 3. This derives the interior half at its honest tier and names the single condition that closes it.**

## The sourced structure (GIVEN — from the ledger + corpus, not memory)
- **The mass operator (F681):** Ô = T_φ, the Toeplitz condensate operator on H²(D_IV⁵) — multiply by the VEV profile φ, project back to holomorphic. Multiplication (full-rank), not a rank-1 projector; its restriction to the generation subspace is Hermitian. The symbol φ is the F583 neutral SU(2)_L-singlet condensate (sourced, not tuned — F681 O2).
- **The Jordan frame (T2511):** the rank-2 spin factor V = ℝe₀ ⊕ ℝ⁴ (dim n_C = 5) has primitive idempotents **c₁, c₂ = (e₀ ± v)/2** and Peirce decomposition **V = ℝc₁ ⊕ ℝc₂ ⊕ V₁₂**, with the idempotent frame = the rank-2 causal core and V₁₂ = the 3D color off-diagonal (dim N_c = n_C − rank). The **r = 2 idempotents are the frame** (= the 2 discrete Wallach points, K947).
- **The generic norm (Bergman):** K(z,z) ∝ N(z,z)^{−(n+1)}, N = the generic norm (the rank-2 Jordan determinant). The invariant radial coordinate is N; the frame-stabilizer subgroup acts trivially on spectral (N, trace) data.

## The mechanism (the target-innocent route)
By the EJA spectral theorem, every element of the rank-2 spin factor has a spectral decomposition x = λ₁(x) c₁ + λ₂(x) c₂ on the frame — exactly **r = 2** spectral eigenvalues. A **Jordan-spectral function** is one that depends on x only through {λ₁, λ₂} (equivalently, through the generic norm N = λ₁λ₂ and trace τ = λ₁+λ₂) — i.e., invariant under the frame-stabilizer.

**Claim (the reduction):** the Toeplitz operator T_φ commutes with the Peirce frame projections {P_{c₁}, P_{c₂}} **iff the symbol φ is a Jordan-spectral function.** Consequence, if φ is spectral:
- T_φ is block-diagonal on the Peirce decomposition; on the interior (idempotent-frame) part it has exactly **r = 2 eigenmodes, one supported on each primitive idempotent c₁, c₂** — the interior seats, one generation each. **This is the interior identification, derived.**
- The two eigenvalues are **φ evaluated at the two spectral positions** (the masses of the two interior generations) — they come out of the symbol's own structure; the *ordering* (which is heavier) is fixed by φ, not by the observed masses. So the mass **assignment is structural, and the lightest state — the electron — is NOT placed; it emerges as the boundary "+1" (the continuum ground mode, Deliverable B), falling out at the bottom** (K880 guard held).

Why the symbol is plausibly spectral: φ (F583) is the neutral SU(2)_L-**singlet**, SO(5)-invariant condensate — a *zonal/radial* profile, hence naturally a function of the invariant generic norm N (the Bergman kernel itself is K ∝ N^{−(n+1)}, a function of N). A condensate built from the invariant structure is a spectral function → commutes with the frame → interior modes = the idempotents. **But this must be checked on the sourced φ, not assumed.**

## What this closes, and the ONE bounded piece it does not
- **CLOSED (given the condition):** the interior COUNT (r = 2, Cal/K947) *plus* the interior IDENTIFICATION "generation = idempotent mode" — provided φ is Jordan-spectral. Target-innocent: r from the EJA rank (not the observed 3), masses from φ (not fed in), electron falls out at the boundary (K880 guard).
- **NOT CLOSED — the single check:** **is the sourced condensate symbol φ (F583) a Jordan-spectral function** (a function of N and τ only, with no V₁₂/off-diagonal dependence that would mix the frame)? If yes → interior identification DERIVED. If φ carries a genuine V₁₂ (color off-diagonal) component that does not commute with the frame → the interior modes are not the clean idempotents and the identification fails. **This is the bounded, checkable condition Deliverable A reduces to** — and it is exactly F681's O2 (the symbol, sourced) read as a commutation test.

## Target-innocence audit (against the four smuggle-warnings, K945)
1. "2 discrete + continuum = 3 assumes one mode each" — handled: the interior 2 come from the r = 2 idempotents (EJA rank, structural), the +1 is Deliverable B; I do **not** assume each Wallach piece hosts one generation, I derive the interior 2 from the frame.
2. "{5/2,3/2,0} = ρ-vector; 0 is not a ρ-component (K876)" — I do **not** use the {5/2,3/2,0} assignment; the interior modes are the idempotents c₁, c₂, and the ν-labels are not the anchor.
3. "electron k=1 fit to m_e (K880)" — **held:** the electron is not placed; it is the boundary continuum ground mode (Deliverable B), emerging at the bottom as a check.
4. "singleton infinite tower doesn't cap (F338)" — avoided: the cap is the **EJA rank** (r = 2, intrinsic), not a singleton truncation.

## Tiers / handoffs
- **@Elie (the toy — Deliverable A verification)** — the checkable rep-theory fact: on a rank-2 spin factor with frame {c₁, c₂}, form T_φ for a **spectral** symbol φ = f(N, τ) and confirm (i) T_φ commutes with the Peirce projections P_{c₁}, P_{c₂}, and (ii) its interior spectral decomposition has exactly r = 2 idempotent eigenmodes, one per c_i, with eigenvalues f evaluated at the two spectral positions. Then the **contrapositive check**: a symbol with a genuine V₁₂ component does NOT commute (mixes the frame). This is the toy that turns the mechanism into a verified fact. (Source the Peirce calculus from FK Ch. VI — do not reconstruct.)
- **@Grace (the one open check)** — Deliverable A reduces to: **is the sourced condensate φ (F583) a Jordan-spectral function** (a function of the generic norm N and trace τ only, no V₁₂ off-diagonal dependence)? Your F583 sourcing lane answers exactly this. If φ is spectral → the interior identification is derived; if φ carries V₁₂ → flag it, the interior modes aren't the clean idempotents. This is F681's O2 as a commutation test.
- **@Keeper** — Deliverable A: the interior IDENTIFICATION is **derived modulo one bounded, target-innocent condition** (φ Jordan-spectral), on the sourced structure (F681 + T2511 + generic norm). Not claimed closed — the "φ spectral" check (Grace) is the remaining piece, and it is a *check*, not an open search. Guards held (operator-anchored, electron falls out, r from EJA rank not observed count). With Deliverable B (Cal, the boundary +1), if both land: generations = r+1 = 3 target-innocently → premise ELIMINATED. Until the φ-check + B land: REDUCED.
- **@Casey** — Deliverable A, the interior half of "why exactly three generations." The domain is a rank-2 Jordan algebra — think of it as having exactly two natural "pure directions" (idempotents), a structural fact, not a fitted count. The mass operator is multiplication by the condensate. Here's the clean part: **if** the condensate is a "spectral" profile — one that only sees the domain's two natural invariants (its size and its trace), which is exactly what a neutral, symmetric condensate should be — then the mass operator automatically lines up with those two pure directions and hands you *two* interior generation-modes, one per direction, with the masses just being the condensate's value in each direction and the *lightest* one (the electron) falling out at the edge as the third, boundary mode — never placed there by us. So the whole interior half reduces to one honest yes/no question we can check: is the actual condensate a spectral profile, or does it secretly lean on the "color" direction? If spectral, the interior is derived; if not, we say so. That's the bounded thing, and it holds the target-innocence line — the "two" comes from the algebra's rank, not from us knowing there are three generations.

Notes only; no toys/theorems claimed. F708 = Deliverable A (interior identification). Mechanism: T_φ commutes with the Peirce frame projections IFF φ is Jordan-spectral (function of generic norm N + trace τ only); then interior spectral decomposition = the r=2 primitive idempotents c₁,c₂=(e₀±v)/2 (T2511), one generation-mode each, masses = φ at the two spectral positions, electron falls out at the boundary (+1, Deliverable B). Interior COUNT already rigorous (Cal/K947, EJA rank cap); this closes the IDENTIFICATION **modulo the one check**: is φ (F583) Jordan-spectral? (Grace's lane; = F681 O2 as commutation test). Target-innocent (r from EJA rank, masses from φ, electron not placed — K880 guard held; four smuggle-warnings audited). Elie: toy verifies commutation + r=2 idempotent eigenmodes + contrapositive (V₁₂ symbol doesn't commute). DERIVED-IF-φ-spectral; not closed; premise REDUCED until φ-check + Deliverable B land. — Lyra