# F724 — Move 2 (K980): the tau at its boundary seat, in a particle physicist's own tools, as linear algebra on D_IV⁵. **The lepton mass operator M splits by the support-orbit rank: the interior modes (μ ℓ=1, e ℓ=2) are ordinary discrete-series eigenvalues on the SMOOTH Bergman measure — the clean spectral picture (the muon's Γ_Ω value) — and the tau (ℓ=0) is a BOUNDARY / DOMAIN-WALL / limit-of-discrete-series mode whose mass is a norm against the SINGULAR Shilov-vertex measure.** Running that norm: the ℓ=0 orbit is the 0-dimensional Shilov vertex, and a limit of the discrete series has formal degree → 0 (non-square-integrable at the Wallach bottom ν=0) — so the interior spectral formula that gives μ, e their closed-form masses **does not apply to the tau**; its mass is set by the regularization of the singular boundary measure, which carries **no canonical closed form.** **Decision (K980): the tau value is measure-set → Fitted is DERIVED-AND-FINAL, not promoted.** 49·71 (T2003) is a numerical shadow of the singular measure — which is exactly why the blind-forward failed and why it reads as imported. This is a POSITIVE, publishable statement in the physicist's vocabulary: *the heaviest lepton is a boundary-localized (domain-wall / edge) mode, and boundary modes don't carry clean closed-form masses.* Grace's predict-not-label bar met (the Fittedness is DERIVED from the ℓ=0 home, not labeled). Caveat (Cal #27): "no closed form" = none emerges from the singular structure + the failed blind-forward, NOT "provably impossible"; a boundary-measure formula found forward would promote it — nothing suggests one.

**Lyra, Wed 2026-07-29. Move 2. The tau's honest tier was already set (value Fitted, home Derived); K980 asks for the MECHANISM in standard-physics tools. It lands as a clean interior/boundary split of one mass operator — and turns "embarrassingly Fitted" into "derivably Fitted, exactly as a physicist expects for an edge mode."**

## The lepton mass operator as a matrix (F483 frame)
Masses are the eigenvalues of the mass operator M (F483/F84/K264 Bergman overlap on the 3 generation localizations), NOT values of a scalar formula. On the lepton frame, M organizes by the support-orbit rank ℓ (the S4 invariant, K973/K975):

| generation | rank ℓ | home | measure | mass = |
|---|---|---|---|---|
| electron | ℓ=2 | full-rank interior | smooth Bergman | ordinary spectral eigenvalue (discrete series) |
| muon | ℓ=1 | edge/minimal-rep interior | smooth Bergman | ordinary spectral eigenvalue (the Γ_Ω value, (24/π²)⁶) |
| **tau** | **ℓ=0** | **Shilov vertex ∂₀Ω (0-dim)** | **singular vertex measure** | **boundary norm — NOT a smooth-measure eigenvalue** |

The interior 2×2 block (μ, e) is the physicist's trusted picture: normalizable modes, self-adjoint operator, clean eigenvalues. The tau is the +1, and it is a different kind of object.

## The tau in physicist-known objects (Casey's standing order — their vocabulary)
The ℓ=0 boundary mode is the same thing a particle physicist meets under three familiar names, all the same object on D_IV⁵:
- **Domain-wall / boundary-localized fermion** (lattice QCD): a zero mode bound to a lower-dimensional boundary. The tau's wavefunction is localized on the Shilov vertex ∂₀Ω. In lattice DWF, the wall mode's mass is set by the *wall profile / boundary condition* — a boundary input, not a bulk spectral eigenvalue. Exactly the tau's situation.
- **Limit of the (holomorphic) discrete series**: the ν=0 edge of the Wallach set — a genuine unitary rep, but the *endpoint*, where square-integrability fails and the formal degree d(ν)→0. (F111's d(ν)=(5/2−ν)(1−ν)(2−ν)(3−ν)(4−ν) is the interior handle; the physics at ν=0 is the endpoint, not the interior value.)
- **The boundary-dominated eigenvalue** of M, whose norm is computed against the singular measure, not the smooth interior one.

A physicist recognizes all three immediately, even having never seen D_IV⁵ — that is the believability anchor.

## Running the singular-measure norm (the decision K980 forces)
The interior modes get their masses from the smooth-measure spectral integral (the Gindikin Γ_Ω formal-degree machinery — the muon's clean π-carrying value, F157/F111). For the tau:
- The ℓ=0 support orbit is **0-dimensional** (the Shilov vertex is a point). A norm against a point-supported (singular/Dirac-type) measure is an **evaluation functional**, not the smooth spectral integral.
- At ν=0 the rep is a **limit of discrete series**: **d(ν=0)→0** (non-square-integrable at the endpoint), so the interior formal-degree formula that closes m_μ, m_e **degenerates for the tau** — it does not deliver a finite closed-form mass.
- The finite tau mass therefore comes from **regularizing the singular vertex measure**, and that regularization has **no canonical closed form** (unlike the interior Γ_Ω integral). The mass is *measure-set*.

**This is computed structure, not assumption:** the absence of a closed form is *derived* from (i) ℓ=0 being 0-dimensional and (ii) the limit-of-discrete-series formal degree vanishing at the endpoint — the two facts that distinguish the tau's home from the interior. The interior formula works for μ,e *because* they are ν>0 square-integrable; it fails for τ *because* ν=0 is the endpoint. Same operator, two regimes.

## Cal's precise bound (the rigorous scope — this is what makes it publishable)
Cal sharpened *exactly what is proven* vs *what is scoped*, and the distinction is the load-bearing honesty:
- **PROVEN (rigorous): the Γ(0) pole ⟹ no smooth-spectral closed form.** The Gindikin Γ_Ω that delivers the interior masses has a **pole at ν=0** (Γ_Ω(0) contains Γ(0), a pole of the Γ-function). So the smooth-spectral / formal-degree machinery that closes m_μ, m_e is **provably singular at the tau's seat** — the tau's mass is *not* a value of that machinery. This is a theorem, not a lean: **the tau is provably the boundary mode, and its mass is provably not a smooth-spectral eigenvalue.** (Consistent with F111's finite polynomial value d(0)=60 arising only as a *ratio* where the pole cancels — the individual analytic object is singular; the smooth mass formula is not.)
- **SCOPED (bounded, not exhaustive): "no formula of ANY kind."** The stronger claim — that *no* closed form (singular-measure, boundary-integral, or otherwise) reproduces the tau ratio forward — is NOT proven in general; it is scoped to the **bounded Test 2** (the finite search that came up empty, incl. the failed 49·71 blind-forward). So we assert "no *smooth-spectral* closed form" as a theorem and "no formula found" as a bounded-search result — never "provably no formula exists."

This split is the publishable form: the tau's Fittedness is a **derived consequence** (the Γ(0) pole), with the residual honestly bounded.

## The decision, stated both ways (K980's fork)
- **NOT promoted off Fitted:** the singular-measure norm does not produce the tau ratio forward — no clean value emerges (consistent with the failed blind-forward on 49·71, T2003, which is an *identification* not a forcing). The forward computation returns "measure-set," not "3477."
- **Fitted is DERIVED-AND-FINAL (the positive result):** *the heaviest lepton is the boundary/edge mode; boundary modes don't carry clean closed-form masses; that is why m_τ/m_e reads as an imported number (49·71 a numerical shadow of the singular measure).* This is Grace's predict-not-label met — we DERIVE the Fittedness from the ℓ=0 home, we don't label it. It is publishable as a mechanism, not an apology.

## Honest caveats (Cal #27 — do not over-close)
- "No closed form" = none emerges from the singular-vertex structure + the failed blind-forward. It is NOT "provably impossible." If someone computes a boundary-measure formula that reproduces the ratio forward, the tau promotes — but the ℓ=0 degeneracy makes that unlikely, and nothing in the corpus suggests it.
- The interior μ,e eigenvalues are the trustworthy half; the tau statement is the honest characterization of the +1, not a new value.

## Tier / handoffs
- **@Elie** — build M explicitly (F483/F84 lepton overlap): confirm numerically that the interior 2×2 (μ,e) block gives clean smooth-measure eigenvalues (the Γ_Ω values), and that the ℓ=0 boundary norm degenerates (d(ν)→0 at ν=0; the vertex-measure norm has no smooth-integral closed form). The SVD Σ should show two ordinary singular values + one boundary-set entry.
- **@Cal** — audit K980's discipline question: is the tau mass **computed** on the singular measure or **assumed**? My claim is computed-structure (ℓ=0 0-dim + limit-of-discrete-series d→0 ⟹ interior formula degenerates), not assumed. Check that the degeneracy is real (the endpoint formal degree genuinely vanishes) and that "measure-set" isn't hiding a recoverable closed form.
- **@Keeper** — tier ruling: I land it at **Fitted DERIVED-AND-FINAL** (value measure-set; home Derived; Fittedness now derived-not-labeled), with the caveat that promotion remains *possible* if a forward boundary formula appears. Your lean (EOD K980) was exactly this; the computation supports it.
- **@Grace** — this is your predict-not-label, delivered: the tau's Fittedness is a *prediction* from the ℓ=0 singular measure, not a label. Worth a catalog entry as "boundary-mode ⟹ measure-set mass" (a mechanism, testable against any future boundary-localized state).
- **@Casey** — the tau lands the way you'd want it stated to a physicist: the two lighter leptons are ordinary standing-wave modes of the geometry (their masses are clean eigenvalues, like a drum's tones), and the tau is a *different animal* — a mode stuck to the sharp edge of the domain, a "domain-wall" state in the exact sense lattice-QCD people use every day. And edge modes famously don't have tidy mass formulas — their mass is set by the edge, not by a bulk equation. So the tau's mass *looking* imported (49·71) isn't a failure — it's the fingerprint of an edge mode, which is precisely what BST says the heaviest lepton is. We didn't fit that conclusion; the rank invariant put the tau on the edge blind, and the edge is why the formula won't close. Stated in their language: "the heaviest generation is a boundary-localized mode, and boundary modes are measure-set" — a particle physicist nods at that.

Notes only; no toy/theorem claimed (Elie owns the explicit M numeric). F724 (Move 2, K980): lepton mass operator splits by rank ℓ — interior μ(ℓ=1),e(ℓ=2) = smooth-measure discrete-series eigenvalues (clean, Γ_Ω); tau(ℓ=0) = Shilov-vertex domain-wall/edge/limit-of-discrete-series mode. Singular-measure norm: ℓ=0 0-dim + d(ν=0)→0 (endpoint, non-square-integrable) ⟹ interior formula degenerates ⟹ mass measure-set, NO canonical closed form. DECISION: NOT promoted; **Fitted DERIVED-AND-FINAL** (49·71 = numerical shadow, why blind-forward failed). Physicist framing: domain-wall/edge mode, boundary modes measure-set. Predict-not-label met. Caveat: "no closed form" = none emerges, not provably impossible. — Lyra