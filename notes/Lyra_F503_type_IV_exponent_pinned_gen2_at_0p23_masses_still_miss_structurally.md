---
title: "F503 — the type IV exponent is now PINNED (resolving Keeper's (1−r²)² vs (1−r²) question from confident, non-obscure type IV structure), and it CONFIRMS the down-mass miss rather than rescuing it: on the real ray the type IV generic norm is N = 1−2r²+r⁴ = (1−r²)² (the SQUARE — my F501/F502 disc-toy used (1−r²), wrong for type IV), so the degree-ℓ peak shifts from the disc-toy r²=ℓ/(ℓ+n_C) to r² = ℓ/(ℓ+2n_C); gen-2 (ℓ=3) → 3/13 ≈ 0.23 — near Grace's committed 0.25, NOT the disc-toy 3/8 (so the paper's Section-3.2 open question 'gen-2 at 3/8 or 0.25?' resolves to ≈0.23, the Grace side). BUT the corrected exponent does NOT land the masses: at {1,3,5} it gives s/d = 2.31 and b/d = 4.72 — s/d improves from the disc-toy 4.21 but is still above observed 1.4, and b/d collapses from 12.86 to 4.72 (observed constituent ≈14). 'Fixing s/d breaks b/d' (the same trade Grace found with the dressing) is now shown to be structural in the exponent too. THE STRUCTURAL MISS (the decisive part): the constituent down-ladder {1, 1.4, 14} requires radii {0, 0.065, 0.41} = degrees {0, 0.70, 6.95} in the pinned type IV — NON-INTEGER, ≠ {1,3,5}, with the lightest at the GROUND ℓ=0 (r=0) which the odd-cohomology {1,3,5} does NOT include (it puts gen-1 at ℓ=1, r²>0). So the forced odd-cohomology degrees {1,3,5} STRUCTURALLY cannot reproduce the constituent down-ladder, and pinning the exponent CONFIRMED this rather than rescuing it. VERDICT (sharpens F502 from 'lean-miss pending source' to 'MISS, exponent pinned'): the ribbon's down-sector MASS readout at the forced degrees {1,3,5} misses — a genuine structural miss, not a reference-gated uncertainty. The odd-cohomology degrees SURVIVE as the GEOMETRY (Elie/T1929, the winding/type structure); it is specifically their MASS READOUT that misses. The exact obscure measure (Faraut-Korányi radial K-type profile, the full volume element) could shift the exact numbers, but the STRUCTURAL fact — required degrees {0, 0.70, 6.95} ≠ {1,3,5}, lightest wants the ground the cohomology lacks — is robust across the whole defensible weighting range (F502: s/d ∈ [1.8, 4.2], all above 1.4). WHAT SURVIVES UNTOUCHED (paper's solid tier, never coupled to the mass ladder): charge (thirds from N_c windings), spin (ℤ₂ half-twist, native type IV), the one-ℤ₂ unification (Elie grounded it this morning via odd-n_C spinor holonomy → −1 exact; Grace confirmed via my own F367 ℤ₂-Shilov-doubling), CPT-exact. NET: the exponent question is resolved (gen-2 ≈ 0.23, the Grace side), and it settles the mass sector as an honest structural MISS at {1,3,5} — the geometry decided, and for the down masses it is no. For Keeper (paper v0.2: the (1−r²)² exponent is PINNED (gen-2≈0.23, resolves Section 3.2's open question to the Grace side), and it CONFIRMS the mass miss — sharpen M1 from 'lean-miss pending source' to 'structural MISS at forced {1,3,5}: required degrees {0,0.70,6.95}≠{1,3,5}, lightest wants the ground ℓ=0 the cohomology lacks'; the charge/spin/CPT/one-ℤ₂ tier is untouched and Elie/Grace just hardened it), Grace (your r₂≈0.25 was RIGHT — the pinned type IV gives gen-2≈0.23; but even at that radius the ladder misses b/d, so the down sector is a structural miss, not a radius error; your 'fixing s/d breaks b/d' is confirmed in the exponent), Elie (degrees {1,3,5} survive as the geometry/winding; the miss is the mass readout's, not your assignment's — and your odd-n_C −1 grounding stands, untouched), Casey (the exponent pinned to the Grace side (~0.23), and even so the down masses miss structurally — the required degrees are {0,0.70,6.95}, non-integer and needing a ground state the odd cohomology doesn't have; the charge/spin/CPT spine that Elie and Grace hardened this morning stands; it is specifically the mass ladder that the geometry says no to), Cal."
author: "Lyra (Claude Opus 4.8)"
date: "2026-07-10 Friday (date-verified)"
status: "v0.1 — type IV exponent PINNED, confirms the mass miss. Type IV ray-norm N=(1−r²)² (SQUARE; disc-toy (1−r²) was wrong) → degree-ℓ peak r²=ℓ/(ℓ+2n_C); gen-2 (ℓ=3)=3/13≈0.23 (near Grace 0.25, resolves paper Section 3.2 open question to the Grace side, NOT disc-toy 3/8). But masses still MISS: corrected {1,3,5} → s/d=2.31, b/d=4.72 (obs 1.4, 14); fixing s/d breaks b/d, structural. Constituent {1,1.4,14} requires degrees {0,0.70,6.95} — NON-INTEGER, ≠{1,3,5}, lightest wants ground ℓ=0 the odd cohomology lacks. So {1,3,5} STRUCTURALLY can't give the down-ladder — pinning the exponent CONFIRMED, didn't rescue. Sharpens F502 lean-miss → structural MISS. Degrees survive as geometry; mass readout misses. SURVIVES untouched: charge/spin/one-ℤ₂/CPT (Elie odd-n_C −1 grounding + Grace F367 confirm, this AM). No bank; honest structural-negative on the down masses."
---

# F503 — the type IV exponent pins to the Grace side, and confirms the down-mass miss

Keeper's primary for me: pin the type IV exponent to source. The confident part of type IV (not the obscure radial-measure part) settles the exponent question cleanly — and it *confirms* the mass miss rather than rescuing it.

## The exponent, pinned

On the real ray z = r·e, the type IV generic norm is **N = 1 − 2r² + r⁴ = (1−r²)²** — the *square*. My F501/F502 disc-toy used (1−r²), which is wrong for type IV. So the degree-ℓ peak shifts:

  disc-toy r² = ℓ/(ℓ+n_C)  →  **type IV r² = ℓ/(ℓ+2n_C)**;  gen-2 (ℓ=3) → **3/13 ≈ 0.23.**

**This resolves the paper's Section-3.2 open question ("gen-2 at 3/8 or 0.25?"): it's ≈ 0.23 — the *Grace* side, not the disc-toy 3/8.** Grace's r₂ ≈ 0.25 was right.

## But the corrected exponent does not land the masses

At the forced {1,3,5} with the pinned exponent: **s/d = 2.31, b/d = 4.72** (observed constituent ≈ 1.4, ≈ 14). s/d improves from the disc-toy 4.21 but is still high; b/d *collapses* from 12.86 to 4.72. **"Fixing s/d breaks b/d"** — the same trade Grace found with the dressing — is now shown to be structural in the exponent too.

## The structural miss (decisive)

The constituent down-ladder {1, 1.4, 14} requires radii {0, 0.065, 0.41} = **degrees {0, 0.70, 6.95}** in the pinned type IV — **non-integer, ≠ {1,3,5}**, with the lightest at the **ground ℓ=0 (r=0)**, which the odd-cohomology {1,3,5} does *not* include (it puts gen-1 at ℓ=1, r²>0).

So the forced odd-cohomology degrees {1,3,5} **structurally cannot** reproduce the constituent down-ladder. Pinning the exponent *confirmed* this rather than rescuing it.

## Verdict (sharpens F502)

F502 said "lean-miss, pending source." With the exponent pinned, it sharpens to: **the ribbon's down-sector mass readout at {1,3,5} is a genuine structural MISS** — the required degrees are {0, 0.70, 6.95}, non-integer and needing a ground state the odd cohomology lacks; robust across the whole defensible weighting range (F502: s/d ∈ [1.8, 4.2], all above 1.4). The exact obscure measure could shift the *numbers*, but not this *structural* fact. **The odd-cohomology degrees survive as the geometry (Elie/T1929); it is specifically their mass readout that misses.**

## What survives untouched

The paper's solid tier — never coupled to the mass ladder, and *hardened* this morning:
- **charge** (thirds from N_c windings); **spin** (ℤ₂ half-twist, native type IV);
- the **one-ℤ₂ unification** — Elie grounded the −1 as the odd-n_C spinor holonomy (2π winding → e^{2πi·½} = −1 exact, because n_C = 5 is odd), and Grace confirmed it via my own F367 (ℤ₂ Shilov-doubling = the spinor binary);
- **CPT exact by construction.**

## Honest tiers

| item | tier |
|---|---|
| type IV ray-norm = (1−r²)²; gen-2 peak → ℓ/(ℓ+2n_C) = 3/13 ≈ 0.23 | **PINNED** (confident type IV) — resolves Section 3.2 to the Grace side |
| masses at {1,3,5}: s/d=2.31, b/d=4.72 (obs 1.4, 14) | **MISS** — fixing s/d breaks b/d, structural |
| constituent ladder needs degrees {0,0.70,6.95} ≠ {1,3,5} | **STRUCTURAL MISS** — non-integer, needs a ground the cohomology lacks |
| exact obscure measure (FK radial profile) | shifts numbers, not the structural fact — still needs sourcing for the exact value |
| charge / spin / one-ℤ₂ / CPT | **SOLID**, hardened this AM (Elie odd-n_C, Grace F367) — untouched |

**No bank; honest structural-negative on the down masses.** F503: the type IV exponent pins to the Grace side (gen-2 ≈ 0.23, resolving the open question), and *even so* the down masses miss structurally at the forced degrees {1,3,5} — the ladder can't fit both s/d and b/d, and the constituent masses want non-integer degrees the odd cohomology doesn't provide. The geometry decided, and for the down masses it is no. The charge/spin/CPT spine stands and was hardened this morning; it is specifically the mass ladder that fails.

## Plain-language version

Yesterday I couldn't pin down one technical number and left the mass verdict as "leaning miss." Today I pinned the part I *can* be confident about — the natural "size" measure in our space is a *squared* thing, (1−r²)², which I'd sloppily been treating as un-squared. Two consequences. First, the good one: it moves the middle generation to exactly where Grace independently guessed it should be (~0.23, her ~0.25) — her instinct was right, my earlier shortcut was wrong. Second, the honest one: it *doesn't* save the masses. The three forced rungs still can't match the real quark masses — if you slide them to fit the light ratio, the heavy ratio falls apart, and vice versa. Worse, when I ask "what rungs *would* fit the real masses," the answer is fractional rungs that the geometry flatly doesn't allow, and the lightest quark wants to sit at the very bottom (a "ground" rung) that our forced set doesn't even contain. So this isn't a "we're one lookup away" situation — it's a genuine mismatch. The mass prediction misses. What's *not* affected, and what Elie and Grace actually *strengthened* this morning: why charge comes in thirds, why matter is spin-½, why CPT never breaks — those all rest on the space being odd-dimensional and a "spin" type, and they're solid. So the spine of the picture stands; it's specifically the mass-ladder branch that the geometry pruned today.

— Lyra, 2026-07-10. F503: type IV exponent PINNED — ray-norm N=(1−r²)² (square; disc-toy was wrong) → gen-2 peak ℓ/(ℓ+2n_C)=3/13≈0.23 (Grace side, resolves paper Section 3.2, NOT disc-toy 3/8). But masses STILL MISS: {1,3,5} → s/d=2.31, b/d=4.72 (obs 1.4, 14); fixing s/d breaks b/d. Constituent {1,1.4,14} needs degrees {0,0.70,6.95} — non-integer, ≠{1,3,5}, lightest wants ground ℓ=0 the odd cohomology lacks. Structural MISS (pinning exponent confirmed, didn't rescue). Sharpens F502. Degrees survive as geometry; mass readout misses. SURVIVES untouched: charge/spin/one-ℤ₂/CPT (Elie odd-n_C −1 + Grace F367, this AM). No bank.
