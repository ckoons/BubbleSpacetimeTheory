---
title: "F510 — the Yukawa-as-overlap frame computed BLIND (Keeper's PRIMARY 2, the deepest lane, Cal #27 armed): the fermion Yukawa = the boundary amplitude of the normalized weight-n holomorphic mode, y_n = A_n/A_{n_max} with A_n = 1/√B(n+1,α+1) in the weighted Bergman measure (1−r²)^α. THREE outcomes, tiered hard. (1) NEW, IN KIND — the SIZE of the fermion hierarchy has a geometric origin: the boundary-overlap over the FORCED weight range [0, N_max] with the FORCED Bergman weight α = n_C = 5 spans A(137)/A(0) ≈ 1.0×10⁵ (α=C_2=6 gives 4.7×10⁵), which is the observed top/lightest Yukawa span (y_t/y_e ≈ 3×10⁵) IN ORDER OF MAGNITUDE — target-innocent (α and the range [0,N_max] are forced, not fit). So the ~5-6 orders of magnitude of the fermion hierarchy come from the mode-weight running from 0 (deep bulk) to N_max (the boundary cutoff) — the hierarchy's magnitude is the boundary cutoff N_max read through the Bergman weight n_C. (2) GATE 3 CLARIFIED, NOT CLOSED — t/c = N_max is NOT an amplitude ratio (that needs k≈114 weight-steps, not 'one shell'); it is DIMENSIONAL: the boundary Hilbert space holds N_max quanta (the cutoff), the top is fully aligned (y=1), and charm spread over the boundary gives per-mode overlap y_c = 1/dim(boundary) = 1/N_max. Observed y_c = 0.00729 = 1/137.2 — a clean 0.1% fact. So gate 3 reduces to the identification dim(Shilov boundary mode-space) = N_max = N_c³·n_C+rank — plausible (N_max IS the max-winding cutoff) but NOT yet forced, and the y = winding/N_max reading works for top (w=N_max) and charm (w=1) but does NOT cleanly extend to up (y_u·N_max = 1.7×10⁻³, non-integer) — so it is a candidate mechanism, not a bank, and doesn't obviously extend. (3) HONEST LIMIT (Cal #27) — computing the INDIVIDUAL Yukawas requires the per-fermion weight assignment (which weight n each of ν,d,u,e × 3 generations sits at), which is NOT yet forced; that assignment is the fitting risk, and I will not fake per-fermion values to paint a full tower. TIER: y_t=1 RIGOROUS (Cauchy-Schwarz, F509); hierarchy-SPAN IN KIND (forced α=n_C + range [0,N_max] → ~10⁵, order-of-magnitude match, target-innocent); gate 3 CANDIDATE (dimensional dim(boundary)=N_max, y_c=1/N_max clean but doesn't extend to up); individual Yukawas OPEN (weight-assignment-gated, fitting risk named). NET: the overlap frame is right IN KIND for the whole hierarchy (its SIZE is geometric, top=1 rigorous) but the individual values and the exact shell/dimension law are not yet forced — a genuine deepest-lane partial, honestly bounded, not a full-tower bank. For Keeper (PRIMARY 2 result: overlap frame right IN KIND — the hierarchy SPAN ~10⁵ falls out of the forced weight range [0,N_max] with α=n_C, target-innocent, giving a geometric origin for WHY the fermion hierarchy is ~5-6 orders of magnitude; but individual Yukawas need the per-fermion weight assignment = the fitting risk, NOT faked; tier: kind-yes, values-open, top=1 rigorous), Elie (gate 3 is now a clean DIMENSIONAL target, not an amplitude one: derive dim(Shilov boundary mode-space) = N_max — that IS the t/c=N_max shell-count, and it connects to your weight→radius theorem via the boundary mode-count; note y=winding/N_max fits top+charm but not up, so it's not the whole story), Grace (the hierarchy-span-from-[0,N_max] result is the magnitude half of your thermal u/d law — the SIZE is geometric, the per-generation ordering is thermal), Casey (the whole fermion hierarchy is a spectrum of boundary-overlaps, and the geometry gives its SIZE for free: modes running from the deep bulk (weight 0) to the boundary cutoff (weight N_max=137), read through the Bergman weight n_C, span ~10⁵ — the observed top-to-electron range; the top at 1 is rigorous; but the individual masses need the per-fermion weight assignment, which I have NOT forced and won't fake — so the frame is right in kind for the whole tower, exact only at the top), Cal (Cal #27 armed and applied: I banked only the KIND result and the rigorous top; the individual-value tower is explicitly held as weight-assignment-gated fitting risk)."
author: "Lyra (Claude Opus 4.8)"
date: "2026-07-11 Saturday (date-verified)"
status: "v0.1 — Yukawa-overlap frame computed BLIND. y_f = boundary amplitude of normalized weight-n mode, A_n=1/√B(n+1,α+1), measure (1−r²)^α. (1) NEW IN KIND: hierarchy SPAN from forced range [0,N_max] with forced α=n_C=5 → A(137)/A(0)≈1.0e5 (α=C_2→4.7e5) = observed y_t/y_e≈3e5 in order of magnitude, target-innocent → geometric origin for the SIZE of the fermion hierarchy (bulk weight 0 → boundary cutoff N_max, read through n_C). (2) GATE 3 CLARIFIED not closed: t/c=N_max is DIMENSIONAL (dim boundary=N_max, charm per-mode 1/N_max; y_c=1/137.2 clean 0.1%) NOT an amplitude ratio (needs k≈114 steps); reduces to 'is dim(Shilov boundary)=N_max forced?' — plausible, y=w/N_max fits top+charm but NOT up (1.7e-3 non-integer), candidate not bank. (3) HONEST LIMIT (Cal #27): individual Yukawas need per-fermion weight assignment = fitting risk, NOT faked. TIER: y_t=1 rigorous; span in-kind (target-innocent); gate 3 candidate; individual values open. Right IN KIND for whole tower, exact at top. Deepest-lane partial, honestly bounded."
---

# F510 — the Yukawa hierarchy is a boundary-overlap spectrum: its SIZE is geometric, its values are weight-gated

Keeper's PRIMARY 2 — the deepest thing on the board, Cal #27 armed: does the whole fermion hierarchy fall out of boundary-overlaps, computed blind? The model (from F509): the Yukawa is the **boundary amplitude of the normalized weight-n holomorphic mode**,

$$y_n = \frac{A_n}{A_{n_{max}}}, \qquad A_n = \frac{1}{\sqrt{B(n+1,\alpha+1)}} \ \text{ in the weighted Bergman measure } (1-r^2)^\alpha.$$

Three outcomes, tiered hard.

## (1) NEW, in kind — the SIZE of the hierarchy is geometric

With the **forced** Bergman weight α = n_C = 5 and the **forced** weight range [0, N_max]:

$$\frac{A(n{=}137)}{A(n{=}0)} \approx 1.0\times10^5 \quad (\alpha=C_2=6:\ 4.7\times10^5)$$

The observed top/lightest span is y_t/y_e ≈ 3×10⁵. **Order-of-magnitude match, and target-innocent** — α = n_C and the range [0, N_max] are forced, not fit. So the ~5–6 orders of magnitude of the fermion hierarchy come from the mode weight running from **0 (deep bulk) to N_max (the boundary cutoff)**, read through the Bergman weight n_C. The *magnitude* of the hierarchy is the boundary cutoff N_max seen through n_C. That is a genuine geometric origin for a thing the SM just inputs.

## (2) Gate 3 clarified (not closed) — t/c = N_max is dimensional, not an amplitude

Testing "charm one shell in" as an amplitude ratio fails: A(137)/A(137−k) = N_max needs k ≈ 114 weight-steps, not one shell. The clean reading is **dimensional**: the boundary Hilbert space holds **N_max quanta** (the cutoff); the top is fully aligned (y = 1); charm spread over the boundary has per-mode overlap **y_c = 1/dim(boundary) = 1/N_max**. Observed y_c = 0.00729 = **1/137.2** — a clean 0.1% fact. So **gate 3 reduces to: is dim(Shilov boundary mode-space) = N_max = N_c³·n_C + rank forced?** Plausible (N_max is the max-winding cutoff), but not yet derived — and the y = winding/N_max reading fits top (w=N_max) and charm (w=1) but **not** up (y_u·N_max = 1.7×10⁻³, non-integer). Candidate mechanism, not a bank, and not obviously universal.

## (3) Honest limit (Cal #27) — individual Yukawas are weight-assignment-gated

Computing each fermion's Yukawa needs the per-fermion weight assignment (which weight n each of ν, d, u, e × 3 generations sits at). That assignment is **not yet forced**, and it is the fitting risk. **I will not fake per-fermion values to paint a full tower.**

## Honest tiers

| item | tier |
|---|---|
| y_t = 1 (boundary-aligned, Cauchy–Schwarz, F509) | **RIGOROUS** |
| hierarchy SPAN ~10⁵ from forced α=n_C + range [0,N_max] | **IN KIND** — order-of-magnitude, target-innocent; geometric origin for the hierarchy's SIZE |
| t/c = N_max via dim(boundary) = N_max; y_c = 1/N_max (0.1%) | **CANDIDATE** — dimensional, plausible, but not forced and doesn't extend to up |
| individual Yukawas (full tower) | **OPEN** — per-fermion weight assignment gated; fitting risk named, not faked |

## Plain-language version

The idea is that a particle's mass comes from how close its "mode" sits to the edge of the space — right at the edge means heavy (like the top), deep inside means nearly massless (like the electron). I tested whether that one idea can produce the whole spread of masses, without me hand-tuning anything. One thing came out cleanly and it's new: the *size* of the spread. Fermion masses run over about five or six factors of ten from the top down to the electron — an enormous range the Standard Model just writes down by hand. In this picture that range isn't arbitrary: it's what you get when a mode's "distance from the edge" runs from all the way inside to right at the edge, and the edge is at the number 137 (our cutoff). Plug in the forced numbers and you get ~100,000, which is the observed range. What I could *not* do honestly is compute each individual mass — that needs knowing exactly which "rung" each of the twelve fermions sits on, and that assignment isn't forced yet, so filling in the whole ladder would be fitting, and I won't. So: the frame explains why the ladder is *as tall as it is* and nails the top rung exactly, but the individual rungs are still open.

— Lyra, 2026-07-11. F510: Yukawa-overlap frame blind. NEW in-kind — hierarchy SPAN ~10⁵ from forced α=n_C + range [0,N_max] (target-innocent), geometric origin for the hierarchy's SIZE (bulk weight 0 → boundary cutoff N_max through n_C). Gate 3 clarified: t/c=N_max is DIMENSIONAL (dim boundary=N_max, y_c=1/137.2 clean) not amplitude; candidate, doesn't extend to up. Honest limit: individual Yukawas weight-assignment-gated = fitting risk, not faked. TIER: top=1 rigorous, span in-kind, gate 3 candidate, values open.
