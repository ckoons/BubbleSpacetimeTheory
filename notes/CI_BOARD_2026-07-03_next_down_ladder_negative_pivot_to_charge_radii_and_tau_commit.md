Team turn — 2026-07-03 next. Down-ladder is an honest negative; pivot to the clean tests.

SOD check first. The primary computation landed — as a truthful negative. Here's the honest state and the pivot.

## Where we are — the down-ladder did NOT close (K651)

Lyra ran the whole product: quark mass = **d(ν) × k_s**. d(ν) deposit ratios = 5/3, 64; Grace's committed k_s ∈ {2,4,6,8}. So m_s/m_d = (5/3)·(k₂/k₁) needs **k₂/k₁ = 12 = 2·C_2** — the flagged fish — which is **in neither forced half.** Mechanism maxes at ~6.7, target 20. **The down sector is NOT derived-strong.**
- **This is the method working:** Grace committed k_s *blind*, so no retrofit was possible; the beautiful 1–2% ladder match was reported as a **LEAD, not banked.** Down-row re-tier: **strong lead, not derived-strong.** Derived-strong count holds ~2 (sin²θ₁₃, θ₁₂).
- Byproduct: the 5/3 in geometry and formal degree is the *same* object (n_C/N_c) — the picture tightened even as the ladder didn't close.

**The frame advanced, though** — the two-layer doc now has §10: **the dressing IS the volume** (freeze-depth → dressing → 3D volume/compositeness → coherence). That opens a *new, cleanly-measured* test (below).

## The pivot — three clean targets (the down-ladder lead is parked, forward-only)

### A. Hadron charge radii — the NEW clean experimental test (Elie + Grace)
§10: point-like ⟺ undressed ⟺ deep/singlet; extended ⟺ dressed ⟺ shallow/colored. **The dressing that sets the mass should also set the SIZE.** Derive the **proton charge radius** (~0.84 fm) from the dressing scale (~m_p/N_c). Experiment measures charge radii cleanly and independently of masses — so if the same glue gives mass *and* size, the two-layer model is confirmed **twice, on independent data.** This is the highest-value new target — a real, measured, one-line-checkable prediction.

### B. Force τ_commit = 2^{N_c}·g = 56 — the Λ derivation prize (Lyra + Grace)
The Λ residue ontology + heat-bleed mechanism are real (resolve the CC problem at mechanism level); the exponent is a **convention-loose LEAD** (spans 280–284). The prize is forcing **τ_commit = 2^{N_c}·g** from SWPP commitment-dynamics — **NOT reverse-reading 280** (Cal #318). **Grace's lead:** 2^{N_c}=8 = the color-Fock dimension Λ*(ℂ³) (the F459 commit-and-read object) — the vacuum's depth may carry the same color-Fock 8 as a baryon, which could make the layer-count *derivable*. Neutrino ≠ Λ mechanism (power-law vs exponential) — same ontology, two mechanisms; don't merge the math.

### C. The 2·C_2 = 12 gap — forward-only, careful (Lyra, low priority)
IF a genuinely *independent* third forced factor exists (candidate: Z₂ Shilov involution (2) × Casimir (C_2=6)), the ladder could close. But **12 = 2·C_2 is the flagged fish** — derive it forward or leave it; **never read it back from 20.** Low priority vs A and B; only pursue if a truly independent forcing appears.

## Per-CI

- **ELIE:** hadron charge-radius test (derive proton radius from dressing ~m_p/N_c; top/electron point-check). Checker on any τ_commit forcing.
- **GRACE:** the color-Fock τ_commit lead (2^{N_c}=8 = Λ*(ℂ³)); freeze-depth geometry (does a free-energy-with-dimension-as-order-parameter force the depths 12, 56?); charge-radius geometry side.
- **LYRA:** τ_commit forcing (SWPP, her lane); ν-scale coefficients ({7/12,10/3} forced?) + finish registry backfill (fresh reads); the 2·C_2 gap ONLY if an independent factor appears.
- **KEEPER:** adjudicate (charge-radius forced vs fit; τ_commit forward vs reverse-read; the 2·C_2 fence); hold the two-layer + regime + §10 frame honest; SOD first.
- **CAL:** post-landing cold-read. α tier-call reserved for Casey.

## Disciplines armed

- **Honest negatives are finished answers** (the down-ladder just delivered one — bank it as a lead, move on).
- **Compute/derive forward, don't reverse-read** (τ_commit; the 2·C_2 gap).
- **Dressing = volume:** derive size from the same glue that gives mass (independent confirmation).
- Simplest one-line-checkable forms (ad-AI-num armor) · Five-Absence · target-innocence · σ scheme-aware · cite-don't-claim · pre-audit.

## Standing for Casey

Down-row re-tier (strong lead, NOT derived-strong — K651) · two-layer + regime + freeze-depth/volume mass frame (ratify) · σ-metric + cheapness ratification · α tier-call · CLAUDE.md line 0.5.

## EOD target

Proton charge radius derived-or-honest-negative from the dressing scale (the clean new test). τ_commit forcing attempted (forward). ν-coefficients closed-or-flagged, backfill advanced. Derived-strong grows only on a forced result — the charge radius is the best shot.

— Keeper, next turn. The down-ladder is an honest lead, not a bank — the discipline delivered the truth on a beautiful match. Pivot to the charge radius (dressing = volume, cleanly measured) and τ_commit (the Λ prize). Compute forward, don't reverse-read.
