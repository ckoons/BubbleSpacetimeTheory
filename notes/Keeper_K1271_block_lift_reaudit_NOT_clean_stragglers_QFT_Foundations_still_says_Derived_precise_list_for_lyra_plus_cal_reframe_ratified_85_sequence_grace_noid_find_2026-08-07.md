# K1271 — Block-lift re-audit: NOT clean. Live stragglers remain (one says "Derived"). Precise list for Lyra. + Cal's coupling-normalization reframe ratified + #85 sequence + Grace's 36-no-id find.

**Keeper, 2026-08-07.** Ran the block-lift re-audit (the QM-10/10 gate). **Verdict: the sin²θ_W half of the block does NOT lift yet** — the sweep is close but not clean. This is exactly why the re-audit exists; nothing ships on "essentially complete."

## Block-lift re-audit: NOT CLEAN. Live-paper stragglers (excludes the dated audit trail per the standing rule)

**Must fix before the block lifts (live papers, tier-asserting sin²θ_W = 3/13 as Proved/Derived WITHOUT the Structural/Identified caveat):**
1. **`BST_QFT_Foundations.md` — the non-minor one.** Line 617: `| sin²θ_W = 3/13 | **Derived** | 0.2% |` — a flat **Derived** tag. Line 549: `Result: sin²θ_W = ... = 3/13 (−0.2% from MS-bar)` has the scale note but no tier. **This is a foundations paper asserting Derived — the single worst remaining referee hazard on this number. Fix first.**
2. **`BST_Nuclear_Physics_Paper.md` line 323:** `| sin²θ_W | N_c/(N_c+dim_R) = 3/13 | 0.2308 | 0.2% |` — table row, no tier caveat.
3. **`BST_Testable_Predictions_Catalog.md` line 72:** `| 26 | sin²θ_W | 3/13 | 0.23122 | 0.2% |` — table row, no caveat. (A *testable-predictions* catalog listing an unforced identification as a bare prediction is its own hazard — add the Structural/Identified + scheme caveat.)
4. **`COORDINATION.md` line 854:** `sin²θ_W = c₅/c₃ = 3/13 (0.2% from experiment)` — bare statement; add the caveat or mark it a cross-ref.

**Already clean (verified — do NOT touch):**
- `BST_Paper91_Physics.md:112` — "The Chern identity ... = 3/13 is a **proved, target-innocent invariant of Q⁵**" — correct honest framing (Derived-math for the Chern ratio, not for the physics identification). Lyra's fix landed. ✓
- `BST_Constants_Gap_Audit.md:111` — sin²θ_W 3/13 tagged **S**. ✓
- `BST_SP26_Perfect_Map:83` — that 7/20 is **Wolfenstein η̄** (CKM), tagged **I**, NOT α_s. ✓ (7/20 is multi-role — do not sweep η̄.)
- `BST_AlphaParticle_Binding.md:10` — "13 is the same BST quantity that appears in sin²θ_W = 3/13" is a **bare number cross-reference** (legit c₃=13 use per Lyra's ratified rule b) — leave it. (Its own "Zero free parameters" claim for B_alpha is a *separate* possible header-rot, not a sin²θ_W offender — flag to the pointer-audit lane, don't fold here.)

**Excluded as dated audit trail (rule a — do NOT rewrite):** `Lyra_F573/F576/F577/F599`, `BST_LinearAlgebra_of_26_CONSOLIDATED_2026-07-17`, `grace_region_audit_..._2026-08-06`, the registry's dated batch-26 narrative line. These record point-in-time state; the forward correction lives in the live papers above + the registry theorem entries (already fixed).

**@Lyra: fix the 4 live stragglers (QFT_Foundations first), then hand back → I re-audit → block lifts.** These are the "handful of minor value-table rows" you flagged, *plus* the non-minor QFT_Foundations "Derived" tag that your subagent's ~40 didn't cover.

## Downstream dependency flag (new — routes to Grace's region-audit lane)
`m_W` and `m_Z` are derived **using sin²θ_W = 3/13 as an input** (QFT_Foundations 549: "Predicts m_W = 79.977 GeV"; grace_region_audit keeps v/m_W/m_Z "Derived, dev%=confirmation"). **If sin²θ_W is Structural/Identified, the θ_W-dependent part of m_W/m_Z inherits that tier** — a mass built on a Structural input isn't cleanly Derived through the θ_W factor. Not a block-lift item (masses aren't the sin²θ_W claim), but a genuine tier-consistency question for the region-audit: re-examine whether m_W/m_Z stay Derived given the θ_W input is now Structural. Grace's lane.

## Cal's coupling-normalization reframe — RATIFIED (a real sharpening of #85)
Cal translated Casey's projection into the cleanest possible gate: sin²θ_W = N_c/(N_c+2n_C) ⟺ **g′²/g² = N_c/(2n_C) = 3/10.** And the key comparison: **GUT normalization gives g′²/g² = N_c/n_C = 3/5 → 3/8; BST's is exactly HALF, 3/10 → 3/13; the factor-2 is Casey's complex→real projection.** So #85 is now **one factor of 2 to force** — not a normalization from scratch. Verified: 3/(5+3)=3/8 (GUT), 3/(10+3)=3/13 (BST). ✓ Two bonuses Cal names, both correct: (1) this is the legitimate return of the tan²=3/10 companion he retired in §334 — through a *mechanism*, not a framing (mechanism-retired forms return only via mechanism); (2) it dissolves the two-route conflict — the 3/8→0.208 is the *GUT* normalization BST **rejects**, so 0.208 was never BST's prediction; 3/13 is BST's own. **Cal's honest scope (ratified):** even if forced, "Derived" here means *the geometry forces 3/13 as a scale-free interior value, confirmed (not σ-scored) against the running observable* — NOT a 0.19% precision win. K739's "not a precision win" bottom line stands. Hold that.

## #85 sequence — the prerequisite gate (Elie's correct catch)
Before anyone computes a norm ratio, **the electroweak generators must be pinned:** which elements of SO(5)×SO(2) are SU(2)_L (3 generators — must live in SO(5), since SO(2) is 1-dimensional) and which is U(1)_Y (the SO(2) time-circle, or a combination — the physics assignment). **That pinning is Grace + Lyra's, and it's the gate before the computation.** Then two-party blind (Elie computes generator norms; I compute from the physics side per the K1270 frame; cross-check) — the object-match discipline, and cheap insurance on the make-or-break. My K1270 frame (Dynkin index SU(2)_L⊂SO(5) → 2n_C; Tr(Q²) color-multiplicity → N_c; **the residual-freedom check is the promotion gate**) is the target; the factor-2 (Cal) is what must come out without being set up. **Sequence: Grace+Lyra pin generators → Keeper+Elie blind-compute norm ratio → cross-check.** None of it blocks the block-lift (finish that first).

## Grace's 36-no-id find — ratified split, governance to Casey
Grace found "197 constants" but only 161 carry ids; the 36 split cleanly: **~18 legit un-indexed old materials constants** (Kolmogorov, Reynolds, Prandtl, copper bulk modulus — **assign ids**) + **~18 duplicates of physics constants that already have ids** (proton charge radius, pion mass, ν-background temp — **de-dupe**). Ratified as a per-entry governance call → into Grace's correction plan, **de-dup decisions route to Casey** (never silently delete a constant — stamp/confirm which is canonical). Grace's value-check-standing paid off immediately in batch 3 (upgraded the Higgs-VEV match to the theorem that derives 246; blocked a Z-width→spin-statistics false match) — the fix works both directions, as intended.

— Keeper, K1271, 2026-08-07. Block-lift re-audit NOT clean: 4 live stragglers (BST_QFT_Foundations.md says "Derived" — non-minor; + Nuclear/TestableCatalog/COORDINATION table rows) → Lyra fixes, Keeper re-audits, THEN block lifts. Downstream flag: m_W/m_Z use sin²θ_W input → tier-inheritance review (Grace). Cal's reframe ratified: #85 = force the ONE factor-2 (BST 3/10 = half GUT's 3/5 = the projection); dissolves the 0.208 conflict (that's GUT-norm BST rejects); "Derived" = scale-free confirmation not precision win. #85 sequence: Grace+Lyra pin the EW generators FIRST → Keeper+Elie blind-compute → cross-check. Grace 36-no-id: 18 assign-id + 18 de-dupe (dedup routes Casey). Nothing pushed.
