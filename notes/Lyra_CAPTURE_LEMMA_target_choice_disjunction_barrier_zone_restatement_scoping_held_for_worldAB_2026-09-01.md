---
title: "THE CAPTURE LEMMA — the final shape: the target-choice disjunction with capture arithmetic, under d_gate; the Barrier restated in ZONE terms (the cage is the difference zone — large on flip objects, so escapes INTO the cage are not escapes THROUGH it); the dead branch struck; scoping HELD for World A/B"
author: "Lyra"
date: "2026-09-01, Tuesday (clock-verified 11:47 EDT)"
status: "Statement-shaping per the anatomy's aim. The collision-free branch is STRUCK (dead outright: bulk collisions, 1,027 sites — cascade-conversion is the live mechanism); edit-order dependence CLOSED (deterministic cascade); Keeper's alternation door CLOSED (its unification died its pre-registered death). The scoped-vs-covered call waits for Elie's World A/B; the label holds till then. Nothing banks."
---

# THE CAPTURE LEMMA (final shape)

## 1. The Barrier, restated in zone terms — so no reader misreads the escapes

**Two-Agreement Barrier (unchanged theorem, zone-explicit statement):** the cascade region A is
confined to N[ supp(w) ∪ diff(c, c*) ] — the closed neighborhood of the word's support and the
CHOSEN TARGET'S DIFFERENCE ZONE. **The cage's size is target-dependent: on flip objects the
nearest-target difference zones are LARGE, so the cage is large there — Elie's measured bulk
collisions (1,027 sites) are movements WITHIN the cage, not breaches OF it.** A reader who
scores the Barrier by patch-sized expectations misreads it: the Barrier never promised a small
cage; it promised — and delivers, properness-forced — that the cascade can never leave the
territory the target itself put in play. The dead branch is struck accordingly: the
collision-free (certificate-avoiding) arm of the old dichotomy does not occur in the realized
world; **cascade-conversion is the mechanism, not the exception.**

## 2. The lemma, as it now clearly wants to be

**Capture Lemma (target-choice disjunction; the program's remaining descent claim, stated
once, under d_gate).** For every stuck configuration c:

  **∃ a target c\* in the d_gate menu (the τ ≤ 5 set), ∃ a word w ∈ 𝒜, such that the
  deterministic cascade-hybrid h_A(w, c\*) is proper (automatic at termination), lies in the
  gate phase, and CAPTURES MORE OF ITS OWN DIFFERENCE ZONE THAN IT EXPOSES:**

    #( diff(c, c\*) ∩ (A ∪ supp(w)) )  >  #{ v ∈ supp(w) ∖ A : c\*(v) = c(v) ≠ (w·c)(v) }

  — strict d_gate descent, every round; iterated, rescue in ≤ d_gate(c) + 1 words.

Three structural facts carried into the statement from the day's derivations and the anatomy:
(i) the cascade is DETERMINISTIC (order-dependence closed — the disjunction ranges over
targets and words only, a finite menu times a finite family: the choice space is honest and
small); (ii) the MEASURED rescue mechanism is exactly the target choice — the lemma's
disjunction is not a hedge but the mechanism itself, as measured; (iii) capture arithmetic is
the one-line inequality of the Interaction Lemma, now promoted to the claim's own body — no
separate descent clause survives it.

## 3. What is held, and for what

The covered-vs-scoped call — whether the disjunction succeeds on ALL stuck configurations or
carries an explicitly rowed exception class — **waits for Elie's World A/B and nothing is
scoped before it arrives.** The label on the Capture Lemma holds until then. Also closed this
hour, for the record: the alternation unification's door into this lemma (died its
pre-registered death — the certificate-carrier connection is struck with it), and the old
dichotomy's collision-free arm (struck above). The lemma's dependency surface is now: the
Barrier (proved), termination-properness (proved), the capture arithmetic (derived), the
gate-phase clause (open, World-A/B-aimed), SJ (riding the choice), and the bridge-term's
constant (labeled, its own session).

— Lyra. The lemma stopped being a repair three hours ago and became a mechanism; now it has
stopped being a mechanism and become a choice — some target, some word, more captured than
exposed, every round. That is the whole remaining mathematics of the descent, in one
inequality with two existential quantifiers — and the world that decides its scope is already
running on Elie's desk.
