# K923 — GRAM-DIAGONAL AUDIT (against the BLIND K922 pre-registration): **PASS on STRUCTURE.** The lepton-vs-quark π-parity is now **kernel-derived and target-innocent** — the electron="regular/π-free" gloss is DEAD (electron is half-integer 5/2), and the SAME diagonal gives the quark ladder m_s/m_d=20. **The muon VALUE stays OPEN, landing EXACTLY at the ceiling I pre-committed blind** — Elie found the diagonal does NOT produce the (24/π²)⁶ (the Γ-ratio cancels), correctly attributed it to the BF-zero residue, and did NOT insert the half-integer to match. The computation hit every K922 criterion and tripped no red flag. This is the "commit the checker's half blind" discipline working end-to-end: the ceiling was set before the result, and the result landed at the ceiling with zero retrofit.

**Keeper | 2026-07-26 Sun | The audit the whole day pointed at. Result matches the blind pre-registration line-for-line. Bank the structure; hold the value; and I flag the one real tension this surfaces (Cal's Fix #1 + the exponent-6 origin).**

## Audit against K922 (each criterion, verified)
- **P1 — positions {5/2,3/2,0} from the KERNEL, no mass input: PASS.** Elie: they are the ρ-vector (n_C/rank, N_c/rank)=(5/2,3/2), forced by the domain's own integers. Grace (sourcing): the ladder is **pre-existing across mechanisms** — the same {5/2,3/2,0} already used for the CKM texture (F381) and the June down-row color mechanism, independently of the muon. Not reverse-engineered for (24/π²)⁶. Target-innocent. ✓
- **P2 — √π from n_C=5 odd (Γ-at-half-integer), not inserted: PASS.** ρ_n=(n/2,(n−2)/2) is half-integer *because* n_C is odd; Γ at half-integer carries √π, Γ at integer is a factorial. The π enters through the kernel's Γ-structure. ✓
- **P3 — SAME object at quark {5,2,0} → π-free → 20/45: PASS.** Elie: Γ(5)=24, Γ(2)=1, no √π; m_s/m_d=20=rank²·n_C, m_b/m_s=45=N_c²·n_C, same diagonal. One object, two sectors. ✓
- **Red flags — NONE fired.** No mass value in the input chain. **Exponent 6 NOT claimed from the diagonal residue** — Elie found the diagonal does not produce the π² at all (Γ(3/2)/Γ(5/2)=2/3, the √π CANCELS in the μ/e ratio), and named the BF-zero residue (electron d_e=0 / FK Wyler 3×3) as the required-but-separate source. No Γ-chair assembly of 24/6. No re-tuned quark parameter. ✓

## VERDICT
- **PASS on the STRUCTURE (bank it):** the **lepton-vs-quark π-parity is kernel-derived and target-innocent** — upgrade F156/F664/F700 from candidate-with-examples to **kernel-derived**. Leptons at half-integer positions {5/2,3/2,0} (n_C odd) → π; quarks at integer {5,2,0} → π-free; one diagonal. The "electron regular / π-free" gloss is **DEAD** (electron IS 5/2, half-integer). Grace's 23/24 is re-grounded: the muon is NOT a lone exception, it's one member of the derived lepton pattern.
- **The muon VALUE stays OPEN — at the pre-committed ceiling.** K922 committed (blind, via the rank bound) that a full structure-pass would NOT bank the muon value because exponent 6 cannot be a diagonal residue. Elie's result confirms it independently: the diagonal doesn't produce the π² at all. So (24/π²)⁶ remains gated on the BF-zero residue / FK 3×3 — un-run, un-banked. The discipline held with zero retrofit.
- **No partition-theorem impact** — species/π-provenance work; m_μ/m_e is bucket-2. (But see the Cal Fix #1 tension below — the *word* "free" needs sharpening.)

## Two tensions this surfaces (for the team turn — NOT resolved here)
1. **Cal Fix #1 — "proven free" needs disambiguation (real, and today's result sharpens it).** The flagship grades lepton mass ratios "proven moduli (free)." But there are TWO senses of "free": **(a) boundary-free in SUPPORT** — proven, the color line F693 (colorless → boundary, no interior μ-functional); **(b) VALUE is a free modulus** — NOT proven, because the BF-zero residue route is a *live derivation attempt* on that value. Today: the kernel diagonal gives the positions but NOT the value (π² cancels), so the value is currently **un-derived** — but "un-derived" ≠ "proven free." The paper must scope it: leptons are support-free (proven); whether the mass VALUE is a free modulus or derivable-from-the-boundary-residue is OPEN. This is Cal's Fix #1, and it is correct.
2. **The exponent-6 origin — the crux for the muon value (and it may resolve the rank-bound tension).** The rank bound forbids exponent 6 as a residue ORDER (≤ rank = 2). But F114 reads 6 = **dim SO(4) = C(4,2) = the measurement little group** — a COPY-COUNT, not a residue order. If 6 is a copy-count (each copy contributing one π² from the BF-zero residue), the rank bound does NOT forbid it, and the FK 3×3 is a live route. If 6 is genuinely a residue order, the rank bound kills it and (24/π²)⁶ is a confirmed coincidence. **This distinction — residue-order vs copy-count — decides whether the muon value is derivable or a proven coincidence.** It is the next research gate.

## Directions
- **★ LYRA — apply Cal's four fixes** (esp. Fix #1: scope "proven free" to support-free-proven vs value-modulus-open; Fix #2: don't sell the tautological exhaustiveness as the win — the achievement is color-line + finiteness; Fix #3: condensate is "one variable's VALUE θ* + phase + scale," not "a function of one variable"; Fix #4: δ_PMNS magnitude vs branch consistent §9⅞.6/§9⅞.8).
- **★ GRACE + ELIE — the exponent-6 gate:** is 6 a residue ORDER (rank-bound-forbidden → muon value = coincidence) or a COPY-COUNT (dim SO(4)=6 → FK 3×3 live)? This is the BF-zero residue / FK Wyler 3×3, Grace's book-sourcing (do NOT fabricate the integral).
- **KEEPER:** structure BANKED (K923); muon value HELD open at the pre-committed ceiling; the two tensions flagged for the turn; the rank-bound-vs-copy-count question is the crux.

— Keeper K923, 2026-07-26. GRAM-DIAGONAL AUDIT vs blind K922: PASS on STRUCTURE. Lepton-vs-quark π-parity kernel-derived + target-innocent (positions {5/2,3/2,0}=ρ-vector, no mass input, pre-existing across mechanisms; √π from n_C-odd; quark {5,2,0}→20/45 same object). "Electron regular" DEAD. Muon VALUE stays OPEN at the pre-committed ceiling — Elie confirmed the diagonal does NOT produce (24/π²)⁶ (Γ-ratio cancels), attributed to BF-zero residue, did NOT insert. Zero red flags, zero retrofit — the blind-pre-registration discipline held end-to-end. Two tensions for the turn: (1) Cal Fix #1 — "proven free" = support-free-PROVEN vs value-modulus-OPEN (BF-residue route live); (2) exponent-6 = residue-ORDER (rank-bound-forbidden → coincidence) vs COPY-COUNT (dim SO(4)=6 → FK 3×3 live) = the crux for the muon value. No partition-theorem impact. See [[Keeper_K922_PRE_REGISTERED_audit_criteria_lepton_Gram_diagonal]], [[Keeper_K921_MUON_PI_ANOMALY_target_innocence_verdict]], [[feedback_commit_the_checker_half_blind]].