# F720 — The address-dig SEQUENCE (staging note, prompt-28d): the five threads are one discrete-series-address computation on D_IV⁵, so — committed now, before Cal's reduction level lands — here is the **single shared intermediate + the read-order**, so the dig is *instant and non-redundant* the moment the level arrives (no computing the addresses five times, no fumbling the sequence). This does not need Cal's level to write; it needs it to *run*. **Tier: operational plan only — no claim; each read carries its own already-committed blind gate (F713/F714/F718/F719).**

**Lyra, Tue 2026-07-28 11:00 EDT. Staging the linchpin moment. Everything is built and blind-committed; this just fixes the order so the five answers fall out of one computation.**

## STEP 0 (Cal, the linchpin) — the reduction level
Cal's reduction level = the Kac–Kazhdan/Shapovalov reduction point of the covering generalized-Verma N(λ) at the spinor-singleton hw (E₀ = 2, primary-pinned FG-2014). **This is the single input.** Everything below reads off it.

## STEP 1 (shared intermediate — compute ONCE) — the addresses (a,b)
From the reduction structure, extract the **discrete-series address (a,b) of each fermion mode** (the three lepton generations, the six quarks) — the radial position each sits at. *This is the one object all five threads use; compute it once, hand it to all five.* Guard: the addresses come from the reduction data (structural), NOT from the observed masses (K880).

## STEP 2 — the FIVE reads, each with its committed gate
| # | thread | read off the addresses | committed gate |
|---|---|---|---|
| 1 | **Generation count** | the **radical** of the contravariant form at those addresses → its **Jordan–Hölder length** (# fermion-family constituents) = 3 or the honest 4 | F713 (object) + F714 (JH-length invariant, structure-only) → Keeper §108 → §105 |
| 2 | **Muon promotion** | evaluate **Γ_Ω at the muon's address**; does it give (24/π²)⁶ target-innocently? | F719: address *derived* not *noticed* → Cal #27 (45, π² seductive); Keeper K967 S2–S5 |
| 3 | **Tau** | Γ_Ω / orbit→mass at the **tau's address**; does it produce **71** with no peek? | Elie blind-forward; sourced → DERIVED, else FITTED-held |
| 4 | **y_t / m_t Value** | the top's **radial overlap** with the spherical boundary condensate O at the top's (outermost) address; does it saturate y_t = 1? | F603/K964: Ceiling DERIVED already; this reads the Value |
| 5 | **Quark-precision lead** | **boundary-distance d(a,b)** for each quark → does ε(d) *predict* the 0.09%→8% blur ladder? | F718: quantitative ε(d) + instability cross-check + lepton null-control ε≈0 (Insight-tier) |

## Read-order (dependencies)
1 first (the count — it validates the address structure itself and is the load-bearing prize). Then 2/3/4 in parallel (independent evaluations at their addresses). 5 last (needs all quark addresses + the ε(d) fit). The **lepton null-control** (F718) cross-checks 2/3 against 5: leptons carry π (analytic Γ_Ω, F719) *and* stay sharp (ε≈0) — same addresses, two consistency checks.

## Discipline (carried)
Addresses from the reduction structure, not observed masses (K880). Every read keeps its blind gate (above). The count (1) is OFF the tier ladder until it returns 3/4. The muon (2) stays IDENTIFIED until the address is *derived* (F719). Nothing here is a result — it's the sequence so the results fall out cleanly and fast.

## Tiers / handoffs
- **@Cal** — you are STEP 0; the whole dig is one input from you. When the reduction level lands, I run STEP 1 (the addresses) once and hand them to all five.
- **@Elie** — STEP 1 is the shared compute (with me); then you take read #3 (tau blind-forward) and support #5 (the ε(d) numerics). One address table, not five.
- **@Keeper** — the sequence + the five gates are staged blind; #1 hits your §108→§105, #2 your K967 S2–S5. Nothing pre-judged.
- **@Grace** — your exact-argument π-check (K968) is the mechanism-type label that rides read #2 (is the muon's 24 the analytic Γ_Ω value at its address?) and #5 (leptons π-ful + sharp).
- **@Casey** — everything's built; this is just the order of operations so that when Cal's one number lands, the five answers — the generation count, the muon's exact spot, the tau, the top-quark Yukawa, and your quark-precision idea — fall out of a single computation instead of five, cleanly and fast. One dig, five reads, one input away.

Notes only; no toys/theorems claimed. F720: the address-dig sequence. STEP 0 = Cal's reduction level (the one input). STEP 1 = the discrete-series addresses (a,b), computed ONCE (shared intermediate, from reduction structure not observed masses). STEP 2 = five reads, each with its committed blind gate: (1) count = JH length of the radical [F713/F714→§108/§105]; (2) muon = Γ_Ω at its address [F719, Cal #27, K967]; (3) tau = 71 blind-forward [Elie]; (4) y_t = top radial overlap [F603/K964]; (5) quark-precision = ε(d) predicts the blur [F718]. Read-order: 1 → {2,3,4} → 5; lepton null-control cross-checks. Operational plan only, no claim; each read keeps its blind gate. — Lyra