# Elie — round 6 PREREG, toys 5804–5806 (written 2026-09-26 11:39 EDT, before any code)
Invariant first, then numbers. The invariant throughout: the CONJUGACY TYPE of an element of sl(2,ℝ), read from the sign of its Killing norm (for aT₃ + bT₁ + cT₂: a² − b² − c² > 0 elliptic, = 0 parabolic (nonzero), < 0 hyperbolic). No coordinate sign is quoted as the result.

## 5804 — the tilt toy (energies). Cal hashes; K1878 overlap is Cal's call.
Hydrogen, m = 1, radial sector ℓ: T₁ = ½(r p² − r), T₃ = ½(r p² + r), T₂ = r p_r (differential operators, symbolic).
Checks: [T's] close to so(2,1); r = T₃ − T₁ and r p² = T₃ + T₁ are the two PARABOLIC directions; Schrödinger × r ⇔ A(E)ψ = 2Zαψ with A(E) = (T₃ + T₁) − 2E(T₃ − T₁); Killing norm of A(E) = −8E ⇒ elliptic iff E < 0, parabolic iff E = 0, hyperbolic iff E > 0 (invariant); the tilt e^{θT₂} carries A(E) to √(−8E)·T₃; T₃-eigenvalue n ⇒ E_n = −Z²α²/2n²; the exact radial functions n = 1..4 satisfy A(E_n)ψ = 2Zαψ.
CONTROL: the isotropic oscillator: H itself is a fixed element of the so(2,1) (E is an eigenvalue, not a coefficient) ⇒ spacing linear (E = ω(2n_r + ℓ + 3/2)), never 1/n². The 1/n² needs E INSIDE the coefficients (the tilt).
BST: the same tilt applied to a lowest-weight sl(2,ℝ) module with lowest J-weight λ ∈ 5/2 + ℤ≥0 (the J-weights of H²(D_IV⁵) and of every summand H_{5/2+k}(D_IV⁴)): E(ν) = −m g²/(2ν²), ν = 5/2, 7/2, …
DIRECTION: the tilt is BST-compatible and yields a Coulomb-type ladder with ν half-integral (a quantum defect of −3/2 relative to hydrogen's integers), ratios E(ν)/E(5/2) = 25/49, 25/81, …; the scale m g² is NOT supplied by BST's sl(2) — TWO inputs (a mass and a coupling), or one energy. Hydrogen's integer n requires λ = 1 (the D_IV⁴ Wallach point), which is not in H²'s tower (5798).
KILL (K1878): if the construction outputs a ground energy of 5 Ry (68 eV) with no free input, it is K1878's lane and STOPs. Expect NOT: with g = α, m = m_e the ν = 5/2 ground energy is −(4/25) Ry.

## 5805 — the holographic (symmetry-breaking) operators D_IV⁵ → D_IV⁴
Model: holomorphic functions on the tube over the future cone of ℝ^{1,4} (Šilov = compactified ℝ^{1,4}); so(2,5) acting with parameter λ: P_μ = ∂_μ, M_μν, D = z·∂ + λ, K^μ = Q(z)∂^μ − 2z^μ(z·∂) − 2λz^μ. Self-check first: [K,P] ∈ span{D, M}, [K,K] = 0 on polynomials (else the model is wrong and nothing below counts).
Operator: D_k f = [Σ_j a_j (Δ′)^j ∂₄^{k−2j} f]_{z₄=0}, Δ′ the Lorentzian Laplacian on z₀..z₃. Solve for a_j (a₀ = 1) from intertwining with the tangential K^μ (μ = 0..3) on all polynomials to degree k + 3.
DIRECTION: a unique solution for each k = 0..4 (k = 3, 4 extra), matching an inflated Gegenbauer C_k^μ with μ = λ − 1 = 3/2 (λ − (n−2)/2 for n = 5? stated as a guess: my prior is μ = λ − (n−2)/2 = 1; I record BOTH candidates and let the computation choose). CONTROL: μ ± ½ fails intertwining. KILL: no nonzero solution at some k ≤ 2.

## 5806 — three times, spectra on truncations
Lowest-weight sl(2,ℝ) modules D⁺_λ, λ = 5/2 (and 7/2 control), truncated at N = 50, 100, 200, 400 in the J-basis. Elliptic J: spacing exactly 1 at every N (discrete). Parabolic P = T₃ + T₁ (Killing norm 0): lowest eigenvalue → 0⁺ and spacing → 0 as N grows (continuous [0, ∞)). Hyperbolic T₁: eigenvalues fill both signs, spacing → 0 (continuous ℝ).
KILL: a parabolic or hyperbolic truncation whose low-end spacing does NOT shrink with N (would mean discrete).
