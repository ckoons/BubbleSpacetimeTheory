# Grace — Boltzmann u/d law: scaffold staged for next session (Keeper's assignment, EOD-eve)
*2026-07-11. My clear #1 for next session, staged warm so I don't walk in cold. Derive the monotonic u/d crossing
(0.46 → 13.6 → 41.6) from the τ-gradient / info↔energy, tied to matter stability (the cold-regime intrinsic ordering).*

## The assignment (Keeper + Lyra + Elie all routed it to me)
The 2×3 frame ACCOMMODATES the gen-1 inversion (my inversion test passed structurally; Lyra F508 + Elie 4619 confirm
via the "ceiling-hung up ladder"), and it's a FEATURE (proton stability). But WHY down is intrinsically heavier when
the excitation is weak (the cold-regime ordering) is NOT yet derived. Derive the Boltzmann u/d law from first
principles.

## The structure (from Elie 4619 — the two ladders hang from opposite ends)
- **down** = FLOOR-anchored (m_d, bulk), gentle spacing s/d = (N_c+1)(N_c+2) = 20 (FK Pochhammer, N_c). Stands UP.
- **up** = CEILING-anchored (m_t = v/√2, forced boundary), steep spacing t/c = N_max = 137. Hangs DOWN.
- u/d per gen = (ceiling-ladder)/(floor-ladder). The cold rung (gen-1) is where the two anchors are closest → the
  ceiling-hung up bottom drops BELOW the floor-standing down bottom → m_u < m_d → inversion.

## KEY STAGING FINDING (so I don't chase the wrong form)
**u/d is NOT a single-τ Boltzmann.** log(u/d) = −0.78, 2.61, 3.73; gen-to-gen Δlog = 3.39, 1.12 — NOT constant (the
cold→warm jump is ~3× the warm→hot jump). So the law is NOT exp(τ·gen) with one τ. The τ-gradient must be
POSITION-DEPENDENT (steeper at the cold end) — OR the u/d law is genuinely (ceiling/floor) = two different ladders,
not a single thermal factor. **Do NOT fit a single-τ Boltzmann — it won't work. The crux is the two-anchor structure.**

## The approach to try next session
1. Write the down ladder from the floor: m_down(gen) ∝ (ν)_λ at ν=N_c (F506, forced) — {1, 20, 840}.
2. Write the up ladder from the ceiling: m_up(gen) = (v/√2) / [N_max-shell spacing] — {80556, 137, 1}⁻¹ read down.
3. u/d(gen) = m_up/m_down. Check it reproduces 0.46, 13.6, 41.6 (it should — it's the observed spacings).
4. The DERIVATION target: is the cold-regime ordering (m_u < m_d at gen-1) FORCED by the two-anchor geometry (floor
   vs ceiling at scale v), or does it need the absolute scale (v and m_d independently)? Tie to matter stability:
   the proton is stable IFF m_d > m_u at gen-1 IFF the ceiling-hung up bottom stays below the floor-standing down —
   a condition on v / m_d / the shell count. Derive THAT condition (info↔energy τ-gradient), not the bare numbers.
5. Guard: NOT a single-τ Boltzmann (staged finding above). The τ-gradient is the SWPP info↔energy (today's direction
   result) applied to the TWO ladders. Norm-square guard (1−r²)² if radii enter.

## Dependencies (both are other lanes' gates — my law is consistent-conditional on them)
- y_t = 1 forced independent of m_t (Lyra gate 1) — the ceiling anchor. If circular, the up ladder's absolute
  position is unset.
- weight→boundary-localization rigorous (Elie gate 2) — why up is ceiling-anchored. If it holds, the two-anchor
  structure is a mechanism.
My piece (the cold-regime ordering / u/d law) sits ON TOP of both; I derive the ordering CONDITION, they force the anchors.
