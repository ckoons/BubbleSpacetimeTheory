# Today — 2026-09-02, v9 at 12:19 (ROUND 103): the lock as a kernel element with a local core; the two KP tests; cut-at-distance-one derived.
**Keeper. Supersedes v8. Read after K1845. Investigate; the EOD recommendation stands unless one of three objects appears (a depth-3 witness; a two-swap Kempe exit at a lock; a fixed word that exits every far-bit-off bridge-fail).**

## ASSIGNMENTS (relay order: Elie → Grace → Lyra → Cal → Keeper)

**ELIE.** (1) **T1:** on all 349 locks, apply Kempe's two swaps (ζ then η, and η then ζ, as plain swaps, not the
commutator) — count insertions. Predicted 0. (2) **T2:** the bridge-fail configurations WITHOUT the far-chain
condition (~85 of 1,211): for each, the set of family words that exit; the minimum hitting set; whether Lyra's
named word is in every set. (3) Reconcile with Grace: 14,279/14,279 (bridge-stuck images) vs 93% (c₀ bridge-fail)
— same predicate, two populations; state both. (4) n = 25 status. FINAL = T1 count; T2 table; reconciliation.

**GRACE.** (1) **The kernel instrument:** for every lock, the component of X₃ − C containing the near copy — its
size, its vertex set relative to the link (distance profile), and the extra kernel dimension of L(X₃ − C) vs
L(X₃); then after each exiting first word: does the kernel element vanish (X₃′ reconnected) or does C′ = ∅?
Tabulate. (2) Cut distance-to-link on all 349 and on the 14,279 bridge-stuck images (extend 5610's "always
exactly 1"). (3) Register KP on Cal's pass; the far-chain condition as a necessary-condition row with both
population counts. FINAL = kernel table; distance table; rows.

**LYRA.** (1) Derive **cut ⊆ N(link)**: the cut is X₃ ∩ X₄ at r-vertices; X₃ is seeded at B₂ (s_i) in c₂, X₄ at
B₁ (s_j) in c₃; why must their r-intersection sit at distance one from the link? (Confinement/Forced-Excision
for stages 3–4.) (2) Name, before T2 lands, the family word predicted to exit when the far bit is off (KP's
prefix in a non-commutator word), with its kill. (3) The far-chain necessity derivation citing which words'
failures (Cal's questions), or its floor. FINAL = the distance lemma or its floor; the named word; the
necessity status.

**CAL.** (1) Cold read of Theorem KP. (2) Pre-score T1 and T2 (what zero/nonzero and fixed/scattered each
prove). (3) Rule "purity by fragmentation is not classification" in one paragraph for the paper. (4) Read the
kernel reading (K1845 §1): is "the lock is a kernel element of L(X₃ − C)" an honest restatement of Lemma T or an
over-reach? FINAL = numbered sections.

**KEEPER.** K1845 filed. Verify T1/T2 from the artifacts. Board, rubric, sundown at EOD on Casey's word.

**CASEY.** The EOD recommendation stands. The DM-row contradiction (K1844) is off-lane and owned by Lyra + Grace
for the Writeup context.

## STANDING
No EOD before 5pm. Two instruments per number. Purity by fragmentation is not classification. Nothing says
"proof." The null travels. The key has three columns.

*— Keeper. Round 103: the lock is a kernel element with a local core; two tests from Kempe's own chains decide what necessity means.*
