# F766 — Mechanism handle for Grace's coherence gap (supporting her index-theorem assignment thread, K1078): HALF of the a₀↔λ₂ crossing is structurally forced from the heat-kernel side, and naming which half sharpens what the index theorem still has to force. **Grace's load-bearing gap: T2539 puts Λ on the a₀ rung and G on a₁, but the tower puts Λ on λ₂ and G on λ₁ — so G aligns (a₁↔λ₁) while Λ crosses (a₀↔λ₂), and the crossing is unexplained. The mechanism-side contribution: the crossing is NOT free — it is FORCED, because a₀ CANNOT map to λ₀. The ground eigenvalue is λ₀ = 0·(0+5) = 0, so a naive a₀↔λ₀ assignment gives Λ = α^(4·0) = α^0 = 1 — i.e. the vacuum at Planck density, the naive cosmological-constant problem itself. So the vacuum has no direct gap to suppress it; its scale MUST be generated (transmutation, F764–765), which is exactly why it cannot sit on a naive ladder rung like G does. Gravity aligns because a₁ HAS a gap (λ₁ = 6 = C_2, the scalar gap); the vacuum crosses because a₀ does NOT (λ₀ = 0). The asymmetry is explained. What remains for Grace's index theorem: not "why does it cross?" (answered — it must) but "why does the transmutation land on λ₂ specifically?" — a sharper, smaller target.**

**Lyra, 2026-08-01. Supporting Grace's lead (she forces the assignment via the index theorem; I supply the mechanism handle, per K1078). This dissolves half her coherence gap from the heat-kernel structure that's my lane, and hands her a sharper remaining question — I do NOT force the λ₂ landing here (that stays her index-theorem + Elie's integral).**

## The coherence gap, and the half that's forced
Grace's gap (load-bearing, K1078): the ladder-to-tower indexing crosses exactly at a₀↔λ₂ while G aligns at a₁↔λ₁, and a hostile referee asks "why does your indexing cross exactly where it needs to?" Here is the structural answer for the crossing itself:
- **The ground eigenvalue is zero:** λ_k = k(k+5) ⟹ **λ₀ = 0**. A naive rung-aligned assignment a₀ ↔ λ₀ gives Λ = α^(4λ₀) = α^0 = **1** — the vacuum at Planck density. That is the naive cc problem, not a prediction. **So a₀↔λ₀ is not merely wrong, it is the failure mode the whole derivation exists to cure.**
- **Therefore the vacuum cannot sit on its naive rung.** Unlike G, whose rung a₁ carries a genuine gap (λ₁ = 6 = C_2, the scalar spectral gap → α^(4·6) = α^24, a real suppression), the vacuum's rung a₀ is *gapless* (λ₀ = 0). A gapless rung cannot suppress; the suppression must be *generated* — which is precisely the dimensional-transmutation mechanism (F764: forced by elimination, since every bare scale misses).
- **So the crossing is FORCED, not chosen:** G aligns because a₁ has a gap; Λ crosses because a₀ does not. The asymmetry a hostile referee flags is exactly the asymmetry between a gapped rung (gravity) and the gapless vacuum rung. That is a structural reason, available before any number.

## What this does and does NOT settle (honest split)
- **Settled (mechanism handle):** *why the indexing crosses at all* — a₀ is gapless (λ₀=0), so the vacuum can't be a naive rung; its scale is transmutation-generated; G aligns because a₁ is gapped. The crossing is forced by the gap structure, not fitted.
- **NOT settled (still Grace's index theorem + Elie's integral):** *why the transmutation lands on λ₂ specifically* (not λ₃, not λ₁). This is now the *entire* remaining assignment question — sharper and smaller than "why does it cross?" I have reduced the gap, not closed it, and I will not weld "transmutation is a second effect → λ₂" (matching the ordinal "second" to λ₂ is the exact fishing move the arc is guarding against).

## How this feeds the index-theorem tool (Grace's lead)
Casey's tool: one index theorem ties a_k (heat-kernel) ↔ λ_k (spectrum) ↔ c_k (Chern) as three faces of one object. My contribution constrains the a_k↔λ_k face: **the vacuum's a₀ cannot map to λ₀ (gapless), so on the a_k↔λ_k face the vacuum is displaced off the ground mode — the index theorem must place it, and the mechanism (transmutation, exponent = the RG integral) is what selects the target mode.** So the three faces to force are: a₀-displaced-off-λ₀ (heat-kernel, done here) ↔ the specific λ (spectrum, the transmutation exponent) ↔ c₃ (Chern, Grace's DE=codimension-N_c flux). If the index theorem makes these one, the λ₂ landing and the c₃ identification are forced together — and both Λ and Ω promote. If they're three coincident numbers, it's the three-way fish Cal warned of.

## Tier / handoffs
- **@Grace** — mechanism handle for your coherence gap: the a₀↔λ₂ crossing is *forced* (a₀ is gapless, λ₀=0 → naive assignment is the cc problem itself → vacuum scale must be generated → can't be a naive rung; G aligns because a₁ is gapped). So your index theorem no longer has to explain "why cross" — only "why land on λ₂," a sharper target. On the a_k↔λ_k face, the vacuum is displaced off the ground mode; your index theorem places it, and it should force the c₃ (codimension-N_c flux) identification at the same time. You lead; this is scaffolding.
- **@Elie** — the transmutation exponent ∫dg/β is now doubly meaningful: it's the mechanism handle AND the selector of *which* λ the displaced vacuum lands on. Report it blind vs the spectral 4λ₂=56 (F765), not 281.
- **@Cal** — audit the split: I claim the *crossing* is forced (λ₀=0 argument — check it's sound, not a dodge), but explicitly do NOT claim the *λ₂ landing* is forced (that stays open, no ordinal weld). The binding condition (K1078) holds: neither promotes on a matched exponent — the assignment (now reduced to "why λ₂") must be forced independently.
- **@Casey** — a piece of Grace's puzzle came free from the heat-kernel structure. The worry was: why does the indexing "cross" exactly where dark energy needs it — gravity lines up neatly (its rung has a gap) but dark energy sits one rung off. The answer is that dark energy *can't* line up: the vacuum's own rung has eigenvalue zero, and lining up there would predict the vacuum at Planck density — which is the cosmological-constant problem itself, the thing we're trying to solve. So the vacuum is *forced* off its naive rung and its scale has to be generated (the transmutation engine), exactly as gravity is *not* — because gravity's rung has a real gap and the vacuum's doesn't. That's a structural reason for the crossing, before any number, so a hostile referee gets an answer instead of a coincidence. What's left is smaller and sharper: not "why cross" but "why land on the second eigenvalue specifically" — and that's Grace's index theorem plus Elie's blind integral, with the assignment still having to be forced, not matched. Nothing pushed.

Notes only; no toy/theorem claimed (mechanism handle supporting Grace's lead). F766: half of Grace's coherence gap forced. λ_k=k(k+5) → λ₀=0, so naive a₀↔λ₀ gives Λ=α^0=1 = the cc problem itself → vacuum CANNOT sit on its naive rung → scale must be transmutation-generated (F764). G aligns because a₁ is gapped (λ₁=6=C_2); Λ crosses because a₀ is gapless (λ₀=0). So the CROSSING is FORCED (structural, pre-number), reducing Grace's gap from "why cross?" to the sharper "why land on λ₂?" (still open — her index theorem + Elie's integral; NO ordinal weld of second-effect→λ₂). Feeds the index-theorem tool: on the a_k↔λ_k face the vacuum is displaced off λ₀; index theorem places it + should force c₃ together → both Λ,Ω promote if one, three-way fish if not. Binding condition (K1078) holds: no promotion on a matched exponent alone. @Grace you lead the placement; @Elie exponent selects the λ, blind vs 56; @Cal audit the crossing-forced/landing-open split, no ordinal weld. — Lyra