Team wake — Friday... Saturday 2026-07-04. The forward ζ-computation day.

Happy Fourth. One computation is at the front of the board today, and it decides the down-ladder either way — no reverse-reading. SOD check first.

## Where we stand (honest, from yesterday)

- **Count 8.** Derived-strong ~2 firm (θ_QCD exact, θ₁₃/θ₁₂) **+ a strong new lead: the charge-radius receipt.**
- **POSITIVE (K653):** the confined-field Casimir frame predicts mass ~ ℏc/R, and it landed — **(m_p/N_c)·R_p = (rank²/N_c)·ℏc to 0.04%** (R_pred 0.8412 vs 0.8409, muonic-H). Coefficient **C_F = 4/3 = rank²/N_c** (QCD color Casimir + clean substrate form). Confirmed on the *radius* — data independent of masses. **Pending: the forward ζ-computation to derive C_F rather than recognize it.**
- **NEGATIVE (K651):** the down-ladder mechanism (d(ν)×k_s) ran in full and came up short by exactly **2·C_2 = 12** — reported a strong lead (1–2% match), NOT banked (Grace committed k_s blind → no retrofit).
- **SELF-CORRECTION (K652, validated by Elie+Lyra):** the radius win (C_F, Layer-2 dressing) does NOT close the down-ladder (2·C_2, Layer-1 bare) — two Casimirs, two scales, 16× apart.

## THE frontier — the forward ζ-computation (Grace + Elie + Lyra)

The confined-field Casimir energy is **E = (ℏc/R)·Z**, Z = the ζ-regularized spectral sum over the colored quark boundary (lowest Casimir eigenvalue = C_2). Compute Z **forward.** It settles two things at once:

1. **The radius, derived forward:** does Z yield **C_F = 4/3**? If yes, the charge-radius receipt goes from *recognized* to *derived-strong* (a real bank on independent data).
2. **The down-ladder, un-fishable pass/fail** — Lyra's identity **2·C_2 = N_c² · C_F** (12 = 9 × 4/3). The whole ladder hinges on ONE question:

> **Is the extra N_c² an independent color multiplicity of the glue field, or the bare-frame N_c² double-counted?**
> - independent → m_s/m_d = (5/3)(12) = **20** — ladder CLOSES (down sector derived-strong)
> - double-count → m_s/m_d = (5/3)(4/3) = **2.2** — ladder FAILS (stays an honest negative)

**Derive it forward from the actual spectral sum — NEVER assume the N_c² because it makes 20.** The radius is the fish-detector: a fit gives only the mass; the real computation gives the radius (C_F) AND settles the N_c². Stated pass/fail, no reverse-read.

## Per-CI

- **GRACE (geometry):** the ζ-regularized spectral sum Z over the colored boundary (Shilov S⁴ spatial, color on the dual Q⁵ — your Lane-B). Does Z = C_F, and does its color structure carry an *independent* N_c²?
- **ELIE (numerics):** compute Z numerically; verify Z = C_F for the radius (forward, not recognized); check the extra-N_c² factor comes out independent or double-counted. Report both receipts (radius + ladder verdict) with numbers.
- **LYRA (color structure):** the C_F vs N_c²·C_F color-factor bookkeeping — is the ladder's second N_c² the glue-field color multiplicity or the bare-frame one re-used? Parallel: **force τ_commit = 2^{N_c}·g = 56** for Λ (forward, don't reverse-read; your color-Fock 8 lead); ν-scale coefficients; finish registry backfill (8 theorems, fresh reads — SOD still flags).
- **KEEPER:** adjudicate Z (forward-derived vs recognized/fitted); enforce the un-fishable gate (independent N_c² must come *from the spectral sum*, not from needing 20); hold the two-layer/K652 line; SOD first.
- **CAL:** post-landing cold-read. α tier-call reserved for Casey.

## Disciplines armed

- **Forward-compute, don't reverse-read** (Z, the N_c², τ_commit).
- **The radius is the fish-detector** — a real Casimir gives radius AND ladder; a fit gives only the mass.
- **Honest negatives are finished answers** — if the N_c² is double-counted, the ladder fails at 2.2 and that's a clean result.
- Simplest one-line-checkable forms (ad-AI-num armor) · Five-Absence · target-innocence · σ scheme-aware · cite-don't-claim · pre-audit.

## Standing for Casey

Forward ζ verdict (pending) · charge-radius receipt (strong lead → derived-strong if Z=C_F forward) · down-row (honest negative until the N_c² question resolves) · ratify the frames (two-layer + regime + freeze-depth/volume + Casimir-dressing-for-size) · σ-metric + cheapness ratification · α tier-call · CLAUDE.md line 0.5.

## EOD target

Z computed forward → (1) radius derived-strong-or-not; (2) the down-ladder N_c² question answered with a number (20 or 2.2), no reverse-read. Plus τ_commit forcing attempted, ν-coefficients, backfill. **This is the day the down-ladder either closes forward or fails cleanly — either is a real result.**

— Keeper, 2026-07-04 wake. One ζ-computation decides it: does the confined-field Casimir give C_F (radius forward) and an *independent* N_c² (ladder → 20), or a double-count (→ 2.2)? Compute forward, don't reverse-read. Happy Fourth.
