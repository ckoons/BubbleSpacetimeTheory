# K1860-P (09:17 EDT Mon 2026-09-07, clock) — the corrected sentences of K1860 A, B, H, J after Cal §857's three catches, so the substrate lane starts from true statements; and the ranking of the falsifier candidates with each one's map status

**Rule for this file:** every corrected sentence names what it replaces. Nothing in the branching (k/(2k+3), 3/7, the frame theorem, the threshold curve) changes — those were computed and are untouched by the catches.

## Results 4 (the "one bit") — replaced
WAS: "closed/realized = 2.0000 exactly … the substrate's decision cost is ONE BIT — the arrow." IS: **the ratio is 2 by construction** — "closed" was defined as the ±m symmetrisation of the realized cone, so it doubles the set; no independent closed set exists on Š (K1858) and no operation that could break the bit is named. The arrow is the choice of the Hardy sign, paid in the definition of the Hardy space, not a decision cost. (K1835's vacuity pattern; Cal's Q1.)

## A (the writing operator) — two sentences replaced, the rest stands
WAS: "no erasure is the theorem that the Hardy space is shift-invariant and the shift has no inverse." IS: **the write has no inverse in the algebra it generates, but its adjoint W_v* is a left inverse on H² (a contraction, not an isometry): erasure is possible as a LOSSY map — Landauer's shape, not impossibility.**
WAS: "Wold/Arveson: any such tuple = (unitary part) ⊕ (pure shift); the unitary part is the conserved dynamics, the shift is the writing." IS: **on H²(Š) the coordinate tuple (W₁,…,W₅) is a PURE spherical isometry — its Wold unitary summand is ZERO. The conserved fact is the identity Σ_j‖W_j f‖² = ‖f‖² (information conserved across the five directions jointly); there is no unitary summand to call "the conserved dynamics."** "Conserved AND ratchet" is one identity about a pure isometry, not a two-summand decomposition. Stands: multiplication by a coordinate raises frequency by one; the branching k/(2k+3) (checked at k = 1, 2); the family rule k/(2k+d−2); the leads (entropy per write h₂(k/(2k+3)); Arveson's curvature invariant of the tuple — now the natural invariant of a pure isometry, which is exactly Arveson's setting).

## B (the fold) — one paragraph withdrawn, the discriminator kept
WAS: "even m are functions on Š, odd m are sections of the nontrivial line bundle … a spin-statistics-shaped grading supplied by the geometry." IS: **WRONG. On Š = (S¹×S⁴)/ℤ₂ the mode e^{imθ}Y_k picks up (−1)^{m+k} under the half-turn, and L²(Š) was built with m ≡ k (mod 2) precisely so that this is +1: EVERY Hua mode, odd m included, is an honest function on Š. The twisted sections are the m ≢ k modes, which are not in L²(Š) at all. There is no geometric twist separating odd from even write counts; m mod 2 (= k mod 2) is a legitimate abstract grading and nothing more.** Consequences: dictionary (ii) ("matter = the twisted odd write count") loses its geometric support and survives only as a parity label; "three writes as a unit is odd" is a parity of a count, named, not identified. The discriminator stands as written: does the corpus's mass carrier (T2138's winding) sit on the K-invariant sector or on the odd-count sector? — a registry grep, not algebra. The 2 + 3 split, the phase-locked = full-winding identification, and the three-write unit's 3/7 (family 3/(d+2)) are unchanged.

## H (the ledger) — the DERIVED list corrected
REMOVE from DERIVED: "erasure impossible"; "Wold = conserved ⊕ ratchet"; "closed/realized = one bit (the arrow)". ADD to DERIVED: "the write tuple is a pure spherical isometry (zero unitary part); Σ‖W_jf‖² = ‖f‖²; erasure is lossy (the adjoint is a left inverse)". Everything else in H stands, including its verdict: **theory-shaped, NOT a theory — no statement can fail until WRITE ↔ PROCESS is written.** The Shannon extension is still the information theory of a pure isometry (append-only), which is Arveson's subject.

## J (carry and push) — the first sentence replaced; the challenge promoted
WAS: "Carry = the isometric part … the time flow rotates its phase (e^{imθ}) and preserves its norm — Wold's unitary summand." IS: **the time flow e^{imθ} is the SO(2) ⊂ K rotation acting on H² — a unitary, norm-preserving, but NOT a Wold summand of the write tuple (which has none). "Carry free" = the K-rotation is unitary; "push paid" = the Bergman → Hardy map is the one non-isometric step, cost 1/(m+2) in mean intensity (K1860-I, computed).** Single occupancy (rank 2 = one Jones vector ⟹ one photon = one word ⟹ word = frame = n − 2 = 3) stands as derived GIVEN the dictionary. **The Compton-trend challenge is promoted from "owed" to the lane's first falsifier shape:** the push cost 1/(m+2) FALLS with word length while Compton's transfer ΔE/E ≈ E/mc² GROWS with photon energy — so either m is not photon energy (m ↔ cycles, i.e. wavelength, flips the trend) or the push is not the recoil. This is the first place the dictionary can be WRONG rather than unnamed.

## The ranking of the candidates for today's sentence (Cal pre-scores blind; this is the owner's map-status column)
| # | candidate | what the map must fix | my status guess (can be wrong) |
|---|---|---|---|
| **C5** | the Compton trend (J's challenge) | one assignment: m ↔ photon energy or m ↔ cycles | the cheapest decidable item; if m ↔ energy, ALREADY REFUTED by Compton; if m ↔ cycles, a quantitative sentence follows (transfer per photon vs frequency) — TESTABLE against Compton data |
| C2 | the tick, 137 turns = 1.1 as | which observable "delay" is (a floor / a quantum / a rate α·ν_C per electron) | floor: CONSISTENT (20 as > 1.1 as, Cal); quantum: NOT YET MEASURED at that resolution; rate bound: possibly ALREADY REFUTED by strong-field ionization rates — the map decides which sentence the dictionary actually makes |
| C1 | push cost 1/(m+2) as 2.72 eV | whether 1/(m+2) is an ENERGY or an INTENSITY fraction (K1860-I says "mean intensity") | probably a category error as I wrote it in the prompt — Lyra says which; if intensity, no Landauer sentence exists |
| C3 | "circular light cannot complete a winding" | what "winding" is in the laboratory | RETIRE unless a quantity is named; circular dichroism exists |
| C4 | one photon = one word = 3 | a process where the 3 is a count QED gives otherwise | likely a REPRESENTATION (control), not a sentence |

— Keeper. K1860's A/B/H/J now read with this file; the original is untouched except its pointer.
