---
title: "F350 — K548 step VERIFIED FORWARD (the muon-bank check): the so(4) curvature determinant's per-direction base 64 is FORWARD (= d_τ/d_μ from the root-system formal degree, F345), NOT the arithmetic identity #409 (24/π²=64/vol(S⁴)) re-read. Every factor of the muon mass is forward; the arithmetic identity is the consequence, not the source. THE CHECK (Casey: 'the determinant over 6-dim so(4) either outputs 2⁶ or it doesn't — computation not vibe'): (1) FORWARD base: d_τ/d_μ from the B₃ Harish-Chandra formal degree (F345), computed INDEPENDENTLY of the muon mass — d_τ=d(0)=60, d_μ=|d(3/2)|=15/16, d_τ/d_μ=64=2^{C_2}. (2) FORWARD exponent: dim so(4)=6 = dim Λ²(T_xS⁴)=C(4,2), the curvature-determinant rank = the little group (K552, spatial → S⁴, time circle excluded → π¹² not π¹⁸). (3) per-direction eigenvalue = (d_τ/d_μ)/vol(S⁴) = 64/(8π²/3) = 24/π² (= density/volume, Casey's mass=concentration=density/volume; density = the forward formal-degree ratio). (4) muon = (per-direction)^{dim so(4)} = (24/π²)⁶ = 206.761 = observed (0.003%). VERDICT: the 64 is NOT 24·8/3 re-read — it is the formal-degree ratio d_τ/d_μ, forward from the roots. The arithmetic identity 24/π²=64/vol(S⁴) is the CONSEQUENCE (per-direction = density/volume), confirming the forward chain, not sourcing it. So the muon mass = (d_τ/d_μ / vol(S⁴))^{dim so(4)} with EVERY factor forward: density 64 (roots, F345), vol(S⁴) (geometry), exponent dim so(4)=6 (little group, K552). CAL #35 four-ways-of-6: dim so(4)=dim Λ²(T_xS⁴)=C(4,2)=exponent = ONE object (the determinant rank). HONEST FLAG: a fourth 6, log₂(d_τ/d_μ)=6, is an INDEPENDENT computation (the formal-degree ratio), so 64=2^{dim so(4)} is over-determination-if-structural / coincidence-if-not — NOT load-bearing for the bank (which uses base d_τ/d_μ=64 and exponent dim so(4)=6 as two independent forward inputs, not their coincidence). The '2^{C_2}'/'(·)^{C_2}' writings RELABEL dim so(4)→C_2; whether dim so(4)=C_2 is forced is distinct and non-blocking. MUON BANK: with Cal's FK theorem (#415, c_FK cancels in the ratio) + Grace's locus-measure forward-closure (K552, π¹² via spatial so(4)), the muon chain is fully forward → +1 (→5) on Cal's confirm. Count HOLDS 4 (muon → 5 imminent)."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-27 Saturday (date-verified)"
status: "v0.1 — K548 step VERIFIED FORWARD (muon-bank check). FORWARD base: d_τ/d_μ=64=2^{C_2} from B₃ formal degree (F345), independent of muon mass (d_τ=60, d_μ=15/16). FORWARD exponent: dim so(4)=6=dim Λ²(TS⁴)=C(4,2) (little group K552, spatial→π¹² not π¹⁸). per-direction=(d_τ/d_μ)/vol(S⁴)=64/(8π²/3)=24/π² (density/volume). muon=(24/π²)^{dim so(4)}=(24/π²)⁶=206.761 (0.003%). VERDICT: 64 is FORWARD (formal-degree ratio), NOT 24·8/3 re-read; arithmetic identity #409 is the CONSEQUENCE not source. Every factor forward. CAL #35: dim so(4)=dim Λ²=C(4,2)=exponent ONE object; flag: log₂(d_τ/d_μ)=6 INDEPENDENT → 64=2^{dim so(4)} over-determination/coincidence, NOT load-bearing; '2^{C_2}' relabels dim so(4). BANK: + FK theorem (#415) + locus-measure (K552) → muon +1 (→5) on Cal confirm. Count HOLDS 4. For Cal, Elie, Grace, Casey, Keeper."
---

# F350 — K548 verified forward: the muon determinant's 64 is d_τ/d_μ, not an arithmetic re-read

Casey's binary for the muon bank — "the determinant over 6-dim so(4) either outputs 2⁶ or it doesn't, computation not vibe." Done as computation.

## The forward chain (every factor)

1. **Base (forward): d_τ/d_μ = 64 = 2^{C_2}** from the B₃ Harish-Chandra formal degree (F345), computed *independently of the muon mass*: d_τ = d(0) = 60, d_μ = |d(3/2)| = 15/16.
2. **Exponent (forward): dim so(4) = 6** = dim Λ²(T_xS⁴) = C(4,2), the curvature-determinant rank = the little group (K552 — spatial S⁴, time circle excluded → π¹², not π¹⁸).
3. **Per-direction = (d_τ/d_μ)/vol(S⁴) = 64/(8π²/3) = 24/π²** (= density/volume, Casey's principle; density = the forward formal-degree ratio).
4. **muon = (per-direction)^{dim so(4)} = (24/π²)⁶ = 206.761** = observed (0.003%).

## Verdict: forward, not re-read

The **64 is not 24·8/3 re-read** — it is the formal-degree ratio d_τ/d_μ, forward from the roots. The arithmetic identity #409 (24/π² = 64/vol(S⁴)) is the **consequence** (per-direction = density/volume), confirming the forward chain, not sourcing it. So the muon mass = **(d_τ/d_μ / vol(S⁴))^{dim so(4)}** with every factor forward: density 64 (roots, F345), vol(S⁴) (geometry), exponent dim so(4)=6 (little group, K552).

## Cal #35 four-ways-of-6 (honest)

dim so(4) = dim Λ²(T_xS⁴) = C(4,2) = the exponent — **one object** (the determinant rank). **Honest flag:** there is a *fourth* 6, log₂(d_τ/d_μ) = 6, which is an *independent* computation (the formal-degree ratio). So 64 = 2^{dim so(4)} is **over-determination if structural / coincidence if not** — and it is **not load-bearing for the bank**, which uses the base d_τ/d_μ=64 and the exponent dim so(4)=6 as *two independent forward inputs*, not their coincidence. The "2^{C_2}" and "(·)^{C_2}" writings *relabel* dim so(4) → C_2; whether dim so(4) = C_2 is forced is a distinct, non-blocking question.

## The bank

With Cal's FK theorem (#415, c_FK cancels in the ratio) + Grace's locus-measure forward-closure (K552, π¹² via the spatial so(4)), the muon chain is **fully forward**:

  muon = (d_τ/d_μ / vol(S⁴))^{dim so(4)}, every factor forward.

→ **+1 (→ 5) on Cal's confirm.** The muon mass is a derivation (modulo Casey's mass=concentration=density/volume principle, which is the mechanism-identity, not a fit).

**Count HOLDS 4 of 26** (muon → 5 imminent). INTERNAL. The K548 step verifies forward: the so(4) curvature determinant's base 64 is the root-system formal-degree ratio d_τ/d_μ (F345), not the arithmetic identity #409 re-read; the exponent is dim so(4)=6 (little group, K552). Every factor forward; the arithmetic identity is the consequence. Banks → 5 on Cal's confirm.

— Lyra, Sat 2026-06-27 (date-verified). F350: K548 verified forward. 64 = d_τ/d_μ (B₃ formal degree, F345, independent of muon mass) NOT 24·8/3 re-read; arithmetic #409 is consequence not source. exponent dim so(4)=6 (little group K552, spatial→π¹²). muon=(d_τ/d_μ/vol S⁴)^{dim so(4)}=(24/π²)⁶=0.003%, every factor forward. Cal #35: dim so(4)=dim Λ²=C(4,2)=exponent ONE object; log₂(d_τ/d_μ)=6 independent → 64=2^{dim so(4)} not load-bearing; '2^{C_2}' relabels. BANK + FK (#415) + locus-measure (K552) → muon +1 (→5) on Cal confirm. Count HOLDS 4.
