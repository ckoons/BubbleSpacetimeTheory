# Elie PREREG — Toy 5776: the region-count toy for the 09-15 resolution-limit marker (round-3 addendum, Casey's word). Written 2026-09-22 12:15 EDT, BEFORE the published bound is read — the bound is the hidden target of the can-fail. Cal hashes the rule; I read the bound after his line.

## 0. The rule, stated from the ledger picture and nothing else
K1919 §7's ledger: the committed count grows like an area, d ln N/dt = 2H ⇒ N ∝ a², normalised on the horizon as N_H = A_H/ℓ_P² (= 4π(c/H₀)²/ℓ_P² = 9.06 × 10¹²² with H₀ = 67.4, ℓ_P² = ħG/c³ — Keeper's 9 × 10¹²²). **Rule R: the commitment-cell count of a laboratory region is the area of its boundary in Planck units with the SAME coefficient as the horizon count: N(region) = A(∂region)/ℓ_P².** No horizon quantity enters N(region); for a sphere of radius R, N = 4πR²/ℓ_P². The coefficient 1 is fixed by the horizon normalisation already on the record; the 1/(4 ln 2) (bit-per-Bekenstein-area) variant is REPORTED beside it and changes 1/√N by a factor 1.67 — irrelevant to any verdict below, said now.

## 1. The predicted floor (the marker's statistics, before any number)
Closings of commitment cells are a counting process: variance = mean, so a quantity carried by the region's record fluctuates about it with relative floor **σ_rel = 1/√N(region)** — bounded, time-INDEPENDENT (does not accumulate over the measurement time T), scale-fixed by R alone, shrinking as the region grows. The CQ master equation's signature is the opposite: Gaussian noise whose variance grows ∝ D·T with a free coefficient D. The discriminator is the T-scaling, and it has opposite signs; that sentence is the marker, not a detection claim.

## 2. Predictions (hashed; the bound's numbers are NOT in this file)
- P1 (arithmetic): for R = 1 m, N = 4.8 × 10⁷⁰, σ_rel = 4.6 × 10⁻³⁶; for a 1 kg tungsten/gold-class test mass (R ≈ 2.4 cm), N ≈ 2.8 × 10⁶⁷, σ_rel ≈ 6 × 10⁻³⁴; for a 10 μm silica microsphere, N ≈ 4.8 × 10⁶⁰, σ_rel ≈ 4.6 × 10⁻³¹.
- P2 (the can-fail, stated as the ORDER it is): the floor exceeds the published bound only if σ_rel(region) > the relative precision at which the bound's experiment reads the region's mass or potential. **I predict it does NOT fire by ≳ 20 orders of magnitude** (laboratory mass/G precision is ~10⁻⁸–10⁻¹⁰ relative; the floor is 10⁻³¹–10⁻³⁴), so the resolution-limit floor is invisible to every experiment that bounds CQ diffusion, and the honest sentence is: BST's floor is consistent with the bound trivially; the marker's content is the T-scaling, which no existing bound tests.
- P3 (can fail): Oppenheim et al. 2023's bound is on a DIFFUSION coefficient (variance ∝ T); expressed as a mass-measurement variance it is a rate, so the comparison to a T-independent floor requires naming T — the toy states the T at which the bound's experiment ran (from the pin) and compares at that T only; a floor "beating" a diffusion bound at one T is not a statement at another T. This is the register's Section-D form: two markers with opposite T-slopes.
- P4 (control): the rule reproduces Keeper's horizon count when the region is the Hubble sphere: N(R = c/H₀) = 9.06 × 10¹²² (the normalisation, not a prediction).
- P5 (control): the rule cannot be stated FROM the target — the bound's number does not appear in §0 or §1; Cal's hash certifies it.
- Verdict line the toy prints: rule R; N and σ_rel for the three regions; the pinned bound's form, experiment and T (Grace's pin, read after the hash); the ratio floor/bound at that T; the marker sentence.

## 3. Not claimed
No claim that quantum randomness is explained away (Ch 02's tier line); no "BST kills post-quantum gravity"; the honest form is Lyra's R152 sentence — he posits the noise and bounds it; we derive the form of the randomness we have and pre-register the test.
