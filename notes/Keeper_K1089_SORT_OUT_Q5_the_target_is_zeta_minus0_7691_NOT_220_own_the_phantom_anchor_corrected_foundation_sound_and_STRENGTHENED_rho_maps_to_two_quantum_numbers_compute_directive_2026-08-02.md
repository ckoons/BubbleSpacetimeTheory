---
node_type: k_audit
id: K1089
title: SORTING OUT the Q⁵ correction (Casey: "make sure our work is based on Q⁵ correct; get the team to compute properly — we know the gates, we need correct calculations"). THE CORRECTION CHAIN, owned fully including my part: (1) OPERATOR S⁶→Q⁵ — the vacuum is the rank-2 complex quadric Q⁵=SO(7)/[SO(5)×SO(2)], NOT the rank-1 sphere S⁶=SO(7)/SO(6) (Grace's catch, confirmed). (2) TARGET 220.64→−0.7691 — MY OWN K1087/K1088 "safe anchor 220.64" was ALSO a phantom: Elie found 220.64 was ã₅ (the UN-normalized Seeley-DeWitt heat integral), NOT ζ(0). The genuine target is ζ_{Q⁵}(0) = −0.7691 (Elie, direct spectral sum on the rank-2 spectrum, calibrated against Grace's S⁶ ζ(0)=−0.70). I own that my anchor was wrong — the auditor's "safe number" was a mislabeled coefficient. THE CORRECTED FOUNDATION IS SOUND AND STRENGTHENED: the genuine Q⁵ spectrum λ_{a,b}=a(a+n_C)+b(b+N_c)=a(a+5)+b(b+3) has two shifts (5,3)=(2ρ₁,2ρ₂) with ρ=(5/2,3/2) — so the TWO quantum numbers map onto the TWO ρ-components (Lyra's hypothesis CONFIRMED; gate (b)'s shift-tension, awkward crammed onto one sphere index, likely falls out naturally on rank-2). The old k(k+5) sphere tower is exactly the b=0 slice. SURVIVES (spectrum-independent): ρ=(5/2,3/2), Γ_Ω, the transmutation mechanism (F764), channel-separation (F757), the structural "vacuum forced off the gapless ground mode." COMPUTE DIRECTIVE: (a) Elie/Cal deliver the FULL 2-index multiplicities from SO(7)↓SO(5)×SO(2) branching; (b) confirm ζ_{Q⁵}(0)=−0.7691 via BOTH the direct sum AND the properly-normalized Gilkey (a₅/(4π)^{d/2}−dim ker) — they must agree; drop 220.64; (c) Grace re-runs Barnes–Gindikin on the 2-index spectrum targeting −0.7691, ρ mapping to the two indices; (d) redo step-1 norm-form + the α^(4λ_k) hierarchy on Q⁵. BLAST RADIUS: REDO on Q⁵ = determinant/ζ-sum + step-1 + hierarchy (4λ₂=56, λ₂=14) + coarse-menu (all used the b=0 sphere slice). LIKELY SAFE = the MUON VALUE (24=Γ(5) is a Gindikin-Gamma value, root-data / Γ_Ω-based, NOT the spectral sum) — but flag its k-index PLACEMENT (electron@k=1, §118-rung) for a light re-check. Both Λ and Ω stay PD.
date: 2026-08-02
author: Keeper
verdict: SORTED. Operator = Q⁵ rank-2 (a(a+5)+b(b+3)); target = ζ_{Q⁵}(0)=−0.7691 (NOT the phantom 220.64 = un-normalized ã₅ — my own anchor was wrong, owned). Foundation SOUND + STRENGTHENED: the two shifts (5,3)=2ρ confirm the two quantum numbers map to the two ρ-components (Lyra). Survives: ρ, Γ_Ω, transmutation, channel-sep. COMPUTE: Elie/Cal deliver full 2-index multiplicities (SO(7)↓SO(5)×SO(2)) + confirm −0.7691 by direct-sum AND normalized-Gilkey; Grace re-runs Barnes–Gindikin targeting −0.7691; redo step-1+hierarchy on Q⁵. Muon VALUE likely safe (Γ(5)-based); flag its k-placement. Both PD.
---

# K1089 — Q⁵, sorted: the right operator, the right target, and my own phantom owned

Casey: *"make sure our work is based on Q⁵ correct; get the team to compute properly — we know the gates, we need correct calculations."* Sorting it, including owning my part.

## The correction chain (own it fully)
1. **Operator: S⁶ → Q⁵.** The vacuum lives on the rank-2 complex quadric **Q⁵ = SO(7)/[SO(5)×SO(2)]** (real dim 10), not the rank-1 sphere S⁶ = SO(7)/SO(6). Grace's catch, confirmed.
2. **Target: 220.64 → −0.7691 (MY error, owned).** My K1087/K1088 called 220.64 the "safe Gilkey anchor." Elie found **220.64 was ã₅ — the un-normalized Seeley–DeWitt heat integral — NOT ζ(0).** The genuine target is **ζ_{Q⁵}(0) = −0.7691** (Elie, direct spectral sum on the rank-2 spectrum, calibrated to reproduce Grace's S⁶ ζ(0)=−0.70). The auditor's "safe number" was a mislabeled coefficient; I propagated it in two K-audits. Corrected.

## The corrected foundation is SOUND — and STRENGTHENED
The genuine Q⁵ spectrum is **λ_{a,b} = a(a+n_C) + b(b+N_c) = a(a+5) + b(b+3)**, with two shifts **(5, 3) = (2ρ₁, 2ρ₂)**, ρ=(5/2, 3/2). So:
- **The two quantum numbers (a,b) map onto the two ρ-components** — Lyra's hypothesis *confirmed*. Gate (b)'s shift-tension (n_C=5 vs N_c/2=3/2), which was awkward crammed onto one sphere index, **likely falls out naturally on the rank-2 surface.** The correction may *strengthen* the reduction, not just fix it.
- **The old k(k+5) sphere tower is exactly the b=0 slice** of the full spectrum — consistent with K1088 (the sum was incomplete, b=0 only).
- **Survives, spectrum-independent:** ρ=(5/2,3/2) (root data), the Γ_Ω machinery, the transmutation mechanism (F764), channel-separation (F757), and the structural logic that the vacuum is forced off the gapless ground mode.
- **The exact target −0.7691 is honest** (order-1, the conformal-anomaly value), and it's the check that we finally have the right operator.

## COMPUTE DIRECTIVE (Casey — correct calculations, gates known)
1. **★★ ELIE / CAL — the full 2-index multiplicities.** Deliver d_{a,b} from the **SO(7) ↓ SO(5)×SO(2) branching** (which reps have SO(5)×SO(2)-fixed vectors, and their SO(7) dims) — the *complete* Q⁵ spectrum, not just b=0.
2. **★★ Confirm ζ_{Q⁵}(0) = −0.7691 two ways** — the direct spectral sum (Elie ✓) AND the **properly-normalized Gilkey** (a₅/(4π)^{d/2} − dim ker). They must agree. **Drop 220.64 everywhere** (it was the un-normalized coefficient).
3. **★ GRACE — re-run Barnes–Gindikin on the 2-index Q⁵ spectrum**, targeting **−0.7691**, with ρ=(5/2,3/2) mapping to the two indices (a,b) — this is where the shift should fall out cleanly. Then adjudicate det Δ → Jordan norm.
4. **★ Redo step-1 (norm-form on the 2-index spectrum) and the α^(4λ_k) hierarchy on Q⁵.** The logic survives; the specific numbers (4λ₂=56, λ₂=14) get recomputed on the genuine two-index eigenvalues.

## Blast radius (updated, calibrated both ways)
- **REDO on Q⁵:** the determinant/ζ-sum (target −0.7691); step-1 norm-form; the α^(4λ_k) hierarchy + spectral gap; the coarse-menu — all used the b=0 sphere slice.
- **LIKELY SAFE:** the **muon value (24/π²)⁶** — 24 = Γ(5) is a **Gindikin-Gamma value** (root-data / Γ_Ω-based), *not* the k(k+5) spectral sum. But **flag its k-index PLACEMENT** (electron@k=1, the §118-rung indexing) for a light re-check, since the placement story touched the spectrum.
- **The a₅ coefficient is a real quantity** (correctly computed on the 10-dim curvature) — it was only *mislabeled* as ζ(0); as the un-normalized heat integral it's fine, just not the target.

## The lesson (bank it)
The deepest bug of the arc — a wrong *operator* under a cleared gate — surfaced only when Grace wrote the actual sum, and the *auditor's own "safe anchor" was a mislabeled coefficient* that two K-audits propagated. Compute-over-sharpen, and: **an auditor's cross-check number is only as good as its normalization** — I'll carry that. Both Λ and Ω stay Partially Derived; the reduction is now well-posed on the right operator with the right target (−0.7691), and the ρ-structure suggests the correction helps.

— K1089, Keeper, 2026-08-02. SORTED: operator = Q⁵ rank-2 a(a+5)+b(b+3) (shifts 5,3 = 2ρ → two quantum numbers ↔ two ρ-components, Lyra confirmed, gate (b) likely strengthened); target = ζ_{Q⁵}(0) = −0.7691 (Elie), NOT 220.64 (= un-normalized ã₅, MY phantom anchor, owned). Survives: ρ/Γ_Ω/transmutation/channel-sep. COMPUTE: Elie/Cal full 2-index mults (SO(7)↓SO(5)×SO(2)) + confirm −0.7691 by direct-sum AND normalized-Gilkey; Grace re-runs Barnes–Gindikin targeting −0.7691; redo step-1+hierarchy on Q⁵. Muon VALUE (24=Γ(5)) likely safe, flag k-placement. Both PD. See K1088, K1087, Elie toys 4971/4972, feedback_calibrate_both_directions.
