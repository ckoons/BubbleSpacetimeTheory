# F738 — The three tasks, honestly (29r). **(#2, PINNED) The 2-variable-vs-full-ring convention resolves to 2-variable, and it's not a choice — FK: the spherical polynomials on a rank-r cone are Jack polynomials in the r Jordan-frame eigenvalues; D_IV⁵ has rank r = 2, so exactly two eigenvalues, partitions of length ≤ 2, and the ≥3-part terms (m₍₁,₁,₁₎) DO NOT EXIST (there is no third eigenvalue). Keeper's lean confirmed against the source, not asserted. (#3, DERIVED-form) The lepton cross-ν off-diagonal is a Gindikin c-function ratio between ν-addresses — NOT the Jack binomial — and the corpus already has its form (F666): the electron↔muon overlap is c₅/c₃ = Γ(n_C)/π², so the muon's π² in (24/π²)⁶ IS this cross-ν c-function ratio. That is exactly why the muon off-diagonal has been open since June: it's a different object (cross-ν c-function, not degree-Jack), and Keeper's two-tier decomposition is right. (#1, HONEST CORRECTION) The up two-row assignment is NOT a quick input — I checked the obvious candidate (up = down + one box in row 2, {(1,1),(3,1),(5,1)}) and it FAILS: at a=3 it gives m_c/m_u = 20 (same as m_s/m_d, because the shared (ν−3/2)₁ factor cancels in the ratio), but observed m_c/m_u ≈ 577. So the up frame is a genuine derivation off the boundary α-ladder, not a partition hand-off — I will not fabricate it to make CKM "fire."**

**Lyra, Wed 2026-07-29. Fire what's ready, name what's a derivation, don't force a number — the through-line. #2 pinned clean, #3's deep object identified (the muon π² is the c-function ratio, F666), #1 honestly downgraded from "reachable input" to "real derivation" after the naive candidate failed its own check.**

## #2 — 2-variable, pinned to FK (rank-2 = two eigenvalues)
The spherical polynomials Φ_λ on a symmetric cone of rank r are Jack polynomials in **r variables** = the r eigenvalues of the Jordan-frame spectral decomposition (FK: every element x = Σ_{k=1}^{r} λ_k c_k, r primitive idempotents). **D_IV⁵ is rank r = 2** (spin factor, two idempotents c₁, c₂) ⟹ **exactly two eigenvalues** ⟹ partitions λ = (λ₁, λ₂) of **length ≤ 2** ⟹ the length-3 terms (m₍₁,₁,₁₎) that the full-ring solver keeps **do not exist** (there is no third eigenvalue to carry them). So the **2-variable domain integral is correct; the full-ring Jack over-counts.** This is intrinsic (rank-2 = 2 eigenvalues), not a convention choice — Keeper's lean confirmed, and it's the same FK-source discipline as the d-pin (not an assertion). Cal's kept-vs-dropped m₍₁,₁,₁₎ difference is resolved: **dropped** (they're outside the rank-2 spectrum).

## #3 — the lepton cross-ν object = Gindikin c-function ratio (the June open core, form derived)
Leptons are **ν-address-indexed** {5/2, 3/2, 0} (= {n_C/rank, N_c/rank, 0}, the two ρ-components + 0, F93), NOT degree-indexed — so their off-diagonal is a **cross-ν overlap between the strata sub-domains** (F666), a fundamentally different object from the quark degree-Jack binomial. The corpus has the form:
- The three lepton strata ARE boundary sub-domains with genera {n_C, N_c, 0} = {5, 3, 0} (position = genus/rank, F666): **electron on the full domain (genus 5), muon on the D_IV³ sub-domain (genus 3), tau at the Shilov point (genus 0).**
- The **electron↔muon off-diagonal = the c-function ratio c₅/c₃ = Γ(n_C)/π²** (F666, the D_IV³ ↪ D_IV⁵ embedding overlap). **The π² in the muon's (24/π²)⁶ IS this cross-ν c-function ratio** — the June-open muon-π² off-diagonal is the Gindikin c-function of the strata embedding. This is the real derivation, and its form is in hand (F666); the remaining work is evaluating the full 3×3 cross-ν c-function matrix (e↔μ = Γ(n_C)/π², μ↔τ and e↔τ = the Shilov-point limits) and confirming the muon ratio closes on it — NOT a Jack computation.
- **This confirms Keeper's two-tier decomposition and dissolves the "one kernel" over-tidiness:** quarks = degree-Jack(2/3) (validated engine); leptons = cross-ν Gindikin c-function (this object). Two engines, two Peirce sectors, honestly.

## #1 — the up two-row assignment: NOT a quick input (I checked; the obvious candidate fails)
Keeper's "up just needs the two-row partition assignment" is optimistic — I tested the natural candidate and it fails its own check:
- **Candidate: up = down + one box in row 2**, {(1,1), (3,1), (5,1)} (the T₃^R doublet-flip reading). At a = 3, ν = N_c = 3:
  (3)_{(1,1)} = 3·(3/2) = 4.5; (3)_{(3,1)} = 60·(3/2) = 90; (3)_{(5,1)} = 2520·(3/2) = 3780 → ratios **1 : 20 : 840** — identical to the down, because the shared (ν−3/2)₁ = 3/2 factor **cancels in every ratio**. So this gives **m_c/m_u = 20**, but observed **m_c/m_u ≈ 577**. **FAILS.**
- Consistent with F730 (the doublet-flip is closed-disfavored — the up does NOT read as a flip of the down's degrees). The up masses come from the **boundary α-ladder** (m_c = α·v/√2, m_t = (1−α)v/√2, Derived) — a *different* mechanism than the down's degree-Pochhammer. So the up FRAME (for CKM = U_up†U_down) must be derived off the **boundary sector**, not a two-row degree partition.
- **Honest status:** the up masses are Derived (α-ladder); the up *frame* for CKM is a genuine derivation off the boundary structure, NOT a partition hand-off. I will not supply a fabricated partition to make CKM fire — the naive one gives the wrong charm/up ratio, which is exactly the kind of forced number the discipline rejects. The reachable quark CKM win is the **down sector** (V_us fired 0.8σ); the up-side CKM waits on the boundary-frame derivation.

## Tier / handoffs
- **@Elie** — (#2) use the **2-variable** domain integral / rank-2 Jack (drop the m₍₁,₁,₁₎ full-ring terms — no third eigenvalue). (#1) do NOT fire the up-side CKM on a two-row partition — the naive {(1,1),(3,1),(5,1)} gives m_c/m_u = 20 (wrong; obs 577); the up frame needs the boundary-sector derivation (below). The **down-sector** quark results (V_us) are the fired win.
- **@Cal** — (#2) audit: rank-2 ⟹ 2 eigenvalues ⟹ length-≤2 partitions ⟹ m₍₁,₁,₁₎ don't exist (FK spectral theorem, not a truncation choice). Your kept-terms were the over-count. (#1) confirm the naive up=down+box fails m_c/m_u (the 3/2 cancels) — so the up frame is a real derivation, not a hand-off.
- **@Keeper** — the honest decomposition is right and it sharpens: quarks degree-Jack(2/3) [down fired], leptons cross-ν Gindikin c-function [F666, the muon π²]. Two corrections to the plan: (#1) the up-side CKM is a derivation, not a quick input (naive candidate fails m_c/m_u = 20 vs 577); (#3) the lepton object is the c-function ratio (form in F666), not the Jack binomial. Fire the down CKM; leptons + up-frame wait on derivations, not inputs.
- **@Casey** — three things, and I'm giving you the honest version rather than the tidy one. (1) The convention question resolves cleanly to the book: our domain is rank 2, which means every element has exactly two eigenvalues, so the polynomials only ever have two variables — the extra terms Cal was keeping simply don't exist here, and that's forced, not chosen. (2) The deep lepton piece — the one open since June — I can now name exactly: the leptons aren't labeled by "degree" like the quarks, they sit at fixed radial addresses, and the overlap between them is a ratio of the domain's gamma-function-like c-functions. And beautifully, that ratio for the electron-muon pair is Γ(5)/π² — which is *literally where the muon's π² comes from*. So the muon's famous (24/π²) isn't mysterious; it's this cross-address overlap, and we already had its form in F666. (3) The one I have to correct: the up-quark mixing wasn't the quick win we hoped — I tested the obvious partition assignment and it gives the wrong charm-to-up ratio (20, but nature says ~577), because the up quarks get their masses a different way (off the boundary) than the down quarks (off degrees). So I'm *not* handing over a number that would make CKM "fire" on a fabricated input — the up-side is a real derivation, and after this week's caught errors that's exactly the line to hold. Fire the down mixing (it's clean); the up mixing and the leptons are genuine derivations, named honestly, not forced.

Notes only; no toy/theorem claimed. F738: (#2) 2-variable PINNED — FK rank-2 ⟹ 2 eigenvalues ⟹ length-≤2 partitions ⟹ m₍₁,₁,₁₎ don't exist (not a truncation, the spectrum). (#3) lepton cross-ν off-diagonal = Gindikin c-function ratio (F666): e↔μ = c₅/c₃ = Γ(n_C)/π²; the muon π² IS this ratio — the June open core, form in hand, different object from quark Jack. (#1) up two-row = NOT a quick input: naive down+box {(1,1),(3,1),(5,1)} gives m_c/m_u=20 (3/2 cancels) vs obs 577 — FAILS; up masses = boundary α-ladder (Derived), up frame = real derivation off the boundary, NOT a partition hand-off (won't fabricate). Fire down CKM; up-frame + leptons are derivations. @Elie 2-var, don't fire up on bad partition. @Cal audit rank-2-spectrum + up-candidate-fails. — Lyra