# Cal — Round 130 BLIND PRE-SCORE (kinematics on the record space), before Lyra's hashed file and Elie's computation. 2026-09-07 (Mon) 09:42 EDT

(i) THE EINSTEIN CONTROL — can it fail? Two maps are possible and the control decides between them, not between BST and the world. (A) If the binding W is what the boundary RETAINS as a fraction of the absorbed word, W = f(j,k)·hν, then KE = (1 − f)·hν: linear through the origin with slope 1 − f — this FAILS Einstein/Millikan (slope exactly h to 0.5 %, intercept −W ≠ 0). Productive: it says the retained fraction is not an energy. (B) If W is the shell's own winding (one shell unit, or a k-dependent Casimir value) and the photon's energy is additive in writes (m units), then KE = hν − W(k) passes — BY CONSTRUCTION: a definition-level pass (K1835), empty as a test of the map. Prediction: the map passes only via (B), so the control is EMPTY as written; the one way to make (B) fall is to compare W(k) with a measured shell spectrum — if k is a shell label and W(k) ∝ k(k+5) (Casimir), that is ALREADY REFUTED by the Rydberg series (Ry/n², decreasing), and if k is not a shell label, W(k) is unconstrained. So: the control cannot fail unless the map commits W(k) to a spectrum, and then it fails.
(ii) THE k-LAW vs WIGNER — (a), (b) or (c)? Wigner's E^{l+½} is a threshold law in the CONTINUUM kinetic energy, from the centrifugal barrier of an outgoing wave in ℝ³; it needs an E → 0 limit of a scattering state. The dictionary's Hua weights (k+3)/(2k+3), k/(2k+3) are BRANCHING RATIOS with no energy variable — the boundary Š is compact and the record space has no continuum, no outgoing wave, no E. Prediction: (c) — not a threshold law at all; what the dictionary predicts is a k-dependent branching ratio (light : matter), and "k-law vs Wigner" is a category mismatch unless the map manufactures an energy dependence through m, which would be a reparametrisation (count the free parameters). (a) is impossible without a continuum; (b) would be a fabricated exponent.
(iii) THE k ↔ l SEAM. k = degree of a spherical harmonic on S⁴ (an SO(5) representation of dimension (k+1)(k+2)(2k+3)/6); l = the outgoing partial wave in ℝ³ (SO(3), dimension 2l + 1). The physical ℝ³ is K1860-B's exterior: ℝ⁵ = 2 (the polydisc plane) + 3. Restricting H_k(S⁴) to SO(3) × SO(2) ⊂ SO(5) branches into EVERY l ≤ k (with multiplicities depending on the SO(2) weight) — a write that raises k by one does not raise l by one, and "k = l" is false as a statement about representations. Prediction: the first draft will write k for l by fiat; the map must exhibit the SO(5) ↓ SO(3)×SO(2) branching rule and say which l-component the ejected electron carries before any exponent is compared. Until then the same integer is two objects (the round's own brake).
Score shape: (i) EMPTY unless committed, then REFUTED; (ii) (c); (iii) the seam bridged by fiat in the first draft. Kill for the pre-score: a map in which f(j,k)·hν is a constant in hν by a computed mechanism (not by definition), a derived energy variable on Š with a threshold, or a written SO(5) ↓ SO(3) branching that picks one l.
