# Cal — Round 123 C11 BLIND HOLD on E11 (the kernel swap {2,∞} → {3,∞}), written before L10 pins the form and before E11 runs. 2026-09-06 (Sun) 16:04 EDT
Instrument run first (retained: cal_E11_3adic_kernel_swap_prescore_instrument_2026-09-06.py + OUTPUT): the rank-one intertwining integral at p = 3 for q₀ = x² + 3y² + 3z² (trace-zero norm form of (−1,−3)_ℚ, ramified exactly at {3, ∞}: anisotropic at 3 by the mod-3 argument, split at 2 since (−1,−3)₂ = +1), lattice ℤ₃³ (maximal: no superlattice is integral), planes hyperbolic over ℤ₃ (⟨1,−1⟩ ≅ H at every odd p because 2 is a unit — the odd/even distinction of §855 is 2-adic only). P(val₃ q₀(u) ≥ 1 | primitive) = 4/13, ≥ 2: 0. Result: c₃(σ) = (1 − y)(1 + 9y)/(1 − 27y²), y = 3^{−σ}, σ = λ + 3/2, i.e. c₃ = (1 − 3^{−λ−3/2})(1 + 3^{½−λ})/(1 − 3^{−2λ}) — the same shape as the 2-adic EVEN model (§854), as it must be: at 3 the planes are hyperspecial.

H14 (the spacing). Global correction to the split formula at 3: (1 − 3^{1−2λ})/(1 − 3^{−1−2λ}) → poles at λ = −½ + iπk/ln 3, ALL k: **spacing π/ln 3 = 2.8596, not Keeper's 2π/ln 3 = 5.719.** Keeper's number is the Steinberg-only surgery (1 − 3^{½−λ})/(1 − 3^{−½−λ}) transplanted from p = 2; the direct integral carries the unramified-quadratic partner (1 + 3^{½−λ})/(1 + 3^{−½−λ}) in addition, exactly as the even 2-adic model did. At p = 2 the corpus's ODD planes supplied the factor that cancelled the partner (§855: (1 + 2y) in place of (1 + 4y)); at p = 3 no plane is odd, so nothing cancels it. Prediction: the 3-comb appears at 2.8596 spacing (Keeper's 5.719 comb is its even-k half). Kill for H14: a DIRECT computation (integral or Macdonald on the tree) on the ℤ₃-maximal lattice giving only the 2π/ln 3 members.
H15 (the 2-adic side after the swap). The new kernel is split at 2, so the KERNEL's 2-comb disappears — but the planes ⟨1,−1⟩ are still odd at 2, and §861 (n = 3) showed the odd plane alone makes a level-type odd-k 2-comb with no kernel. Prediction: a residual 2-adic comb (odd-k type, at Re λ = −½ + iπ(2k+1)/ln 2) survives the swap if E11 keeps the corpus's planes; it vanishes only if the planes are taken even (H ⊕ H). E11 must STATE its 2-adic lattice; "comb spacing follows the prime" is true of the kernel comb and false of the total comb set.
H16 (ψ(½)). Unmoved: the archimedean factor is Harish-Chandra's Γ(λ)/Γ(λ+3/2) for ANY definite ternary kernel (§857 II); the −2 ln 2 from ψ(½) stays, the ε-derivative becomes −2 ln 3 (if the ε is 3^{½−s}) — so after the swap the two ln's SEPARATE (ln 2 archimedean, ln 3 from the prime), which is the discriminator for C9 and for T1448's label in one run.
H17 (instrument validity). If E11 is run by substituting 3 for 2 in L1's JL formula, it tests nothing (construction-guaranteed 2π/ln 3). The can-fail instrument is the direct rank-one integral (this file's, generalised to Lyra's pinned lattice) or Macdonald's c-function on the inner form's tree at 3. Score E11 only against a direct computation.
Sharpening owed by me: if L10 pins a different ℤ-lattice for the kernel (e.g. the maximal order's trace-zero lattice in another basis), rerun; the 3-adic result depends only on the ℤ₃-isometry class, which for the maximal lattice of an anisotropic ternary is unique — so H14 should survive any pin that is 3-adically maximal.
