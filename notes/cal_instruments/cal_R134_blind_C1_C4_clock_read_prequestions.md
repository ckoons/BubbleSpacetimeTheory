# Cal — Round 134 BLIND holds — C1 (rule or relabeling), C2 (adjective-class audit: "complexity" vs "push cost"; L3 pre-scored), C3 (the lane floor, K1882 §5), C4 (the Schur decision, K1882-PRE-A) — from the round prompt, K1882 §3/§5 and PRE-A only; before any Lyra/Elie R134 post

## C1 — "the successor reads j₀ through the push cost": rule or relabeling?

H1. RELABELING, and the two "facts" are not in opposite directions — they are about different operators. K1860-P's write is a PURE ISOMETRY: it does not lose norm. The push cost c(j,k) is the norm deficit of the UNNORMALIZED operator "multiply by z_u" in the Bergman norm (Round 130: 1/(m+2) is the disc's number, the deficit of w ↦ w·f). The isometric write is that multiplication with the deficit divided out. So c is the number the write DIVIDES BY — bookkeeping of the normalization — and no process in the dictionary consumes it: the branching k/(2k+3) is j-blind (fact 1), the word 3/7 is j-blind, the saturation trigger is j-blind. "The successor reads j₀ exactly once, through the cost of every commitment" is a sentence with no reader inside the record space.

H2. What would make it a rule, and what that costs. The cost becomes physical only if something depends on it — the one candidate reading is failure: "a push succeeds with probability 1 − c(j,k)." Then cycles differ (later cycles fail less) and the chain becomes j-dependent. But that contradicts K1860-P (writes are isometries; every write is made, K1860-G) — it is a Born-type posit at the write, the same species as the measurement PRE-A excludes at the reset. Falsifier of "cycles differ" inside the corpus: NONE without that posit; E3's control (statistics identical to the digit at j₀ = 0 and 2329) will confirm that nothing in the chain reads j₀. Verdict: relabeling; fact 1 is the theorem; fact 2 is a property of an operator the dictionary normalizes away.

H3. Keeper's guess c(j,0) = 5/(2(j+5)): a one-parameter form n/(2(j+n)) in which c(0) = ½ fixes nothing, c(1) = 5/12 fixes n = 5, c(2) = 5/14 confirms — one check, modest. Family test at rank 1: with m = 2j (the degree of (z·z)^j) the disc's 1/(m+2) = 1/(2(j+1)) = n/(2(j+n)) at n = 1 ✓ — the form passes its own family check. Refuse "5 = n_C" as a signature until L2's closed form says which parameter the 5 is (genus p = 5 of the Bergman kernel, or n = 5, or a = 3 + 2); the disc check says it is the rank-1 member's "1", so it is the dimension-type parameter, not n_C by name. Large-j: c ~ 5/(2j) at k = 0 on the guess; I hash only the SHAPE A(k)/j with A(k) increasing in k (the k-lines fall more slowly: c(0,1) = 10/21 > c(1,1) = 25/63 > c(2,1) = 15/44 but all above the k = 0 line at the same j).

## C2 — adjective-class audit: "complexity" (T633) vs "push cost" (c) — L3 pre-scored

H4. NO MAP, and fact 1 is what forbids it. T633's object: G(n), the D0-fraction of the theorem graph, target f (19.1 %), driven per CYCLE by T307's recursion with rate r_eff = 1/n_C, gap ~ n⁻³ (per the prompt) — a fraction with a positive asymptote. The push cost: c(j₀) → 0, target zero, gap ~ 1/j₀. A forced map must carry (i) the target (f ≠ 0 — c has none), (ii) the clock (per cycle vs per winding), (iii) the exponent. On (iii): the j-blind theorem says the per-cycle winding INCREMENT Δj is cycle-invariant, so after n cycles j₀ ≈ nΔj and c ~ 1/n — exponent 1. T633's gap is n⁻³ (or geometric (4/5)ⁿ if T307's η is the constant 1/5). Exponent 1 ≠ 3: the map does not exist unless Δj grows like n² across cycles, which contradicts fact 1. **Fact 1 kills fact 2's candidacy for T633.** If L3 exhibits a map, count its free parameters first: T307's η_n sequence, if free, fits ANY monotone approach (definitionally empty); if fixed by T307 at 1/5, the exponents disagree. Pre-score L3: "shape only, no map" is the honest outcome; an exhibited map will have η_n chosen to fit.

H5. E2 hashes: Σc(j₀) monotone decreasing in j₀ ✓ trivially (every term falls); asymptote 0 ✓; gap ~ 1/j₀ ✓ on the guess. The ratio Σc(cycle 2)/Σc(cycle 1) will look rational; it is a ratio of harmonic-type sums with the cap in it and carries no integer. E3: identical to the digit (cannot fail — a factor that rides along cannot enter a branching that never sees it; report it as such).

## C3 — the lane floor (K1882 §5) — word with amendments

H6. WORD GIVEN with six amendments. (1) Name the three hypotheses in the row as hypotheses: (a) the reset is SO(5)-covariant (uses no external input), (b) the reset is LINEAR on states / posits no measurement (K1860-P: writes are isometries; no row posits collapse), (c) the reset ERASES angular content (its output is SO(5)-invariant — this is the definition of "reset," R131 C1). The trace channel is forced by (a)+(b)+(c); PRE-A's text names only (a). (2) Add the sentence the lane actually proved for Casey's question: "no initialization VECTOR is lawful — a linear covariant map from any angular block to the invariants is zero (Schur); the lawful survivor is a STATE, a distribution on j." That is the theorem; the rest is bookkeeping. (3) The environment clause: "the traced-out angular content is the reset's Stinespring environment (dimension d_k on both branches); it is NOT K1860's beyond-band sector (a cut in frequency); re-entry is legible and not remembered (5733)." (4) Strike or fence PRE-A §4's "the direction waits outside the horizon at full weight" — after 5733 that is a comfort clause; the environment is read once per write and forgotten. (5) "posited nowhere" for (D)'s measurement stays; add "if posited, (D) is a Born row at the nucleation, collapse-at-reset, and collides with K1860-P's isometry class." (6) No dark-matter sentence follows from the floor (three DM rows in the corpus, unreconciled; §916). Tier line as Keeper wrote it: theorems DERIVED, bits MEASURED, clause POSITED, floor a NEGATIVE with three witnesses (5726 P1, 5731, 5733) — add the fourth witness 5733's S3 (steering saturates).

## C4 — the Schur decision (K1882-PRE-A): definition, theorem, or posit?

H7. A THEOREM GIVEN THREE POSITS, of which PRE-A names one. "A lawful reset is covariant" is the weakest posit and it is honest — but it is not the one that decides the fork. Covariance alone admits the identity channel (no reset), any covariant channel with non-invariant output, and the NONLINEAR covariant map ξ*(Y) = argmax (PRE-A §3 says so itself: covariant, nonlinear). What excludes (D) is (b) no measurement / linearity and (c) invariant output — and (c) is the fork restated: "(D) keeps a direction" is exactly "(D) violates (c)." So the math decides between the rows only after the definition of "reset" has been chosen to exclude one of them. The smuggled posit is (c), written as a property of lawfulness ("no external label") when it is the definition of erasure. Verdict: DERIVED given (a)(b)(c); (c) is the definitional posit; (b) is the corpus's ABSENCE of a measurement row (a status, not a theorem); Schur's real content is item (2) of H6 — no vector survivor is lawful.

H8. The sentence for Casey, which is what §918 asked for and PRE-A supplies in a different form: no ruling between rows is needed. The math gives (C) because the corpus has posited no measurement at the reset. If a direction is to survive, that is a NEW physical posit (collapse at nucleation, a Born row), not a choice between two written rows — and it should be argued as a posit with its collision against K1860-P on the table, or left out. Registration on that reading: (C) row with the three hypotheses named; L2's bulk-point paragraph in the distribution category; (D) row kept as "the mathematics of the unposited branch" with T2625 intact.

## Refusals
- "Cycles differ" without a consumer of c; c as anything but a normalization bookkeeping number until a consumer is exhibited (and a consumer is a Born posit at the write).
- "T633 derived / has a mechanism" — exponents 1 vs 3; fact 1 forbids the map.
- Any BST integer read off Σc ratios; "5 = n_C" in c(j,0) until L2 names the parameter.
- "The math decided" without naming (b) and (c).

— Cal, 2026-09-08, before any Round 134 post. Hash in §919.
