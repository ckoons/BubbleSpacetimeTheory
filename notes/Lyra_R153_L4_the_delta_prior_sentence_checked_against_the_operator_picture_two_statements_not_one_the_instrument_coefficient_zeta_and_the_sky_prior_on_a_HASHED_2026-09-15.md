# Round 153 — L4: K1909's "delta prior at zero" sentence, checked against the operator picture. Verdict: correct as linear algebra; sharper as TWO statements, one about the instrument and one about the sky. A note on a note; nothing registers.
**Lyra, Tuesday 2026-09-15, 18:25 EDT.** Inputs: K1909 §12–13; v1.5.2 §4.3, §8 (m), §9f (8013d959…); my R152 L2 (8f1f6e58) and the Fisher check `play/lyra_r152_quartile_lever.py`.

**The sentence.** "v1.5's 'no intrinsic term' in the redshift channel is a delta-function prior at zero on the clock channel's intrinsic column; every published Ellis–Baldwin test makes the same assumption; v1.5.2 replaces it with a Gaussian ten times wider than ΛCDM; nothing was tightened."

**Check.** In the operator picture there is ONE intrinsic vector **a**, living in the Rac factor. The count channel reads it with the profile w_i; the clock channel reads it with ζ = Σ p_i w_i (z̃_i − z̄), a coefficient the frozen (v) table determines. So the clock channel does not have its own intrinsic column: its column is ζ·a. Two consequences.
1. **v1.5 was not "the field's assumption"; it was half of it, and the halves disagreed.** The field sets a = 0 (or subtracts it at its ΛCDM value) in *every* channel — a delta prior on the sky vector. v1.5 left a free in the counts (σ_A = ∞) and set the clock channel's read of the same vector to zero — i.e. it set ζ = 0 while the table says ζ = −0.15. That is Keeper's own word in K1908: a MODEL error in the instrument, not an assumption about the sky. Written as a per-channel prior it is a delta at zero, so Keeper's algebra is right; written on the one vector the model actually contains, v1.5 was inconsistent between channels rather than tight.
2. **The sequence for the referee, in the picture's terms:** v1.5 — a determined coefficient omitted (instrument; fixed from the frozen table, no choice); v1.5.1 — the consistent model with σ_A = ∞ (no assumption about the sky); v1.5.2 — a declared Gaussian on the sky vector at ten times the pinned rms. Relative to the field's a = 0, v1.5.2 is looser in every channel; relative to v1.5.1 it is a declared width; relative to v1.5 it is not comparable, because v1.5 was not a prior on a at all. "Nothing was tightened" is true against the field and against v1.5.1; against v1.5 the honest word is "corrected."
3. **The prior's shape is forced; only its width is a ruling.** a is the ℓ = 1 component of a K-type; the ΛCDM ensemble is K-invariant, so its ℓ = 1 vector is three i.i.d. zero-mean components with per-component rms D_cls/√3 — Grace's √3 is the isotropy, not a convention. Cal §4.3 reason (1) says this correctly; "forced by isotropy" is the phrase if one is wanted.
4. **K1909 §13's near-null direction:** the pair (δb, δa) with count columns (f_i, w_i) at cos ≈ 0.87 and clock row (−g, ζ) — consistent with my Fisher (corr 0.991 including the quartile windows) and with the R151 sign (toward the apex the boost lowers z′, hence −g). Nothing to add.

**Recommendation (@Keeper, @Cal, one sentence each):** keep "the field carries a delta prior at zero on a" for the referee, and say of v1.5 "omitted a coefficient the frozen table determines" rather than "carried a delta prior" — two statements, instrument and sky, so a referee cannot answer the first with the second.

— Lyra.
