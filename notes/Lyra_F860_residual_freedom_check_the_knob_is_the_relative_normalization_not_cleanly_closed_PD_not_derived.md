# F860 — the #85 last gate (residual-freedom check, my assignment): the knob does NOT cleanly close — it is the RELATIVE weak:hypercharge normalization (the SM's own free parameter), and closing it needs one concrete computation not yet exhibited. Honest verdict: **Partially-Derived (explicit-split), one gate short of Derived — NOT full Derived on what we have.** **The parity result (F859) forces the trace-STRUCTURE (Case A, the 3/13 ratio-form) target-innocently. The LAST gate to full Derived is whether the coupling NORMALIZATION is embedding-forced or hides a free constant (the α_s "/π" trap). I checked it, holding the calibrate-both-directions line hard at peak convergence, and the honest answer is: the knob is PRESENT and is NOT rigorously closed. (1) sin²θ_W = g′²/(g²+g′²) is a ratio — an OVERALL normalization cancels, but a RELATIVE weak:hypercharge rescaling does NOT, and that relative scale is exactly the SM free parameter that makes sin²θ_W un-predicted without a GUT. (2) The knob's home: K = SO(5)×SO(2) is a PRODUCT group, so the relative scale between the SO(5) block (weak, T₃) and the SO(2)-mixing hypercharge (Y = T₃R + (B−L)/2) is NOT fixed by K alone — the same reason SU(3)×SU(2)×U(1) has independent couplings. (3) The plausible CLOSER, BST-native but NOT exhibited: both legs are fixed IFF normalized by the ONE Bergman metric = the trace over the COMMON tangent p (not per-factor) — the geometric analog of a GUT (there U(1)_Y ⊂ SIMPLE SU(5) fixes √(3/5); here the SIMPLE isometry SO(5,2), with D_IV⁵ = SO(5,2)/K, could play SU(5)'s role, giving one metric → one normalization). IF the gauge kinetic terms are FORCED to inherit the common-tangent trace (no free relative constant) → knob closes → Derived. IF a per-factor relative scale survives → knob open → Identified. This is NOT decided by anything the corpus exhibits (no gauge-coupling-normalization derivation found). So the honest tier is PD (explicit-split): the ratio-STRUCTURE/Case-A is FORCED (parity, F859); the coupling-NORMALIZATION-is-the-common-tangent-trace step is PLAUSIBLE but UNEXHIBITED. It does NOT reach full Derived on what we have — it is one concrete computation short, and I name that computation exactly.**

**Lyra, Saturday 2026-08-08. The last #85 gate, checked with the brake ON at maximum convergence pull. Calibrate BOTH directions: I do NOT declare Derived (the α_s /π lesson — a knob that LOOKS forced but isn't), and I do NOT demote below what parity earned (the 3/13 ratio-structure IS forced). Honest landing: PD explicit-split, one exhibited computation short of Derived, with that computation named. RUBRICS Layer-2 done-bar below.**

## The knob, located precisely (target-innocent)
1. **Ratio cancels overall, not relative:** sin²θ_W = g′²/(g²+g′²). Overall rescale (g,g′)→(cg,cg′) cancels; **relative** rescale g′→kg′ does **not**. The surviving freedom = the relative weak:hypercharge normalization — **exactly the SM free parameter** (why sin²θ_W is un-predicted without a GUT).
2. **The knob's home = the product group.** K = SO(5)×SO(2) has a block-diagonal invariant form → the relative SO(5):SO(2) scale is not fixed by K (same reason the SM has independent g_s, g, g′). Y = T₃R + (B−L)/2 mixes an SO(5) generator and an SO(2) generator; their relative normalization is the knob.

## The closer — plausible, BST-native, NOT exhibited
- **Forced reading:** both legs normalized by the ONE Bergman metric = the trace over the COMMON tangent p. Then g²:g′² = Tr_p(T₃²):(the Q-trace), no free constant → **Derived**. The simple isometry **SO(5,2)** (D_IV⁵ = SO(5,2)/K) is the geometric analog of a GUT's simple group — it could fix the relative scale the way SU(5) fixes √(3/5).
- **Residual-freedom reading:** a per-factor relative scale survives (product-group generic) → **Identified**.
- **Which holds is NOT exhibited.** The corpus has no derivation of the gauge kinetic terms / coupling normalization from the one geometric action (grep-confirmed). So the common-tangent normalization is **assumed, not shown** — and "assumed" is exactly the α_s-/π failure mode.

## What closes it (named precisely — the one computation)
> **Derive the gauge kinetic terms −(1/4g²)W² − (1/4g′²)B² from the ONE geometric action on D_IV⁵ (the Bergman/heat-kernel action), and show the relative normalization g²:g′² = Tr_p(T₃²):Tr_p(Q²) falls out with NO free relative constant — via the simple SO(5,2) fixing the SO(5):SO(2) relative scale (the GUT-analog).** If exhibited → Derived. If a free relative constant survives → Identified.

## Honest verdict (calibrated both directions)
- **NOT full Derived:** the coupling-normalization gate is open; the common-tangent prescription is plausible but unexhibited (α_s lesson).
- **NOT demoted below parity's result:** the 3/13 ratio-STRUCTURE/Case-A IS forced (F859, target-innocent).
- **⟹ Partially-Derived (explicit-split):** forced ratio-structure (parity) + plausible-but-unexhibited coupling normalization. State both parts separately; never a bare "Derived." One named computation from promotion.

## RUBRICS Layer-2 done-bar (DERIVATION / tier assessment)
- [x] Precise statement: the residual knob = relative weak:hypercharge normalization; closed iff the common-tangent (SO(5,2)-fixed) normalization is forced.
- [x] Forcing chain audited: ratio-structure FORCED (parity F859); relative-normalization OPEN (product group; closer = one Bergman action / SO(5,2), unexhibited).
- [x] Target-innocent: the knob is located by ratio + product-group structure, not by 0.231.
- [x] Tier assigned: **PD (explicit-split)**, NOT Derived; the named computation is the promotion gate.
- [x] No over-claim (didn't declare Derived at peak convergence) AND no over-demotion (parity's forced structure credited).

## Handoffs
- **@Keeper (adjudicate the final tier)** — Gate 1 (parity → Case A) PASS is yours-confirmed. Gate 2 (residual-freedom): **the knob is PRESENT (relative SO(5):SO(2) normalization = the SM free parameter) and NOT rigorously closed.** My honest verdict is **PD explicit-split, not full Derived** — one named computation short (gauge kinetic terms from the one geometric action, relative g:g′ = common-tangent trace via simple SO(5,2), no free constant). If you or Casey judge the "one Bergman metric → one normalization" reading as forced-enough, it's Derived; my calibrated read is it's assumed, so PD. Your adjudication.
- **@Cal (§341 / the /π guard)** — this is exactly your α_s guard applied honestly to my own convergence: the normalization LOOKS forced (one geometry) but I could not exhibit that it IS (product-group relative scale is the SM's free knob). I refused to wave it to Derived. Guard the PD framing; the named computation is the gate.
- **@Casey** — the honest bottom line on the prize: the *shape* of the answer is forced — the geometry genuinely makes sin²θ_W a ratio of the form N_c/(N_c+2n_C) = 3/13, and the reason color doesn't double is the same reason space is 3-dimensional (parity — really lovely, really forced). But turning that shape into the *number* being fully Derived needs one more thing: that the two coupling strengths (weak and hypercharge) are set by the *same* geometric ruler and not by two independent ones. In the Standard Model they're independent — that's the whole reason nobody can predict this angle. BST's claim is that the one geometry ties them, and the honest state is that this is *very plausible* (one shape, one metric, and SO(5,2) is a single simple group that could pin it the way the grand-unified groups do) but *not yet shown* — and I've been burned this week trusting a normalization that turned out to hide a free knob (α_s). So I'm calling it Partially-Derived: the structure forced, the last normalization step plausible-but-owed. One clean computation — the gauge kinetic terms from the one geometric action — turns it into Derived, and I've written down exactly what that computation is. I did not let the beauty of the parity result carry me past what's shown. Nothing pushed.

Notes only; no toy/theorem claimed (residual-freedom assessment, tier verdict PD). F860: the #85 last gate. The surviving knob = the RELATIVE weak:hypercharge normalization (sin²θ_W ratio cancels overall not relative; the SM's own free parameter). Home: K=SO(5)×SO(2) product group → relative SO(5):SO(2) scale not fixed by K. Closer (plausible, unexhibited): both legs normalized by the ONE Bergman metric = common-tangent trace; simple SO(5,2) as the GUT-analog fixing the relative scale. NOT exhibited in corpus (no gauge-coupling-normalization derivation). Verdict: **PD explicit-split** — ratio-structure/Case-A FORCED (parity F859), coupling-normalization PLAUSIBLE-but-UNEXHIBITED → NOT full Derived, one named computation short (gauge kinetic terms from one geometric action, g²:g′² = common-tangent trace via SO(5,2), no free constant). Calibrated both ways: no wave-to-Derived (α_s /π lesson), no demote-below-parity. @Keeper adjudicates final tier; @Cal guard the PD framing. — Lyra