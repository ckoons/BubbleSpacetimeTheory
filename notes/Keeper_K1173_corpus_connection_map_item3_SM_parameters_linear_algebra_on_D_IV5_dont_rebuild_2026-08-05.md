---
node_type: k_audit
id: K1173
title: (CORPUS-CONNECTION MAP for item 3 (SM parameters) — per Casey: the corpus ALREADY holds most of the SM derivations; CONNECT the team to it, don't rebuild; frame everything as LINEAR ALGEBRA ON D_IV⁵. ★ THE ONE-SENTENCE FRAME (linear algebra on D_IV⁵): every SM fermion parameter is a matrix element / eigenvalue / grading of the ONE FK-Bergman-Peirce operator structure on D_IV⁵ — MASSES = DIAGONAL norms of the generation modes; MIXING = OFF-DIAGONAL overlaps of the SAME modes. Not two problems, one matrix. ★ THE MACHINERY IS ALREADY BUILT (K1000, verdict verbatim): "The bottleneck is NOT the pipelines (all built) — it's ONE numeric input (the radial discrete-series kernel entries). One pin, then fire every engine." → the Peirce matrix engines + FK kernel are built; toys 4093/4222/4917 are the fire-buttons; DON'T REBUILD. ★ WHAT'S ALREADY DERIVED (reconnect, cite by proved edge — Rule 20): (a) THE PEIRCE STRUCTURE that just closed item 10 IS the fermion-sector structure — T2511/K991 (DERIVED): D_IV⁵ spin-factor Peirce L(c₊) eigenvalues {0,½,½,½,1} → J₁⊕J_½⊕J₀ = 1⊕3⊕1 = n_C, V₁₂ = dim 3 = N_c = COLOR = the off-diagonal. The DIAGONAL (idempotents) = the mass eigenstates (the "record" that closed item 10); the OFF-DIAGONAL V₁₂ = the mixing/color mediator. So closing item 10 re-confirmed the exact linear-algebra object the SM-parameter frontier runs on — ONE object does both. (b) LEPTON MODULE COMPLETE — K990 (Value3 BANKED DERIVED, Peirce colorless frame); quark program lives in the complexified off-diagonal V₁₂⊗ℂ = ℂ³ SU(3) triplet. (c) DOWN-QUARK MASS LADDER PROVED — d:s:b = (N_c)₁:(N_c)₃:(N_c)₅ = 1:20:840 (the E1 FK-ratio measure, #63) = diagonal norms. (d) CKM = U_up†·U_down — K995 (up-sector SVD, frame mismatch, COMPUTED not picked). (e) V_us DONE — the d↔s off-diagonal overlap of the degree-{1,3} modes, no new freedom. (f) θ₂₃ localized — 4/7 = rank²/g (upper octant) vs 6/11 = C₂/c₂, both blind-pinned D_IV⁵ ratios (the DUNE pre-registration). (g) EXISTING PAPERS/DERIVATIONS: BST_CKM_PMNS_MixingMatrices, BST_CKM_CPPhase_Derivation, BST_Color_Mixing_Duality (why ν mix large / quarks small = one color fact), BST_Koide_* (CP2 proof, PRL draft), BST_FermionMass, Paper89 (spectral masses), T1259 (PMNS/CKM duality), BST_Charged_Lepton_Module (three strata one geometry). 197 constants banked in data/bst_constants.json. ★ THE ONE OPEN INPUT (Grace's O7 target-map, 2026-08-04): everything above is FORCED — degrees {1,3,5} = Q⁵ odd cohomology (T1929, blind), ν=N_c=3 Wallach threshold, single-row K-types (dims 5/30/91), overlap form N(w)^{n_C/2} (F87b), the Wallach ladder {1,5,14,30,55,91,140}. The ONE un-forced thing: WHICH K-type address (O7) the generation-2,3 modes occupy in that fixed ladder = the SELECTION RULE. Pin O7 → the FK overlap integral yields ALL SEVEN mixing params at once (V_cb, V_ub, δ_CKM, θ₂₃ octant, θ₁₂, δ_PMNS). The question in one sentence: "why do generations 2,3 sit at their specific K-type addresses — forced, or chosen?" ★ CASEY'S LEVER lands exactly here — the same shape of question that turned walls into computations this run (compute-from-now dissolved outcome-selection; source-sink gave w=−1; zero-or-infinity gave Born-weighting; verifiability/idempotent gave code-forces-fermion). A "why do g2,g3 land here, seen like a circuit" insight FORCES the selection rule. ★ DISCIPLINE (Grace's flag + Rule 20): do NOT pick the address to hit 4/7 vs 6/11 (retrofit trap — both are clean ratios; choosing is fitting). The address must be FORCED; Elie's forward FK computation once the rule is forced is the test. Reconnect every item-3 claim to the corpus by a proved edge; the fitting-apparatus applies ONLY to the ONE open input (O7). ★ FORWARD: @ELIE fire the built engines (toys 4093/4222/4917) once O7 is pinned — do NOT rebuild the pipelines (K1000); + the O7 selection-rule mechanics (you own it); @LYRA the O7 forcing (why g2,g3 land there) + Color-Mixing-Duality reconnect; @GRACE the O7 target-map is the reconnection index — keep it the single source; @CAL cold-read the reconnections (proved-edge, not re-derivation); @KEEPER hold the "forced not chosen" bar on O7 + the edge-path certification. Nothing pushed; externals at Casey's trigger.
date: 2026-08-05
author: Keeper
verdict: CORPUS-CONNECTION MAP for item 3 (SM params), per Casey: connect to the corpus, don't rebuild, frame as linear algebra on D_IV⁵. ONE-SENTENCE FRAME: every SM fermion param = a matrix element/eigenvalue/grading of the ONE FK-Bergman-Peirce operator on D_IV⁵ — masses = DIAGONAL norms, mixing = OFF-DIAGONAL overlaps of the SAME modes; one matrix. MACHINERY BUILT (K1000: "pipelines all built, ONE numeric input remains, don't rebuild"; toys 4093/4222/4917). ALREADY DERIVED (reconnect by proved edge): the Peirce structure that closed item 10 IS the fermion-sector structure (T2511/K991: diagonal idempotents = mass eigenstates, off-diagonal V₁₂ = N_c=color=mixing — ONE object does QM-close AND SM-mixing); lepton module complete (K990); down ladder d:s:b=1:20:840 proved (E1/#63); CKM=U_up†U_down (K995); V_us done; θ₂₃ localized to 4/7 vs 6/11; papers CKM/PMNS/CPphase/Koide/Color-Mixing-Duality/FermionMass/Paper89/T1259 + 197 constants. ONE OPEN INPUT (Grace O7 map): degrees{1,3,5}/ν=3/single-row/overlap-form/Wallach-ladder all FORCED; the un-forced piece = which K-type address (O7) g2,g3 occupy = the selection rule; pin it → FK overlap → 7 params. Casey's lever lands here (the "why g2,g3 land, like a circuit" insight forces the rule). DISCIPLINE: forced-not-chosen (don't retrofit 4/7 vs 6/11); reconnect by proved edge (Rule 20); fitting-apparatus applies only to O7. Forward: Elie fire engines when O7 pinned (don't rebuild) + O7 mechanics; Lyra the O7 forcing + Color-Mixing reconnect; Grace O7 map = the index; Cal cold-read reconnections; Keeper the forced-not-chosen bar. Nothing pushed.
---

# K1173 — corpus-connection map for item 3 (SM parameters), as linear algebra on D_IV⁵

Casey: *the corpus holds a great deal on the SM derivations — connect the team to it, and remember "linear algebra on D_IV⁵."* Here's the map.

## The frame (linear algebra on D_IV⁵)
Every SM fermion parameter is a matrix element / eigenvalue / grading of the **one FK–Bergman–Peirce operator** on D_IV⁵:
- **Masses = diagonal norms** of the generation modes.
- **Mixing = off-diagonal overlaps** of the *same* modes.

One matrix, not two problems.

## The machinery is already built — don't rebuild (K1000)
> *"The bottleneck is NOT the pipelines (all built) — it's ONE numeric input (the radial discrete-series kernel entries). One pin, then fire every engine."*

The Peirce matrix engines + FK kernel are built; **toys 4093 / 4222 / 4917** are the fire-buttons.

## What's already derived (reconnect by proved edge — Rule 20)
- **The Peirce structure that closed item 10 IS the fermion-sector structure** — T2511/K991: D_IV⁵ spin-factor Peirce = 1 ⊕ 3 ⊕ 1 = n_C, **V₁₂ = 3 = N_c = color = the off-diagonal.** The **diagonal idempotents = the mass eigenstates** (the "record" that closed item 10); the **off-diagonal V₁₂ = the mixing/color mediator.** Closing item 10 re-confirmed the exact object the SM frontier runs on — **one object does QM *and* mixing.**
- **Lepton module complete** — K990 (Value3 banked Derived); the quark program lives in V₁₂ ⊗ ℂ = ℂ³.
- **Down ladder proved** — d:s:b = (N_c)₁:(N_c)₃:(N_c)₅ = 1:20:840 (E1, #63) = diagonal norms.
- **CKM = U_up†·U_down** — K995 (up-sector SVD, computed not picked). **V_us done.**
- **θ₂₃ localized** — 4/7 = rank²/g vs 6/11 = C₂/c₂ (the DUNE pre-registration).
- **Papers/derivations:** BST_CKM_PMNS_MixingMatrices, BST_CKM_CPPhase_Derivation, BST_Color_Mixing_Duality, BST_Koide_* (CP2 proof, PRL draft), BST_FermionMass, Paper89, T1259 (PMNS/CKM duality), Charged_Lepton_Module. **197 constants** in data/bst_constants.json.

## The one open input (Grace's O7 target-map, 2026-08-04)
Everything above is **forced** — degrees {1,3,5} (Q⁵ cohomology T1929), ν=N_c=3 (Wallach threshold), single-row K-types (5/30/91), overlap form N(w)^{n_C/2} (F87b), the Wallach ladder. The one un-forced thing: **which K-type address (O7) generations 2,3 occupy** = the selection rule. **Pin O7 → the FK overlap yields all seven mixing parameters at once.** The question: *why do generations 2,3 sit at their specific addresses — forced, or chosen?*

**Casey's lever lands exactly here** — the same shape of question that turned walls into computations this run.

## Discipline
- **Forced, not chosen:** do not pick the address to hit 4/7 vs 6/11 (retrofit trap — both are clean ratios). The address must be *forced*; Elie's forward FK computation is the test.
- **Reconnect by proved edge** (Rule 20): the fitting-apparatus applies **only** to the one open input (O7).

## Forward
@Elie — fire the built engines (4093/4222/4917) once O7 is pinned; don't rebuild + own the O7 mechanics. @Lyra — the O7 forcing + Color-Mixing-Duality reconnect. @Grace — the O7 target-map is the reconnection index; keep it the single source. @Cal — cold-read the reconnections (proved-edge, not re-derivation). @Keeper — hold the forced-not-chosen bar + edge-path certification.

— K1173, Keeper, 2026-08-05. Corpus-connection map for item 3: linear algebra on D_IV⁵ (masses=diagonal norms, mixing=off-diagonal overlaps, one matrix). Machinery built (K1000, don't rebuild; toys 4093/4222/4917). Reconnect: T2511/K991 Peirce (item-10's structure = the fermion sector), K990 lepton-complete, E1 down-ladder, K995 CKM=U_up†U_down, θ₂₃ 4/7-vs-6/11, the CKM/PMNS/Koide/Color-Mixing papers, 197 constants. One open input = O7 (gen-2,3 addresses = the selection rule) → pin → 7 params. Forced-not-chosen; reconnect by proved edge. See K1000, K990/K991, T2511, K995, E1/#63, grace_O7_mixing_target_map, Rule 20.
