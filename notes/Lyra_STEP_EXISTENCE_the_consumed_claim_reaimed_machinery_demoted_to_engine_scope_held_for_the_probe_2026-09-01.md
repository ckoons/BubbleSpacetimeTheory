---
title: "STEP-EXISTENCE — the consumed claim, re-aimed: in the canonical context, some family word strictly descends d_gate. The Capture arithmetic demoted to derivation engine, the Barrier to cage; the ∀-side is the One-Context Lemma's; every scope decision HELD for the probe on the one configuration"
author: "Lyra"
date: "2026-09-01, Tuesday (clock-verified 11:54 EDT)"
status: "Re-aim per the instruments' wind (Elie: 50-of-53 single-step recoveries as the measured texture). No new mathematics claimed; the statement the CHAIN CONSUMES is isolated from the machinery that will prove it. Both futures pre-stated. The Barrier's confirmation path (Cal's tagging demand) noted as running. Nothing banks."
---

# STEP-EXISTENCE (the claim the chain consumes)

## 1. The statement, re-aimed to exactly what the assembly needs and nothing more

  **Step-Existence: in the canonical context, some family word strictly descends d_gate.**

That is the entire claim. The ∀-side (every stuck configuration presents the canonical
context, in every sphere triangulation) is the One-Context Lemma's — already derived, zero
census joints. The iteration (≤ d_gate + 1 words) is the Assembly's counting, already
restated under the seam ruling. What the chain consumes from THIS lemma is one existential:
one word, one strict step, in one (canonical) context. Everything else this morning built —
the hybrid cascade, the Two-Agreement Barrier, the capture arithmetic, the target menu — is
DEMOTED, correctly, from claim to MACHINERY: the Barrier is the cage the derivation works
inside; the capture arithmetic is the engine that will certify the step; the target choice is
how the engine finds it. Machinery does not appear in the statement; it appears in the proof.

## 2. The measured texture (context, not citation)

Elie's wind: 50-of-53 single-step recoveries — the step the lemma asserts is not merely
existent but TYPICAL in the measured world; the three multi-step cases and the one
configuration under probe are where the scope question lives. **Every scope decision is HELD
until the probe renders on the one configuration. Nothing is scoped, rowed, or excepted
before that render.**

## 3. The two futures, pre-stated so both land in built slots

- **Probe dissolves the residue ⟹** the derivation aims at a claim with ZERO exceptions
  across everything ever measured — the cleanest target this program has ever pointed the
  engine at, and the derivation proceeds: capture arithmetic on the canonical context's
  forced structure, Barrier-caged, target-menu-quantified.
- **Probe hardens ⟹** the exhibit's anatomy becomes the counterexample-guided derivation —
  the week's standing pattern (every corpse taught more than its conjecture), applied to the
  last lemma. The exhibit would be the most valuable single object since the frozen twins,
  and it would be treated as such: anatomy first, hypothesis after, kill conditions before
  contact.

## 4. Running alongside

The Barrier's confirmation path runs (Cal's tagging demand — cage-tagged collision accounting;
his verdict is the Barrier's to earn, not mine to assume). SJ rides the choice as before; the
bridge-term keeps its label and its own session; the One-Context Lemma and the stage table
stand beneath everything, untouched by today's re-aims — which is what foundations are for.

— Lyra. The morning built an engine, a cage, and a menu; the instruments then read us the one
sentence the proof actually needs. Statement re-aimed; machinery in its place; and the whole
program now waits, correctly, on one probe and one configuration — the way it has waited all
week: with the slot already built for either answer.
