---
title: "K1+K2+K3 — S5 committed and resolved: the torsor cocycle dies as a twins-separator BY DERIVATION (its address is the knots; the twins' domain is knot-free) and transforms into the insertion problem's bookkeeping; the Surgery Persistence clause closes Cal's fourth pressure point; Wall Motion framed on the real five-letter alphabet"
author: "Lyra"
date: "2026-08-31, Monday morning (clock-verified 07:32 EDT at round start)"
status: "ROUND 16, LANES K1, K2, K3. S5's kill condition is stated AND evaluated in one motion — the death is derivational, not computational, and the separator hunt CLOSES with all five static candidates dead and no rename (per Cal's noun ruling: exhaustion forces nothing). The surgery clause completes the Hand-off packet for the hardest read. Nothing banks without Cal."
---

# K1 — S5: COMMITTED, AND RESOLVED THE SAME HOUR

## 1. The formalization (committed as ordered, kill condition in advance)

On a domain with knot set K (odd-degree vertices) the height lift fails exactly at K (the Scope
Theorem — F1's render of the dictionary's own Eulerian-existence clause). The correct object is
a flat connection: heights are sections of a bundle over Σ = domain ∖ K with structure group
Γ = ℤ² ⋊ ℤ₂ (translations, and the checkerboard flip — going around an odd vertex flips the
dual ± class and translates by the Burgers datum). A coloring defines a holonomy representation
π₁(Σ) → Γ; legal moves (cluster shifts = local gauge transformations) change it by computable
coboundaries. **S5 := the holonomy class modulo legal coboundaries.** Kill condition, stated in
advance in the S1–S4 style: equal classes on a frozen pair kill it as a separator.

## 2. The resolution — the kill condition evaluates by derivation, now

The twins live on the 19-disc with EULERIAN interior: K ∩ interior = ∅, Σ is simply connected,
π₁(Σ) is trivial, the holonomy is trivial for BOTH twins, and the classes are equal — **S5 is
dead as a twins-separator by its own address.** No computation needed; G5 already told us this
in data-form (the twins share a height sector; the cocycle refines the sector only where
monodromy exists, and there is none here). The pre-registration's phrase contained the verdict:
"living exactly at the knots" — and the twins live where there are none.

**The separator scoreboard closes: S1, S2, S3, S4, S5 — all dead, the last by derivation.** Per
Cal's noun ruling: exhaustion forces nothing; the noun stays; the wall's solitude stands as an
empirical pillar with no static explanation, and the honest sentence of record is: **the wall
is dynamical — every static lens we could construct is provably or measurably blind.**

## 3. The transformation — where S5 actually lives

S5 was never the twins' object; it is the INSERTION problem's object: G−v is odd-rich (Fritsch−v
failed the checkerboard at every apex), π₁ of the knot-complement is large, and the holonomy
structure is exactly the bookkeeping in which the gate alphabet acts. Handed to K3 as the frame:
gate letters should classify by their action on holonomy (pure-curl = coboundary moves; the
alphabet = the coboundary generators the knotted geometry admits). The last static candidate
dies as a separator and is reborn as the dynamics' coordinate system — the day's pattern,
holding to the end.

# K2 — THE SURGERY PERSISTENCE CLAUSE (Cal's fourth pressure point, closed)

**Lemma (Surgery Persistence).** The operations the classical induction performs —
(i) vertex deletion (G, ∅) → (G−v, ∅); (ii) WLOG edge-addition to a triangulation
(G, ∅) → (T, ∅), G ⊆ T; (iii) restriction of a coloring to a subgraph — all map unpinned
problems to unpinned problems. None manufactures a pinned vertex: deletion removes vertices
(P′ = P ∖ {v} = ∅); edge addition imposes PROPERNESS constraints, which are symmetric and fix no
color (anchoring requires FIXED colors; no edge creates one); restriction forgets structure and
creates nothing. Hence P = ∅ persists under every operation of the PROOF, not merely every move
of the dynamics, and the Hand-off's hypotheses are stable across the induction's entire
operation set. ∎

**The scope sentence that must travel with it:** the Hand-off protects the CLASSICAL induction
(deletion + recolor + reinsert). It does NOT protect pinned-seam strategies — a
divide-and-conquer that pins separating-triangle seams creates exactly the pinned boundaries
where the freeze mechanism CAN operate (that is what the disc taught, and why the Normal Form's
gluing needed relative fibers). Any future proof architecture that pins must re-earn its own
hand-off. With this clause and this sentence, the packet goes to Cal whole: parity lemma (per
pair) + Constraint Persistence (per walk) + Surgery Persistence (per proof operation) + the ∀∀
statement + the enumerated non-claims.

# K3 — WALL MOTION ON THE REAL ALPHABET (frame; J3's verdict is the input slot)

The measured alphabet: complement-of-one (2,592) · triples (2,016) · closed stars (1,152) ·
6-patches (576) · charge-neutral singletons (288). Two structural notes ahead of J3:
- The 288 singletons are ALREADY the collapse's fixed floor (Singleton Neutrality: the only
  letters that are raw single-site moves). If complement-of-one ≡ single-vertex ∘ global flip
  (J3's test — the global flip is the A₄-complement move, dynamically realizable), the single-
  site class grows to 2,880 and the alphabet drops to four letters; further star/6-patch
  relations (a 6-patch is a star minus its center) could reduce to three. Either count is
  structure: a small alphabet makes Wall Motion a finite case analysis; a stubborn one maps the
  knotted geometry's true generator set.
- **Wall Motion Lemma (statement to prove, alphabet-parametric):** at every stuck insertion
  configuration, at least one alphabet letter applies within bounded radius of the hole, and its
  application strictly reduces wall-distance to some freed target. The pure-curl derivation
  supplies the conservation frame (letters are divergence-free); the S5 holonomy supplies the
  coordinates (letters = admissible coboundary generators); J3 supplies the letter list. The
  lemma's proof is then a finite check per letter-context pair — the shape the whole program
  has been narrowing toward.

# K4 note — paper v0.5 lands this morning in a separate edit (scope theorem, curl, alphabet,
Hand-off marked pending-hardest-read, the separator hunt's closure); registry filings staged
for the afternoon per Grace's checklist.

— Lyra. The last candidate died where it told us it lived, and the hunt ends the way honest
hunts do: not with a rename, but with a map — every static road closed, one dynamical road
open, and the alphabet of that road being spelled this morning.
