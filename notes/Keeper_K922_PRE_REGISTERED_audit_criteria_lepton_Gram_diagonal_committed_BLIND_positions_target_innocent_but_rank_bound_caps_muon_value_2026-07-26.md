# K922 — PRE-REGISTERED audit criteria for the lepton Gram-diagonal, committed **BLIND** (before Grace+Elie return the computation). "Commit the checker's half blind" — so the verdict can't be retrofitted to whatever comes back. Three anchors independently verified below. **The load-bearing pre-commitment: even a full PASS banks the POSITION structure (lepton-vs-quark π-parity, target-innocent) but NOT the muon VALUE — the rank bound provably forbids exponent 6 from the diagonal residue, so the muon value must come from elsewhere (BF-zero residue / FK Wyler 3×3), no matter how elegant the diagonal result looks.**

**Keeper | 2026-07-26 Sun | Blind pre-registration. The ceiling on this computation is set BEFORE seeing it, by the rank bound. This is the discipline that caught the muon honestly on 07-23 (B3 blind criteria).**

## The three anchors — verified BLIND (independent of any mass value)
1. **Lepton positions {5/2, 3/2, 0} are root-data-fixed.** ρ(SO(5,2)) = (5/2, 3/2, 1/2) (type B₃ half-sum of positive roots). So 5/2 = ρ₁ (electron/BF), 3/2 = ρ₂ (muon/unitarity-bound), 0 = trivial (tau). **These are fixed by the domain's roots, NOT by any mass.** ✓ (Matches the corpus's four-lens target-innocent sourcing, K921.)
2. **Quark ladder arithmetic:** m_s/m_d = rank²·n_C = 20; m_b/m_s = N_c²·n_C = 45; m_b/m_d = 900. All exact. ✓
3. **Rank bound (decisive):** a rank-r domain's Gindikin Γ has r factors → residue order ≤ r. D_IV⁵ rank 2 → **max residue order 2.** The muon (24/π²)⁶ needs **exponent 6 > 2** → **exponent 6 CANNOT come from the diagonal residue.** ✓ (Lyra F671, banked 07-23.)

## PASS criteria (target-innocent) — ALL required for the STRUCTURE claim
- **P1.** The three lepton diagonal exponents {5/2, 3/2, 0} emerge from the **kernel K(z,z)** of D_IV⁵ (the ρ-vector / Berezin ε structure), with **no mass value used as input.** If the muon mass (206.77) or the (24/π²)⁶ form is used to fix a position or a parameter → target-AWARE → FIT.
- **P2.** The half-integer character of 5/2, 3/2 (→ √π) traces to **n_C = 5 odd** via the Γ-at-half-integer (Gindikin, F157/F664) — the π enters through the kernel's Γ-structure, not inserted.
- **P3.** The **SAME object** (same kernel, same parameterization), evaluated at the quark integer positions {5, 2, 0}, yields m_s/m_d = 20 and m_b/m_s = 45 **without re-parameterization.** If the quark ladder needs a DIFFERENT kernel/parameter than the leptons → two objects, not one → the "one diagonal, two sectors" claim FAILS.

## FIT / FAIL red flags (any one → not target-innocent)
- The muon mass value used anywhere as input.
- **Exponent 6 claimed to come from the diagonal residue** — this VIOLATES the rank bound (order ≤ 2). Any derivation asserting exponent 6 from the lepton diagonal residue is WRONG on its face; flag immediately.
- The quark {5,2,0} ladder requiring a re-tuned parameter that the lepton {5/2,3/2,0} did not.
- "24" or "6" assembled from Γ-values of the chair to hit (24/π²)⁶ (the K847 reverse-engineering trap in a new costume).

## PRE-COMMITTED VERDICT STRUCTURE (committed blind)
- **IF P1∧P2∧P3 hold:** the **LEPTON-vs-QUARK π-parity is CONFIRMED target-innocent and kernel-derived** — upgrade F156/F664 from candidate-with-examples to kernel-derived; the "electron regular / π-free" gloss (F156 old) is **DEAD** (electron is half-integer 5/2). This is a real, bankable result — the π-parity STRUCTURE.
- **The muon VALUE (exponent 6) does NOT bank on this, no matter how clean the diagonal.** By the rank bound (anchor 3, committed blind), exponent 6 cannot be a diagonal residue — so the muon value stays **OPEN**, its source being the BF-zero residue structure (electron d_e=0) / the un-run FK Wyler 3×3. A PASS here = "π-parity structure derived (lepton-vs-quark)," NOT "muon value derived." **I commit to this ceiling now, before seeing the result.**
- **IF any red flag fires:** the muon refinement stays candidate; the specific failing criterion is named; no partial credit laundered.
- **No impact on the partition theorem either way** — m_μ/m_e is a bucket-2 modulus; this is entirely species/π-provenance work.

## What I will check when it returns
- Is any mass value in the input chain? (P1 target-innocence)
- Does the π come from the kernel Γ-at-half-integer, or is it inserted? (P2)
- Is it literally the same object for leptons and quarks? (P3)
- Does anyone claim exponent 6 from the diagonal residue? (rank-bound violation → immediate flag)
- Then: bank the STRUCTURE if P1∧P2∧P3; hold the muon VALUE open regardless.

— Keeper K922, 2026-07-26. PRE-REGISTERED (blind) audit criteria for the lepton Gram-diagonal. Anchors verified blind: ρ(SO(5,2))=(5/2,3/2,1/2) → lepton positions {5/2,3/2,0} root-data-fixed; quark {5,2,0}→ m_s/m_d=20, m_b/m_s=45; rank bound → max residue order 2, so exponent-6 CANNOT be a diagonal residue. PASS(structure) = positions from kernel (no mass input) + √π from n_C-odd Γ + quark ladder from SAME object. Red flags: mass-as-input, exponent-6-from-residue (rank-bound violation), re-tuned quark parameter, Γ-chair assembly. PRE-COMMITTED CEILING: a full pass banks the LEPTON-vs-QUARK π-parity STRUCTURE (kills 'electron regular'), NOT the muon VALUE (exponent 6 forbidden from the diagonal by the rank bound → value stays open, source = BF-zero residue/FK 3×3). No partition-theorem impact. See [[Keeper_K921_MUON_PI_ANOMALY_target_innocence_verdict]], [[feedback_commit_the_checker_half_blind]], [[feedback_score_sigma_not_devpct]].