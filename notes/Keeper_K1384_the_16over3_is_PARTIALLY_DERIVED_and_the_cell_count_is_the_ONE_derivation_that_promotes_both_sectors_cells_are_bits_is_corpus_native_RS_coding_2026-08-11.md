# K1384 — GOVERNANCE + CORPUS-RECONNECT ruling. ★ Grace's flag VERIFIED: the DM 16/3 is duplicated (const_015 was [D], const_197 was [S]) and its honest tier is **PARTIALLY DERIVED**, not Derived — the count 2^(2·rank) is ASSERTED (channel-capacity story), not derived by decomposing the boundary into cells. So the gravity same-object arc **inherits a PD floor** and must NOT cite 16/3 as Derived. ★ The good news in the negative (Grace): the SINGLE missing derivation — the substrate cell-count of the Shilov S⁴ — promotes BOTH sectors at once (16/3 PD→D AND closes the gravity seam). ★ CORPUS-RECONNECT (the load-bearing enabler): Lyra's "a cell is a bit" is NOT an assumption — it's the substrate's already-banked **Reed-Solomon coding structure on GF(2^g)=GF(128)** (K59 cyclotomic mechanism, Paper #122). So the forward route is corpus-native. Plus: ratify Cal's dimension retraction (d=10≡2 mod 8, rank=2 withdrawn), Cal's edge-sign reduction (ONE parity comparison), and bank Cal's DE-falsifier constraint on Casey's cosmology. Cal cold-reads the tier ruling.

**Keeper (2026-08-11, ~11:55. Grace found a data-integrity + tier issue the gravity arc is about to lean on; I verified it against bst_constants.json and rule. Then the reconnect Casey asked for: the "cells=bits" conditional Lyra needs is the substrate's banked coding structure. Cal cold-reads. Nothing pushed.)**

## ★ VERIFIED: the DM 16/3 duplicate + its honest tier (my ruling)
From `data/bst_constants.json` (read this session):
- **const_015** (`Omega_DM/Omega_b = 2^(2·rank)/N_c = 16/3`): mechanism = "Channel capacity of D_IV⁵ boundary." Grace's 2026-08-02 tier-refresh already flags **[D]→PARTIALLY DERIVED**: exponent 2·rank=4 is structural, "channel capacity" is a plausible count, **but the count 2^(2·rank) is ASSERTED not derived, and 16/3 is the closest 2^k/N_c to obs 5.36 (mild form-selection).** 0.58%, 0.5σ.
- **const_197** (bare duplicate, was tier S): Grace stamped `duplicate_of: const_015`, defers tier to const_015. **The S/D mismatch is already mechanically resolved** (const_015 authoritative, const_197 dedup'd).
- **KEEPER RULING:** the honest tier of the DM 16/3 is **PARTIALLY DERIVED**, ratified. const_015 authoritative at PD; const_197 dedup'd (retained per never-delete, tier defers). The downstream Ω_b=18/361 (const at line 803) correctly inherits the PD floor. **This ruling is data-layer-applied in the EOD sync (Grace's lane) with my sign-off; Cal cold-reads.**

## ★ Why this matters to the gravity arc (the load-bearing consequence)
The gravity same-object argument (K1379/K1381) says "gravity-16/3 is *literally* the DM-16/3." **But the DM-16/3 is PD, not D** — its count is asserted. So:
- **The gravity arc cannot cite 16/3 as a Derived anchor.** It inherits the PD floor. Saying "it's our banked DM object" imports a partially-derived number, not a rock. This is exactly the kind of quiet tier-inflation the survival audit (K1383) exists to catch — flagged before it propagated.
- **BUT** — and this is Grace's real find — **the one derivation that's missing fixes both:** if the substrate cell-count of S⁴ is derived from first principles and returns 2^(2·rank)/N_c = 16/3 on its own, then **the DM-16/3 goes PD→D AND the gravity same-object seam closes** (the two 16/3's are provably one count read twice). **One computation, two payoffs.** The PD tier isn't a wound — it's a pointer at the single derivation that closes the whole arc.

## ★ CORPUS-RECONNECT (Casey's directive: "derive the substrate cell count; see the 137 channel capacity and the boundary")
Lyra's F920 forward route ("count bits, not volume; S⁴ has 2⁴=16 bits ÷ N_c=3 → 16/3") rests on one conditional: **"a cell is a bit."** I checked the corpus — **that conditional is already banked, not new:**
- **The substrate IS a Reed-Solomon coding machine on GF(2^g) = GF(128)** (CLAUDE.md substrate framing; **K59 cyclotomic mechanism RATIFIED**; **Paper #122 Information Substrate**). The substrate's native alphabet is bits over GF(2^7).
- **"Channel capacity of the boundary" is already the mechanism attached to the DM 16/3** (const_015). So Lyra's bit-count and Grace's channel-capacity are the SAME banked idea from two sides.
- **⟹ The forward cell-count is not inventing machinery — it's connecting two already-banked pieces** (RS-coding substrate + channel-capacity boundary) to *derive* what was previously *asserted*. That's the corpus-native route, and it's the strongest possible footing for the make-or-break.

## ★ The discipline that still governs it (Lyra's "16 has four faces" — do NOT wave through)
The RS structure gives bits, but the corpus shows **16 has ≥4 provenances**: 2⁴, 2^(rank²), rank⁴, 2^(2·rank) — and const_197 reads it as **rank⁴ (the C-tensor-O dimension of one generation)** while const_015 reads it as **2^(2·rank) (channel capacity)**, and line 5553 notes a **K3-cohomology/N_c** reading too. Four faces coincide at 16 numerically. **So the proof is NOT "the bits give 16/3."** The proof is: **does the RS-tiling of the *4-sphere specifically* FORCE 2⁴ (e.g. one bit-channel per sphere dimension) as THE count — and no other face?** If S⁴'s four dimensions give four independent bit-channels → 2⁴, forced, and that's the unique reading → the count is derived. If a different face is forced, follow it. **Blind, never toward 16/3.** (This is Grace's same-object test made concrete: the bit-count is now the thing to verify.)

## ★ The 137 hook (Casey's "137 channel capacity") — flag as a LEAD, recused from declaring
Casey points at N_max=137 as the FULL boundary channel capacity (the DM/gravity 16/3 being a *sub*-count). Corpus: **137 = N_c³·n_C + rank; the RS alphabet is 2^g=128.** A bit/channel route to 137 (does the full boundary S⁴×S¹/ℤ₂ capacity give 137?) is a genuine lead — BUT **137-numerology is a known trap** (CLAUDE.md K231c "α-from-π numerology trap territory"). So: **route it as a lead with the trap flagged; I stay recused (K1377) from declaring any count.** If the RS channel-capacity of the boundary gives 137 forward and blind, that's a major reconnect; it is not to be asserted from 128+9 or the like.

## Ratify Cal's three advances (all clean)
- **★ Dimension pin — d=10≡2 (mod 8), rank=2 RETRACTED (healthy walk-back — updates K1383's tally to ~21–26).** Class D needs d≡2 (mod 8) for ℤ; **D_IV⁵ is 10 real dimensions, 10≡2 → KO₀=ℤ.** Corpus-native, dissolves the "d=2 isn't a BST dimension" worry. Owed: confirm the Toeplitz-boundary-map KO-degree lands at KO₀. **Cleaner than rank=2; ratified as the candidate.**
- **★ Edge sign — reduced to ONE parity comparison.** The 3 quark-color copies share one parity (color is gauge, preserves the holomorphic grading → color-universal). So the whole check = **does the lepton-doublet weight (Y=−1/2) land in the same parity as the quark-doublet (Y=1/6)?** Same → uniform +4 = SM; opposite → mirror. Forced by two weights. Elie/Lyra compute the two degrees; Cal holds the bar.
- **Identical-strata:** 3 gens = 3 Wallach strata; gauge-identical ⟹ identical ±4 **iff the gauge bundle is uniform across strata** (same reps, differ only in radial localization → mass). Verify the uniform bundle.

## Bank Cal's DE-falsifier constraint on Casey's cosmology (K1382 governance)
Casey's EM-drives/gravity-gates cosmology stays **I-tier**, and Cal adds the load-bearing constraint: **it MUST respect the already-banked DE falsifier — no phantom crossing / w_a>0, which is under DESI DR2 tension (3–4σ).** The new cosmology **inherits** that tension; it does NOT get to predict a different w(a) than the breathing-mode one. **Consistency-web, not a free story.** Update K1382: the w(a) handle is a *consistency* check against the banked breathing-mode w(a), not a new prediction.

## Route
1. **★ Lyra + Grace — the forward cell-count (THE single load-bearing derivation):** does the RS-coding/bit-tiling of the Shilov S⁴ FORCE 2⁴=16 (one bit-channel per dimension?) ÷ N_c → 16/3, blind, corpus-native (K59/Paper #122)? Which of the four faces of 16 is forced, and is it unique? If 16/3 falls out → DM PD→D AND gravity seam closes (one computation, two payoffs). Track: is the "3" honestly N_c (color channels quotiented) — Lyra's F920 fix for the Γ-3 seam.
2. **★ Cal + Elie — the edge sign** (ONE comparison: lepton Y=−1/2 vs quark Y=1/6 parity) + the d=10≡2 KO-degree confirm + the uniform-bundle check across strata.
3. **Lead (recused-declare) — the 137 channel capacity:** does the full-boundary RS capacity give 137 forward? Trap-flagged; nobody asserts it from arithmetic.
4. **DE frontier (I-tier, whoever's free):** w(a) *consistency* vs the breathing mode, respecting the no-phantom-crossing falsifier (Cal); the G–H–c relation.
5. **Keeper — recused from the counts (K1377);** I ruled the tier (PD), reconnected the corpus (cells=bits is banked RS-coding), and hold the K1377 bar for when the forward count lands. Data-layer PD tier applied in the EOD sync (Grace's lane) with my sign-off.

— Keeper, K1384, 2026-08-11. VERIFIED Grace's flag against bst_constants.json: DM 16/3 duplicated (const_015 [D], const_197 [S]); RULING: honest tier = PARTIALLY DERIVED (count 2^(2·rank) asserted, not cell-derived; mild form-selection); const_015 authoritative, const_197 dedup'd; downstream Ω_b inherits PD. Consequence: gravity same-object arc inherits the PD floor — cannot cite 16/3 as Derived. Grace's find: the ONE missing derivation (substrate cell-count of S⁴) promotes BOTH (16/3 PD→D + gravity seam closes) — one computation, two payoffs. CORPUS-RECONNECT: Lyra's "cell=bit" is banked — substrate = Reed-Solomon on GF(2^g)=GF(128) (K59 RATIFIED, Paper #122); "channel capacity of boundary" already the DM mechanism → the forward count connects two banked pieces, corpus-native. DISCIPLINE: 16 has ≥4 faces (2⁴, 2^rank², rank⁴, 2^(2rank), + K3-cohom/N_c) — proof = RS-tiling of S⁴ FORCES 2⁴ uniquely, blind, never toward 16/3. 137 hook (Casey): full-boundary channel capacity → 137? LEAD only, numerology-trap-flagged (K231c), Keeper recused-declare. Ratify Cal: dimension d=10≡2 mod 8 (rank=2 RETRACTED, healthy walk-back); edge sign = ONE parity comparison (lepton Y=−1/2 vs quark Y=1/6); identical-strata iff uniform gauge bundle. Bank Cal's DE constraint: Casey cosmology inherits the banked no-phantom-crossing/w_a>0 falsifier (DESI DR2 3–4σ) — w(a) is a CONSISTENCY check vs the breathing mode, not a free prediction. Route: Lyra/Grace forward cell-count (the load-bearing derivation); Cal/Elie edge sign + d=10 + uniform bundle; 137 as trap-flagged lead; DE I-tier; Keeper recused, tier applied EOD. Cal cold-reads. Nothing pushed.
