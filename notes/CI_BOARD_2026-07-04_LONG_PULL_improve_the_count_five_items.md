Team LONG PULL — 2026-07-04→. Improve the count: five items, each with existing forward machinery. Derive, don't re-derive.

SOD check first. Casey's directive: improve the banked count (stuck at 8) and derive more. The corpus sweep shows **most of these already have forward machinery** — the work is finishing the explicit computation, not starting over. Forward-only; each bank requires the explicit derivation, not the identified form. Count moves only when a mechanism forces forward.

## The five items — ranked by how close the bank is

### 1. LEPTONS (muon + tau) — BEST near-term, 2 parameters (8→10). LYRA primary.
**Corpus:** T190 has an explicit **4-step conjectured kernel chain**: (1) set up the Bergman kernel matrix element, (2) evaluate the per-step factor = 24/π² [24 = N_c·|W(B₂)|, π² = 2 angular integrations], (3) show the step count = C_2 = 6 for e→μ, (4) assemble (24/π²)^{C_2}. The structural ingredients are explicit; only the **explicit Bergman matrix-element computation** is unfinished (was "multi-week" — but the eigentone map is now PINNED, so it's tractable). Tau (T2003, 49·71 or (g/N_c)^{10/3}) has a DIFFERENT per-transition structure — compute it too.
**Forward test (K657):** the kernel must reduce to the muon (24/π²)^{C_2} and produce it from the *integral*, not assert it. **Bank when steps 1–4 compute forward.**

### 2. θ_QCD — closest single item, 1 parameter. LYRA + KEEPER.
**Corpus:** K222 PASS at FRAMEWORK tier — θ_QCD = 0 via substrate-CP preservation (Casey #14 chirality projection). It's *downstream* of Casey #14, and RIGOROUS closure is the **explicit substrate-CP-preservation derivation** (π₁ = 0 → contractible → trivial bundle → no θ-term, made explicit).
**Forward test:** the contractible→trivial-bundle→no-θ chain written out (graph/topology — Casey's lever). **Bank when the topological closure is explicit, not framework-asserted.**

### 3. α — a TIER-CALL (Casey's) + a per-step mechanism, 1 parameter. TEAM stages, CASEY rules.
**Corpus:** F429 — 6π⁵ = 1835× is *derived*, "gate is per-step alpha"; Wyler closed form gives 137.036 at 0.6 ppm. The question is judgment: is Wyler's forced-geometry derivation a genuine bank or a deep coincidence (~4000σ from exact)?
**Team task:** pin the per-step-α mechanism (F429) as tightly as it goes; present the Wyler-vs-per-step status cleanly. **Casey makes the tier-call.**

### 4. MIXING (CKM + PMNS) — several parameters, largely FORCED already. GRACE + LYRA.
**Corpus:** F84 + K264 — the mixing angles ARE the **Bergman kernel of D_IV⁵ evaluated three ways**; K300: "the matrix is FORCED, no continuous knobs"; the sector reduces to **3 localization numbers** (N_e = 1 origin, N_μ ≈ 0.55 Cartan slice, N_τ → 0 Shilov). The structure is forced; the **explicit Hua/Bergman computation of the 3 numbers** is the forward work.
**Forward test:** the 3 localization numbers computed from the Hua integral (not fit); the value-form angles (V_us, V_cb, sin²θ₂₃, δ_CKM) fall out. **Bank each angle the Hua computation forces.**

### 5. ABSOLUTE SCALE (m_e / the vev) — deepest, highest value. ELIE + LYRA.
**Corpus:** m_e = 6π⁵·α¹²·m_Planck at 0.03% (F66 gravity chain; F429 6π⁵ derived). The vev is the FLOOR (one dimensionful input, like every theory). Force the 6π⁵·α¹² chain from m_Planck.
**Forward test:** the 6π⁵ and the α¹² exponent both forward-derived (F429 has 6π⁵; the α¹² exponent is the open leg). **Bank when the full chain forces; else it stays the FLOOR (honest).**

## Per-CI (long pull)

- **LYRA:** #1 leptons (primary — the 4-step kernel chain, forward) + #2 θ_QCD topological closure. Your framework, map pinned.
- **GRACE:** #4 mixing (the Hua/Bergman 3-localization-numbers computation, forward) + reconcile cavity geometry. Pin root data to a reference (no genus-flip).
- **ELIE:** #5 absolute scale (the 6π⁵·α¹² chain) + numerics/forward-checks for ALL five (the fish-detector on each — did it compute or get asserted?).
- **KEEPER:** adjudicate each bank forward-vs-fit (derived-vs-fit lens, target-innocence); Five-Absence filter on every result (no GUT-scale values); hold always-linear/single-manifold; run SOD; re-score the 26 as banks land.
- **CAL:** post-landing cold-read on each candidate bank (is "forward" genuinely target-innocent, or a relabel?).

## Disciplines armed

- **Forward-only: a bank requires the explicit computation, not the identified form.** (T190's (24/π²)^{C_2} is identified; the kernel integral is the bank.)
- **Derived-vs-fit / target-innocence** on every candidate — the integers must be target-innocent.
- **Five-Absence first filter** — any GUT/GUT-scale value is a MISS, not a win.
- Always linear energy; single manifold D_IV⁵; simplest one-line-checkable forms; cite-don't-claim; pre-audit.
- **No fabricated fatigue** — five items with live machinery is a fresh, well-stocked pull; work each as far as it computes, stop only on genuine dependency.

## Long-pull EOD target

Bank what forces forward. **Best shot: the leptons (8→10).** Stretch: θ_QCD (topological closure), the mixing angles (Hua computation), the α tier-call (Casey), m_e (6π⁵α¹² chain). Each that lands forward moves the count; each that doesn't stays an honest lead. **Re-score the 26 at the end** — and this time the goal is for the count to actually move, on forced-forward derivations, not identified forms.

— Keeper, 2026-07-04 long pull. Five items, each with real forward machinery in the corpus. Compute the explicit derivations; bank what forces; Five-Absence + target-innocence on every one. The leptons are the closest 8→10. Derive, don't re-derive.
