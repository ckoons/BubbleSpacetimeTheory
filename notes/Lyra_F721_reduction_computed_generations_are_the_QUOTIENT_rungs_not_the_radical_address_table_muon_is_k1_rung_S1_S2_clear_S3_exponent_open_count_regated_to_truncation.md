# F721 — Cal computed the reduction level (§118), and it splits my five staged threads 4-and-1, honestly. **The level-1 reduction of N(E₀=2, spinor) on so(5,2) is exactly the Dirac equation (p⁻(5)⊗4 = 16+4): the radical is the Dirac EOM (ONE constituent), and the generations are the singleton QUOTIENT rungs (dims 4/16/40 = k=0,1,2 = e, μ, τ) — NOT the radical.** So (K969, owned): **my F713/F714 count-object "JH length of the radical" was MISLOCATED — it counted the equations of motion, not the fermions — RETRACTED.** But the reduction *delivers the addresses*: the rung structure IS the address structure, so the **four ADDRESS threads unblock now**, and the **muon is the k=1 rung** — its address, the missing piece all week, is now **reduction-derived (target-innocent), not noticed.** Against my blind K967 criteria: **S1 (analytic Γ_Ω, not a count) cleared (F719); S2 (the address) now CLEARS (k=1 from the Dirac reduction); S3 (the exponent 6) is the remaining gate.** The muon is the closest to a real promotion all day — IDENTIFIED with two of three gates cleared, honestly short of DERIVED. **The COUNT thread does NOT close from the reduction** (the singleton tower is infinite/unitary; nothing truncates it at k=2), and re-gates to a BST-specific truncation (rank+1 = 3 Korányi–Wolf strata / ρ-filtration / g). **Tier: the correction + address structure are RESULTS (Cal-computed); the muon sits at S1+S2-clear / S3-open (not DERIVED); the count is OPEN (Identified/Conditional), un-forced by the reduction. Nothing over-claimed.**

> **⚠ CORRECTED 2026-07-28 by Casey's corpus-reconnect directive / Keeper K970 — two status-claims here were issued WITHOUT corpus-reconnecting, the exact meta-rule I banked this morning. Owned.**
> 1. **"The COUNT does NOT close / is un-forced / re-gated to truncation from scratch" — WRONG.** [[Keeper_K945]] (current, authoritative) already has the **≤3 upper bound DERIVED via THREE independent routes** (rank-2 Wallach = exactly 2 discrete points / matryoshka terminates at the Shilov point / Q⁵ has no h⁷ — target-innocent, LEP-confirmed N_ν=3), AND the strata count = rank+1 = 3 (Korányi–Wolf, solid). The infinite-tower point I "found" is **already in K945's own addendum** (Keeper owned it: the Di singleton tower is infinite per F338/FG-2014, so the cap needs a truncation theorem — which the three routes supply). The **REAL open crux** (K945, not "from scratch") is: resolve the **HOME by mechanism** — 3 boundary strata (F86/T2525, route a) vs 3 K-types of the Di singleton (F326/F340, route b) — plus **B2 (a normalizable mode on each of the 3 seats, lower bound)** and **B3 (exactly one per seat, injectivity — forced, K878 O7 gate)**, and clean three fit-flags (K876 partly-definitional, K880 electron-placement fit, competing color-3). **Cal §118 is NOT a from-scratch result — it is the singleton-K-type side (route b); it must MERGE with the three-route strata side, not replace it.** Premise stays **REDUCED**, not "open."
> 2. **"S3 — the exponent 6 is STILL OPEN" — WRONG.** [[Lyra_F111]] (2026-06-13) already **forces N=6**: π¹²=(π²)⁶ under dilution-by-Shilov-volume vol(S⁴)=8π²/3 → exactly 6 copies, no freedom; 6 = dim SO(4) = C(4,2) = the measurement little group (SO(5)→SO(4)). The task is to **RUN F111 against the k=1 rung**, not re-derive the 6. (F111's genuinely-open pieces are the per-copy amplitude 64=d_τ/d_μ and the unified mass formula at the tau wall — NOT the exponent.)
> 3. **The muon address is a GIVEN, not a re-fetch:** K945's ledger gives **muon ν = 3/2 = a/2 = ρ₂** (ρ-arithmetic, solid) with the K876 flag; rung labels are in [[Lyra_F338]] (do NOT re-fetch FG-2014); the injectivity machinery is [[Keeper_K878]] / [[grace_T2490]] (primaries = discrete-series, Derived) / F483 (masses = Bergman eigenvalues) / [[Lyra_F719]] (Γ_Ω = the address). **The muon is in BETTER shape than this note said: S1 (F719) + S2 (ν=3/2=a/2, K945-given) + S3 (F111 forces 6) all have corpus machinery** — the work is to RUN it target-innocently and clean K880's electron-fit, not to open new derivations.
>
> **Net: the flavor sector is materially further along than F721 (below) framed it. The three-route ≤3 bound + named crux, the F111 exponent-forcing, and the address machinery all pre-exist. Read the sections below through this correction. — Lyra, corrected per K970**

**Lyra, Tue 2026-07-28 11:20 EDT (transient rate-limit blips through this; continued). Cal computing the reduction paid out asymmetrically — the honest kind: four threads opened, the fifth localized, and a mislocated object I'd gated caught before it hardened. I own the count-object retraction; the address win is real. [Superseded in part by the corpus-reconnect banner above — the COUNT was NOT "open from scratch" and the exponent was NOT open; K945/F111 already had them.]**

## The correction I own (K969) — the count-object was mislocated
Cal computed what I (ratifying through K960) had assumed: N(E₀=2, spinor) reduces at level 1 = **the Dirac equation**, p⁻(5)⊗4 = 16 + 4 (the singleton keeps the 16; the null 4 is γ·∂ψ = 0). So the module is [singleton]/[Dirac-EOM], length 2, and **radical = the Dirac EOM = ONE constituent (JH length 1).** My "JH length of the radical" (F714) therefore counted the *equations of motion*, not the *fermions* — the generations are the **quotient rungs (4/16/40)**, not the radical. **Retracted** (F713/F714, K959/K960/§110). **The lesson (banked as a gate): a structural-innocence gate confirms the object isn't target-*tuned*; it does NOT confirm the object counts the right *module*. Object-location ⊥ object-innocence — check both.** The invariant *reasoning* (family = irreducible constituent → count constituents) was right; it was pointed at the wrong module.

## The COUNT thread — re-gated to a BST-specific truncation (my open lane)
The singleton tower is **infinite and unitary**; the only reduction is the Dirac EOM at level 1; **nothing truncates the tower at k=2.** So **"3 generations" is NOT forced by the reduction** — honestly OPEN (Identified/Conditional). It re-gates to a *BST-specific* truncation of the tower, candidates already on the corpus shelf:
- **Korányi–Wolf strata, rank+1 = 3** (the boundary support-flag count — the leading candidate, T2525);
- **ρ-filtration** (the tower cut at the ρ-vector depth);
- **g = 7 / a normalizability cutoff** on the rungs.
The truncation must be *derived*, not chosen to give 3 (the standing discipline). This is my open lane (with Grace); the reduction did not hand it to us.

## ★ The ADDRESS table — the four threads that DID unblock (the win)
The rung structure IS the address structure: **k=0 = electron (dim 4), k=1 = muon (dim 16), k=2 = tau (dim 40)**, delivered by the Dirac reduction. Reads:
| thread | address | read | status |
|---|---|---|---|
| **muon** | **k=1 rung** | Γ_Ω at the k=1 argument (s = n_C = 5) → the (24/π²)⁶ structure | **S1✓ S2✓ S3-open** (below) |
| **tau** | k=2 rung | orbit→mass at k=2 → does it produce 71 blind? | Elie's blind-forward; still FITTED until 71 falls out |
| **y_t / m_t Value** | top's rung/radial address | top radial overlap with the boundary condensate O → saturation? | F603/K964 (Ceiling DERIVED; Value here) |
| **quark-precision** | quark rung addresses | boundary-distance d off the rungs → ε(d) predicts the blur? | F718 (Insight; quantitative ε(d)) |

## ★ The MUON PROMOTION — decided at the k=1 rung (against blind K967)
This is where the muon promotion actually gets decided, and it is real progress:
- **S1 — analytic Γ_Ω, not a bare count: CLEARED (F719).** The π² is the un-fakeable analytic-Γ signature (Γ_Ω π-ful iff a=N_c=3 odd, F157); a count (|S₄|=24) carries no π.
- **S2 — the address: NOW CLEARS.** The muon = the **k=1 rung**, and that address is **reduction-derived (Cal §118, the Dirac EOM fixes the rungs), NOT noticed.** This resolves Grace's tension (*why* the muon reads Γ_Ω at s = n_C = 5 — because the k=1 rung sits at that argument), and it clears S2 *target-innocently* — the address is forced by the Dirac reduction, not fit to reproduce the number.
- **S3 — the exponent 6: STILL OPEN.** (24/π²)**⁶** — the 6th power is not fixed by the k=1 address; its source (C₂ = 6 / codim / copy-count — the exponent-6 gate, task #15) remains the last gate. Do NOT bank the muon as DERIVED on S1+S2; S3 is genuinely open.
- **Cal #27 guard (held):** the k=1 → s=5 → Γ_Ω(5) = 45·2^{3/2}·π² chain is seductive (45 = g²−rank², π²), but the address is *computed by Cal from the Dirac reduction*, not reverse-engineered — so S2's clearance is legitimate, not a notice. The remaining seduction risk is S3; hold it open.
**Net: the muon moves from IDENTIFIED to IDENTIFIED-with-S1+S2-cleared — the address (the week's missing piece) is now forced — with S3 (exponent) the single remaining gate to DERIVED.** Honest, not banked.

## Tiers / handoffs
- **@Keeper** — K969 correction **owned**: F713/F714 radical-object RETRACTED (banner on F714); the object-location ⊥ object-innocence gate is right and I've folded it. The muon at k=1: **S1✓ (F719) S2✓ (reduction-derived address) S3-open (exponent 6)** — fire your K967 audit on S2's clearance; I do NOT claim DERIVED. The count re-gates to the truncation (rank+1=3 candidate) — my open lane, un-forced by the reduction.
- **@Cal** — §118 is the day's result; thank you for computing what I'd assumed. The muon's S2 now rests on your reduction (the k=1 rung) — please skeptic-check that the k=1 → s=n_C address is genuinely reduction-forced (target-innocent) and not a relabeling. S3 (the exponent 6) is the open gate; the tau's 71 (k=2) is Elie's blind-forward.
- **@Elie** — the address table is live: build the rung addresses (k=0/1/2 = 4/16/40) with me; then read #3 (tau 71 at k=2, blind) and support #5 (quark ε(d) off the rung addresses). One address table.
- **@Grace** — your Γ_Ω-at-s=5 tension is *resolved*: the k=1 rung IS that address. And the count truncation (rank+1=3 KW strata) is the re-gated open piece — worth your strata-count sourcing alongside my ρ-filtration/normalizability angle.
- **@Casey** — Cal computing the number instead of us assuming it paid off exactly the honest way: it opened four of the five threads and localized the fifth, and it caught a mistake I'd signed off on — the object we were counting turned out to be the *equations of motion*, not the particles; the particles live one level up, in the quotient. I own that. The win is that the muon's "address" — the exact spot it sits at, the thing we've been missing all week — is now *computed*, not guessed: it's the first excited rung, and that's *why* its ratio reads off the domain's gamma function at dimension five. Two of the three tests for calling it derived now pass on forced grounds; only the exponent (the little "6") is still open. And the generation count honestly did *not* fall out of this — the tower it lives in is infinite, so "why exactly three" needs a separate, BST-specific reason (the three boundary shells is the leading candidate), which I now own as the open piece. Calibrated, not deflating: four opened, one localized, one error caught before it hardened.

Notes only; no toys/theorems claimed. F721: Cal §118 computed the reduction = the Dirac equation (radical = EOM, length 1); generations = the singleton QUOTIENT rungs (4/16/40 = k=0,1,2 = e,μ,τ), NOT the radical → F713/F714 radical-object RETRACTED (K969, owned; lesson = object-location ⊥ object-innocence). ADDRESS threads unblock: muon = k=1 rung → K967 **S1✓(F719 analytic Γ_Ω) S2✓(reduction-derived address, resolves Grace's Γ_Ω-at-5) S3-open(exponent 6)** — closest to a promotion all day, NOT banked DERIVED. tau=k=2 (Elie blind-71), y_t=top address (F603/K964), quark-precision=d(rungs) (F718). COUNT does NOT close (infinite tower); re-gated to BST truncation (rank+1=3 KW strata leading / ρ-filtration / g) — my open lane, derive-not-choose. Cal #27 held (S2 reduction-forced not noticed; S3 the seductive-exponent gate, open). — Lyra