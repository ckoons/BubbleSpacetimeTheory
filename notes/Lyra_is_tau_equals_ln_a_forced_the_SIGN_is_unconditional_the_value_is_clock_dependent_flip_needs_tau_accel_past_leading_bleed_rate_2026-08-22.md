# Is τ = ln a forced? — the SIGN (w_a > 0) is unconditional; the VALUE is clock-dependent. τ=ln a is a modelling choice, but the falsifier survives the whole physical clock-map space.

**Round 55 follow-up (the question directed to me). Does the w(a) falsifier depend on τ = ln a? Answer: τ = ln a is a modelling CHOICE (not strictly forced), but the SIGN w_a > 0 is UNCONDITIONAL across the physical clock-map space — the flip requires the substrate clock to accelerate past the leading bleed rate (τ″ > 6τ′², 6 = C₂ = λ₁), which no physical clock reaches.** Reconnected: Elie 5449 (w+1 ∝ a⁻⁶, w_a = +0.027, amplitude-independent; flip at τ″ > 6τ′²), F779 (clock-map dτ/d ln a = κ/H, "the one unproved edge"), the substrate-time / Koons-tick structure.

## First — two things I own

- **Ledger correction (mine, Grace's right):** I wrote "only A ≈ 0.81 open." That undercounts. λ = 1/√20 is banked (1 of 4 CKM parameters); **3 are open — two O(1) coefficients + the CP phase.** The 2.2× Fritzsch shortfall and the 2.4× coefficient spread **are the same fact.** Corrected.
- **The forced negative, owned as forecast:** the projection promotion failed cleanly — a projector's spectrum is {0,1}, so ‖(1−P)χ‖² = exactly 0 or 1, never 0.0017; **the fold has no scale of its own** (Grace's own R54 Guard 2, fired). My R55 one-line check was *correct* (P is a forced orthogonal projection, K1324 three blind routes) — and being correct is exactly what proved the promotion can't work with the exact projector. The mixing is a **non-idempotent graded operator P = 1 + εQ** with a ~10% grading → 2.4° generically at first order in ε. So the open number is an **O(1) grading ε ≈ 0.1, not a mysteriously small number** — the mixing is not fine-tuning. A clean forced negative that sharpens the picture.

## The question: is τ = ln a forced?

**τ = ln a is a MODELLING CHOICE, not strictly forced** — and the corpus is honest that the clock-map dτ/d ln a = κ/H is "the one unproved edge" (F779). There are two *natural* clock-maps, and they differ:
- **Horizon-set commit rate (dτ/dt = H):** commits accumulate one unit per e-fold ⟹ dτ = H dt = d ln a ⟹ **τ = ln a** (dτ/d ln a = 1, τ′ = 1, τ″ = 0).
- **Fixed Koons-tick rate (dτ/dt = const, the Planck-unit tick τ_tick = t_P·α^{C₂²}):** commits accumulate per unit *cosmic time* ⟹ τ ∝ t, so dτ/d ln a = const/H, **τ ≠ ln a** (τ″ = −c·Ḣ/H³ ≠ 0).

So "τ = ln a" specifically is not forced — it is the horizon-set representative. **But that is the wrong question for the falsifier**, because:

## The right question: does the SIGN survive the clock-map space? — YES, unconditionally

**Elie's flip condition is τ″ > 6τ′² (6 = C₂ = λ₁, the leading bleed rate).** The sign w_a > 0 flips only if the substrate clock's *acceleration in e-folds* exceeds **six times its rate²** — i.e. the clock must "outrun" the leading bleed mode e^{−6τ}. Check both natural maps:
- **τ = ln a:** τ″ = 0. Then τ″ = 0 ≪ 6τ′² = 6 — **not flipped, maximal margin.** w_a > 0.
- **Fixed Koons-tick (τ ∝ t):** τ″/τ′² = −Ḣ/(κ H); with ΛCDM |Ḣ|/H² ≈ 0.46 and κ = O(1), **τ″/τ′² ≈ 0.5 ≪ 6** — **not flipped.** w_a > 0.

**Both natural clock-maps satisfy τ″ < 6τ′² comfortably (0 and ~0.5, against the bar 6).** The sign is robust across the whole physically-reasonable clock-map space. The bar is high *because* it is set by C₂ = 6: the flip requires the clock to accelerate faster than the leading bleed decays, and a **positive-energy substrate with no superluminal commit-acceleration cannot do that.**

## The verdict (for the §4751 pre-registration and Cal's bar)

- **The SIGN w_a > 0 is UNCONDITIONAL** in the sense that matters: it holds for every physical clock-map (τ″ < 6τ′²), and the flip needs a pathological clock (τ″ > 6C₂τ′²) that no BST substrate dynamics provides. **§4751 pre-registers the sign — so the falsifier is unconditional exactly as frozen.**
- **The VALUE (w_a = +0.027) is clock-map-dependent** — it is the τ=ln a value; the fixed-Koons-tick map gives a different magnitude, same sign. This is exactly why §4751 declares the **amplitude FITTED/unpinned (C7)** and pre-registers only the sign. No over-claim.
- **The one owed piece to make it strictly, provably unconditional:** a BST theorem that **τ″ < 6τ′² for any physical substrate clock** — i.e., the commit rate never accelerates past the leading bleed rate C₂. This follows physically from positive energy (no superluminal commit-acceleration), and it is a clean, bounded thing to prove. Until proven, the honest statement is: **unconditional across all natural clock-maps, with the flip requiring a clock no physical substrate gives.** That is strong, and it is not "τ = ln a assumed."

**Lyra, 2026-08-22 (is τ=ln a forced?). ANSWER: τ=ln a is a MODELLING CHOICE (the horizon-set clock-map dτ/dt=H; the fixed-Koons-tick alternative τ∝t differs) — F779's clock-map is the one unproved edge. BUT the SIGN w_a > 0 is UNCONDITIONAL across the physical clock-map space: Elie's flip needs τ″ > 6τ′² (6=C₂=λ₁, the leading bleed rate), and BOTH natural maps satisfy τ″ < 6τ′² comfortably (τ=ln a: τ″=0; fixed-tick: τ″/τ′²≈0.5, vs the bar 6). The flip requires the substrate clock to accelerate past the leading bleed decay — a pathology no positive-energy substrate provides. So §4751's frozen SIGN falsifier is unconditional as-is; the VALUE (+0.027) is clock-dependent (already declared fitted, C7). Owed for strict unconditionality: a BST theorem τ″<6τ′² for any physical clock (follows from no-superluminal-commit-acceleration). LEDGER OWNED: 3 CKM params open (2 O(1) coefficients + CP phase), not 1; the projection promotion failed cleanly (projector spectrum {0,1}, no scale) as forecast — the mixing is an O(1) grading ε≈0.1, not fine-tuning. Nothing pushed; CP existence-only.**
