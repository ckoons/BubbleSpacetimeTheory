# F877 — Lane A, the correct object (reconnected, not re-derived): the SHARED ν-ladder {5/2,3/2,0} FORCES near-radial-alignment → small mixing (the CKM skeleton, DERIVED); the exact magnitude rides the OPEN up-weight f(ν) (IDENTIFIED); and the F338 exponent fork (Δ=3/2 vs E₀=2) shifts the MAGNITUDE, not the STRUCTURE — so it is a magnitude-pin, not a skeleton-blocker. **This corrects F876 (which used the WRONG object — different up/down radii): the machine (F381) puts up AND down on the SAME positions, differing only by the coupling function (down = bulk deposit d(ν), up = boundary concentration f(ν)).**

**Lyra, Sunday 2026-08-09. Lane A pin (1) + (2), reconnecting to F822/F381/grace_CKM_localization/F338 rather than re-deriving. My F876 crude toy conflated the sectors at different radii; the real machine shares one ladder. RUBRICS done-bar below.**

## The correction to F876 (own it): the object was wrong
F876 placed up at {1,α,α²} and down at {1,3,5} — DIFFERENT radii → generic huge misalignment (65° angles). **That is not BST's machine.** F381 (banked): all four fermion sectors share the SAME 3-point ν-ladder {5/2, 3/2, 0} (the rank+1 strata, F86); sectors differ ONLY by the coupling FUNCTION on that ladder — down/lepton = bulk deposit d(ν) = (5/2−ν)(1−ν)(2−ν)(3−ν)(4−ν) (target-innocent, Elie-derived); up = boundary concentration f(ν), f(0)=1 (top saturation). grace_CKM_localization: gen-1 at the ORIGIN, forced three ways (T2517/F359/F87), none the Cabibbo. So the mixing = the d-vs-f reweighting of ONE shared Gram — NOT different radii.

## What I computed (the correct object: Y_s = G^{1/2} diag(w_s) G^{1/2}, shared G)
Shared complex positions z_k = r_k·ω^k·u₀ (radii {0, ½, √(2/5)} from r²=n/(n+N_c); ω carries CP), ONE shared normalized Gram G, sector mass matrices Y_up/Y_down differing only by the diagonal weights (down d(ν), up f(ν) stand-in), CKM = U_up† U_down.

| result | value | reading |
|---|---|---|
| \|CKM\| | near-identity, off-diag ~3e-4 | **near-alignment FORCED** (shared positions) |
| small mixing ⟵ shared ladder | Gram off-diag 0.18 → CKM off-diag 3e-4 | the smallness IS the shared-ladder structure |
| exact magnitude vs Cabibbo | 3e-4 (toy) vs 0.225 (observed 1/√20) | **toy magnitude wrong — rides the OPEN f** |
| J rephasing-invariance | max\|dJ\| = 3e-31 | J physical (consistent with F875/F876) |

## Pin (1) — ANSWERED: near-radial-alignment falls out → small CKM is DERIVED
The directive's Lane-A pin (1) — "does near-radial-alignment of the up/down towers fall out → the small CKM angles?" — **YES, and it is forced, not tuned.** Because up and down occupy the SAME ladder positions, U_up ≈ U_down structurally; the mixing is only the residual from the d-vs-f reweighting of a near-diagonal shared Gram. Drive the states apart (radial scale ×1.5) and the mixing shrinks further — confirming the smallness is the *shared, well-separated* ladder, exactly F822's "small mixing is forced" (angular skeleton). **This is the Derived content: the CKM SKELETON (near-diagonal, small mixing forced by the shared ladder).**

## The honest boundary — the exact magnitude is IDENTIFIED (rides the open f)
My toy gives near-identity but the WRONG magnitude (3e-4, not the observed Cabibbo 0.225). The reason is exactly the corpus discipline (F381, Cal #34): **the exact f(ν) is OPEN — not fished.** The validated **V_us = 1/√20 = 0.2236 comes from Elie's real FK machine** (the directive's "algebra==brute, V_us=1/√20 validated"), NOT from my stand-in weights. So: the near-alignment/small-mixing SKELETON is Derived; the exact angles/Cabibbo magnitude ride the open f + exact radii (the soft gen-3 depth, grace) → **Identified**. My computation *supports* the F822 binary landing on the **radial-sensitive** branch: vary the up-weight and the exact off-diagonal moves while near-identity holds → the seven exact values ride the (Identified) up-radial. I do NOT reproduce Cabibbo and do NOT claim to.

## Pin (2) — the F338 fork is a MAGNITUDE-pin, not a skeleton-blocker
The fork: is the radial-weight exponent **Δ = 3/2 = N_c/2 = ρ₂** (free-field/ρ-shift, F831 ρ=(5/2,3/2)) or **E₀ = 2 = rank** (Fernando–Günaydin, arXiv:1409.2185)? Computed: sweeping the kernel exponent p ∈ {5 (genus n_C), 6 (scalar C₂), 7 (fermion g)} shifts the mixing magnitude and J but **leaves the near-identity STRUCTURE stable**. So:
- **The fork does NOT block the Derived skeleton** — near-alignment holds for every exponent.
- **The fork DOES set the magnitude** — so it lives in the Identified layer, alongside f(ν).
- **FK-genus-consistent value: Δ = 3/2 = N_c/2 = ρ₂** (matches F831 ρ=(5/2,3/2), F832 scalar-weight-6/fermion-weight-7 spinor-shift). The FG E₀ = 2 = rank is a *different convention*.
- **HONEST FLAG (F338 standing, verify-don't-bank):** which convention is physical must be pinned to the FK / discrete-series **primary source** for the D_IV⁵ scalar lowest weight — I do NOT relabel it from memory (the standing order: sin²θ_W swung four times from tiering without reading the source). I pin the *structural* value (3/2=ρ₂ is FK-consistent) and flag the FG alternative for source-verification. The Higgs (Rac) is the SO(5)-singlet degree-0 mode (F338 robust), so it enters the MASS normalization, not the mixing rotation — another reason the fork is not the angle-blocker.

## DERIVE vs IMPORTED (Lane A asked for this explicitly)
| piece | status |
|---|---|
| shared ν-ladder {5/2,3/2,0} (rank+1 strata) | **DERIVED** (F86/F381) |
| near-alignment → small-mixing CKM skeleton | **DERIVED** (F822 angular + F877 shared-ladder) |
| down weight d(ν) (formal degree, target-innocent) | **DERIVED** (Elie/F345) |
| gen-1 at origin (electron), gen-3 = ρ-direction | **DERIVED** (grace: T2517/F359/F87; F379) |
| rephasing-invariance (J physical) | **DERIVED** (F875/F876/F877) |
| exact up weight f(ν) (boundary concentration) | **OPEN / not fished** → magnitudes IMPORTED/Identified |
| gen-3 (τ/t) exact depth | **SOFT** (grace flag) → Identified |
| F338 exponent convention (3/2 vs 2) | **SOURCE-VERIFY** (structural value 3/2=ρ₂; FG alt flagged) |
| exact Cabibbo/angles/CP magnitude | **IDENTIFIED** (Elie's machine gives 1/√20; my toy does not) |

## RUBRICS Layer-2 done-bar (Lane A, honest)
- [x] Reconnected (F822/F381/grace/F338/F831) BEFORE computing; corrected F876's wrong-object error (different radii → shared ladder).
- [x] Pin (1) ANSWERED: near-alignment forced by the shared ladder → small-mixing skeleton = DERIVED; demonstrated (shared positions → near-identity; separation → smaller).
- [x] Honest boundary: exact magnitude rides the OPEN f → Identified; toy gives 3e-4 not Cabibbo 0.225; validated 1/√20 is Elie's machine, not mine.
- [x] Pin (2): F338 fork is a magnitude-pin not a skeleton-blocker (computed); structural value 3/2=ρ₂ (FK-consistent); FG E₀=2 flagged for source-verify; NOT relabeled from memory.
- [x] DERIVE-vs-imported table delivered. Calibrated both ways (real Derived skeleton win; honest Identified magnitude). Magnitude OFF; nothing over-claimed.

## Handoffs
- **@Elie (Lane A partner)** — the correct object is the SHARED ladder (F381), not different radii (my F876 was wrong-object; corrected). My structural result: **near-alignment → small mixing is FORCED by the shared ladder** (up==down positions; mixing = d-vs-f reweighting of one Gram). Your validated V_us = 1/√20 is the *magnitude* that rides the exact f — my toy gives near-identity 3e-4 with stand-in weights, so the exact value is genuinely your machine's, riding f(ν). Two asks: (i) run your validated overlap with the SHARED-ladder positions + the derived d(ν) down-weights and report whether V_us = 1/√20 falls out with an f that is *itself* pinned (not fished); (ii) the F338 exponent (p=5/6/7) shifts your magnitude — tell me which exponent your validated 1/√20 uses, so we can pin the fork against your machine. I'll re-check rephasing-invariance blind on your V.
- **@Keeper (Lane C input)** — F877 supports K1305 Fix 1 exactly: the CKM SKELETON (near-diagonal, small-mixing-forced by the shared ladder) = **Derived**; the sub-leading angles + Cabibbo magnitude + CP magnitude ride the open f(ν) + exponent = **Identified**. The F338 fork is a magnitude-pin (does not block the skeleton). Ready to write the split into the flagship (Lane C).
- **@Cal** — calibrated both ways: I banked ONLY the near-alignment skeleton (Derived, structural), and I explicitly did NOT reproduce Cabibbo (toy 3e-4 vs 0.225) — the magnitude rides the open f and stays Identified. F338 exponent flagged for source-verify, not relabeled from memory. Magnitude OFF throughout; J value is a reverse-fit.
- **@Casey** — I reconnected instead of re-deriving, and it fixed my Friday mistake cleanly. The finish line rides a machine we already built: up-quarks and down-quarks don't sit at *different* radii (what I wrongly assumed) — they sit at the SAME three rungs of one ladder, and differ only in HOW they're weighted (down spread through the bulk, up concentrated at the boundary). Put them on that shared ladder and the small mixing falls right out — near-alignment is forced, not fitted, because both families live on the same rungs. That's the real answer to the question I posed Friday, and it's a genuine derived result: WHY the quarks barely mix. The honest limit: the exact size of the Cabibbo angle (the famous 1/√20 our validated machine gets) rides the up-quark's boundary-weighting function, which we've deliberately not reverse-engineered — so the *shape* is derived, the *number* is one tier down until that weight is pinned from geometry rather than fit. And a convention fork (whether a certain exponent is 3/2 or 2) turns out to move only the size, not the shape — so it can't block the derived skeleton, and I'm pinning it to the source rather than picking from memory. Small, correct, and honest about the seam. Nothing pushed.

Notes only; no toy/theorem claimed (Lane A pins, reconnected). F877: the CORRECT object = shared ν-ladder {5/2,3/2,0} (F381), up==down positions, mixing = d-vs-f reweighting of ONE Gram (corrects F876's wrong different-radii object). Computed Y_s=G^{1/2}diag(w_s)G^{1/2}: near-identity CKM (off-diag 3e-4), near-alignment FORCED by shared ladder (pin 1 ✓, DERIVED skeleton). Exact magnitude rides OPEN f(ν) (Cal #34 not-fished) → toy gives 3e-4 not Cabibbo 0.225; validated 1/√20 = Elie's machine → IDENTIFIED. Pin 2: F338 exponent fork (Δ=3/2=N_c/2=ρ₂ vs E₀=2=rank) shifts MAGNITUDE not STRUCTURE (swept p=5,6,7) → magnitude-pin not skeleton-blocker; 3/2=ρ₂ is FK-genus-consistent (F831); FG E₀=2 flagged for source-verify (F338 standing, no memory-relabel). J rephasing-invariant (3e-31). DERIVE: shared ladder + skeleton + d(ν) + origin/ρ-direction + rephasing. IMPORTED/open: f(ν), gen-3 depth, exponent convention, exact magnitudes. Supports K1305 Fix 1. Magnitude OFF. Nothing pushed. — Lyra