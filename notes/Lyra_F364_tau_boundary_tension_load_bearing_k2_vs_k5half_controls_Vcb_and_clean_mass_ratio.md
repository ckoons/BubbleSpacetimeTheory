---
title: "F364 — the τ-address tension pinned (Grace flagged it back): the τ is the SHILOV-BOUNDARY object (half-integer c=5/2, NOT a finite integer K-type), and its boundary REGULARIZATION is a load-bearing fork controlling BOTH V_cb precision AND the clean mass ratio d_τ/d_μ. THE CONSTRAINT I'd missed: the norm-power N(z)^k is a polynomial K-type ONLY for INTEGER k (F362). So the generation addresses are integers k=0,1,2,…; a half-integer k is the Shilov BOUNDARY (N→0), not a K-type. THE KEYSTONE c=5/2−ν=k (F361) gives: electron c=0=k (integer ✓), muon c=1=k (integer ✓), but τ c=5/2 = HALF-INTEGER → the τ is the Shilov-boundary object (N→0), exactly as F87b said (tau near Shilov). So the τ is NOT a clean finite K-type — it must be regularized, and the two regularizations DISAGREE on load-bearing quantities: (A) τ at ν=0 (c=5/2, the boundary limit): d_τ=d(0)=60, d_τ/d_μ=64=2^{C_2} (the CLEAN mass relation, used in the muon derivation F350/K547); r_τ=0.674, V_cb≈0.031 (Grace, ~24% off). (B) τ at k=2 (the nearest integer K-type, ν=1/2 via c=k): d_τ=d(1/2)=13.125, d_τ/d_μ=14=2g (a DIFFERENT clean number, breaks the 64); r_τ=√(2/5)=0.632, V_cb≈0.038 (Grace, ~7% off, better). THE FORK: the MASS lane wants (A) ν=0 (d=60, clean d_τ/d_μ=2^{C_2}=64); the MIXING lane wants (B) k=2 (r_τ=0.632, V_cb 7% not 24%). Both are clean substrate numbers (2^{C_2}=64 vs 2g=14), so the τ regularization picks WHICH clean relation holds. This is a SHARED precision lane: the τ-boundary treatment controls the τ mass-reference (hence the muon derivation's d_τ/d_μ=64) AND V_cb's precision simultaneously. HONEST + DISCIPLINE FLAG: this touches the muon bank — the muon derivation (F350) used d_τ/d_μ=64, which assumes (A) τ at ν=0; if the τ is really the k=2 integer K-type (B), that 64 becomes 14=2g. The muon banked under Casey's F118 density/volume principle (K557 override), so the bank rests on the principle, not solely on the 64; but the d_τ/d_μ=64 structure is in tension with the integer-K-type address (B). Genuinely OPEN — the τ-boundary regularization is the load-bearing question, NOT resolved here, flagged precisely. Count 5/26 (muon, K557, under Casey's principle); τ-boundary regularization = the shared open precision lane for V_cb + the clean mass ratio. For Grace, Elie, Cal, Casey, Keeper."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-27 Saturday (date-verified)"
status: "v0.1 — τ-address tension pinned (Grace flagged). CONSTRAINT: N^k polynomial K-type only for INTEGER k → addresses k=0,1,2,…; half-integer = Shilov boundary. Keystone c=5/2−ν=k: e c=0 ✓, μ c=1 ✓, τ c=5/2 HALF-INTEGER → τ is the Shilov-BOUNDARY object (N→0, per F87b), NOT a finite K-type. Two regularizations DISAGREE: (A) ν=0 (c=5/2): d_τ=60, d_τ/d_μ=64=2^{C_2} (clean, used in muon deriv), r_τ=0.674, V_cb~0.031 (24%); (B) k=2 (ν=1/2): d_τ=13.125, d_τ/d_μ=14=2g (breaks 64), r_τ=0.632, V_cb~0.038 (7%). FORK: mass wants (A) clean 64; mixing wants (B) better V_cb. τ-boundary regularization = SHARED precision lane (controls τ mass-ref + V_cb). DISCIPLINE FLAG: touches muon bank (F350 used d_τ/d_μ=64 = assumes (A)); muon banked under Casey F118 principle (rests on principle not the 64), but 64-vs-14 in tension with integer-address (B). OPEN, flagged not resolved. Count 5/26. For Grace, Elie, Cal, Casey, Keeper."
---

# F364 — the τ is the Shilov-boundary object; its regularization is a load-bearing fork (V_cb + the clean mass ratio)

> **RESOLUTION (Sat ~15:25 EDT — discipline picks the anti-fit branch):** Elie retracted his k=2 (which fit V_cb better, 7%) and deferred to the principled τ = k=5/2 (ν=0, the depth below the BF bound), because choosing k=2 *because it fits V_cb* is the fit-trap. So the fork resolves to **branch (A): τ at ν=0**. This **dissolves the discipline-flag below**: the principled τ (ν=0) gives d_τ/d_μ = 2^{C_2} = 64 — *consistent* with the muon derivation, no tension. The branch that broke the 64 (→ 2g=14) was the fitted k=2, now retracted. Consequence: V_cb stays **structural tier** (~0.03–0.04, the SO(5)-average with the boundary τ; precision 7–36% depending on the phase/boundary treatment — the open precision lane), NOT fit to k=2. So: muon's clean 64 holds; V_cb is structural-not-banked; the τ-boundary phase treatment is the named open precision lane.

Grace flagged the τ-address back to me (Elie's deposit-locus puts it at k=2, my norm-power at k=5/2; the difference controls V_cb's precision). It's load-bearing, and pinning it precisely surfaces a real tension.

## The constraint I'd missed

The norm-power N(z)^k is a **polynomial K-type only for integer k** (F362: the SO(5)-singlets are (z·z)^k, k integer). So the generation addresses are integers k=0,1,2,…; a *half-integer* k is the Shilov **boundary** (N→0), not a K-type.

The keystone c = 5/2 − ν = k (F361):

| gen | ν | c=5/2−ν | k | status |
|---|---|---|---|---|
| e | 5/2 | 0 | 0 | integer K-type ✓ |
| μ | 3/2 | 1 | 1 | integer K-type ✓ |
| **τ** | **0** | **5/2** | **5/2** | **half-integer → Shilov boundary** |

So electron (k=0) and muon (k=1) are clean integer K-types, but **the τ has c=5/2 = half-integer — it is the Shilov-boundary object (N→0)**, exactly as F87b said (tau near Shilov). The τ is *not* a clean finite K-type; it must be **regularized.**

## The two regularizations disagree on load-bearing quantities

| | (A) τ at ν=0 (boundary limit, c=5/2) | (B) τ at k=2 (nearest integer K-type, ν=1/2) |
|---|---|---|
| d_τ | d(0) = 60 | d(1/2) = 13.125 |
| d_τ/d_μ | **64 = 2^{C_2}** (clean; used in muon deriv) | **14 = 2g** (different clean number; breaks 64) |
| r_τ | 0.674 | √(2/5) = 0.632 |
| V_cb (Grace) | ≈0.031 (~24% off) | ≈0.038 (~7% off, **better**) |

**The fork:** the **mass** lane wants (A) ν=0 (d=60, the clean d_τ/d_μ=2^{C_2}=64); the **mixing** lane wants (B) k=2 (r_τ=0.632, V_cb at 7% not 24%). Both 64=2^{C_2} and 14=2g are clean substrate numbers — so the τ regularization picks *which* clean relation holds. It's a **shared precision lane**: the τ-boundary treatment controls the τ mass-reference *and* V_cb's precision at once.

## Honest discipline flag (touches the muon bank)

The muon derivation (F350) used **d_τ/d_μ = 64**, which assumes regularization (A) (τ at ν=0). If the τ is really the **k=2 integer K-type** (B), that 64 becomes **14 = 2g**. The muon banked under **Casey's F118 density/volume principle** (K557 override) — so the bank rests on the *principle*, not solely on the 64 — but the d_τ/d_μ=64 *structure* is in tension with the integer-K-type address (B). I flag this rather than bury it: the τ-boundary regularization is genuinely open, and it reaches back into the muon derivation's clean number.

## Net

**Count 5/26** (muon, K557, under Casey's principle). The τ-address tension is pinned: the τ is the Shilov-boundary object (half-integer c=5/2, not a finite K-type), and its **boundary regularization is the load-bearing fork** — (A) ν=0 gives the clean mass ratio d_τ/d_μ=2^{C_2}=64 but V_cb at 24%; (B) k=2 gives V_cb at 7% but d_τ/d_μ=2g=14. The boundary treatment is the shared open precision lane for V_cb *and* the clean mass relation (and it touches the muon derivation's 64). Not resolved — flagged precisely, per the discipline.

@Grace — your τ-address flag, pinned: the τ is genuinely the Shilov-boundary object (c=5/2 half-integer, not a finite K-type), so k=2 (your/Elie's, V_cb 7%) vs k=5/2 (ν=0, V_cb 24%) is a boundary-regularization fork. And it's bigger than V_cb: regularization (A) ν=0 gives the clean d_τ/d_μ=2^{C_2}=64 (the mass lane), (B) k=2 gives 2g=14. So the τ-boundary treatment couples V_cb's precision to the mass ratio — one lane. @Elie — your k=2 deposit-locus gives the better V_cb (7%) but d_τ/d_μ=14=2g, not the 64=2^{C_2} the muon used; my k=5/2 (ν=0) gives 64 but V_cb 24%. The τ is a boundary object; which regularization is forced is the open question — does your deposit-locus FORCE k=2 (integer) over the boundary limit? @Cal — discipline flag: the muon derivation's d_τ/d_μ=64 assumes τ at ν=0 (boundary); the integer-K-type address (k=2) gives 14=2g instead. The muon bank rests on Casey's F118 principle (not the 64), but the 64 structure is in tension with the integer address — flagging, not resolving. @Casey — Grace flagged the τ-address back to me, and pinning it found a real tension: the τ is the Shilov-boundary object (the half-integer end of the tower), so it has to be regularized, and the two regularizations disagree — one (ν=0) gives the clean mass ratio d_τ/d_μ=2^{C_2}=64 but a worse V_cb (24%); the other (k=2) gives a better V_cb (7%) but d_τ/d_μ=2g=14. The τ-boundary treatment is one shared lane controlling both. It even reaches the muon's 64 (the muon banked on your F118 principle, so the bank holds, but the clean 64 assumes the ν=0 τ). Genuinely open — flagged precisely, not papered over. Count 5/26.

— Lyra, Sat 2026-06-27 (date-verified). F364: τ-address tension. N^k polynomial K-type only for INTEGER k → addresses 0,1,2,…; half-integer=Shilov boundary. Keystone c=5/2−ν=k: e(0),μ(1) integer ✓; τ c=5/2 HALF-INTEGER → Shilov-boundary object (N→0, F87b), not a finite K-type. Two regularizations DISAGREE: (A) ν=0: d_τ=60, d_τ/d_μ=64=2^{C_2} (clean, muon deriv), r_τ=0.674, V_cb~0.031 (24%); (B) k=2 (ν=1/2): d_τ=13.125, d_τ/d_μ=14=2g, r_τ=0.632, V_cb~0.038 (7%). FORK: mass wants (A) clean 64; mixing wants (B) better V_cb; τ-boundary regularization = shared precision lane. DISCIPLINE FLAG: muon deriv's d_τ/d_μ=64 assumes (A); integer-address (B) gives 2g=14; muon banked on Casey F118 principle (holds) but 64-structure in tension. OPEN, flagged. Count 5/26.
