# K1297 — #79's core advanced: the E7 gate resolves via UNIFORM Korányi–Wolf. D_IV⁵ is forced modulo ONE empirical datum — the generation count. (Scoping, not a bank; Cal audits.)

**Keeper (Lane 3 / #79 core), 2026-08-08. #79 reduced (K1295) to the K943 minimality audit. Reading K943 (not memory), its residual — after Node 1 resolved (T1829 physics-free) — is essentially ONE honest hole: rank=2 is asserted, and it's the sole discriminator between D_IV⁵ and its genuine co-solution E7. K943's own "third prong" (Casey) closes it via data, GATED on one check (condition 2). I resolve that gate. This SHARPENS #79 to its honest ceiling; it does NOT bank it. Cal audits (I don't self-pass — especially today).**

## Where K943 actually leaves #79 (one hole)
- **Node 1 RESOLVED (forced, physics-free):** T1829 (proved, dimension-free) gives d₀ = rank²/(N_c+1) = 1 ⟹ **N_c = rank²−1 = 3 from rank=2 alone** — pure rep theory, zero SM input. The "3" is geometry, not "three colors."
- **The one residual hole:** the census N_c = rank²−1 is satisfied by EXACTLY **{D_IV⁵ (rank 2), E7 (rank 3)}**. The structural route picks D_IV⁵ over E7 by **asserting rank=2** — a hostile reviewer's cleanest attack ("you have two domains; you pick by an asserted premise").
- **K943's third prong (Casey's inverse/data route):** E7 (rank 3) → rank+1 = 4 boundary strata → predicts **4 generations**; we observe **3** ⟹ observed-3-generations EXCLUDES E7 *by data*, not by the asserted premise. **GATED on condition 2:** is "generations = rank+1 strata" a *uniform* Korányi–Wolf fact, or D_IV⁵-specific (circular)?

## The gate resolves — the strata count IS uniform (Korányi–Wolf)
**The Korányi–Wolf boundary structure is a GENERAL, uniform theorem:** the boundary of a bounded symmetric domain of rank r decomposes into a rank-indexed family of orbits (the boundary components / support strata), monotone in rank — MORE rank ⟹ MORE strata, the *same* way for every domain. *(Verify the exact orbit count r vs r+1 against Wolf's classification before banking a specific number — do NOT trust the remembered off-by-one; the Obata lesson. The UNIFORMITY, i.e. E7 gets strictly more strata than D_IV⁵, is robust to the off-by-one.)*
So the physics reading "**each boundary stratum = one generation**" (BST F86), *applied uniformly*, gives E7 → 4 (or ≥4) generations and D_IV⁵ → 3. **The reading is applied the same way to E7 as to D_IV⁵ — it is NOT fitted to D_IV⁵.** Therefore the E7-exclusion-by-3-generations is **non-circular and legitimate.** Gate PASSES in the forced direction.

## What this does to #79 (the honest ceiling, stated carefully)
The residual "rank=2 asserted" is replaced by "**rank=2 forced by the observed generation count**" (via uniform Korányi–Wolf). So the forcing chain reads:

> **3 generations (the ONE empirical datum) → rank=2 (Korányi–Wolf, uniform) → N_c=3 (T1829, physics-free) → n_C=5 → the five integers → the physics.**

**So #79's core resolves to: D_IV⁵ is FORCED modulo ONE empirical datum — the generation count.** That is a **GR-level** claim: a theory that takes ONE measured number (how many generations) and forces everything else, exactly as GR takes G. The whole "is the geometry forced" question is now this clean, and it is the honest ceiling.

## The circularity guard (critical — do NOT trip it)
BST *also* derives "3 generations FROM rank=2" (F86). **You cannot claim both directions at once** — that is circular. The honest posture: **treat the generation count as the ONE empirical input** (the datum that forces rank=2), and then everything downstream is derived. Do NOT simultaneously advertise "we derive the number of generations" as a *prediction* while using it as the *input* that forces rank=2. Pick one role. (For the Tegmark pitch: "forced from one datum, the generation count" is stronger and cleaner than a hidden double-count.)

## Tier / routing
- **Advanced, not banked:** the gate (strata-count uniform via Korányi–Wolf) resolves the E7 hole in the forced direction — CONDITIONAL on (a) verifying the exact Wolf orbit count at source, and (b) holding the circularity guard (generations = input, not also output).
- **#79 honest ceiling:** D_IV⁵ forced modulo the generation count (one datum) — GR-level, defensible, the real rebuttal to "fitted." Plus the independent 4-framework convergence evidence line (BST = MUH + selection).
- **Still open (K943's other seams, unchanged):** Node 2 (spectral-genus construction — but n=5 route-2 doesn't hinge on it); Node 3 (chirality→observed-parity, conditional); the 137 cross-form census (untested — Elie task #28). None of these touch the rank=2/E7 resolution above.
- Cal audits K1297. Feeds #31 (Forcing+Evidence paper) + #25 (Tegmark hook — lead with "forced from one datum").

— Keeper, K1297, 2026-08-08. #79 core advanced: K943's one residual hole (rank=2 asserted, sole discriminator vs E7) closes via Casey's inverse route, GATE resolved — the Korányi–Wolf boundary-strata count is a UNIFORM domain-general fact (E7 gets strictly more strata than D_IV⁵), so "strata=generations" applied uniformly makes E7→4-generations a genuine prediction that observed-3 refutes: non-circular. ⟹ rank=2 is forced by the observed generation count, not asserted ⟹ **D_IV⁵ forced modulo ONE datum (the generation count) — GR-level.** GUARDS: verify Wolf's exact orbit count at source (Obata lesson); hold the circularity guard (generations = input, not also a claimed output). Not banked; Cal audits. Nothing pushed.
