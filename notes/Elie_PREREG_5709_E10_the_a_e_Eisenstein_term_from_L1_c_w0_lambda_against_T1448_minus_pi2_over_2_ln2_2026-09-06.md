# PRE-REGISTRATION — Toy 5709, E10 (Round 123): the a_e Eisenstein term from L1's c(w₀,λ), against T1448's −(π²/2) ln 2 = −3.42049…

**Elie, 2026-09-06 (Sunday) 16:09 EDT (shell-copied). Hashed before the run. Inputs: Lyra L8 (7230f1dc), Cal C9 (0d1e9599), T1448's note (h_V(k) = d_k^{(1)}/λ_k², d_k^{(1)} = C(k+3,4), λ_k = k(k+5)).**

## Objects
c(w₀,λ) = f_L(λ₁−λ₂)f_L(λ₁+λ₂)f_S(λ₁)f_S(λ₂) with L1's factors (5704). Diagonal λ₁ = λ₂ = λ: f_L(0) is constant, so D(λ) := d/dλ log c(w₀,(λ,λ)) = d/dλ log[f_L(2λ) f_S(λ)²]. Analytic decomposition (chain rule on ξ, Λ, ε): (A) Γ-terms = ψ values; (B) ζ′/ζ at the shifts 2λ, 2λ+1 (three ratios) and λ±½, λ+3/2 (from Λ(λ), Λ(λ+1)); (C) the ε constants: c ∝ ε(λ)^{−2} on the diagonal, ε = 2^{½−λ}, so ∂ log c ∋ +2 ln 2 (∂ log c^{−1} ∋ −2 ln 2; L8's P1 quotes the magnitude); (D) the comb terms ln 2·[2^{½−λ}/(1−2^{½−λ}) − 2^{−½−λ}/(1−2^{−½−λ})] per short root.

## Hashed lines
- **H1 (L8 P1):** D(λ) by complex-step finite difference at λ = it, t = 5, 10, 20, minus the analytic (A)+(B)+(D), equals +2 ln 2 = 1.3862943611 to 10⁻¹⁰ (sign: on log c; on log c^{−1} it is −2 ln 2). Long roots contribute no constant.
- **H2 (L8 P2 and Cal C9's "two sources"):** at λ = 0 the log-derivative is singular (ξ(2λ)³ poles: ψ(0) and ζ′/ζ(1)); the REGULAR archimedean part carries ψ(½) = −1.9635100260 once per ξ(2λ+1) factor — THREE on the diagonal (f_L(2λ), f_S twice), each contributing −ψ(½) = γ + 2 ln 2 — while the Γ_ℂ(λ+½)/Γ_ℂ(λ+3/2) ratios in f_S give ψ(½) − ψ(3/2) = −2 exactly (their ln 2's cancel). So the regular ln 2 content at λ = 0 is 6 ln 2 from (A) and 2 ln 2 from (C): both present, different provenance (Lyra), and per source each unit is a "−2 ln 2" (Cal's "same coefficient") but the COUNTS are 3 : 1 — (A)+(C) ≠ −2 ln 2 (L8 P2 HIT), and Cal's "same coefficient" is true per unit and false as a total.
- **H3 (L8 P3, ln 137):** for a primitive character χ mod 137 the completed ratio ξ(λ,χ)/ξ(λ+1,χ), ξ(s,χ) = (137/π)^{(s+a)/2}Γ((s+a)/2)L(s,χ), has log-derivative with the constant −½ ln(137/π) = −(½ ln 137 − ½ ln π): a ln 137 piece with coefficient −½ per twisted ξ-ratio (so −ln 137 for a twisted short-root factor with two ratios) — present, exhibited numerically with mpmath's L(s,χ) for one χ; L8's "k ≥ 1 per ramified factor" is right in kind, and k is ½ per ξ-ratio in this normalisation.
- **H4 (L8 P4/P5, Cal C9 "the π²/2 is the test function"):** T1448's h_V lives on the discrete Bergman index k and is not a function on i𝔞*. Under L8's schematic E₀[h] = −(1/(2π)²)(1/|W|)∫Σ_w ∂ log c(w,λ) h(λ)dλ, the ε-constant part is E_ε[h] = −(ln 2/(4π²))·∫h (each short root is inverted by 4 of the 8 Weyl elements), and with the three ψ(½) units added the ln 2 coefficient is C = −∫h/π². T1448's −(π²/2) ln 2 then REQUIRES ∫h = π⁴/2 = 48.70 (or 2π⁴ = 194.8 from ε alone): a normalisation of the test function, not a computed number — L8 P4 HIT, P5's C and ∫h reported.
- **H5 (kernel-swap forecast for E11, no run):** replacing the {2,∞} kernel by a {3,∞} one changes (C) to 2 ln 3 and (D)'s period to 2π/ln 3 = 5.7192, and leaves (A)'s ψ(½) untouched — so the two sources separate by construction; hashed here as the shape E11 must show.

Score X/5.
