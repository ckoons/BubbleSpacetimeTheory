# K1792 — PRE-REGISTERED GATE: route C, V_cb/V_ub as the up/down frame mismatch

**Filed:** 2026-08-22
**Status:** PRE-REGISTRATION — written BLIND, before Elie's route-C compute exists.
**Why blind:** route C (CKM = U_up†·U_down) is the forced survivor of the partial-isometry dichotomy (K1791) and the banked spine (T2530). But "forced structure" is not "forced magnitude" — the magnitude rides the up-sector *eigenvectors*, and that is precisely where a texture can be quietly fitted to V_cb. The bar is fixed here, now, before the numbers.

---

## Context — what route C IS, and what it still owes
- **Structure (forced, banked):** CKM = U_up†·U_down (T2530). Mixing = the off-rank-1 breaking (T2519) = the misalignment of **up = top-saturation ladder {y_t=1, y_c=α, y_u=α²}** vs **down = FK-bulk {1,3,5}** (T2547). V_us = 1/√20 = 0.31% already DERIVED, frame-independent (down-only).
- **What it owes:** V_cb and V_ub are the frame-MISMATCH observables — they take rotation from BOTH sectors, and "over-predict from the down rotation alone" (T2530). So the deliverable is the **up-sector rotation** (U_up's off-diagonal), which needs the up mass-matrix *texture*, not just its spectrum.

## THE GATE — seven conditions. PASS requires all seven. G2, G3, G4 are immovable.

**G1 — the up-sector EIGENVECTORS are forced, not just the eigenvalues.** {1,α,α²} fixes the up *masses* (the spectrum). V_cb/V_ub ride the up *rotations* = the off-diagonal texture of M_up. State the texture (e.g. a geometric texture-zero) and ITS forcing BEFORE computing any angle. "The spectrum is {1,α,α²}" is necessary, not sufficient — a diagonal M_up gives U_up=1 and zero up-rotation.

**G2 — TARGET-INNOCENT (immovable).** The observed V_cb/V_ub never enter the computation. State both frames, compute the mismatch, compare second.

**G3 — THE V_us ROUND-TRIP (immovable, LOAD-BEARING, MAY FAIL — read this one).** The same machinery MUST reproduce the banked V_us = 1/√20 without re-tuning. **Sharp risk, pre-registered:** V_us = 1/√20 was Derived assuming the up 1–2 rotation is negligible (T2530 used √(m_u/m_c)=0.04 ≪ 0.22 from *observed* masses). But the BST up-spectrum {1,α,α²} gives m_u/m_c = α², wait — gives m_u/m_c = α (one α-step per generation), so the naive Gatto up 1–2 rotation would be **√α ≈ 0.085 — NOT negligible against 0.2236.** If route C's up-frame produces θ_u^{12} ≈ √α, then V_us ≈ |0.2236 ∓ 0.085| moves to ~0.14 or ~0.31 and the 1/√20 success BREAKS. So route C must show ONE of: (a) the up texture SUPPRESSES the 1–2 rotation below the naive Gatto value (with a stated mechanism, not a knob), preserving V_us; or (b) V_us shifts, and route C reports the shift honestly. **A route-C V_cb that lands while silently sacrificing V_us is a fail, not a win.** This is the check most likely to be skipped; it is the one I will look at first.

**G4 — NO MAGNITUDE-FITTING of the texture (immovable).** Using the *spectra* {1,α,α²} and {1,3,5} is legitimate — they are the forced masses. The trap is choosing the texture / rotation angles so the mismatch lands on V_cb. The rotations must come from geometry (the forced texture), never back-solved from the observed CKM.

**G5 — tier the up-SPECTRUM honestly; V_ub inherits its coarseness.** {1,α,α²} matches m_t and m_c well but **over-predicts the observed m_u by ~4×** (α²·m_t ≈ 9 MeV vs ~2.2 MeV). V_ub lives in the lightest-up (1–3) sector, so it inherits that ~4× coarseness. V_cb (2–3, charm–top, where {1,α} is clean) can be sharper. Declare V_cb and V_ub at DIFFERENT tiers accordingly; do not let a coarse V_ub discredit a good V_cb, or a good V_cb imply a sharp V_ub.

**G6 — tier the STRUCTURE and the MAGNITUDE separately.** "V_cb/V_ub = the {1,α,α²}↔{1,3,5} frame mismatch" is the forced-form claim (route-C deliverable, one tier). The numerical precision is another. A forced form with a coarse magnitude still PASSES as a structural result — that is real content (K1791: "the mixing lives in the frame mismatch").

**G7 — route-inventory + selection-honesty (C6) + symbol hygiene.** Route C is declared the forced survivor of the exhaustive dichotomy (K1791), not #3-of-N-tried — so no ~log N penalty on the structure. But report the up-texture candidates as a full sweep (which textures close vs don't), not one lifted from an undeclared pool. Subscript every ν_W.

---

## Disposition
When Elie's route-C compute lands, I measure it against G1–G7, with **G2/G3/G4 immovable**. The single most likely quiet failure is G3 (V_us sacrificed to land V_cb) — I check it first. A forced up-texture that (a) preserves V_us and (b) predicts V_cb from the frame mismatch, target-innocent, closes the largest open SM gap at its honest tier — even if V_ub stays coarse (G5). A texture fitted to V_cb, or one that breaks V_us, is CONDITIONAL and goes back to the compute.

**Keeper, K1792, pre-registered blind 2026-08-22. The structure is forced; this gate is whether the magnitude is earned or fitted — and the V_us round-trip is where it will be decided.**
