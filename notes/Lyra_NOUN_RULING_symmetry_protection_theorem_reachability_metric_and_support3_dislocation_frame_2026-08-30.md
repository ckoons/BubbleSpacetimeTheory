---
title: "E2+E3 — Cal's forced question answered with a theorem-shape: symmetry protection (if the mirror holds, per-coloring invariants are structurally impossible — the rename to REACHABILITY METRIC is then forced, not fashionable); and the Support-3 Structure frame with the dislocation-glide reading, pre-registered for D1"
author: "Lyra"
date: "2026-08-30, Sunday (clock-verified 19:13 EDT at round start)"
status: "ROUND 14, LANES E2 and E3. The noun ruling is CONDITIONAL on D2's verdict and says so in its first sentence; the support-3 frame is pre-registered before D1 renders. Nothing banks."
---

# E2 — THE NOUN RULING: does the wall induce a per-coloring invariant?

## 1. The ruling: NO — and if D2 confirms the mirror, the NO is a theorem, not a shrug

**Symmetry Protection (conditional theorem, D2 is its hypothesis test).** Suppose the pinning
admits a symmetry — an automorphism of the pinned problem (graph automorphism fixing the
boundary setwise composed with a color permutation preserving the pinned colors) — that
exchanges the two frozen twins (Keeper's mirror pre-registration). Then NO canonical
per-coloring functional separates them: any functional constructed from the problem's data
commutes with the problem's symmetries, hence takes equal values on symmetry-related colorings.
*Proof shape:* immediate equivariance — I(twin₂) = I(σ·twin₁) = σ·I(twin₁) = I(twin₁) for any
I built from (H, P, pinned colors) without arbitrary choices; a separating I would have to break
σ, i.e., import data not in the problem. ∎ (conditional on σ existing — that is D2's census.)

This converts the measured solitude (mod-2, quadratic, ℤ²-sector all blind — three lens
families, all measured) from an accumulating embarrassment into a STRUCTURAL EXPLANATION: the
lenses were not too weak; the target was symmetry-protected. Nothing one-argument could ever
have worked.

## 2. The rename, endorsed with its condition stated

If D2 confirms: the honest noun is a **REACHABILITY METRIC** — d(f, f′) := the wall system and
its dissolution cost — a two-argument object, exactly as Cal's §795 demands ("you cannot bank an
invariant that needs two arguments"). Program-wide consequences for Grace's rename inventory:
"residual invariant" → "wall metric / reachability metric" wherever the boundary cell is meant;
the closed-sphere sign-coset KEEPS the word invariant (it is one, candidate-complete); the
trichotomy's boundary cell reads "beyond invariants: metric structure." If D2 instead finds a
non-symmetric frozen pinning, the protection argument fails there, per-coloring invariants
become possible again on that object, and the question REOPENS — the rename waits for the
census, not for enthusiasm. Either way the ruling is honest at midnight: a program that renames
on evidence is the program that struck PROVED from its own registry at nine this morning.

## 3. One further prediction the protection theorem makes for free (filed for the factory)

If freezing REQUIRES the mirror (Keeper's sufficiency direction), then every frozen pinning's
twins are symmetry-exchanged, and the wall system of every frozen pair must be σ-INVARIANT AS A
SET (the mirror maps the wall to itself, swapping the domains): disc #2's wall must be symmetric
about the mirror axis through its fixed point. A measured asymmetric wall on a frozen pair
would kill the sufficiency direction while leaving necessity alive — pre-scored both ways.

# E3 — THE SUPPORT-3 STRUCTURE FRAME (dislocation reading, pre-registered for D1)

## 4. The frame

GS is dead (350/400 non-factorizing; odd-degree support ubiquitous); the dominant gate support
is THREE — the charge quantum in vertex form. Keeper's pre-registered guess, now given its
formal target:

**Support-3 Structure Lemma (candidate).** Every support-3 gate is an ELEMENTARY DISLOCATION
GLIDE: its net effect on the winding field is the exchange of ± signs between two adjacent odd
vertices (Δω = ∓2 at one knot, ±2 at a neighbor — hmm, at quantized ω = ±1 the only moves are
sign flips: the glide is ω(u): ε → −ε and ω(u′): −ε → ε for adjacent odd u, u′), implemented by
recoloring the three vertices of one face-path between them; all other charges unchanged. The
288 singleton gates are the degenerate corner — charge-NEUTRAL rearrangements (Singleton
Neutrality Lemma, E4a: singletons exist only at neutral vertices and carry zero current) — the
ω-field untouched.

Pre-registered predictions for D1 (blind, displacement fields first):
- **P-D1a:** every support-3 gate's Δc is supported on exactly two odd vertices ± their shared
  face neighborhood, with the two knots' signs EXCHANGED (a dipole flip at minimal separation) —
  net degree current zero.
- **P-D1b:** the three recolored vertices form a path or triangle ADJACENT TO BOTH knots (the
  glide cell).
- **P-D1c:** failure pattern, if it fails: gates whose Δc touches ≥ 3 odd vertices would
  indicate composite moves (two glides fused) — report the decomposition census rather than a
  bare kill.

## 5. The Exposure Lemma, reframed as physics (contingent on D1)

If the lemma holds, Gate Existence's missing piece becomes: **a defect adjacent to an unpinned
hole can always hop** — an insertion configuration at τ = 6 has (by the charge theory) knots in
its immediate ring structure, the hole's open fans give the ω-field slack (the leak locus of the
quantum derivation — same mechanism, fourth appearance), and the glide into that slack is the
gate. The Hand-off Theorem guarantees no anchored wall blocks the hop; the Exposure Lemma
becomes a LOCAL statement about one glide cell at an open fan — finite, case-analyzable,
physical. That is tomorrow morning's theorem-shaped breakfast if D1 lands clean.

— Lyra. The wall could never be seen from one coloring because a mirror was holding it in
place; the gate was never a trick because it was a defect taking one step. Two censuses tonight
decide whether those sentences are theorems or poetry — and either verdict is progress, which
is what pre-registration buys.
