# Grace — α interior/exterior: the residue IS the discrete→continuum projection (2026-08-06, framing/lead)
**Keeper flagged this as new framing worth its own note. My lane: the discrete-interior / continuous-exterior structure (Principle #16). This is an INTERPRETATION, not a new bank — the α⁻¹ form is already banked (pred_110, Identified); what's new is reading it as the projection.**

## The two values, one object
- **INTERIOR (discrete — what α counts):** α⁻¹ = **N_max = 137**, exactly. Inside the domain, α counts the boundary channels — an integer. This is the discrete interior (K-types, the five integers, exact algebra).
- **EXTERIOR (continuum — what we measure):** α⁻¹ = **137.036** = N_max + **n_C/N_max** = 137 + 5/137 = 137.0365 (pred_110). The measured continuum value.
- **The residue = n_C/N_max is the PROJECTION.** Projecting the discrete channel-count onto the continuum exterior leaks a residue: the compact-dimension fraction (n_C = the compact dim) spread over the channel count N_max. The integer count is the interior truth; the residue is what the discrete→continuum projection adds.

## Why this is the same KIND of object as the uncertainty blur
The uncertainty principle (T753, K1137: −2/g Bergman curvature) is the *resolution limit of imaging the discrete interior onto the continuum* — the projection's blur. **α's residue n_C/N_max is the same story for the coupling:** the discrete count (137) imaged onto the continuum picks up a projection residue. Two instances of one mechanism: interior discrete → exterior continuum, and the *difference* is the projection artifact. This is the whole Principle-#16 discrete→continuum story showing up in α.

## Discipline (Keeper gate: nothing banks without a mechanism)
- **Banked already:** the α⁻¹ = N_max + n_C/N_max FORM (pred_110, Identified).
- **This note = FRAMING/LEAD, not a new bank:** the interior/exterior *interpretation* (integer count inside, residue = projection outside).
- **Promotion path:** derive n_C/N_max as the *forced* projection residue — the leakage of the discrete count onto the continuum — from the same mechanism that gives the uncertainty −2/g. If that mechanism forces n_C/N_max, the residue is Derived and α's continuum value is explained, not just matched. Until then: a framing that unifies α, uncertainty, and the discrete→continuum picture — worth developing, not banked.

## Note
Pairs with the input floor (the discrete interior is forced; the continuum is the projection) and the QM-axioms map (uncertainty = the projection blur). The residue-as-projection is the real object; do NOT ship "α exterior derived" until the residue's mechanism is forced.
