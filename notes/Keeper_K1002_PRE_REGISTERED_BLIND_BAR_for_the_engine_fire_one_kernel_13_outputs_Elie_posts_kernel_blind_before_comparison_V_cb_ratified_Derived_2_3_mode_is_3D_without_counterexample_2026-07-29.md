---
node_type: k_audit
id: K1002
title: THE PRE-REGISTERED BLIND BAR for the engine-fire — one kernel K(ν_i,ν_j) → ~13 outputs (9 masses + CKM + PMNS + CP) is the sharpest target-innocence test the program will run (Cal §139). Committed BEFORE the numbers: Elie POSTS the FK-sourced kernel BLIND (the checker's half) before running the SVD/comparison, so it can't be retrofitted. PASS = falls out with no per-observable tuning. Plus: V_cb ratified "Derived (2-3-mode is 3D-projected, without counterexample)" (Cal §139, muon-template).
date: 2026-07-29
author: Keeper
verdict: The bar is committed blind, now, before the kernel is evaluated. One input, ~13 outputs, no per-observable dial = Derived (whole sector); any dial = named + tiered separately. V_cb carries the identification in its label (never bare).
---

# K1002 — The blind bar for the engine-fire (committed before the numbers)

Cal §139 named the endgame exactly: **the engine-map turns the entire fermion spectrum into ONE blind pin.** One kernel K(ν_i,ν_j) → ~13 outputs. That is the sharpest target-innocence test BST will ever run — and the only way to run it honestly is to **commit the bar before the kernel is evaluated.** This note is that commitment. Filed BEFORE Elie evaluates the FK entries.

## ★★ THE BLIND PROTOCOL (load-bearing — the checker's half committed first)
1. **Elie EVALUATES the kernel from the book, target-innocent.** Diagonal = generalized Pochhammer (ν)_λ (banked — gave m_s/m_d=20, the down ladder 1:20:840). Off-diagonal = FK Ch XII generalized-binomial coefficients coupled through the degree-1 Higgs vector (the (2,2), F603, pinned target-innocently). **The ν-addresses × the FK radial integral — NO observed mass or mixing may enter any entry.**
2. **★ Elie POSTS the full kernel matrix (the INPUT) with its FK provenance BEFORE running the SVD/comparison.** This is the discipline that makes it a real test (commit-the-checker's-half-blind): once the kernel is public and sourced, it cannot be retrofitted to make the outputs land. The kernel is the forced piece; posting it blind is what earns the whole result.
3. **THEN fire toy_4920 (Elie's driver) → all ~13 outputs at once**, compared to PDG. **Score σ = |pred−obs|/experimental-error, NEVER dev%** (each output carried at its own 1σ, scheme-aware).

## ★★ PASS / FAIL (committed)
- **PASS → DERIVED (the whole sector):** the ~13 outputs fall out of the ONE kernel-filled matrix with **zero per-observable tuning.** One object, thirteen numbers, no dials. That is the biggest Derived on the board — the SM fermion spectrum from one geometric object.
- **FAIL / FIT:** any output that needs a separate dial → **name EXACTLY which ones.** Those bank at their own (lower) tier; the rest can still stand. A partial win is a partial win, stated honestly — most fall out, these N need tunes, here they are.
- **The ~13 outputs:** 9 masses (e,μ,τ; u,c,t; d,s,b) + |V_us|,|V_cb|,|V_ub|,δ_CKM/J + PMNS θ₁₂,θ₂₃,θ₁₃,δ + the m₁=0 prediction. Pre-listed so none can be quietly dropped.

## ★ SCRUTINY FLAGS (Cal §133 confined-bulk law — scrutinize clean, don't celebrate it)
- **Soft quantities that come out SUSPICIOUSLY CLEAN → look HARDER, not celebrate.** Light-quark masses (m_u, m_d, m_s), δ_CKM, V_ub are confined-bulk / Tier-2-messy by the §133 law; a too-clean soft number signals a hidden tune or a lucky coincidence, not a triumph. Banked-clean checks (m_s/m_d=20, V_us=1/√20) already cleared their blind bar — new clean softs get the §133 scrutiny.
- **Sub-% match against ~5%-known data → confirmation STRUCTURAL, not a precision claim** (the V_cb lesson: value can be forced while the check stays coarse because the measurement is coarse).
- **Any output that only lands after a convention choice** (basis, phase, normalization) → the choice must be pinned to FK Ch XII / F603 blind, not chosen to fit (contested-target discipline).

## ★ V_cb — RATIFIED at Cal §139's honest tier (accept the refinement)
K1001 ruled V_cb "Derived (value)." Cal §139 refined it correctly, and I ratify the refinement — it is my own Rule-10 burden-flip discipline applied properly:
- **Label: "Derived (2-3-mode is 3D-projected, without counterexample)"** — muon-template, **NEVER bare "Derived."** The identification travels in the label.
- **The split Cal drew (correct):** the **RMS-applies piece is FORCED** (the ground modes are S⁴-zonal / spherically symmetric → the RMS is the definite value √((d−1)/d) by symmetry, not a choice); the **mode-is-3D piece is the open IDENTIFICATION** (does the 2-3 mode fill all 3 spatial dims, or is it effectively lower-localized?). **Lyra pins the mode's dimension; Cal confirms it's genuinely 3, not chosen → clean Derived.** That's the space I audit, she pins.
- Direction 5/√34 forced (F384); confirmation STRUCTURAL (~5%, the data's own 20-year incl/excl puzzle, 0.044 nearest inclusive).
- **Drop the C₂/(C₂+N_c)=6/9 form** (Elie retracted it, correctly) — coincidental form-match. V_cb rests on the physical 3D→2D RMS alone.

## ★ THE PRINCIPLE (Casey's phase directive: improve derivations, find simplifications/principles)
If the fire clears this bar: **the SM fermion sector = the SVD of ONE FK overlap matrix on H²(D_IV⁵)** — not nine derivations, one object. That is precisely the simplification Casey's current phase is hunting: the corpus's scattered fermion results (masses across a dozen notes, both mixing matrices, CP) collapse to one matrix + one crank. **Candidate PRINCIPLE — banked only when the blind bar passes.** Hold it; don't name it until it fires.

— K1002, Keeper, 2026-07-29. The blind bar is committed before the kernel is evaluated: Elie posts the FK-sourced kernel BLIND → fires toy_4920 → ~13 outputs vs PDG at σ, no per-observable dial = Derived (whole sector), any dial = named + tiered. Soft-clean → §133 scrutiny. V_cb ratified "Derived (2-3-mode is 3D-projected, without counterexample)" (§139, muon-template, RMS-forced / mode-3D-identification-open). Principle held: fermion sector = SVD of one FK matrix. See [[Keeper_K1001_V_cb_promotes_to_DERIVED_via_the_3D_to_2D_RMS_projection_theorem_use_physical_RMS_not_form_match_theorems_for_Grace_the_target_is_the_kernel_entries_2026-07-29]], [[Keeper_K1000_PEIRCE_MATRIX_ENGINES_ALREADY_BUILT_the_whole_fermion_sector_is_ONE_input_away_pin_the_kernel_entries_then_fire_toy_4093_4222_4917_dont_rebuild_2026-07-29]], Cal §139, Lyra F603, Elie toy 4920.
