---
title: "K1839 — PROGRESS REVIEW v3 (09:36): the One-Word Lemma is KILLED IN FRAME (49 exhaustive witnesses, n ≤ 22, out of 374,658 stuck colorings); depth is TWO everywhere and never three; the tunneling cut is the stuck-leaf signature (Lemma T 548/548); potential #6 and its fallback dead; Kittell's eight = our forced chains. NEXT: Tunnel Non-Recurrence as the derivation target, a GRADE hunt with the corpus's Kempe invariants, and the successor registered as an OBSERVATION, not a lemma"
author: "Keeper"
date: "2026-09-02, Wednesday (clock-verified 09:36 EDT)"
status: "Review + ruling + research. Mode: investigate, don't gate. Nothing banks except the kill, which is banked as a kill."
---

# 0. Ledger 09:15–09:33 (verified from RUNNING_NOTES 73374–73628 and the witness files on disk)
- **THE KILL.** Elie 5600/5601/(n=22): in the 5-connected frame, exhaustive over every degree-5 vertex and every
  4-coloring mod S₄ of T−v, n = 12…22: **374,658 stuck colorings; 374,609 reach the gate phase in ≤ 1
  fully-legal family word; 49 need exactly 2 (n=17: 8 · n=21: 18 · n=22: 23); 3 or more: 0; unreached: 0.**
  One witness hand-verified by an independent code path (n=17, plantri -c5 graph 0, v=4). Grace 5603-B
  reproduces n=22's 804/23 through the rank instrument. **T2585 → REFUTED** (registry, both graphs, gallery
  FCW-029–054; the 23 new witnesses owed FCW-055–077). Cal's pre-registered order was followed exactly: direct
  → gate → all 186 words → kill on all-T → in-frame. Out of frame the one-word form NEVER failed (3,581
  configurations incl. Poussin 38/38, Kittell 670/670) — the frame ruling is what found the kill.
- **Depth, not alphabet, is the object.** In-frame one-word hitting set GROWS: 1·2·8·4·6·9·14 (n=14…21);
  no fixed word set covers the frame. But **depth stays 2** through 22. The single-swap null on the 827
  no-direct-exit colorings at n=22: gate 258, direct 0 — the family is deeper than the null everywhere.
- **Lemma T verified 548/548** (c₄ stuck ⟺ Q3 ∧ Q4); the six gate leaves are exactly (Q3, ¬Q4). **The tunneling
  cut is the signature:** on every stuck leaf X₄ meets X₃ only at r-vertices and removing them disconnects
  the near copy from the far singleton inside X₃; on gate leaves the meeting is one vertex and does not
  disconnect. Wall verified 669/669. §2(b) verified 121/121.
- **Potential #6 (Φ = |X₃|) and #7 (|X₃|+|X₄|) DEAD** on the S-leaves (decrease < ¼; modal unchanged at 4).
- **Kittell 1935 = our alphabet at chain level:** One-Context merges the 15 seeds into exactly EIGHT link-seeded
  chains everywhere (2,927/2,927), and those are Kittell's eight; the 186 words are 52 commutators. **Novelty
  (Cal):** W_i composes the NON-freeing switches — Kempe and Gethner's gadget swap each copy with its
  non-adjacent singleton's pair; nobody composes the useless ones. That sentence survives the check.
- Frame signed by Cal as "5-connected sphere triangulations" with three amendments (adopted). Registry merge
  done: EA = T2587, Barrier = T2588, counter 2589.

# 1. What died and what stands (calibrated both ways)
Died: the One-Word Lemma as a universal statement, and with it "proof = finite pattern table." Died before it:
six potentials, all pre-registered. **Stands, derived:** One-Context, T2580–T2584, EA, Barrier, Lemmas L, D, T,
the Copy-Path Principle, the wall, Birkhoff A5 — every one true and none of them the theorem. **Stands,
measured:** depth ≤ 2 in frame through n = 22 on 374,658; class-insertability in frame (one class per stuck
(T,v) so far); depth 1 on every classical Kempe-killer. **The honest sentence: the commutator alphabet reaches
the gate phase in at most two words on every configuration ever measured, and the reason is not known.**

# 2. Rulings
- **The successor is registered as an OBSERVATION, not a lemma or a candidate lemma** (Cal §819: a k-word
  statement is a different lemma needing a pre-registered target-innocent potential; none is alive). Row
  content = the depth curve and the 49, status "measured n ≤ 22 in frame; no mechanism; not a lemma."
  Naming is Casey's; placeholder "Two-Word Reach (measured)." Nothing consumes it.
- **The Assembly's Section 3 now reads:** the induction's degree-5 step is supplied by NO proved lemma; the
  measured Two-Word Reach is the candidate; the chain is honest at "classical induction + one open lemma,
  measured to depth 2 on 374,658 in-frame configurations." That is where it was on Monday, one lemma deeper
  and with the false floor removed.
- **Frame adopted** (Casey silent = no veto; the census needed it; the derivations do not).

# 3. What to do next — three lanes, all investigation
**Lane A — TUNNEL NON-RECURRENCE (the derivation target).** The tree is self-similar: at an S leaf the mirror
word W′ runs the same tree in c₄'s frame. Depth 3 = 0 on 374,658 says: *a tunnel never recurs.* Derive why.
What the first tunnel leaves behind is known exactly — the cut vertices of X₄ ∩ X₃ are now s_j; the fence P
is broken there; the (r,s_i)-world is reshaped. Target statement: **if c₄ is stuck via a tunnel, W′·c₄ is not
stuck via a tunnel.** Existence already checked (49 tunnels, 0 double tunnels); kill = one depth-3 witness at
n = 23 or 24 (Elie runs them: 1,970 and 6,833 graphs). Casey's question, mutated: *why would a tunnel not
recur?* — the instinct that fences shrink ("untangle subpatches") is now testable as region size (Lane C).
**Lane B — THE SECOND WORD'S IDENTITY.** On the 49: which first words lead to a stuck image, and which second
word exits? If the exit is always W′ (or a role-set of ≤ 3), the two-word statement is a finite tree of depth 2
and Lane A closes it. If the second word ranges widely, the depth-2 statement is as alphabet-dependent as the
one-word one was (the in-frame hitting-set growth suggests this — measure it).
**Lane C — A GRADE (linear algebra on the coloring lattice; the corpus's own instruments).** The linearization
order: every result an element/eigenvalue/grading of one operator. Here the operator is the cluster shift
(T2577); its known invariants on closed triangulations are the Mohar–Salas degree mod 12 and Lemma R's sign
coset. On T−v they are broken ONLY by chains that touch the pentagon — and every family chain is link-seeded.
So for a coloring c of T−v define I(c) = the closed invariant on the triangular faces; a link-seeded swap
shifts I by a boundary term computable from the link crossing (Straddle-Flip computes Δdeg exactly, banked
08-30). **Hypothesis H_grade, pre-registered here:** the 49 two-word configurations differ from depth-1
configurations in I-distance to the insertable sector; one word's four shifts cannot bridge it. Three
candidates, ONE trial each (selection honesty): (i) Mohar–Salas degree with pentagon term; (ii) odd-vertex /
Eulerian distance near v (toy 5517's lens, which already asked "odd degree vs rescue depth" on 08-30);
(iii) fence-region size (vertices enclosed by P ∪ v on the near-singleton side) — Casey's subpatch. Test on
the 49 vs a matched sample of 49 depth-1 configurations from the same graphs. Pre-score: a candidate that
separates 49/49 vs 0/49 is a GRADE and a potential candidate #8 with a mechanism; partial separation is a
feature, not a grade; no separation kills the candidate. **A grade that explains the 49 would be the first
target-innocent mechanism the program has found for depth.**

# 4. Literature (this hour)
Bonamy–Heinrich (STACS 2020, "Shortest reconfiguration of colorings under Kempe changes"; and "Diameter of
colorings under Kempe changes") — the field's bounds are on reconfiguration LENGTH between two given
colorings, not on reach-to-a-property; Ito et al. (arXiv 2210.17105) — recoloring reconfiguration on sphere
triangulations, PSPACE-hard for k ≥ 4 in general: no bounded-depth certificate is promised; our in-frame depth
2 is either special to the degree-5 insertion problem or an artifact of n ≤ 22. **n = 23–24 decide which.**
Gethner et al. 2009: random Kittell switches needed up to 73 on Heawood — unbounded-looking; ours is depth 1
on Heawood's class. The field has no census of this kind; the exhaustive 5-connected census through 22 with
the 49 witnesses is itself a contribution.

# 5. The external question (for Casey; not today's work)
The honest paper now has a shape: *"Kempe's commutators: an exhaustive census on 5-connected triangulations
through 22 vertices"* — Lemmas L, D, T; Copy-Path; the wall; the tunnel signature; Kittell's alphabet = the
forced chains (52 commutators); 49 two-word witnesses; depth ≤ 2; the hitting-set curve; the open question
stated as Gethner's, sharpened. Nothing in it says 4CT. Decide after n = 23–24 render.

# 6. Proof Machine log
Third validated kill by protocol: the frame ruling (09:06) → in-frame existence (09:14) → one-word test
(09:23) → kill with independent re-verification (09:25) → registry refuted (09:29) → second instrument
reproduces (09:33). Ninety minutes, two instruments, zero over-claim. The machine works; today it worked on us.
— Keeper

---
**ERRATUM 09:45 (same K-number):** Elie's record fix — the "T3" population is the Errera graph, and the out-of-frame
census counts 2,873 / 2,927 quoted above (and in K1835-B, K1838) include relabeling duplicates; the distinct
count modulo relabeling is **1,438**. Every verdict quoted was N/N, so the verdicts stand; the N is corrected
here, and the earlier files carry a pointer. In-frame counts (374,658; 49) are mod S₄ already and unaffected.

**ADDENDUM 09:45 — the second word and the three-switch shape.** On all 49: exit = a middle-swapping first
word (four orbits) then the bridge word in the image's frame; bridge-first works on 10/49; second-word hitting
set = one orbit. Kittell-switch distance ≤ 3 on the 49 (30 exactly 3). Lane A is therefore the
MIDDLE-THEN-BRIDGE lemma (derive: after M, Q3 ∧ Q4 cannot hold for W); Lane D's sharp question is whether the
shortest switch path is always (one s_M-switch, then Kempe's two swaps) — "Kempe succeeds after one preparatory
move." Both pre-registered on the board before paths are read. Grace registered T2589 "Two-Word Reach
(measured)" as the observation row at 09:43.

**NAMING POINTER 12:41 (K1846, Cal §824):** every 'lock' / 'two-word-locked' in this artifact means COMMUTATOR-LOCKED AT DEPTH ONE — relative to the fixed-seed 186-menu (link seeds only). 27/349 have a plain Kempe swap exit seeded off the link; Kempe's pairing inserts on 2. Not a property of the Kempe landscape. Verdicts unchanged; the noun is corrected here, not silently.
