# K772 — Pre-audit of Lane A (the band-edge computation), + a more principled home for "127": it is |GF(128)*|, the multiplicative group of the RS code field. Sharpens guards 3 & 4; load-bearing guards 1 & 2 unaffected. Still a LEAD.

**Keeper | 2026-07-20 08:xx EDT (Mon) | Working the condensate-study core (Lane A) as lead while Lyra computes. Two deliverables: (a) a refinement of the "why 127" motivation; (b) the audit criteria for when the band-gap computation lands. `gf128_why_127.py`.**

---

## (a) A more principled home for 127 (refines guard 3 of K771)
The 127/128 lead's weakest guard was 3: "top at level 127 has to be *forced*, not chosen because 127/128 is pretty." A cleaner structural home:
- **127 = |GF(2^g)*| = the number of NON-ZERO elements of the Reed–Solomon code field GF(128)** (Paper #122, the field the substrate codes on). 127 = M_g is prime → **GF(128)* is cyclic of prime order, generated by one primitive element.**
- **Reading:** the top (the maximal mode) couples to the **full multiplicative group** — all 127 non-zero code symbols; the one it cannot reach is the **additive identity 0** (the vacuum). So **y_t = |GF(128)*|/|GF(128)| = (2^g − 1)/2^g = 127/128** is the *non-zero fraction* of the code field.
- **Why this is better than "level 127":** 127 is now a *forced structural count* (the multiplicative group, prime order, one generator), not an arbitrary level. It also **sharpens guard 4 (uniqueness):** the multiplicative-group reading picks 127 specifically — not 136/137 or another n/(n+1) — because |GF(128)*| = 127 exactly.

**Tier discipline (fire hardest — this is peak-convergence and pretty):** this is **MOTIVATION (why the number is 127), NOT the geometric overlap computation.** It strengthens guards 3 & 4 but leaves the **load-bearing guards untouched:**
- **Guard 1 (scheme-dependence):** 127/128 matches the *pole* mass, not MS-bar (~0.94). Unaffected.
- **Guard 2 (RG-degeneracy, decisive):** 127/128 is indistinguishable from exact-1 + RG running. **The number still cannot decide the fork.** Unaffected.
So the GF(128)* reading *explains* 127/128 IF Lane A's computation lands it — it does **not replace the computation.** **STILL A LEAD.** Do not bank.

## (b) Audit criteria for Lane A (when Lyra's band-gap computation lands)
The band-edge / last-gap computation is CONFIRMED as a derivation of y_t = M_g/2^g only if ALL hold:
1. **Non-circular:** the top's discrete-series address / band-edge is computed from the geometry *independent of the observed y_t* (not fit to 0.992 or 127/128).
2. **The last gap is COMPUTED**, and it comes out **exactly 1/2^g** (not "≈", not "within the error bar" — an algebraic 1/2^g). If it computes to 0 → exact-1 + RG, 127/128 retired. If to another value → a new computed prediction, report it.
3. **Leading-only consistency:** the 127/128 is a statement about the *top* (the leading rank-1 edge). The other fermions being non-integer / Tier-2 is CONSISTENT (they are the off-rank-1 corrections) — do NOT require them at clean integers; but the computation must not *contradict* the Tier-2 structure.
4. **Scheme resolved:** a reason the geometric y_t is the *pole* value (guard 1), OR an explicit statement that the claim is pole-specific and why.
5. **If confirmed:** the GF(128)* reading (a) becomes the *explanation* — the top couples to the multiplicative group, misses the additive identity. Bank as a theorem; K771 lead → derived.
6. **If not:** 127/128 retires cleanly; the derived-clean result (mass ordering = boundary-reach ordering) stands regardless.

## Standing note
The four guards were the model of peak-convergence discipline (K771). The GF(128)* refinement is offered in that same spirit — it makes the lead *more* principled, which is exactly when to be *most* careful not to let the added elegance feel like added proof. Guards 1 & 2 are the wall; only Lane A's computed gap clears it.

— Keeper K772, 2026-07-20. Pre-audit of Lane A + refinement: 127 = |GF(128)*| (the RS field's multiplicative group, cyclic prime order M_g, one generator); top couples to all non-zero symbols, misses the additive identity (vacuum) → y_t = (2^g−1)/2^g = 127/128 as the non-zero fraction. Sharpens guards 3 (why 127) & 4 (uniqueness); guards 1 (scheme) & 2 (RG-degeneracy) UNAFFECTED — still a LEAD, motivation not computation. Lane A criteria set (non-circular, gap = exactly 1/2^g, leading-only consistency, scheme resolved). See [[Keeper_K771_audit_yt_equals_Mg_over_2g_candidate_lead_2026-07-19]], Paper #122 (GF(128) RS substrate).
