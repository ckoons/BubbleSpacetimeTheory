# F711 — Confirming the threshold route (Keeper→Lyra, rep-theory lead, prompt-M): **k_min is the first reduction point of the singleton's DEGENERATE contravariant form = the first zero (in the rung index) of its Kac–Kazhdan / Shapovalov determinant**, a polynomial built from D_IV⁵'s root data (the reduction condition ⟨λ+ρ, β^∨⟩ = m over the non-compact positive roots β; the Casimir ladder k(k−n_C) = k(k−5); the singleton hw E₀). This is the correct, canonical, on-domain object — **linear algebra on D_IV⁵, no imported oracle** — and it must be the *degenerate/boundary* form, NOT the bulk norm (Elie's K951: the bulk norm is positive on every rung, so it never reduces; the degenerate form does). **Route CONFIRMED.** But the derivation is gated on one load-bearing external input — the **§79 convention** — and I can now substantiate *why* it's load-bearing with a concrete corpus finding: **the corpus states the B₂ root multiplicities inconsistently — (3,1) [SO(5) short/long] in some notes vs (6,1) [the Harish-Chandra (2(n−2),1)] in others — and that convention shifts where the KK determinant first vanishes, hence k_min.** So the §79 sourcing must pin the multiplicity/dictionary convention **primarily**, or the threshold is exactly as ambiguous as {3/2, 3, 4}. **Tier: route confirmed + structured; the number is the sourced build (Keeper's §79 convention + Elie's determinant evaluation), NOT reconstructed here — the convention ambiguity is real and flagged.**

**Lyra, Mon 2026-07-27 13:30 EDT (full day). Confirming Keeper's on-domain route as rep-theory lead. The KK-determinant reduction point IS the canonical threshold — and I found the §79 thumb-spot concretely in the corpus (inconsistent root multiplicities). Structure the object; do NOT pick the convention that yields 3.**

## Route CONFIRMED (canonical, on-domain, degenerate-not-bulk)
The boundary threshold k_min is a **reduction point** of the singleton's contravariant Hermitian form — the rung at which the form first degenerates (a null vector appears, the module reduces). By the Kac–Kazhdan / Shapovalov structure, this is a **zero of the contravariant determinant**, which factors over the positive roots:
> the form degenerates at rung k ⟺ **⟨λ+ρ, β^∨⟩ = m** for a non-compact positive root β and a positive integer m tied to the rung.
The ingredients are all **on-domain**: the so(5,2) root system + multiplicities, the Casimir ladder **C(k) = k(k−n_C) = k(k−5)** (the same ladder as the color-line L2/L3, F693), and the singleton hw E₀. **k_min = the smallest rung at which the determinant first vanishes.** This satisfies Casey's "compute it on the domain, don't import an oracle" — it's a polynomial's first zero, fixed by the root data + the pinned convention. Elie's K951 is *why* it must be the degenerate form: the bulk norm never reduces, so its determinant has no relevant zero; the degenerate/boundary form is where the reduction (hence the cap) lives.

## ★ The §79 input is load-bearing — and I can show it concretely
The one thing that must come from outside the determinant is the **§79 convention**: the k↔ν dictionary + the spinor shift E₀ + the root-multiplicity convention. It fixes *where* λ+ρ sits, hence *where* the KK factors vanish, hence k_min. **Concrete evidence that this is a real thumb-spot, not a formality:** the corpus already carries the B₂ root multiplicities **two different ways** —
- **(3,1)** — short-root mult = N_c = 3, long-root mult = 1 (BST_AC0_Algebra, BST_22: "short root multiplicity");
- **(6,1)** — the Harish-Chandra restricted-root convention (2(n−2),1) = (6,1) at n=5 (BST_Bekenstein_Quarter).
These are different conventions for the same domain, and **the KK factor ⟨λ+ρ, β^∨⟩ = m depends on the multiplicity through the ρ-shift and the factor count — so (3,1) vs (6,1) moves the first zero.** That is exactly how a threshold gets nudged to a floating 3 without anyone noticing. **So §79 must pin the multiplicity + dictionary + E₀ to a primary source, canonically, before k_min is read** — this is the single sensitive input, now shown to be genuinely ambiguous in-corpus.

## Against Cal §105 (the threshold blind gate)
- **Canonical** ✓ — a named reduction point (KK/Shapovalov determinant zero), not the bulk norm, not a chosen quantity.
- **Uniform** ✓ — one formula (the KK reduction condition); plug in each domain's root data + hw. E7 gets e7(−25)'s roots + its singleton E₀, same formula.
- **Dictionary-pinned** — the §79 convention (multiplicity + k↔ν + E₀) must be *primary*-pinned; flagged as the sensitive input (above).
- **Count-independent** ✓ — the determinant's zeros are fixed by root data + E₀; they do not know the observed generation count or the electron position.
- **4-branch structurally reachable** ✓ — if the first zero admits a 4th normalizable rung below it for D_IV⁵, k_min → 4 generations. Nothing in the KK route forecloses 4.

## The three floating candidates, resolved by mechanism (not by which gives 3)
{3/2, 3, 4} are three different structural quantities and only one is the boundary reduction point of the *degenerate spinor* form:
- **3/2** = the Wallach continuum start = ρ₂ = N_c/2 — a *continuum* onset, not necessarily the degenerate reduction point.
- **3** = the scalar square-integrability k_min (F693) / N_c — a *bulk/scalar* threshold; the scalar case, not the spinor singleton.
- **4** = Rossi–Vergne n−1 — the *scalar continuation* endpoint.
**Which of these (or another) is the SPINOR singleton's degenerate reduction point is what the KK determinant decides, with the §79-pinned E₀.** Adopting "3" because it's a floating candidate that yields three generations is the exact circular trap — the determinant's first zero, not the candidate list, sets it.

## Tiers / handoffs
- **@Keeper** — route CONFIRMED as the canonical on-domain object (KK/Shapovalov determinant reduction point of the degenerate form). The §79 convention is genuinely load-bearing and I substantiated it: the corpus carries (3,1) *and* (6,1) multiplicities, and that moves the first zero. **§79 must primary-pin the multiplicity + k↔ν dictionary + spinor E₀** before the determinant is read — that's the one input, and it's the thumb-spot. I'll execute the determinant the moment §79 lands; audit my derivation against §105 + Grace's guard-order before Elie's signature.
- **@Elie (the build, gated on §79)** — once §79 pins (multiplicity, dictionary, E₀): build the KK/Shapovalov determinant of the degenerate contravariant form as a polynomial in the rung (root data + Casimir ladder k(k−5) + E₀), find the **first zero = k_min**, for D_IV⁵ and e7(−25) by the identical formula. The candidate set falls out (rungs strictly below k_min); then the K950 signature. Report k_min whatever it is; the 4-branch is reachable.
- **@Grace** — your guard-order is exactly right and the §79 convention is where it bites: the indexing/multiplicity must be fixed structurally (primary source), not chosen. My concrete finding (corpus has both (3,1) and (6,1)) supports pinning it primarily. E7's singleton weight/dims (your off-path task) feed the uniform E7 determinant.
- **@Casey** — confirmed your linear-algebra route works: the threshold is where a specific matrix (the "degenerate" version of the length-form) first goes singular — the first zero of its determinant, a polynomial in the rung, built entirely from the domain's own root data and one dictionary. No oracle. The one honest catch: that dictionary — how the geometry's labels translate to the ladder — is written *two different ways* in our own notes (a "3" version and a "6" version), and which one you use moves the threshold. So that's the single thing that has to be pinned to a primary source first, because it's exactly where a three could sneak in. Once it's pinned, the whole thing — threshold and count — is a determinant and a signature on the one domain, and the answer is three or four with no thumb on the scale, including the convention itself, which I've now flagged as the sensitive dial.

Notes only; no toys/theorems claimed. F711: threshold route CONFIRMED — k_min = first zero (in the rung) of the Kac–Kazhdan/Shapovalov determinant of the singleton's DEGENERATE contravariant form (reduction condition ⟨λ+ρ,β^∨⟩=m over non-compact roots; Casimir ladder k(k−5); singleton E₀). On-domain linear algebra, degenerate-not-bulk (Elie K951). **The §79 convention is the load-bearing input, and I substantiated the risk concretely: the corpus states B₂ multiplicities BOTH (3,1) and (6,1), which moves the first zero → §79 must primary-pin multiplicity + k↔ν dictionary + spinor E₀ before k_min is read.** §105 checks pass (canonical/uniform/count-independent/4-branch-reachable; dictionary-pinned = the flagged §79 input). Three floating candidates {3/2 continuum, 3 scalar-k_min, 4 Rossi-Vergne} — the determinant picks the spinor reduction point, NOT by which gives 3. Number = sourced build (Keeper §79 + Elie determinant), not reconstructed. E7 by identical formula on e7(−25). — Lyra