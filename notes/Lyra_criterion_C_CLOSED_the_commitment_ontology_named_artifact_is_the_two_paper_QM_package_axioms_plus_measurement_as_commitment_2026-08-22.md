# Criterion C CLOSED — the commitment ontology now has a single named artifact: the two-paper QM package

**Round 50 (forward progress). Internal-rubric criterion C (the commitment ontology) was the one criterion "~80% drafted, without a single named artifact." This finishes it — a consolidation, not new research.** Reconnected: the Axioms paper (10/10 Dirac–von Neumann), the Measurement-as-Commitment paper (the ontology mechanism), the commit-operator dig (K1607/K1609), the Rubric's criterion-C line, the W10-corrected spine.

## The named artifact (this is what closes criterion C)

> **The QM package = two companion papers, one deliverable:**
> 1. **"The Axioms of Quantum Mechanics from D_IV⁵"** — the sober companion: **10/10 Dirac–von Neumann axioms derived, zero posits** (QM *structure* is a reading of one operator H_B on H²(D_IV⁵)). This is external-rubric item 2 (Derive QM), DONE.
> 2. **"Measurement as Commitment"** — the novel paper: the QM *process* (measurement/collapse/Born) is the **contractive face of the same operator**, and the commit's internal structure is the graded operator **C = P_record ⊕ P_encode** (write ⊕ confined-3-encode — the Peirce split of a primitive idempotent, K1607/K1609). This is the **ontology mechanism** — internal-rubric criterion C.

Together they are the single named artifact criterion C lacked: **QM's structure and QM's measurement process are two faces of one operator on one geometry, with the commitment mechanism exhibited internally (C = P_record ⊕ P_encode).** Measurement-as-commitment (Principle #16) is no longer "a criterion without an artifact" — it is these two papers.

## What the finish did (R50)

- **Absorbed §2a into §2 as a permanent subsection** — the commit-operator dig (C = P_record ⊕ P_encode = write ⊕ check) is now integrated into "one operator, two faces," not a flagged to-do. The dig sharpens the commit from "structureless settling" to "one graded operator": **P_record** (the WRITE — a rank-1 arrow-line projection, an irreversible binary record; *this is where rank=2 is externally forced, from the structure of a binary outcome*) ⊕ **P_encode** (the CHECK — the confined 3-block V₁₂, a detection/erasure structure at d_min=2, self-validating but *not* "unforgeable" — a corrected claim kept in-text).
- **Reconciled the odds status** — Born-from-the-commit is now **Derived** (Elie's verified sort-weighting: the commit = forced Wallach-drop ∘ forced invariant measure T754); the stale "until (1)… not Derived" handoff is retired. Only the eraser/Bell CHSH computation (1/2^{N_c}) remains as one gate.
- **Aligned the spine to the corrected forcing claim (W10)** — the honest floor already reads "n_C=5 stays an honest input"; this is exactly the W10 result (**one measured input, N_c=3 the ruler, n_C=5 selected; zero free/tunable parameters**). The QM package was already consistent; now it's named as such.

## The honest ledger (unchanged spine, now closed)

- QM structure (10/10 Dirac–von Neumann axioms) — **Derived, zero posits.**
- Measurement process (Schrödinger = unitary face; arrow = contractive-face positivity; becoming-definite = the commit) — **Derived.**
- Born rule = the forced Bergman measure (T754); odds survive the commit — **Derived.**
- The commit's internal structure C = P_record ⊕ P_encode — **Derived** (Peirce split, K1607/K1609).
- rank=2 externally forced (binary write; Shannon alphabet = Jordan rank) — **Derived (external).**
- Spacetime *type* (Lorentzian type-IV) forced mod P0+P1 — **Derived-conditional**; **dimension n_C=5 = one measured input** (W10).
- The single outcome = a **boundary datum** (given, by category — as GR takes its initial data). **Not** "measurement solved."

**The line we hold, exactly as far as it goes:** BST derives everything about measurement that can be derived — the becoming-definite, the arrow, the odds, and the commit's internal mechanism — and names the single outcome for what it is: irreducible data, not an unexplained dynamical gap. That is criterion C, and it now has its artifact.

## Disposition

- **Internal-rubric criterion C: CLOSED** — the ontology has a single named artifact (the two-paper QM package). @Keeper — this closes the one internal-rubric line that lacked one; fold into the Rubric (the "ARTIFACT NOW EXISTS" cell → "CLOSED, named: the QM package").
- **Ships on the standing gate** (Cal hostile-read Sections 6–7 + Keeper pass + Casey GO) — the guardrail: never "measurement solved," never "randomness explained away." One open computation remains (eraser/Bell CHSH), Identified, not blocking the criterion-C close.
- **No new theorem, no counter consumed** — a finish.

**Lyra, 2026-08-22 (criterion C closed, R50 forward progress). The commitment ontology now has a single named artifact: the two-paper QM package — "Axioms of QM from D_IV⁵" (10/10 Dirac–von Neumann, zero posits, QM structure) + "Measurement as Commitment" (the process = the contractive face of H_B; the commit's internal structure = C = P_record ⊕ P_encode, the Peirce split of a primitive idempotent, K1607/K1609). Finish: §2a absorbed into §2 (dig integrated); odds Derived (Elie's verified sort-weighting, stale handoff retired); spine aligned to W10 (n_C=5 one measured input). Criterion C CLOSED; ships on the standing gate (never "solved"), one open CHSH computation not blocking. Nothing pushed; CP existence-only.**
