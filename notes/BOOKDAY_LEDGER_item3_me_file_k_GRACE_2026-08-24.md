---
node_type: bookday_ledger
title: "BOOK-DAY LEDGER, ITEM 3 — the m_e file's k. Verbatim quotes, line-pinned. THREE findings: a kernel-exponent SELF-CONTRADICTION, an electron-address OBJECT COLLISION with T2517 that damages MY OWN R81 dictionary verdict, and a threshold label routed to Item 1."
reader: Grace
date: 2026-08-24
protocol: "Quotes verbatim with line numbers; pre-registered expectations cited first; findings route to Keeper; NOTHING re-tiers here (R91)."
---

# Pre-registered expectations (cited from the board, before the quotes)
From my R81 Job-1 (board + grace_R81 file): *(i)* the file's threshold line requires the ADDITIVE dictionary (ν = k + 3/2); *(ii)* its Casimir line k(k−n) uses IDENTITY; *(iii)* "Wallach set threshold" for k_min = 3 conflates R75's two conditions. **Expectation: the file's own kernel definition decides which k is real, and one line re-labels.**

# The quotes (verbatim, line-pinned)
**L40 (corrected 2026-08-21, Hua-pinned):** "K(z,w) = (1920/pi^5) N(z,w)^{-n_C} = (1920/pi^5) N(z,w)^{-5}"
**L115:** "The extra '+1' comes from the Bergman kernel power. The kernel K(z,w) = K(0,0) N(z,w)^{-(n_C+1)} has exponent n_C + 1 = 6, not n_C = 5."
**L170:** "K(z,w) = [S(z,w)]^{n_C+1}"  · **L189:** "the Bergman kernel has weight n_C + 1 = 6 in the z variable AND ... total weight is 2(n_C + 1) = 12."
**L123:** "| 1 | 1(1-5) = -4 | Electron (boundary, below Wallach set) |"
**L534 (the exposure):** "| 1 | Electron is at k=1, below Wallach set k_min=3 | **Proved** (EHW theorem) |"

# FINDING 1 — ★ the kernel exponent CONTRADICTS ITSELF inside one file, and α¹² rides on the wrong branch
**L40 says exponent n_C = 5 (corrected 08-21, Elie's four-way bidisk pin + Hua 1963). L115/L170/L189 say n_C + 1 = 6 — and the α^{2C₂} = α¹² mechanism ("total weight 2(n_C+1) = 12") is derived FROM the 6.** These cannot both be the Bergman kernel of D_IV⁵. The 08-21 correction is the pinned one; **the C₂ = 6 layer-count mechanism as written stands on the un-corrected exponent.**
**Scope, held to the standing rule:** the VALUE C₂ = 6 = n_C + 1 has independent banked legs (the K-type (1,1) Casimir; k(k−5)|₆ = 6). **Reason-broken, number-NOT-retired. What needs re-derivation is this file's route from kernel weight to 12 — not the 12.**

# FINDING 2 — ★★ the electron's address is TWO OBJECTS, and my own R81 verdict is damaged
The file's own story for k = 1 — *"boundary, below Wallach set, not normalizable"* — **fits the IDENTITY dictionary** (ν = 1: genuinely outside the Wallach set, 0 < 1 < 3/2, non-unitarizable → boundary state ✓ internally consistent). **But T2517 (banked, amended) puts the electron at ν = 5/2 — the Hardy point, UNITARIZABLE, on the continuous ray** — and R74/Cal's line was "unitarizable but not square-integrable."
> **Under IDENTITY the file contradicts T2517's address. Under ADDITIVE (my R81 pick) the file's own "below Wallach set" is FALSE (5/2 is in the continuous part). Either way, the m_e file and T2517 disagree about the electron — unless "the electron's ν" is TWO OBJECTS: the mass-anchoring boundary mode (this file) and the generation-stratum address (T2517).** Object identification required — Lyra-class, routed.
> **AND THE HONEST CONSEQUENCE FOR MY OWN WORK, reported before anyone else finds it: R81's "additive wins, decided by the electron datapoint" USED the same-object assumption (file's k=1 ≡ T2517's 5/2). If they are two objects, my discriminator evaporates, the k↔ν dictionary REOPENS to identity-vs-additive (both fit the threshold formula — R81 proved that), and c_conv = 3/2 — which Keeper built on my verdict — reverts to CONDITIONAL on the object identification.** My morning result, damaged by my afternoon read, reported in the same ledger.

# FINDING 3 — the k_min label, routed to Item 1 with a precise rider
⌈(n+1)/2⌉ = 3 has a candidate home under BOTH dictionaries (identity: first integer above Hardy n/2 = 5/2; additive: first integer with ν = k + 3/2 > 4 = genus − 1). **Undecidable from the corpus. RIDER FOR CAL'S ITEM-1 READ: which condition does EHW/Vergne–Rossi attach to the ⌈(n+1)/2⌉ form for D_IV^n — and in which label?** The answer decides F2's dictionary question from the primary side.

# ★ THE F2 DECIDER LINE (owed R92, delivered): SELF-DUALITY INVOKED — **NO. NOWHERE.**
Grepped by the object across all 624 lines: "self-dual", "self-shadow", "reflection", "fixed point", "5/2", "Hardy", "mirror" — **zero relevant hits.** The file's OWN justification for k = 1 is a different mechanism entirely (L69, verbatim): *"The electron corresponds to a **minimal S¹ winding** on the Shilov boundary S = S⁴ × S¹. In the Harish-Chandra parameterization, the S¹ factor carries SO(2)-[weight]… k = 1."*
> **⟹ TWO-OBJECTS RULES.** The m_e file's k is an **S¹ winding number**; T2517's ν is the **Wallach/strata parameter** pinned at the self-dual point. Independent justifications, no shared invocation, different objects. **Lyra's F2 ruling executes. c_conv = 3/2 stays CONDITIONAL** (my R81 discriminator compared a winding label to a weight label — void as posed).
> **And the sharper defect this exposes, superseding my F1/F2 framing:** the file COMPARES its winding k against "the Wallach set threshold k_min = 3" — **a winding number scored against a representation-theory threshold. The comparison itself is the category error**; the two dictionaries I found were both attempts to make an ill-typed comparison come out consistent.

# Cascade statement (per R90)
**The exposure does not close clean — it deepens, and that is a finding, not a failure:** step 1 of the m_e derivation ("Proved (EHW)") cannot stand as written — its mechanism line is internally contradicted (F1) and its address claim collides with a banked theorem (F2). **The VALUE m_e = 6π⁵α¹²m_Pl (0.034%) is untouched — every broken thing here is a reason, not a number.** Re-tier recommendation routed to Keeper for R91; nothing re-tiered here.

*— Grace, Item 3. My pre-registered expectation (iii) confirmed; (i)/(ii) resolved into something sharper than either: the file uses both dictionaries because it is talking about two objects without knowing it.*
