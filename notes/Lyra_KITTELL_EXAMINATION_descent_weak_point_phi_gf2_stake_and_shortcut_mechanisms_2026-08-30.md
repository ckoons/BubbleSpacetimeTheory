---
title: "N3 — the Kittell examination: DC's weakest point inspected before the harvest; Φ re-based onto the GF(2) lattice post-Z2; the stake (Φ_GF2 = 4) and the three named shortcut mechanisms that would falsify it"
author: "Lyra"
date: "2026-08-30, Sunday (clock-verified 16:53 EDT at round start)"
status: "ROUND 7, LANE N3. Pre-harvest examination of my twice-registered risk, per Cal's ruling that the potential's functional form WAITS for the harvest. Stakes filed; no fitting done. Nothing banks."
---

# THE KITTELL EXAMINATION

## 1. Why Kittell, restated once

Twice-registered risk (rounds 5 and 6): deepest measured graph (depth 4), coarsest lattice
([2²⁰, 8]), largest residue rung, support-2 gates at depth 4 (support ≠ depth died there). If
the Descent Conjecture has a counterexample in the natal family, it lives here — better to meet
the harvest with this examined than to learn it from a hundred anonymous rows.

## 2. Post-Z2 re-basing (a correction to my own M1, made before data forces it)

M1 defined frustration Φ on the ℤ charge lattice. Z2 killed ℤ-GC-I: the charge quotient cannot
see frozenness, so a Φ built on it can undershoot depth for a reason that has nothing to do with
descent — the metric's own floor is blind. **Correction, filed now: the potential's home is the
GF(2) coset lattice** (where the lens is 8/8 complete so far): Φ₂(f, v) := minimum word length
expressing any element of {ε(f) − ε(f′) : f′ freed at v} in step-available... no — kinematic
version first, per the two-cost discipline: Φ₂ := min over freed f′ of the W-word-length of
ε(f) − ε(f′) (superset columns, realizability ignored). Both Φ_ℤ and Φ₂ go into the harvest
comparison; DC-2's live form is **depth = Φ₂**.

## 3. The stake

**Registered: Φ₂(Kittell stuck states, apex) = 4 — DC-2 survives its stress case.** If it
returns < 4, DC-2 dies and the surcharge (δ − κ gap) must be characterized; I lose the
twice-defended position and the potential program restarts from the availability profile
(N1's Gap A table) instead.

## 4. Three named shortcut mechanisms (how Φ₂ < 4 would happen — the falsifier's anatomy)

1. **Cross-class column borrowing:** the superset W contains indicators of chains that exist
   only in OTHER Kempe classes; a 3-term formal sum crossing through them undercuts the real
   4-step path. (Most likely; this is Cal's realizability warning specialized to the metric.)
2. **Big-chain columns at relaxed states:** chains available only after partial rescue have
   large straddle sets; formally combining two of them may reach the freed coset in 2. (The
   kinematic metric's unit ball is state-blind.)
3. **Freed-set aliasing:** Ω_free's GF(2) shadow may contain freed colorings of OTHER apexes'
   classes reachable cheaply in the formal metric. (A definition artifact, curable by
   apex-restricting the freed set — pinned here so a Φ₂ < 4 result is diagnosed before it is
   celebrated or mourned.)

## 5. What survives either way

If Φ₂ = 4: the twinning consistency (T_3 ≡ Errera, depth 3) plus Kittell at 4 makes the
kinematic GF(2) metric a serious depth predictor going into the harvest — and DC gets its
functional-form decision made on a population it didn't grow up on, per Cal's architecture. If
Φ₂ < 4: mechanisms 1–3 say exactly where to look, and the availability profile becomes the
potential's foundation instead of the coset metric. Either way the harvest meets DC at its
weakest point with the weakness already mapped — which was the assignment.

— Lyra. I have defended Kittell twice on paper; this note is where I find out whether I was
defending a theorem or a habit.
