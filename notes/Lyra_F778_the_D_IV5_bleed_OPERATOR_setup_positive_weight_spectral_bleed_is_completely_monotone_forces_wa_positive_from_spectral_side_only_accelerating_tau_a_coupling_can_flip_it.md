# F778 — The D_IV⁵ bleed OPERATOR, set up as linear algebra (Casey's directive; my task per K1120), with an uncomfortable structural finding that runs AGAINST the exciting answer: **a positive-weight spectral bleed is COMPLETELY MONOTONE, which FORCES wₐ > 0 (deviation shrinking, w → −1 from above) from the spectral side alone — the OPPOSITE of DESI's wₐ < 0 phantom-crossing. This is a theorem about the operator's structure, not a proxy artifact. The ONLY escape to wₐ < 0 (the DESI/Derived horn) is if the τ↔a coupling map is ACCELERATING enough to overcome the robust spectral deceleration — which is exactly where the age-vs-Hubble coupling enters. So the sign of wₐ — the whole decision — reduces to one clean contest: robust spectral wₐ>0 vs the coupling's acceleration. I set the operator up so that contest falls out blind; I do NOT declare the sign; and I flag honestly that the structural DEFAULT is wₐ>0 (the near-miss, K1040's own kill), against my own F777 excitement.**

**Lyra, Sun 2026-08-02. Owning two things first: (1) my F777 "falsifiable fork" was a mis-frame — it's ONE model (K1040 + F220), not two; drop the fork language. (2) my F777 "evolving matches DESI → Derived" was premature — the banked model evolves the WRONG way (wₐ>0), and now I find that's structurally robust, not a proxy. The mirror-bias had flipped me toward the exciting DESI-matching answer; the operator's own structure contradicts it. That's the discipline catching me in the new direction.**

## The operator (linear algebra)
The bleed is the substrate heat semigroup on the boundary zero-point:
$$ \rho_\Lambda(\tau) = \langle \psi_0 |\, e^{-\tau H_B}\, |\psi_0\rangle = \sum_k c_k\, e^{-\lambda_k \tau}, \qquad c_k = |\langle k|\psi_0\rangle|^2 \ge 0, $$
- **H_B** = Casimir of K = SO(5)×SO(2) (Tier-0 substrate operator), spectrum {λ_k} (the K-Casimir eigenvalues on D_IV⁵), λ_k ≥ 0.
- **ψ₀** = the boundary/Planck-scale zero-point (a₀ = 225), decomposed in the H_B eigenbasis.
- **c_k = |⟨k|ψ₀⟩|² ≥ 0** — POSITIVE weights (they are squared overlaps). This positivity is the load-bearing fact.
- **τ** = elapsed commitment time; **w(a) = −1 − (1/3) d(ln ρ_Λ)/d(ln a)**, with a the scale factor and τ↔a the coupling map.

## The structural theorem — positivity FORCES wₐ > 0 (spectral side)
ρ_Λ(τ) = Σ c_k e^{−λ_k τ} with c_k ≥ 0 is a **completely monotone** function. Its effective decay rate is a variance-decreasing average:
$$ r(\tau) := -\frac{d\ln\rho_\Lambda}{d\tau} = \langle\lambda\rangle_\tau = \frac{\sum c_k \lambda_k e^{-\lambda_k\tau}}{\sum c_k e^{-\lambda_k\tau}}, \qquad \frac{dr}{d\tau} = -\mathrm{Var}_\tau(\lambda) \le 0. $$
So **r(τ) decreases monotonically** (larger-λ modes die faster → the weight shifts to smaller λ → the rate falls from ⟨λ⟩_c toward λ_min). Since the deviation w+1 = (1/3)·r(τ)·(dτ/d ln a), a decreasing r gives a **shrinking deviation → w → −1 from above → wₐ > 0** (w was larger in the past, decaying to −1). **This is FORCED by c_k ≥ 0 alone — independent of the specific spectrum, the domain dimension, or any proxy.** So the banked wₐ > 0 (the near-miss, opposite to DESI, tripping K1040's July kill) is not a scalar-toy artifact — it is the generic behavior of *any* positive-weight spectral bleed. That is the uncomfortable part, and it is against the exciting answer.

## The ONE escape — the coupling map (where age-vs-Hubble lives)
The full deviation is w+1 = (1/3)·r(τ)·(**dτ/d ln a**). The spectral factor r(τ) robustly *decreases*; the sign of wₐ is decided by whether the **coupling factor dτ/d ln a** *increases* fast enough to overcome it:
$$ \frac{d(w+1)}{d\ln a} = \tfrac{1}{3}\Big[\underbrace{-\mathrm{Var}(\lambda)\,(dτ/d\ln a)^2}_{<0,\ \text{spectral, robust}} + \underbrace{r(\tau)\,\frac{d^2\tau}{d(\ln a)^2}}_{\text{coupling acceleration}}\Big]. $$
- **Age-coupling** (τ ≈ cosmic time, gentle map, small d²τ/d ln a²) → the spectral term dominates → **wₐ > 0** → near-miss, K1040 fires.
- **Hubble/horizon-coupling** (τ tied to 1/H, which *accelerates* in a Λ-dominated era, large d²τ/d ln a²) → the coupling term can flip the sign → **wₐ < 0** → DESI phantom-crossing, value **Derived**.
So **the whole decision — the sign of wₐ — is: does the geometric τ↔a coupling accelerate enough (d²τ/d ln a² large and positive) to beat the robust spectral deceleration?** That is a clean, blind, decidable computation on the real operator — not a proxy.

## Honest state (both mirror-biases named)
- **Structural default: wₐ > 0** (spectral positivity, robust) → the near-miss, against my F777 excitement. I flag this loudly because I *wanted* the DESI-matching Derived answer, and the operator's own structure opposes it.
- **BUT I do NOT declare wₐ > 0 either** — that would be the *other* mirror-bias (over-declaring the disciplined-looking falsifying answer). The escape (accelerating coupling) is real and geometric; the sign genuinely hinges on the τ↔a map, which must be computed from the geometry, blind.
- **Doubly blind, still:** the coupling map is determined by the geometry, blind to which tier we'd prefer (Derived now tempts) AND which sign DESI prefers (wₐ<0). Geometry predicts; data tests.

## Tier / handoffs
- **@Elie / @Grace** — the operator is set: (1) the K-Casimir spectrum {λ_k} and the overlap weights c_k = |⟨k|ψ₀⟩|² on the real D_IV⁵ (not a scalar proxy); (2) the geometric τ↔a coupling map (this is the age-vs-Hubble determination, and its **d²τ/d ln a²** is the whole question); (3) w(a) = −1 − (1/3)r(τ)(dτ/d ln a), sign of wₐ, overlaid on the DESI DR2 (w₀,wₐ) contour. **The spectral side robustly gives wₐ>0; only an accelerating coupling flips it — so the coupling map is the decisive computation.** Data tests, never fits.
- **@Cal** — guard the sign doubly blind, and note the mirror-bias is now *split*: I'm biased toward wₐ<0 (exciting/DESI/Derived) but the structural default is wₐ>0 (disciplined-looking) — so I could over-declare *either*. Audit that I neither reach for Derived (the coupling escape) nor over-claim the falsifier (the spectral default). Re-scrub the DESI numbers.
- **@Keeper** — owned: F777's fork was a mis-frame (one model), and the completely-monotone structure makes wₐ>0 the robust spectral default (the near-miss is structural, not proxy) — against my own prior excitement. The sign reduces to the coupling's d²τ/d ln a². #9 stays conditional on the sign.
- **@Casey** — the operator's set up as your linear algebra, and it hands us a clean, hard fact I have to report even though it cuts against what I got excited about last turn: the bleed is a sum of fading modes with *positive* weights, and a sum like that always fades *slower* over time (the fast parts die first), which makes dark energy settle *toward* constant from above — the opposite of the "crossing" DESI is hinting at. That's forced by the math, not a rough toy. The one thing that can flip it is how the substrate's clock maps onto the expanding universe: if that map speeds up steeply enough (the horizon-coupling, your original instinct), it can outrun the fading and produce the crossing — and *that* version makes the value Derived. So the entire question is now one blind computation: does the geometry's own clock-to-expansion map accelerate hard enough to beat the fading? I set it up so that falls out of the real operator, and I flagged — against myself — that the boring answer is the structural default, so nobody (me included) should assume the exciting one. Nothing pushed.

Notes only; no toy/theorem claimed (operator setup + structural finding). F778: bleed operator ρ_Λ(τ)=⟨ψ₀|e^{−τH_B}|ψ₀⟩=Σ c_k e^{−λ_k τ}, c_k=|⟨k|ψ₀⟩|²≥0, H_B=K-Casimir. STRUCTURAL THEOREM: positive weights → COMPLETELY MONOTONE → r(τ)=⟨λ⟩_τ decreases (dr/dτ=−Var(λ)≤0) → deviation shrinks → **wₐ>0 FORCED from the spectral side** (near-miss, K1040 kill, opposite DESI wₐ<0) — robust, NOT a proxy artifact, AGAINST my F777 excitement. ESCAPE: w+1=(1/3)r(τ)(dτ/dln a); sign of wₐ = spectral-deceleration [−Var·(dτ/dln a)²] vs coupling-acceleration [r·d²τ/dln a²]. Age-coupling (gentle)→wₐ>0/near-miss; Hubble-coupling (accelerating, d²τ/dln a² large)→wₐ<0/DESI/Derived. WHOLE DECISION = does the geometric τ↔a map accelerate enough to beat robust spectral wₐ>0? Blind, one computation. Both mirror-biases named (want wₐ<0 exciting; over-declaring wₐ>0 disciplined-looking) — declare NEITHER. Mis-frame owned (one model not fork). @Elie/@Grace: K-Casimir {λ_k}, weights c_k, the τ↔a map (d²τ/dln a² is the question), w(a) vs DESI. @Cal guard split mirror-bias. — Lyra