# R56 — two clean swings: (1) the rail forces Q's STRUCTURE but not its WEIGHT ε (confirms 1-of-4); (2) τ — the SIGN is forced, the value is a choice, so the falsifier survives

**Round 56 (the last swing before checkpoint). Two bounded questions, one clean swing each. Reconnected: Cal §696 (the sharper negative + the τ-sign stakes), Grace's P=1+εQ reframe, T2544 (J_W one-rung), R53/R55 rail.**

## Swing 1 — does the discreteness rail force the block weights ε? Honest verdict: NO — it forces Q's STRUCTURE, not its WEIGHT.

The mixing is Grace's non-idempotent graded P = 1 + εQ (a ~10% graded perturbation of the identity, θ first-order in ε). Split what the rail does and does not reach:

- **The rail FORCES the graded operator Q's STRUCTURE.** Q is the **one-rung step** — the degree-1 cohomology operator J_W on ℤ[h]/h⁶ (T2544), which connects adjacent shelves (odd↔even, the only way up {0,2,4} and down {1,3,5} couple). Q's *form* is forced by discreteness (the ring's degree-raising is canonical; there is no other generation-space grading, R55). **On self-adjointness (Grace's one line, my read): the physical Q is the Hermitian one-rung operator J_W + J_W† (the mass matrix is Hermitian), so Q† = Q — the ordering bit stays dead** (Cal §696.2); J_W alone (raising) is not self-adjoint, but the physical graded perturbation is.
- **The rail does NOT force the WEIGHT ε.** ε is the *strength* of the grading = the matrix element ⟨k±1|Q|k⟩ on the **normalized** modes, and the normalization is the FK/Wallach kernel — **mass-space, the "wrong-space" the prompt names.** The discrete addresses fix *which* shelves couple; they do **not** fix *how strongly* — that rides the wavefunction profiles (the norms), which are not discrete-address data. **So ε is not forced target-innocently on the generation space; it is the O(1) coefficient (param 2), and the FK ladder / Jordan algebra / degree grid all give the mass structure, not the generation-grading weight.**

**⟹ Verdict: the rail forces the structure (positions, parity, Q's one-rung form, Q self-adjoint), NOT the weight ε.** This is the same shape as the projection negative one level down: structure forced, scale open. It **confirms the 1-of-4 ledger** — λ = 1/√20 derived; ε (param 2) open, the second O(1) coefficient open, the CP phase open. **Partially Derived ships now.** *(And it re-earns the physical reading: ε ≈ 0.1 is an honest O(1) grading, not fine-tuning — the smallness was always λ² = 1/20, derived. Grace's point stands.)*

## Swing 2 — is τ = ln a forced? Verdict for Cal §696: the SIGN is FORCED; the VALUE is a choice. The falsifier survives.

Cal §696.6 is exactly right that this is the whole falsifier: since the flip is τ″ > 6τ′² (6 = C₂ = λ₁), **if the sign were a free convention the falsifier could not fail.** The decisive answer:

- **The specific map τ(a) is a modelling choice** (τ=ln a from a horizon-set rate; the Koons-tick fixed-rate map τ∝t differs). So the *value* w_a = +0.027 is clock-dependent (already declared amplitude-independent-but-clock-dependent; §4751 pre-registers the sign, not the value).
- **But the SIGN is FORCED, because the sign-flip condition τ″ > 6τ′² is physically unreachable.** The bar is 6 = C₂ = the leading bleed rate; flipping the sign requires the substrate clock to *accelerate in e-folds faster than the leading bleed decays* (τ″/τ′² > 6). Both natural maps sit far below: **τ=ln a gives τ″/τ′² = 0; the Koons-tick map gives ≈ 0.5** (ΛCDM |Ḣ|/H²≈0.46) — against the bar 6. A **monotone, positive-energy substrate clock with a bounded commit rate (the Koons tick is a finite minimum) cannot reach τ″ > 6τ′²** — that would require super-6×-exponential commit acceleration, which no finite-rate substrate provides.
- **⟹ The SIGN w_a > 0 is forced across the entire physical clock-map space — the falsifier is UNCONDITIONAL as a sign test** (it CAN fail: DESI firming w_a < 0 at ≥3σ refutes it). It is not "τ=ln a assumed"; it is "the sign-flip requires a clock physics forbids." **The best falsifier BST has, and it ships as unconditional.**
- **The one owed rigor** (to make it a proven theorem rather than a physical argument): a BST theorem that **τ″ < 6τ′² for any physical substrate clock** — following from the finite Koons-tick commit rate + positive energy (no super-exponential commit acceleration). Bounded, one-line, and until proven the honest tier is *"sign forced by the physical bound τ″ < C₂τ′²; value clock-dependent."*

**Order (Cal §696.6): τ settled here (sign forced) → then C3 (dataset + Δχ² threshold) → then look.** The §4751 pre-registration already freezes the sign; C3's dataset/threshold can now be pinned against a sign that IS the theory's.

**Lyra, 2026-08-22 (R56 two swings, checkpoint-ready). (1) The rail forces Q's STRUCTURE (one-rung J_W, T2544; Hermitian so Q†=Q, ordering bit stays dead) but NOT its WEIGHT ε — ε rides the FK/Wallach norm (mass-space, wrong-space), so it is the O(1) input (param 2). CONFIRMS 1-of-4: λ=1/√20 derived; ε + second coefficient + CP phase open; PD ships. (2) τ: the specific map τ(a) is a modelling CHOICE (value w_a=+0.027 clock-dependent), but the SIGN w_a>0 is FORCED — the flip τ″>6τ′² (6=C₂) is physically unreachable (both natural maps give τ″/τ′² = 0 and ≈0.5 vs the bar 6; a bounded-rate positive-energy clock cannot super-6×-exponentially accelerate). So the falsifier is UNCONDITIONAL as a sign test (can fail: DESI w_a<0 at ≥3σ refutes). Owed rigor: τ″<C₂τ′² as a BST theorem (from finite Koons-tick rate + positive energy). Nothing pushed; CP existence-only.**
