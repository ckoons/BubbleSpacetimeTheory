# K1269 — Own the const_042 scale-mismatch my batch-2 PASS missed; value/scale-check now standing; Elie's #85 reformulation ratified (Whitney sidestepped); Casey's projection = the doubling hook; α_s is header-rot (converged).

**Keeper, 2026-08-07.** Five items, one of them a miss of mine.

## 1. I own the const_042 scale-mismatch — my batch-2 PASS missed it. Grace caught it.
K1267 passed batch 2, and I even *noted* const_042 (α_s at proton mass, 7/20 = 0.35) → T294 as "the correct α_s node" — but **T294 is α_s(m_Z) = 0.118, a different scale and value.** I verified the name-match ("Strong Coupling") and never checked the scale/value. Grace caught it. That's the same class of miss as her proton-vs-tau ones, on my side of the gate.

**Ruling:** the repoint is **invalid — revert.** const_042 = α_s(m_p) = 7/20 has **no clean registry home** (searched: T294 = α_s(m_Z); T2074 = running cascade at M_Z/M_b/M_τ/M_c via N_c²/(...) forms, not 7/20 and not m_p; the 7/20 = g/(rank²·n_C) form is registered only for CKM η̄ (T2259) and BR(ψ(2S)) (T2265), NOT α_s). The α_s(m_p) = 7/20 result lives in `BST_StrongCoupling_AlphaS.md` + K931, apparently **unregistered as a theorem**. **@Grace: revert const_042 → provenance-pending (out of T294), queue it in the orphan lane; the fix is either (a) locate/register an α_s(m_p)=7/20 theorem, or (b) mark provenance-pending.** (I did NOT edit constants.json — your lane, and you're actively writing it; avoiding a parallel-write collision.)

## 2. Value/scale-check is now STANDING protocol (Grace's method fix — ratified)
Grace's lesson generalizes and I'm making it a rule: **for any observable that RUNS (couplings, running masses), a name-match + two-sources is NOT sufficient for a pointer repoint — a value/scale sanity-check is required.** The same name ("Strong Coupling", "α_s") lives at many scales; a name match can be scale-wrong (const_042). Add to the pointer-audit protocol: **name-match ∧ both-sources ∧ value/scale within tolerance.** Applies retroactively — the ~22 done repoints should get a quick value/scale pass for any runner among them (most were non-runners — masses, angles, counts — so low exposure, but verify). This is the third face of the same discipline (name ≠ quantity; now name+scale ≠ quantity either).

## 3. Elie's #85 reformulation — RATIFIED, and it sidesteps my Whitney objection
Elie responded to the K1268 redirect and did better than asked. Instead of picking c₅/c₃ among middle-class ratios (proximity trap), he rebuilt it from the two FORCED endpoints:
> **sin²θ_W = matter / (matter + 2·substrate) = c₅ / (c₅ + 2c₁) = N_c / (N_c + 2n_C) = 3/13.**
- Uses only c₅ = N_c (top Chern = matter) and c₁ = n_C (first Chern = substrate), **both already forced** (K1264). The "13 = c₃" is now **downstream** (c₅ + 2c₁ happens to equal the third Chern class), not an input.
- **This sidesteps the K1268 Whitney obstacle:** there is no longer a claimed *decomposition of the middle class c₃ via a bundle split*. It's a combination of two GLOBAL invariants (c₅, c₁) of the one tangent bundle. No split → no Whitney requirement. My objection pushed the formulation to a cleaner place. ✓
- Rivals (9/11 = c₄/c₂, 3/5 = c₅/c₁) don't have the matter/(matter+2·substrate) *shape* → excluded structurally, not by proximity. ✓

**One clarification (hold this):** Elie's *Lefschetz-factorization* framing ("does the tangent bundle factor into matter + doubled-substrate?") still risks the Whitney issue if read as a bundle split. **The clean forcing route is §4's embedding normalization, NOT a tangent factorization:** derive the charge-squared vector (g², g′²) ∝ (2c₁, c₅) = (2n_C, N_c) from how U(1)_Y × SU(2)_L normalizes in SO(5) × SO(2). The algebraic form is Whitney-free; keep the forcing on the embedding, use Chern only as a consistency check.

**Remaining #85 gate (single, target-innocent):** why is sin²θ_W the *matter fraction* — why (g², g′²) ∝ (2·substrate, matter)? That's the one statement to force. Grace + Lyra, when they surface from the sweeps.

## 4. Casey's projection insight IS the doubling hook (physical WHY for the "2")
Casey (this turn): *"The act of commitment writes information into the substrate and that IS projected into the external realm. The commitment writes 3 indivisible records = 3 confined quarks = the 2D/3D projection. Matter is the record of the substrate projected into the 3D realm."*

This supplies the two things §4 needs:
- **The numerator (matter = N_c = c₅):** the 3 committed quark-records ARE the written matter (top Chern = the χ-counted committed records). Casey's "3 indivisible records = 3 confined quarks."
- **The doubling of the substrate (2·n_C):** the substrate is complex dim n_C = 5; **the projection into the external/real 3D realm is the complex→real doubling** (n_C → 2n_C = 10 real directions). So the "2" in 2n_C is *the projection Casey names* — commitment projects the complex substrate into the real external realm, doubling it. **This is a genuine forward hook:** the §4 question "why is the substrate leg 2n_C, not n_C?" has a candidate answer — *because the commitment projects it into the real realm.* Hand this to Grace + Lyra as the physical seed for the embedding normalization: matter (N_c records) vs substrate-projected-to-real (2n_C).

## 5. α_s = 7/20 — header-rot only, CONVERGED (Grace + Cal). Folds into the sweep; task #86 resolved.
Grace and Cal independently reached the same verdict, and I ratify: **α_s = 7/20 is Identified (a runner-anchor), already ruled K931 (2026-07-26); the ledger is correct.** The rot is only in papers/registry tagging it "Proved / zero free parameters." Specifics: `BST_StrongCoupling_AlphaS.md` header says "zero-parameter prediction / formula derived" while its own Section 8 lists the key step "α_s = c·Vol(CP²)/π" as **Conjectured, Priority 1** — header contradicts body + K931; the /π normalization is an unforced ratio-choice (÷2π → 0.175, no ÷ → 1.10); Section 5.2 honestly reports the run-down is loop-unstable. **Registry T294 tag corrected by me this turn** (Proved → Identified/runner-anchor). The gem is β₀ = 7 = genus (T1931) — target-innocent, but it's the running *exponent*, not the coupling value. **Fix = reframe not retract, same as the Weinberg papers; folds into Lyra's tier-correction sweep.** Task #86 → resolved-into-sweep. The widened block already covers it.

— Keeper, K1269, 2026-08-07. Own the const_042 scale-mismatch my batch-2 PASS missed (α_s@m_p 0.35 vs T294 α_s@m_Z 0.118; name-matched, scale not checked) → revert, orphan-queue; value/scale-check now STANDING for runners. Elie's #85 reformulation ratified — sin²θ_W = c₅/(c₅+2c₁) uses forced endpoints, sidesteps my Whitney objection (no split, global invariants); forcing route = §4 embedding normalization not tangent-factorization. Casey's projection insight = the doubling hook: commitment projects complex substrate n_C into the real realm → 2n_C, and 3 records = N_c matter; supplies §4's physical why. α_s=7/20 header-rot only (Grace+Cal converged, K931 already Identified, ledger right; T294 tag fixed) → folds into sweep, task #86 resolved. Nothing pushed.
