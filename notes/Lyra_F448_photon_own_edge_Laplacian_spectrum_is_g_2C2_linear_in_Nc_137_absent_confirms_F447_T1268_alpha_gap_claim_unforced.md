---
title: "F448 — target-innocent computation on the α frontier (Casey away, bar held hard): the photon's OWN structure carries N_c only LINEARLY, and 137 is absent — confirming F447 from the corpus's own photon model (T1268), and auditing T1268's 'α = 1/N_max = eigenvalue gap' sub-claim as UNFORCED. T1268 (Proved-structural, April) models the photon as S¹ edges on ∂Δ⁶ ≃ S⁵ = the complete graph K_g on g=7 vertices. Its edge count is C(g,2) = 21 = N_c·g — LINEAR in N_c, exactly F447's prediction (the EM sector carries N_c to first power). I computed the actual spectra: K₇ vertex-Laplacian = {0, g=7}; edge-Laplacian (line graph L(K₇)) = {0, g=7, 2·C_2=12}; trace = 210 = 2·N_c·n_C·g. **N_max = 137 = N_c³·n_C + rank (CUBIC in N_c) is ABSENT from the photon's own structure** — there is no 137 eigenvalue and no 1/137 gap. So T1268's P4 / T1234-connection assertion 'α = 1/N_max is the eigenvalue gap of the photon edge Laplacian' is UNFORCED — asserted, not computed; the actual spectrum is {0, g, 2C_2}. This does NOT touch T1268's core (photon = edge; m=0, Q=0, s=1, 2 helicities from topology — that stands); only the embedded α-identification sub-claim. NET: α = 1/(photon's OWN channel count) is ruled out rigorously — the photon's own invariants are the N_c-LINEAR set {g, 2C_2, N_c·g, 2N_c·n_C·g}, never the cubic 137. So 137's N_c³ is the TOTAL-substrate color-sector share (F447), and α's per-channel identification, if it exists, lives in Elie's total cell-count lane — NOT in the photon's own structure. Confirms F447 from an independent corpus object. No bank, count 10. Staged for Keeper (T1268 P4 audit) + Elie (frontier confirmation). For Casey on return, Elie, Grace, Keeper, Cal."
author: "Lyra (Claude Opus 4.8)"
date: "2026-07-01 Wednesday (date-verified, Casey away long-pull)"
status: "v0.1 — target-innocent α-frontier computation. T1268 photon = S¹ edges on ∂Δ⁶≃S⁵ = K₇. Edge count 21=N_c·g LINEAR (confirms F447). Computed spectra: vertex-Lap {0,g=7}; edge-Lap {0,g=7,2C_2=12}; trace 210=2N_c·n_C·g. 137=N_c³·n_C+rank ABSENT (cubic). T1268 P4 'α=1/N_max=eigenvalue gap' UNFORCED (asserted, actual spectrum {0,g,2C_2}). T1268 core (photon=edge, quantum numbers) UNTOUCHED — only the embedded α-id sub-claim. α=1/(photon own count) ruled out; photon own invariants are N_c-LINEAR {g,2C_2,N_c·g,2N_c·n_C·g}; 137's N_c³ = total-substrate color share (F447) → Elie's cell-count lane. No bank, count 10. For Casey/Elie/Grace/Keeper/Cal."
---

# F448 — the photon's own structure is N_c-linear (g, 2C_2); 137 is absent; T1268's α=1/N_max gap-claim is unforced

Autonomous α-frontier work while Casey is away, bar held hard: rather than assert, I computed — and audited an existing "Proved" claim in the process. This confirms F447 from the corpus's *own* photon model and rules out the "α = 1/(photon's own channel count)" reading rigorously.

## The corpus's own photon model (T1268, Proved-structural, April 2026)

T1268 models the photon as an **S¹ edge on ∂Δ⁶ ≃ S⁵** — the complete graph K_g on **g = 7** vertices (each carrying a U(1) phase circle), with edge count

  C(g,2) = C(7,2) = **21 = N_c·g**.

Two claims are embedded in T1268: (core) the photon = edge, with m=0, Q=0, s=1, 2 helicities derived from edge topology — solid, and untouched here; and (P4 / T1234-connection) **"α = 1/N_max = 1/137 is the eigenvalue gap of the photon edge Laplacian,"** stated but never computed. I checked both the linearity and the gap.

## Result 1 — the photon's own N_c-content is LINEAR (confirms F447 from the corpus itself)

The edge count is **21 = N_c·g** — N_c to the **first power**. This is exactly F447's central claim reached from the other side: the EM/photon sector carries N_c *linearly*, never cubically. The corpus's own photon model already encodes the linearity. (This is the graph-theoretic image of the R-ratio's single color trace.)

## Result 2 — the photon's own spectrum is {0, g, 2C_2}; 137 is absent

I computed the actual Laplacian spectra of the T1268 photon graph (exact, target-innocent — no reference to 137):

| operator | spectrum | multiplicities |
|---|---|---|
| K₇ **vertex** Laplacian | {0, **g = 7**} | 0×1, 7×6 |
| L(K₇) **edge** Laplacian (line graph) | {0, **g = 7**, **2·C_2 = 12**} | 0×1, 7×6, 12×14 |
| trace of edge Laplacian | **210 = 2·N_c·n_C·g** | — |

The photon's own invariants are **{g = 7, 2C_2 = 12, N_c·g = 21, 2N_c·n_C·g = 210}** — every one **linear in N_c**. **N_max = 137 = N_c³·n_C + rank (cubic in N_c) is ABSENT**: there is no eigenvalue 137, and no 1/137 gap (graph-Laplacian gaps here are O(1) integers {7, 12}, not a small 1/137 spacing).

## Result 3 — T1268's "α = 1/N_max = eigenvalue gap" sub-claim is UNFORCED

The embedded identification "1/N_max = α is the eigenvalue gap of the photon edge Laplacian" (T1268 T1234-connection; and P4's "photon size = 1/137 of the Shilov diameter") is **asserted, not derived** — and the actual computed spectrum {0, g, 2C_2} does not bear it out. This is a peer-audit catch on a sub-claim of an existing Proved-structural theorem:

- **T1268 core stands** (photon = edge; quantum numbers from topology; wave-particle dissolution) — I am not touching it.
- **T1268's embedded α = 1/N_max identification is unforced** — the photon's own structure gives {g, 2C_2}, not 137. (The separate geometric "size = 1/137 of Shilov diameter" claim I did not compute; it is likewise stated without derivation and should not be cited as forced.)

Flagging per the unsupervised-pull discipline (hold the bar harder): an existing theorem carries an unforced α-identification that a target-innocent computation does not support. Keeper to log; T1268 core unaffected.

## What this does for the α frontier

It **rigorously rules out** the reading "α = 1/(photon's own channel count)," and confirms F447 from an independent corpus object:

- The photon's own count is **N_c-linear** ({g, 2C_2, N_c·g, 2N_c·n_C·g}). A color-blind photon's own structure cannot produce the **cubic** N_c³.
- Therefore the N_c³ in 137 = N_c³·n_C + rank is **not** the photon's — it is the **total-substrate color-sector share** (F447), and α's per-channel identification, *if* it exists, is a **total-substrate equipartition** living in Elie's cell-count lane, never in the photon's own structure.

Two independent lines now agree that the photon's own structure is N_c-linear: F447 (vacuum polarization, R-ratio) and F448 (the T1268 graph spectrum, {g, 2C_2}). The frontier is exactly where Grace/Elie/F447 placed it — the total substrate DOF count — and this computation removes the tempting "just use the photon's 137" shortcut by showing the photon's own numbers are 7, 12, 21, 210, not 137.

## Honest tiers

| claim | tier |
|---|---|
| photon edge count 21 = N_c·g is linear in N_c (T1268, arithmetic) | **SOLID**; confirms F447 |
| photon own Laplacian spectrum = {0, g, 2C_2}; 137 absent | **SOLID** (computed exact) |
| T1268 P4 / "α = 1/N_max = eigenvalue gap" is unforced | **peer-audit catch** — asserted, not borne out by the spectrum; T1268 core untouched |
| ⟹ α ≠ 1/(photon's own count); N_c³ is total-substrate (Elie's lane) | **structural** (confirms F447) |

**No bank; count stays 10.** F448 is a target-innocent confirmation of F447 + an honest audit of an embedded sub-claim. It does not move the count; it sharpens the α frontier by rigorously removing the photon's-own-structure shortcut, leaving Elie's total-substrate cell-count as the one live route.

## Plain-language version

The theory's own model of light (from April) pictures a photon as an "edge" connecting 7 dots — and it has 21 edges, which is 3 (the number of colors) times 7. Notice: color shows up **once** (the "3"), not three-times-over. I then computed the natural vibration-numbers of that 7-dot structure and got 7 and 12 — and, importantly, **not 137**. So the old note's claim that "α = 1/137 is the gap in the photon's vibrations" was a hopeful label, not a calculation — the real numbers are 7 and 12. The upshot: the "137" that equals 1/α cannot come from the photon by itself (the photon only ever shows color *once*), so the "27" it needs must come from the whole substrate's book-keeping — exactly where the team already relocated it. I confirmed the relocation by showing the photon's own numbers are small and color-once, never 137.

— Lyra, Wed 2026-07-01 (Casey away, long pull). F448: T1268 photon = S¹ edges on K₇ (g=7 vertices), edge count 21=N_c·g LINEAR (confirms F447). Computed spectra: vertex-Lap {0,g=7}, edge-Lap {0,g=7,2C_2=12}, trace 210=2N_c·n_C·g. 137=N_c³·n_C+rank ABSENT (cubic). T1268 P4 'α=1/N_max=eigenvalue gap' UNFORCED (asserted; actual spectrum {0,g,2C_2}); T1268 core untouched. α=1/(photon own count) ruled out; photon own invariants N_c-LINEAR; 137's N_c³ = total-substrate (F447) → Elie's cell-count. No bank, count 10.
