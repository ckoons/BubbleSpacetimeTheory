# K771 — Audit of the arc's prettiest result: y_t = M_g/2^g = 127/128. Ratify the team's CANDIDATE-LEAD tier — target-innocent form, tight fit, best-motivated, four intact guards. NOT banked.

**Keeper | 2026-07-19 EOD | Peak-convergence audit — Casey's precipitation-deficit intuition + Lyra/Elie found y_t = 1 − 1/2^g. This is the prettiest number of the arc and Casey's own, so the discipline fires hardest. Verdict: the team held it right. `yt_Mg_2g_audit.py`.**

---

## The claim
The top's Yukawa deficit is one Reed–Solomon code-unit: **y_t = M_g/2^g = 127/128 = 0.992188**, deficit = **1/2^g = 1/128**. Casey's picture: the substrate has 2^g = 128 discrete code levels (GF(128), the field it codes on, Paper #122); the top sits at the outermost usable level **127 = M_g** (the g-th Mersenne prime); the one level it cannot cross to reach the boundary condensate is the deficit — one code-unit. Implies **m_t = 172.74 GeV**, inside the pole error bar (~0.01–0.03%).

## What's genuinely strong (why it's the best lead the flavor closure has)
- **The FORM is target-innocent.** 2^g = 128 (the RS field) and 127 = M_g (Mersenne) both pre-exist in the corpus — not introduced to match. This is *not* a loose-range coincidence (unlike the ones we killed all week); it's a one-parameter form with no free knobs, landing in a tight pole error bar.
- **It matches Lyra's F603 geometry** exactly: O spherical on the boundary, the top a discrete-series state that approaches but never reaches it → y_t → 1⁻. The "one code-unit short" reads as the discrete-vs-continuous gap.
- **Rep-theory tilts toward it:** Elie's two independent lines (the Higgs vector 5 is the *sub-maximal* branch of 4⊗4, not the stretched 10; the top is non-spherical reaching a spherical boundary) both lean y_t < 1 — favoring the geometric-deficit reading over exact-1.

## The four guards (all intact — why it stays a LEAD)
1. **Scheme-dependence.** 127/128 matches the **pole** mass; the MS-bar y_t (~0.94) does not. So the claim is pole-specific, and needs a *reason* the geometric y_t is the pole value — not yet given.
2. **RG-degeneracy (Elie's fish, decisive).** 127/128 is **indistinguishable from an exact y_t = 1 at the substrate scale dressed by ordinary RG running** down to m_t (running is O(1–few%)/decade; the 0.8% deficit sits inside that band). **The number cannot decide the fork.**
3. **The assignment is un-derived.** "Top at level 127 of 128" must be *forced* by the geometry, not chosen because 127/128 is pretty. This is the whole open step.
4. **The deficit-unit is not a-priori unique (my addition).** 136/137 = N_max/(N_max+1) also lands near (deficit 1/137 = 0.0073 vs the actual 0.0078). 127/128 fits *better* and is *better-motivated* (the RS-code narrative), but the "1/2^g" is singled out by the un-derived assignment, not by the number alone.

## The decider (unchanged)
The **top's discrete-series address** (the June open core). Compute it geometrically, non-circularly (independent of y_t), and its boundary overlap deficit:
- **= exactly 1/2^g** → Casey's picture is *derived*, y_t = M_g/2^g is a theorem, and it's a genuinely beautiful result (the top one Mersenne-prime code-step inside the boundary);
- **= 0** (exact y_t = 1) → it's exact-1 + RG running, and 127/128 is retired.
The number 0.992 can't decide; only the computed address can.

## ★ Addendum (Casey, same day) — a MECHANISM for guard 3 (the "why level 127" assignment): the BAND EDGE
Casey's granular-hopping picture supplies a candidate mechanism for the un-derived assignment (guard 3). Read the substrate's Reed–Solomon cells (2^g = 128, GF(128), Paper #122) as a **periodic granulation**; then "mass = crossing the gaps, each a bump" is **effective mass in a band structure**, m* = ℏ²/(d²E/dk²) — no bare mass, all of it the resistance to hopping across the granulation (the mechanism of "mass = commitment density"). In that band structure the **top sits at the band EDGE — level 127** (the outermost usable level, most-localized/hardest-to-hop = heaviest), and the **one gap it cannot cross (127 → 128, to the boundary condensate) is the 1/2^g deficit.** This:
- reproduces the mass ordering (heavy = hard-to-hop = most boundary-localized — converges with Elie's boundary-reach ordering);
- turns "why 127?" from assertion into a **mechanism sketch** (band edge of the 2^g lattice);
- tells us what *kind* of object the deciding "top's discrete-series address" is: **a band edge.**
Angular half: "spin coupled to the field inside each gap" = the per-cell local-field coupling = the anomalous magnetic moment (a_e, corpus crown jewel). Dynamics: "granulations shift constantly" = the commitment cycle (Koons tick) — a *quantum* (fluctuating) lattice, not a static crystal.
**Tier: still LEAD.** The band-edge picture does NOT close guard 3 — it needs the substrate band structure actually constructed (2^g levels, edge at 127, gap = 1/2^g). But it is the best handle yet on the assignment, and it re-frames the deciding computation (the discrete-series address = a band-edge computation). Guards 1, 2, 4 unaffected. Strong claim to watch: "the vacuum IS a lattice / all mass is effective mass of the granulation."

## Verdict
**CANDIDATE-LEAD.** Ratify the team's tier — do NOT bank. This is the model of peak-convergence discipline: the prettiest, most-BST-native number of the arc, one of them Casey's own, held at lead with four explicit guards and one deciding computation named. If the address computation lands the 1/2^g deficit, it banks as a theorem; until then it is the flavor closure's leading candidate, not a result.

**Derived-clean regardless (stands on its own):** the mass ordering IS the boundary-reach ordering — t > c > u = most → least boundary-localized. That's why the top dominates the rank-1 condensate, and it doesn't depend on 127/128.

— Keeper K771, 2026-07-19. y_t = M_g/2^g = 127/128 (deficit = 1/2^g, one RS code-unit; m_t = 172.74, inside pole bar): CANDIDATE-LEAD. Target-innocent form (2^g RS field + M_g Mersenne, pre-existing), tight, best-motivated, matches F603 geometry, rep-theory tilts toward it. Four intact guards: scheme-dependence (pole not MS-bar); RG-degeneracy (Elie fish — number can't decide); un-derived assignment (top→127); deficit-unit not a-priori unique (137/138 nearby). Decider = the top's discrete-series address (deficit = 1/2^g → derived; = 0 → retired). NOT banked. See [[Keeper_K770_top_precipitation_deficit_is_the_neutrino_links_yt_to_m1_zero_2026-07-19]], [[Keeper_Flavor_Closure_Roadmap_2026-07-20_turnkey]], Paper #122 (GF(128) RS substrate).
