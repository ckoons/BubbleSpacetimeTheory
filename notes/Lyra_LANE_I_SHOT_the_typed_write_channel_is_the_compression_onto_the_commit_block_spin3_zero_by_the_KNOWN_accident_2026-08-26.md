# LANE I — THE SHOT: exhibits and values first, verdict last
**Lyra (+ Cal post-audit), 2026-08-26 (clock-verified at post). One shot, fired under the gated
prereg + addendum + Keeper's pin (composition order must be TYPE-FORCED). Cap stated in advance and
repeated at the verdict: everything here is at most DERIVED-CONDITIONAL on the Internal A floor
(P0 + P1′ + the ruler).**

---

## 1. THE NON-EQUIVARIANCE THEOREM (I-1(a), discharged by citation — the door's "say which")

C = P_record ⊕ P_encode is the Peirce split of a chosen primitive idempotent e (#67 §2a, Derived,
K1607/K1609); isotropy_commit (banked) makes the primitive idempotents a single orbit; hence
k·C(e)·k⁻¹ = C(k·e) ≠ C(e) off Stab(e) ⊊ K. **Equivariance breaks; antisymmetry of the bracket is
untouched. Declared before the computation, per the prereg.**

## 2. KEEPER'S PIN: THE TYPED ASSEMBLY, ENUMERATED BEFORE ANY "THEREFORE" (all candidates, then the survivor)

The banked objects: commit_op C ∈ End(J) (with e = the banked ARROW idempotent — the bank names it;
no choice) · S_Δ (twisted Szegő, v3 exhibit + symbol page) · T_f = S_Δ M_f S_Δ (Toeplitz). Candidate
assemblies of a write-dressing W from these, with zero new choices:

- **A. Symbol pullback** W(T_f) = T_{f∘C}: **ILL-TYPED** — C is a projection on J and does not
  preserve the Shilov boundary Σ (it maps the sphere into the ball); f∘C is not a boundary symbol.
- **C. Conjugation by e^{it·dπ(e)}**: introduces t — **KNOB. Dead.**
- **D. Literal one-sided C ∘ S_Δ**: S_Δ lands in H²_Δ; C's domain is J; no banked map connects them
  without choosing a vector — **CHOICE-DEMAND. Dead.**
- **E. Clifford lift** W(T_f) = T_{γ(C)} T_f T_{γ(C)}: the Clifford image of a projection is not
  canonical (which lift of P_record ⊕ P_encode into Cl(V) acts on Δ?) — **CHOICE-DEMAND. Dead.**
- **B. Module compression** W(T) = Q T Q, where Q = the projection onto the commit block's module
  home = (record line) ⊕ (the V₁₂ shell): **TYPED AND ZERO-CHOICE.** Both factors banked: the
  V₁₂ shell is the #108 arc's own "3-dim carrier" (the closure computation certifies it as V₁₂'s
  module home; #67 §2a names V₁₂ as P_encode's block), and the record line rides the banked arrow
  idempotent. **The ORDER is type-forced twice over:** the bank's own form of the commit on
  states/observables is the CONTRACTIVE FACE — a compression ("becoming-definite = the contractive
  commit," Derived, #67 ledger) — and compression Q·Q is the unique assembly of these factors that
  preserves self-adjointness of observables (QT and TQ do not). **Exactly one candidate survives.
  The pin is answered: W = QTQ, order forced, exhibited — not chosen.**

## 3. CONTROLS, RUN PRE-SHOT (the instrument = exact triangle/dimension arithmetic on shells)

- **Must-catch:** on any shell L ≥ 2, End(shell) contains spin 3 (max spin 2L ≥ 4; Δm = 3 reachable,
  max m-separation 2L ≥ 4 ≥ 3) — the instrument CATCHES a spin-3 home where the banked measured
  nonzero scalar lives (K1826-adjacent arc, cited as the anchor). ✓ RUN.
- **Must-reject:** on the 3-dim carrier (L = 1), max m-separation = 2 < 3 — no spin-3 home; the
  instrument reproduces the banked closure accident. ✓ RUN.
- **Win-limb reachability check (§770 line 1, both directions):** had the typed W landed on a block
  containing an L ≥ 2 shell, the spin-3 home exists and the banked scalar is nonzero there — the WIN
  limb was reachable. **The types decided the landing, not the instrument.**

## 4. THE EXACT COMPUTATION (values before verdict)

Q's block = (record line) ⊕ (V₁₂ shell). Exact matrix elements of the symbols Y_{2m} (built on the
V₁₂ directions) on this block, by Gaunt/Clebsch arithmetic — no numerics, all zeros structural:
- ⟨record | Y_{2m} | shell⟩: the record direction v₀ ⊥ V₁₂ (the summands of C are orthogonal); the
  record mode is odd in the v₀-coordinate, Y_{2m} does not depend on it — **integral = 0.**
- ⟨record | Y_{2m} | record⟩ ∝ ⟨0‖Y₂‖0⟩: triangle {2} ∌ 0 — **0.**
- ⟨shell | Y_{2m} | shell⟩: triangle |1−2| ≤ 1 ≤ 3, parity even — **nonzero = the known carrier
  action, unchanged.**

⟹ **Q T_{Y_{2m}} Q = 0_record ⊕ (the known 3-carrier action), exactly.** Hence
[W(T_{Y22}), W(T_{Y21})] = 0 ⊕ [the known compressed bracket] — **the dressed bracket COINCIDES with
the compression onto the known closed 3-dim algebra** (the record line inert). Its spin-3 scalar:
End(singlet ⊕ V₁₂) decomposes under the V₁₂ SO(3) into spins {0,1,1,0,1,2} — **maximum spin 2 < 3.
The spin-3 scalar is 0, structurally, in exact arithmetic — a zero by CONFINEMENT.**

## 5. VERDICT (last; the pre-ruled meanings are the only ones available)

The addendum's degenerate limb fires verbatim: **spin-3 = 0 by confinement, AND the dressed bracket
coincides with the known compression ⟹ "the wall deepens via the KNOWN accident — ZERO NEW
CONTENT."** Not a novel exclusion; not closure-achieved. **The write channel, in its one typed
zero-choice form, joins R68c's excluded list in compression form: it does not supply new dynamics
for the five quadrupole directions.** k/N: 4 checks, of which 3 could fail (assembly could have
demanded a choice → NOT-READY; the block could have carried L ≥ 2 → spin-3 present; the shell
arithmetic could have failed a control) — none did; 1 (the coincidence check) is decisive rather
than can-fail. **Tier of the whole chain: DERIVED-CONDITIONAL on the Internal A floor — the cap the
prereg set, honored.** One shot: SPENT. The lane closes at this floor.

## 6. BOARDED OBSERVATION — outside the verdict, uncomposed, SUGGESTIVE only

From the commit's side the "dimension accident" is not an accident: **P_encode's home IS the closure
carrier.** The commit confines physics to exactly the block where the quadrupole algebra closes by
dimension. That reframes WHY the closed 3-dim carrier is the physical one — but it supplies no new
dynamics, and this line claims nothing beyond its label.

*— Lyra. The door demanded "break equivariance or antisymmetry and say which"; the answer broke
equivariance, and the geometry answered back: the only typed write is the compression, and the
compression lands on the accident we already knew. A wall that deepens under an honest shot is a
result — the record will deserve belief, which is the thing we build.*
