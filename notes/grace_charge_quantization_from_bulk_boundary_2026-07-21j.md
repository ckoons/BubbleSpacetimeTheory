# Charge quantization from bulk/boundary — the 1/N_c fractionalization (new row, first pull)

*Grace | 2026-07-21j | the pivot row: electric charge from bulk-vs-boundary (confinement) geometry — discrete, structural, exact, my native medium. First pull. I derived the exact charges from the confinement skeleton + neutrality, then self-critiqued: the clean target-innocent win is the 1/N_c fractionalization; the exact values are derived-conditional (the doublet is SM input). Honest first-pull tier.*

## The structural skeleton (Casey's confinement diagnosis)
```
  quarks  = colored BULK   → charge quantized in units of 1/N_c = 1/3   (the "1/3" IS color N_c)
  leptons = colorless BOUNDARY (Shilov) → INTEGER charge
```
Bulk-vs-boundary = **thirds-vs-integers.** This is the clean handle, and it's discrete/exact — nothing runs, clean pass/fail.

## ★ The clean win (target-innocent): the 1/N_c fractionalization
**Quark charges have denominator N_c = 3; lepton charges are integers.** The denominator is **N_c, forced by color** (quarks are colored bulk states), not fit. The fingerprint is exact: the "1/3" in every quark charge = 1/N_c. This is **DERIVED-structural** — the bulk(colored)/boundary(colorless) split *produces* the fractionalization pattern. **Bank as candidate** (the cleanest, most-likely-to-land piece — the pull's first target).

## The exact values — produced, but derived-CONDITIONAL
With three inputs, the exact charges fall out with **no free parameters:**
| input | statement | status |
|---|---|---|
| (i) Q_ν = 0 | neutrino = neutral colorless boundary edge-mode | motivated (input) |
| (ii) isospin split = 1 | Q_u−Q_d = 1, Q_ν−Q_e = 1 (weak doublet T₃=±½) | **SM input** (electroweak SU(2), imported) |
| (iii) N_c-weighted neutrality | N_c(Q_u+Q_d) + (Q_e+Q_ν) = 0 | **the geometric crux** |
Solving: **Q_u = +2/3, Q_d = −1/3, Q_e = −1, Q_ν = 0 — EXACT.**
```
  N_c(Q_u+Q_d)+(Q_e+Q_ν)=0, Q_ν=0, Q_ν−Q_e=1, Q_u−Q_d=1
    → Q_e=−1, Q_u+Q_d=1/N_c, Q_u−Q_d=1 → Q_u=(1+1/N_c)/2=2/3, Q_d=(1/N_c−1)/2=−1/3  ✓
```

## Self-critique (the discipline — I used SM input, flagging it)
**Honest test:** does bulk/boundary + neutrality *alone* fix the charges? **No** — neutrality is 1 equation in 4 unknowns; it needs the doublet (2 eqs) + Q_ν=0. So the pull produces the values from {1/N_c-fractionalization + doublet + Q_ν=0 + neutrality}, **not from geometry alone.** The **doublet (ii) is SM input**, not derived here. So:
- **Clean/target-innocent:** the 1/N_c fractionalization (thirds ↔ color).
- **Derived-conditional:** the exact values, *given* the weak doublet + Q_ν=0.
- **The geometric crux (iii):** the N_c-weighted neutrality = anomaly cancellation. **BST candidate reading: total charge of the closed domain = 0 (a Gauss-law/neutrality of the bounded geometry).** If *that* is geometric, the values are genuinely derived-conditional; if it's just imposed, it's SM repackaged. **This is the load-bearing next question.**
Five-Absence OK (charge quantization is standard SM structure, not a forbidden value).

## Next (the row's real targets, ranked)
1. **Bank the 1/N_c fractionalization** — is "quark = colored bulk → charge in units of 1/N_c" a clean geometric theorem? (the strongest piece).
2. **Derive the N_c-weighted neutrality geometrically** — is it "total domain charge = 0" (Gauss law on the bounded domain)? If yes, the exact values bank derived-conditional.
3. **Then** whether the full hypercharge structure (Y_Q=1/3, Y_L=−1) is geometric — likely needs more than bulk/boundary.

## Net
- **Structural skeleton:** quarks colored bulk → 1/N_c fractional; leptons colorless boundary → integer. Discrete, exact, clean pass/fail.
- **★ Clean win (candidate):** the **1/N_c fractionalization** — quark charge in thirds because bulk-color N_c, target-innocent (denominator forced, not fit).
- **Exact values {+2/3,−1/3,−1,0} produced** from {fractionalization + doublet + Q_ν=0 + N_c-neutrality}, no free params — but **derived-CONDITIONAL** (the doublet is SM input; the N_c-neutrality = anomaly = "total domain charge 0" is the geometric crux to derive).
- **Held honest:** self-critiqued, separated geometric-new from SM-input; don't over-bank a first pull.

— Grace, 2026-07-21j. Charge-quantization row (pivot, discrete/structural). Skeleton: quarks=colored BULK→charge in units 1/N_c=1/3 (the "1/3" IS color); leptons=colorless BOUNDARY→integer. CLEAN WIN (target-innocent, candidate): the 1/N_c fractionalization (denominator forced by color, not fit). Exact charges {+2/3,−1/3,−1,0} PRODUCED no-free-params from {fractionalization + weak-doublet(isospin=1) + Q_ν=0 + N_c-weighted neutrality} — but DERIVED-CONDITIONAL: the doublet is SM input, and the N_c-neutrality (= anomaly cancellation) is the geometric crux (BST candidate: total domain charge=0, a Gauss law). Self-critiqued: geometry-alone (1 eq, 4 unknowns) does NOT fix values without the doublet. Next: bank the 1/N_c fractionalization + derive the neutrality as total-domain-charge-0. FA OK. First-pull honest tier, not over-banked.
