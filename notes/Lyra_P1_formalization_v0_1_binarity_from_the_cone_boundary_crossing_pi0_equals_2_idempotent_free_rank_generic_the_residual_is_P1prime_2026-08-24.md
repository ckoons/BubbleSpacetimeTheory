# P1 FORMALIZATION v0.1 — **binarity from the interface, idempotent-free.** π₀(J ∖ ∂Ω) = 2 by convexity + a three-line funnel; the residual posit becomes P1′ ("a commitment is a crossing of the interface"), which has the outside motivation P1 never had.

**Lyra, 2026-08-24. Grace's binding adopted in full: no complements-of-idempotents anywhere below — her iff (1−e primitive ⟺ rank 2) is verified and it is exactly K1815's "complement is atomic" naming, so the e/1−e route was the posit in a costume. Keeper's cone-boundary candidate taken up and it lands. Rule 3: @Cal gates. Nothing banked. Cell: Internal A.**

## LEMMA 1 (two-sidedness — rank-generic, idempotent-free)
Let J be a Euclidean Jordan algebra, Ω its symmetric cone. Then **J ∖ ∂Ω has exactly two connected components.**
*Proof.* int Ω is convex ⟹ connected. For the exterior: any x with λ_min(x) < 0 connects to −1 by the **funnel path x(t) = (1−t)x − t·1**, whose spectrum is exactly {(1−t)λᵢ(x) − t} ⟹ λ_min(x(t)) = (1−t)λ_min(x) − t < 0 for all t ∈ [0,1] — **exact, not an estimate** (a multiple of the identity shifts the spectrum rigidly). So the exterior is path-connected. π₀ = 2. ∎
**Verified numerically** across Sym₂, Sym₃, Sym₄ (300 paths each, min margin 0.007/0.043/0.28 — all strictly outside). **Rank-generic — 2, 3, 4 alike — and no idempotent is named.** The face stratification of ∂Ω, where rank information lives, is **unused and hereby BANNED from this argument** (contact-at-a-face language is where the smuggle would re-enter).

## P1′ (the residual posit, stated as the posit)
> **A commitment is an irreversible crossing of the interface ∂Ω.**
This is Casey's physical picture formalized on the cone side: **his two sides are the 2D substrate and the 3D continuum — a geometric interface, not an idempotent pair** — and ∂Ω is the codim-1 object in J that has an inside and an outside **by convexity alone.** SWPP's absorption → commitment → emission is a crossing narrative; K1607/K1609's commit-at-the-boundary sits on ∂Ω. **The motivation predates this derivation by months — that is the outside-motivation the costume test demands, and it is what P1 ("the cheapest axiom") never had.**

## LEMMA 2 (binarity)
The topological record of a single ∂Ω-crossing is **which component the state ends in, given where it began: an element of a 2-element set. One bit.** A commitment therefore records **exactly one binary distinction** — **P1.** ∎ *(v0.1 form; Cal may want the invariant sharpened — ordered pair vs endpoint class — but every version is valued in a 2-set because Lemma 1 says there are only two rooms.)*

## THE CHAIN AND THE HONEST PRICE
```
   P0 (Euclidean Jordan arena)  ⟹  Ω symmetric cone, convex          [banked]
   LEMMA 1: pi_0(J \ dOmega) = 2                                      [derived, rank-generic, idempotent-free]
   P1' (commitment = interface crossing)                              [THE residual posit — outside-motivated]
   LEMMA 2: crossing record = one bit  ⟹  P1                          [derived]
   P1 ⟹ rank 2 ⟹ spin factor ⟹ type IV                               [banked chain; idempotents appear only HERE, downstream]
```
- **What is bought:** P1 stops being an axiom and becomes a theorem of (P0 + P1′). **The floor's soft entry changes from "binarity, unexplained" to "commitment = boundary crossing, physically motivated"** — strictly better under the outside-motivation test, because P1′ does work elsewhere in the corpus (SWPP, the commit ontology, the 2D/3D interface) and was written before anyone needed binarity from it.
- **What is NOT bought, stated plainly:** P1′ is **stronger** than P1 as ontology (P1′ ⟹ P1; not conversely). **Irreversibility is carried by P1′, not derived.** And the identification "the physical interface = ∂Ω of THIS J" leans on P0's arena choice — the inheritance is P0's, not new.
- **Anti-smuggle audit (Grace's binding):** no complement, no primitive, no idempotent, no face named anywhere above the "downstream" line. Rank enters only through the banked P1 ⟹ rank-2 step. **The two-sidedness used is π₀ data only, proved rank-generically — it cannot be rank-2 in disguise because it is TRUE at rank 3 and 4.**

**Lyra, P1 formalization v0.1. Binarity derived from (P0 + P1′) via two lemmas, the first proved in three lines with an exact funnel, the second reading the one-bit record off π₀ = 2. Grace's binding satisfied by construction and her iff verified. The residual posit P1′ is Casey's interface picture, which predates the need. @Cal gates; the sharpening he will want is the crossing-invariant's exact form. Nothing banked.**
