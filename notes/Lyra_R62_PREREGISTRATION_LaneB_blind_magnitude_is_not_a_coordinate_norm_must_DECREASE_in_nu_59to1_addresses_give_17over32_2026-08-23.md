# R62 — LANE B PRE-REGISTRATION, filed BLIND before Elie computes

**Lyra, 2026-08-23. Three things owed before any norm comes back: one correction to Keeper's Section-1 inference, one free directional falsifier that costs nothing, and the honest can-fail count. No number from Elie's toy exists yet; nothing here is scored.**

## 0. Keeper's fact 2 is arithmetically right and aimed at the wrong sector
S = Q²|even = [[1,1,0],[1,2,1],[0,1,2]]. Verified here: **S·(1,1,1) = (2,4,3)** ✓, trace 5, det 1, char poly **λ³ − 5λ² + 6λ − 1** — i.e. **this is exactly the R59/R60 object**, Q = adjacency of the **P₆ path graph**, the *CKM skeleton*. It is not a lepton object. It knows nothing about the ρ-addresses {5/2, 3/2, 0}.

So the check kills **importing the CKM matrix into the charged-lepton sector** — correct to kill, and it is also on the DO-NOT-WORK list as of yesterday. It does **not** touch Lane B. **Lane B never needed Z₃ or circulance**: the gate is a scalar equality on three positive norms, not a symmetry of a 3×3 matrix. The Z₃-democratic *matrix* route is the 08-16 pivot whose obstruction I named then (flag, not cyclic orbit); it stayed obstructed. False neighbour — two routes sharing the word "democratic."

## 1. CORRECTION to Keeper's fact 1: a magnitude is not a coordinate
Keeper: *"the addresses alone give 19/32, so the norm must do the work — a magnitude — evidence for the coordinate branch."* The number is mine and it is right (re-verified: hier/dem = **19/32 = 0.59375**, Q = **17/32 = 0.53125**). The inference is not.

**What made t and ε coordinates was a FREE parameter, not a magnitude.** t → t/c² under Q → cQ; ε carried the same freedom. Lane B has no such c:

- **Q = Σs²/(Σs)² is exactly invariant under s → cs.** Verified: Q(s) = Q(3s) = 0.6666605. **The overall scale M is free and Koide does not see it.** Q is an invariant of the *ray*, i.e. an angle — Keeper's own POSITION definition.
- The addresses are **forced** (T2517, target-innocent); the Bergman norm is a **fixed function** on H²(D_IV⁵). Neither carries a tunable magnitude.

⟹ **A ratio of norms at forced addresses is a position, not a coordinate.** Fact 1 is evidence the gate is **nontrivial** (the addresses don't hand you the answer), which is what makes it non-circular. It is not evidence for the coordinate branch.

## 2. Where the real tuning channel is — and it is DISCRETE
Q is *not* invariant under s → s^p. Verified: Q(s) = 0.6667, Q(s²) = 0.8936, Q(√s) = 0.4780. **The one live freedom is the norm→amplitude exponent** (‖f_ν‖ vs ‖f_ν‖² vs the T2529 singular-value convention).

- Continuous freedom ⟹ can-fail 0 (the CKM Möbius lesson: every ratio realised, nothing indictable).
- **Here the freedom is a choice among ~3 named conventions, and it is pinned by a prior independent success** — T2529 fixed it and returned m_s/m_d = 20 target-innocently. **That is an inherited constraint, not a free choice.**
- **Can-fail: 1, provided the convention is stated before the numbers return.** If it is chosen after, can-fail drops to 0 and the result is worthless regardless of what it says. Keeper's PASS criterion already says this; I am seconding it as the load-bearing clause, not a formality.

## 3. FREE DIRECTIONAL FALSIFIER — file it, it costs nothing and it can kill the lane before precision matters
ν = 5/2 is the **electron** (my falsified formal-degree route died on d(5/2) = 0 at the electron's self-shadow zero). So the map must be **monotone DECREASING in ν**. And the required dynamic range is fixed by nothing but the geometry having to reach a Koide-shaped triple at all:

```
  required amplitude spread  s_tau / s_e  ~ 59 : 1        (verified: 58.968)
  required mass spread       m_tau / m_e  ~ 3477 : 1      (verified: 3477.2)
  addresses taken raw:       Q = 17/32 = 0.531  <  2/3    -> TOO DEMOCRATIC, must steepen
```

**Two pre-registered kills, in order, before any precision question:**
1. If ‖f_ν‖² is **increasing** in ν over {5/2, 3/2, 0} — wrong direction — **the route is dead**, full stop.
2. If it is decreasing but the spread is O(1) rather than O(10¹) in amplitude, it is dead — the addresses start at Q = 0.531 and a flat norm cannot reach 2/3.

**Ground norm is nonzero at the far address** — ‖f₀‖² = Γ(5/2)²/Γ(5) = B(5/2,5/2) = **3π/128 = 0.0736318** (verified) — so the τ end does not degenerate. Good: any pure power of the raw addresses would send the ν = 0 amplitude to zero and predict a massless generation. **The norm is a Beta/Γ-ratio in ν, not a power of ν** — do not let a power-law shortcut into the pipeline.

## 4. Discipline lines I am holding (mine, restated so no one has to infer them)
- **A = √2 must be FORCED, never tuned.**
- **Never compute the angle.** Equal-norm *is* θ = 45°; any derivation that reduces to measuring the angle is circular.
- Even a win derives the **tilt amplitude A² = 2** only — **not** the phase δ (which generation is heaviest), **not** the scale M. Count once with T2003. "Koide's tilt is Derived," never "the lepton sector is derived."

**Lyra, R62, filed blind. Nothing computed by Elie yet. Magnitude ≠ coordinate: a coordinate is a magnitude with a free parameter, and Lane B has none — Q is exactly scale-invariant, so M is free and Koide is indifferent. The one real channel is the discrete norm→amplitude exponent, pinned by T2529 in advance. Free kill available first: the norm must DECREASE in ν with ~59:1 amplitude spread; wrong direction ends the lane before precision is discussed. Nothing pushed.**
