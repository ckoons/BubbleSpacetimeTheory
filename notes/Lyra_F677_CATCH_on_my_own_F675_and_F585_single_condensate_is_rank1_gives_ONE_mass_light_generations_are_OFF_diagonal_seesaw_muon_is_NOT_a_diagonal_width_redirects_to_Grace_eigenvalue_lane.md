# F677 — CATCH on my own F675 (and a latent inconsistency in F585): a SINGLE condensate is rank-1, which gives ONE massive generation and TWO massless — so the muon mass is NOT a diagonal width. **F585 says both "M_ij = O_i O_j (rank-1 single condensate)" AND "diagonal M_ii sets the masses" — these are inconsistent, because a rank-1 3×3 matrix has eigenvalues {trace, 0, 0}: only the third (tau) generation gets mass from the single condensate; the diagonal entries are NOT the physical masses.** The light-generation masses (muon, electron) are lifted from zero by the **OFF-DIAGONAL** coupling via a Fritzsch/Gatto seesaw: m_light ≈ b²/a = (off-diagonal)²/(heavy diagonal). Verified: [[0, √(m_e m_μ)],[√(m_e m_μ), m_μ]] → eigenvalues {0.509, 106.2} ≈ {m_e, m_μ}, with the light mass emerging from the off-diagonal and M₁₁ = 0. **CORRECTED answer to Casey's off-diagonal-vs-diagonal question: the off-diagonal DOES feed the hierarchy — it GENERATES the light masses. The heavy (3rd) generation is the diagonal single-condensate entry; the light generations are off-diagonal seesaws.** This CHALLENGES my F675 (the muon is not a diagonal self-width) and REDIRECTS the muon computation to the off-diagonal overlap — Grace's eigenvalue lane. My F675 "two-point overlap to the condensate stratum" was groping toward the right object but MISLABELED it: the overlap ⟨ψ_μ | O(at k₀=0) | ψ_τ⟩ is an OFF-DIAGONAL element M_μτ (muon stratum to tau/condensate stratum), not a diagonal width. Owning the mislabel within the hour.

**Lyra, Fri 2026-07-24 08:26 EDT. Self-catch on F675/F585, owned promptly. The muon is off-diagonal, not diagonal — Grace's lane. And it's a real answer to Casey's question, not just a correction.**

## The catch (linear algebra, robust)
F585 committed to **M_ij = O_i O_j** — a single condensate O — which is a **rank-1** matrix. A rank-1 3×3 matrix has eigenvalues **{Tr(M), 0, 0}**: exactly ONE nonzero (≈ the heavy generation, m_τ) and two exact zeros. So:
- A single condensate gives the **third generation its mass (diagonal), and leaves the first two massless.** This is standard — a single Higgs VEV gives a rank-1 Yukawa.
- F585's other statement, "diagonal M_ii = |⟨ψ_i|O⟩|² sets the masses," is **inconsistent** with rank-1: for a rank-1 matrix the diagonal entries are NOT the eigenvalues. The physical masses are the eigenvalues {Tr, 0, 0}, not the diagonal {w_e, w_μ, w_τ}.
- **Therefore the muon mass is NOT a diagonal localization width** (F675's framing). My F675 diagonal-width object was the wrong object.

## The resolution (Fritzsch/Gatto seesaw — the light masses are off-diagonal)
Three separate masses require **off-diagonal** structure. In the nearest-neighbor (Fritzsch) texture, the light mass is lifted from zero by the off-diagonal via a seesaw:
$$ M_{2\times2} = \begin{pmatrix} 0 & b \\ b & a \end{pmatrix},\quad a = m_{\text{heavy}},\ b = V_{ij}\ \Rightarrow\ m_{\text{light}} \approx \frac{b^2}{a}. $$
Verified numerically: a = m_μ, b = √(m_e m_μ) → eigenvalues {0.509, 106.2} ≈ {m_e, m_μ}, and **m_e = b²/a emerges from the off-diagonal**, with M₁₁ = 0. The Gatto mixing θ ~ √(m_i/m_j) is the *same* off-diagonal b = √(m_i m_j) — one object, giving both the light mass AND the mixing.

## ★ CORRECTED answer to Casey's off-diagonal-vs-diagonal question (K864/K865)
- **Heavy (3rd) generation:** DIAGONAL — the single-condensate rank-1 entry (m_τ). This is the one mass the condensate gives directly.
- **Light generations (muon, electron):** OFF-DIAGONAL — lifted from zero by the V₁₂/V₂₃ couplings via seesaw m_light ≈ V²/m_heavy.
- **So the off-diagonal DOES feed the hierarchy — it GENERATES the light-generation masses.** (My first pass this morning said the opposite; that was wrong, corrected here.)
- This is exactly why Grace's eigenvalue lane (K-prompt computation 2) is the load-bearing one for the muon, and my diagonal-width lane (F675) is not.

## What this means for the muon computation (the redirect)
- **The muon mass ≈ V_μτ² / m_τ**, where V_μτ = ⟨ψ_μ | O | ψ_τ⟩ is the **off-diagonal overlap between the muon stratum (k₁=3/2) and the tau/condensate stratum (k₀=0)**. Numerically, m_μ = V_μτ²/m_τ ⟹ V_μτ = √(m_μ m_τ) = 433 MeV (geometric mean — a μ-τ inter-stratum overlap).
- **F675 was groping toward this** (its "two-point overlap to the condensate stratum") but mislabeled it a *diagonal* width. Correctly labeled, that overlap is the **off-diagonal** M_μτ. The muon lives in the off-diagonal, and the decisive question is unchanged in spirit but relabeled: **is the μ-τ inter-stratum overlap V_μτ forced by the Wallach geometry?**
- **Rank bound status:** V_μτ is a two-point overlap, not a residue — so F674's rank bound still doesn't directly forbid it. But the pre-registered bar (F675 W1–W5) carries over verbatim to the off-diagonal, especially W5 (the same machine must give the electron seesaw too: m_e ≈ V_eμ²/m_μ).

## Tiers (honest)
- **SOLID (linear algebra + standard flavor):** single rank-1 condensate → one mass; light masses require off-diagonal; seesaw m_light = V²/m_heavy is the mechanism. Robust.
- **CANDIDATE:** the specific Fritzsch nearest-neighbor texture (which entries zero) and V_μτ = √(m_μ m_τ). One ansatz among several — not established; the geometry must pick the texture.
- **REDIRECT:** muon computation → off-diagonal overlap (Grace's eigenvalue lane), not my diagonal width (F675 superseded on the labeling, its decisive-question + W1–W5 discipline preserved).

## Tiers / handoffs
- **@Grace** — your eigenvalue lane is the load-bearing one, and here's the catch that makes it so: F585's single condensate is **rank-1** → {Tr, 0, 0}, only tau massive. If you build the pure M_ij = O_iO_j you'll get one mass + two zeros — you need the Fritzsch-type off-diagonal (nearest-neighbor, M_heavy diagonal + V off-diagonal), and your λ± = ½(λ₁+λ₂) ± √(¼(λ₁−λ₂)² + |v|²) IS the seesaw. **The light masses come from |v|²/Δ — the off-diagonal.** So computation (1) answers Casey: off-diagonal generates the hierarchy of the light generations. The muon is m_μ ≈ V_μτ²/m_τ with V_μτ the μ-τ inter-stratum overlap. Your branching computation (2) — if the D_IV⁵↓D_IV³ reduction coefficient 3/10 = N_c/(2n_C) is the off-diagonal V normalization, that's the forced-vs-fit test for V_μτ (my W1/W2 applied to the off-diagonal). Hold 3/10 as a CANDIDATE until the branching FORCES it (target-innocence — it's Casey's coefficient, so target-aware).
- **@Elie** — the cross-check just sharpened: the muon is OFF-diagonal (seesaw), not a diagonal width. Your blind criterion should be on V_ij (the inter-stratum overlaps) and the seesaw eigenvalues, with W5 the killer (one machine gives muon m_μ=V_μτ²/m_τ AND electron m_e=V_eμ²/m_μ). QCD mass-gap still your parallel front.
- **@Keeper** — self-catch on F675 (mislabeled the muon as a diagonal width) and a latent inconsistency in F585 (rank-1 texture vs "diagonal sets masses" — can't both hold). Corrected: single condensate = rank-1 = one mass; light generations = off-diagonal seesaw. This ANSWERS Casey's off-diagonal question (off-diagonal generates the light hierarchy) and redirects the muon to Grace's eigenvalue lane. F675's decisive-question + pre-registered W1–W5 carry over to the off-diagonal intact. Log the correction; the diagonal-width framing is superseded, the discipline around it is not.
- **@Casey** — a catch on my own morning setup, owned right away, and it turns into a real answer to your off-diagonal question rather than just a fix. Here it is: a single condensate is a rank-one object, and a rank-one mass matrix gives exactly one massive generation and two massless. So the single Higgs-like condensate gives the *tau* its mass, on the diagonal — and the muon and electron would be massless if that were the whole story. They get their masses from the *off-diagonal* couplings, through a seesaw: the light mass is (off-diagonal)² over the heavy mass. So your instinct that the hierarchy is an off-diagonal question was right, and more specifically: the off-diagonal is what *generates* the light generations. My F675 "muon = diagonal width" had the wrong label — the muon is the overlap between its own stratum and the tau/condensate stratum, which is an off-diagonal element, and its mass is that overlap squared over the tau mass. That's Grace's eigenvalue lane, not my diagonal one, so the two attacks converge on hers. The decisive question is the same one, just moved to the off-diagonal: is that muon-tau inter-stratum overlap *forced* by the Wallach geometry, or is it a free scale? Same raised bar, same blind criteria — and the killer test is that one machine has to give the muon and the electron seesaws both. I'd rather flag the mislabel an hour in than compute the wrong object all morning.

Notes only; no toys/theorems claimed. CATCH: single condensate = rank-1 → {Tr,0,0} = ONE mass (tau, diagonal); F585 "diagonal sets masses" inconsistent with rank-1. Light generations = OFF-diagonal seesaw m_light=V²/m_heavy (verified). Answers Casey: off-diagonal GENERATES light hierarchy. F675 muon-as-diagonal-width MISLABELED → muon is off-diagonal overlap M_μτ (μ-stratum to τ/condensate-stratum), m_μ≈V_μτ²/m_τ, V_μτ=√(m_μm_τ). Redirects to Grace's eigenvalue lane; F675 decisive-question + W1–W5 preserved. 3/10=N_c/(2n_C) = CANDIDATE until branching forces it. — Lyra
