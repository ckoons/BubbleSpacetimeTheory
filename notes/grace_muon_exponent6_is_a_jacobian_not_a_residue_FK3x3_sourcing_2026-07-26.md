# Muon exponent-6: the corpus has a JACOBIAN reading (not a residue) — evades K864, beats FF-20, consistent with Elie's diagonal-cancellation. But the per-coordinate factor is unexecuted book territory.

*Grace | 2026-07-26 Sun | team_prompt -b, my sourcing lane: does the BF-zero residue / FK Wyler 3×3 produce the muon's π² and exponent-6? Keeper pre-committed a 3-part win-condition and flagged FF-20 on the '6', leaning negative. Sourcing turned up a stronger route than the generic copy-count — surfacing it honestly, holding the line on what's unexecuted.*

## The residue-order route is dead — confirmed
K864 rank bound (BACKLOG N-muon-diagonal, REFUTED): rank-r Gindikin Γ has r factors → exponent 6 is **impossible from rank-2 residues**. ~8 diagonal reframes over 2 days, one dead route. So "6 = pole order" is out.

## ★ The corpus has a JACOBIAN reading — a different object than a residue
BST_Arithmetic_Algebra_Spacetime §9.1: the three generations = the **odd sub-domain tower** D_IV^{1,3,5}, and
> **m_μ/m_e = (24/π²)⁶ = the VOLUME JACOBIAN of the embedding D_IV¹ ↪ D_IV³ — a determinant.**

| domain | lepton | dim_ℝ | factor |
|---|---|---|---|
| D_IV¹ | e | 2 | 1 (ref) |
| D_IV³ | μ | **6** | (24/π²)⁶ |
| D_IV⁵ | τ | 10 | 3483.8 |

So **exponent-6 = dim_ℝ(D_IV³)** and (24/π²)⁶ is a **determinant** with one (24/π²) factor per real dimension.

## Why this is stronger than the generic "copy-count" (and beats the two objections)
- **Evades K864.** A volume Jacobian/determinant is NOT a residue — its exponent is the **dimension** (6), which is *not* rank-bounded. The rank-2 refutation kills the residue route, not the Jacobian route. This is the real opening Keeper sensed (the "different mechanism").
- **Beats FF-20 on the '6'.** The coincidence-trap is real — 6 reads as dim SO(4), C₂, n_C+1, N_c!, 2N_c (≥5 loose readings). But the Jacobian reading pins it *specifically* to **dim_ℝ(D_IV³)** — the muon's own domain dimension — not one of five loose labels. A specific forced reading, not a match.
- **Consistent with Elie's diagonal result.** Elie found the one-domain Gram-diagonal ratio cancels the √π (Γ(3/2)/Γ(5/2)=2/3, π-free). That's *correct and expected* — because the muon's π² lives in the **embedding Jacobian** (a different object), NOT the single-domain diagonal ratio. The two results reconcile: diagonal-ratio π-free (Elie) + Jacobian-determinant π-carrying (this note). The "BF-zero residue / FK 3×3" Elie pointed to is plausibly **this embedding Jacobian**.

## The win-condition, checked honestly (Keeper's 3 parts)
1. **π² per copy** — structurally ✓: each of the 6 real dimensions carries one (24/π²) factor, and 24/π² has π². But **WHY 24/π² per dimension** is the per-coordinate Bergman-metric Jacobian — **unexecuted**.
2. **6 relabels as a forced count** — ✓ as **dim_ℝ(D_IV³)** (beats FF-20). *Caveat:* this requires the generation→domain assignment (e→D_IV¹, μ→D_IV³, τ→D_IV⁵) to be **independently forced**, not chosen to fit — that target-innocence check is open (is the odd-tower assignment forced by the Wallach strata / ρ-tower, or assigned?).
3. **per-copy amplitude 64** — does **not** map onto this form: the corpus factor is 24 = 4! = Γ(5) per dimension, not 64. Keeper's "64" (=2⁶=2^C₂) targets a *different* decomposition; the Jacobian reading has per-dimension **24=Γ(5)** — and that same Γ(5)=24 is exactly the quark-side integer-position value. **Flag the discrepancy** rather than force it.

## Honest verdict (my sourcing; discipline held, FK metric NOT fabricated)
- The muon exponent-6 route is a **Jacobian/determinant**, not a residue — which is a genuinely **live, sharper** route than the pessimistic "generic copy-count" lean: it evades K864 (non-residue), beats FF-20 (dim_ℝ D_IV³ specific), and reconciles with Elie's diagonal π-cancellation.
- **But it is NOT derived.** Two unexecuted gates: (a) the per-coordinate factor 24/π² — the FK/Bergman-metric Jacobian, genuine **book territory, I will not fabricate it**; (b) target-innocence of the generation→domain assignment (forced by the tower, or fit?).
- **(24/π²)⁶ stays identified-coincidence**, muon VALUE open (count 2) — but the *route* is upgraded from "likely dead" to "live Jacobian, two clean gates." My honest lean is less pessimistic than Keeper's: this evades the two objections that sink the other readings, so it deserves the FK-Jacobian computation before we call it a coincidence. If that computation produces 24/π² per dimension with the assignment forced → the muon derives; if the per-coordinate factor needs fitting → coincidence, and we say so.
- **Partition theorem untouched** — muon is bucket-2 either way.

## For the team
- **Elie:** the π² isn't in the one-domain diagonal (you're right) — it's plausibly the **embedding Jacobian** D_IV¹↪D_IV³ (a determinant over 6 dims). If your harness can do the volume Jacobian of the nested embedding, that's the decider object, distinct from the diagonal.
- **Keeper:** the route is a Jacobian, not a residue → it evades your K864 concern and beats your FF-20 on the '6' (dim_ℝ D_IV³ specific). I'd soften "comes back negative more often" to "two clean gates, one of them a real book computation." The '64' win-condition doesn't fit this form (per-dim is 24=Γ(5)); flag which decomposition you meant.

— Grace, 2026-07-26. Muon exponent-6 sourcing: residue route DEAD (K864 rank bound). Corpus has a JACOBIAN reading (BST_Arithmetic_Algebra §9.1): (24/π²)⁶ = volume Jacobian of D_IV¹↪D_IV³, exponent-6 = dim_ℝ(D_IV³)=6, one (24/π²) per real dimension. Stronger than generic copy-count: evades K864 (determinant, exponent=dimension not rank-bounded), beats FF-20 (dim_ℝ D_IV³ specific not a loose '6'), reconciles Elie's diagonal π-cancellation (π² is the EMBEDDING Jacobian, not the diagonal ratio). NOT derived: gates = (a) per-coordinate 24/π² Bergman-metric Jacobian [book territory, won't fabricate], (b) target-innocence of generation→domain assignment. '64' win-condition doesn't map (per-dim=24=Γ(5)=quark-side value). Muon VALUE open (count 2), (24/π²)⁶ identified-coincidence, route upgraded to live-Jacobian-two-gates. Theorem untouched (bucket-2).
