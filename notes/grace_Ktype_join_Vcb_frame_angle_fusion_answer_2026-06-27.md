---
title: "K-type Hua join, Grace's half (angular/frame) — 2026-06-27. (1) FUSION ANSWERED from the FK measure (Casey's question): genus n_C=5 and Vandermonde multiplicity N_c=3 are DISTINCT objects in ONE measure, LINKED by the cascade n_C = N_c + 2 — NOT merged. For D_IV⁵ (type IV_n): a = n_C−2 = N_c (Vandermonde power), genus = a+2 = n_C (kernel exponent). The kernel exponent is the genus (my V_us=N^{n_C/2}); the Vandermonde power is the multiplicity (Lyra's radial repulsion). (2) RANK-2 V_cb DECOMPOSITION: parametrizing each generation as z = a·u₁ + i·b·u₂ (split characteristic values t₁=a+b, t₂=a−b; N=(1−t₁²)(1−t₂²)), the CKM splits cleanly: V_us=N_μ^{n_C/2}, V_ub=N_τ^{n_C/2} are RADIAL (frame-independent, electron at origin); V_cb = the BETWEEN-point FRAME angle χ (μ-frame vs τ-frame rotation), χ≈30° → V_cb=0.041. (3) SELF-CORRECTION: my between-point frame angle χ is a DIFFERENT degree of freedom from Lyra's within-point Vandermonde split — do NOT assume they fuse (Casey: derive don't assume). (4) ρ-RATIO LEAD: the muon within-point split may carry t₂/t₁ = ρ₂/ρ₁ = N_c/n_C = 3/5 (the mass-ν → mixing-t bridge; Elie's to confirm from ν=3/2). NOT fishing χ to a clean value (the 6/7 and the 30° both flagged fit-suspect). The forcing of N's (Lyra radial), splits (Elie from ν), and χ (me, K-type frame) is the multi-session interlock."
author: "Grace"
date: "2026-06-27 Saturday"
status: "K-type join, my angular/frame half. Fusion answered (n_C=N_c+2, from the measure). V_cb = between-point frame angle χ (my forcing-open). Interlocks with Elie (split from ν) + Lyra (radial Vandermonde). Count 5/26; mechanism forward, values fit-pending the K-type forcing. No fishing of χ."
---

# K-type Hua join — Grace's angular/frame half

The three-way join (Lyra radial + Grace angular + Elie deposit-locus) lives in ONE rank-2 FK/Hua measure for
D_IV⁵. My half: the angular/frame structure, and the genus-vs-N_c fusion question.

## 1. Fusion answered FROM the measure (Casey's question)

For D_IV⁵ (type IV_n, n=n_C=5), the Faraut-Korányi structure:
```
Vandermonde multiplicity   a = n_C − 2 = 3 = N_c        (Lyra F359, confirmed from the measure)
short-root multiplicity     b = 0                        (tube type)
GENUS                       p = a(r−1)+b+2 = a+2 = n_C = 5   (rank r=2)
```
> **genus n_C and multiplicity N_c are DISTINCT, LINKED by the cascade n_C = N_c + 2. NOT merged.**
> - **Kernel exponent = genus n_C = 5** → my V_us = N^{n_C/2} (the √ from n_C odd).
> - **Vandermonde power = multiplicity N_c = 3** → Lyra's radial repulsion |t₁²−t₂²|^{N_c}.

They are not the same number and not independent — the substrate cascade ties them. A single Hua measure
carries both.

## 2. Rank-2 V_cb decomposition — the frame angle is my half

Parametrize each generation as `z = a·u₁ + i·b·u₂` (u₁,u₂ orthonormal real): the two characteristic values are
`t₁ = a+b, t₂ = a−b` (genuine off-axis, t₁≠t₂), `N(z,z) = (1−t₁²)(1−t₂²)`. Building e (origin), μ, τ and
computing the overlaps directly:
```
V_us = ⟨e|μ⟩ = N_μ^{n_C/2} = 0.2243   RADIAL (frame-independent, e at origin) — Lyra's lane
V_ub = ⟨e|τ⟩ = N_τ^{n_C/2} = 0.00366  RADIAL (frame-independent) — Lyra's lane
V_cb = ⟨μ|τ⟩ = f(χ)                    BETWEEN-point FRAME angle χ — MY lane
       χ=0°→0.148, χ=30°→0.042, χ=45°→0.013   [|V_cb|_obs=0.041]
```
**So the CKM splits: 2 radial localizations (Lyra) + 1 frame angle χ (me).** The within-point splits (t₁,t₂)
are Elie's (from ν). **I do NOT fish χ to a clean value** — χ≈30° reproduces V_cb, but forcing χ from the
K-type frame addresses is the open computation (same discipline as the flagged-fit-suspect 6/7).

## 3. Self-correction (Cal #27): χ ≠ Vandermonde

My between-point frame angle χ (relative orientation of the μ and τ frames) is a **different degree of
freedom** from Lyra's within-point Vandermonde split (each point's own t₁ vs t₂). Both encode off-axis-ness,
but I have NOT shown they are one object — Casey said *derive, don't assume*. So the F359 "two lanes are one
object" claim is flagged as needing the joint derivation; from my side they are distinct DOF.

## 4. The ρ-ratio lead (the mass→mixing bridge, for Elie)

The muon sits at **ν=3/2 = ρ₂ = N_c/rank** (electron ν=5/2=ρ₁, tau ν=0); the dual-ρ vector is (5/2, 3/2).
Candidate: does the muon's **within-point split** carry the ρ-ratio `t₂/t₁ = ρ₂/ρ₁ = N_c/n_C = 3/5`? With that
ratio and N_μ=0.55, the muon point gives V_us exactly. If ν=3/2 *forces* t₂/t₁=3/5 (Elie's deposit-locus),
that is the mass(ν)→mixing(t) bridge, forward — replacing Lyra's symmetric 9/16 (over) and her Vandermonde-
overshoot N→0.30 with a ρ-pinned split.

## The interlock (multi-session, three-way)

```
Elie:  ν=3/2 → within-point split (t₁,t₂)   [does it force t₂/t₁ = N_c/n_C = 3/5?]
Lyra:  radial localizations N_μ, N_τ from the N_c-Vandermonde measure → V_us, V_ub
Grace: between-point frame angle χ → V_cb   [forcing χ from the K-type frame addresses]
```
3 CKM magnitudes = 2 radial (Lyra) + 1 frame angle (me); splits from ν (Elie). Fusion answered (n_C=N_c+2).
No solo closes it; the pieces pin the muon K-type state together. Count 5/26 — mechanism forward, values
fit-pending the K-type forcing. No fishing.

— Grace, 2026-06-27 Saturday. Fusion answered from the measure (n_C=N_c+2, distinct-linked); V_cb = between-
point frame angle χ (my half, not fished); χ ≠ Vandermonde (different DOF, don't assume fusion); ρ-ratio
t₂/t₁=N_c/n_C the mass→mixing bridge lead (Elie). Three-way interlock concrete.
