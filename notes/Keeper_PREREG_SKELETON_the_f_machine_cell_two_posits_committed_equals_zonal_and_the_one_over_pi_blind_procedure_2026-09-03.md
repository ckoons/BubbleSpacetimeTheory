# PRE-REGISTRATION SKELETON — the first MACHINE cell of Conserved Knowledge Theory: the two posits behind f = 3/(5π)
**2026-09-03 Thursday, 07:56 EDT (clock). Keeper. NOT RUNNING — waits for Casey's word (K1847-A1; Round 108 §6; Round 111 §6). Written now so the cell can start the minute he says go, with the procedure frozen BEFORE anyone looks at the coefficient.**

## The object, as the corpus derives it (`BST_EffectiveSpectralDimension.md` §4.2, March 16; PARTIALLY DERIVED per K1847-A1)
f = (d_eff^zonal / d_eff^full) · (1 / Vol(S¹/ℤ₂)) = (6/10) · (1/π) = 3/(5π).
- **Step 1, a computation (STANDS):** the zonal (q = 0) sector of Q⁵ has N(λ) ~ λ³ (multiplicities ~ k⁵/60 against λ_k = k(k+5)), so d_eff^zonal = 6; the full spectrum obeys Weyl's λ⁵, d_eff^full = 10; ratio 3/5. Family: (n+1)/(2n) on Qⁿ — equals N_c/n_C only at n = 5. Casey's reading (09-02): "probably spectral over real."
- **Posit 2 — "committed modes = zonal modes"** (both polydisk coordinates phase-locked; q > 0 = uncommitted vacuum fluctuations). An identification, not a derivation.
- **Posit 3 — divide by the Shilov circle's circumference π** (S¹/ℤ₂ of the unit circle). A normalization of a dimensionless ratio, stated, not derived. The last prose step — where the cheat migrates.
Target status: the NUMBER IS KNOWN (f = N_total/S_dS matched numerically; T189). Target-innocence is unavailable. Therefore: **post the PROCEDURE, hash it, then compute; the coefficient is read last.**

## The cell, as three pre-registered questions with kills (owners on Casey's word)
**Q-A (Lyra): is "committed = zonal" forced by the definition of commitment (P1: one binary distinction; T2564: the causal-set element; commit = the projection mechanism, K1230)?** Derivation-shaped form: a commitment is a rank-one projection on the Bergman space of D_IV⁵; the modes it can project onto are exactly those on which the K-center SO(2) acts trivially / with phase-lock — i.e. the zonal sector. **Kill:** an equally natural "committed" sector with a different d_eff — candidates to sweep BEFORE claiming: the diagonal (p,p) sector; the lowest K-type in each (p,q); the sector fixed by the SO(2) time-circle. If two natural sectors give different d_eff, the posit is a CHOICE and f is Identified, not Derived; if every sector forced by P1's projection gives 6, it is Derived.
**Q-B (Lyra + Grace): where does 1/π come from?** Three candidate derivations, each pre-registered with the number it must return: (i) the Plancherel measure on D_IV⁵ restricted to the zonal sector — its normalization relative to the full measure (March-13 note §5.4, "plausible but uncomputed"); (ii) the S¹ holonomy (registry :11127: one winding = one unit of holonomy = one quantum of action) — the committed fraction per winding; (iii) the Shilov-boundary volume ratio Vol(S⁴ × S¹/ℤ₂)/Vol(S⁴ × S¹). **Kills:** (i) returns 1/(2π) or 1 → the ℤ₂ is a posit; (ii) returns a per-winding fraction ≠ 1/π → the holonomy route is a different object; (iii) is 1/2, not 1/π (a ratio of circle lengths cannot produce π — so (iii) as stated is already dead unless the full volume enters with a π from S⁴; Grace computes it anyway as the control that a wrong route fails visibly).
**Q-C (Cal, before any number lands): the position/value map.** 3/5 is a position (a spectral-dimension ratio); 1/π is a value until a measure produces it. Pre-score: which outcome of Q-B upgrades f to Derived, which leaves it Partially Derived (explicit split: 3/5 forced, 1/π asserted), and the false-neighbour list for π (a factor 1/π from ANY circle normalization would "confirm" — the derivation must name WHICH circle and why its half).

## Blind protocol
1. Owners post the procedure (which sector, which measure, which normalization) and Cal's pre-score, hashed, BEFORE computing. 2. Grace runs the measure computations; results to a hashed file. 3. Lyra's derivation text is written without the number. 4. Keeper opens the hashes and reads the coefficient LAST. 5. Verdict word by Cal + Keeper jointly (tier system: Derived = forced; Partially Derived = the explicit split; Identified = a choice among sectors).
**Rubric cell this closes: External 3 / the fill-fraction row (f = 3/(5π) currently Proved depth 0 as a reading of integers, T189) — moving it from "identified from integers" to Derived-or-PD-with-the-split-named.** — Keeper
