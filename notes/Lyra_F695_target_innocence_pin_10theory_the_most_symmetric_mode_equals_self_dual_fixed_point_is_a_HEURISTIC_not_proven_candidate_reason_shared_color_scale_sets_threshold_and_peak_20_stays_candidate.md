# F695 — Task #10-theory: the target-innocence pin ("most-symmetric confined mode = self-dual fixed point"). **Honest verdict: NATURAL, with a real structural candidate, but NOT established — so the down-quark 20 stays CANDIDATE, and I do NOT assert the pin (which would be exactly the target-aware trap Grace flagged).** The verdict-mover rides on an *independent* reason the ν=N_c ground-ladder rungs sit at the c-function's self-dual point (c(−iρ)=1, where the D₃-ratio = 1 → exactly 20); without it, placing them there to get 20 is target-aware. **The genuine candidate reason:** the confined-quark threshold rung ν = N_c = k_min (color-forced, L1–L3) and the c-function self-dual peak D₃(0) = N_c (the D₃ = SU(N_c) Dirichlet kernel; peak = the short-root Weyl-dimension) are **set by the SAME object — the short-root/color structure a = N_c (K865/T2511)** — so the rung and the self-dual peak share the color scale, *not independently placed to hit 20*. That is real and target-innocent as far as it goes. **But it is necessary, not sufficient:** both being N_c shows the coordinates share the color scale; it does *not* prove the rung lands *at* the fixed point x=0. The rigorous pin needs the load-bearing claim *the discrete-series ground ladder is invariant under the boundary↔bulk (λ→−λ) duality, hence at the fixed point λ=0* — and that is a **heuristic, not a theorem**: a lowest-weight ground vector sits at a chamber corner, not the Weyl-center, so "most symmetric = self-dual center" needs proof, not assertion. **⟹ The target-innocence gate is OPEN. It reduces to whether Grace's map (ν=N_c, ℓ={1,3,5}) → x lands the d/s rungs at (or symmetric about) x=0 from the c-function structure ALONE (target-innocent), or only if tuned. 20 stays candidate; the pin is a promising candidate, not a bank.**

**Lyra, Sat 2026-07-25 12:49 EDT. #10-theory: worked the target-innocence pin at maximum scrutiny (peak convergence + RH allure). Found a genuine structural candidate (shared color scale sets both the threshold and the self-dual peak), and honestly flagged the gap that keeps it from being a proof. The 20 stays candidate — I refuse to assert the pin to move the verdict.**

## The gate (Grace's crux, restated precisely)
The self-dual reframe (F694) makes R_i = 1 → exactly 20 *at* the self-dual point. But that is only a *derivation* if there is an **independent, target-innocent reason** the ν=N_c ground-ladder rungs sit at that point. If we place them there *because* it gives 20, it is target-aware — a dressed-up fit. So the whole verdict rides on: **is the confined ground ladder at the self-dual point for a reason that doesn't reference the mass 20?**

## The candidate reason (real, as far as it goes)
Two target-innocent facts, both = N_c = 3:
- **(A)** the confined quark rung **ν = N_c = k_min** — the Wallach threshold, color-forced by L1–L3 (colored → interior → ground rung). No reference to masses.
- **(B)** the c-function self-dual **peak D₃(0) = N_c** — the D₃ = SU(N_c) Dirichlet kernel's peak is the short-root Weyl-dimension. No reference to masses (Gindikin–Karpelevich, B₂ multiplicities (3,1)).

**Both are set by the SAME structure — the short-root/color datum a = N_c (K865/T2511).** So the quark's threshold rung and the c-function's self-dual peak are the *same color scale*, not two independent placements tuned to coincide at 20. **This is a genuine target-innocent candidate for the pin** — the color mechanism that placed the quark at ν=N_c is the *same* mechanism that sets the self-dual peak at N_c.

## Why it is NOT yet established (the discipline — necessary ≠ sufficient)
- **Both being N_c is necessary, not sufficient.** That the threshold and the peak share the value N_c shows the *coordinates share the color scale*; it does **not** prove the rung sits *at* the fixed point x = 0 (the ratio D₃(x_d)/D₃(x_s) = 1 needs the rungs *at or symmetric about* the center, a stronger statement than "the center is at N_c").
- **The load-bearing step is a heuristic.** The rigorous pin is: *the discrete-series ground ladder is invariant under the boundary↔bulk (λ→−λ) duality, so it sits at the fixed point λ = 0.* But a **lowest-weight ground vector sits at a chamber corner of the spectral parameter, not the Weyl-center** — so "most symmetric confined mode = self-dual center" is a *natural heuristic*, not a proven theorem. I cannot assert it.
- **The genuinely independent test is Grace's map.** (ν=N_c, ℓ={1,3,5}) → x in the D₃ argument: does the c-function structure *alone* land the d/s rungs at/symmetric-about x = 0 (target-innocent), or only under a tuned map? That computation — not the elegance — decides the target-innocence.

## Verdict (held at candidate, peak-convergence discipline)
- **The pin is NATURAL and has a real structural candidate** (color sets both the threshold and the self-dual peak — same a = N_c).
- **It is NOT established** — the "ground-ladder-is-self-dual" step is a heuristic, and shared-N_c is necessary-not-sufficient.
- **⟹ m_s/m_d = 20 stays CANDIDATE.** The target-innocence gate is OPEN, reducing to Grace's color-forced-vs-tuned map.
- **I do NOT assert the pin to move the verdict** — that is precisely the target-aware trap (place at c=1 because it gives 20). At peak convergence, with the RH allure making it maximally tempting, the honest move is: name the candidate, name the gap, keep it candidate.
- **What would close it (for whoever takes it next):** prove the ground ladder is λ→−λ self-dual (or that the color-forced ν=N_c threshold *is* the c-function self-dual center, not merely equal in value) — a rep-theory statement, in linear algebra on the one domain, target-innocent.

## Tiers / handoffs
- **@Keeper (#10-theory)** — target-innocence pin worked: NATURAL with a real candidate (the threshold ν=N_c and the self-dual peak D₃(0)=N_c are *both* the short-root color datum a=N_c — same structure, not tuned to coincide), but NOT established (shared-N_c is necessary-not-sufficient; the ground-ladder-self-dual step is a heuristic — a lowest-weight vector is at a chamber corner, not the Weyl-center). **20 stays CANDIDATE; the gate is OPEN, on Grace's map being color-forced not fitted.** I refuse to assert the pin — asserting it to get 20 is the target-aware trap. Log as candidate-with-a-real-lead, not closed.
- **@Grace/@Elie (#10)** — the target-innocence reduces to your map: does (ν=N_c, ℓ={1,3,5}) → x land the d/s rungs at/symmetric-about x=0 from the c-function structure ALONE? My theory found the candidate reason (shared color scale a=N_c sets both the threshold and the peak) but it's necessary-not-sufficient — your map is the sufficiency test. If it lands them there color-forced (not tuned), the pin closes and 20 derives; if only under a tuned map, it's target-aware and stays candidate.
- **@Casey** — worked the target-innocence pin, which is the crux Grace rightly flagged: for the 20 to be *derived* rather than *fitted*, we need a reason the quarks sit at the self-dual point that doesn't secretly assume the answer. I found a real one, and it's your color mechanism again: the same color number that forces the quark onto its rung (3) is the number that sits at the peak of the c-function (3) — they're the same short-root structure, not two things tuned to line up. That's genuinely target-innocent as far as it goes. But I have to be straight that it's not yet a proof: "they're both 3" shows the coordinates share the color scale, not that the rung lands exactly on the fixed point — and the clean symmetry story ("the most symmetric state sits at the symmetric point") is a heuristic, because the ground state actually lives at a corner, not the center. So I'm holding the 20 at candidate and NOT asserting the pin — because asserting it *because* it gives 20 is exactly the trap we've spent the week not falling into, and it's most tempting right here where it's elegant and touches RH. The honest gate is Grace's map: does the geometry put the rungs at the self-dual point on its own, or only if we push them there? That decides it, and it's a computation, not a story.

Notes only; no toys/theorems claimed. #10-theory target-innocence pin: NATURAL + real candidate (threshold ν=N_c and self-dual peak D₃(0)=N_c BOTH the short-root color datum a=N_c, K865/T2511 — same structure, not tuned) but NOT established (shared-N_c necessary-not-sufficient; ground-ladder-self-dual is a HEURISTIC — lowest-weight vector at chamber corner ≠ Weyl-center). ⟹ 20 stays CANDIDATE; gate OPEN on Grace's map (color-forced vs tuned). Do NOT assert the pin (target-aware trap; max at peak-convergence+RH allure). Closes iff ground ladder proven λ→−λ self-dual / threshold ν=N_c IS the self-dual center. Capstone untouched. — Lyra
