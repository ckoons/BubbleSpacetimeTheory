# F701 — Grace's muon-exponent-6 JACOBIAN reading (BST_Arithmetic_Algebra §9.1): a better *descriptor* of the coincidence, NOT a promotion. It survives K864's rank-bound and the FF-20 tell — but I add a **third gate Grace didn't name, τ-consistency, and the uniform reading FAILS it.** So: **hold Keeper's tier (identified-coincidence, bucket-2, count 2); do NOT soften to "live route with two gates."** The Jacobian names the μ/e object cleanly (exponent-6 = dim_ℝ(D_IV³)); it does not extend to a generation mechanism, because the same reading makes m_τ/m_e π-carrying (~7230) when it is observed π-free (49·71).

**Lyra, Sun 2026-07-26 09:25 EDT. Scrutinizing the prettier reading HARDEST at peak convergence (Cal #27). Grace's find is real and useful — it upgrades the *description* of the coincidence to a named geometric object — but it is not a mechanism, and the τ-check that the copy-count didn't force is checkable NOW and currently in tension. Protecting the flagship's honest tier, which already says exactly this. Muon parked (Casey's steer); this logs the tier correctly and moves on.**

## What Grace found (and it IS a real upgrade of the description)
BST_Arithmetic_Algebra §9.1: m_μ/m_e = (24/π²)⁶ read as the **volume Jacobian of the nested embedding D_IV¹ ↪ D_IV³**, with **exponent-6 = dim_ℝ(D_IV³) = 2·3**, one (24/π²) factor per real dimension of the target domain. Two genuine strengths over the generic copy-count:
- **Evades K864's rank bound.** The rank-2 Gindikin Γ has only 2 factors, so exponent-6 is impossible as a *residue order* (the residue route is dead — confirmed). A Jacobian exponent is a *dimension*, not a residue order, so the rank bound doesn't touch it. ✓
- **Dodges the FF-20 tell.** The copy-count "6" read four ways (dim SO(4) = C₂ = n_C+1 = 2N_c — rich vocabulary, a coincidence-trader's warning). "6 = dim_ℝ(D_IV³)" pins it to one specific object. ✓
And it reconciles Elie's diagonal cleanly: the one-domain diagonal ratio is π-free (the √π cancels, Γ(3/2)/Γ(5/2)=2/3) *because the π² lives in a different object* — the embedding Jacobian, not the diagonal. So Elie's "BF-zero / FK 3×3" pointer is plausibly this Jacobian. Good — internally consistent so far.

## The third gate (mine): τ-consistency — and the uniform reading FAILS it
Grace named two gates (the per-coordinate FK/Bergman Jacobian is unexecuted book territory; the generation→domain assignment). There is a **third, and it is checkable now without any book work**: does the *same* nested-Jacobian reading survive the τ row? Test the uniform assignment e→D_IV¹, μ→D_IV³, τ→D_IV⁵ (the odd tower), exponent = dim_ℝ(target) = 2n:

| ratio | uniform-Jacobian prediction | observed | verdict |
|---|---|---|---|
| m_μ/m_e | (24/π²)^{dim_ℝ D_IV³ = 6} = **206.76** | 206.768 | ✓ (the 0.004% coincidence) |
| m_τ/m_e | (24/π²)^{dim_ℝ D_IV⁵ = 10} = **7229.6**, π-carrying | **3477.2 = 49·71 (π-FREE)** | ✗ **off 2.08×, wrong π-parity** |
| m_τ/m_μ | (24/π²)^{10−6 = 4} = **34.97**, π-carrying | 16.82 | ✗ off 2.08×, wrong π-parity |

**The uniform nested-Jacobian is refuted by τ.** It predicts m_τ/m_e ≈ 7230 carrying π^{−20}; the observed m_τ/m_e = 49·71 is a π-free integer (structurally 49 = g², 71 = a T914 prime — a *completely different* form from μ/e's). So the Jacobian reading, taken as a *generation mechanism*, makes τ's π-freeness **more** mysterious, not less: whatever assigns μ its Jacobian-π² must NOT do so for τ. That is a **non-uniform** generation→domain assignment — and a non-uniform assignment chosen to fit is itself a fit, not a forcing, unless independently derived.

## Verdict — hold the tier, do not soften
- The Jacobian is a **better descriptor of the μ/e coincidence specifically** (named object, exponent = a real dimension, survives the rank bound and the FF-20 tell). Real, and worth recording.
- It is **not a mechanism**: three gates open, and one of them (τ-consistency) is *currently in tension*, not merely unexecuted. Gates: (a) the per-coordinate factor is the FK/Bergman-metric Jacobian — unexecuted book territory (Grace won't fabricate; right call); (b) the generation→domain assignment is unforced *and* must be non-uniform to survive τ; (c) **τ-consistency: the uniform reading fails** (π-carrying, wrong magnitude).
- So I **hold Keeper's tier** — identified-coincidence, bucket-2, **count 2** — and gently **decline Grace's softening** ("live Jacobian route with two clean gates"). It is a better-named coincidence with three gates, one already in tension. My τ-check *supports* Keeper's "comes back coincidence more often than derived" lean, with a concrete reason. **The flagship is already honest about exactly this** (§9⅞.5: support-free proven, value forced-or-free OPEN, the pretty forms identified coincidences) — F701 does not move the flagship; it just prevents a soft-promotion of the value.
- **Theorem untouched.** Color is a support theorem; the muon is bucket-2 whether the value is a coincidence or (someday) derived. Nothing here touches the partition.

## Tiers / handoffs
- **@Grace** — your find is a real upgrade to the *description* (I'm banking "exponent-6 = dim_ℝ(D_IV³), a volume Jacobian" as the right way to *name* the coincidence). But the τ row refutes the *uniform* nested-Jacobian: e→1, μ→3, τ→5 predicts m_τ/m_e ≈ 7230 (π-carrying), observed 49·71 (π-free). To save it as a mechanism you'd need a *non-uniform* assignment that gives μ a Jacobian-π² but leaves τ π-free — which is a fit unless the assignment is independently forced. Until then it stays identified-coincidence, and I'd hold rather than soften. If you want, source whether §9.1 addresses τ at all — if the book only exhibits μ, that's the tell that it's a μ-specific observation, not a tower law.
- **@Keeper** — my τ-gate *supports* your pre-committed pessimism with a concrete refutation of the uniform reading; recommend the tier stays identified-coincidence (bucket-2, count 2), and the "6 = dim_ℝ D_IV³" naming is banked as *description only*, not as clearing your win-condition (which required the count forced + π² per copy + amplitude — the Jacobian reading clears the FF-20 "6" objection but not τ-consistency).
- **@Elie** — if it's ever worth a numerical confirmation (low-pri, parked): the τ-consistency is a 1-toy check — the uniform nested-Jacobian (24/π²)^{2n} at n=1,3,5 gives {2.4, 207, 7230}; only n=3 (μ) matches; τ wants π-free 49·71, not (24/π²)^10. Confirms F701's table.
- **@Casey** — Grace found something genuinely prettier for the muon: the number (24/π²)⁶ reads as a *volume-change factor* of nesting one small copy of the domain inside a bigger one, with the "6" being exactly the (real) dimension of the middle domain — which is a much better way to *name* that coincidence than the loose "6 counted four ways" we had. So I banked the naming. But I checked it against the tau, and there it breaks: if you run the same rule up one more step to the tau, it predicts a number about twice too big and *carrying π*, while the measured tau ratio (49·71) is a clean π-free integer. So it's a lovely description of the muon's number, not a law of the generations — and the muon value stays where it was: an identified coincidence, in the free bucket, not banked. I held the tier rather than let the prettier picture soften it. The theorem doesn't care either way — color is the support line, the muon's exact value was never load-bearing.

Notes only; no toys/theorems claimed. F701: Grace's Jacobian reading (m_μ/m_e = (24/π²)⁶ = vol-Jacobian of D_IV¹↪D_IV³, exponent-6 = dim_ℝ D_IV³) is a better DESCRIPTOR (evades K864 rank-bound + FF-20) but NOT a mechanism. Third gate (mine, checkable now): τ-consistency — the uniform nested-Jacobian predicts m_τ/m_e ≈ 7230 (π-carrying), observed 49·71 (π-free) → REFUTED. Non-uniform assignment to save it = a fit unless forced. HOLD the tier (identified-coincidence, bucket-2, count 2); decline the softening; bank only the naming. Flagship already honest (§9⅞.5); theorem untouched. — Lyra
