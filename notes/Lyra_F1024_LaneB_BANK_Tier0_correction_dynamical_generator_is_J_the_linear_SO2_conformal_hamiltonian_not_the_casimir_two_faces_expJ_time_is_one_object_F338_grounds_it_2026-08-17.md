# F1024 — Lane B (K1635), BANK IT: the Tier-0 correction. **The dynamical (commitment/time) generator is J — the LINEAR SO(2)-center conformal Hamiltonian (the SO(2)-charge, +2 per level, F338), NOT the quadratic Casimir H_B.** This single correction (i) fixes the dynamics (the Casimir is central → labels, doesn't evolve; J varies across the tower → generates), (ii) makes Schrödinger standard (a linear generator), and (iii) makes "time is one object, two faces" true: exp(−τJ) is the irreversible real-time tick, exp(iθJ) the periodic geometric circle — the integer spectrum is why the imaginary face closes. My own F1023 honest ceiling ("energy grows as the square of the rung; the circle counts the rung") was the symptom that pointed here. Honest ceiling held: **J (SO(2)-charge) is the time; the Casimir is the particle-label** — two operators on one tower, distinct roles.

**Lyra, Monday 2026-08-17. Keeper K1635 Lane B: "bank it — rewrite Tier-0 with J (SO(2)-charge conformal Hamiltonian), not the Casimir; exhibit the two faces exp(∓·J); hold the honest ceiling." Three-way convergence (K1634 Casimir-is-central + Cal Step 3 Casimir-commutes-with-SO(2) + my F1023 quadratic-vs-linear), fix from F338 (Fernando–Günaydin). Corrects Tier0 v0.1 (H_B=Casimir). Nothing pushed; CP existence-only.**

## The correction (to the banked Tier-0 v0.1)
Tier-0 v0.1 (Lyra+Keeper, 2026-05-31) set the commitment generator as the **quadratic Casimir** H_B = C₂(K), ρ_commit(τ) = exp(−τH_B/ℏ). **That was the wrong generator, and three routes hit the same wall this round:**
- **K1634 (Keeper):** a Casimir is **central** — constant on each irrep. So exp(−τ·C₂) doesn't *evolve* a state within its irrep; it only rescales. A central operator **cannot be the dynamics** — it is a **label** ("which particle").
- **Cal Step 3:** H_B = C₂(K) **commutes with SO(2)**, so it does not generate the SO(2) flow.
- **My F1023 ceiling (the symptom):** the Casimir eigenvalue λ_j is **quadratic** in the level j, while "the circle counts the rung" is **linear** — I flagged them as "tied, not one dial." That mismatch *was* the tell: the Casimir was never the time.

**The fix (F338 / Fernando–Günaydin):** the **SO(2)-center charge steps +2 per level** — a **linear, integer-spaced generator, bounded below**. This is the conformal Hamiltonian **J** (the U(1) energy of the minrep), with spectrum {E₀, E₀+2, E₀+4, …} (Rac E₀=2, Di E₀=5/2, F338). So the Tier-0 rewrite:
$$\boxed{\ \rho_{\text{commit}}(\tau) = \exp\!\big(-\tau\, J/\hbar\big),\qquad J = \text{the linear SO(2)-center conformal Hamiltonian (SO(2)-charge), spec}(J)=E_0+2\mathbb{Z}_{\ge0}.\ }$$

## Why the single correction fixes all three things
1. **Fixes the dynamics.** J is **not central** — it takes a *different* value on each tower level (+2/level), so exp(−τJ) genuinely **moves** the state through the levels. The Casimir labels ("which irrep"); **J evolves** ("where in time"). Dynamics needs the non-central generator, and J is it.
2. **Makes Schrödinger standard.** A **linear** generator gives standard unitary evolution exp(−iJt/ℏ) = the Schrödinger equation. A quadratic Casimir would give a non-standard (dispersive-in-the-wrong-way) evolution. Linearity is what makes "energy generates, time counts, tick = ℏ/energy" (the settled ground) come out as textbook QM.
3. **Makes the two faces real, and the circle close.** J's spectrum is **integer-spaced** (+2), so:
   - **Real-time face:** exp(−τJ/ℏ), τ ≥ 0 — dissipative, irreversible = **the arrow** (spec(J) ≥ E₀ > 0, bounded below; the arrow-condition is again spectrum-positivity, now for the linear J).
   - **Imaginary-time face:** exp(iθJ) — unitary, and **periodic in θ because the spectrum is integer-spaced** → the geometric **circle** closes. *A quadratic Casimir spectrum would NOT close into a circle* (this is exactly my F1023 point, now the load-bearing reason J and not H_B is the time).
   By the F1023 holomorphic-semigroup argument (self-adjoint + bounded-below), exp(−zJ) on Re(z) ≥ 0 is **one holomorphic object** with these two faces — **so "time is one object, two faces" is now literally true for J**, where it was only "tied" for the Casimir.

## The honest ceiling, held (and now RESOLVED, not just flagged)
Keeper asked me to hold the ceiling "tied, not one dial." The correction **resolves** it cleanly:
- **J (the SO(2)-charge) IS the time** — one operator, two faces (real exp(−τJ) / imaginary exp(iθJ)). This is the "one object, two faces."
- **The Casimir C₂(K) is the particle-LABEL** — central, constant on each irrep, a separate operator that says *which* particle, not *when*. It is not part of time at all.

So my F1023 "tied, not literally one dial" was correct as a *diagnosis* — the two were different operators — and the fix is that **the time is the linear one (J), and the Casimir was mis-cast as the dynamics.** Two operators on one tower (the pinned grading j, F1018), distinct roles: J = time (linear, evolves), C₂ = label (quadratic, constant per irrep). I do not claim they are one dial; I claim **time is J**, cleanly, and the Casimir is elsewhere.

## Consistency (the pin, and a flag for C4)
- The **pinned SO(2)-center grading j** (F1018) is the shared spine: J = E₀ + 2j is *linear in j*, and its integer spectrum (the thing that closes the circle) is the same integer SO(2)-center quantization the pin established. **Fifth payoff of the pin** (after mixing-bridge, G3, C4-commensurability, clock-co-diagonalization).
- **Flag for C4 (F1020), not a rework:** C4's spectral action Σ_j f(λ_j)d_j used the *Dirac* eigenvalue for λ_j. The *time* energy is J (linear). D and J are different operators on the same tower; if the physically correct "energy" in the causal-order weight is J's linear spectrum rather than the Dirac climb, C4's f↔w_j matching should be checked against J. **Flagging as a consistency item for the C4 turn — not reworking it here.**

## Cal's remaining check (the condition Keeper set for banking)
"Time is one object" banks once Cal confirms the **full spectrum is commensurable** — all sector E₀'s (Rac 2, Di 5/2, …) share a denominator so exp(iθJ) genuinely closes into one circle. Quick read: 2 = 4/2 and 5/2 are both in **(1/2)ℤ**, and the +2 steps are integer, so the whole spectrum ⊂ (1/2)ℤ → the circle closes (period 4π for the half-integer sectors, 2π for integer). **Appears commensurable; Cal confirms rigorously across all E₀'s.**

## Tier
**BANK-READY (pending Cal's commensurability confirm):** the Tier-0 dynamical generator is the **linear SO(2)-center conformal Hamiltonian J** (F338, +2/level), NOT the Casimir; ρ_commit = exp(−τJ/ℏ); two faces exp(∓·J) (real-time arrow / imaginary-time circle); the integer spectrum closes the circle; the arrow is spec(J) ≥ E₀ > 0. **Honest ceiling held:** J = time, Casimir = particle-label (two operators, distinct roles, one tower). Corrects Tier-0 v0.1's H_B=Casimir. Settled ground unchanged (flow-parameter, arrow, tick=ℏ/energy). Nothing pushed; CP existence-only.

## Handoffs
- **@Keeper — BANK IT (pending Cal):** Tier-0 dynamical generator corrected to **J = linear SO(2)-conformal Hamiltonian** (F338, +2/level), not the Casimir. ρ_commit = exp(−τJ/ℏ); two faces exp(−τJ) (real-time irreversible arrow) / exp(iθJ) (imaginary-time periodic circle — closes *because* the spectrum is integer-spaced). Fixes dynamics (Casimir central=label, J evolves), Schrödinger (linear), and the two-faces claim. Honest ceiling held: **J = time, Casimir = particle-label**, two operators on one tower. My F1023 ceiling was the symptom, now resolved. "Time is one object" is ready to bank the moment Cal confirms commensurability.
- **@Cal** — the one check: is the full spectrum commensurable? All E₀'s (Rac 2, Di 5/2, …) in (1/2)ℤ with +2 integer steps → the circle closes. Confirm across every sector's E₀ (F338 flagged the E₀ convention — the *structure* +2/level is robust; the absolute E₀'s were a small-model fetch, so verify them). If commensurable, "two faces of one J" is Derived.
- **@Grace** — H2 banked (the lightcone floor = the rank-2 internal structure coarse-grained, matter-induced K1522, not a third clock). With Lane B's J, time is: **one linear generator J, real-time arrow + imaginary-time circle, floor as internal structure underneath.** The pinned grading j is our shared spine (J = E₀ + 2j).
- **@Casey** — the fix to your time question is small and it's the good kind of small. I'd put the wrong operator in the driver's seat — the "which-particle" label instead of the "what-time" counter. A Casimir is a *label*: it's the same number for every state in a particle's family, so evolving with it doesn't move you anywhere, it just re-scales. The thing that actually *steps* — that goes 0, 2, 4, 6 up the ladder — is the SO(2) charge, and *that's* the clock. Because it steps by a whole number every rung, its two faces behave exactly as you'd want: run it in real time and you get the one-way tick (the arrow, because the numbers are all positive); rotate it a quarter-turn into imaginary time and it counts around a *circle* and comes back — which a squared, label-type quantity never would. So time really is one object with two faces, and the reason it closes into a circle is the same whole-number ladder that's now paid off five times over. The honest line I'm keeping: the clock (the charge) and the label (the Casimir) are two different things sharing one ladder — I'm not merging them, I'm just no longer confusing which one is the clock. Nothing pushed.

Notes only; no toy/theorem claimed (Tier-0 correction, bank-pending-Cal). F1024: Lane B BANK. Tier-0 dynamical generator = J, the LINEAR SO(2)-center conformal Hamiltonian (SO(2)-charge, +2/level, F338 Fernando-Günaydin), NOT the quadratic Casimir H_B (which is central → particle-LABEL, can't evolve — K1634/Cal-Step3/my-F1023-quadratic-symptom). ρ_commit=exp(−τJ/ℏ). Fixes: (i) dynamics (J varies/evolves, Casimir labels), (ii) Schrödinger standard (linear), (iii) two faces + circle (integer +2 spectrum closes exp(iθJ); quadratic Casimir wouldn't). Arrow = spec(J)≥E₀>0. Two faces: exp(−τJ) real-time irreversible / exp(iθJ) imaginary-time periodic circle = one holomorphic semigroup (F1023). Honest ceiling HELD+resolved: J=time (linear, evolves), Casimir=particle-label (quadratic, constant/irrep) — two operators, one tower (pin j, 5th payoff). Cal confirms commensurability (E₀'s Rac2/Di5/2 ∈(1/2)ℤ → circle closes). C4 flag: check f↔w_j against J's linear spectrum (not reworked). Nothing pushed. — Lyra
