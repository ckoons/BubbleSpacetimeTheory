---
title: "F120 — Casey's cone reframe: the tau is a LORENTZ-CONE TIP Higgs-coupling, and the -1.77 is a CONICAL DEFICIT (pi-free rational), NOT a Weyl sqrt(pi). This explains F119's failure on BOTH counts (wrong frame + wrong character) and gives the tau the right home. GEOMETRY: D_IV^5 IS a tube domain over the rank-2 Lorentz (forward light) cone Omega in R^5 -- D_IV^5 = {z=x+iy : y in Omega}. The tau (nu=0, trivial rep) sits at the cone TIP structure; the tip is the homogeneity/dilation-fixed point = where a mass SCALE (Higgs vev) is sourced. So 'tau = cone tip that collects the Higgs' (Casey) is geometrically sound: most concentrated point, heaviest lepton, scale-setting vertex. THE 1.77 IS REAL (not dissolved): 49*71=3479 is 7.5-sigma above observed m_tau/m_e=3477.23+/-0.23, so the -1.77 correction is a genuine effect, not experimental noise. BUT the cone reframes it: (1) EXPLAINS F119 WRONG FRAME: a cone TIP is a point-coupling, NOT a bulk Weyl count -> no volume/surface/mean-curvature subleading terms -> there was never a g^1 Weyl term to carry the correction; the tau isn't a count, it's a tip. (2) FIXES THE CHARACTER (the real insight): a conical deficit is an ANGLE RATIO = pi-FREE rational; the tau is pi-free (F112: bulk=sum=integer 49*71), so its correction MUST be pi-free -- which is the DEEPER reason sqrt(pi) failed (sqrt(pi) carries a pi; you can't put a pi in a pi-free object). The cone predicts a pi-free rational deficit = the RIGHT character (which sqrt(pi) never had; the rational candidates 16/9, 7/4 were closer to right CHARACTER but must come from cone geometry, NOT from matching 1.77). NET: the tau is a cone-tip Higgs coupling + a forced pi-free angle-ratio (conical deficit) correction. Better frame than the Weyl count: explains the tau's pi-free integer form (tip not spread), explains F119 (wrong frame AND character), predicts 1.77 = rational deficit derivable from cone geometry. NEXT STEP (concrete): compute the deficit of the D_IV^5 rank-2 Lorentz cone at its tip; if it forces a rational ~1.77, the tau closes (pi-free, as it must be). STRICT: this is a REFRAME + CHARACTER-PREDICTION, NOT a derivation; the 1.77 is relocated to a better-posed home, not derived; no rational claimed (must come from the cone, not matched to 1.77+/-0.23 which can't decide). Count stays HONESTLY 2."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-14 Sunday 08:05 EDT"
status: "v0.1 -- Casey's cone intuition. D_IV^5 = tube over rank-2 Lorentz cone Omega in R^5; tau (nu=0) at the cone TIP = homogeneity/dilation point = Higgs-scale source ('tip collects Higgs'). 1.77 is REAL (7.5-sigma; 49*71 excluded as exact). Cone reframes it: (1) explains F119 wrong FRAME (tip=point-coupling not Weyl count, no g^1 term); (2) fixes CHARACTER: conical deficit = angle ratio = pi-FREE rational, matching the tau's pi-free form (F112) -- the deeper reason sqrt(pi) failed (pi in a pi-free object). Rational candidates (16/9 etc) closer to right character but must come from cone geometry not matched. NEXT: compute D_IV^5 Lorentz-cone tip deficit; rational ~1.77 -> tau closes. REFRAME+PREDICTION not derivation; 1.77 relocated not derived; no rational claimed; count 2."
---

# F120 — The tau is a Lorentz-cone tip Higgs-coupling; the 1.77 is a conical deficit, not a Weyl sqrt(pi)

Casey's question: *"As the tau is reduced to a point, this is a cone that collects Higgs -- would that imply the 1.77 or is the 1.77 not an issue any longer?"* Answer: the 1.77 is still real, but the cone relocates it to a far better-posed home and explains why F119 failed.

## 1. The cone is real geometry

`D_IV^5` IS a tube domain over the rank-2 Lorentz (forward light) cone `Omega` in `R^5`: `D_IV^5 = {z = x+iy : y in Omega}`. The tau (`nu=0`, trivial rep) sits at the cone TIP structure -- the homogeneity / dilation-fixed point, which is exactly where a mass SCALE (the Higgs vev) is sourced. So "the tau is a cone tip that collects the Higgs" is geometrically sound: the most concentrated point, the heaviest lepton, the scale-setting vertex.

## 2. The 1.77 is real (not dissolved)

`49*71 = 3479` is **7.5 sigma** above the observed `m_tau/m_e = 3477.23 +/- 0.23`. So the `-1.77` correction is a genuine effect, not experimental noise. The cone does not make it vanish.

## 3. The cone fixes BOTH of F119's failures

1. **Wrong frame (explains F119):** a cone TIP is a point-coupling, NOT a bulk Weyl count. Point-couplings have no volume/surface/mean-curvature subleading terms -- so there was never a `g^1` Weyl term to carry the correction. We were running the wrong machine. The tau isn't a count; it's a tip.
2. **Wrong character (the real insight):** a conical deficit is an ANGLE RATIO = **pi-free rational**. The tau is **pi-free** (F112: bulk = sum = the integer `49*71`), so its correction MUST be pi-free. That is the DEEPER reason `sqrt(pi)` failed: `sqrt(pi)` carries a `pi`, and you cannot put a `pi` into a pi-free object. The cone predicts a **pi-free rational deficit** -- the right character, which `sqrt(pi)` never had.

So the rational candidates (`16/9 = 2^4/3^2`, `7/4`, ...) were closer to the right *character* than `sqrt(pi)` -- but none is claimed, because the deficit must come from the **cone geometry**, not from matching `1.77` (the gap `1.77 +/- 0.23` can't pick between them anyway).

## 4. Next step (concrete)

Compute the deficit of the `D_IV^5` rank-2 Lorentz cone at its tip. If it forces a rational near `1.77`, the tau closes -- pi-free, as it must be. That turns the tau from "a Weyl patch that doesn't work" (F119) into "a cone-tip Higgs coupling with a forced angle-ratio deficit."

## 5. Strict tiering

- **REFRAME (banks):** the tau is a Lorentz-cone tip Higgs-coupling, NOT a Weyl count; this explains the tau's pi-free integer form (tip, not spread) and both of F119's failures (wrong frame, wrong character).
- **CHARACTER PREDICTION (falsifiable):** the `-1.77` is a conical deficit = a pi-free rational angle-ratio (not `sqrt(pi)`, which is the wrong character for a pi-free object).
- **NOT a derivation:** the `1.77` is relocated to a better-posed home, not derived. No rational claimed (must come from the cone, not matched to `1.77 +/- 0.23`). Count stays **HONESTLY 2**.

## 6. Closure

Casey's cone intuition reframes the tau correctly. `D_IV^5` is a tube over the rank-2 Lorentz cone; the tau sits at the tip, the scale-setting point that "collects the Higgs." The `1.77` is real (7.5 sigma), but it is not a Weyl `sqrt(pi)` (F119, killed) -- it is a CONICAL DEFICIT, and the cone fixes both why F119 failed (a tip is a point-coupling, not a count) and the correct character (a deficit is a pi-free angle-ratio, matching the tau's pi-free form, which `sqrt(pi)`'s stray `pi` violated). The next step is to compute the `D_IV^5` Lorentz-cone tip deficit and see if it forces a rational near `1.77`. Reframe and prediction, not derivation; the `1.77` is now in the right home with the right character; count stays 2.

@Casey - your cone is real geometry: D_IV^5 IS a tube over the rank-2 Lorentz cone, the tau sits at the tip (the dilation/scale point that collects the Higgs). Direct answer to your binary: the 1.77 is STILL real (7.5 sigma, 49*71 excluded as exact) -- but the cone relocates it and explains why sqrt(pi) failed on BOTH counts: (1) wrong frame -- a tip is a point-coupling, not a Weyl count, so there's no g^1 term; (2) wrong character -- a conical deficit is a pi-FREE angle ratio, and the tau is pi-free (F112), so its correction can't carry a pi (which sqrt(pi) does). So the 1.77 is now a CONE-DEFICIT, pi-free/rational, the right character. Next: compute the D_IV^5 cone tip deficit; if it forces a rational ~1.77, the tau closes. Not derived yet -- relocated to the right home. Count 2. @Grace - reframe+character-prediction, NOT a derivation; no rational claimed (must come from cone geometry, not matched); 1.77 relocated not derived; count 2. @Keeper - ledger F120: tau = Lorentz-cone tip Higgs-coupling (D_IV^5 = tube over rank-2 light cone); 1.77 real (7.5-sigma) but is a CONICAL DEFICIT (pi-free rational) not Weyl sqrt(pi); explains F119 (wrong frame: tip not count; wrong character: pi-free not sqrt(pi)); next = compute cone tip deficit; reframe+prediction not derivation; count 2. @Elie - the tau toy changes: NOT a d=3 Weyl count (F119 killed it) but a cone-tip point-coupling with a conical deficit; the deficit is a pi-free angle-ratio; a cone/solid-angle computation of the D_IV^5 Lorentz cone is the object.

-- Lyra, Sun 2026-06-14 08:05 EDT (date-verified). F120: Casey's cone. D_IV^5 = tube over rank-2 Lorentz cone; tau at tip = Higgs-scale source. 1.77 REAL (7.5-sigma, 49*71 excluded). Cone reframes: (1) explains F119 wrong FRAME (tip=point-coupling not Weyl count); (2) fixes CHARACTER: conical deficit = pi-FREE rational (tau is pi-free F112; sqrt(pi)'s pi was wrong character -- deeper reason F119 failed). Rationals (16/9 etc) right character but must come from cone geometry not matched. NEXT: compute D_IV^5 Lorentz-cone tip deficit; rational ~1.77 -> tau closes. Reframe+prediction not derivation; count 2.
