---
title: "F160 — pull-until-done on M_ν (and the frontier map): I tested FOUR forward approaches to the two off-pole neutrino splittings; all fail to give the mild δ-ratio without the cell-map fine-structure; saved findings + named close-analysis + REFUSED the fishing temptation. SETUP (from the handshake): m_1=0 (pole, ν=1/2), m_2=κ·δ_2, m_3=κ·δ_3 (linear in off-pole distance, simple pole, F158-ext); the OBSERVABLE is the ratio δ_3/δ_2 = m_3/m_2 (the ordering + Δm² ratio); absolute κ below experimental resolution (Casey). The two off-pole distances δ_2, δ_3 are what the cell-map must supply. APPROACHES TESTED (forward, blind), ALL FAIL: (1) Frobenius orbits — the 18 generic orbits are multiplicatively DEGENERATE (127 prime), so they can't supply TWO DIFFERENT δ's; FAIL. (2) lower Γ_Ω poles (ν=−1/2, −1) — give δ ~ O(1), hence O(1) masses, NOT tiny; wrong scale; FAIL. (3) exponential suppression (like Λ=exp(−280) / α^k) for tiny δ — unfounded for the ν sector without a specific mechanism; FAIL (no forward hook). (4) charged-lepton-squared seesaw (δ_i ~ m_charged,i²/M) — gives a STRONG hierarchy (m_charged ratio ~17², way too steep), contradicting the observed MILD ν hierarchy (m_3/m_2 ≈ √33 ≈ 5.75); FAIL (and consistent with F148 mild-hierarchy). REFUSED FISHING (explicit): the observed δ_3/δ_2 = m_3/m_2 ≈ √(Δm²_31/Δm²_21) ≈ √33 ≈ 5.75 is NEAR C_2 = 6 (~4%); I did NOT match it to C_2 (that's the textbook trap — a BST primary near an observed ratio; Grace #36/#37, Cal #27). The ratio must come FORWARD from the cell-map, not be crowned at 6. META-FINDING: all four failed approaches converge on ONE need — the cell-map ORBIT→POINT fine-structure (the K-type quantization pin, F150 master lock): the mild off-pole δ-ratio is a fine-structure of WHERE the two nonzero seats sit near the pole, which the degenerate orbits / poles / naive seesaw don't supply. So M_ν gates on the cell-map (genuine, multi-session open core), NOT bypassable solo. SAVED + close-analysis NAMED. FRONTIER MAP (pull-until-done, all 3 tracks): M_ν gates on cell-map; #418 bulk-color {+1,−1,0} is the deep #418 core (cold solo = thin, gated); tau √π coefficient is ONE object (Gindikin Γ, F157/F159-rigor-flag) with the coefficient open + severe back-fit risk on ½ (don't grind solo). ALL THREE gate on the orbit→point cell-map (M_ν directly; #418 = the bulk-color version of the same address map; tau √π coefficient = the same cone-curvature object). So the genuine end-state: the continuum side is COMPLETE; the single deep open core is the orbit→point cell-map (K-type quantization). TIER: m=κδ linear forward; the failed approaches are honest negatives (map what the mechanism is NOT); the δ-ratio + cell-map = close-analysis (gated); no fishing. Count HOLDS 4 (description); no manufactured progress, no manufactured wall — tested and genuinely gated."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-16 Tuesday 16:55 EDT"
status: "v0.1 — pull-until-done on M_ν + frontier map. SETUP: m_1=0 (pole), m_2=κδ_2, m_3=κδ_3 (linear, simple pole); OBSERVABLE = ratio δ_3/δ_2 = m_3/m_2; κ below resolution. FOUR forward approaches to the off-pole δ's TESTED, ALL FAIL: (1) Frobenius orbits — multiplicatively degenerate (127 prime), can't give two different δ; (2) lower Γ_Ω poles (−1/2,−1) — δ~O(1), masses too big; (3) exp-suppression — unfounded for ν sector, no forward hook; (4) charged²-seesaw — too-strong hierarchy (m_charged ratio ~17²), contradicts observed mild (m_3/m_2≈√33≈5.75). REFUSED FISHING: √33≈5.75 NEAR C_2=6 (~4%) — did NOT match to C_2 (BST-primary-near-observed trap; Grace #36/#37, Cal #27); ratio must come FORWARD. META: all four converge on the cell-map ORBIT→POINT fine-structure (K-type pin, F150 master lock) — the mild δ-ratio is seat-fine-structure near the pole; genuine multi-session open core, not solo-bypassable. FRONTIER (3 tracks): M_ν, #418 {+1,−1,0}, tau √π coeff — ALL gate on the orbit→point cell-map. End-state: continuum side COMPLETE; single deep core = the cell-map. TIER: m=κδ forward; failed approaches = honest negatives; δ-ratio gated; no fishing. Count HOLDS 4. Tested + genuinely gated, not manufactured wall."
---

# F160 — Pull-until-done on M_ν: four approaches tested, all gate on the cell-map; fishing refused; the frontier mapped

Casey said pull until done — so I pulled on M_ν for real, tested the off-pole splittings forward, and report honestly what holds, what fails, and where the genuine wall is.

## Setup (from the handshake, F158-ext)

`m₁ = 0` (pole, `ν=1/2`); `m₂ = κ·δ₂`, `m₃ = κ·δ₃` (linear in off-pole distance, from the simple pole). The **observable** is the **ratio** `δ₃/δ₂ = m₃/m₂` (the ordering + the Δm² ratio); the absolute `κ` is below experimental resolution (Casey's catch). So the cell-map's job is the two off-pole distances `δ₂, δ₃`.

## Four forward approaches tested — all fail

| # | approach | result |
|---|---|---|
| 1 | **Frobenius orbits** supply the δ's | the 18 generic orbits are multiplicatively **degenerate** (127 prime) — can't supply *two different* δ's. **FAIL.** |
| 2 | **lower Γ_Ω poles** (ν=−1/2, −1) as the seats | give `δ ~ O(1)` → `O(1)` masses, not tiny. Wrong scale. **FAIL.** |
| 3 | **exponential suppression** (à la Λ=exp(−280), α^k) for tiny δ | no forward hook in the ν sector — would be assumed, not derived. **FAIL.** |
| 4 | **charged-lepton² seesaw** (`δ_i ~ m_charged,i²/M`) | gives a **strong** hierarchy (charged ratio ~17²), but the observed ν hierarchy is **mild** (`m₃/m₂ ≈ √33 ≈ 5.75`). Contradicts data + F148. **FAIL.** |

All four fail — and the failures are informative (they map what the mechanism is **not**).

## Fishing refused (explicit)

The observed `δ₃/δ₂ = m₃/m₂ ≈ √(Δm²₃₁/Δm²₂₁) ≈ √33 ≈ 5.75` is **near `C₂ = 6`** (~4%). I did **not** match it to `C₂`. That's the textbook trap — a BST primary sitting near an observed ratio (Grace #36/#37, Cal #27). The ratio must come **forward** from the cell-map, not be crowned at 6 because it's close. Flagging it so no one else reaches for it either.

## Meta-finding: all four converge on the cell-map

Every failed approach points to the same missing thing: the **orbit→point cell-map fine-structure** (the K-type quantization pin, the F150 master lock). The mild off-pole δ-ratio is a *fine-structure* of where the two nonzero seats sit near the pole — and the degenerate orbits, the lower poles, and the naive seesaw all miss it. So **M_ν genuinely gates on the cell-map** — not solo-bypassable. Tested, not manufactured.

## Frontier map (pull-until-done, all three tracks)

- **M_ν** → gates on the orbit→point cell-map (above).
- **#418 bulk-color {+1,−1,0}** → the deep #418 core; cold solo = thin; gated (it's the bulk-color version of the same address map).
- **tau √π coefficient** → ONE object (the cone's Gindikin Γ, F157 + the F159-followup rigor flag), coefficient open with severe back-fit risk on the ½; don't grind solo.

**All three gate on the same thing — the orbit→point cell-map (K-type quantization).** So the genuine end-state of the pull: the **continuum side is complete**, and the single deep open core is the cell-map. That's where the close analysis goes, and it's genuinely multi-session (the K-type pin has been the open core all program).

## Tier and count

- `m = κ·δ` (linear, simple pole) — **forward**.
- The four failed approaches — **honest negatives** (what the off-pole mechanism is *not*).
- The δ-ratio + the cell-map — **close-analysis, gated**.
- **No fishing** (refused `C₂`), **no manufactured wall** (tested four ways), **no manufactured progress.**
- **Count HOLDS at 4** (description). 

@Elie — for the joint cell-map: I've ruled out four ways the off-pole δ's *don't* come (degenerate orbits, lower poles, exp-suppression, charged²-seesaw); the mild ratio is genuinely the **seat fine-structure near the pole**, which is your orbit→point map's job. The continuum frame is fixed (m = κ·δ, ratio = the observable); when your cell-map gives the two off-pole positions, the masses + ordering + PMNS fall out. And I explicitly did NOT match the ratio to C₂ — derive it forward.
@Casey — pull-until-done outcome, honest: I tested four forward routes to the neutrino splittings; all genuinely gate on the orbit→point cell-map (the K-type pin), and I refused the C₂≈√33 fishing temptation. The continuum side is complete on all three tracks; they all funnel to that one cell-map, which is the genuine deep open core — multi-session, and genuinely Elie's discrete structure to supply, not something I can force solo without fitting. Not a wall I built; a wall I tested into. Count holds 4.

— Lyra, Tue 2026-06-16 16:55 EDT (date-verified). F160: pull-until-done on M_ν. m_1=0 pole, m_i=κδ_i (linear, simple pole); observable = ratio δ_3/δ_2=m_3/m_2; κ below resolution. FOUR forward approaches to off-pole δ's TESTED ALL FAIL: (1) Frobenius orbits degenerate (127 prime); (2) lower poles δ~O(1) too big; (3) exp-suppression no hook; (4) charged²-seesaw too-strong-hierarchy vs observed mild (√33≈5.75). REFUSED fishing: √33≈5.75 near C_2=6 (~4%) NOT matched. META: all converge on the orbit→point cell-map (K-type pin, F150). FRONTIER: M_ν + #418 {+1,−1,0} + tau √π coeff ALL gate on the cell-map. End-state: continuum COMPLETE; single deep core = cell-map (multi-session). TIER: m=κδ forward; 4 failed = honest negatives; ratio gated; no fishing/no manufactured wall. Count HOLDS 4.
