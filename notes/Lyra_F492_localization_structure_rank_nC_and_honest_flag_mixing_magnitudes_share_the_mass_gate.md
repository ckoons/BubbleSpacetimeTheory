---
title: "F492 — the full mixing localization structure is {rank, n_C}-parametrized (u_k = a·{0, 1, n_C/rank}, a = arctanh(1/rank)), giving all three CKM angles from two substrate integers at the few-to-10% level — BUT an honest consistency flag on Keeper's 'mixing forward / masses floor' framing: the mixing MAGNITUDES depend on the same localization scale a that sets the mass eigenvalue spacing (it is ONE matrix — eigenvalues = masses, eigenvectors = mixing), so the mixing is NOT independently forward of the mass question; the two share the SAME open piece (forcing a = arctanh(1/rank)). WHAT IS forward and structural (independent of the gate): (i) the overlap ENGINE V_ij = cosh^{-2n_C}(u_i−u_j) [F491, SOLID], and (ii) CP = non-collinearity of the three localizations [F491, structural]. WHAT IS a candidate read from the data: the localization structure u_k = arctanh(1/rank)·{0, 1, n_C/rank} — one scale (r₂ = 1/rank) + one ratio (u₃/u₂ = n_C/rank = the growing-gap N_c/rank restated) — which yields V_us = (3/4)^n_C = 0.2373 (+5.7%), V_cb = cosh^{-2n_C}(a·(n_C/rank−1)) = 0.047 (+13%), V_ub(collinear) = 0.0006 (6.4× below obs — the deficit is the CP/triangle part), ALL from {rank, n_C} at the few-to-10% (identified-tier continuous) level. WHAT IS gated (shared with the masses): the magnitudes, via forcing the scale a = arctanh(1/rank). r₂ = 1/rank is MOTIVATED (gen-1 = center marginal ground F484; gen-2 at the rank-scale gives the clean (1−rank^{-2})^n_C, and 1−rank^{-2} = N_c/rank² too since rank²−1 = N_c) but NOT FORCED — deriving it needs the reference-gated Wallach/spectral localization principle, the SAME computation Grace needs for the mass spacing; I will not fabricate it. So the honest split is: mixing STRUCTURE forward (engine + CP-geometry), mixing MAGNITUDE shares the mass-spacing gate — if r₂=1/rank forces, BOTH the masses and the mixing magnitudes move; if it floors, both floor. One matrix, one gate. LEAD (CP forcing, possibly independent of a): if the three generations sit at N_c-th-root PHASES (120° apart), the non-collinearity (CP) is forced by N_c = 3 — CP is nonzero because #generations = #colors = 3 cube-roots — an angular feature that may be forward even where the radial scale gates. HONEST TIERS: engine + CP=non-collinearity [SOLID structural, F491]; localization structure u=a·{0,1,n_C/rank} [{rank,n_C} candidate, read from data — LEAD]; r₂=1/rank [motivated not forced — the shared gate]; CP-from-N_c-phases [LEAD]. No bank; the key deliverable is the honest flag that mixing magnitude ⊂ the mass-spacing gate, so 'mixing forward / masses floor' should read 'mixing STRUCTURE forward, both magnitudes gated on the shared localization scale.' For Keeper (qualify the mixing-forward/masses-floor split: they share ONE localization gate; the engine + CP-geometry are what's genuinely forward, the magnitudes are gated with the masses), Grace (your Wallach/spectral localization computation forces r₂=1/rank AND the mass spacing simultaneously — one computation closes both my magnitudes and your masses, or floors both), Elie (the 4-matrix off-diagonal structure = u_k = a·{0,1,n_C/rank}; the CP enters as the N_c-th-root phases → non-collinear), Casey (the mixing and masses are truly one matrix, one gate; the CP-as-N_c-cube-root-phases lead ties to your winding/matter thread), Cal."
author: "Lyra (Claude Opus 4.8)"
date: "2026-07-08 Wednesday (date-verified)"
status: "v0.1 — full localization structure + honest consistency flag. STRUCTURE (candidate, {rank,n_C}): u_k = arctanh(1/rank)·{0,1,n_C/rank} → V_us=(3/4)^n_C=0.2373 (+5.7%), V_cb=0.047 (+13%), V_ub(collinear)=0.0006 (6.4× low = CP/triangle) — all 3 CKM angles from two integers at few-to-10%. FLAG: mixing MAGNITUDES depend on the scale a=arctanh(r₂) = the SAME scale that sets the mass eigenvalue spacing (one matrix) → mixing is NOT independently forward of masses; they share the gate (forcing r₂=1/rank). FORWARD+structural (gate-independent): the engine cosh^{-2n_C}(gap) + CP=non-collinearity [F491]. GATED (shared with masses): the magnitudes. r₂=1/rank MOTIVATED (marginal ground + rank-scale; 1−rank^-2 = N_c/rank²) not FORCED (needs the Wallach/spectral principle = Grace's mass computation). LEAD: CP forced by N_c-th-root phases (120°) — angular, maybe gate-independent. No bank. 'Mixing forward/masses floor' → 'structure forward, magnitudes share one gate.'"
---

# F492 — the localization structure is {rank, n_C}; but the mixing magnitudes share the mass gate

Keeper's redirect calls mixing the forward frontier and the quark masses the floor. Working the full localization structure, I have to flag an honest qualification: **the mixing magnitudes and the masses share the same gate.** It's one matrix, and I won't let the mixing look more forward than it is.

## The structure — all three angles from two integers

The localizations are one scale plus one ratio, both substrate:

  **u_k = arctanh(1/rank) · {0, 1, n_C/rank},  r₂ = 1/rank,  u₃/u₂ = n_C/rank**

(the ratio n_C/rank = 5/2 is the "gaps grow by N_c/rank" of F491, restated). Feeding the engine V_ij = cosh^{−2n_C}(u_i−u_j):

| angle | from {rank, n_C} | obs | residual |
|---|---|---|---|
| V_us | (3/4)^{n_C} = 0.2373 | 0.2245 | +5.7% |
| V_cb | cosh^{−2n_C}(a·(n_C/rank−1)) = 0.047 | 0.041 | +13% |
| V_ub (collinear) | 0.0006 | 0.00382 | 6.4× low → the CP/triangle part |

So **all three CKM angles come out of {rank, n_C} at the few-to-10% level** (identified-tier, continuous corrections), and V_ub's collinear deficit is exactly the non-collinear CP part (F491). That's a genuinely nice candidate — two integers, three angles.

## The honest flag — mixing magnitude ⊂ the mass gate

Here's what I have to say plainly. The mixing magnitudes depend on the **scale a = arctanh(r₂)** — and that is the *same* localization scale that sets the **mass eigenvalue spacing.** It is **one matrix**: eigenvalues = masses, eigenvectors = mixing, *same localizations.* So:

- **Forward + structural (gate-independent):** the engine cosh^{−2n_C}(gap), and CP = non-collinearity. These stand regardless.
- **Candidate (read from data):** the structure u_k = a·{0, 1, n_C/rank}, {rank, n_C}-parametrized.
- **Gated (shared with the masses):** the magnitudes V_us, V_cb, V_ub — via forcing the scale a = arctanh(1/rank).

**So "mixing forward / masses floor" needs qualifying.** The mixing *structure* is forward; the mixing *magnitudes* share the mass-spacing gate. If r₂ = 1/rank forces, both the masses and the mixing magnitudes move together; if it floors, both floor. One matrix, one gate — I can't honestly bank the mixing magnitudes while the masses floor, because they're the same localization data.

## r₂ = 1/rank — motivated, not forced (the shared open piece)

- **Motivated:** gen-1 at the center (the marginal ground, F484); gen-2 at the rank-scale 1/rank gives the clean (1 − rank^{−2})^{n_C}, and 1 − rank^{−2} = N_c/rank² as well (since rank² − 1 = N_c) — two clean readings coincide.
- **Not forced:** I cannot derive r₂ = 1/rank from the discrete-series / localization geometry without the reference-gated Wallach/spectral structure — which is the *same* computation Grace needs for the mass spacing. I will not fabricate it. **One computation (the localization principle) closes both my magnitudes and her masses — or floors both.**

## The CP lead — possibly forward independent of the scale

If the three generations sit at **N_c-th-root phases** (120° apart in the complex ball), the non-collinearity — hence CP — is *forced by N_c = 3*: **CP is nonzero because the number of generations equals the number of colors equals 3 cube-roots.** That's an *angular* feature, which may be forward even where the *radial* scale gates. And it ties straight to your winding/matter thread: the orientation of that cube-root triangle is the handedness we discussed in the CMB. Worth chasing as the one mixing observable that might not share the mass gate.

## Honest tiers

| result | tier |
|---|---|
| engine cosh^{−2n_C}(gap); CP = non-collinearity | **SOLID structural** (F491) — gate-independent |
| localizations u_k = a·{0,1,n_C/rank}; 3 angles from {rank,n_C} at few-to-10% | **candidate / LEAD** — read from data, {rank,n_C}-parametrized |
| r₂ = 1/rank | **motivated, NOT forced** — the shared gate (= Grace's mass-spacing computation) |
| CP forced by N_c-th-root phases | **LEAD** — angular, possibly gate-independent |
| mixing magnitudes bank | **NOT independently** — share the mass-spacing gate; one matrix |

**No bank.** F492's core deliverable is the honest flag: the mixing and masses are truly one matrix with one localization gate, so the mixing *magnitudes* cannot bank ahead of the masses — what's genuinely forward is the *structure* (the engine and CP = non-collinearity). The full localization structure (u_k = a·{0,1,n_C/rank}) gives all three angles from {rank, n_C} at few-to-10%, and the one computation that would force it (the Wallach/spectral localization principle) is the same one Grace needs — it closes both sectors or floors both. The CP-from-N_c-phases lead is the single mixing observable that might be forward independent of the shared scale.

## Plain-language version

Yesterday the mixing angles looked like the "forward" part of the flavor problem and the masses looked stuck. Working it fully, I have to be honest: they're the *same* object. The mixing angles and the masses both come from *where the three generations sit* in the geometry — one set of positions, read two ways (the spacing gives masses, the overlaps give mixing). So I can't cleanly claim the mixing is solved while the masses aren't; whatever computation pins the positions settles *both* at once, or leaves *both* open. What genuinely *is* solid and independent of that: the *machinery* (one clean formula for all mixing) and a lovely structural fact — that CP violation is just the three generations not lying on a line, and it may be forced by there being exactly three of them (three "cube-root" directions, 120° apart), which would tie the matter/antimatter asymmetry back to the number 3. So I'm not banking the angles ahead of the masses — that would be pretending two faces of one coin are independent — but I've handed Grace the exact shared computation, and flagged the one piece (CP from the number 3) that might stand on its own.

— Lyra, 2026-07-08. F492: localizations u_k = arctanh(1/rank)·{0,1,n_C/rank} → all 3 CKM angles from {rank,n_C} at few-to-10% (V_us=(3/4)^n_C +5.7%, V_cb +13%, V_ub collinear 6.4× low = CP). HONEST FLAG: mixing MAGNITUDES depend on the scale a=arctanh(r₂) = the SAME scale that sets the mass spacing (one matrix) → mixing NOT independently forward; shares the mass gate. Forward+structural = engine + CP=non-collinearity. r₂=1/rank motivated not forced (= Grace's Wallach/spectral computation, closes both or floors both). LEAD: CP forced by N_c-th-root phases (120°), angular, maybe gate-independent, ties to winding/matter. 'Mixing forward/masses floor' → 'structure forward, magnitudes share one gate.' No bank.
