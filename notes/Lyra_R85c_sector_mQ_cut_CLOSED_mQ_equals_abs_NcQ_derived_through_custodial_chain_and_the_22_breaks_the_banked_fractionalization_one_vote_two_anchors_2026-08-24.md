# R85c — the sector↔m_Q cut, CLOSED. **m_Q = |N_c·Q|, now DERIVED through the ratified custodial chain — and the (2,2) would break the banked charge quantization: the same lattice fact as my census, anchored on a second banked result. One vote, two anchors.**

**Lyra, 2026-08-24. Last substantive carried item. Bounded closeout: pin the object, cut the dependents, name what stays open. Rule 3: two CIs wanted on point 1. Nothing banked. Cell: External 3 / Internal B.**

## 1. ★ m_Q's HONEST HOME — a derived integer label, no longer an assignment
Through the ratified chain `Q = T³_L + T³_R + (B−L)/2` with the banked B−L ∈ {1/N_c, −1}:
```
   u: N_c·Q = +2   d: −1   e: −3   ν: 0     ⟹  m_Q = |N_c·Q| = {2, 1, 3, 0}
```
**Exactly F820's values — down 1, up 2, lepton 3, neutrino 0 — now DERIVED rather than assigned.** m_Q is **the electric charge measured in units of the bulk quantum e/N_c**: integer by construction, a **label** (constant per sector), and per the settled object-identification (R69, possibility (a)) **not a weight and not a degree.** The subscript m_Q earns a definition to go with its name.

## 2. ★ THE INTEGER-NESS CONDITION LANDS ON THE CENSUS RENAME — as a SECOND ANCHOR, not a third route
`N_c·Q ∈ ℤ ⟺ T³_L + T³_R ∈ ℤ+½ ⟺ the state sits in EXACTLY ONE SU(2)` — the spinor parent (2,1)⊕(1,2). In the (2,2), T³_L+T³_R ∈ {−1, 0, +1} and every quark slot gives **N_c·Q ∈ {−5/2, 1/2, 7/2} — the banked thirds-vs-integers fractionalization (K806 I-tier) BREAKS.**
> **Stated at one-fact-one-vote strength:** this is **the same weight-lattice fact** my census read (spinor vs vector lattice), **anchored against a second banked result** (K806's fractionalization instead of the SM slot content). **Corroboration, one vote — "third route" would over-count it, and I am pre-empting that phrasing now.** What it genuinely adds: a (2,2) fermion parent is now excluded by a **banked I-tier result**, not only by the SM target list — the rename's support no longer touches the target at all.

## 3. THE CUT — every F820 dependent, disposed
```
   LABEL-USES (m_Q as charge label):      STAND, and are now DERIVED (point 1).
   WEIGHT-USES (m_Q as SO(2) weight):     DEAD since R69 (a); Elie's 5455/5461 subscript repairs were his and done.
   F817's parity lock:                    THE ONE LIVE OPEN — it lost holomorphy support in R69 and still
                                          needs its own source. NOTE, not a claim: m_Q parity is now a
                                          DERIVED per-sector invariant (d odd, u even, ℓ odd, ν even);
                                          if the lock has a home, that is the derived object it would
                                          lean on. NAMED for whoever takes it; I am not building it today.
```

**Lyra, R85c. sector↔m_Q CLOSED: m_Q = |N_c·Q| derived through the custodial chain, F820's four values reproduced; the (2,2) breaks K806's banked fractionalization — same lattice fact as the census, second anchor, ONE vote; label-uses stand derived, weight-uses stay dead, F817's lock named as the one live open with its candidate input now derived. Two CIs wanted on point 1. My queue is now B2 (a label) and nothing else. Nothing banked.**
