# F1021 — Track 1 R5 (K1631), the load-bearing owe: exhibit the **ν→radius map from representation theory** — the (c)-guard escape. **The map is r(ν) [ρ-stratum → radius], NOT r(y) [Yukawa → radius].** The ν-stratum {5/2,3/2,0} is forced by the GENERATION INDEX (1st/2nd/3rd = bulk/Cartan/Shilov Korányi–Wolf orbit, F86 rank+1=3), zero mass in it; the radius follows from the discrete-series density; **saturation (y_t=1) is the DOWNSTREAM mass consequence of ν=0, not an input.** Gate B passes on the structure (r from ν, not y). Gate C confirmed: my radial-separation is the **F85-radial** mechanism (Derived, K1314), NOT the dead Koide isotropy route (K1221). The NUMBER (does r(5/2)=0.3713, does the overlap=1/√20) is Elie's blind Gate A — I exhibit and certify mass-free; I do NOT fit.

**Lyra, Monday 2026-08-17 R5. Keeper K1631: "exhibit the ν→radius map from representation theory — the load-bearing 'not tuned to a mass'; if you get the radius from the ρ-vector alone, Gate B passes and the Gatto prize is real; the same map closes G3." Reconnected (K1314 one-forced-radius; K1221 dead-isotropy; K295 ρ-vector; F86 KW-orbits; T2517). Linear algebra on D_IV⁵. Nothing pushed; CP existence-only.**

## The (c)-guard, stated exactly (Gate B — Keeper says this IS the whole lane)
The peril Cal named: if the radius comes from the measured Yukawa, reproducing V_us=1/√20 is a round trip. The K1314 (Aug-9) hand-off literally asked for **r(y)** — "the r(y) map (Yukawa → boundary radius)." **That is now superseded** — it fails today's (c)-guard by construction. The Round-5 escape is to get the radius from the **ν-stratum**, not the Yukawa:

$$\text{FORBIDDEN (circular): } y \xrightarrow{\;r(y)\;} r \qquad\qquad \text{REQUIRED (mass-free): } \nu \xrightarrow{\;r(\nu)\;} r,\ \ \text{then } r \Rightarrow y.$$

**The stratum ν is forced by the generation index alone — no mass.** A rank-2 bounded symmetric domain has exactly **rank+1 = 3** Korányi–Wolf boundary orbits (F86): interior/bulk, Cartan slice, Shilov. The three generations occupy them **in order** — gen-1 = bulk, gen-2 = Cartan slice, gen-3 = Shilov — and the ν-labels {5/2, 3/2, 0} are the ρ-vector components {n_C/rank, N_c/rank} plus the Shilov point 0 (K295: half-sum-of-roots + Shilov, pure representation theory). **Zero mass enters:** the assignment is "3rd generation = highest orbit = Shilov," a counting fact about the orbit stratification, not a Yukawa reading.

**The direction of causation is the (c)-guard, and it runs the safe way.** ν → radius → Yukawa. The top sits at the Shilov boundary because it is **gen-3 = ν=0 = the highest Korányi–Wolf orbit** (rep theory); its saturation **y_t=1 is the CONSEQUENCE** of a boundary-pinned mode's overlap hitting the Cauchy–Schwarz ceiling (F889), not the cause of its position. So "top at the boundary" is a rep-theory fact, and saturation is downstream — the (c)-guard holds **provided ν=0 is read from the orbit count, not from y_t=1.** That is the one thing to hold, and it holds.

## Gate C — my mechanism is NOT the dead one (grep confirmed)
Cal flagged: a "physical symmetry" negative on {5/2,3/2,0} exists. It does — **K1221**: the Koide **isotropy / B₂-Weyl-symmetry** route (the Gram-matrix-isotropy mechanism for A²=rank) is **FALSIFIED, both routes dead** (F839: forced positions break Weyl symmetry, isotropy NOT forced). **That is a different object.** My use here is the **radial SEPARATION** between strata driving the mixing OVERLAP — the **F85-radial boundary-concentration** mechanism (K1314, **Derived**): the same boundary-concentration that grows the diagonal coupling makes a boundary-pinned mode barely overlap its interior neighbor → off-diagonal suppressed. **F85-radial (live, Derived) ≠ Koide-isotropy (dead).** I am not resurrecting the dead mechanism; the ν-stratum's radial position is a Korányi–Wolf/F85 fact, its Weyl-isotropy (the dead thing) plays no role in the overlap.

## The ν→radius map, exhibited (representation theory, mass-free — the FORM; the constant is Elie's blind FK compute)
A discrete-series mode at Wallach parameter ν on the Lie ball has a radial probability density set by the reproducing structure (genus p = n_C = 5, K1217):
$$\varrho_\nu(r) \;\propto\; r^{\,2a-1}\,(1-r^2)^{\,b(\nu)}\,,\qquad \text{peak at } r_\ast^2(\nu) \text{ solving } (2a-1)(1-r^2) = 2\,b(\nu)\,r^2,$$
with $a$ fixed by the real dimension of the KW orbit and $b(\nu)$ the ν-dependent boundary exponent (from the discrete-series address structure — this is the "cross-ν exponent" K1314 flagged, which the two-point $\Gamma_\Omega$'s force to differ from the diagonal $\Delta=3/2$). The load-bearing PROPERTIES — all mass-free, all I need for Gate B:
1. **$r_\ast(\nu)$ is monotone DECREASING in ν** (higher ν = more interior). So $r(5/2) < r(3/2) < r(0)$.
2. **$r_\ast(0) = 1$** — the ν=0 stratum IS the Shilov boundary (the density's mass sits at $r=1$). Fixed by the stratum, not by y_t.
3. The three generation radii $\{r(5/2), r(3/2), r(0)=1\}$ are then three numbers **read off ν**, no Yukawa.

**The Cabibbo prize, structurally:** V_us = the overlap of the gen-1 (ν=5/2) and gen-2 (ν=3/2) coherent modes through the one-rung current, which depends ONLY on $r(5/2)$ and $r(3/2)$ — both mass-free. Because in this geometry **one radial coordinate does two jobs** (F85-radial: radius sets both the diagonal Yukawa AND the off-diagonal overlap), the SAME 5/2↔3/2 separation that gives the overlap gives $m_s/m_d = 20$ — so $V_{us} = 1/\sqrt{20} = \sqrt{m_d/m_s}$ (Gatto) is **one radial coordinate read two ways**, and its appearing on both sides is the unification, not a round trip (position is forced first; mass is downstream).

**What I do NOT do:** I do not tune $a$, $b(\nu)$, or the constant to hit Elie's $p_u = 0.3713$ or to hit $1/\sqrt{20}$. That would be fitting. **The FORM + mass-freeness is the (c)-guard deliverable (Gate B); the NUMBER is Elie's blind FK computation (Gate A)** with the exact genus-5 kernel and the forced $b(\nu)$ from the two-point $\Gamma_\Omega$ address structure. If his blind $r(5/2)$ lands at 0.3713 (0.49% bar) and the 5/2↔3/2 overlap at $1/\sqrt{20}$, the prize is real; if the forced $b(\nu)$ misses, that is the honest boundary of the sector, localized to the cross-ν exponent.

## G3 closes with the same object (Keeper's "predicted two rounds ago")
The neutrino reconciliation is now the SAME map read on both gradings: shelf = SO(2)-level {0,2,4} (pinned = h-power, F1018) and center = ν-stratum {5/2,3/2,0} (this note), related by ν = 5/2 − level/2 (interior) + Shilov ν=0. The ν→radius map I exhibit here gives the neutrino radii too; the massless ν₁ sits at level-0 = ν=5/2... — no: the massless neutrino is at ν=0 (Shilov, m₁=0), consistent with r=1 being the "zero-mass ground" only for the neutrino Majorana branch (T1985). **G3's three-part bar:** forced-not-fitted ✓ (ν from the orbit count); one-rule-whole-grid ✓ (the single r(ν) map for all three); same-massless-mechanism — hand to Grace to confirm the ν=0 ↔ m₁=0 identification holds on both the charged and neutrino readings (the one place the two parametrizations must agree, and the pin says they do at the zero).

## RUBRICS Layer-2 done-bar
- [x] Exhibited the ν→radius map's FORM from rep theory (discrete-series radial density, genus n_C=5, monotone-decreasing r(ν), r(0)=1 Shilov) — mass-free.
- [x] Nailed the (c)-guard (Gate B): the map is r(ν) NOT r(y); ν forced by the generation index / KW-orbit count (F86), not by Yukawa; causation runs ν→radius→mass (saturation is downstream); the (c)-guard holds iff ν=0 is read from the orbit count, which it is.
- [x] Confirmed Gate C: my radial-separation is F85-radial (Derived, K1314), NOT the dead Koide isotropy/B₂-symmetry route (K1221/F839) — different object, dead mechanism not resurrected.
- [x] Refused to fit: I do NOT tune to 0.3713 or 1/√20; the FORM+mass-freeness is mine (Gate B), the NUMBER is Elie's blind FK compute (Gate A). Handed the exact object (genus-5 kernel, forced cross-ν exponent b(ν)).
- [x] Closed G3 with the same map (shelf↔center↔radius, one r(ν)); handed Grace the ν=0↔m₁=0 same-massless check. Nothing banks until Elie's blind number lands at the 0.49% bar. Nothing pushed; CP existence-only.

## Handoffs
- **@Elie (Gate A, the number)** — compute the coherent-state radial center on the T2517 ν-stratum: $r_\ast(\nu)$ = the peak of the genus-5 discrete-series radial density $\varrho_\nu(r)\propto r^{2a-1}(1-r^2)^{b(\nu)}$, with $b(\nu)$ the FORCED cross-ν exponent from the two-point $\Gamma_\Omega$ address structure (NOT the diagonal Δ=3/2 — K1314 flagged the cross-ν differs). Post $r(5/2), r(3/2), r(0)$ **blind**; check $r(5/2)$ vs 0.3713 (0.49% bar) and the 5/2↔3/2 overlap vs $1/\sqrt{20}$. **Do not use any Yukawa to set the radius — only ν.** Tau (ν=0) is the Shilov boundary: use the Szegő/oscillation measure (F666), not the interior peak.
- **@Cal (Gates B, C)** — (B) the (c)-guard: the map is r(ν), and ν is forced by the KW-orbit count (F86 rank+1=3, "3rd gen = Shilov"), not by y_t=1 — saturation is the downstream mass consequence of ν=0. Audit that the "3rd gen = highest orbit" assignment references the orbit stratification, not the mass ordering. (C) confirmed: F85-radial (Derived) ≠ the dead Koide isotropy (K1221); my overlap uses radial separation, not Weyl-isotropy. The one residual for you: is the cross-ν exponent $b(\nu)$ itself forced (two-point $\Gamma_\Omega$), or a knob? If a knob, Gate A is a fit.
- **@Grace** — the shelf≠center split is exactly your call; the tweak-radius 0.378 vs my ν-stratum r(5/2): your three-part bar decides if they're the same object or a coincidental neighbor (Cal's null: 10 routes within 1%). And G3: confirm ν=0 ↔ m₁=0 on both the charged and neutrino readings (the same-massless leg).
- **@Keeper** — the (c)-guard escape is exhibited: r(ν), not r(y); ν forced by the KW-orbit count (mass-free); saturation downstream. F85-radial (Derived) is the live mechanism, not the dead Koide isotropy (K1221). The map's FORM + mass-freeness is Gate B (mine); the NUMBER is Elie's blind Gate A. If his blind r(5/2)→0.3713 and the overlap→1/√20 with the forced b(ν), the Gatto prize is real and G3 closes with it. I did not fit; I exhibited and certified mass-free.
- **@Casey** — the whole lane comes down to one honest question, and it's a beautiful one: *where does a quark's distance-from-the-edge come from — its mass, or the geometry?* If it comes from the mass, then "computing the mixing from the distance" is just laundering the mass back out — a round trip, worthless. The escape is that the geometry fixes the distance *first*, with no mass anywhere: a rank-2 bowl has exactly three natural ledges (that's rank+1), the three generations sit on them in order, and "third generation = the rim" is a counting fact about the ledges, not a weight I looked up. The mass is what a mode *does* when it sits on the rim (it saturates), not why it's there. So the arrow runs geometry → distance → mass, never backward — and that's the whole (c)-guard. If Elie then computes the distance from the ledge alone and it reproduces the Cabibbo 1/√20 we already trust, we haven't gone in a circle: we've learned *why* that mixing angle equals a square-root of a mass-ratio — because one distance sets both. I gave the map and certified it's mass-free; I did not tune it to land — that's Elie's blind shot, and it has to land on its own. Nothing pushed.

Notes only; no toy/theorem claimed (map exhibited + (c)-guard). F1021: ν→radius map from rep theory. Map is r(ν) [ρ-stratum→radius], NOT r(y) [Yukawa→radius, superseded K1314 hand-off, circular by (c)-guard]. ν forced by GENERATION INDEX / KW-orbit count (F86 rank+1=3: gen 1/2/3 = bulk/Cartan/Shilov), {5/2,3/2,0} = half-sum-of-roots+Shilov (K295), ZERO mass. Causation ν→radius→mass; saturation y_t=1 is DOWNSTREAM consequence of ν=0 (F889), not input → (c)-guard holds iff ν=0 read from orbit count (it is). Gate C: mechanism = F85-radial boundary-concentration (Derived, K1314), NOT dead Koide isotropy/B₂ route (K1221/F839). Map FORM: genus-5 discrete-series radial density ϱ_ν(r)∝r^{2a-1}(1-r²)^{b(ν)}, monotone-decreasing r(ν), r(0)=1 Shilov; b(ν)=forced cross-ν exponent (two-point Γ_Ω, ≠diagonal Δ=3/2). V_us=5/2↔3/2 overlap=√(m_d/m_s)=1/√20 (one radial coord two jobs, F85-radial). NOT fitting: FORM+mass-freeness=Gate B (mine); NUMBER (r(5/2)=0.3713? overlap=1/√20?)=Elie blind Gate A. G3 closes same map (shelf↔center↔radius); Grace confirms ν=0↔m₁=0. Nothing banks until blind number at 0.49% bar. Nothing pushed. — Lyra
