# K777 — The condensate is a spherical code (circles tiling a sphere = an error-correcting code). Casey's tiling picture closes the loop to the origin — and gives Lyra's 18-polynomial test a geometric answer: the geometry SELECTS the code.

**Keeper | 2026-07-20 | Casey's speculation: the condensate tiles the boundary sphere with circles; the tiling IS the code; the geometry picks it. Web-grounded (Conway–Sloane: packing ↔ code). Tier: FRAME + one addressable LEAD (geometry selects the polynomial). Guards on 127/128 stand.**

---

## The rigorous core: circles tiling a sphere = an error-correcting code
This is not loose — it is Conway & Sloane's central correspondence: **spherical codes** are codewords placed as points on a sphere, and **error-correcting codes ↔ dense sphere packings** (Construction A: a code builds a packing; the packing recovers the code). The Leech lattice = the (24,12,8) Golay code (kissing number 196,560) — the *exemplar*, and BST already lives near it (24 = Leech/M₂₄/Golay). So **Casey's "the condensate tiles the sphere with circles" IS "the condensate is a spherical code"** — the same RS-over-GF(128) code, seen as a packing on the boundary.

## Casey's picture, mapped (frame)
- **The circles/cells = the codewords** = points/regions on the boundary sphere (the code positions, 2^g = 128).
- **Where circles touch (kissing points) = the code's minimum distance = the low-energy hops.** "Where circles touch we have lower energy to pass" = adjacent codewords are the cheapest transitions. (For the LFSR cycle, each state touches its shift-neighbors — the 127-cycle is a *necklace* of tangent cells; the dead state is off it.)
- **The circle boundary = the bump.** Matter sits *inside* a circle (a cell/well); crossing the boundary is the barrier. Angular = 1 (derived) = perfectly seated inside a circle.
- **Two emissions from the circle edges** — **light tangent, neutrino radial.** Light (massless, conformal) propagates *along* the surface (tangent = the conformal/EM boundary, F66); the neutrino escapes *radially* (perpendicular — the deficit/dead-state mode leaving the packing). Two directions = the two currents' escape modes.
- **Cooling = tiling.** As the substrate cools, the circles pack (the code crystallizes, the gaps form) — the nucleation/freeze-out.
- **The tiled boundary = a lower-D mirror of the 3D bulk = holography.** The spherical code on the boundary *encodes* the bulk (the holographic code, K773). Casey's "2D/5D mirror of our 3D continuum."
- **Loop closure:** BST began with *circles tiling a sphere* ("the simplest object that can do physics"). The condensate IS that tiling; the tiling is the code. **Origin = current state** — the circle-tiling was always the condensate.

## ★ THE LEAD — the geometry SELECTS the code (answers Lyra's 18-polynomial test)
Lyra's sharpest open test (round 3): "the substrate IS an LFSR" is under-determined — there are **18 primitive degree-7 polynomials** (φ(127)/7 = 18) over GF(2), and nothing yet forces which one. Her constructive test: is one *forced* (by the K59 cyclotomic mechanism) or *spectrum-selected* — else the LFSR is decorative.

**Casey's circle-tiling gives the geometric route:** if the condensate is a *specific* circle packing determined by D_IV⁵'s boundary geometry, then **that packing = a specific spherical code = a specific primitive polynomial.** So **the geometry selects the polynomial** — the one whose spherical code tiles D_IV⁵'s boundary. This turns the 18-fold ambiguity from "unforced" into a **computable geometric question:** *which of the 18 primitive polynomials generates the packing natural to the Shilov boundary (S⁴×S¹)/ℤ₂?* If a unique one tiles it, the LFSR earns "real"; and the *same* packing fixes the codeword-distances that set the mass spectrum (Q4). This is the strongest addressable lead the code thread has produced — it connects the geometry, the code uniqueness, and the spectrum in one object.

## Where it does NOT help (discipline — held hard, this is peak-convergence)
- **Guards 1 & 2 on 127/128 are UNTOUCHED.** The packing picture is a geometric frame for the code; it does not compute the radial band-edge gap or resolve pole-vs-MS-bar / RG-degeneracy. **127/128 stays a lead.**
- **The full spectrum is NOT solved by "reliability."** Elie's round-3 catch stands: "mass = codeword reliability" is **Froggatt–Nielsen in code language** — an exponential with free charges + free ε fits *any* hierarchy (~10 params to 9 numbers). It has the right *shape* and no *forced* map → a frame, not a prediction. **The packing only helps IF it FORCES the codeword-distances** (via the selected polynomial) — then the spectrum is a *postdiction*, not a fit. That is the bar for Q4, and the geometry-selects-the-polynomial lead is exactly the route to clear it (or to fail cleanly).
- **The neutrino is TWO independent routes, not three** (Lyra's correction): the LFSR dead state = the RS overflow symbol (one code feature, two names); the independent convergence is **code ↔ rank-2 counting**, plus odd-g for the chirality. Still real evidence; not inflated.
- The tiling/emission picture is a **frame** — beautiful, consistent, but adds no derivation on its own.

## Net — where the thread stands after Casey's speculation
- **New addressable lead:** the geometry selects the LFSR polynomial (via the natural boundary packing) — resolves Lyra's 18-fold test *and* forces the codeword-distances for the spectrum (Q4's only escape from Froggatt–Nielsen). **This is the highest-value new direction.**
- **Frame closure:** condensate = spherical code = circle tiling = the origin object; light tangent / neutrino radial; cooling = tiling; holographic mirror. Recognition, not derivation.
- **Unchanged:** 127/128 = lead (Q1 the bottleneck); angular = 1 derived; neutrino = 2 routes; reliability = fits-anything until the polynomial forces the distances.

— Keeper K777, 2026-07-20. Condensate = a spherical code (Conway–Sloane: circles tiling a sphere = an error-correcting code); Casey's tiling closes the loop to the origin ("circles on a sphere" = the condensate = the code). THE LEAD: the geometry SELECTS the primitive polynomial (which of 18 tiles D_IV⁵'s boundary) — resolves Lyra's LFSR test AND forces the codeword-distances that set the spectrum (Q4's escape from Froggatt–Nielsen). Frames: cells=codewords, kissing=min-distance=low-E hops, circle-boundary=bump, light tangent/neutrino radial, cooling=tiling, boundary=holographic mirror. Guards 1,2 stand (127/128 lead); neutrino=2 routes; reliability=fits-anything until distances forced. See [[Keeper_K776_open_questions_register_with_computed_leads_2026-07-20]], [[Keeper_K775_substrate_is_a_shift_register_LFSR_generates_the_code_where_the_thread_goes_2026-07-20]].

## Sources
- Sphere packings ↔ error-correcting codes; spherical codes (codewords as points on a sphere); Construction A; Leech = (24,12,8) Golay. [Conway & Sloane, *Sphere Packings, Lattices and Groups*](https://www.gbv.de/dms/goettingen/245890696.pdf); [Spherical code — Error Correction Zoo](https://errorcorrectionzoo.org/c/spherical)
