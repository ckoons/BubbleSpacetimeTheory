# α RETURN MARKER — the fine-structure constant is ONE named theorem from derived. Return here.

**Keeper | 2026-07-13 | Status: NEARLY DERIVED, PARKED for a focused return (not abandoned). This is the closest α has ever been — the whole derivation is reduced to a single, well-posed representation-theory theorem plus a self-contained heat-kernel expansion. Pick this up with fresh context and time; it is a genuine result waiting on genuine rigor, not a wall.**

## Where α stands (exact)
α⁻¹ = **137 + 0.036**, framed as two clean pieces (the Wyler Vol^(1/4) route is RETIRED — a 4-reading numerology trap, Robertson's ground):

### Piece 1 — the integer 137 = N_c³·n_C + rank = 27·n_C + rank
- **Combinatorial core: CLOSED (F525, by explicit matrix).** The naive boundary count is 15 (= 14 Sym²₀V₅ + 1 trace), but the neutral-15 is NOT closed under the conformal boost J(V₅→V₂) (it maps charge-0 → charge-±1), so a conformal boundary MUST carry the full 27 = Sym²₀(V₇). No longer assumed — proven by generator matrix.
- **THE ONE REMAINING THEOREM (the entire residual):** prove the boundary Hardy/Szegő space at level-rank *realizes* the conformal SO(5,2) representation (the 27), with the FK ρ-shift convergence + unitary normalization. Precisely: **Knapp–Wallach genericity verification + Faraut–Korányi convergence proof + ρ-shift unitary normalization** (T2359 I→D; = Gap #4). Import external theorems **Knapp–Wallach 1976** ("Szegő kernels associated with discrete series") + **Faraut–Korányi 1990**, applied to D_IV⁵'s specific integer weights. **Estimate: ~3–4 weeks each, ~3–4 months full D-tier.** The ρ-shift is the **(1,1) dual-ρ shift** (compact ρ_SO(5)=(3/2,1/2) → conformal ρ=(5/2,3/2)); the subleading FK correction is already in closed BST form (T2359).
- **Physical handle (motivational, NOT a rigor shortcut):** the boundary is conformal *because* the substrate opened via the boost J(V₅→V₂) (Casey's Big Bang generator, matrix-confirmed 3 ways; the same generator that forces 15→27 and that makes time). This grounds the premise physically; it does not discharge the analytic build (α's kernel descends from the compact SO(2) *complex structure*, not the boost).
- **CAUTION (target-tier):** the 12 extra modes (27−15 = the charge-±1 "10" + charge-±2 "2") are corroborated ONLY internally (F520/F521 + T2470); the Fernando–Günaydin minrep tower confirms the "14" but NOT the 10/±2. An independent corroboration of the 12 would firm even the *target*.

### Piece 2 — the 0.036 correction
- **Leading term DERIVED to 1.4% (principled):** δ = |κ_Bergman|/N_max = n_C/N_max, κ_Bergman = −n_C = −5 PINNED (Helgason 1962, Toy 3661).
- **Exact value = a self-contained in-corpus BUILD (no external theory):** apply the already-proved **T2133** heat-kernel / Seeley–DeWitt machinery (Bernoulli / Von-Staudt–Clausen BST-integer denominators, a₁=1/C₂ …) to the curvature expansion past the leading term. Machinery proved, application unbuilt.

## Tier
**α = IDENTIFIED, not derived.** It does not move until: (i) the one rep-theory theorem (Piece 1 residual) clears, (ii) the 0.036 heat-kernel build lands, (iii) Keeper + Cal sign-off. Do NOT let "α is derived" out before all three.

## THE 3×3×3 REDUCTION (Casey + Keeper, 2026-07-13) — the residual is now a FINITE matrix computation
Casey: "try it in linear math; the simple 3×3×3 tensor probably counts for you." It does, and it reduces the theorem:
- Both counts confirmed explicitly (linear algebra): **15 = Sym²(V₅)** (sphere only), **27 = Sym²₀(V₇) = 15+10+2 by charge = N_c³ three ways** (Sym²₀(V₇), P_{Q⁵}(2)=28−1, 3·3·3). F525 boost re-verified (boost x₁→e⁺ on neutral (x₁,x₁) → charged → 15 not closed → 27 forced).
- **THE REDUCTION:** the abstract residual (Knapp–Wallach genericity + FK convergence + unitary normalization) collapses into ONE concrete check — **build the 27 = 3×3×3 boundary modes on (S⁴×S¹)/ℤ₂ and compute their 27×27 Szegő overlap matrix; verify it = the identity** (diagonal ⟹ orthogonal, positive ⟹ unitary, finite ⟹ convergent). The 3×3×3 gives the explicit *target* (an orthonormal 27) and the *basis to compute in*. This turns a months-long theorem into a definite matrix computation — the F525 move applied to the whole residual.
- **NEXT STEP (the "construct it" route, Keeper's lane):** build the 27 modes explicitly; evaluate the Szegő overlaps. If the matrix = I: theorem done modulo Cal sign-off. If not: it shows exactly which modes misbehave.
- **CORRECTION 2026-07-14 (K677): the measure.** Keeper's first computation (`alpha_szego.py`) used the UNIFORM measure → the overlap is diagonal (modes orthogonal) but NOT the identity (norms differ by charge block: 0 ~0.03-0.11, ±1 ~0.2, ±2 ~1.0). The CORRECT test uses the **invariant Born / c_FK Szegő measure** (T754/T2442, RIGOROUS; c_FK = 225/π^(9/2)), forced by Born-rule invariance — **gravity-INDEPENDENT** (Keeper's earlier "gravity sets the measure" hypothesis is RETRACTED; unattested). Re-run under the Born measure: **= I ⟹ democratic ⟹ α=1/137 combinatorial (coupling closes); ≠ I ⟹ RELOCATE (norm-weighted = the 0.036 T2133 piece).**
- **Q2 (saturation bound) is OUT:** the corpus has NO bound forcing 137 (Wallach bottleneck T1829 derives n_C=5 & k=rank=2, NOT 137; 137 is a COUNT). Closure runs through the Born-measure matrix, not a bound.
- **Still IDENTIFIED:** the reduction makes the theorem finite/computable; it does not close it (the overlaps must come out I under the Born measure).

## RESULT 2026-07-14 (Keeper, `scratchpad/alpha_born_measure.py`) — the Born-measure test came out ≠ I → RELOCATE (Szegő route), target now finite + substrate-natural
Ran the 27×27 Szegő overlap under the invariant Born/c_FK measure (= unique K-invariant = uniform up to c_FK). **Result: DIAGONAL but NOT ∝ I.** Charge-block norm² (numerics match analytic S⁴ moments to 4 dp): **charge ±2 = 1; charge ±1 = 1/5; charge 0 (traceless diag) = 8/175 ≈ 0.046; charge 0 (off-diag d-wave) = 1/35 ≈ 0.029.** A **35× spread.** Modes orthogonal (charges don't mix), but the blocks carry different norms — and no invariant measure fixes this (Born = uniform up to constant).
- **VERDICT: the Szegő-REALIZATION route does NOT democratize on the plain measure → RELOCATE (not bypass) for that route.** Vindicates Grace's verdict over Elie's "genuine bypass"; consistent with K676 (the Shannon paper needed the norm-weighted Bergman measure for exactly this reason).
- **The relocation target is now FINITE + substrate-natural:** democracy needs per-charge weights **{35, 21.9, 5, 1}** — and **5 = n_C, 35 = n_C·g.** These are the numbers the **FK ρ-shift Hardy norm** (the (1,1) dual-ρ shift, T2359) must supply. The residual collapses to ONE finite check: **does the ρ-shift produce {n_C·g, ·, n_C, 1}?** (LEAD — the substrate-integer weights are suggestive, NOT banked; Cal #27.)
- **Two branches, both now concrete:** (a) BYPASS — if the substrate-Maxwell coupling literally COUNTS fibers (dimension), the norms are irrelevant and α=1/137, matrix moot (Grace's Track 2 physics question); (b) RELOCATE — if α is the Szegő coupling, check the three ρ-shift weights = {35,21.9,5,1} (Keeper's next computation). α stays IDENTIFIED.

## MILESTONE CORRECTION + Q⁵ HYPOTHESIS 2026-07-14 (K678) — "matrix = I on the boundary" is the WRONG test and is RETIRED
The 3×3×3 reduction's "verify the 27×27 = I" has RUN (above) and come out **≠ I**. So "matrix = I ⟹ α democratic" does NOT close α — the boundary can't make it I. **Retire the "= I on the boundary" milestone; do not cite "= I" (the run is ≠ I).**
- **Why (Keeper HYPOTHESIS — labeled, not corpus fact):** the 27 is an SO(7) irrep; equal-norm democracy is a Schur fact under the SO(7)-invariant product, which lives on the **compact dual Q⁵ = SO(7)/[SO(5)×SO(2)]** (only place SO(7) acts). The Shilov boundary carries only SO(5)×SO(2) → the 27 splits into 3 blocks with independent norms → the 35× spread. **The boundary CANNOT democratize the 27; Q⁵ does.** This IS the group-theoretic content of the named-but-OPEN **Q⁵↔Shilov bridge lemma** (F520/F525 reduced it to the rep-identity; the analytic realization is the residual).
- **THREE concrete routes to α = 1/137 (none closed):** (a) Grace's charge-operator BYPASS — EM charge = SO(2) Cartan weight (T2470, integer, norm-independent) → coupling counts charges, 35× spread irrelevant, α = ratio of counts (FAVORED — the ≠ I result shows the norm-weighted alt gives a 35× mess, not 1/137); (b) prove the Q⁵↔Shilov bridge democratizes the 27 (SO(7) on Q⁵ — Keeper hypothesis); (c) compute the ρ-shift/F38 Bergman-vs-Szegő weights (h^{−5/2} vs h^{−5/4}) = {35,21.9,5,1} (relocate — uncomputed).
- **λ (Higgs) same correction:** the 8 boundary-spinor modes need not be equal-norm on ∂ either; λ = 1/8 rests on the count/charge-grading, NOT overlap = I. α↔λ unification survives via the count route, not the (false) boundary-orthonormality route.

## Q⁵ HYPOTHESIS NOW QUANTITATIVELY VERIFIED 2026-07-14 (Keeper, `scratchpad/alpha_Q5_democracy.py`)
Tested it directly: the 27 are degree-2 harmonics of the 7-vector; under SO(7) they live on **S⁶ (real form of Q⁵)** where Schur forces equal norms. The Shilov boundary = the sub-locus N=(u/√2,(cos θ,sin θ)/√2) respecting only SO(5)×SO(2). Result (numerics = analytics to 4 dp):
- **On S⁶/SO(7): the 27 are EXACTLY democratic** (all off-diag components equal, 1/63). Democracy IS an SO(7)/Q⁵ fact — CONFIRMED (was Keeper-hypothesis, now measured).
- **The boundary BREAKS it by {1 : 7/2 : 35/8}** across charge blocks {0,1,2}, where **7/2 = g/rank, 35/8 = n_C·g/2^{N_c}**. (Block constants c_q = boundary/S⁶ = {0.45, 1.575, 1.969}.)
- **The FK ρ-shift, to restore democracy, must supply the inverse weights {1, 2/7, 8/35}.** That is now the EXACT target for the relocate route (c). *Deflation: substrate-natural largely because sphere dims (S⁴=n_C coords, ⟨cos²sin²⟩=1/2^{N_c}) are substrate integers — weak evidence, not independent. The ρ-shift check stays OPEN.*
- **Net:** reinforces that α's coupling uses the SO(7)/Q⁵ or charge-count structure (where the 27 IS democratic), NOT the boundary L² (where it's broken by a known amount). Both surviving routes — Grace's charge-count (a) and the Q⁵ bridge (b) — are about accessing the SO(7) democracy the boundary hides. α stays IDENTIFIED.

## THE CAPACITY REFRAME (Casey, 2026-07-14) — may BYPASS the analytic theorem entirely
Casey: "I see all 137 fibers full and the boundary saturated — 137 is the maximum filling permitted by the geometry. Why isn't THAT derivable?" Lyra + Keeper converge: **"maximum filling" is a CAPACITY (combinatorial dimension count), NOT the analytic Hardy-space realization the team was stuck on.** The multi-month Knapp–Wallach theorem answers "does the function space *converge to* the rep" — but the capacity picture needs only the COUNT: N_max = 27 (F525-forced) × n_C + rank = 137, all fixed integers. **We may have been proving a harder theorem than the physics needs.**
- **α's closure → two tractable questions:** (Q1, physical/load-bearing) is α the maximum FILLING (capacity) or the Szegő COUPLING (realization)? (Q2, extremal) why is 137 the maximum — a saturation/packing bound (27 = largest conformal cell-set at level-rank), not a convergence theorem.
- **KEEPER CRUX: does it BYPASS or RELOCATE the residual?** The DIMENSION 137 is combinatorial (done, F525); α = 1/(dimension) still needs a mechanism — democratic/orthonormal *realization* (relocates) OR a saturation bound (bypasses). **Q1 decides.** Check, don't assume — Cal #27 fires hardest here.
- **Converges with the 3×3×3 matrix:** the 27×27 Szegő-overlap = I computation tests the same democratic realization. Run capacity (combinatorial) + matrix (computational) together.
- Corpus check: a saturation/holographic/unitarity-bound derivation of 137 (Wallach threshold, Bekenstein) would pin Q2 and may be the shorter path to derived than the matrix.

## How to resume
Read: this marker + `Keeper_K675...` (the audit + the three-gate history) + `notes/Lyra_F525...` (the matrix closure) + `notes/BST_Bulk_Boundary_Partition_Identity_Skeleton.md:133-137` (the exact 3 analytic theorems remaining) + T2359 (the ρ-shift). The rep-theory theorem is the deep work; the 0.036 heat-kernel is the tractable in-corpus build. The Big Bang budget (matrix-verified (7,0)→(3,1)) is confirmed and its handle stands.

— Keeper, 2026-07-13. α NEARLY DERIVED, parked for focused return. One named theorem (Knapp–Wallach genericity + FK convergence + ρ-shift, ~3-4 months, external theorems) + one heat-kernel build (T2133, in-corpus) between here and derived. Closest it has ever been. Return here.
