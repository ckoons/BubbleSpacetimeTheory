# LANE A — confinement property tests: RESULTS (L1 CONFIRMS, L2 REFUTES — reported beside)

**Two pre-registered membership questions, run as property tests, never mass computations (mass gate closed). One confirms, one refutes — and the refutation is the more useful result.** Reconnected first (grep the proved theorem): T1930 (color = short-root spaces, m_s = N_c = 3), F727 (charged leptons color-singlet, V₁₂=0), T2517 (generation addresses {5/2,3/2,0} radial), round-4 Šilov (λ₂>0 ⇒ zero boundary), F603/OP-6 (rank-1 (1,0) spherical = Higgs).

## L1 — is the color block V₁₂ at λ₂>0? **CONFIRMED (DERIVED-structure).**

**The membership chain, each step banked:**
- **color lives in the short-root spaces** (T1930): short-root multiplicity m_s = N_c = 3; the three short-root directions per short root are the SU(3)-color fundamental. The color SO(3) = SO(V₁₂) acts on the short-root space.
- **the short-root axis is λ₂** (round-4 / B₂): a K-type (λ₁, λ₂) carries short-root weight ⇔ **λ₂ > 0**.
- **therefore: colored ⇔ λ₂ > 0 ⇔ confined** (round-4: λ₂>0 ⇒ zero Šilov value). Color-nonsinglet modes carry short-root weight, so they never reach the boundary.

**⇒ The confinement projection's kernel IS the colored sector.** Color confinement is a *reading of one projection* Π_Š on D_IV⁵: **colored = short-root (λ₂>0) = confined = zero Šilov value; color-singlet (λ₂=0) = emitted = reaches the boundary.**

**Two independent cross-checks (both banked, both agree):**
- **charged leptons are color-singlets** (F727, V₁₂=0, live in the frame ℝc₁⊕ℝc₂) → λ₂=0 → **emitted** (leptons are free/observable, not confined). yes
- **the Higgs condensate** = rank-1 O = SO(5) vector **(1,0)** (F603/OP-6) → λ₂=0 → spherical, **boundary-reaching** (an emitted, observable condensate). yes

**Tier: DERIVED-structure.** The identification "confinement kernel = colored sector" composes two banked theorems (T1930 + round-4); it is a *property* statement (which modes are confined), not a spectrum/mass claim. Target-innocent. This is the geometric statement of color confinement, and it survives its cross-checks.

## L2 — is the ν=0 generation in the confinement kernel? **REFUTED — and the refutation clarifies.**

**Pre-registered form:** "m₁=0 ⇔ the ν=0 generation ∈ ker Π_Š (λ₂>0)." **Run:**
- **factual correction first:** ν=0 is the **tau** (Šilov-point address, T2517), the *heaviest* charged lepton — **not** the lightest (I mis-stated it as lightest in the pre-registration; owned).
- **the tau is a charged lepton ⇒ color-singlet** (F727) ⇒ **λ₂ = 0** ⇒ **emitted**, *not* in the λ₂>0 kernel.

**⇒ L2 REFUTES: the ν=0 generation is emitted, not confined.** And the reason is structural and worth banking: **the confinement axis (λ₂, short-root / color) is *orthogonal* to the generation / mass axis (ν, radial / Korányi–Wolf stratum).** My lead conflated two different coordinates:
- **λ₂** = the SO(5) short-root weight (angular, color) — sets *confinement*;
- **ν** = the radial boundary-orbit address {5/2, 3/2, 0} (the Cartan-slice / KW stratum) — sets the *generation*.

A charged lepton runs down the ν-axis at fixed λ₂=0 (always color-singlet, always emitted). So **the "m₁=0 engine" is a *radial* (ν) rank-1 phenomenon — the rank-1 condensate (1,0), itself λ₂=0 — and is NOT the λ₂-confinement kernel.** The corpus caption "confinement, m₁=0 engine" bundles two distinct mechanisms on two distinct axes; this test separates them.

**Tier: REFUTED lead → a structural clarification** (two orthogonal axes). No mass computed. Reported beside L1 (C6 discipline: the miss beside the hit).

## What promotes, what the two axes are

| reading | axis | tier |
|---|---|---|
| color confinement = ker Π_Š (λ₂>0 = colored) | **λ₂** (short-root / color) | **DERIVED-structure** (L1) |
| generation / m₁=0 rank-1 condensate | **ν** (radial / KW stratum) | separate (mass gate closed) |
| the two axes are orthogonal | — | **DERIVED** (L2 refutation — a clarifying result) |

## Ledger update (this note)

- **A-L1 → HIT (Derived-structure):** colored ⇔ λ₂>0 ⇔ confined; leptons (color-singlet, λ₂=0) and Higgs (1,0) emitted — cross-checks pass.
- **A-L2 → MISS (refuted, reported):** ν=0 (tau) is color-singlet → emitted, not confined; confinement (λ₂)  perp  generation (ν). Owned the "lightest" mis-statement.

**Lyra, 2026-08-21 (Lane A, confinement property tests). L1 CONFIRMED at DERIVED-structure: color confinement = the round-4 projection kernel — colored ⇔ short-root ⇔ λ₂>0 ⇔ zero Šilov value ⇔ confined (T1930 + round-4), with two banked cross-checks (leptons color-singlet emitted; Higgs (1,0) emitted). L2 REFUTED: ν=0 is the tau (color-singlet, λ₂=0, emitted) — NOT in the kernel; the confinement axis λ₂ is ORTHOGONAL to the generation/mass axis ν; owned the mis-stated 'lightest.' The refutation is the useful result — it separates two mechanisms the corpus bundled. Membership tests only, no masses. Nothing pushed; CP existence-only.**
