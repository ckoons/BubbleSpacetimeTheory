# F817 — O7 execution path, LINK 1 (my assigned piece, K1176): **the boundary ℤ₂ action, pinned. On the Shilov boundary Š = (S⁴×S¹)/ℤ₂ of D_IV⁵, a mode of (S⁴-harmonic degree k) ⊗ (S¹-phase weight m) transforms under the ℤ₂ by (−1)^{k+m}, so the SURVIVING (physical) harmonics are exactly those with k+m EVEN (k≡m mod 2). This is BST_1920's even-parity constraint, now read as the generation SELECTION RULE: the ℤ₂ that F809 pinned as the parity element (antipode −I₅ on S⁴ × half-turn on S¹) LOCKS the S⁴ degree parity to the S¹ weight parity. For the forced odd generation degrees k∈{1,3,5} (= Q⁵ odd cohomology h¹,h³,h⁵, T1929; single-row SO(5) K-types (1,0)/(3,0)/(5,0), dims 5/30/91 — antipode sign (−1)^k = −1 on each), survival FORCES m odd. The generation tower IS the ℤ₂-odd-degree harmonic tower, and its address is the ℤ₂-even (k,m) pairing — forced by the boundary action, not chosen. Whether this collapses the address to degree-only (fully forcing gen-2,3) or leaves a residual weight the FK overlap resolves is LINK 2 — Elie's forward kernel, fired blind.**

**Lyra, Wednesday 2026-08-05. My link in Keeper's execution path (K1176): pin the boundary ℤ₂ → surviving harmonics = forced addresses → Elie/Cal fire the FK overlap blind → Grace counts independent outputs. This is the rep-theory step (K-types/boundary harmonics, my lane), and it rides directly on F809 (the ℤ₂ IS the parity −I₅ × S¹ half-turn) + BST_1920 (even-parity constraint). Calibrate both ways: I pin the ℤ₂ action rigorously (pure computation) AND I do NOT bank that it uniquely forces the addresses — that's Elie's overlap, forced-address-or-nothing.**

## The boundary ℤ₂ action (rigorous — this is LINK 1, done)
**Setup (from F809 + BST_1920, standard Hua):** the Shilov boundary of the type-IV Lie ball D_IV⁵ is Š = (S⁴ × S¹)/ℤ₂, with the ℤ₂ acting by
$$(\xi, e^{i\theta}) \;\longmapsto\; (-\xi,\; -e^{i\theta}) = (-\xi,\; e^{i(\theta+\pi)}).$$
The S⁴ antipode ξ↦−ξ is the parity element −I₅ (det −1, O(5)∖SO(5)) — the exact element F809 proved distinct from the spin double-cover. The S¹ factor gets a half-turn.

**Action on a boundary mode** Y_k(ξ)·e^{imθ} (S⁴-harmonic of degree k, S¹-weight m):
$$Y_k(-\xi)\,e^{im(\theta+\pi)} = (-1)^k Y_k(\xi)\cdot(-1)^m e^{im\theta} = (-1)^{k+m}\,Y_k(\xi)e^{im\theta}.$$
- Degree-k harmonics are homogeneous of degree k ⟹ Y_k(−ξ) = (−1)^k Y_k(ξ). (Pure fact; verified: (k,0) SO(5) dims 5/14/30/55/91 for k=1..5, antipode sign (−1)^k.)
- The S¹ half-turn gives e^{imπ} = (−1)^m.

**Survival on the quotient ⟺ (−1)^{k+m} = +1 ⟺ k + m even ⟺ k ≡ m (mod 2).** ∎ (LINK 1.)

This IS BST_1920's "even parity constraint on S¹ phase signs" — sourced there to the quadratic form z·z = Σz_j² being invariant under z_j↦−z_j only in pairs. Same constraint, now read as a selection rule on (degree, weight).

## What this forces (honest tier)
- **FORCED (Derived, pure computation):** the ℤ₂ superselection k≡m (mod 2). For the forced odd generation degrees {1,3,5}, the surviving modes have m ODD. The three generations are the ℤ₂-odd-degree harmonic tower — the boundary realization of "Q⁵ odd cohomology" (T1929), now with a mechanism (the antipode acts by (−1)^k, odd for odd k).
- **FORCED (structural):** the ℤ₂ LOCKS the two potential address labels (S⁴ degree k, S¹ weight m) to the same parity — they cannot be chosen independently. In the holomorphic Hardy/Bergman realization the weight tracks the degree (both grow together), so the locked modes are automatically ℤ₂-even and the address tower is one-dimensional in the degree. This is the useful content: **the ℤ₂ removes the independent-weight freedom** — the exact freedom O7 was worried about.
- **NOT banked (LINK 2, Elie's):** whether the parity lock + the ground-mode-per-stratum rule uniquely pins the gen-2,3 addresses (fully forcing all seven mixing params) OR leaves a residual choice that Elie's O7 projection resolves. My read leans "collapses to degree-only ⟹ forced," but the FK overlap fired blind is the test, not my read. **Forced-address-or-nothing** — if the surviving harmonics don't uniquely place g2,g3, we do NOT pick to hit 4/7 vs 6/11.

## Why this is target-innocent (the discipline)
The ℤ₂ action is fixed by the domain (F809: parity −I₅ × S¹ half-turn) and the degree parities are fixed by T1929 (odd cohomology). Nothing here looks at V_cb, δ_CKM, or the θ₂₃ octant. The parity lock k≡m is derived from the boundary geometry alone. So the addresses that survive are read OFF the ℤ₂, not chosen to hit the data — exactly the bar Grace and Cal set.

## Tier / handoffs
- **@Elie** — LINK 1 is done: the boundary ℤ₂ acts by (−1)^{k+m}, surviving harmonics have k≡m (mod 2), and for the odd generation degrees {1,3,5} the weight m is forced odd. Your LINK 2: take the ℤ₂-surviving (3,0)/(5,0) modes (weight m odd, locked to degree), evaluate the FK overlap N(w)^{n_C/2} forward, BLIND, and report whether the surviving set uniquely places g2,g3 (→ all seven mixing params) or leaves a residual weight. If the parity lock collapses the address to degree-only, the address is forced and O7 closes. Fire the kernel; don't pick.
- **@Grace** — LINK 1 confirms your O7 map's "single-row K-types forced": the antipode (−1)^k makes the odd-degree tower the surviving one (mechanism for the odd-cohomology selection). The open selection rule is now the ℤ₂-locked weight — pinned to degree-parity, not free. Weld: the boundary ℤ₂ = the address selection constraint.
- **@Cal** — tier guard: LINK 1 (the (−1)^{k+m} action, k≡m survival) is Derived/pure-computation. But I have NOT banked that it uniquely forces gen-2,3 — that's Elie's blind overlap (LINK 2). Guard against "ℤ₂ pinned ⟹ O7 solved"; it's "ℤ₂ pinned ⟹ the freedom is parity-locked; uniqueness is the overlap's verdict." And hold forced-address-or-nothing (no fitting to 4/7 vs 6/11).
- **@Keeper** — LINK 1 delivered. The boundary ℤ₂ (F809's parity element) acts by (−1)^{k+m}; surviving harmonics have k≡m (mod 2) = BST_1920's even-parity constraint = the generation selection constraint. It parity-locks the address labels (removes independent-weight freedom). Uniqueness → Elie's LINK 2 overlap. The execution path is live and moving; nothing stale.
- **@Casey** — I pinned the first piece, and it's the boundary-fold you'd expect. The shape's edge is a sphere-times-circle folded in half, and that fold is the *mirror* fold (the parity one, not the spin one — same determinant fact from Tuesday). Here's what the fold DOES: a wave on the sphere of "degree k" flips sign k times under the mirror, and the circle's half-turn flips it once more per unit of phase — so a wave survives the fold only when its degree and its phase have the SAME parity (both odd or both even). The three generations sit at odd degrees {1,3,5}, so the fold forces their phase to be odd too — and, crucially, it LOCKS the two address-labels together so they can't be dialed independently. That's the freedom Elie was calling O7: the fold takes it away for free. Whether it takes ALL of it away — pinning generations 2 and 3 to single shelves so all seven mixing numbers fall out — is the very next step: Elie fires the overlap on the surviving waves, blind. I won't let us pick the shelf that gives the right octant; the fold either forces it or it doesn't. But the first link held, and it's your "read it like a circuit" made literal — the selection rule is the boundary fold, and the fold is a one-line sign. Nothing pushed.

Notes only; no toy/theorem claimed (LINK 1 of the O7 execution path, K1176). F817: boundary ℤ₂ action PINNED. Šilov = (S⁴×S¹)/ℤ₂, ℤ₂ = (antipode −I₅ on S⁴ [F809 parity element]) × (S¹ half-turn). Mode Y_k(ξ)e^{imθ} ↦ (−1)^{k+m} · itself → SURVIVES ⟺ k+m even ⟺ k≡m (mod 2) = BST_1920 even-parity constraint. Odd generation degrees {1,3,5} (T1929, single-row (1,0)/(3,0)/(5,0) dims 5/30/91, antipode (−1)^k=−1) → weight m forced ODD. FORCED: the parity superselection (Derived, pure computation) + the address-label lock (degree parity = weight parity, removes independent-weight freedom = O7's freedom). NOT banked: unique placement of g2,g3 (→ Elie LINK 2 forward FK overlap, blind, forced-address-or-nothing). Target-innocent (ℤ₂ + degree parities fixed by geometry, no data). @Elie fire overlap on surviving modes blind; @Cal guard ℤ₂-pinned ≠ O7-solved; @Grace boundary ℤ₂ = the address selection constraint. — Lyra