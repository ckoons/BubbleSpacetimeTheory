Team LONG PULL — 2026-07-06. THE ARENA SORT: which algebraic arena for which quantity. Headline correction: MASS lives on SO(3) (rest-frame little group = the color/generation triplet), NOT the SO(4,2) conformal Casimir. Build the generation-triplet mass matrix and diagonalize — forward.

`date` + SOD. Count **8 + 2 structural banks** (Peirce, descent). Close-attempt on the down-ladder; honest wall-map is an equally good outcome.

## THE ARENA SORT (Casey's directive: sort which arena for which calculation)
The descent we banked is a **tower**: SO(5,2) ⊃ SO(4,2) ⊃ SO(3,1) ⊃ SO(3). **The arena is set by how the quantity is defined/observed** — and they are NOT all the same:

| observable | defined/observed how | ARENA | operator |
|---|---|---|---|
| 5 integers, Peirce, uniqueness | substrate structure | **D_IV⁵ / SO(5,2)** (d=5) | domain invariants |
| conformal dimension Δ (scaling weight) | how an operator scales in 4D | **SO(4,2)** (d=4) | Δ(Δ−4) conformal Casimir |
| **MASS** | **in the REST FRAME** | **SO(3)** — massive little group = the color/generation triplet (3-dim) | **a MATRIX on the triplet; masses = eigenvalues** |
| mixing (CKM/PMNS) | overlap *between* generations | **SO(4,2) OPE ≅ D_IV⁵ curvature** (holographic dual) | 3-pt coeff / eigenvectors |
| Λ | inter-universe coupling | parent-SO(4,2)/baby interface | heat-kernel action |

**Three headline corrections (the sort):**
1. **MASS is NOT the SO(4,2) conformal Casimir.** That gave the *factors* {12, 45} (scaling dimensions), never the masses. Mass is a rest-frame quantity → **SO(3) little group** (Wigner) → and that SO(3) is the color/generation triplet SO(V₁₂) we already pinned in the descent. Δ(Δ−4) sets the *scaling*; the physical **mass** lives on SO(3).
2. **Mass on SO(3) is a MATRIX on the 3-dim triplet, not a diagonal Casimir.** Elie: a scalar can't compress a 2.25× input (Δ span) into a 900× output — you need off-diagonal structure. So masses = **eigenvalues** of a matrix on the generation triplet. (Grace's trace-kill ruled out only the *diagonal conformal Casimirs* {45,12,0}; a general SO(3) matrix survives.) This unifies Elie's matrix insight + Lyra's diagonalize-the-matrix + Casey's SO(3) arena.
3. **SO(3) is the right-arena signature:** the factors {12, 20} are **consecutive** SO(3) Casimirs (j(j+1) at j=3, j=4) — neighboring rungs of one spectrum. On SO(4,2) they were 12 and 45 (non-adjacent). Numbers stop looking like coincidences on the right arena.

## THE close-attempt — Casey's SO(3) generation-triplet mechanism (build it, diagonalize forward)
Casey's picture, to build and test (his questions, made concrete):
1. **Are the three generations three SO(3) reps at FORCED spins?** (The color triplet: is the lightest the trivial **j=0** rest state, heavier generations at forced higher spins from the KW/color structure?)
2. **Is mass the Casimir or a forced function of it** (so the b-quark's 900 is natural, not an object-switch from j(j+1) to [j(j+1)]²)?
3. **Build the down-sector mass MATRIX on the triplet** (diagonal = generation Casimirs, off-diagonal = the generation overlaps that give the 900× spread) and **diagonalize** — do the eigenvalues land {1, 20, 900} forward?

## Per-CI (long pull — deep, forward-only)
- **GRACE (lead):** build the SO(3) generation-triplet mass matrix — the forced spins (from the color-triplet / KW strata as SO(3) reps) on the diagonal, the forced generation overlaps off-diagonal. Diagonalize. **The spins AND the entries must be forced by the SO(3)/color structure, not chosen to hit {1,20,900}.**
- **ELIE (numerics/target-innocence):** eigenvalue check — does the forced matrix give {1,20,900}? Guard: no object-switch (Casimir vs its square across rungs), spins forced not fitted, Five-Absence. Your "off-diagonal gives the spread" is the mechanism; verify it's forced. Fires on Grace's matrix.
- **LYRA (theorem):** reconcile the two mass-pictures — is your F84 Bergman matrix, restricted to the generation triplet, the SAME as the SO(3) mass matrix (holographic)? Pin whether mass = SO(3) Casimir-matrix eigenvalue is the *forced* mass definition. The operator-ID third-bank rides here.
- **KEEPER:** hold the arena sort (mass=SO(3), mixing=SO(4,2)/dual, dimensions=SO(4,2), substrate=D_IV⁵ — no cross-arena leakage); adjudicate the matrix forward-vs-fit; **watch for over-sell #4** (this thread over-sold 3× today — Grace AM map, Lyra AM rule, Grace PM (k!)²/formal-degree). Bar fires hardest. No object-switch; spins forced.

## Bank condition
Down-ladder banks **8→10** (operator-ID banks with it) iff: the SO(3) generation-triplet matrix — **spins and entries forced** by the color/KW structure — diagonalizes to **{1,20,900}** forward, no object-switch, ground = forced j=0. Partial: if the matrix STRUCTURE is forced but one entry floats → strong lead, not bank.

## Reserved for Casey
- **α tier-call (K659)** — arena-supported (Wyler value on d=5); 8→9 on your ruling.
- **Mass-frame ratification** — clears the deferred data-layer sync (needed if the ladder banks).

## Disciplines armed
The arena sort is the day's real tool — **use the RIGHT arena per quantity, don't leak across.** Mass=SO(3) is well-motivated (rest frame + the pinned color SO(3)) but the SPINS and the MATRIX ENTRIES must be FORCED, not fitted — adding a matrix is adding knobs, and this is the arc's serial over-seller. No object-switch (one function of the Casimir for all rungs). Investigate-don't-gate; use linear algebra (it's been the key every single time — genus swamp, operator flip, and now the arena). Count 8 until the matrix lands {1,20,900} forward with forced entries.

## Long-pull target
The SO(3) generation-triplet mass matrix, built forward, diagonalizes to {1,20,900} (down-ladder + operator-ID bank, 8→10) — OR the wall is mapped: which forced spins the color triplet gives, and precisely why they do/don't reach 900. Both wins. Count 8 until forward.

— Keeper, 2026-07-06 long pull. ARENA SORT: mass → SO(3) (rest-frame little group = the pinned color/generation triplet), NOT SO(4,2) conformal Casimir (that's scaling dimensions); mixing → SO(4,2) OPE/dual; dimensions → SO(4,2); substrate → D_IV⁵. Mass is a MATRIX on the 3-dim triplet (eigenvalues = masses; off-diagonal gives the 900× spread — unifies Elie+Lyra+Casey). Right-arena signature: {12,20} consecutive j(j+1). Build Casey's generation triplet forward (lightest=j=0 rest state?), diagonalize; spins+entries FORCED not fitted; no object-switch; over-sell #4 watch. Count 8 until {1,20,900} forward.
