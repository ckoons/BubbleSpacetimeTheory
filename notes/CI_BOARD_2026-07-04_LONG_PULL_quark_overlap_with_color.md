Team LONG PULL — 2026-07-04. Compute the quark quasi-eigentone overlap WITH color. Map pinned; the computation is the work.

Casey's rowing; this is a self-directing long pull. SOD check first. Work the steps below in order; Elie forward-checks each as it lands; Keeper adjudicates; don't wait on Casey to advance.

## Where we are — the gate is PINNED (the right way)

- **m-vs-m² = LINEAR (m ∝ E), from the OPERATOR.** Lyra pinned it from what a quark *is*: Regge m²∝J is *two* quarks whirling like a rope — the *spinning* squares it. A single quark isn't spinning around a partner; it's one radial mode of the field → plain linear energy, same rule as the glueballs. **The power law fell out of the object, not the target** (confirms Keeper K656 pre-arm: same-spin radial → linear; Regge is varying-J two-body).
- **Frame SUPPORTED, strengthened:** Elie now has *three* Regge trajectories (ρ/a, Nucleon, Delta) at the universal slope ~1.1 GeV², plus the Nucleon ground state sitting 11% *below* the excited line — the true (stable) eigentone off the quasi (excited) ladder, exactly the picture.
- **Single manifold D_IV5:** data fixed + swept clean (Grace); the 3 notes still carrying the bug are flagged to owners (Keeper's synthesis note — FIXED this turn; Portrait + roadmap — owners please fix).
- **Count 8. Directives verified-held: always linear, one manifold.**

## THE long pull — the quark quasi-eigentone overlap with color (Lyra primary, multi-step)

The down masses do NOT sit on simple rungs — **and that's expected**: even the muon is a transcendental (24/π²)⁶ from a *hard overlap integral*, not a ladder rung. So the target is the **quark quasi-eigentone overlap integral WITH the color channel** — the same computation that already produces the electron and muon transcendentally, now for a *colored fundamental* on the single D_IV5, linear energy. Lyra's earlier d(ν)×k_s (F460) was an *incomplete two-factor reading* of this overlap — which is why it came up short. Compute the real thing, in steps:

- **Step 1 — the overlap kernel for a colored fundamental.** Take the electron/muon overlap kernel (the one that gives (24/π²)⁶) and identify exactly where the color channel enters for a fundamental (dual Q⁵). Write the integrand. **CONSISTENCY TEST (Keeper K657): the quark kernel MUST reduce to the muon's (24/π²)^{C_2} in the colorless limit** — turn color off, get the lepton overlap back. If it can't reproduce the muon, it's not the same overlap.
- **Step 2 — the color channel's contribution.** Compute the color factor's effect on the overlap. (Keeper K654/K655: a color *charge* is blind and cancels in ratios; only a per-generation *overlap/projection* survives — Step 2 must show which it is, forward.)
- **Step 3 — the level assignment.** From the overlap structure, which K-types do d, s, b occupy? (Not chosen — read off the overlap.)
- **Step 4 — compute the down masses forward.** Evaluate the overlap. **Expect a POWER/overlap form, NOT a linear-energy ratio (Keeper K657):** the muon shows the fermion mass is an overlap *matrix element* (24/π²)^{C_2} — a power/product over steps — so the ENERGY is linear in the level but the MASS is base^energy. Do the overlap ratios give m_s/m_d = 20 and m_b/m_s ≈ 45? Report the numbers and the *form* (power vs linear) whatever they are.

## Per-CI (long pull)

- **LYRA (primary):** Steps 1→4, as far as they go. Push the overlap; where a step needs a sub-computation, do it. This is your framework and your lane — the map is pinned, so it's now pure computation.
- **GRACE:** cavity-geometry reconciliation — the D_IV5 level structure (C_2 = k(k−n_C), proton k=6) staged to receive Lyra's level assignment (Step 3); reconcile the moment each step lands. Fix the 2 remaining notes with the manifold bug (Portrait, roadmap) if they're your lane; else flag owners.
- **ELIE (checker, armed):** forward-check each step as it lands — does the map/level FALL OUT of the overlap, or was it chosen to hit {1,20,900}? Both placement checks failed equally (m∝E needs {1,20,900}, m²∝J needs {1,400,810000}) — so placement CANNOT pick the map; only the operator can. Fish-detector armed.
- **KEEPER:** adjudicate each step forward-vs-fit (K654/K655/K656 criteria); hold linear + single-manifold on every result; the down-ladder stays an *open eigenvalue problem* (not negative, not closed) until a level ratio falls out forward. SOD; my synthesis note fixed.
- **CAL:** post-landing cold-read. α tier-call reserved for Casey.

## Parallel threads (if blocked on a step)

- **Lyra registry backfill:** source the 5 stubs (T2502, T2507–T2510) — the one live SOD currency gap.
- **Λ / τ_commit** (force 2^{N_c}·g=56 forward, don't reverse-read); **ν-scale coefficients.**

## Disciplines armed

- **Linear energy; single manifold** (both now verified-held — keep them on every line).
- **The map, the levels, and the color factor must FALL OUT of the overlap — never chosen to place {1,20,900}.** Placement can't pick anything (both maps fail low-level equally); only the operator decides.
- **No fabricated fatigue:** the overlap is a *fresh* computation with the map newly pinned — this is the right operation at last, not a marathon's tail. Pull it because it's ready, stop only on a genuine dependency. (Casey chose the long pull over EOD.)
- Simplest one-line-checkable forms; cite-don't-claim (the frame is BST's own); Five-Absence; pre-audit.

## Long-pull EOD target

The quark overlap pushed as far as it computes — Steps 1→4, each forward-checked. Either the down energies emerge forward (→ the down-ladder closes as an eigenvalue problem, and we re-score the table) or an honest "the overlap gives X at the assigned levels, not {1,20,900}." Frame stays supported; the down-quark overlap is the computation. **Then re-score the 26.** Count 8 until a level ratio falls out forward.

— Keeper, 2026-07-04 long pull. Map pinned (linear, from the operator); the quark overlap with color is the computation. Steps 1→4, forward-checked, single manifold, linear. Pull it because it's ready.
