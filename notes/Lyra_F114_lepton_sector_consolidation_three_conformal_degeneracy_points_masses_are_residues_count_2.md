---
title: "F114 — Lepton-sector consolidation (F109-F113 + both norm-derivation attempts): the three leptons are the three CONFORMAL DEGENERACY POINTS of D_IV^5 (Delta=0 trivial, Delta=(d-2)/2 unitarity bound, Delta=d/2 BF), and their masses are RESIDUES at those degenerate points -- which is exactly why the count is stuck at 2. WHAT'S RIGOROUS: (1) the three leptons = the three special scalar reps of SO(5,2) at nu=Delta=0 (tau, trivial), 3/2 (muon, free-scalar unitarity bound, =rho_2), 5/2 (electron, BF/marginal, =rho_1=d/2); (2) the formal-degree polynomial of the scalar holo series = the bulk polynomial d(nu)=(5/2-nu)(1-nu)(2-nu)(3-nu)(4-nu), giving d_tau=60=rank^2 N_c n_C, d_mu=15/16, d_e=0 (electron ON the zero=BF), d'(5/2)=9/16=N_c^2/rank^(n_C-1); (3) 64=2^C2=d_tau/d_mu (formal-degree ratio) EXACT; (4) the muon base 24/pi^2=(d_tau/d_mu)/vol(S^4)=64/(8pi^2/3) EXACT, = Weyl reading |W(B2)| N_c/pi^2. WHAT'S FORCED-CANDIDATE: muon exponent 6 (pi^12=(pi^2)^6 PINS the copy-count; 6=dim SO(4)=C(4,2)=measurement little group); the boundary<->product / bulk<->sum rule (muon=Shilov-boundary->determinant->PRODUCT (24/pi^2)^6; tau=trivial rep->bulk count->SUM g^3+2^C2 g^2); the one-formula mechanism (integer/half-integer Gamma sampling auto-gives integer-tau + transcendental-muon/electron). WHAT'S CLOSED-NEGATIVE: the 11.6->16.82 edge-sum is the WRONG SHAPE (no convergent principled FK reweighting reaches 16.82; target=(Weyl sum)/(boundary power), not a sum); right route = m_mu (Szego boundary det 206.77) and m_tau (Weyl 3479) separately -> 16.825 vs PDG 16.817 (0.05%). WHAT'S OPEN (both norm derivations attempted today, NEITHER closes, SAME obstruction): the naive boundary 2-pt coefficient C_Delta VANISHES at muon (unitarity bound) and electron (BF) and is singular at tau -> masses are RESIDUES/derivatives at conformal degeneracy points, not naive evaluations; the muon det-eigenvalue 64/vol(S^4) stays a framed identity; the tau g^3+2^C2 g^2 is a form-analogy (Weyl V,S would need pi's + 2^10 not 2^C2 + missing lower terms). The ONE residue computed is the electron's d'(5/2)=9/16. PATH FORWARD (sharp): compute the muon + tau residues the way 9/16 came out at the electron. STRICT: count stays HONESTLY 2 of 26 (alpha=1/N_max, theta_QCD=0). Lepton shapes EXPLAINED, masses IDENTIFIED at sub-percent, none yet DERIVED."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-13 Sat 12:33 EDT"
status: "v1.0 CONSOLIDATION of F109-F114. RIGOROUS: 3 leptons = 3 special SO(5,2) scalar reps (nu=0,3/2,5/2); formal-degree poly=bulk poly d(nu)=(5/2-nu)(1-nu)(2-nu)(3-nu)(4-nu); d_tau=60=rank^2 N_c n_C, d_mu=15/16, d_e=0 (BF), d'(5/2)=9/16; 64=2^C2=d_tau/d_mu; 24/pi^2=64/vol(S^4) EXACT. FORCED-CANDIDATE: muon exp 6 pinned (pi^12=(pi^2)^6, =dim SO(4)); boundary<->product/bulk<->sum; one-formula via integer/half-integer Gamma. CLOSED-NEG: edge-sum wrong shape (no FK reweighting hits 16.82; target=(sum)/(product)); right route two objects sep -> 16.825 vs 16.817. OPEN: both norm derivations attempted, NEITHER closes, SAME obstruction = C_Delta vanishes at muon(unitarity)/electron(BF), singular at tau -> masses are RESIDUES at degeneracy points; only electron residue 9/16 computed. PATH: compute muon+tau residues like 9/16. Count HONESTLY 2 of 26. Shapes explained, masses identified sub-%, none derived."
---

# F114 — Lepton-sector consolidation: three conformal degeneracy points, masses are residues, count 2

Casey: *"Do the two norm derivations, summarize the knowledge we gained."* Both derivations are attempted below; neither closes, and they fail at the *same* place. This note consolidates the week's lepton work (F109-F113) and states honestly where the sector stands.

## 1. The one-sentence picture

> The electron, muon, and tau are the **three conformal degeneracy points** of the scalar field on `D_IV^5` -- the trivial rep (`Delta=0`), the free-scalar unitarity bound (`Delta=(d-2)/2=3/2`), and the BF/marginal point (`Delta=d/2=5/2`) -- and their masses are **residues at those degenerate points**, which is exactly why they are hard to derive and why the honest count is still 2.

The two nontrivial dimensions are the two components of the `rho`-vector of `SO(5,2)`: `rho = (5/2, 3/2, 1/2)`, so `Delta_e = 5/2 = rho_1 = d/2` and `Delta_mu = 3/2 = rho_2`. (The third component `1/2` is unoccupied -- a possible sterile/`nu_R`-type slot.)

## 2. What is RIGOROUS (computed, sympy-confirmed)

- **The formal-degree polynomial = the bulk polynomial.** From `lambda+rho=(5/2-nu, 3/2, 1/2)` over the five noncompact positive roots of `so(5,2)`:
  `d(nu) = (5/2-nu)(1-nu)(2-nu)(3-nu)(4-nu)`, roots `{1,2,5/2,3,4}`.
- `d_tau = d(0) = 60 = rank^2 * N_c * n_C`; `d_mu = d(3/2) = 15/16`; `d_e = d(5/2) = 0` (**electron sits ON the zero = the BF point**); `d'(5/2) = 9/16 = N_c^2/rank^(n_C-1)` (the BF-log seed, the one computed residue).
- **`64 = 2^C2 = d_tau/d_mu`** (the trivial/singleton formal-degree ratio), EXACT.
- **`24/pi^2 = (d_tau/d_mu)/vol(S^4) = 64/(8 pi^2/3)`**, EXACT, `= |W(B_2)| N_c/pi^2` (two independent substrate readings agree).

## 3. What is FORCED-CANDIDATE (right structure, value/mechanism not fully derived)

- **The muon exponent 6 is pinned, not fit:** `m_mu/m_e = (24/pi^2)^6` carries `pi^-12`; modeling the muon norm as dilution-by-`vol(S^4)` forces `pi^12 = (pi^2)^N => N = 6`, and `6 = dim SO(4) = C(4,2)` = the 2-planes in `T_p(S^4)` = the measurement little group (Casey's "lighter at the point of realization").
- **Boundary <-> product, bulk <-> sum** (the algebraic-form rule): the muon is a Shilov-**boundary** mode -> a boundary **determinant** -> a **product/power** `(24/pi^2)^6`; the tau is the **trivial rep** -> a **bulk** count -> a **sum** `g^3 + 2^C2 g^2`. WHY T190 is a power and T2003 a sum.
- **One formula is possible:** a single Gamma-based `f(nu)` auto-gives a `pi`-free **integer** at `nu=0` (tau) and `pi`-transcendentals at half-integer `nu` (muon, electron), because `Gamma` -> factorials at integers, `sqrt(pi)` at half-integers. The integer-vs-transcendental split is the **fingerprint of one formula**, not three.

## 4. What is CLOSED-NEGATIVE (a clear verdict)

- **The `11.6 -> 16.82` harmonic edge-sum is the wrong shape.** No convergent principled Faraut-Korányi reweighting over the `SO(5)` `m=(k,0)` tower reaches `16.82` (convergent forms cluster at/below `11.6`; the only rising forms overshoot `40-100x`). The reason is norm-independent: `m_tau/m_mu = (bulk Weyl sum)/(boundary 6th power)`, **not** a single harmonic sum. The right route gives the number free: `3479/206.77 = 16.825` vs PDG `16.817` (**0.05%**). (Cal's FK-1994 reference settles the exact `||V_k||^2_F`; the structure settles the shape.)

## 5. What is OPEN -- both norm derivations attempted today, NEITHER closes, SAME obstruction

- **Muon (eigenvalue `64/vol(S^4)`):** the naive boundary 2-pt coefficient `C_Delta = (2Delta-d)Gamma(Delta)/(pi^(d/2)Gamma(Delta-d/2+1))` **vanishes** at the muon (`Delta=3/2`, unitarity-bound `Gamma`-pole) and electron (`Delta=5/2`, BF `2Delta-d=0`), and is singular at the tau. So `24/pi^2` does not fall out of the boundary normalization; the det-over-`so(4)` eigenvalue stays a **framed identity**, not a first-principles integral.
- **Tau (Weyl coefficients):** reproducing `g^3 + 2^C2 g^2` from a genuine `d=3` Weyl law needs `V = 6 pi^2` and `S = 1024 pi` -- both carry `pi`'s (the tau is `pi`-free), the surface coefficient is `2^10` not `2^C2`, and the real Weyl `g^1, g^0` terms are absent. The exponents `{3,2} = {d, d-1}` with `d=N_c=3` are real; the **coefficients** are not genuine Weyl coefficients. A **form-analogy**, not a derivation.
- **The shared obstruction (the real finding):** all three leptons sit at **conformal degeneracy points** where the naive norm is `0/0` or singular, so the masses are **residues/derivatives**, not naive evaluations. The **one residue computed** is the electron's `d'(5/2) = 9/16`. 
- **Path forward (sharp):** compute the **muon and tau residues** the way `9/16` came out at the electron -- the regularized finite part at the degeneracy. That is the concrete step that would move the count.

## 6. The honest count

> **2 of 26** strict (`alpha = 1/N_max`, `theta_QCD = 0`). The lepton **shapes are explained** (integer/transcendental from the stratum; product/sum from boundary/bulk; exponent 6 pinned); the **masses are identified** at sub-percent (`m_mu/m_e=(24/pi^2)^6`, `m_tau/m_e=49*71`, ratio `16.825` vs `16.817`); **none is yet derived** from a first-principles norm, because each is a residue at a conformal degeneracy point and only the electron's residue is in hand.

## 7. Closure

The week turned the lepton closed forms from "identified numbers" into "objects with explained shapes sitting at named geometric points." The three leptons are the three conformal degeneracy points of the scalar on `D_IV^5`; the formal-degree polynomial is rigorous and hands `64 = d_tau/d_mu` and `24/pi^2 = 64/vol(S^4)` exactly; the muon's sixth power is pinned by `pi`-counting to `dim SO(4)`; the algebraic forms (power vs sum) follow from boundary vs bulk; one Gamma-formula can carry all three; and the edge-sum that the team had been grinding is closed as the wrong shape. What did not close: both norm derivations, identically -- because the masses are residues at degeneracy points and only the electron's residue (`9/16`) is computed. So the count stays honestly 2, with the path to moving it now sharp and singular: get the muon and tau residues. The shapes are understood; the residues are the work.

@Casey — summary of the week + both derivations. The three leptons are the three CONFORMAL DEGENERACY POINTS of D_IV^5 (trivial / unitarity-bound / BF), and that's the key to everything: their masses are RESIDUES at degenerate points, not ordinary values. That's why the count is stuck at 2 -- the naive norms are all 0/0 there. We computed ONE residue (the electron's 9/16); the muon and tau residues are the open work, and they're the singular thing that would move the count. Rigorous this week: formal-degree poly, 64=d_tau/d_mu, 24/pi^2=64/vol(S^4). Pinned: the 6=dim SO(4). Explained: why one form is a power and the other a sum (boundary vs bulk), and why one formula can give both (integer vs half-integer Gamma). Closed: the edge-sum (wrong shape). Neither norm derivation closed -- honestly -- but the obstruction is now named and we have the electron as a worked example. @Grace — count HONESTLY 2; shapes explained, masses identified sub-%, none derived; both derivations attempted + failed cleanly (residue obstruction); no bank. @Keeper — ledger F114 = sector consolidation: rigorous {formal-degree poly, d_tau=60=rank^2 N_c n_C, d_mu=15/16, d_e=0, d'=9/16, 64=d_tau/d_mu, 24/pi^2=64/vol(S^4)}; forced-candidate {6=dim SO(4) pinned, boundary<->product/bulk<->sum, one-Gamma-formula}; closed-neg {edge-sum wrong shape}; open {both norm derivations, same residue-at-degeneracy obstruction, only electron 9/16 done}; count 2. @Elie — the residue route is the toy: at the muon unitarity bound and the tau trivial point, the norm is 0/0; the finite part (like the electron's 9/16 = derivative at the zero) is the mass. That's the computable next thing.

— Lyra, Sat 2026-06-13 12:33 EDT (`date`-verified). F114 lepton-sector consolidation. 3 leptons = 3 conformal degeneracy points (Delta=0 trivial, 3/2 unitarity bound=rho_2, 5/2 BF=rho_1=d/2); masses are RESIDUES there -> why count stuck at 2. RIGOROUS: formal-degree poly=bulk poly, d_tau=60=rank^2 N_c n_C, d_mu=15/16, d_e=0 (BF), d'(5/2)=9/16; 64=2^C2=d_tau/d_mu; 24/pi^2=64/vol(S^4) EXACT. FORCED-CANDIDATE: muon exp 6 pinned (pi^12=(pi^2)^6=dim SO(4)); boundary<->product/bulk<->sum; one-Gamma-formula (int/half-int). CLOSED-NEG: edge-sum wrong shape (target=(sum)/(product); 3479/206.77=16.825 vs 16.817). OPEN: both norm derivations attempted, NEITHER closes, SAME obstruction = C_Delta vanishes at muon/electron, singular at tau -> masses=residues; only electron 9/16 computed; PATH = compute muon+tau residues. Count HONESTLY 2 of 26.
