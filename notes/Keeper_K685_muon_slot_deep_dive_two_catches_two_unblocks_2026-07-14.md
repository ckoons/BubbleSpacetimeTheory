# K685 — Muon-slot deep dive: two consistency CATCHES (the spinor object; a cross-lane strata inconsistency) + two UNBLOCKS (the canonical-mode closer lemma; the muon determinant is already substantially built).

**Keeper | 2026-07-14 | A deep dive on Lyra's canonical-mode premise (F539) and Elie's muon boundary determinant (toy 4662) surfaced four things the team's own notes missed. Two are consistency catches that must be settled before any muon bank; two are concrete unblocks that put the muon much closer than "not yet."**

## UNBLOCK 1 — the canonical-mode premise has a concrete, bounded CLOSER (hand to Lyra)
F539's premise ("a particle sits in the slice's canonical mode, not a frame-dependent symmetric one") is NEW — no prior corpus principle. But there is a bounded route to FORCE it: **define "localized on stratum Σ" as the localization current [Σ]; by Poincaré/Thom duality its K-type is Λ^{dim Σ}(TΣ) — an antisymmetric top form — which STRUCTURALLY EXCLUDES the symmetric (2,0)** (symmetric tensors are never currents). This is a finite lemma on a Hermitian symmetric space, not a research program. If it lands, the muon (1,1) is **forced**, and it's the template for the whole charged sector.
- **It OVER-DELIVERS:** the top-form reading forward-closes BOTH electron and muon: bulk (dim n_C=5) → Λ⁵(V₅) = **(0,0)** singlet (= the orientation/volume form = ground state = electron); Cartan slice (dim rank=2) → Λ²(T) = **(1,1)** (muon). Stronger than F539 (which claimed only the muon).
- **It FAILS for the tau:** Shilov point (dim 0) → Λ⁰ = trivial, degenerate with the electron. So the top-form template closes e + μ and STOPS. The tau needs the separate cone-tip/transverse-fiber route (F517/F368) — do NOT force the tau by this template.

## UNBLOCK 2 — the muon boundary determinant is ALREADY substantially built (F116/F117/F115); toy 4662 re-opened it
Elie's toy 4662 correctly scoped the muon as a boundary determinant (not a formal-degree derivative, since d(3/2)=−15/16≠0) — but it under-cited the corpus. The F112–F117 lineage already built most of it:
- **F116:** the zeta-regularized functional determinant is RULED OUT (= O(1)); the finite object is the **so(4) curvature determinant**: m_μ/m_e = det_{so(4)}M = (24/π²)⁶ = (64/vol(S⁴))⁶. Three of four ingredients rigorous (6 = dim so(4); R_{Λ²} = Identity; 64 = d_τ/d_μ = formal-degree ratio = 2^{C_2}).
- **F117:** the π-structure is DERIVED — the boundary norm vanishes linearly at the unitarity bound, residue = 1/π² (Γ(3/2)/π^{5/2} + Γ(−1) pole), traced to boundary dim 5 + Δ=3/2. **So the π¹² ORIGINATES at the boundary Szegő residue at the unitarity bound.** Soft spot (honest): the flat→sphere **3/8 normalization** (= 1/vol(S⁴) factor) is ASSERTED, not derived.
- **F115:** the "which object" map, already written — three leptons, three degeneracy mechanisms: electron = LOG (derivative at the zero → 9/16); muon = det/PRODUCT over the 6 so(4) directions → (24/π²)⁶; tau = Tr log/SUM over bulk modes (a DIFFERENT object — Weyl count, not a boundary determinant).
- **NET: the muon VALUE is ~ONE un-derived factor from forward** — the flat→sphere 3/8 step (F117's own flagged gap) + the relative-residue "Szegő=1" check (F343/F344: c_FK is a bulk red herring that cancels; the muon's is a boundary π^{−12} object). **Immediate action: reconcile toy 4662 against F116/F117/F115, then close the 3/8 factor.** Elie's "harder object, research problem" framing is over-cautious relative to F116/F117. (d_τ/d_μ=64=2^{C_2} is ALREADY the multiplier inside the F116 determinant — it aids the muon, NOT the tau; 64 is a ratio, gives no tau absolute.)

## CATCH 1 (MOST SEVERE, unowned) — the spinor object: is (1,1) the muon's K-type, or a dressing on a spinor?
K539/Cal #402 (a VERIFIED catch) established the lepton MATTER sector is the **Δ=2 Di SPINOR** of the F(4) minrep, NOT a Δ=3/2 scalar. But F538/F539's entire (0,0)/(1,1)/(2,0) discussion is about **integer tensors**. If the lepton K-type is fundamentally a half-integer spinor ((½,½)-type), then the tensor labels are an **orbital/dressing factor multiplying a fixed spinor**, NOT the lepton's actual K-type. F539 never addresses this; F368 independently treats the muon as a color-singlet dressed by a transverse short-root fiber (again a dressing). **"Muon = (1,1)" and "muon = a Δ=2 spinor" are two unmerged claims. This MUST be settled before any muon-slot bank** — otherwise the canonical-mode template may be describing the wrong object (the dressing, not the K-type).

## CATCH 2 — a cross-lane stratification inconsistency (mixing lane vs mass lane disagree on e/τ)
- **Mixing/support lane** (F86/F361/F536/F539): electron → **bulk** (dim 5), muon → Cartan (dim 2), tau → **Shilov point** (dim 0).
- **Mass-residue lane** (F115/F116/F117): electron → **0-dim** shadow locus, tau → **bulk** (dim N_c=3). The OPPOSITE endpoints.
- **They agree ONLY on the muon** (middle/boundary either way) — which is exactly why the muon is the closable one and the electron/tau are softer. The canonical-mode template rides lane 1; the determinant machinery rides lane 2. **Nobody has reconciled them.** Until they're reconciled, the template's electron/tau assignments are NOT on the same geometric footing as its muon. **This is a real internal inconsistency (Keeper takes the reconciliation).**

## Net direction
- **Lyra:** prove the localization-current ⇒ Λ^{top} lemma (forces the canonical mode, kills (2,0), over-delivers e+μ) — the sector template. AND settle CATCH 1 (is (1,1) the K-type or a spinor dressing?) — gates any bank.
- **Elie:** reconcile toy 4662 against F116/F117/F115 (the muon determinant is mostly built); close the flat→sphere **3/8** factor — the last un-derived multiplicative piece. The muon VALUE is ~one factor from forward, not from-scratch.
- **Keeper:** reconcile CATCH 2 (the cross-lane strata) — a consistency audit.
- **Tau:** a SEPARATE track (cone-tip/Tr-log, F517/F368); do NOT force it by the top-form template or pre-commit to "6."
- **Bank discipline:** no muon-slot bank until CATCH 1 (spinor object) is settled AND the 3/8 factor is derived. The muon is close on BOTH the label (lemma) and the value (determinant) — but each has one honest gap.

— Keeper K685, 2026-07-14. Closer for the canonical-mode premise = the localization-current ⇒ Λ^{top} lemma (forces (1,1), over-delivers e+μ, fails τ). Muon determinant already built (F116/F117/F115); ~one factor (flat→sphere 3/8) from forward — reconcile toy 4662. CATCH 1 (severe): muon may be a Δ=2 spinor, (1,1) a dressing — settle before bank. CATCH 2: mixing-lane vs mass-lane strata disagree on e/τ (agree on μ) — reconcile. See [[Keeper_K683...]], [[BST_Odd_Dimensionality_Unification_2026-07-14]].
