# K800 — Bank the flavor UNIFICATION (masses + mixings = ONE inter-stratum overlap object); tier V_us = 1/(rank·√n_C) honestly (structural leading-Gatto ~1%, NOT 0.3%-precision); the CKM row's high-value part is DONE. Next investigation (the capstone): PIN THE K-TYPE ADDRESSES from the banked exact constraints — bounded, with a pivot-exit.

**Keeper | 2026-07-21 Tuesday | CKM row opened and landed the same way the mass row did — because it's the SAME object. Casey: what next? The real result is the unification; the frontier is the addresses. Web-grounded (GST tier).**

---

## ★ BANK — the flavor unification (the real result of the last two rows)
**Quark masses and CKM mixings are ONE geometric object — the inter-stratum overlaps on the D_IV⁵ strata — read two ways: mass ~ overlap² (radial), mixing ~ overlap (angular). So mixing = √(mass ratio) (Gatto, F585).** This is derived-structural and publishable. It means:
- The Wolfenstein hierarchy λ:λ²:λ³ = the stratum-distances (adjacent / next / extreme) — matches |V_us|:|V_cb|:|V_ub|. Structural.
- The CKM row **inherits the mass row's tier** — one object, so one exact rung + structural rest, necessarily.
- Masses + mixings are NOT two derivations; pinning either half constrains the shared object.

## ★ V_us = 1/(rank·√n_C) — bank as STRUCTURAL (I-tier), tier honestly
|V_us| = √(m_d/m_s) = √(1/(rank²·n_C)) = **1/(rank·√n_C) = 1/√20 = 0.2236**; observed 0.2243. Chains the banked m_s/m_d = 20 through the Gatto relation — **no new integers**, target-innocent, ONE geometric fact → two observables (m_s/m_d AND V_us). GENUINE cross-consistency.
- **BUT tier it right:** the Gatto relation V_us = √(m_d/m_s) is LEADING order; the up-correction √(m_u/m_c) ≈ 0.041 is the theory error (~1% on V_us). So this is a **structural result accurate to ~1%**, NOT a 0.3%-precision derivation — the 0.3% number-agreement is at/beyond the relation's own accuracy. Do NOT bank "0.3%." (Web: GST classically *undershoots* V_us; the correction adds in quadrature. [arXiv:2001.07401])
- **Nice consistency (note, don't over-claim):** the leading term matching so well ⟺ small θ_u = √(m_u/m_c) ⟺ the up-type is MORE suppressed — exactly the up-type-steeper result from the mass row. Self-consistent, but keep V_us at structural ~1%.

## ★ The CKM row's high-value part is DONE — do NOT chase V_cb/V_ub
V_cb, V_ub: the naive single-sector √ is off by 2–3× (needs both sectors + O(1) coeffs + relative phase) → **Tier-2**. Lyra + Elie both flagged: **do NOT hunt primary-forms for V_cb/V_ub magnitudes** — that's the ratio-trap again (small experimental errors ≠ derivable; the theory error is O(1)). The Jarlskog/CP part is already in hand (rank-1 → J small, K788). So the CKM row banks: the unification + V_us + the structural Wolfenstein hierarchy. Close its high-value part.

## ★★ THE NEXT INVESTIGATION (the capstone) — PIN THE K-TYPE (a,b) ADDRESSES
Every Tier-2 flavor quantity is Tier-2 for ONE reason: **the K-type addresses of the generations aren't pinned, so the radial/angular overlaps aren't computed.** Pin the addresses → the overlaps compute → the whole Tier-2 set (m_c/m_u, V_cb, V_ub, neutrino Δm²…) becomes PREDICTIONS. This is the culmination of the "mixings as the cleaner window into the shared addresses" (K799 follow-up).
- **The question (Lyra's lead):** are the 3 generation addresses **fixed by the 3 support strata** (F86: bulk/Cartan/Shilov → compute their K-types directly) or a **bounded discrete choice** (then SEARCH the finite (a,b) space against the banked exact constraints)?
- **The constraints in hand (test/fit target-innocently):** m_s/m_d = rank²·n_C = 20 (down radial); the charged-lepton ladder m_μ/m_e, m_τ/m_e (lepton radial, 2 constraints); V_us = 1/(rank·√n_C) (down angular — though = the same down fact); Elie's selection rule (a coupling ⟹ not (a,0)).
- **The payoff test:** once addresses are pinned, PREDICT a Tier-2 quantity (e.g. V_cb or m_c/m_u geometrically) and check against data. That's the target-innocent validation — predict, don't fit.
- **★ BOUNDED, with a pivot-exit (discipline):** this is the long-open "June core" — HARD. Give it a FOCUSED attempt with the new tools (the selection rule, V_us, the unification). **If the addresses are geometrically fixed → compute and win. If a bounded discrete search pins them → win. If it's genuinely under-determined / no clean fit → say so, bank the flavor synthesis, and PIVOT to a fresh sector.** Do NOT grind multi-week; timebox it (feedback: investigate-with-timebox).

## After the capstone (either outcome) — the flavor synthesis + pivot
The flavor sector (masses, mixings, CP, neutrino) is now a coherent mapped whole: one overlap object, localization-driven, a handful of exact rungs (m_s/m_d, V_us, lepton ladder), the up/down asymmetry located, CP small (rank-1), δ free. **Bank the flavor-synthesis paper.** Then pivot to a FRESH not-derived sector (candidates: gauge couplings sin²θ_W/α_s running; neutrino Δm² scale; gravity/G; Λ) to test whether the machinery generalizes.

## Tiers / discipline
- BANK: unification (derived-structural); V_us=1/(rank·√n_C) (structural, ~1%, NOT 0.3%); Wolfenstein hierarchy (structural).
- DO NOT: chase V_cb/V_ub primary-forms (Tier-2, fit-trap); claim V_us at 0.3%.
- Address search: target-innocent (fit addresses to banked exacts, then PREDICT the rest); bounded with pivot-exit.

— Keeper K800, 2026-07-21. BANK the flavor unification: masses + mixings = ONE inter-stratum overlap object (mass~overlap², mixing~overlap → Gatto). V_us=1/(rank·√n_C)=1/√20 structural I-tier (~1%, leading-Gatto, NOT 0.3%; the good match ⟺ BST up-suppression, self-consistent). CKM high-value part DONE — don't chase V_cb/V_ub (Tier-2 fit-trap). NEXT (capstone): PIN the K-type (a,b) addresses from the banked exacts (m_s/m_d=20, lepton ladder, V_us) — fixed-by-strata (compute) or bounded search; then PREDICT the Tier-2 set. BOUNDED with pivot-exit (June core is hard). Then flavor-synthesis paper + pivot to a fresh sector. See [[Keeper_K799_quark_hierarchy_thread_CLOSE_audit_candidates_only_m_s_m_d_survives_2026-07-21]], F585 (Gatto), T2513 (m_s/m_d=20), T190/T2003 (leptons), K-Type Address Registry.
