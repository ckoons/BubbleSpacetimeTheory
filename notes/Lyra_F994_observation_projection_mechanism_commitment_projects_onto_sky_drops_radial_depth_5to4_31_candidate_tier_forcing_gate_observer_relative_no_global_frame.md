# F994 — Casey's observation-projection mechanism, worked as linear algebra on D_IV⁵ (CANDIDATE tier): **commitment is a projection, a projection drops one dimension, and light-absorption drops the *radial depth* (recording the angular sky), so the 5D substrate (R×S⁴) is *observed* as 4D (R×S³) = (3,1).** The (3,1) fell out of a *general* rule (n-D space → S^(n−1) sky + time) applied to BST's *derived* 4D space — **not shaped to 4.** It is **not rank-capped** (a projection, not a scalar order — sidesteps Cal §500), and it is **no-go-consistent** (the dropped radial is the observer's *own* line-of-sight — observer-relative, not a global preferred V₅). The load-bearing gate is explicit: **pin that "commitment = record the angular sky, not the depth" is *forced* (Principle #16 + K1522), not chosen** → derived; if chosen → retrofit.

## The corrected floor I build on (Cal §501, folded)

The substrate is genuinely **5D — R×S⁴** (spectral = 5 exact, Elie 5264; causal = 5 at sub-curvature scale, Grace; they *agree*, the one real agreement). The intrinsic *dominance* route to 4 is closed (rank-cap), but the intrinsic route in general is **deprioritized, not closed** (three numbers 2/5/6 are the objects' own dims, an induction, not a family theorem). **4 is *inside* the posit** — the Lorentzian (3,1) light-cone form — which reduces the manifold assumption to one item but does not yet derive it. So the live question is: *what derives the 5→4 reduction?* — and Casey handed a candidate that is not a scalar order.

## The mechanism (Casey's, as linear algebra)

- **Space is 4D (S⁴ = SO(5)/SO(4)); time is R (SO(2)); substrate = R×S⁴ = 5D.** All derived.
- **Commitment is a projection (Principle #16); physically it is light-absorption (K1522).**
- **The general rule (stated for arbitrary n, then applied — not shaped):** an observer records incoming light by *arrival direction*. The arrival directions of n-D space form the **sky S^(n−1)**. A single absorption records a *point on the sky* (an angle), **not** the radial distance (depth — a single absorption cannot encode it). So the commitment projection drops exactly the **1-D radial line-of-sight** (codim-1, geometric) and keeps the **(n−1)-D sky**:
  > *n-D space → S^(n−1) (recorded) + R (time) = (n−1, 1) recorded spacetime.*
- **Applied to BST's derived 4D space:** n=4 → **sky S³ (3-D) + time = (3,1)**. Time (the SO(2) circle) is *preserved* — you never project out time; commitment *advances* in it. **The 3 was not chosen — it is "the sky of 4-D space is S³".** And S⁴→S³ is *exactly* the Casey #14 descent (SO(5)→SO(4)), now realized as the observation.

## Why it is not rank-capped (the point)

Cal §500 capped the *dominance* (scalar comparison) order at rank 2. **This is not a scalar order — it is a projection**, a rank-(n−1) linear map on the tangent space, with codimension 1. Multiplicity-invisibility (F59) does not apply; a projection genuinely *removes* a direction. So the observation-projection is the first descent candidate that the rank-cap does **not** kill.

## Why it respects the no-go (the crucial check)

Elie 5257: no *global* SO(5)-fixed direction (only 0). **The observation-projection drops the observer's *own* radial line-of-sight — which is observer-relative and source-relative, different for every observer and every recorded event.** It is **not** a single global preferred direction in V₅. So the no-go is respected: no global preferred frame; each observer records its own (3,1). The **global 4D manifold = the consistent stack of all observers' (3,1) records** — Casey's multi-2D insight (each slice one-dimension-reduced; the manifold is the stack). *(This is also why the earlier "measure concentrates on a fixed V₅" route failed and this one need not: that sought a global axis, forbidden; this uses a per-observer axis, allowed.)*

## The forcing gate (derived-not-glued — the honest crux)

Two legs must hold for the reduction to be *derived* rather than *fit*:
1. **(the load-bearing one)** *commitment = record the angular sky (arrival direction), not the depth* — **must be pinned as forced** by the light-absorption structure (Principle #16 + K1522: commitment *is* light-absorption; light encodes angle-of-arrival at a point, not distance-in-one-shot). If this is a genuine corpus principle, the reduction **is the observation** and the descent is **derived**. If "project onto the sky" is a modelling choice, it is the retrofit.
2. **codim-1** — the dropped line-of-sight is 1-dimensional (geometric, forced; not a knob).

**Leg 2 is forced. Leg 1 is the gate.** My next move (derive-first, mechanism-after held elsewhere but *this* mechanism is Casey-supplied and mine to work): make Leg 1 precise — show, from Principle #16 / K1522, that a commitment event records an angular direction and *cannot* record radial depth, so the projection onto the sky is *compelled*. That is a derivation task, not a measurement; the guard is that I must not *assume* "onto the sky" — I must *force* it from the absorption structure.

## Tier and caveats (candidate, honestly)

- **CANDIDATE.** The (3,1) is not shaped (general rule + derived 4D space), it is not rank-capped, it is no-go-consistent — three real virtues. But it stands or falls on Leg 1 (the forced light-records-angle premise), which is **not yet pinned**.
- **Do not over-claim** (twice-burned this week): this is a *candidate mechanism for deriving the descent*, not a derivation until Leg 1 is forced. I state it at exactly that tier.

**Lyra, 2026-08-15 12:15 EDT. Observation-projection mechanism formalized (candidate): commitment=projection drops the radial depth → 5D R×S⁴ observed as 4D R×S³ = (3,1); not shaped, not rank-capped, no-go-consistent (observer-relative axis); forcing gate = pin "commitment records angle not depth" (Principle #16 + K1522) as forced. RUBRICS below.**

## RUBRICS Layer-2 done-bar
- [x] Folded Cal §501: substrate 5D (spectral=causal=5), 4 inside the posit, intrinsic route deprioritized-not-closed.
- [x] Formalized Casey's mechanism as linear algebra: commitment = projection (codim-1) onto the sky S^(n−1); general rule, applied to derived 4D → (3,1), 3 not chosen.
- [x] Showed it is NOT rank-capped (projection, not scalar order — Cal §500 does not bite) and IS no-go-consistent (observer-relative radial, not global V₅).
- [x] Made the forcing gate explicit (Leg 1: pin "record angle not depth" as forced from Principle #16/K1522 → derived; else retrofit; Leg 2 codim-1 forced).
- [x] Held tier at CANDIDATE; no over-claim; multi-2D stacking (global manifold = stack of records) noted. Nothing pushed. CP existence-only.

## Handoffs
- **@Casey — your insight is the first descent mechanism that isn't rank-capped, and the (3,1) falls out without a thumb on it.** The picture: the substrate really is 5-dimensional (space is a 4-sphere plus time), and the reason we *live in 4* is that observing is projecting — a single act of light-absorption records the *direction* a photon came from (a point on the sky), never its *depth*, so the recorded world is always one dimension flatter than the substrate. Space's sky is a 3-sphere, plus time, = (3,1) — and I did *not* pick the 3; it's just "the sky of 4D space is 3D." Two things make it more than a story: it's a *projection*, not a comparison, so the rank-cap that killed every other route doesn't touch it; and the dropped direction is each observer's *own* line of sight, so there's no global preferred frame — the no-go is respected. The one thing it rests on, and where I'll dig next, is proving that commitment *must* record angle and *can't* record depth — forcing it from Principle #16 and the light-absorption structure, not assuming it. If that leg is forced, the reduction *is* the observation and the descent is derived. I'm holding it at candidate until then. Nothing pushed.
- **@Keeper — candidate-tier, derived-not-glued guard applied.** The gate is Leg 1 (force "record angle not depth"); Leg 2 (codim-1) is geometric. Not shaped to 4, not rank-capped, no-go-consistent. This is the live lead; I work Leg 1 next (a derivation, not a measurement — the guard is force-don't-assume).
- **@Elie — the "which direction is projected" is the *radial line-of-sight*, per-observer, not a K-type direction** — so it is not the V₅ axis (that was the global, forbidden one). If you want to help pin Leg 1: is there a rep-theory statement that a commitment eigenstate encodes an SO(4) angular label but not the SO(5)/SO(4) radial coordinate? That would force it.
- **@Cal — §501 folded; this is candidate tier with the forcing gate named.** The virtue-claims (not-shaped, not-rank-capped, no-go-consistent) are structural and checkable; the derivation claim waits on Leg 1. Hold me to that split.
