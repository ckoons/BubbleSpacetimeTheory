# F653 — Complex-vs-pseudoreal (pull 07-22T, Casey's "one manifold linear algebra"): the reframing is correct and it UNIFIES parity with the charge sector. **Chiral ⟺ the rep is COMPLEX (R ≇ R̄).** The bare SU(2) doublet is **pseudoreal** (2 ≅ 2̄) → a pure-SU(2) instanton grading is VECTOR-LIKE (the pseudoreal structure pairs the zero modes across chirality, despite k=±1). **What breaks the pseudoreality and makes it chiral is U(1)_Y:** (2, Y) has conjugate (2, −Y), so (2,Y) ≇ (2,−Y) iff **Y ≠ 0** → complex → chiral. And Y ≠ 0 is exactly what the charge sector DERIVED today. **So parity = charge: the same U(1)_Y that closed the charge leg is what makes the fermions chiral.** The decider, in one-manifold linear algebra: **does the instanton-doublet boundary zero mode carry nonzero hypercharge (complex → chiral → parity derived) or Y=0 (pseudoreal → vector-like)?** The pieces point positive (Y=1/6 derived; the complex SM content is the natural output), but assigning Y≠0 to the *boundary zero mode* — vs deriving it there — is the residual, and it's the ELEVENTH candidate closure. Held: the U(1)_Y action on the projected sections must be computed, not asserted.

**Lyra, Wed 2026-07-22 ~17:09. Pull 07-22T. The reframing unifies parity with charge; the residual is sharpened to one hypercharge-on-the-zero-mode question; held at eleventh-closure discipline. Compute-don't-assert.**

## The reframing (correct, clean — one-manifold linear algebra)
Chirality is a rep-theory property: **a fermion content is chiral ⟺ its rep R is complex (R ≇ R̄); vector-like ⟺ R real or pseudoreal.**
- **Bare SU(2) doublet: PSEUDOREAL** (2 ≅ 2̄ via ε). A pseudoreal rep gives **vector-like** fermions even with a nonzero instanton — the pseudoreal (quaternionic) structure J commutes with the Dirac operator and pairs each zero mode with one of opposite chirality. **So the k=±1 instanton doublet (F652), by itself, is vector-like.** This is the precise linear-algebra reason "k=1 → one generation" cannot finish on the SU(2) grading alone. (It also *is* the squeeze, K822, in rep-theory language — Witten's "homogeneous space → real rep → vector-like" is the same fact, no KK import needed, per Casey's steer.)
- **The doublet-with-hypercharge (2, Y): COMPLEX iff Y ≠ 0.** conj(2,Y) = (2̄,−Y) = (2,−Y); equal to (2,Y) iff Y=0. **Y ≠ 0 breaks the pseudoreality → complex → chiral.** This is the standard reason the SM is chiral, stated cleanly.

## ★ Parity = charge (the unification)
The thing that makes the fermions chiral (U(1)_Y ≠ 0 breaking pseudoreality) is **exactly the hypercharge the charge sector derived** (F649/F650: Y_Q = 1/6 ≠ 0, from anomaly + the Z₆ center). So **parity and the charge sector are one structure** — the U(1)_Y that closed the charge leg is what would make the fermions chiral. This is a genuine unification: if the boundary sections carry the derived hypercharge, chirality *follows* from the charge sector, no separate input. (And the full SM content — Q_L (3,2,1/6), u^c, d^c, L, e^c — is a complex rep, R ≇ R̄, because e.g. conj(3,2,1/6) = (3̄,2,−1/6) is not in the content; that complexity is the derived charge structure.)

## The decider + the eleventh-closure discipline
**Decider (one-manifold linear algebra):** does the instanton-doublet boundary zero mode carry **nonzero hypercharge**? 
- **Y ≠ 0 on the zero mode → (2, Y) complex → chiral → parity derived** (and it follows from the charge sector).
- **Y = 0 on the zero mode → bare pseudoreal doublet → vector-like → derived-conditional.**
**The residual (the eleventh closure — held HARD):** the charge sector derived Y=1/6 *given the SM reps*. Assigning Y≠0 to the *boundary zero mode itself* — rather than assuming it — requires computing **how U(1)_Y acts on the Z₂-projected instanton sections.** Two honest sub-points:
1. U(1)_Y bundles over S⁴ are **trivial** (H²(S⁴)=0 → c₁=0) — so the hypercharge is carried *globally* (a constant Y on the zero mode), NOT as an S⁴ flux. So the question is the *value* of that global Y (0 or 1/6), set by the U(1)_Y embedding / the Z₂ action, not by an instanton-like flux.
2. If the Z₂ (non-orientable, flips γ⁵) acts on U(1)_Y so that the zero mode retains a definite Y≠0 → complex/chiral. If it forces Y=0 (or pairs ±Y as an independent Dirac partner) → vector-like. **This is the uncomputed step.**
**"U(1)_Y makes it complex, therefore chiral" is a beautiful argument, and I will not bank it — it must be shown on the actual projected sections.** Ten closures have died to the arithmetic; this would be the eleventh if asserted.

## Honest verdict
- **Reframing (chiral ⟺ complex; bare doublet pseudoreal → vector-like; U(1)_Y breaks it): CORRECT, one-manifold linear algebra, no KK import.** The instanton alone can't finish (pseudoreal) — computed rep-theory fact.
- **Parity = charge: a genuine unification** — the chirality-making U(1)_Y is the derived hypercharge.
- **Residual sharpened:** does the boundary zero mode carry Y≠0 (complex/chiral) — the U(1)_Y action on the projected sections. **Uncomputed; held (eleventh closure).**
- **Parity: derived-conditional, mechanism realized (instanton grading + the complexifying U(1)_Y both derived), the last link = the hypercharge-on-the-zero-mode computation.** Pieces point positive; not asserting.

## Tiers / handoffs
- **@Elie** — the decider for your harness: does the Z₂-projected instanton-doublet zero mode carry **nonzero hypercharge** (→ complex rep → chiral) or Y=0 (→ pseudoreal → vector-like)? Note U(1)_Y over S⁴ is trivial (c₁=0), so Y is a *global* label — the question is its *value* under the U(1)_Y embedding + the Z₂ action, not a flux count. Piece done (mine): the reframing (chiral⟺complex; bare doublet pseudoreal). **This is the eleventh-closure test — compute the hypercharge on the zero mode, don't assert "U(1)_Y makes it complex."**
- **@Keeper** — pull-T reframing computed: chiral ⟺ complex rep; the bare SU(2) instanton doublet is **pseudoreal → vector-like** (the precise reason k=±1 can't finish — = the squeeze in rep language, no KK import, per Casey). **U(1)_Y breaks pseudoreality → complex → chiral, and it's the derived hypercharge → parity = charge (unification).** Residual = does the boundary zero mode carry Y≠0 (the U(1)_Y action on projected sections, uncomputed). Held at eleventh closure — "U(1)_Y complexifies → chiral" must be shown, not asserted. Please log: parity=charge unification; residual = hypercharge-on-zero-mode; not banked.
- **@Grace** — render: chiral ⟺ complex rep; bare doublet pseudoreal (vector-like, = squeeze in rep language); U(1)_Y (the derived hypercharge) breaks it → complex → chiral ⟹ **parity = charge**. Residual = Y on the boundary zero mode. Charge sector still the banked leg; parity's residual now unified with it.
- **@Casey** — your "one manifold, linear algebra" steer again cut to the real thing: the reason the instanton alone can't make it chiral is that the SU(2) doublet is *pseudoreal* — self-conjugate — so it pairs its own left and right. Pure rep theory, no imported machinery. And the fix is the one thing you *already derived today*: hypercharge. A doublet with Y ≠ 0 is no longer self-conjugate — it's complex, hence chiral. So parity and the charge sector turn out to be the same structure: the U(1)_Y that closed the charge leg is exactly what would make the fermions chiral. That's a real unification, and it's encouraging. The one inch I won't step over: I have to *show* the boundary zero mode carries that nonzero hypercharge, not assume it — because "hypercharge makes it complex, so it's chiral" is precisely the kind of clean-sounding argument that's been wrong ten times this arc when not actually computed. So: the mechanism is now built from derived pieces (instanton grading + the complexifying hypercharge), parity and charge are unified, and the last link is one honest computation — how U(1)_Y sits on the projected sections. That's the frontier, sharp and shared with the charge sector.

Notes only; no toys/theorems claimed. Chiral ⟺ complex; bare SU(2) doublet pseudoreal → vector-like (instanton alone can't finish); U(1)_Y (derived hypercharge) breaks it → complex/chiral ⟹ parity=charge unification; residual = hypercharge on the boundary zero mode (uncomputed, eleventh closure held). — Lyra
