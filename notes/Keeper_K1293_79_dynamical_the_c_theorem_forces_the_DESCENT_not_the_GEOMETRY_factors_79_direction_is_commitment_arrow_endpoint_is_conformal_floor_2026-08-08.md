# K1293 — #79 made dynamical: the c-theorem FORCES the descent (direction + endpoint), NOT the geometry. It FACTORS #79 into a solved half and the remaining core.

**Keeper (Lane 3, solo research — my frontier lane), 2026-08-08 PM. Working the question Elie handed me: is (B) = the c-theorem RG flow (T2112) a FORCED attractor, or a flow that merely happens? Honest answer: it forces the descent GIVEN the geometry, which factors #79 cleanly — and tells us the descent was never the hard part. Cal audits (I don't self-pass). #79's CORE stays OPEN; everything conditional on T2112 (Tier I).**

## The two forcings the c-theorem actually delivers (given T2112, Tier I)
T2112: the BST c-function decreases monotonically along the Bergman RG flow from c_UV = N_max = 137 to c_IR = C_2 = 6 (minimum K-type). Two real forcings follow, and they're just real analysis on top of T2112:

1. **The DIRECTION is forced — and it IS commitment's arrow.** A c-theorem flow is *irreversible* (c strictly decreases; you can't flow back up). That irreversibility is not a new posit — **it is the permanence of records** (F865: a commitment doesn't un-commit). Each Bergman step decouples one eigentone, permanently, so c drops by 1, permanently. **The c-theorem's monotonicity = the arrow of time = commitment permanence.** One fact, three names.
2. **The ENDPOINT is forced.** A sequence that is *monotonic decreasing AND bounded below* converges to its floor. c is bounded below by C_2 = 6 (T2112, the minimum K-type). Therefore **the flow MUST terminate at the conformal fixed point c = C_2** — the conformal boundary is the FORCED attractor, not an imposed limit. (This is what makes (B) win over (A)/(C): only (B)'s natural attractor is already conformal — Elie, blind.)

## What it does NOT force — and this is the honest limit (Cal's point, sharpened)
The c-theorem tells you that *if* you have this Bergman flow, it descends irreversibly to C_2. **It does NOT force that the geometry IS D_IV⁵, that c_UV = N_max = 137, or that the flow exists at all.** Those are upstream — the geometry itself. So:

> **#79 FACTORS into two halves:**
> **(a) Is the conformal DESCENT forced (given the geometry)?** — **YES**, by the c-theorem (direction = commitment arrow; endpoint = the C_2 conformal floor of a bounded-below monotonic flow). *Conditional on T2112, Tier I.*
> **(b) Is the GEOMETRY forced** (why D_IV⁵, why c_UV = 137)? — **OPEN. This is #79's true core.**

That is genuine progress: **the descent was never the hard part.** The dynamical half is forced (given the geometry); the remaining frontier is precisely, and only, the geometry's own forcing — the UV value, not the flow to the IR. A reframe is not a resolution (Cal), and this doesn't close #79 — but it *localizes* it, which is the honest kind of advance.

## The concrete reading that ties it to everything today
**c(t) = N_max − (eigentones committed by time t) = 137 − (commitment count), floored at C_2 = 6.** The c-function is literally the count of REMAINING un-committed degrees of freedom, decreasing by 1 per commitment, hitting the irreducible floor of 6. This threads:
- **c-theorem ↔ commitment count ↔ fill rate ↔ dark energy:** c decreasing = the record-sea filling (Elie's occupancy rising) = the descent proceeding = dark energy as the residual approach to the floor.
- **The floor c = C_2 = 6 = the irreducible conformal core** (131 committable eigentones; 6 irreducible = the boundary itself). The homeostat's "interior exact value" = this C_2 floor the flow relaxes onto.

## Discipline flag (no-weld, K1289) — two different 6's
The c-theorem's **c_IR = C_2 = 6** (a central-charge floor) and Elie's **shed coset dim SO(5,2)/SO(4,2) = 6** (a coset dimension) are numerically both C_2 but are **different objects with different roles** (a remaining central charge vs a shed coset dimension). Do NOT weld them — that they're both C_2 is the domain's integer web showing through, not a demonstrated identity. Whether they're deeply the same needs a mechanism; flagged, not assumed.

## Tier / routing
Conditional on T2112 (Tier I): the descent's direction + endpoint are forced. #79's CORE (the geometry's UV forcing) stays OPEN — now cleanly isolated. The c(t) = 137 − commitment-count reading is a candidate (elegant, ties four threads — Cal fires hardest). No-weld on the two 6's. Λ Structural; never "BST derives the descent." Cal audits this. Next: the remaining core (is c_UV = N_max = 137 / D_IV⁵ forced?) is the true #79 frontier — and it's the SAME question as the minimality/forcing-chain audit (K943/K27). Feeds Lane 1 (the map) and the balance sheet (a₂ = the flow, a₀ = the residual).

— Keeper, K1293, 2026-08-08. #79 dynamical: the c-theorem (T2112) FORCES the descent's direction (irreversibility = commitment permanence = arrow of time) and endpoint (bounded-below monotonic → the C_2 conformal floor is the forced attractor) — GIVEN the geometry. It does NOT force the geometry (c_UV=137, D_IV⁵). So #79 factors: descent-forced [YES, given geometry, conditional on T2112 Tier I] vs geometry-forced [OPEN core, = the real #79, = the K943 minimality audit]. c(t) = 137 − commitment-count (floored at C_2=6) ties c-theorem↔commitment↔fill-rate↔DE. No-weld: c_IR=6 (central charge) ≠ shed coset dim=6, both C_2 different roles. Reframe ≠ resolution; #79 core stays open, now localized to the geometry's UV forcing. Cal audits. Nothing pushed.
