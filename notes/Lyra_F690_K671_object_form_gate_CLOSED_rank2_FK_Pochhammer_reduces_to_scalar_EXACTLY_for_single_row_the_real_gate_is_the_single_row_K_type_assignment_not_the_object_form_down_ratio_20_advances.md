# F690 — K671/K901 assignment (pin the rank-2 FK object-form for the down-quark 20): **the object-form gate is CLOSED — the rank-2 two-factor FK generalized Pochhammer REDUCES to the scalar (ν)_ℓ *exactly* for single-row (ℓ,0) K-types**, so the scalar F506 used is not an approximation and the 20 is not at risk from the object-form. This was in fact already resolved inside F506 (Section "GATE ... RESOLVED"); K901 re-raised it as if open — it is closed. **The real remaining load-bearing assumption is NOT the object-form but the single-row K-type ASSIGNMENT** (that the three generations sit at pure (ℓ,0) radial modes, not two-row mixed modes) — verified here that only pure single-row {(1,0),(3,0),(5,0)} gives the full ladder 1:20:840; two-row alternatives all miss (s/d = 6, or b/d = 180). That assignment is principled (a generation = a diagonal-ray / highest-weight radial mode = single-row; two-row = the mixing sector — consistent with the banked generation-as-support-stratum picture, F676/T2525), but its rigorous confirmation — that the blind cohomology degrees {1,3,5} (T1929) are single-row K-types — is **Elie's rep lane, not the object-form.** So: **the down-quark ratio m_s/m_d = (N_c+1)(N_c+2) = 20 (0.5%, zero parameters, ν=N_c forced) ADVANCES — the FK object-form gate is closed — and it is now gated on (a) the single-row K-type assignment (Elie) and (b) the mass-direction (mass ∝ (ν)_λ, Grace), NOT on the FK object-form.** Via F689 (λ = 1/√(m_s/m_d) by the banked Gatto syzygy), the Cabibbo λ = 1/√20 = 0.2236 inherits this: **candidate-derived, advanced by one gate.** Held at candidate — I do NOT over-swing to "derived" (two gates remain).

**Lyra, Sat 2026-07-25 09:21 EDT. My K901 assignment: pin the FK rank-2 object-form. Done — it closes (scalar exact for single-row), and it was already closed in F506. Correcting the framing so the team doesn't re-litigate a settled gate; relocating the real remaining gate to the single-row assignment (Elie) + direction (Grace). Tier held at candidate; knob untouched.**

## The object-form gate (K671/K901) — CLOSED
The FK generalized Pochhammer for rank r, root multiplicity a: **(ν)_λ = ∏_{j=1}^r (ν − (j−1)a/2)_{λ_j}.** For D_IV⁵ (r=2, a=3): **(ν)_λ = (ν)_{λ₁}·(ν − 3/2)_{λ₂}.**
- **Single-row (ℓ,0):** the second factor is (ν−3/2)_0 = 1 (empty product), so **(ν)_{(ℓ,0)} = (ν)_ℓ — the two-factor product is IDENTICAL to the scalar.** Verified: (3)_{(1,0)}=3, (3)_{(3,0)}=60, (3)_{(5,0)}=2520 → 1:20:840, s/d = (N_c+1)(N_c+2) = 20 exact.
- **So the scalar (ν)_ℓ F506 used is EXACT, not a rank-2 approximation.** The object-form does not put the 20 at risk. **K671's "scalar vs two-factor" gate CLOSES.** (Already shown in F506 Section GATE; re-confirmed here — the published FK rank-2 reference is not needed to settle this, the reduction is elementary.)

## The real remaining gate — the single-row K-type ASSIGNMENT (relocated)
The 20 and the full ladder 1:20:840 depend on the three generations being **pure single-row (ℓ,0)** K-types. Verified that two-row alternatives miss:
| assignment | ladder | s/d | b/d |
|---|---|---|---|
| **{(1,0),(3,0),(5,0)}** single-row | 3:60:2520 | **20** ✓ | **840** ✓ |
| {(1,0),(2,1),(3,2)} | 3:18:225 | 6 ✗ | 75 ✗ |
| {(1,0),(3,0),(4,1)} | 3:60:540 | 20 | 180 ✗ |
- **Principled (not tuned):** a generation = a diagonal-ray / highest-weight radial mode = single-row (ℓ,0); two-row (both Cartan directions active) = a *mixed* mode = the mixing sector, not a generation. This is consistent with the banked generation-as-support-stratum picture (F676/T2525: generations = the 3 KW radial support strata) and with F503's diagonal-ray peak radii.
- **But its rigorous confirmation is a rep-theory fact, not the object-form:** are the blind cohomology degrees {1,3,5} (T1929) single-row K-types? **That is Elie's rep lane.** (The object-form is settled; the K-type assignment is the open piece.)

## Honest tier (do NOT over-swing to "derived")
- **CLOSED:** the FK object-form gate (scalar = two-factor for single-row; verified). The 20 is not at risk from the object-form.
- **DERIVED (banked):** the Gatto syzygy λ² = m_d/m_s (F585/F684); the reduction λ ⟺ m_s/m_d (F689).
- **CANDIDATE (advanced, gated):** m_s/m_d = (N_c+1)(N_c+2) = 20 at ν=N_c — object-form closed, now gated on (a) single-row assignment (Elie: is {1,3,5} single-row cohomology?) and (b) the mass-direction ∝ (ν)_λ (Grace's commitment-cost lane). And therefore λ = 1/√20 (candidate-derived, same two gates).
- **NOT claimed:** that m_s/m_d = 20 or λ = 1/√20 is DERIVED. Two gates remain. (Grace over-swung to "derived" and corrected — I hold at candidate: object-form closed ≠ result derived.)

## Tiers / handoffs
- **@Keeper (K901)** — the FK rank-2 object-form gate you re-raised is CLOSED (and was already closed in F506): for single-row (ℓ,0) K-types the two-factor product's second factor is (ν−3/2)_0 = 1, so it EQUALS the scalar (ν)_ℓ exactly. The published rank-2 FK reference is not needed — the reduction is elementary. The 20 is not at risk from the object-form. **Relocate the gate:** the remaining load-bearing assumption is the single-row K-type ASSIGNMENT (Elie's rep lane), not the object-form. Down ratio 20 → object-form-CLOSED candidate, gated on single-row + direction. Cabibbo λ = 1/√20 inherits (via Gatto/F689) — candidate-derived, advanced. Not banked; two gates remain.
- **@Elie** — the real gate is now yours: are the blind cohomology degrees {1,3,5} (T1929) **single-row (ℓ,0) K-types**, or two-row? Only pure single-row {(1,0),(3,0),(5,0)} gives 1:20:840 (verified — two-row alternatives miss). If your rep lane confirms {1,3,5} is single-row cohomology, the object-form-closed + single-row-confirmed down ratio 20 is target-innocently forced, and λ = 1/√20 with it. That's the pin.
- **@Grace** — you correctly reopened your own modulus over-negative (F506 IS mechanism-backed). The object-form gate is now closed (scalar exact for single-row). Your remaining lane: the mass-DIRECTION (why mass ∝ (ν)_λ = 1/‖section‖², not the inverse — the commitment-cost reading). That + Elie's single-row check are the two gates between candidate and derived. Hold at candidate (as you corrected to); the object-form is no longer a gate.
- **@Casey** — pinned the object-form for the down-quark 20, and the good news is it holds: there was a worry that the clean "20 = 4×5" used a simplified version of the math object, and the real rank-2 object might change the number. It doesn't — for the kind of mode a generation is (a pure "radial" mode, one direction active), the fancy two-factor object collapses to exactly the simple factorial, because the second factor is an empty product equal to one. So the 20 is safe from that worry (and it was already settled back in July; today just confirms it and stops us re-litigating it). What the 20 *does* still hinge on is a physical assumption — that each generation really is that pure radial kind of mode, not a mixed one. That's principled (it matches our whole three-generations picture, where a generation is a radial stratum and the mixed modes are the mixing sector), but the rigorous version is a representation-theory check that's Elie's, and I won't claim it done. So the down-quark ratio 20 — and with it the Cabibbo angle, which is locked to it — moves up one notch: the math-object gate is closed, and it's now a strong candidate gated on one rep-theory fact and the mass-direction argument. I'm holding it at candidate, not derived — we just watched that over-swing happen and get caught, and I'm not repeating it.

Notes only; no toys/theorems claimed. K671/K901 object-form gate CLOSED: rank-2 FK Pochhammer (ν)_λ = (ν)_{λ₁}(ν−3/2)_{λ₂} REDUCES to scalar (ν)_ℓ EXACTLY for single-row (ℓ,0) [2nd factor (ν−3/2)_0=1]; verified 1:20:840, s/d=(N_c+1)(N_c+2)=20 exact. Scalar is EXACT not approximation; 20 not at risk from object-form. Already resolved in F506; re-confirmed. Real remaining gate = single-row K-type ASSIGNMENT (only pure single-row gives 1:20:840; two-row miss) = Elie rep lane + direction = Grace. Down ratio 20 & Cabibbo λ=1/√20 (via Gatto/F689) = CANDIDATE-derived, object-form-closed, gated on single-row + direction. Held at candidate, NOT derived (2 gates remain); knob untouched. — Lyra
