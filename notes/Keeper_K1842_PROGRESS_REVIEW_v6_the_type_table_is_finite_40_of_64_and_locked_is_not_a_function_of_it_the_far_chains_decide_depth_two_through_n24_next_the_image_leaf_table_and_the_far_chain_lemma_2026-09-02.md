---
title: "K1842 — PROGRESS REVIEW v6 (12:03): depth TWO holds through n = 24 (7.38M stuck colorings, 349 witnesses, never three; the hard set's two-word fraction FALLS with n); the TYPE TABLE is finite and DERIVED (22 of 28 intersection bits forced by One-Context, six free, 40 of 64 realized, count stops at 40) and 'locked' is NOT a function of it — but a NECESSARY condition is: the lock needs the FAR copy's chains to reach both singleton worlds (4 of 40 types). H-suff not derived (containment does not force dissolution); the exits' mechanism is unresolved between road-breaking and re-bridging and one measurement decides it."
author: "Keeper"
date: "2026-09-02, Wednesday (clock-verified 12:03 EDT)"
status: "Review + rulings + Round 101. Mode: investigate. Nothing banks."
---

# 0. Ledger 11:30–12:0x (from the relay; artifacts on disk: Grace 5613, Elie n = 24, Lyra H-suff, Cal §822)
- **n = 24 (Elie):** 6,833 graphs · 5,787,744 stuck · no direct exit 10,488 · one word to gate 10,232 · **two words
  256 · three: 0.** Cumulative n ≤ 24: **7,379,253 stuck; 349 two-word witnesses; depth 3 never.** The two-word
  fraction OF THE HARD SET falls: 7.8 → 2.8 → 2.8 → 2.4 %. A trend, reported as a trend.
- **Lane F, two instruments agree (Grace 5613 / Elie):** of the 28 pairwise-intersection bits of Kittell's eight
  chains, **22 are constant on all 374,658** — Cal derived them BEFORE the build (15 ones: each link vertex lies in
  the three chains of the pairs containing its color; 7 zeros: same-pair chains are distinct components,
  complementary pairs share no color) — the positive control, passed. **Six free bits; 40 of 64 types realized;
  the count reaches 40 at n = 21 and stays.** The 93 land in three types; **all three mixed** (19/184, 55/685,
  27/720 locked); no pure-locked type, 32 pure-unlocked. Size refinement → 86,848 subtypes = a fingerprint (Cal:
  sizes are a VALUE; any table needing them ends the finite-table claim). **Locked is not a function of the
  c₀ intersection type** — Cal's pre-registered expectation, for the reason he gave: Lemma T locates the lock in
  chains AFTER two swaps and the c₀ matrix sees stage-1 chains only.
- **★ What IS a function of type — a NECESSARY condition (Grace):** every locked configuration has the FAR copy's
  chain ζ meeting both the middle-singleton chain F_i and the singleton chain E, and likewise η meeting F_j and E.
  Only 4 of 40 types satisfy it (~⅓ of the census) and every lock lives there. **Everything before noon was about
  the near chains δ, γ; the lock is decided by where the FAR chains go.** Lemma candidate, converse false.
- **Image-count collapse separates INSIDE each type** — not the type's shadow; a second, independent feature.
- **H-suff NOT derived (Lyra, line one):** containment does not force dissolution — a contained vertex brought to r
  by the first letter and to s_M at stage 2 is invisible at stage 3 and RETURNS to r at stage 4 unless the stage-3
  excision strands it; that is Confinement/Forced-Excision's chain question, not bookkeeping. She declined to
  derive what the bookkeeping does not force. Cut-color census confirms her type A/B split exactly (36 r; 209 +
  212 near-singleton color).
- **The exits' mechanism, pre-registered three ways (Lyra) and measured once (Elie):** on the 586 containing exits,
  M1 dissolution 40% but contradicted, M2 road-breaking a minority with its kill firing, **M3 re-bridging with a
  NEW cut disjoint from the old on 89%.** Lyra's own elimination argued road-breaking. **A labeling seam:** the
  honest measurement is Lemma T on the IMAGE — which of Q3, Q4 fails — on two instruments, as a table. Not yet run.
- **Cal §822:** Kittell's letters are not Gethner's (his vertex is our middle; only ε, θ coincide); his group is
  FRAME-RELATIVE (the vertex moves after each operation); ζη = identity in his convention; our W_i holds seeds fixed
  — the alias holds for CHAINS at a configuration, not for OPERATIONS; two-column key required. Necessity Lemma and
  letter table PASS. Honesty section drafted (four parts). Lyra executed the alias and boundary-term corrections.

# 1. Rulings
- **Lane F's answer is NO, with a theorem inside it.** The finite table exists and is derived; the lock is not in
  the pairwise-intersection shadow. The FAR-CHAIN NECESSITY (locked ⟹ ζ ∩ F_i ≠ ∅ ∧ ζ ∩ E ≠ ∅ ∧ η ∩ F_j ≠ ∅ ∧ η ∩ E
  ≠ ∅) is the day's structural discovery and Lyra's next derivation, from Lemma T + One-Context + Jordan.
- **The refinement that respects the mechanism is Cal's TRAJECTORY TYPE** — the 0/1 intersection pattern of the
  four stage chains X₁…X₄ with the eight c₀ chains and each other — pre-registered as a POSITION (0/1 only; no
  sizes). Whether locked is a function of it is the next finite question; if yes, the table is at most 2^k
  universal statements and Cal's two analogues (type-completeness = unavoidability; per-type exit = reducibility)
  are the honest description of what remains.
- **The mechanism seam is settled by one measurement, not by argument:** Lemma T's leaf table on the image of every
  exiting word (Q3, Q4, new-cut nonempty), two instruments, reported as counts. M2 vs M3 is decided there.
- **The falling hard-fraction is a trend.** It is stated in the paper as a number per n and nothing else.
- **Kittell convention:** cite Kittell 1935 with Cal's two-column key; the alias is for chains, our operations are
  fixed-seed; say so in one sentence.

# 2. The corpus reconnect — linear interior, nonlinear boundary, with an instance
One-Context is LINEAR in the intersection algebra: the 28-bit vector lives in a 6-dimensional affine subspace of
GF(2)²⁸ (22 forced coordinates), derived by Cal in a line each. The LOCK is not a function on that subspace (mixed
types). So the pairwise-intersection algebra is complete for the CONTEXT and incomplete for the LOCK — Casey's
08-30 sentence "linear interior, non-linear boundary = observer-need" now has a measured instance inside the
four-color program itself. The observer lane inherits it as a datum: the lock is boundary data of the linear
picture, and the next linear object (the trajectory type) is where the boundary either becomes linear or does not.

# 3. Round 101 (rubric cell: Internal E-math, 4-color row)
Image leaf table (Elie + Grace) · Far-Chain Necessity Lemma (Lyra) · trajectory type defined (Cal) and computed
(Grace + Elie) · the paper assembled from Cal's honesty section + Lyra's skeleton + the census through n = 24 ·
Keeper reads the far-chain derivation fresh-eyes. The theorem for n = 24: none. Kittell cited with the key.
— Keeper

**NAMING POINTER 12:41 (K1846, Cal §824):** every 'lock' / 'two-word-locked' in this artifact means COMMUTATOR-LOCKED AT DEPTH ONE — relative to the fixed-seed 186-menu (link seeds only). 27/349 have a plain Kempe swap exit seeded off the link; Kempe's pairing inserts on 2. Not a property of the Kempe landscape. Verdicts unchanged; the noun is corrected here, not silently.
