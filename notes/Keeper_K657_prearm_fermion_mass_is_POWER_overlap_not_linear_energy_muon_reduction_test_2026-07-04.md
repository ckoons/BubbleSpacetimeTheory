# K657 (PRE-ARM) — The Fermion Mass Is a POWER/Overlap Form (per the Muon), NOT a Linear Energy. Lyra's "Linear Like the Glueball" Conflates Energy-Linear with Mass-Linear. Consistency Test: the Quark Kernel Must Reduce to the Muon.

**Auditor:** Keeper | **Date:** 2026-07-04 | **Status:** PRE-ARM for the quark-overlap long pull — flags a map tension + gives the faithful-extension test | **Count 8**

Studied the base overlap (T190/muon) to arm the Step-1 adjudication. Found a tension in the pinned map that must be watched.

## The base overlap (muon) is a POWER form — empirical fact

**m_μ/m_e = (24/π²)^{C_2} = 206.761 (0.003%).** 24 = N_c·|W(B₂)| = 3×8 (colorless Weyl-orbit reading, per F48), π² = 2 angular kernel integrations, exponent = C_2 = 6. This is a **power/product form** — the matrix element of the mass operator picks up (24/π²) **per radial step**, C_2 steps → (24/π²)^{C_2}. **The fermion mass is a MATRIX ELEMENT (overlap), a product over steps — exponential in the step count.**

## The tension in the pinned map

- **Glueball (F292/T2490):** m ∝ E = λ₀ + step — the mass IS the energy eigenvalue, **linear** in the level.
- **Lepton (T190):** m_μ/m_e = (24/π²)^{C_2} — the mass is an **overlap matrix element**, a **power** form.

**These are different level-dependences.** Lyra pinned the quark as "linear like the glueball" because a lone quark isn't Regge-spinning (m²∝J). **That inference is half-right and half-conflated:**
- ✅ **Right:** a lone quark is not two-body Regge (m²∝J) — no spinning to square it.
- ⚠️ **Conflated:** "not Regge" does NOT make it the glueball *energy* (linear). The **lepton is also a single object** and gives a **power** overlap, not a linear energy. A single object → power form is the fermion precedent (the muon).

So the honest statement is likely: **the ENERGY (conformal weight) is linear in the level; the MASS = base^{energy} (a power/overlap form).** "m ∝ E linear" (mass linear in energy) contradicts the muon; "E linear in level, mass = base^E" is consistent. **The down-ladder ratios are then POWER/overlap ratios, not linear-energy ratios** — which corrects the long-pull prompt's Step-4 expectation ("do the levels give 20 as *linear-energy* ratios").

## Why placement still fails (consistent with Grace/Elie)

Neither linear (levels ~1,20,900) nor simple single-base power (base^level with base ∈ {24/π², π, n_C, √10} gives non-integer levels 3.37, 7.65…) places {1,20,900} cleanly. So the quark overlap is **more structured than one base to one power** — the color channel adds structure (per-generation, K654/K655). Placement cannot pick the form; the operator must.

## THE consistency test for Step 1 (un-fishable, faithful-extension)

> **The quark overlap kernel must REDUCE to the muon's (24/π²)^{C_2} in the colorless limit.** Turn off the color channel → the quark kernel must give back the lepton overlap (the same machinery that produces (24/π²)^6 at 0.003%). If it can't reproduce the muon, it is **not the same overlap** — it's a reshaped computation, and the down levels it produces are suspect.

This is the strongest faithful-extension check available: the quark overlap is the *color extension* of the lepton overlap, so removing color must return the lepton result.

## Adjudication criteria (for the landing)

1. **Expect a POWER/overlap form (per T190), not a linear-energy form.** If Lyra reports the quark mass as *linear in the level* (glueball-style), flag the conflict with the muon — a fermion mass is an overlap matrix element (power), demonstrated by T190.
2. **Colorless-limit reduction to the muon** is the faithful-extension test — demand it.
3. The color channel supplies the down-ladder structure per-generation (K654/K655/K656) — forward, not fitted.

## Net

The fermion mass is a **power/overlap** form (muon: (24/π²)^{C_2}), so the quark down-ladder should be **overlap ratios, not linear-energy ratios** — a correction to the "linear like the glueball" pin (energy is linear; mass is power). The un-fishable test: **the quark kernel must reduce to the muon (24/π²)^{C_2} when color is off.** Watch Step 1 for that reduction; watch Step 4 for a power (not linear) form.

— Keeper K657 pre-arm. Fermion mass = overlap matrix element (power, per T190), not the glueball energy (linear); "linear like the glueball" conflates E-linear with mass-linear. Consistency test: colorless limit of the quark kernel must give back the muon (24/π²)^{C_2}.
