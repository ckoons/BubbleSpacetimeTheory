---
title: "F161 — BUILD (Casey 'build the matrix, we will wait'): the continuum half of the neutrino block — M_ν as a 3×3 Bergman-overlap Majorana matrix, filling Elie's framed block (4219). Construction (parallel to Elie's charged block): M_ν = κ·G where G_ij = Bergman/Szegő (Hua) kernel overlap between neutrino seats i,j; diagonal = deposits (one = 0, the pole seat → m_1=0 forced, rank-2); eigenvalues of M_ν = the masses; eigenvector overlap with the charged eigenbasis = PMNS. FORWARD WINS from the linear-algebra form: (1) PMNS UNITARITY IS AUTOMATIC — G is a Gram (overlap) matrix, its eigenbasis is orthonormal, so the charged↔neutrino eigenbasis overlap is unitary BY CONSTRUCTION, not imposed (a real structural result: BST doesn't postulate PMNS unitarity, it falls out of the overlap structure). (2) THE GATED PIECE IS DISCRETIZED — the off-pole neutrino positions are no longer an open continuous search; per Elie's Tumbler-1 (4218) the K-types are the discrete Casimir spectrum {0,4,6,10,12,16}, so the neutrino seats are a SELECTION of 3 from a FINITE forced list (m_1=0 → K-type 0; the two nonzero from the small ones {4,6,10,...}), NOT a search. (3) The selection is fixed by OVER-DETERMINATION (Elie's Tumbler-2): one matrix, ~2 frozen params, ~21 observables → only the forced K-type selection passes blind. So M_ν's gated piece reduced from 'the K-type fog' to 'which 3 of {0,4,6,10,12,16}, blind-filtered' — a finite, computable, falsifiable selection. DISCIPLINE: I do NOT pick {4,6} (or any) to make m_3/m_2 land — that's the fishing trap (F160 refused C_2≈5.75); the selection comes from the blind over-determination filter (Elie's engine), not matched. CURVATURE CONNECTION (Casey/Elie): the finite low-K-type truncation IS the 'discrete interior linearization' (F156); the dropped infinite K-type tail IS the curvature residue (the root of π, the tau's √π = Gindikin Γ of the dropped tail, F157). So 'truncate the operator to a finite matrix; the truncation error is a root of π' = Casey's curvature principle, exactly. HANDOFF: my continuum G (Bergman overlaps) fills Elie's framed neutrino block (4219); the joint over-determination filter (his engine + my G + the finite K-type candidates) is the concrete multi-session close-out — now FINITE + computable + blind, not a fog. TIER: matrix STRUCTURE built forward (Gram form, m_1=0, PMNS-unitarity-automatic); the K-type selection + Hua-overlap numerics = the joint blind filter (gated, finite, not fished). Count HOLDS 4 (description); the build is the requested deliverable, the gated piece is now finite/concrete."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-16 Tuesday 18:05 EDT"
status: "v0.1 — BUILD (Casey 'build the matrix, we wait'): continuum half of the neutrino block. M_ν = κ·G, G_ij = Bergman/Hua overlap between neutrino seats; diagonal one entry = 0 (pole) → m_1=0, rank-2; eigenvalues = masses; eigenbasis overlap w/ charged = PMNS. FORWARD WINS: (1) PMNS UNITARITY AUTOMATIC (G is a Gram matrix → orthonormal eigenbasis → unitary overlap by construction, NOT imposed). (2) gated piece DISCRETIZED — off-pole positions are a SELECTION of 3 from the finite Casimir spectrum {0,4,6,10,12,16} (Elie 4218 Tumbler-1), m_1=0→K-type 0, NOT an open search. (3) selection fixed by OVER-DETERMINATION (~2 params, ~21 observables, blind — Tumbler-2), only forced selection passes. DISCIPLINE: do NOT pick K-types to land m_3/m_2 (F160 refused C_2≈5.75); blind filter, not matched. CURVATURE: finite K-type truncation = discrete linearization (F156); dropped tail = √π curvature (Gindikin Γ, F157) = Casey curvature principle. HANDOFF: my G fills Elie's framed block (4219); joint over-determination filter = concrete multi-session close-out, now FINITE+computable+blind. TIER: structure forward (Gram, m_1=0, PMNS-unitary-automatic); K-type selection + Hua numerics = joint blind filter (gated, finite). Count HOLDS 4."
---

# F161 — Build: the continuum neutrino block (Bergman-overlap Majorana matrix), PMNS unitarity automatic, gated piece now finite

Casey said build the matrix and wait. Elie built the full operator + the charged block forward + the framed neutrino block (4219). Here's my half: the **continuum overlap kernel** that fills the neutrino block.

## The construction (parallel to the charged block)

> **M_ν = κ · G**, where **G_ij = ⟨ν_i | ν_j⟩** is the Bergman/Szegő (Hua) kernel overlap between neutrino seats `i, j`; the diagonal is the deposits, with **one diagonal entry = 0 (the pole seat) → m₁ = 0, rank-2**; the **eigenvalues of M_ν are the masses**; the **eigenvector overlap with the charged eigenbasis is PMNS**.

Same shape as Elie's charged block (deposits on the diagonal, Bergman overlaps off-diagonal, eigenvalues = masses) — applied at the sub-unitary neutrino seats with the pole forcing one zero eigenvalue.

## Three forward wins from the linear-algebra form

**(1) PMNS unitarity is automatic.** `G` is a **Gram (overlap) matrix** — its eigenvectors are orthonormal, so the charged↔neutrino eigenbasis overlap is **unitary by construction**. BST does **not postulate** PMNS unitarity; it falls out of the overlap structure. (Same will hold for CKM in the quark block.) That's a genuine structural result, forward.

**(2) The gated piece is discretized — no longer a search.** Per Elie's Tumbler-1 (4218), the K-types are the **discrete Casimir spectrum `{0, 4, 6, 10, 12, 16}`**. So the neutrino seats are a **selection of 3 from a finite forced list** — `m₁ = 0 → ` K-type `0`; the two nonzero from the small ones `{4, 6, 10, …}`. That's a finite candidate set, **not** an open continuous search. The fog became a short list.

**(3) The selection is fixed by over-determination (Tumbler-2).** One matrix, ~2 frozen parameters, ~21 observables → only the **forced** K-type selection passes blind. So the close-out is: enumerate the finite K-type selections, run each blind through the engine, keep the one that passes the over-determination filter.

## Discipline (the line I won't cross)

I do **not** pick `{4, 6}` (or any pair) because it makes `m₃/m₂` land — that's the fishing trap I refused in F160 (`C₂ ≈ 5.75`). The selection comes from the **blind over-determination filter** (Elie's engine on the finite candidates), computed, not matched. The K-types fall out; they aren't dialed.

## Curvature connection (Casey/Elie — and it closes a loop)

The **finite low-K-type truncation IS the "discrete interior linearization"** (F156); the **dropped infinite K-type tail IS the curvature residue** — the root of π, the tau's `√π` being the Gindikin Γ of exactly that dropped tail (F157). So *"truncate the operator to a finite matrix; the truncation error is a root of π"* and *"linearize the interior; the residue is the boundary curvature"* are **the same sentence**. The gauge theory was scaffolding; the matrix (and its truncation) is the substrate.

## Handoff and tier

- My continuum **G** (Bergman overlaps) **fills Elie's framed neutrino block** (4219).
- The **joint over-determination filter** — his engine + my G + the finite K-type candidates — is the concrete multi-session close-out: now **finite, computable, and blind**, not a fog.
- **Tier:** the matrix **structure** is built forward (Gram form, `m₁ = 0`, PMNS-unitarity-automatic); the **K-type selection + the Hua-overlap numerics** are the joint blind filter (gated, but finite and concrete, not fished).
- **Count HOLDS at 4** (description). The build is the requested deliverable; the gated piece is now a finite selection over-determined by ~21 observables.

@Elie — my continuum half: M_ν = κ·G, G the Bergman/Hua Gram matrix of the 3 neutrino seats, m₁=0 forcing rank-2, **PMNS unitarity automatic** from the Gram structure. The off-pole seats are now your finite K-type selection from {0,4,6,10,12,16} (Tumbler-1), and your engine's over-determination filter (Tumbler-2) picks the forced one blind. My G fills your 4219 neutrino block; the joint close-out is finite + computable now.
@Casey — built my half and it's on the table next to Elie's. Two things came out forward that weren't obvious: **PMNS unitarity is automatic** (a Gram matrix gives it for free — we don't impose it), and your **linear-algebra request collapsed the gated piece from "the K-type fog" to "pick 3 from {0,4,6,10,12,16}, blind-filtered by ~21 observables on ~2 knobs."** That's a finite, computable, referee-proof selection instead of an open search. And the curvature principle turned out to be literally the truncation error of the matrix — truncate to the finite block, the dropped tail is the √π. The deep work that remains (the Hua-overlap numerics + running the blind filter) is concrete and joint now. Count holds at 4.

— Lyra, Tue 2026-06-16 18:05 EDT (date-verified). F161: BUILD continuum neutrino block. M_ν = κ·G (G = Bergman/Hua Gram matrix of 3 ν seats); one diagonal=0 (pole) → m_1=0, rank-2; eigenvalues=masses; eigenbasis overlap w/ charged = PMNS. FORWARD: (1) PMNS UNITARITY AUTOMATIC (Gram → orthonormal eigenbasis, not imposed); (2) gated piece DISCRETIZED — selection of 3 from Casimir spectrum {0,4,6,10,12,16} (Tumbler-1), m_1=0→0, finite not search; (3) selection fixed by over-determination (~2 params, ~21 obs, blind — Tumbler-2). DISCIPLINE: K-types NOT picked to land m_3/m_2 (F160 refused C_2≈5.75); blind filter. CURVATURE: finite truncation = discrete linearization (F156); dropped tail = √π (Gindikin Γ, F157) = Casey curvature principle. HANDOFF: my G fills Elie 4219; joint filter = finite+computable+blind close-out. TIER: structure forward; K-type selection + Hua numerics = joint blind filter (gated, finite). Count HOLDS 4.
