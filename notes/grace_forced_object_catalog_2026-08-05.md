# Grace — the FORCED-OBJECT CATALOG for the Ordered-Product Refinement Procedure (2026-08-05, K1191 guard #2)
**Cal's guard #2 (standing): a Type-1/2 "right object / forced scale" claim is legitimate ONLY if the object comes from a NAMED, FINITE, FORCED catalog DECLARED BEFORE the check.** This is that catalog. An object NOT on this list cannot be invoked to explain a fit — it is then a Type-3 input (honest, on the floor) or the claim fails. Declared before Elie's fire so it can refute, not endlessly re-invent. This is the falsifiability armor on the refinement method.

## FORCED OBJECTS (the operators/structures a rung may legitimately be built from)
| # | object | what it forces | tier |
|---|--------|----------------|------|
| O1 | FK Pochhammer norms (ν)_λ at ν=N_c=3 (Wallach threshold) | down/lepton mass ladder (1:20:840) | Derived |
| O2 | Bergman kernel K(0,0)=1920/π⁵ | m_p/m_e=6π⁵, lepton masses, the Born measure | Derived |
| O3 | geodesic-distance VECTOR to the Shilov boundary (F85) | up mass ladder, up ordering, lightest-quark-deepest (k=0 ground) | **task #78 (open)** |
| O4 | degree-1 cohomology operator J_W on ℤ[h]/h⁶ | weak charged current; cross-parity mixing; texture-zero M_11=0 | Derived (op); direction open |
| O5 | Peirce decomposition J_1⊕J_½⊕J_0 = 1⊕3⊕1 | color=V₁₂=N_c, 3 generations, fermions (T2543) | Derived |
| O6 | isotropy chain SO(5,2)→SO(4,2)→SO(3,1) | gauge structure, sin²θ_W-form, 3+1 signature | Derived |
| O7 | Peirce idempotent / commit operator | the committed record (mass = commit), the commit→emit ORDER | Derived |

## FORCED SCALES (the magnitudes a rung may legitimately be normalized by)
| # | scale | value | source |
|---|-------|-------|--------|
| S1 | boundary per-shell quantum | α = 1/N_max | O3/T2515 (one geodesic shell = ln N_max) |
| S2 | Wallach threshold | ν = N_c = 3 | forced (FK singular point) |
| S3 | the commitment tick t_B | the unit | input floor (dimensionful #1) |
| S4 | the cosmic age | cc value | input floor (dimensionful #2) |

## The rule (declared before the fire)
- A Type-1 (scale) fix must pin to one of S1–S4. A Type-2 (object) fix must swap in one of O1–O7. **Both must then AGREE WITH THE BRUTE INTEGRAL (guard #1)** — the object is real only if the two match.
- If no catalog object/scale removes the gap → it is a **Type-3 genuine input**, honestly Identified, sitting on the input floor with the neutrino ratio and the cc value — and it must be REACHABLE (guard #3).
- **No favorable prior (guard #4):** the catalog is fixed; a rung cannot spawn a new object mid-check.

## Current open rungs, classified against the catalog (declared)
- **Up-anomaly (up→charm 4.2×α⁻¹ steeper):** Type-2, object = **O3** (geodesic-distance VECTOR, not scalar α; lightest-up = k=0 ground mode deepest). Promotion = task #78 (F85). Falsifiable: the up-quark must sit at the geodesic distance F85 forces, or it doesn't.
- **V_cb overshoot:** Type-2, object = the full 3-generation product (O4 with both towers), not the 2-gen fragment.
- **Gatto DIRECTION (small-mixing vs its opposite):** the decider. Magnitude forced (O1 gives √20). DIRECTION = the Type-3 decider — Casey's insight is that **O7 (the commit→emit ORDER) may fix it** (commit the mass-ordering first, emit second → the small branch). If O7 forces the direction → "explains Gatto"; if not → "matched." Elie's fire (handed) + must agree with brute integral.

## Note
This catalog IS the "what BST is allowed to invoke" ledger — the complement of the input floor (what BST is forced to assume). Together: input floor = the ~4 assumptions; forced-object catalog = the ~11 forced building-blocks; everything else is derived from these or is an honest Type-3 input. Nothing outside this list explains a fit.
