---
title: "CORRIGENDUM (pre-external) — frozen centerpiece v0.8: the census is multiplicity-free (ℂ⊕ℍ⊕ℝ), not ℂ⊕ℍ⊕M₃(ℝ)"
author: "Lyra (Claude Opus 4.8) — Casey Koons, PI; Keeper re-audit"
date: "2026-08-20 Thursday (date-verified)"
status: "Lane B record, K1743. The frozen v0.8 HEADLINE (census-as-count 0+1+3=4=dim EW, no internal color group) is TRUE and stands. This records ONE notational line in frozen v0.8 that carries the bare-End slip and must be corrected for any external use: the census is the multiset of reality types, multiplicity-free, so End_K(H_F) = ℂ⊕ℍ⊕ℝ (ℝ at mult 1), NOT ℂ⊕ℍ⊕M₃(ℝ). The color 3 is a separate rep-dimension reading (dim V₁₂ = N_c). Do not edit the frozen v0.8 (its hash stands); use the paper v0.4 (or v0.8 + this corrigendum) for any external send. Keeper re-audits to PASS. Nothing pushed; CP existence-only."
---

# Corrigendum — frozen centerpiece (v0.8), the census line

## The frozen text (headline correct; one line needs the fix)

The frozen, hash-certified centerpiece (`..._census_as_count_..._v0.8_...`) states, at line 42:

> **H_F = (ℂ, 1) ⊕ (ℍ, 1) ⊕ (ℝ, 3) ⇒ End_K(H_F) = ℂ ⊕ ℍ ⊕ M₃(ℝ).**

**The centerpiece's headline is TRUE and stands — there is no retraction of the result.** The census-as-count 0 + 1 + 3 = 4 = dim(U(1)×SU(2)) and the "no internal color group" conclusion are correct (K1724; K1743 confirms the *uniform* census read was right all along). Only the *algebra notation* on this one line carries a slip.

## The slip, and the correction (K1743)

"(ℝ, 3) ⇒ M₃(ℝ)" is the **bare-End slip**: M₃(ℝ) = End_ℝ(V₁₂) is the endomorphism algebra of the color 3-space *ignoring the K-action*. The census reads the K-**equivariant** commutant, and for a **real irrep** that commutant is just **ℝ**. So the correct line is:

> **H_F = (ℂ, 1) ⊕ (ℍ, 1) ⊕ (ℝ, 1) ⇒ End_K(H_F) = ℂ ⊕ ℍ ⊕ ℝ** (multiplicity-free).

Two readings must be kept apart, and the frozen line conflated them:

- the **census** = the multiset of reality types {ℂ, ℍ, ℝ}, **multiplicity-free** (each block occurs once);
- the **rep-dimension** N_c = n_C − 2 = dim V₁₂ = 3 — the *dimension* of the color block, a separate reading, **not** a census multiplicity.

The count itself is unaffected: {U(ℝ), U(ℂ), U(ℍ)} = {0, 1, 3} → 0 + 1 + 3 = 4, because U(ℝ) is 0-dimensional. The "no internal color" conclusion is likewise unaffected (the ℝ block gives 0 internal generators; and Coleman–Mandula rules the geometric SO(V₁₂) a spacetime symmetry — a *separate* support line, not the count's second line).

## Disposition

- **Do NOT edit the frozen v0.8** — its hash is the certified freeze record. Leave it intact.
- **The paper (`..._Internal_SM_from_one_measured_integer_n_C_5_..._v0.4`)** carries the corrected algebra (ℂ⊕ℍ⊕ℝ, multiplicity-free) throughout, with the bare-End slip flagged and the rep-dimension reading kept separate. Use **v0.4** for any external send.
- If frozen v0.8 itself must go out as the exact artifact, attach **this corrigendum** so the correction travels with it.

## Keeper re-audit

Requested: Keeper re-audits this corrigendum to PASS — confirming (a) no retraction (the frozen headline is true), (b) the bare-End correction is right and sourced (K1743; K-equivariant commutant of a real irrep = ℝ), (c) the paper v0.4 is the correct external-ready successor. On PASS, external sends use v0.4 (or v0.8 + this corrigendum), never bare v0.8.

— Lyra, Thursday 2026-08-20 (date-verified). Nothing pushed; CP existence-only.
