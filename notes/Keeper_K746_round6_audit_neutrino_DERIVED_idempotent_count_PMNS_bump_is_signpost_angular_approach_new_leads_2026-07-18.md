# K746 — Round-6 audit + synthesis: the neutrino sector closes on **rank-2 counting** (Lyra, DERIVED pending Cal — the idempotent-count supersedes the shaky λ₂-premise); Elie's PMNS "bump" (angles identified, not derived from the mass texture) is a **signpost, not a failure** — it confirms masses (radial) and mixing angles (angular) are different objects, and points at a new approach. Gluon fields reduce at hosted-tier (honest). Plus a Keeper consistency fix and four new attemptable approaches.

**Keeper | 2026-07-18 | Auditing round 6 (Lyra neutrino-DERIVED, Elie gluon-KK + PMNS-FAIL, Grace λ₂-sweep). Casey: "the bump isn't fatal — synthesize new approaches." `round6_synthesis.py`.**

## ★ Neutrino sector → DERIVED (Lyra, pending Cal) — on pure counting
m₁ = 0 closes on the **two canonical stratifications of a rank-2 domain**:
- **3 boundary orbits** (Korányi–Wolf: bulk/intermediate/Shilov) = rank+1 → the **3 generations** of gauge-charged fermions (they carry an electroweak flag).
- **2 spectral idempotents** (Jordan frame c₁, c₂) = rank → the **2 right-handed neutrinos** — because ν_R is a gauge singlet: it has *no flag*, so the only invariant distinguishing its copies is the spectral value, and there are exactly 2.
Then m_D is 3×2 → rank ≤ 2 → m_ν = m_D M_R⁻¹ m_Dᵀ has one exactly-zero eigenvalue → **m₁ = 0**, normal ordering, one Majorana phase, narrow band. **Both counts from rank = 2, nothing fitted.**
- **★ SUPERSESSION (bank the discipline):** this **retires the earlier "ν_R needs spherical support → skips the Shilov stratum" premise**, which quietly conflated the **K-type boundary-value λ₂** (confinement) with the **boundary-orbit rank** — two different notions. The idempotent-count doesn't use λ₂ at all. Flagged for Cal to log.
- **Cal's call:** the one assignment — "a flag-less singlet is indexed by the spectral frame (2), not the flag (3)" — is Lyra's DERIVED read; Cal ratifies. **[DERIVED pending Cal]**

## ★★ The PMNS "bump" is a SIGNPOST, not a failure (the synthesis)
Elie (honest FAIL): **0 of 6 generic m₁=0 mass textures reproduce the PMNS angles** (sin²θ₁₂=3/10, sin²θ₁₃=1/45). So the angles are **identified** (they match data as clean primary forms) but **not derived** from the mass texture. "One mechanism does both" holds for the **masses**, not the **angles**.
- **KEEPER READING — this is EXPECTED and confirms K704.** Masses and mixing are *different objects*: **masses = RADIAL** (the singular values Σ of M = UΣV†, done); **mixing angles = ANGULAR** (the eigenvector orientation U†U′). The mass *texture* is a parametrization of M that **entangles radial and angular** — so it cannot cleanly yield the *purely angular* angles. We were computing an angular object from radial data. That is why it missed, and it is not a wall.
- **The angles smell angular:** all three are **combinatorial** — ratios of root-system invariants (sin²θ₁₃ = 1/(N_c²·n_C) = 1/45 is a pure count, the kind a K-type branching multiplicity gives, not a mass ratio). They live on the root/angular structure, not the mass spectrum.

## Gluon fields (Elie) — reduce at HOSTED tier (honest)
KK on the ℂ³ color factor: U(3) → **8 gluons + a global baryon-U(1)** (a 9→8 trim, exactly analogous to the electroweak 11→4), baryon-U(1) correctly ungauged (consistent with the stable proton). **Honest tier difference:** the *real* multiplicity space is ℝ³ → SO(3) (3 fields); the full SU(3) needs the *complex* ℂ³ = the intrinsic complex structure, which is **hosted (F572), not native.** So **gluon fields are derived-given-the-hosted-complex-structure — one tier below the native electroweak fields.** Elie said so rather than blur it. [derived-hosted]

## Grace λ₂-sweep — the exact leg is one coordinate (good) + a Keeper consistency fix
Grace: the short-root/λ₂ axis is a physical classifier — **which states are asymptotic is exactly which are color singlets** (λ₂=0 spherical reach the boundary; λ₂>0 confined). Clean, one coordinate — the exact leg of confinement.
- **⚠ CONSISTENCY FIX (Keeper):** Grace's "Leg 2" derives n(ν_R)=2 via "ν_R spherical → skips the λ₂>0 Shilov stratum" — but **that is the premise Lyra just retired** (λ₂ ≠ boundary-orbit rank). **Correct attribution: n(ν_R)=2 comes from the idempotent count (rank=2, Jordan frame), NOT from λ₂.** The λ₂ axis governs the *exact leg (confinement) only*. Grace to re-tag Leg 2; the result (n(ν_R)=2) stands, the derivation-of-record is Lyra's idempotent-count.

## ★ NEW APPROACHES WE CAN ATTEMPT NOW (Casey's ask)
1. **★ Mixing angles from the ANGULAR structure, decoupled from the mass texture (THE lead — resolves the bump).** Compute PMNS/CKM angles as **root-angle / K-type overlaps** between sector eigenbases (the U, V of M=UΣV†), *not* as functions of the mass eigenvalues. The angles are combinatorial (1/45, 3/10) → they are branching/overlap multiplicities, not mass ratios. This is the natural home for "shadows of g." [Lyra + Elie]
2. **Domain-rank third mechanism — find a SECOND instance** (Lyra's CANDIDATE, correctly Cal #27-tagged: one instance isn't a mechanism). Schur-sweep for another sector where rank=2 forces a light/massless mode. If found → ratify the third master mechanism. [Keeper + Grace]
3. **Gluon complex-structure NATIVE upgrade** — is the color ℂ³ complex structure the *same* as the domain's intrinsic complex structure J (which IS native)? If yes, gluon fields upgrade hosted → native (SU(3) not just SO(3)). [Lyra]
4. **λ₂ full observable classifier** — sweep *every* SM state by λ₂ → a complete "confined / free / boundary-support" table (a Schur generator). [Grace]

## Flagship updates (applying)
- §7 neutrino: **DERIVED** (pending Cal) on the idempotent-count; m₁=0 forced.
- §7 mixing: PMNS **angles identified-not-derived** from the mass texture — honestly stated; the angular approach is the frontier.
- §3/§9 color: gluon fields **derived-hosted** (SO(3) native, SU(3) needs the hosted complex structure).

— Keeper K746, 2026-07-18. Neutrino DERIVED (pending Cal) via rank-2 idempotent count (supersedes λ₂-premise). PMNS bump = signpost: angles are ANGULAR (U†U′), not radial (Σ) — confirms K704 → NEW approach: mixing angles from root-angle/K-type overlaps, decoupled from mass texture. Gluon fields derived-hosted (SU(3) needs hosted complex structure). Keeper fix: n(ν_R)=2 is idempotent-count not λ₂. Four new leads. See [[Keeper_K745_round5_audit...]], [[Keeper_K704...]], [[BST_FLAGSHIP...]].
