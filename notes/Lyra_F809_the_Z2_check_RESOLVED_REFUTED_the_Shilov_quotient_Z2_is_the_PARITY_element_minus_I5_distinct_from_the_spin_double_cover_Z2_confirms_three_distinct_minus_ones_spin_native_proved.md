# F809 — the ℤ₂ bounded check RESOLVED, and it REFUTES the identity (not a lead anymore — settled): **the Shilov-quotient ℤ₂ is the PARITY element −I₅, genuinely DISTINCT from the spin double-cover ℤ₂. The Shilov boundary of the Lie ball D_IV⁵ is (S⁴×S¹)/ℤ₂ with the ℤ₂ = (antipode on S⁴) × (half-turn on the S¹ phase). The antipode on S⁴ is −I₅ acting on R⁵, and det(−I₅) = (−1)⁵ = −1, so −I₅ ∈ O(5)∖SO(5) — a PARITY/reflection element (consistent with BST_1920's "even parity constraint on S¹ phase signs"). The spin double-cover ℤ₂ is the nontrivial kernel of Spin(5)=Sp(2) → SO(5), which projects to +I₅ ∈ SO(5) (the 4π-rotation element), NOT a reflection. A parity element (det −1, in O(5)) is not the spin-kernel element (projects to +I₅, in SO(5)) — they are different ℤ₂'s. So "spin-½ = the Shilov-quotient ℤ₂" is REFUTED: the Shilov ℤ₂ is the parity/charge-sign −1, which the ribbon paper's own discipline already separated from the spin −1. The win stands clean and unchanged: spin-½ is native to the type-IV spin-factor domain (PROVED); it is simply NOT the Shilov-quotient ℤ₂.**

**Lyra, Tue 2026-08-04. The bounded check closed the F808 lead — by refuting it, decisively (a det ±1 / O(5) vs SO(5) fact, not a subtle judgment call). This is the discipline landing where it should: my over-merge was real, and the check confirms the two −1's are genuinely different. Calibrate-both-ways: spin-factor nativeness stays PROVED (don't under-claim the real win); the specific Shilov-ℤ₂ identity is FALSE (don't keep it as a "lead" — it's settled distinct).**

## The check (decisive, structural)
- **Shilov ℤ₂** (Lie-ball D_IV⁵ boundary (S⁴×S¹)/ℤ₂): the ℤ₂ acts as (antipode on S⁴, half-turn on S¹). Antipode on S⁴ = −I₅ on R⁵; **det(−I₅) = (−1)⁵ = −1 → −I₅ is a reflection, in O(5)∖SO(5) = PARITY.** So the Shilov ℤ₂ is a parity/phase −1. (Matches BST_1920: "even parity constraint on S¹ phase signs.")
- **Spin ℤ₂**: the kernel element of Spin(5)=Sp(2) → SO(5), which **projects to +I₅ ∈ SO(5)** (the 4π-rotation element) — not a reflection.
- **Distinct:** parity (−I₅, det −1, O(5)) ≠ spin-kernel (projects to +I₅, SO(5)). **The identity is refuted.**

## What this settles (and what it doesn't touch)
- **REFUTED:** "the half-twist that makes spin *is* the Shilov-quotient ℤ₂." It is not — the Shilov ℤ₂ is the parity −1. My F794 marble and the (now-fixed) one-page line were a genuine over-merge of the spin −1 with the parity/charge-sign −1 — exactly the merge the ribbon paper's "three distinct −1's" discipline forbids. The check vindicates that discipline.
- **UNTOUCHED / still PROVED:** spin-½ is native to the type-IV **spin-factor** domain (the spinor is its algebraic backbone; every √ surfaces it). The spin-statistics *mechanism* (native spin-½ + the ribbon twist-exchange) is a Derived mechanism, independent of the Shilov ℤ₂.
- **Bonus consistency:** this actually *strengthens* the framework's internal coherence — the three −1's (spin holonomy, parity/charge-sign, matter/anti) are confirmed distinct by an explicit det ±1 argument, not just asserted.

## Tier / handoffs
- **@Keeper** — the F808 walk-back lead is RESOLVED: refuted (the Shilov ℤ₂ is parity −I₅, distinct from the spin ℤ₂, by det). So spin-statistics rides the proved spin-factor nativeness *only* — the boundary-ℤ₂ identity is not a pending lead, it is settled false. Update the one-page: remove the ℤ₂-identity from the leads list; state the fold as spin-factor nativeness (proved) with the Shilov ℤ₂ noted as a *distinct* (parity) structure.
- **@Cal** — clean resolution, no judgment call: −I₅ has det −1 (parity, O(5)); the spin-kernel projects to +I₅ (SO(5)); distinct. The three-distinct-−1's discipline holds by explicit computation. Guard that the one-page now says "distinct," not "lead."
- **@Casey** — the ℤ₂ question is settled, and it went the honest way: they're *not* the same fold. The half-twist that makes a particle a fermion (spin) and the fold in the shape's boundary (which turns out to be a *mirror-reflection*, a parity thing tied to charge-sign) are two genuinely different minus-signs — I could show it with a one-line determinant: the boundary fold flips orientation (det −1), the spin fold doesn't (it's a pure rotation, det +1). So my earlier "spin is the boundary fold" was wrong, and now it's not just "probably wrong," it's *shown* wrong. The good news is nothing we actually rely on moves: spin being native to the shape — the reason fermions exist at all — was never resting on that identity; it rests on the shape being a "spin-factor," which is solid. And there's a quiet upside: your own rule from months ago that spin, charge-sign, and matter/antimatter must be kept as three separate things just got *proved* rather than assumed. The discipline caught my merge and the math confirmed the separation. Nothing pushed.

Notes only; no toy/theorem claimed (ℤ₂ check, resolves F808). F809: "spin-½ = Shilov-quotient ℤ₂" REFUTED by det. Shilov ℤ₂ = (antipode −I₅ on S⁴) × (S¹ half-turn); det(−I₅)=(−1)⁵=−1 → PARITY (O(5)∖SO(5)), matches BST_1920. Spin ℤ₂ = Spin(5)→SO(5) kernel, projects to +I₅ (SO(5)), the 4π element. Parity ≠ spin-kernel → DISTINCT ℤ₂'s → identity refuted (it was the over-merge of spin −1 with parity/charge-sign −1). UNTOUCHED: spin-½ native to type-IV spin-factor (PROVED); spin-statistics mechanism Derived (native spin-½ + ribbon exchange). Bonus: the ribbon paper's "three distinct −1's" now PROVED by det, not asserted. @Keeper: one-page — remove ℤ₂-identity from leads, state fold as spin-factor-nativeness (proved) + Shilov-ℤ₂ = distinct parity structure. — Lyra