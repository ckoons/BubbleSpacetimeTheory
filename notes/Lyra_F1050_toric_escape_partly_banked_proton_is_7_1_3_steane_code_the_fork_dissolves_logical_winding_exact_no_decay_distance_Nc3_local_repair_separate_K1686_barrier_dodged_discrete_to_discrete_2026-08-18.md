# F1050 — The topological-code escape is partly BANKED, and it dissolves the fork. WindingToZeta already carries "the proton as a [[7,1,3]] quantum error code" with a ℤ₃ color-winding syndrome and anyons — the Steane code (distance 3 = N_c, 1 logical = the baryon, 7 physical = g). The fork {N_c=3 forced} XOR {proton stable} assumed correction and stability are the SAME finite-distance protection (→ finite τ_p → decay). In a topological code they SEPARATE: the LOGICAL operator is the topological winding — **exactly** conserved (a ℤ invariant, no local process changes it → EXACT stability, no proton decay); the DISTANCE (=N_c=3) is the LOCAL repair (target-innocent — corrects a single physical error). Both hold at once. And the K1686 type-barrier is DODGED: it failed on winding(discrete)→continuous-generator; but winding(ℤ₃)→code-stabilizer(ℤ₃) is discrete→discrete, type-consistent. Held open, developed; Grace exhibits the map.

**Lyra, Tuesday 2026-08-18, Round 23. Holding the toric-code escape open (the one path to the prize), developed with the corpus find. Reconnected: WindingToZeta (proton=[[7,1,3]], ℤ₃ winding syndrome, anyons, confinement=prime-g), K1686 (the type barrier: winding=discrete homotopy vs continuous generator), F1049 (the fork), T296/T1945 (baryon#=winding). LA on D_IV⁵. Nothing pushed; CP existence-only.**

## The find — the escape is already structured in the corpus
WindingToZeta (Section 9 + Section on winding closure) carries, as a framework claim:
- **"The proton as a [[7,1,3]] quantum error code"** with a fusion ring / **anyons** determined by a 7×7 S-matrix.
- **The ℤ₃ color-winding as the constraint:** "the center ℤ₃ … enforces total winding ≡ 0 mod N_c = 3 — **this IS the color charge constraint**"; a baryon is "total color winding ≡ 0 mod 3 (closed)."
- **Confinement = the primality of g = 7** (no intermediate closure).

So the topological code is **[[7,1,3]] — the Steane code**: **distance 3 (= N_c, corrects one error)**, **1 logical qubit (= the baryon, the committed record)**, **7 physical (= g)**, with the **ℤ₃ winding as the stabilizer/syndrome**. This is exactly the Kitaev-style escape, partly banked (framework-tier, not verified — but the structure is there, not invented).

## Why it DISSOLVES the fork (the key insight)
The fork was: {N_c=3 forced} XOR {proton exactly stable}, because a distance-3 *classical* [3,1,3] code corrects by *active* repair → finite logical lifetime → proton decay. **That assumed the correction and the stability are the same finite-distance protection. In a topological code they are two different things, and both can be exact/target-innocent at once:**

| protection | mechanism | consequence |
|---|---|---|
| **stability of the LOGICAL** (the baryon) | the logical operator IS the **topological winding** — a ℤ invariant no local process can change | **EXACT** — no proton decay (not exponential, *exact*, because a topological charge is exactly conserved) |
| **error-correction, DISTANCE = N_c = 3** | the code corrects a single **local physical** error (restores the configuration) | **target-innocent** — a distance-3 code corrects one error, the coding universal |

The logical (winding) is protected **topologically** (exactly, against *any* weight of local error — a topological charge can't be changed locally), so there is **no finite lifetime** — the finite-distance objection evaporates because the logical protection is not the distance, it is the topology. Meanwhile the distance is N_c = 3 — the local repair — which is the target-innocent coding forcing. **⟹ both {N_c = 3 forced} and {proton exactly stable} hold together.** The fork was an artifact of using a classical repetition code where a topological code belongs.

## The K1686 type-barrier is DODGED (discrete → discrete)
K1686 killed the map because it equated the **discrete** winding (homotopy, T1945) with a **continuous** Lie generator (the color-phase U(1)) — ill-typed. **But the code stabilizer is DISCRETE.** The map here is
$$\underbrace{\text{winding} \equiv 0 \bmod 3}_{\text{discrete } \mathbb{Z}_3} \;\longrightarrow\; \underbrace{\text{the } [[7,1,3]] \text{ stabilizer / syndrome group}}_{\text{discrete } \mathbb{Z}_3},$$
**discrete → discrete, type-consistent.** So the very barrier K1686 hit (continuous ≠ discrete) is *absent* on the code route — the winding was always a discrete object looking for a discrete home, and the stabilizer group is that home. This is why the escape is the *correctly typed* version of the map K1686 couldn't build: it was trying to map the winding to a *generator* (continuous); the code maps it to a *syndrome* (discrete).

## What Grace must exhibit (the decider, now well-posed)
Two things, both discrete-typed and checkable:
1. **The logical operator = the winding.** Show the [[7,1,3]] logical operator (the ℤ̄) IS the topological baryon-number winding (T1945) — so the logical's exact protection IS the winding's exact conservation (this gives exact stability / no decay). Discrete → discrete.
2. **The distance = N_c = 3.** Show the code distance (minimal weight of an undetected error) is exactly 3 = N_c — the target-innocent single-error-correcting length — and that this comes from the ℤ₃/three-fold structure, not from n−2 (else circular). The distance must be a **coding** fact (universal), not the domain descriptor.

If both exhibit, the escape closes: **N_c = 3 forced (= the code distance, target-innocent) + proton exactly stable (= the winding = the logical, topological) + five intrinsic** — Casey's whole claim, non-circular.

## Honest tier / the guard
- **Not banked as a theorem:** the [[7,1,3]] structure is framework-tier in WindingToZeta; the two exhibitions above are owed. **Held open — the frontier.**
- **But structurally more sound than the fork suggested:** the fork assumed classical active correction (→ decay); the topological code separates exact-logical-protection (winding) from local-repair-distance (N_c=3), so the decay objection and the P2b conflict both dissolve, and the type-barrier is dodged (discrete→discrete). So this is a *real* path, not a hope — the one path, and it now has a named structure and a well-posed decider.
- **Guard held:** the escape is exhibited **only** when (1) logical = winding and (2) distance = 3 = a coding universal (not n−2) both close. Until then, N_c=3 is an input. **Never "commitment forces D_IV⁵" until the topological code is exhibited.** I am not claiming it closed — I am claiming the path is real, typed, and one two-part exhibition from the prize.

## Handoffs
- **@Grace** — the decider, now discrete-typed (dodges your K1686 barrier): (1) is the [[7,1,3]] logical operator the baryon-number winding (T1945)? (2) is the code distance 3 = N_c a *coding universal* (single-error-correcting length) rather than n−2 (circular)? Both are discrete → discrete maps; check the types first, then exhibit the parity-check ↔ winding-syndrome map. WindingToZeta Section 9 is your starting structure.
- **@Elie** — your recover-vs-reject check now has the resolution's shape: in a topological code the *logical* is never repaired (it's exactly protected), but a *local physical* error IS repaired (distance 3). So "does a damaged baryon get repaired?" splits: the baryon *number* (logical) can't be damaged at all (topological), while a single-quark *configuration* error is repaired (distance 3). If that split holds on the physics, the escape is right.
- **@Keeper** — the escape is partly structured (WindingToZeta [[7,1,3]]), the fork is dissolved *in principle* (topological logical vs local distance), and the K1686 barrier is dodged (discrete→discrete). Tier: frontier, two exhibitions owed, held open. Do NOT let "the escape resolves the fork in principle" read as "N_c=3 forced" — the map must be exhibited.
- **@Casey** — your instinct has a home, and it's already half-written in our own corpus. The trap we were in was thinking of the proton like three paper copies of a note (fix errors by majority — but then a rare double-error still loses it, so the note doesn't last forever). The escape is that the proton is a *quantum* error-correcting code — the [[7,1,3]] Steane code, which our winding paper already names — and there the thing being protected isn't a copy, it's a *knot*: the baryon number is a winding you cannot undo by any local fumbling, so it lasts *exactly* forever (no decay), while the three-ness is the code's error-correcting reach (fix any single slip). Two different jobs, both satisfied, no contradiction. And the reason our earlier map failed — trying to equate a knot with a continuous dial — doesn't apply, because a code's checks are discrete, same kind of thing as a knot. So the five really may be intrinsic. I won't say it's closed until Grace shows the code's protected quantity *is* the winding and its reach *is* the three — but this is the first time the whole thing hangs together without a contradiction, and it's your reading that got us here.

Notes only; no theorem/toy claimed (holding + developing the escape). F1050: topological-code escape PARTLY BANKED — WindingToZeta: proton = [[7,1,3]] Steane code (dist 3=N_c, 1 logical=baryon, 7 phys=g), ℤ₃ color-winding = syndrome, anyons. DISSOLVES the fork: classical [3,1,3] active-correction → finite τ_p → decay was the artifact; topological code SEPARATES logical-protection (winding = ℤ invariant, EXACT, no decay) from distance (=N_c=3, local repair, target-innocent) → both hold. K1686 barrier DODGED: winding(ℤ₃)→stabilizer(ℤ₃) = discrete→discrete (K1686 failed on winding→continuous-generator). Grace exhibits: (1) logical=winding (T1945) → exact stability; (2) distance=3=N_c coding-universal (not n−2, circular). Both close → N_c=3 forced + proton stable + five intrinsic, non-circular. Tier: frontier, 2 exhibitions owed, held open. Guard: never "commitment forces D_IV⁵" until exhibited. — Lyra
