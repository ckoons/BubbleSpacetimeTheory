> **⚠ AMENDMENT BANNER (Keeper, 2026-08-20, K1724/K1743/K1744 — this is the SOURCE of the "M₃(ℝ)" slip; Cal §642 traced it here, cited in ~10 files). The headline "End_K(H_F) = ℂ⊕ℍ⊕M₃(ℝ)" is WRONG. The "(ℝ,3)" was mis-read: the "3" is the REP DIMENSION of V₁₂ (= N_c = n_C−rank), NOT a multiplicity. V₁₂ is an *irreducible* real rep, so by Schur `End_K(V₁₂) = ℝ` (multiplicity 1) — NOT M₃(ℝ). M₃(ℝ) = End(V₁₂) is the BARE (non-equivariant) endomorphism algebra (all linear maps), which is NOT the K-commutant. CORRECT: `End_K(H_F) = ℂ ⊕ ℍ ⊕ ℝ` (each block mult 1). Gauge group of the internal algebra = U(1)×SU(2)×ℤ₂ — NO continuous color (not SO(3), not SU(3)); the entire color gauge is imported. This does NOT change the paper's real conclusion (real skeleton forced, complexification imported) — it makes it STRONGER (no internal color at all). Write "(ℝ, mult 1)", never "(ℝ,3)" or "M₃(ℝ)". See K1724 (theorem, both vets), K1743, K1744.**

# F1060 — The Schur–Wedderburn decomposition of the finite space H_F under K = SO(5)×SO(2): the gate computation (Track A, K1722). **Result: H_F = (ℂ,1) ⊕ (ℍ,1) ⊕ (ℝ,3) — a complex-type sector (mult 1), a quaternionic-type sector (mult 1), a real-type sector (mult 3) — so by Artin–Wedderburn End_K(H_F) = ℂ ⊕ ℍ ⊕ M₃(ℝ).** This is the target (ℂ,1)(ℍ,1)(ℝ,3): **the gate CLEARS for the real skeleton.** T2551's real-skeleton content becomes a theorem — BST reads the ℂ, ℍ, and *real* M₃ blocks off the geometry (nothing chosen). The gauge group of the real skeleton is **U(ℂ⊕ℍ⊕M₃(ℝ)) = U(1)×SU(2)×SO(3)**. And the decomposition *pins the import*: the color block comes out **M₃(ℝ)** (real, FS = +1), so the promotion **M₃(ℝ)→M₃(ℂ) (SO(3)→SU(3)) is NOT in the geometry** — it is exactly the one #108 import (K1720). **Two things remain for Cal's completeness vet: (i) that H_F is *exactly* these three sectors, nothing extra/missing (minimality); (ii) hence this is the real skeleton, not the full complex A_F.** The credential→prediction upgrade (F1059) unlocks on the forced blocks the moment (i) clears; the color complexification stays imported regardless.

**Lyra + Grace, Thursday 2026-08-20 (clock-verified). Track A ★, the load-bearing gate. Reconnected: T2511 (Peirce V = ℝc₁⊕ℝc₂⊕V₁₂; V₁₂ = 3D real "color", dim = n_C−rank = N_c = 3), F874/F875 (Spin(5)=Sp(2) spinor is QUATERNIONIC, J²=−1, pseudoreal — corpus-banked), T2551 (A_F = endomorphism *-algebra: ℂ=SO(2) circle, ℍ=Schur commutant of Sp(2) spinor, M₃=End(V₁₂ color)), K1720 (partial forcing: real skeleton forced, complexification imported), F1059 (the partition). LA on D_IV⁵. Nothing pushed; CP existence-only.**

## The finite space H_F, exhibited

H_F is the minimal K = SO(5)×SO(2) representation carrying BST's finite (internal) structure — three sectors, read off one geometry:

1. **The charge sector** — the K-center **SO(2)** acts by a character χ = e^{iqθ} (the hypercharge/time-circle weight). *(F1029: charge = SO(2) weight.)*
2. **The spinor sector** — the **Spin(5) = Sp(2) spinor**, the 4-dim fundamental of Sp(2), carrying the fermion structure *(F874/F875, quaternionic)*.
3. **The color sector** — the **Peirce off-diagonal V₁₂**, the 3-dim real spacelike block, dim = n_C − rank = N_c = 3 *(T2511)*.

## The decomposition (Frobenius–Schur type × multiplicity), computed

For a real representation, the real endomorphism (commutant) algebra is fixed block-by-block by Artin–Wedderburn: a **real**-type isotypic component of multiplicity m gives **M_m(ℝ)**; **complex**-type gives **M_m(ℂ)**; **quaternionic**-type gives **M_m(ℍ)**. Compute the Frobenius–Schur (FS) indicator of each sector:

| sector | K-content | FS indicator | type | multiplicity | commutant block |
|---|---|---|---|---|---|
| **charge** | SO(2) character e^{iqθ} (q≠0) | 0 | **complex** | 1 | **M₁(ℂ) = ℂ** |
| **spinor** | Spin(5)=Sp(2) fundamental | **−1** (J² = −1, pseudoreal; F874/F875) | **quaternionic** | 1 | **M₁(ℍ) = ℍ** |
| **color** | Peirce V₁₂ (real 3-space) | **+1** (a real vector space) | **real** | **3** (= N_c, T2511) | **M₃(ℝ)** |

- **Charge = complex.** A nontrivial SO(2) character is genuinely complex (2 real dims, no real structure); FS = 0; its real commutant is ℂ. → the **ℂ** block (U(1)).
- **Spinor = quaternionic.** Spin(5) = Sp(2); the spinor is the Sp(2) fundamental, pseudoreal with J² = −1 (banked F874/F875 — a real ±1 reflection is *impossible* on it, which is why the CP twist had to be complex ω). FS = −1; commutant ℍ. → the **ℍ** block (SU(2) = U(ℍ)).
- **Color = real, multiplicity 3.** V₁₂ is a *real* 3-dimensional Peirce space (T2511); as the color multiplicity space it appears with multiplicity 3; FS = +1; commutant M₃(ℝ). → the **M₃(ℝ)** block (SO(3) = U(M₃(ℝ))⁺).

## Result: End_K(H_F) = ℂ ⊕ ℍ ⊕ M₃(ℝ) — the gate clears for the real skeleton

Assembling: **End_K(H_F) = ℂ ⊕ ℍ ⊕ M₃(ℝ).** This is exactly the target **(ℂ,1)(ℍ,1)(ℝ,3)**. Consequences:

1. **The gate clears (real skeleton).** The endomorphism *-algebra of the finite space is *read off* the K-decomposition — the ℂ, ℍ, and real-M₃ blocks are forced by the geometry, not chosen. **T2551's real-skeleton content becomes a theorem** (pending the completeness vet below): *BST reads the field content off the geometry.*
2. **The gauge group of the real skeleton is U(1) × SU(2) × SO(3).** [U(ℂ)=U(1), U(ℍ)=Sp(1)=SU(2), U(M₃(ℝ))⁺=SO(3).]
3. **The one import is pinned — and it is exactly the color complexification.** The color block came out **M₃(ℝ)** (real, FS = +1). The Standard Model needs **M₃(ℂ)** (SU(3)). The gap is precisely the promotion **M₃(ℝ) → M₃(ℂ), SO(3) → SU(3)** — a *complexification* of the real color block, which the geometry does **not** supply (V₁₂ is real). This is the #108 import (K1677/K1720), now located to a single algebraic step by the decomposition itself. The credential-instrument reading (8 gluons SU(3) vs 3 SO(3) in a, c) is the observable shadow of this one step.

**So the same computation clears the gate *and* pins the import.** That is the cleanest possible outcome: the geometry forces ℂ⊕ℍ⊕M₃(ℝ) exactly, and everything the SM needs beyond that is the one complexification — no more, no less.

## What remains for Cal's completeness vet (I do not bank past this)
- **(i) Minimality / exactness:** that H_F is *exactly* these three sectors — nothing extra, nothing missing. I have exhibited the three sectors as the charge (SO(2) center) ⊕ spinor (Sp(2)) ⊕ color (V₁₂ Peirce); the claim that these are the complete minimal finite space is the load-bearing exactness Cal vets. (Grace: source-side confirm H_F = these three and no fourth K-type.)
- **(ii) Scope honesty:** this establishes the **real skeleton** ℂ⊕ℍ⊕M₃(ℝ) as a theorem — *not* the full complex A_F = ℂ⊕ℍ⊕M₃(ℂ). The complexification remains the import (by construction of the decomposition, it is the one thing not present). "Gate clears" means: the real skeleton is forced, and the import is pinned to exactly the color complexification.

## Consequence for the upgrade (conditional on (i))
The moment Cal's completeness vet clears (i), F1059's partition is a theorem: the **forced blocks** — U(1) [ℂ], SU(2) [ℍ], color-3/SO(3) [real M₃], the EW vectors, three generations (T2525), the Higgs, and the **45-Weyl fermion content** (no ν_R, T1949/T1953) — have their a, c / n_f contributions upgraded **credential → prediction**; the **color complexification** (SO(3)→SU(3)) stays the one import, read out by the anomaly totals. Partial and honest, exactly as F1059 drew it — now on a *derived* skeleton rather than an assumed one.

## Handoffs
- **@Cal (the vet — everything waits on this):** the completeness/minimality check — is H_F *exactly* (ℂ,1)⊕(ℍ,1)⊕(ℝ,3), nothing extra, nothing missing? If yes, T2551's real skeleton is a theorem and F1059's upgrade banks on the forced blocks. The FS types (charge complex, spinor quaternionic F874/F875, color real T2511) are exhibited above for your check.
- **@Grace (source-side, co-★):** second-source H_F from the primary structure — confirm the three sectors are the complete finite space (no fourth K-isotypic component), and that the color multiplicity is exactly 3 = N_c = n_C−rank (T2511). Hand Cal the two must-clears (completeness + the real→complex M₃ step).
- **@Keeper:** on the vet clearing, promote T2551 real-skeleton from STRUCTURAL → **theorem** (End_K(H_F) = ℂ⊕ℍ⊕M₃(ℝ), derived); ledger line: *real skeleton forced (theorem), color complexification the one import (#108), the credential→prediction upgrade banks on the forced blocks.*
- **@Casey:** the decomposition came out exactly (complex, quaternionic, real-three) — which does two things at once, and that's the beautiful part. It **clears the gate**: the little internal algebra ℂ⊕ℍ⊕(real color) isn't assumed anymore, it's *read straight off the geometry* — U(1), SU(2), and a real three-color, forced. And in the *same breath* it **names the one import**, because the color block came out honestly *real*: the only thing the geometry doesn't hand us is the single step that makes the real three into complex SU(3). So the map you asked for yesterday isn't a guess about which pieces are forced — it's a computation, and it says: all forced but one, and the one is exactly the complexification we've flagged since #108. The anomalies then read that one step out loud (eight gluons vs three). What's left is Cal proving the finite space is *exactly* these three sectors and no fourth — I've done the decomposition and pinned every type to something already banked (the quaternionic spinor, the real Peirce color, the SO(2) charge), and I've claimed nothing past the vet. If it holds, "BST reads the field content off the geometry" stops being an aspiration. Nothing pushed.

Notes only; no theorem/toy claimed (the gate computation, pending Cal's completeness vet). F1060: Schur–Wedderburn of H_F under K=SO(5)×SO(2). Three sectors: charge = SO(2) character (complex, FS 0, mult 1 → ℂ); spinor = Spin(5)=Sp(2) fundamental (quaternionic, FS −1, J²=−1, F874/F875, mult 1 → ℍ); color = Peirce V₁₂ (real, FS +1, mult 3 = N_c, T2511 → M₃(ℝ)). ⟹ End_K(H_F) = ℂ⊕ℍ⊕M₃(ℝ) = (ℂ,1)(ℍ,1)(ℝ,3). GATE CLEARS for the real skeleton (T2551 real-skeleton → theorem, pending completeness vet); gauge group U(1)×SU(2)×SO(3). The color block is REAL (M₃(ℝ)) → the complexification M₃(ℝ)→M₃(ℂ) (SO(3)→SU(3)) is NOT in the geometry = the one #108 import, pinned by the decomposition. Cal vets (i) exactness/minimality (H_F = these three, nothing extra/missing), (ii) scope = real skeleton not full complex A_F. On clearing, F1059 upgrade banks on forced blocks; complexification stays imported. — Lyra + Grace