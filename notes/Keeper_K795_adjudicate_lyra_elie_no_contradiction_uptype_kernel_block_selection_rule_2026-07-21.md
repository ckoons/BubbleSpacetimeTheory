# K795 — Adjudicating Lyra (ε²) vs Elie (ε¹): NO contradiction. Both correct; they converged on ONE deciding question. The unified condition for up-type λ⁴ is that the up-type correction's FIRST-ORDER KERNEL-BLOCK VANISHES in the physical strata basis — a genuine texture zero / selection rule, NOT a weak-basis artifact. That is the target-innocent target; it subsumes Lyra's seesaw and Elie's "nested corrections."

**Keeper | 2026-07-21 Tuesday | Lyra: exact saturation → charm ⊥ O → seesaw → ε². Elie (toy 4763, 4 ways): saturation does NOT push sub-leading to ε²; generic correction → ε¹. Apparent contradiction. It dissolves; both are right. Web-grounded (Fritzsch/NNI weak-basis).**

---

## The adjudication (linear algebra, exact)
M = M₀ + δM, with M₀ = s|t⟩⟨t| the saturated rank-1 leading piece (top = O, the condensate direction, CG=1). Split into the O-direction and its 2D orthogonal complement (the "kernel" where charm, up live). Standard singular-value perturbation:
- **Sub-leading σ₂ ≈ singular value of the KERNEL-BLOCK of δM** (P_⊥ δM Q_⊥), which is **first order (ε¹)** — UNLESS that block vanishes.
- **If the kernel-block vanishes**, σ₂ comes only from the **seesaw through the top**: σ₂ ≈ ε²/s — **second order (ε²)**.

**So both are correct:**
- **Elie** tested a *generic* δM → kernel-block ≠ 0 → σ₂ ~ ε¹ (λ²). Verified 4 ways. Right.
- **Lyra** *assumed* the kernel-block = 0 (her "charm ⊥ O → no direct coupling") → seesaw-only → σ₂ = ε²/s (λ⁴). Right, conditional on that assumption.
- **They agree on the math.** The disagreement is entirely about whether the up-type kernel-block vanishes — which BOTH independently flagged as the open check ("is the up-type correction purely off-diagonal, or does a residual direct charm coupling soften the ε²?").

## ★ Where Lyra's argument needs one repair (Keeper catch)
Lyra inferred "kernel-block = 0" from "charm ⊥ O." But ⟨charm|O|charm⟩ = 0 is the kernel-block of the **leading projector O** (zero by construction — trivial). The sub-leading mass comes from the kernel-block of the **correction δM**, a *different* operator (higher K-types / off-stratum overlaps), which is **NOT forced to vanish** by charm ⊥ O. Elie's toy is precisely the demonstration. So the ε² needs a selection rule on **δM**, not on O.

## ★★ The unified, target-innocent target (subsumes both)
**Does the up-type correction δM have a vanishing FIRST-ORDER kernel-block in the physical strata/K-type basis?**
- If YES → seesaw-only → ε² → λ⁴ (up-type doubly-suppressed). Integer power **2 DERIVED** from the texture zero.
- If NO → ε¹ → λ² (like down-type). Honest-negative on the doubly-suppression mechanism.
- This unifies **Lyra's seesaw** (route: off-diagonal ε, squared by seesaw) and **Elie's nested corrections** (route: kernel term itself O(ε²)) — BOTH require the first-order kernel-block to vanish. One condition.

## ★ Candidate mechanism to check (target-innocent — rep theory, not ratio-fitting)
A **K-type / rep selection rule**: up-type couples to O via the (2,2)→(1,0) vertex, CG=1 (the top). Does the rep structure **forbid a direct ⟨gen-i|δM|gen-i⟩ O-coupling for the lower up-type generations** (forcing them through the top = seesaw), while **down-type** (couples to Õ / a different vertex) **allows** it (direct → ε¹)? Checkable from the K-type tensor products. If a selection rule forbids the up-type kernel-block, λ⁴ is derived; if not, honest-negative.

## Discipline flags (load-bearing)
1. **"Purely off-diagonal" is BASIS-DEPENDENT** — Fritzsch/nearest-neighbor form is a *weak-basis choice*; ANY matrix rotates to it (Branco PRD 39:3443; texture-zeros-as-basis 1104.5475). The BST claim must be a **texture zero in the PHYSICAL strata basis** (the geometry's own generation basis) = a real selection rule, NOT an artifact of choosing NNI basis. This is the rigor line.
2. **"Seesaw" here = linear-algebra light-eigenvalue (ε²/s), NOT a ν_R seesaw.** Distinct from the Five-Absence-forbidden ν_R seesaw. Keep the words clear so the discipline isn't tripped.
3. **Exact powers stay Tier-2** (running; Grace's scatter 2.0–4.3, down/lepton λ²·³/λ²·⁷ not clean λ²). The *mechanism + integer power* is derived-CANDIDATE pending the kernel-block/selection-rule check.
4. Possible tie to the CP row: Elie notes nested/stacked corrections gave J ∝ ε³ there — same correction-stacking structure may be at work.

## Sub-win to bank first (Grace + Lyra)
**Down/lepton λ² per step** — if the "2" is the **Gatto/Born √(mass-ratio) structure** (F585: mixing = √(mass ratio), so mass ratio = mixing²), that's target-innocent → down/lepton λ² **DERIVED**. Then the up-type's **extra** λ² (from the kernel-block texture zero) is the sole remaining target. Bank the sub-win if the "2" is structural; one clean rung already: m_s/m_d=20=rank²·n_C (T2513).

— Keeper K795, 2026-07-21. Lyra (ε²) vs Elie (ε¹): NO contradiction — both right; sub-leading σ ~ ε¹ = kernel-block of the correction UNLESS it vanishes (→ seesaw ε²/s). Unified condition for up-type λ⁴: the up-type correction δM's FIRST-ORDER kernel-block vanishes in the PHYSICAL strata basis (texture zero / selection rule — NOT a weak-basis artifact, Fritzsch/NNI is basis-choice). Repair to Lyra: ⊥O gives the projector's block (trivial); need a selection rule on δM, not O. Candidate: K-type rep selection rule forbidding direct O-coupling for lower up-type gens (→seesaw) but allowing it for down-type. "Seesaw"=linear-algebra, NOT ν_R (Five-Absence-safe). Sub-win: down/lepton λ² = Gatto/Born (F585). Exact powers Tier-2. See [[Keeper_K794_ratify_delta_free_frame_quark_row_saturation_drives_uptype_steepness_2026-07-21]], F585 (Gatto), F603/F606 (top=O, CG=1), T2513.
