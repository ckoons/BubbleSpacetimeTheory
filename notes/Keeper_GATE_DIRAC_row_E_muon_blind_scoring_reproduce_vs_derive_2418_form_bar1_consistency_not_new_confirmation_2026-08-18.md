---
node_type: keeper_gate
id: GATE-DIRAC-E
title: "Row E (the muon via K∘D) is PROGRAM-DIRAC's first REAL prediction — a measured number that can fail — so I pin its scoring + tier logic BLIND, before Elie's number lands, so the tier can't be inflated after the fact. THE TARGET (banked, for reference only): m_μ/m_e = (24/π²)⁶ = 206.761 (T190, Identified, 0.0034% vs observed 206.7683); the genus-5 weight already passed one EXACT signature — the electron anchor Γ(5/2)²/Γ(5) = 3π/128 to machine precision (Grace). PRE-REGISTERED SCORING of Elie's K∘D run on the genus-5 Bergman weight, Kostant-D image (3/2,½), partition coords: (1) PASS-VALUE — the run must return m_μ/m_e within ~0.01% of 206.768; a return that does NOT match (and does not reproduce (24/π²)⁶) FALSIFIES the genus-5 lepton weight fork (Grace's pin has teeth here — the muon is a measured number). (2) ★ TIER LOGIC (Bar-1 + recast-is-derivation, decided BEFORE the number): if K∘D REPRODUCES (24/π²)⁶ = the SAME number as the banked T190 form, it is a RE-DERIVATION / CONSISTENCY (two routes to one number), NOT a new prediction and NOT a second confirmation — the muon was already Identified via (24/π²)⁶; per Bar-1 the Dirac-muon and the banked-muon are ONE observable reached two ways, counted ONCE. It PROMOTES the muon Identified→Derived ONLY IF K∘D FORCES the form from the operator — i.e. the 24 comes out as Γ(genus)=Γ(5) AND the exponent 6 (=C₂) and the π² structure are FORCED by the K∘D mechanism (the genus-5 scalar-kernel norm through the Dirac spinor shift), not pattern-matched to the known value. A forced form = a genuine promotion (recast-is-derivation); a value-match with a hand-identified form = consistency only. (3) THE HONEST WIN either way: even a re-derivation is worth banking as a CONSISTENCY (the K∘D translator reproduces a banked mass ratio by an independent route — evidence the translator is right), provided it is labeled consistency, not counted as a fresh prediction. (4) GUARD: do NOT let 'the muon came out' become a PROGRAM-DIRAC confirmation-count entry separate from the geometry-line (24/π²)⁶; it is row E's ONE reading, and it shares the observable with T190. Falsifier and tier both fixed here, blind."
date: 2026-08-18
author: Keeper
status: "OPEN — row-E scoring pinned BLIND before Elie's number. PASS = m_μ/m_e within ~0.01% of 206.768 (miss → genus-5 weight fork falsified). Tier: reproduces (24/π²)⁶ → CONSISTENCY/re-derivation (one observable, counted once, not a new confirmation); FORCES the form (24=Γ(5), 6=C₂, π² from the mechanism) → PROMOTE muon Identified→Derived. Either way label it; never a separate confirmation-count entry."
related: [PROGRAM-DIRAC, "GATE-DIRAC-H", K1680, K1681, T190, "F1039"]
---

# GATE-DIRAC-E — the muon, scored blind

Row E is the Dirac program's **first real prediction** — a measured number (`m_μ/m_e = 206.7683`) that the `K∘D` run can miss. I pin the scoring and the tier *before* Elie's number exists.

## The target (reference)
Banked form: `m_μ/m_e = (24/π²)⁶ = 206.761` (T190, Identified, 0.0034%). The genus-5 weight has already passed one **exact** signature — the electron anchor `Γ(5/2)²/Γ(5) = 3π/128` to machine precision (Grace). So the weight fork has one exact check under it; the muon is the second, teeth-bearing one.

## Pre-registered scoring (Elie's K∘D run: genus-5 weight, Kostant-D image `(3/2,½)`, partition coords)
1. **Pass-value:** the run must return `m_μ/m_e` within ~0.01% of `206.768`. **A miss falsifies the genus-5 lepton weight fork** — the muon is measured, so Grace's pin has teeth here.
2. **Tier logic (fixed now, blind):**
   - If `K∘D` **reproduces** `(24/π²)⁶` — the *same number* as the banked T190 form — it is a **re-derivation / consistency** (two routes to one number), **not** a new prediction. Per Bar 1, the Dirac-muon and the banked-muon are **one observable, counted once.**
   - It **promotes** the muon Identified→Derived **only if** `K∘D` *forces the form* from the operator: the `24 = Γ(genus) = Γ(5)`, the exponent `6 = C₂`, and the `π²` structure all falling out of the mechanism (the genus-5 scalar-kernel norm through the Dirac spinor shift) — **not** pattern-matched to the known value. (Recast-is-derivation: a forced form is a genuine promotion; a value-match with a hand-identified form is consistency.)
3. **Honest win either way:** even a re-derivation banks as a **consistency** (the `K∘D` translator reproduces a banked mass ratio by an independent route — real evidence the translator is right), *if labeled consistency, not a fresh prediction.*

## Guard
"The muon came out" must not become a PROGRAM-DIRAC confirmation-count entry separate from the geometry-line `(24/π²)⁶`. It is row E's **one** reading, and it shares the observable with T190. Falsifier and tier are both fixed here, blind.

— Keeper, GATE-DIRAC-E, 2026-08-18. Row E scored blind: PASS = within ~0.01% of 206.768 (miss → genus-5 fork falsified); reproduces (24/π²)⁶ → consistency (one observable, counted once); forces the form (24=Γ(5), 6=C₂, π² from the mechanism) → promote Identified→Derived. Never a separate confirmation. Nothing pushed.
