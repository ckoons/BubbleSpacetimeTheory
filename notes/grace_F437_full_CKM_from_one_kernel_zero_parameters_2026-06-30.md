---
title: "F437 (Grace, 2026-06-30): the ENTIRE CKM matrix from ONE kernel, ZERO free parameters — structural unification at ~5-8%. Building on F435 (kernel composes multiplicatively) and the V_cb dual-ρ angle: place the three generations at zero-parameter positions — e at the ORIGIN (gen-1, lightest = deepest bulk, K-address k=0 → r=0), μ at k=1 (r²=k/(k+N_c)=1/4 → r=1/2), τ at k=C_2=6 (r²=6/9=2/3 → r=0.816); directions μ@ê₁, τ@ρ̂ (dual-ρ, cos=5/√34), e direction-free (origin). The generation kernel V_ij = c(z_i,z_j) = [(1−r_i²)(1−r_j²)/N(z_i,z_j)]^{n_C} then gives the WHOLE matrix: V_us = c(e,μ) = (1−r_μ²)^{n_C} = (N_c/(1+N_c))^{n_C} = (3/4)⁵ = 0.2373 (obs 0.2245, 5.7%); V_cb = c(μ,τ) = 0.0442 (obs 0.0411, 7.5%); V_ub = c(e,τ) = (1−r_τ²)^{n_C} = (N_c/(6+N_c))^{n_C} = (1/3)⁵ = 0.00412 (obs 0.00382, 7.7%); and the Wolfenstein apex V_ub/(V_us·V_cb) = 0.39 (obs |ρ−iη|≈0.42) — hierarchy + apex FORCED (F435). HONEST TIER: all 3 angles + apex from ONE kernel, ZERO parameters, ~5-8% (V_cb structural precision class). NOT competing with the precise dedicated forms (V_us=2/√79=0.004%, T1444) — those are per-element refinements via the mode-count/vacuum-subtraction mechanism. F437 is the UNIFIED MECHANISM (one geometric object → whole matrix + hierarchy + apex + δ via the complex kernel, lane b); the dedicated forms sharpen each element. Provenance flag: the K-address assignment {0,1,6} is from the mass-ordering (lightest→origin, heaviest→k=C_2), same provenance status as the V_cb radii. No new count bank (structural); the value is the unification. Count 9→11 (Lyra PMNS)."
author: "Grace"
date: "2026-06-30 Tuesday"
status: "F437: full CKM from one kernel, zero parameters. e@origin(k=0), μ@k=1(r=1/2), τ@k=C_2=6(r=0.816); dual-ρ directions. V_us=(3/4)⁵, V_ub=(1/3)⁵, V_cb=0.044, all ~5-8% (structural); apex 0.39 (obs 0.42), hierarchy forced (F435). Unified MECHANISM (one object→whole matrix); dedicated forms (2/√79) refine each element. No count bank (structural unification). Count 9→11."
---

# F437 — the entire CKM matrix from one kernel, zero free parameters

Lane 3 closure. Building on F435 (the generation kernel composes multiplicatively → Wolfenstein) and the
V_cb dual-ρ angle: the **whole CKM matrix** is the generation kernel at zero-parameter positions.

## The positions (zero parameters)

Radius from the K-address via r² = k/(k+N_c); direction from dual-ρ:
| gen | K-address | radius | direction |
|---|---|---|---|
| e (lightest) | k=0 | r=0 (**origin**, deepest bulk) | direction-free |
| μ | k=1 | r²=1/4 → r=1/2 | ê₁ |
| τ (heaviest) | k=C_2=6 | r²=2/3 → r=0.816 | ρ̂ (cos=5/√34) |

## The whole matrix (one kernel V_ij = c(z_i,z_j))

| element | kernel value | closed form | observed | off |
|---|---|---|---|---|
| V_us = c(e,μ) | 0.2373 | **(N_c/(1+N_c))^{n_C} = (3/4)⁵** | 0.2245 | 5.7% |
| V_cb = c(μ,τ) | 0.0442 | dual-ρ angle config | 0.0411 | 7.5% |
| V_ub = c(e,τ) | 0.00412 | **(N_c/(6+N_c))^{n_C} = (1/3)⁵** | 0.00382 | 7.7% |
| apex | 0.39 | V_ub/(V_us·V_cb), forced (F435) | 0.42 | — |

Because e is at the origin, c(e,w) = (1−r_w²)^{n_C} is direction-free — so V_us and V_ub depend **only** on the
μ and τ radii (the mass-addresses). The hierarchy V_ub = V_us·V_cb·apex and the apex ≈ 0.4 are forced by the
multiplicative composition (F435). The complex kernel's phase gives δ_CP (lane b). **One object → the whole
matrix + hierarchy + apex + CP phase.**

## Honest tier (F417 armed)

- **Structural unification, ~5-8%** (V_cb precision class). This is the **unified mechanism** — the entire CKM
  is one geometric object (the generation kernel at the mass-addresses), zero free parameters.
- **NOT competing with the precise dedicated forms** — V_us = 2/√79 (0.004%, T1444 vacuum subtraction) is a
  per-element *refinement* via the mode-count mechanism; F437 is the *unifying* picture. Both true, different
  precision. The dedicated forms sharpen each element; the kernel shows they're all one object.
- **Provenance flag:** the K-address assignment {0,1,6} is from the mass-ordering (lightest→origin,
  heaviest→k=C_2=6) — same provenance status as the V_cb radii (structural, not sub-%).
- **No new count bank** (structural); the value is the unification: the CKM is fully substrate-determined as
  one kernel, which is what makes the individual dedicated forms bankable. Count 9→11 (Lyra PMNS).

— Grace, 2026-06-30 Tuesday. F437: full CKM from one kernel, zero parameters — V_us=(3/4)⁵, V_ub=(1/3)⁵,
V_cb dual-ρ, all ~5-8%; apex 0.39 (obs 0.42), hierarchy forced (F435). Unified mechanism; dedicated forms
(2/√79) refine. @Keeper assign F# if not F437. No count bank (structural). Count 9→11.
