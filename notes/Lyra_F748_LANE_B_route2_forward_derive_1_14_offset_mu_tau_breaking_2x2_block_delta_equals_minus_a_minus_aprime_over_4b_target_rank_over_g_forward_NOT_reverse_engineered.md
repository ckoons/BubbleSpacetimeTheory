# F748 — Lane (b), route 2 to θ₂₃-Derived: the 1/14 offset as a FORWARD μ-τ-breaking computation, set up cleanly and — this is the load-bearing discipline after this morning — **NOT reverse-engineered to 1/14.** The atmospheric angle is sin²θ₂₃ = 1/2 + δ, with the maximal 1/2 DERIVED (Shilov ℤ₂ μ-τ symmetry, F558) and δ = 1/14 = 1/(rank·g) the observed offset. Route 2 (independent of Elie's Clebsch diagonalization, route 1) is to compute δ **forward** from the μ-τ-breaking 2×2 block and see what it gives — the forward number decides the tier, not a target. **Framing: δ = −(a−a')/(4b) for the μ-τ block [[a,b],[b,a']] (small-breaking limit), so δ = 1/14 ⟺ (a−a')/b = rank/g = 2/7 — a clean target-form, but I state it as the CHECK, not the input: the μ and τ diagonal couplings a, a' and the off-diagonal b are computed from the actual mode addresses (μ at ν=3/2, τ at ν=0), blind, and (a−a')/b either comes out rank/g or it doesn't.** If it does → θ₂₃ fully Derived (maximal + forward-derived breaking, two independent routes agreeing). If it doesn't → the offset stays Identified and we say so. **Tier: route-2 SETUP only — a forward computation, no number claimed; the discipline is that I do NOT aim at 1/14 (that is exactly the moment-ratio coincidence, one level up).**

**Lyra, Thu 2026-07-30. Route 2, forward, no reverse-engineering. After the 4/7-moment-ratio coincidence this morning, the one thing I will not do is manufacture 1/14 from a pretty ratio. I set up the forward computation and let it decide.**

## The structure (μ-τ 2×2 block)
The atmospheric mixing lives in the μ-τ 2×2 block of the charged-lepton (or effective) mass-squared matrix:
$$ M_{23} = \begin{pmatrix} a & b \\ b & a' \end{pmatrix}, \quad a = \langle \mu | \mathcal{O} | \mu\rangle,\ a' = \langle \tau | \mathcal{O} | \tau\rangle,\ b = \langle \mu | \mathcal{O} | \tau\rangle. $$
- **Exact μ-τ symmetry (Shilov ℤ₂, F558):** a = a' ⟹ eigenvectors (μ±τ)/√2 ⟹ **sin²θ₂₃ = 1/2 (maximal), DERIVED.**
- **Breaking (μ, τ at different addresses):** a ≠ a'. The mixing:
$$ \sin^2\theta_{23} = \tfrac12 - \frac{a - a'}{2\sqrt{(a-a')^2 + 4b^2}} \;\approx\; \tfrac12 - \frac{a-a'}{4b} \quad (\text{small breaking}). $$
So **δ = −(a−a')/(4b)**.

## The forward computation (the CHECK, not the input)
Observed δ = 1/14 ⟺ **(a−a')/b = rank/g = 2/7**. I write this as the *target-form to test*, NOT as an assumption:
- **Compute a, a', b forward** from the actual mode couplings: μ at address ν = 3/2 (k=1, the rank-1 stratum), τ at ν = 0 (k=2, the Shilov vertex), against the (2,2) condensate — the SAME μ-τ-breaking Clebsch as Elie's route 1, but read as the diagonal asymmetry (a−a') over the off-diagonal (b) rather than diagonalized numerically.
- **The μ-τ-breaking is the address difference** ν = 3/2 vs ν = 0 — a *structural* source (the two modes sit at different strata), target-innocent.
- **Then (a−a')/b either equals rank/g = 2/7 or it does not.** If it does, δ = 1/14 is forward-derived and θ₂₃ is Derived by two independent routes (this + Elie's diagonalization). If it does not, the 4/7 offset is a separate (θ₁₃-tied) effect and stays Identified.

## Why this is route 2 (independent of Elie)
- **Elie (route 1):** the full numeric O_(2,2) Clebsch diagonalization → the eigenvalues/eigenvectors → θ₂₃ directly.
- **Lyra (route 2, this):** the analytic small-breaking expansion δ = −(a−a')/(4b), with (a−a')/b the μ-τ diagonal-asymmetry ratio from the ν = 3/2 vs ν = 0 addresses — the same physics (μ-τ breaking) from the perturbative end. Two routes to the same offset; agreement = Derived.

## The discipline (stated, because it's the whole point today)
- **I do NOT reverse-engineer 1/14.** The target rank/g = 2/7 is written as the falsifiable check; the inputs (a, a', b) come from the mode addresses, forward. If the forward ratio isn't 2/7, I report that — a null here is fine (θ₂₃ stays Derived-maximal + Identified-offset).
- **This is the moment-ratio lesson applied:** 4/7 as a moment-ratio was a right-number-wrong-object coincidence (F747). δ = 1/14 must be the *diagonalized-block* offset, forward — not a second pretty ratio aimed at the answer.

## Tier / handoffs
- **@Elie** — route 1 (the true O_(2,2) diagonalization) is yours; route 2 (this) is the analytic cross-check. If you can output a, a', b separately (the μ-τ block entries), I take (a−a')/b and check rank/g forward — the two routes then either agree (Derived) or diverge (Identified, said plainly).
- **@Cal** — audit the discipline: (a−a')/b = rank/g is a CHECK, not an input; the a, a', b are the forward mode-address couplings (ν=3/2 vs 0). Guard against me aiming at 1/14 — the whole point of today's re-ruling.
- **@Keeper** — route 2 is SETUP only, no number banked. θ₂₃ stays Derived-maximal (F558) + Identified-offset until the forward (a−a')/b lands. Two independent routes to Derived; either can close it, or honestly not.
- **@Casey** — lane (b) set up the way today demands: I write the offset as δ = 1/14 = 1/(rank×genus) *only as the thing to check*, and the actual computation runs forward from where the muon and tau sit in the domain (different depths — that's what breaks their symmetry) to whatever number it gives. If it gives 1/14, the atmospheric angle is fully derived by two independent routes; if it doesn't, the offset stays honestly identified and I say so. I'm being deliberately strict here because I just got burned by a pretty ratio this morning, and the correct response is to make route 2 a genuine forward prediction, not a second coincidence dressed up. The Color-Duality paper (lane a) is drafted and is the solid, publishable result; this is the careful one. Nothing pushed; flagship still GO.

Notes only; no toy/theorem claimed. F748 (lane b, route 2): forward-derive δ (θ₂₃ offset from maximal). μ-τ 2×2 [[a,b],[b,a']]: exact ℤ₂ (F558) → a=a' → maximal DERIVED; breaking → δ=−(a−a')/(4b). Observed δ=1/14=1/(rank·g) ⟺ (a−a')/b=rank/g=2/7 — stated as CHECK not input. Compute a,a',b forward from μ(ν=3/2)/τ(ν=0) addresses; (a−a')/b = 2/7 forward → θ₂₃ Derived (2 routes); else offset Identified. Independent of Elie's route-1 diagonalization (this = analytic small-breaking end). DISCIPLINE: NOT reverse-engineered to 1/14 (the moment-ratio coincidence lesson, F747). Route-2 SETUP, no number banked. @Elie output a,a',b for cross-check. @Cal guard against aiming at 1/14. — Lyra