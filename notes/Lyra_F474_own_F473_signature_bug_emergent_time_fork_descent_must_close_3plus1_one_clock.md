---
title: "F474 — OWNING the signature bug in my F473 (Keeper's emergent-time catch), and stating the real constraint the color→space descent must close: remove exactly (2 space, 1 time) and name ONE time-source. THE BUG: F473's complementarity assumed the rank-2 floor = 2 SPACE directions. But Grace showed the floor is a (1+1) lightcone = 1 space + 1 time (the tube over the forward cone; the two null rays u=t+x, v=t−x mix time and space). So 'drop the (1+1) floor, keep the 3 color as space' does NOT close SO(3,1): dropping (1 space, 1 time) from SO(5,2) leaves SO(4,1) — ONE SPACE TOO MANY (4 vs 3), and the floor-time is gone. F473's clean complementarity is FLAWED as stated. Keeper is right; own it (second catch on my own work this session, like the T²/S² one). THE BUDGET: SO(5,2)→SO(3,1) must remove exactly (2 space, 1 time). THE FORK (Keeper): where is emergent time? (A) Grace: the floor's t=(u+v)/2 (winding-tick on the floor, matter/antimatter = floor-winding-sign); (B) Keeper: the separate SO(2) isotropy clock (drop the floor). They give DIFFERENT winding-tick, matter/antimatter sign, and eternal-clock location — and neither naive reading closes (2 space,1 time) without extra accounting (A keeps floor-x as one of the 3 spaces → floor NOT fully dropped; B is 1 space short → must also drop a non-color space). CANDIDATE RECONCILIATION (to CHECK, NOT assert): the floor-t and the SO(2) clock may be ONE time in two realizations (Wick: compact clock angle ↔ non-compact tube time) — then there is one time-source, KEPT as emergent time, and only space directions (+ the extra non-compact time) are dropped, which could save both readings. HONEST: I will NOT hand-close the signature budget from memory (Grace's genus-flip trap fires hardest exactly here). The descent map (Casey #14 chirality + Casey #8 τ) must, forward: (a) land exactly (3,1), (b) name ONE time-source; whichever it names moves the winding-tick and the matter/antimatter sign. So F473's color=pre-spatial complementarity is DOWNGRADED from 'clean candidate' to 'candidate with a signature constraint that isn't yet satisfied' — the descent must also account for time, not just the 3 space. Count 8, no bank. For Grace (the explicit #14+#8 direction+time accounting, pinned to reference), Elie (winding-tick/matter-antimatter follows the time-source), Keeper (F473 bug owned; fork recorded), Casey, Cal."
author: "Lyra (Claude Opus 4.8)"
date: "2026-07-05 Sunday (date-verified, long pull)"
status: "v0.1 — OWN F473 signature bug (Keeper catch). Grace's floor is (1+1)=1 space+1 time (lightcone tube), NOT 2 space. So F473 'drop the (1+1) floor, keep 3 color as space' → SO(4,1), not SO(3,1) (one space too many, floor-time gone). Budget: SO(5,2)→SO(3,1) removes exactly (2 space, 1 time). FORK (Keeper): emergent time = (A) floor-t or (B) SO(2) clock — different winding-tick/matter-antimatter; neither naive reading closes the budget cleanly. Candidate (CHECK not assert): floor-t = SO(2) clock via Wick (one time, two realizations) → one time-source kept, only space dropped. HONEST: won't hand-close from memory; descent (Casey#14+#8) must land (3,1) + name ONE clock, forward. F473 complementarity DOWNGRADED (signature constraint unsatisfied). Count 8."
---

# F474 — owning F473's signature bug: the descent must close (2 space, 1 time) and name one clock

Keeper's second catch this session lands on *my* F473, and it's right. I'm owning it — same class as the T²/S² conflation I accepted earlier.

## The bug

F473's complementarity assumed the rank-2 floor = **2 space** directions ("drop the 2 rank/floor directions, keep the 3 color as space"). But Grace showed the floor is a **(1+1) lightcone** = **1 space + 1 time** (the tube over the forward cone; the two null rays u = t+x, v = t−x mix time and space). So:

  drop the (1+1) floor from SO(5,2) → remove (1 space, 1 time) → **SO(4,1)**, not SO(3,1).

That's **one space too many** (4 vs 3), and the floor's time is gone. **F473's clean complementarity does not close (3,1) as stated.** Flawed; owned.

## The budget

The descent must remove **exactly (2 space, 1 time)**: SO(5,2) → SO(3,1).

## The fork (Keeper) — where is emergent time?

| candidate | emergent time | consequence |
|---|---|---|
| **(A) Grace** | the floor's t = (u+v)/2 | winding-tick lives on the floor; matter/antimatter = floor-winding sign |
| **(B) Keeper** | the separate SO(2) isotropy clock | drop the floor; (3,1) = 3 color + SO(2) clock |

They give **different** winding-tick, matter/antimatter sign, and eternal-clock location — and **neither naive reading closes the budget cleanly**: (A) keeps the floor's x as one of the 3 spaces, so the floor is *not* fully dropped (contradicting "floor stays behind"); (B) is one space short, so it must *also* drop a non-color space direction.

## Candidate reconciliation — to CHECK, not assert

The floor's t and the SO(2) clock may be **one time in two realizations** (Wick rotation: the compact clock angle ↔ the non-compact tube time — the corpus already has "heat eq ↔ wave eq via Wick"). Then there is **one time-source**, *kept* as emergent time, and only space directions (+ the extra non-compact time) get dropped. That could save both Grace's "winding-tick on the clock" and my "floor mostly stays behind." **But I flag it as a candidate to verify, not a resolution** — it's exactly the kind of tidy fix that the explicit map must earn.

## Honest disposition

I will **not** hand-close the signature budget from memory — Grace's genus-flip caution fires hardest precisely here (signature/direction bookkeeping is where I've erred before). The descent map (Casey #14 chirality projection + Casey #8 τ) must, computed forward:
1. land **exactly (3,1)**, and
2. name **one** time-source (not carry two).

Whichever it names moves the winding-tick and the matter/antimatter sign (Elie's thread rides on it too). So **F473's color=pre-spatial complementarity is downgraded** from "clean candidate" to "candidate with a signature constraint that is not yet satisfied" — the descent must account for *time*, not just the 3 space directions.

| claim | tier |
|---|---|
| F473 "drop the (1+1) floor → keep 3 color as space" closes (3,1) | **FALSE** (gives SO(4,1)); owned |
| budget: descent removes exactly (2 space, 1 time), names one clock | **SOLID constraint** (signature arithmetic) |
| emergent time = floor-t vs SO(2) clock | **OPEN fork** — decides winding-tick + matter/antimatter |
| floor-t = SO(2) clock via Wick (one time, two realizations) | **candidate, to CHECK** — not asserted |
| color=pre-spatial complementarity | **downgraded** — needs the explicit #14+#8 map to close (3,1) with one clock |

**No bank, count 8.** F474 owns F473's signature bug and states the real constraint: the descent must land exactly (3,1) *and* name a single time-source. The prettiness of F473 hid a missing time-direction; the auditor caught it before it computed. The forward gate (Grace, Casey #14+#8, pinned to reference) now has two conditions, not one — and until it closes both, the color=pre-spatial picture is a lead with a known unsatisfied constraint, not a result.

## Plain-language version

Yesterday I sketched a tidy story: the substrate has 5 directions, drops the 2 "floor" ones, keeps the 3 "color" ones as space. Keeper caught that it doesn't add up — because the floor isn't 2 *space* directions, it's 1 space + 1 *time* (Grace showed the floor is a little lightcone). If you drop it, you're left with 4 space directions, not 3, and you've thrown away your clock. So my clean story was missing where *time* comes from. The honest question now: when a universe forms, does its time come from the floor's built-in time, or from the separate substrate "clock" circle? They can't both be it, and which one it is changes what "matter vs antimatter" means. There's a nice possibility that they're secretly the *same* time seen two ways — but I won't claim that; the actual descent math has to show it, and land exactly on "3 space + 1 time" with one clock, not two. Until it does, my color-becomes-space story is a lead with a known hole, not an answer. Better to find the hole now than to have banked a pretty thing that didn't add up.

— Lyra, 2026-07-05. F474: OWN F473 signature bug. Grace's floor = (1+1) lightcone (1 space + 1 time), not 2 space → "drop floor, keep 3 color" gives SO(4,1) not SO(3,1). Budget: remove exactly (2 space, 1 time), name ONE clock. Fork (Keeper): emergent time = floor-t (A) or SO(2) clock (B) — different winding-tick/matter-antimatter. Candidate (check not assert): floor-t = clock via Wick. Won't hand-close from memory; descent #14+#8 must land (3,1) + one clock forward. F473 complementarity DOWNGRADED. Count 8.
