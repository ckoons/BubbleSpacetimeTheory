Team turn — 2026-07-03 next. The down-ladder is now a d(ν) question. Compute it.

SOD check first. The gate sharpened this turn — here's the clean state.

## Where we are

- **The down-ladder gate is now SHARP.** Quark mass = **d(ν) [deposit density] × k_s [cohomology]**, both forced. Grace computed her half: **k_s = dim ker R is small — {2, 4, 6, 8}** (S⁴→2, wall-flag→4, Q⁵→6, color-Fock→8), none near 20. That's a *useful computed narrowing*: since the ladder is {1, 20, 900} and k_s is small, **the large numbers (20, 900) must come from Lyra's d(ν)** — the Harish-Chandra formal degree. The question "why 20, why 900" moved from geometry to rep-theory, with a definite answer from Grace's side.
- **Confinement = "commit 27, read out 1"** (Lyra F459): the 3-quark system commits the full N_c³=27 color tensor (3⊗3⊗3 = 10⊕8⊕8⊕1); only the singlet Λ³=1 manifests; the other 26 are committed-but-confined. Unifies committed-info + Pauli(exterior algebra) + confinement — same N_c³ as the α count. (Algebra is Günaydin-Gürsey — cite, don't claim; ours is the commit-and-read language.)
- **Running vs dressing disentangled (K650, Elie 4553):** the 1-2% cross-sector residual is QCD **running** (cancels same-sector: m_s/m_d=20 is 0σ); membrane **dressing** is the absolute constituent addition ~m_p/N_c. Layer-2 dressing is a constituent-mass prediction with running subtracted — NOT a ratio residual.
- Count 8.

## The frontier — COMPUTE d(ν) (Lyra, highest-leverage)

The whole down sector's derived-strong test is now one rep-theory computation:

> **Compute d(ν) (the Harish-Chandra formal degree) at the three generation K-types, reconcile with Grace's k_s ∈ {2,4,6,8}, and read d(ν) × k_s against the bare ladder N_c² × {1, 20, 900}.**

- **COMPUTE, do NOT fit.** Grace held the χ=4 line (it's the honest wall-flag Betti number, not a fish); Lyra holds the d(ν) side the same way. If d(ν) × k_s forces {1, 20, 900}, the bare down sector is derived-strong. If not, honest negative — the ladder is a lead, not a bank.
- Recall d(5/2)=0 (massless bare gen-1) — the formal-degree zeros are load-bearing; work them carefully.

## Per-CI

- **LYRA (primary):** compute d(ν) at the three K-types → the ladder gate. Reconcile with Grace's k_s. Solo: close the ν-scale coefficients + finish registry backfill (fresh reads). Check Elie's ν-regime lead (does singlet retention ~10⁻⁷ = a truncation residue ~2^{−3g}?).
- **GRACE:** k_s half done ({2,4,6,8}); now geometry-checker on Lyra's d(ν) reconciliation + the constituent-dressing geometry (Layer 2, where running is already subtracted).
- **ELIE:** checker on d(ν) × k_s (forced vs fit — hold the χ=4/12 fence); the ν-regime residue lead (retention = 2^{−3g}?); constituent-dressing (K650 rule — dressing lives in the absolute constituent mass, not the ratio residual; top control f_had≈0).
- **KEEPER:** adjudicate d(ν) × k_s (forced vs fit); **hold the running-vs-dressing line (K650)** — no ratio residual reported as dressing; keep the two-layer + regime frame honest; the residue-family thread (ν, and Λ — see Casey's item). SOD first.
- **CAL:** post-landing cold-read. α tier-call reserved for Casey.

## Disciplines armed

- **Compute the object, don't fit the number** (d(ν) and k_s are both forced; their product is forced-or-not).
- **Derive BARE; predict dressing = retention × thickness, running subtracted first.**
- Simplest one-line-checkable forms (ad-AI-num armor) · Five-Absence · target-innocence · σ scheme-aware · terminal is an answer · pre-audit.

## Standing for Casey

d(ν) close (pending) · two-layer + regime mass model as the mass-sector frame (ratify) · down-row re-tier (reframed to bare) · σ-metric + cheapness ratification · α tier-call (Wyler-informed) · CLAUDE.md line 0.5.

## EOD target

d(ν) **computed** at the three K-types → the ladder {1,20,900} forced-or-not (with Grace's small k_s). ν coefficients closed-or-flagged. Backfill advanced. The residue-family question opened (ν → Λ). Derived-strong grows only if d(ν)×k_s forces the powers.

— Keeper, next turn. The ladder is a d(ν) computation now — Grace narrowed it, Lyra closes it. Compute, don't fit. Mechanism is the prize.
