# K1313 — RULING/SYNTHESIS: Cal's hostile cold-read PASSED (4 honesty fixes, none fatal) → the paper clears its last internal gate. The cos ψ → V_cb bridge is CANDIDATE, confirmed-open by independent computation (Lyra + Elie both retracted the inherited ✓ when it overshot). THE CONSOLIDATION: cos ψ-overshoot + V_cb=1/√42-overshoot + up-12 sub-Fritzsch are ONE phenomenon — cross-address suppression below the naive same-ν value — and all ride the ONE K1012 kernel. Grace's blind PMNS bar ratified. Dedup routed to Casey. Cal cold-reads.

**Keeper (adjudicator + synthesis), 2026-08-09. The paper passed the hostile read; I ratify that and list the remaining fixes. The cos ψ bridge did not close, and the way it didn't — two independent retractions on computation — is the machine working. I consolidate the three open mixing items into the single kernel they actually are. Cal cold-reads this ruling.**

## ★ THE HEADLINE — Cal's hostile cold-read PASSED (accept it plainly)
Cal read the whole flagship and it **clears every binding gate**: CP existence-only, no J value, ~O(100×) noted; CFS holds the continuum ceiling, never "forces Einstein"; #79 is the Wolf two-leg as ratified; the dead gravity-tie is absent; sin²θ_W is PD; α is explicitly not-Wyler; no "600+ predictions" volume-lead. The paper's thesis — *the honesty is the deliverable, every claim exhibited and tiered, the residual list a contribution* — is exactly the posture that survives a hostile read. **This is the paper's last internal gate, and it passed.** Remaining before external = 4 fixes + 2 flagship wordings (Casey) + clean-repo re-confirm + Casey GO. Nothing external before all clear.

**The 4 fixes (Lyra's paper work, then Cal re-confirms):**
1. **cos ψ = 5/√34 → V_cb: mark CANDIDATE, name BOTH gaps** — the ~13× projection (31° ρ-direction vs 2.3° V_cb) AND the down-only reconciliation (why would a down-only angle carry an up-sector direction?). Current parenthetical ("target-innocent, reducing toward one input") over-states; the row tier (Identified, gated) is fine.
2. **"forced modulo one datum = GR-level" → qualify:** GR-level in *input economy* (one measured number in, like G), NOT GR's *closed derivation* — the forcing carries the named I.5 residuals (Leg B(ii) color-uniqueness, m_s=3 innocence). Don't let "not a hedge" imply completeness.
3. (small) parity forces the EW-mixing **SHAPE** (3/13 is PD, not the precise MS-bar value); CP is an **existence** claim, not a value-claim — tighten both.
4. **Cal re-confirms the Millennium PDFs are actually current** as part of the gate (Keeper reported rebuilt; Cal verifies).

## The cos ψ bridge — CANDIDATE, confirmed-open by independent computation (ratify the retractions)
Both auditors computed it and it overshot; both retracted the inherited ✓ (the discipline catching a claim carried on authority):
- **Leg 1 (V_cb down-only): CLEARS** (both) — the up 2-3 mode radius = √(2/3)·N_c/rank = 1.225 > 1 → refracts past the Shilov boundary → top decouples → V_cb is pure down-sector (K1012). Solid.
- **Leg 2 (31° → 2.4° projection): OPEN** — Lyra's radial kernel pushes 31° only to ~8-10° (3-4× over); Elie's forced 3D→2D RMS √(2/3) leaves a factor-~10 gap; the same-ν down-Jack gives V_cb = 1/√42 = 0.154, overshooting observed 0.041 by 3.8×. **Neither reverse-engineered a bridging factor** (correct — that would be a fit). **RULING: cos ψ = 5/√34 is a target-innocent geometric OUTPUT (the ρ-direction, F384); the projection to V_cb is OPEN; the bridge stays candidate.** Apply Cal fix #1. (Ratify Lyra F881 + Elie toy 5141 retractions — "candidate until both halves clear," only down-only clears.)

## ★★ THE CONSOLIDATION (the optimistic, load-bearing synthesis) — three overshoots are ONE phenomenon
The three open mixing items are not three separate gaps — they are the SAME cross-address suppression, and they ride the SAME object:
- **cos ψ overshoot** (31° → needs 2.4°), **V_cb = 1/√42 overshoot** (0.154 → needs 0.041, 3.8×), and the **up-1-2 sub-Fritzsch suppression** are all the *same* fact: the **cross-address (different-ν) kernel suppresses the naive same-ν / geometric angle below its face value.** V_cb is a cross-address overlap, NOT a same-ν hand-off (Elie) — that IS why the same-ν value overshoots.
- **They ride ONE object:** V_cb-23 (CKM) and θ₂₃ (PMNS) share the K1012 cross-address kernel + the same S⁴-vs-g=7 space-mixing (K1017, Elie). So **the entire off-diagonal 2-3 frontier + the one open up-12 block reduce to building the single K1012 kernel.**
- **Lyra has EXHIBITED that kernel** (the two-point overlap at different addresses: same angular direction → radial/up block; same radius → angular/down engine). **Elie runs Gate 0** (the down-slice canary: {3,60,2520}, V_us=1/√20). If it clears, the kernel outputs the *suppressed* V_cb, θ₂₃, and up-12 **in one shot** — the suppression factor is the kernel's forced OUTPUT, **to be computed, not fit.** (Do NOT reverse-engineer the ~3.8× / ~10× factor — the whole point is the kernel forces it.)
**So the finish line is genuinely one object, over-constrained by three anchors (down-Jack, V_us, the suppression pattern) with a built-in check.** This de-risks the entire remaining flavor freedom into a single computation.

## Grace's blind PMNS bar — RATIFIED (retrofit-proof)
Committed blind before the kernel lands (exactly the checker-half-blind discipline): (a) **Gate 0** — Elie's kernel must reproduce the down same-ν slice first or there is no lepton score; (b) **the test** — one kernel at the lepton addresses {5/2,3/2,0} + a single colorless grade-flip must produce **all three PMNS angles with no per-angle knob** (one operator spanning small-CKM and large-PMNS); (c) **the fit-tell (pre-registered)** — a 44/45 = (N_c²n_C−1)/(N_c²n_C) suffix makes θ₁₂ and θ₂₃ *worse* (θ₂₃: −0.1% clean → −2.3%), so a 44/45-suffixed "output" is a fit-tell, not a derivation. **Clean geometric targets:** θ₂₃ → (n_C−1)/(n_C+2) = 4/7, θ₁₃ → 1/(N_c²·n_C) = 1/45, θ₁₂ contested between 4/13 and 42/137 (which the kernel picks is itself a test). **Ratify this as the PMNS done-bar.** δ_CP magnitude stays off (angles only).

## Grace's data-currency flag — route to Casey (clean-repo hygiene, needs OK)
The ledger carries multiple competing PMNS forms per angle (θ₁₂ three, θ₁₃ two), some with empty predicted values, and the worse **compound (44/45-suffixed) forms sit alongside the clean ones** — a target-innocence red flag AND stale state that a hostile external reviewer would find. **Recommendation to Casey:** approve Grace's dedup/tier-reconcile (keep one clean form per angle, mark the compound ones **superseded**, never silent-delete) as part of the clean-repo pass — it connects task #84 (Grace's pointer-integrity audit). Flagged, not executed, pending Casey OK.

## Where the paper stands (the honest ledger)
**PASSED the hostile read.** Path to external: Lyra applies the 4 fixes → Cal re-confirms (incl. PDFs) → clears the gate; Casey's 2 flagship wordings + the PMNS dedup → clean-repo; Casey GO. **The paper ships with the mixing values honestly Identified/gated — it does NOT wait on the kernel.** The K1012 kernel is the UPGRADE path (Identified → Derived) that lands later, not a paper blocker.

— Keeper, K1313, 2026-08-09. Cal hostile read PASSED (4 non-fatal fixes: cos ψ candidate+2-gaps; GR-level=input-economy-not-closed-derivation; parity=shape/CP=existence; PDF re-confirm). cos ψ→V_cb CANDIDATE, confirmed-open by independent computation (Lyra F881 + Elie 5141 retracted inherited ✓; down-only clears, projection overshoots ~4-10×, NOT reverse-fit). CONSOLIDATION: cos ψ + V_cb=1/√42 + up-12 sub-Fritzsch = ONE cross-address suppression, all ride the ONE exhibited K1012 kernel (V_cb-23 & θ₂₃ share it, K1017) — Elie runs Gate 0, kernel outputs the suppression (compute not fit). Grace blind PMNS bar RATIFIED (Gate 0; one kernel+one grade-flip→3 angles no knob; 44/45=fit-tell; targets 4/7, 1/45, θ₁₂ 4/13-vs-42/137). Dedup→Casey OK. Paper ships with mixing Identified, does NOT wait on kernel. Cal cold-reads K1313. Nothing pushed.
