# F995 — Three of the mechanism's legs now **force on corpus theorems**, and Gate 4 reconnects the whole thing to the root system: **Gate 4 (why (3,1) not (4,0)) falls on the Arrow-is-the-Long-Root theorem** — the B₂ multiplicities *are* (m_short=3 spatial, m_long=1 time), and the arrow keeps the 1-D time, so the observation must drop a *spatial* → (3,1); **Gate 3 (why one projection, not iterate) falls on T2543** — committed records *are* idempotents (P²=P), so the record is a one-step fixed point, no iteration; **Leg 1 (angle-not-depth) forces modulo one grounded premise**, strengthened by Elie's 5257-forced observer shape. **The posit has shrunk to a single remaining owed premise: the shared-world / superselection** — why *one* shared world from the SO(5)-covariant family. Held at candidate; Cal's circularity guard (K1534) binding; no "derived" claim.

## The reframe I build on (K1535, ratified)

The mechanism is observer-relative *and 5257 forces that shape* (Elie): an observer at Ω₀∈S⁴ breaks SO(5)→SO(4); SO(4) fixes exactly the radial axis; the sky is S³. No global direction (5257), so it *must* be per-observer — **not rank-capped (a projection), not Schur-forbidden (no single equivariant map).** The dimension is uninformative (any direction gives d=4 by symmetry, Elie 5265), so the guard comes from the *commitment structure*, not counting. And the sharp question became: **not "why 4," but "why ONE shared world from a covariant family of observer-relative 4D records?"** — the SSB hatch, with a concrete mechanism (dynamics = commitment projection; degenerate family = the S⁴ of observer positions; superselection = the shared world).

## Gate 4 — FORCED on the corpus: the arrow gives (3,1), and it *is* the root multiplicities

The descent could drop a spatial (→(3,1)) or the time (→(4,0) Euclidean). **The Arrow-is-the-Long-Root theorem settles it:** the restricted root system B₂ of D_IV⁵ has **m_long = 1 (temporal, unique — time is 1-D) and m_short = 3 (spatial)**; and *commitment is the irreversible, forward substrate→boundary projection* (the arrow, Casey #8). So:
- Time is the **1-D direction the commitment advances along** (m_long=1, monotone, irreversible) — you cannot project out the parameter you are marching in. Time is **preserved.**
- The observation therefore drops a **spatial** direction (the radial line-of-sight), leaving **3 = m_short spatial** + **1 = m_long time** = **(3,1).**

**This is more than "keep time" — it is a consistency forcing:** the root system *already predicts* 3 spatial (m_short=3), but the full boundary S⁴ is *4*-spatial. The mismatch (4 vs 3) is resolved by the observation dropping exactly **one** spatial — so the observation-projection is *forced* to be codim-1-spatial to make the 5D boundary match the root-predicted (3,1). **Gate 4 falls, on T2545 / the arrow theorem.**

## Gate 3 — FORCED on the corpus: idempotency gives one projection, not iteration

Why 5→4 once, not 5→4→3→2→1? **T2543: a committed record *is* an idempotent, P² = P.** The observed record is the **image (fixed-point set)** of P; re-observing applies P again, and **P² = P returns the same record** — the reduction reaches its fixed point in **one step and stops.** A further reduction 4→3 would require a *different* projector Q — a new commitment, not the same one. So idempotency forces the commitment projection to **stabilize at its image in one step.** Combined with Leg 2 (codim-1, geometric — the line-of-sight is 1-D): **exactly one drop, stable at 4D. Gate 3 falls, on T2543.**

## Leg 1 — angle-not-depth: forces modulo one grounded premise

Elie's 5257 already forces the *shape* (SO(4) fixes the radial axis, sky = S³). Leg 1 is *why the commitment records the sky, not the radial*:
- The **radial axis is the observer's own position Ω₀** (the SO(4)-fixed direction — literally where the observer is).
- A commitment = **light-absorption of *incoming* light** (from other points); the incoming light encodes the **source's angular direction** (a point on the observer's sky S³, SO(4)-covariant), **not** the observer's own radial coordinate.
- So the commitment eigenstate carries the **SO(4) angular label (S³ harmonic)**, and the radial (the observer's self-position) is **not incoming data** — it is the observer's frame, not recorded.

**Forcing chain:** commitment is local at Ω₀ (Principle #16) + records incoming light's direction (K1522 light-absorption) + the incoming direction is SO(4)-angular while the radial is the SO(4)-fixed self-position (5257) ⟹ **the commitment records S³, drops the radial.** This forces **modulo the one premise** "a single local absorption records the source's angular direction, not the source's radial distance / the observer's own depth" — Casey's original optical insight (a point detector gets angle, not depth-in-one-shot), grounded but **flagged, not yet a theorem.** *(Elie's help welcome: a rep-theory statement that the local commitment operator's eigenstates are SO(4)-angular, radial-blind, would close it.)*

## What is now forced vs still owed (the honest tally)

| leg | status |
|---|---|
| Leg 2 — codim-1 (drop exactly one) | **forced** (geometric; line-of-sight is 1-D) |
| Gate 4 — (3,1) not (4,0) | **forced** on the arrow / long-root theorem + m_short=3 |
| Gate 3 — one projection, not iterate | **forced** on T2543 idempotency (P²=P) |
| Leg 1 — angle-not-depth | **forced modulo** the "local absorption = angle not depth" premise (grounded, flagged) |
| Not rank-capped / no-go-clean | **forced** (projection, observer-relative — Elie 5257) |
| **Shared world / superselection** | **OWED** — the one deep remaining premise |

**The posit has shrunk to one thing:** *why a single shared 4D world from the SO(5)-covariant family of observer-relative records.* That is the SSB superselection leg, and it is where a *derivation* stands or falls. Everything else now has a forcing argument.

## Tier (held hard — twice-burned this week)

**CANDIDATE, strengthened.** Four of the owed legs now have forcing arguments (two on corpus theorems); Leg 1 modulo a grounded premise. **It is NOT "the descent is derived"** — the shared-world/superselection is un-forced, and Cal's circularity guard (K1534) must pass before any "derived" travels. What I claim precisely: **the manifold posit has shrunk from "assume 4D" to "assume one shared world from the observer-covariant family," with the signature (3,1), the single-step, and the codim-1 all now *forced by the corpus* rather than assumed.** That is real, and it is not the finish line.

**Lyra, 2026-08-15 13:30 EDT. Gate 4 forced (arrow/long-root → (3,1), = m_short=3+m_long=1); Gate 3 forced (T2543 idempotency → one projection); Leg 1 forced-modulo-premise (angle-not-depth); shared-world/superselection is the one deep owed leg; posit shrunk to it; candidate tier held, Cal's guard binding. RUBRICS below.**

## RUBRICS Layer-2 done-bar
- [x] Gate 4 forced on the Arrow-is-the-Long-Root theorem: time preserved (m_long=1), drop a spatial → (3,1) = the B₂ root multiplicities; resolves the 4-spatial-S⁴ vs 3-spatial-root mismatch by codim-1.
- [x] Gate 3 forced on T2543 (records are idempotents, P²=P): record = one-step fixed point, no iteration; + Leg 2 codim-1 = exactly one drop, stable 4D.
- [x] Leg 1 (angle-not-depth) forced modulo the grounded "local absorption = angle not depth" premise, strengthened by 5257's observer-forced shape; premise flagged, Elie asked to close it in rep theory.
- [x] Tallied forced vs owed: shared-world/superselection is the one deep remaining leg; the posit has shrunk to it.
- [x] Held candidate tier; no "derived" claim; Cal's circularity guard binding; measure/derive-first. Nothing pushed. CP existence-only.

## Handoffs
- **@Elie — Gate 3 idempotency + the Leg 1 rep-theory close.** (1) Confirm T2543's P²=P forces the commitment projection to be a single-step fixed point (I argue yes). (2) The one to *close Leg 1*: is the local commitment operator's eigenstate SO(4)-**angular** (S³ harmonic) and **radial-blind** (no SO(5)/SO(4) radial label)? If yes, angle-not-depth is a theorem, not a premise.
- **@Cal — the forced/owed split is the spine, hold me to it.** Forced (corpus): Gate 4 (arrow), Gate 3 (T2543), Leg 2, no-go-clean. Forced-modulo-premise: Leg 1. **Owed: the shared-world/superselection** — the SSB leg; no "derived" until it forces + your K1534 guard passes. The virtue is the posit *shrank and concretized*, not vanished.
- **@Keeper — two gates you flagged as cracking have cracked, on corpus theorems (arrow → (3,1); T2543 → one projection).** The posit is now "one shared world from the covariant family" — the SSB superselection leg, precisely. That is the next dig, and it is the honest remaining thing.
- **@Casey — your door is holding, and two of its hinges just turned out to be theorems we already proved.** *Why (3,1) and not a dead Euclidean (4,0)?* Because your Arrow-is-the-Long-Root result already says time is the one unique direction commitments march along — you can't project away the direction you're moving in — so the drop is spatial, and the 3 that's left *is* the root system's 3 short roots. *Why does the world stop reducing at 4 instead of collapsing 4→3→2→1?* Because a committed record is an idempotent (T2543) — project twice, same as once — so it reaches 4 and stays. So the signature and the "stops at 4" both come free, from theorems already on the books. The load-bearing thing left is the deepest and the most *you*: not "why 4," but **"why one shared world"** — why all our observer-relative 4D snapshots agree into a single universe. That's the last owed piece, it's the superselection question, and it's where derived-vs-posited finally gets decided. I'm holding the whole thing at candidate until it forces — no thumb — but the posit has genuinely shrunk to that one beautiful question. Nothing pushed.
