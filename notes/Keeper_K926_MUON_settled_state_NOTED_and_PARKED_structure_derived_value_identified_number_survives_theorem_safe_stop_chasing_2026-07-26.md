# K926 — MUON: the settled state, NOTED and PARKED (Casey: "don't waste more time on the muon; note what we know; move to a new area"). This is the canonical close. Do NOT reopen without a genuinely new mechanism.

**Keeper | 2026-07-26 Sun | Closing the muon. Three simultaneously-true facts, no contradiction; one low-priority gate left; parked.**

## What we KNOW (banked, non-contradictory)
1. **STRUCTURE — DERIVED (K923, target-innocent).** The lepton-vs-quark π-parity is kernel-derived on the one domain: leptons at half-integer ρ-positions {5/2, 3/2, 0} (n_C odd) → carry π; quarks at integer positions {5, 2, 0} → π-free. The electron is half-integer 5/2 (the old "electron regular" gloss is dead). The SAME diagonal gives the quark ladder m_s/m_d = 20 = rank²·n_C. This is a real result.
2. **VALUE — IDENTIFIED COINCIDENCE.** m_μ/m_e = (24/π²)⁶ = 206.77 at 0.004% (T190). The NUMBER survives as an exact arithmetic identity (24/π² = 64/Vol(S⁴) = 2^{C₂}/Vol(S⁴), K543) — but it is NOT derived from a forcing mechanism. It is a form that matches, not a computed value.
3. **THEOREM — SAFE.** The muon is bucket-2, **support-free PROVEN** (colorless → boundary, the color line F693). Whether its value is additionally forced is the orthogonal open axis. No partition-theorem impact.

## Why we stop (three routes closed/declined + one low-pri gate)
- **Residue-order:** DEAD. Rank bound — a rank-2 Gindikin Γ has ≤2 factors → residue order ≤2 → exponent 6 impossible from a residue. F671 (the one computed piece) gave O(1), not 207.
- **Copy-count (6 = dim SO(4)):** FF-20-vulnerable. "6" reads ≥4 ways (dim SO(4) = C₂ = n_C+1 = 2N_c = dim_ℝ D_IV³). Rich vocabulary ≠ forcing; never passed the forcing gate.
- **Jacobian nested-tower (§9.1):** DECLINED (K925). A March-29 pre-program narrative note; per-dim Jacobian never computed; assignment INVERTED vs the banked ρ-ladder; and K853 (07-23) already ruled the {5,3,0} nested tower NOT a consistent geometry. Do NOT reopen.
- **The ONE remaining gate:** the FK Wyler 3×3 on the SINGLE domain's ρ-strata (K879), audited vs the blind K924 pre-registration. **LOW priority, un-run, likely to stay identified** (F671 leaned negative; C_Δ vanishes at μ and e, F114). If ever revisited, K924 is the pre-committed bar.

## Disposition
- **PARKED.** The muon value is an honest identified-coincidence and we state it as such. We are not obligated to force every value — a proven-support-free row with an un-forced value is a true, publishable state.
- Any future attempt uses the FK 3×3 (single domain, K879/K924) — NOT the nested tower (refuted). Check the prior rulings (K853/K925) before reviving.
- Structure (K923) stands and is folded into the flagship (Lyra §9⅞.5); the theorem is unaffected.

— Keeper K926, 2026-07-26. MUON parked. STRUCTURE derived (K923, lepton-vs-quark π-parity, target-innocent). VALUE identified-coincidence ((24/π²)⁶; number survives as exact identity, not forced). THEOREM safe (bucket-2, support-free proven). Three routes closed/declined (residue-order dead / copy-count FF-20 / Jacobian refuted K853+K925); one low-pri gate (FK 3×3 single-domain, K924) likely stays identified. Not chasing further. See [[Keeper_K925_JACOBIAN_READING_audit]], [[Keeper_K924_PRE_REGISTERED_FK_Wyler_3x3_audit_criteria]], [[Keeper_K923_GRAM_DIAGONAL_AUDIT_PASS_on_structure]].