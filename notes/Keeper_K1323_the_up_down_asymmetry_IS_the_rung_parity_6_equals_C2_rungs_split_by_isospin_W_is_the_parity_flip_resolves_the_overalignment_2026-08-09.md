# K1323 — STRUCTURAL (linear algebra on D_IV⁵, Casey's directive): the up/down asymmetry that breaks the CKM near-cancellation IS the RUNG PARITY. The 6 = C_2 lowest discrete-series rungs {0..5} split by parity into up (EVEN {0,2,4}) and down (ODD {1,3,5}) — 3 generations each; parity = weak isospin T₃. The W flips parity (ΔT₃=±1 ⟺ Δλ odd), so the CKM is a cross-parity overlap that does NOT cancel. This resolves Elie's over-alignment (5148) and ties the mixing to the oddness engine. Structural connection, NOT banked values. Cal cold-reads.

**Keeper (structural lane, per Casey "connect to the corpus, linear algebra on D_IV⁵"), 2026-08-09 ~18:00 EDT. The team found the naive shared-Gram CKM over-aligns (K1322) — the mixing is the up/down breaking. Working the frame, the breaking has a clean corpus-native form: it's the parity split of the lowest rungs, and it explains exactly why the naive construction cancelled. Structural tier; the forcing (is isospin = rung parity?) + the values stay for the team to verify/compute blind. Cal cold-reads.**

## The structure (one operator, its rungs split by parity)
The down-quark modes sit at the ODD rungs λ ∈ {1,3,5} — the (N_c)_λ = {3,60,2520} ladder (K1320, Derived). Lyra flagged the up-quarks at the EVEN rungs {0,2,4}. Laid out:
> **The 6 lowest discrete-series rungs {0,1,2,3,4,5} — and 6 = C_2 — split by PARITY:**
> **UP = EVEN {0,2,4} (3 generations) · DOWN = ODD {1,3,5} (3 generations).**
- **6 = C_2** total rungs, split 3+3 by parity — three up generations, three down generations, from the parity classes of the C_2 lowest rungs. (A clean structural fact: the generation count per sector = 3 = the odd/even rungs in {0..5}.)
- **Parity = weak isospin T₃:** up (T₃=+½) ↔ even, down (T₃=−½) ↔ odd. The isospin doublet IS the parity doublet of the rung ladder.

## Why this resolves Elie's over-alignment (5148) — the mechanism, exactly
Elie's naive CKM = U_up†U_down collapsed to near-identity (~200× too small) because he put **up and down on the SAME rungs** — so U_up and U_down diagonalize the *same* modes and their product is ≈ identity (the cancellation). **The parity split is the fix:** with up on EVEN and down on ODD rungs, U_up and U_down act on *different-parity subspaces*, so **U_up†U_down is a genuine cross-parity overlap — it does NOT cancel.** The over-alignment was the artifact of a symmetric (same-rung) construction; the physical construction is parity-asymmetric. **The up/down breaking IS the rung parity.**

## The W as the parity flip (consistency check — passes)
The W boson changes weak isospin by one: **ΔT₃ = ±1 ⟺ a parity flip ⟺ Δλ ODD.** So the CKM entries are the **even-up ↔ odd-down** overlaps with Δλ odd — the W naturally connects the two parity classes. Consistency: **Lyra's V_cb = charm (rung 2, even) → bottom (rung 5, odd), Δλ = 3 (odd)** — a parity-flipping W coupling, exactly as the structure requires. ✓ (V_us, V_ub likewise Δλ-odd cross-parity overlaps — the team computes the actual CG coefficients blind.)

## The tie to the oddness engine (why this is deep, if it holds)
The parity split is physical **because n_C = 5 is odd** — the quaternionic spinor / ℤ₃ generation twist distinguishes the two parity classes (an even n_C would not carry the twist that makes the parity a physical isospin). So **the up/down breaking traces to the SAME oddness that forces sin²θ_W = 3/13 (odd N_c) and CP (odd n_C).** If the rung-parity assignment is forced, then quark mixing, the Weinberg angle, and CP all come from one fact: **the integers are odd.** One parity, three sectors — *ex parvo omnia* in its sharpest form.

## Tier + the forcing question (honest)
- **This note is STRUCTURAL** — a corpus connection (the up/down asymmetry = rung parity) that resolves the over-alignment mechanism and ties it to the oddness engine. **NOT banked values** — V_cb and the CKM are still the team's blind SO(5) Clebsch-Gordan computation on the parity-split modes.
- **The forcing question (the open gate, for Lyra/the team, blind):** is the assignment **up = even, down = odd** FORCED by the geometry — i.e., is "weak isospin T₃ = rung parity" a forced identification (from the SO(5) doublet embedding / the discrete-series structure), or is it chosen? Verify the up-even-rung assignment from the geometry FIRST (Lyra's flag), NOT from what breaks the cancellation. If forced → the up/down breaking is Derived-structure and the mixing sector reopens as forced; if it's an identification → the breaking is Structural, the values Candidate.
- **Do NOT fit:** the CG coefficients on the parity-split modes are computed blind; a coefficient chosen to land V_cb banks nothing (Grace's guard, K1322).

— Keeper, K1323, 2026-08-09. STRUCTURAL: the up/down asymmetry breaking the CKM near-cancellation IS the rung parity — the 6=C_2 lowest rungs {0..5} split by parity into up(EVEN{0,2,4}) + down(ODD{1,3,5}), 3 generations each; parity = weak isospin T₃. Resolves Elie's over-alignment (same-rung → cancels; parity split → genuine cross-parity overlap). W = parity flip (ΔT₃=±1 ⟺ Δλ odd); V_cb=charm(2)→bottom(5) Δλ=3 odd ✓. Ties to the oddness engine (odd n_C makes parity physical — same oddness as sin²θ_W + CP). Forcing question (open, blind): is up=even/down=odd forced (isospin=rung-parity)? Verify from geometry first, don't fit. Structural connection, NOT banked values. Cal cold-reads. Nothing pushed.
