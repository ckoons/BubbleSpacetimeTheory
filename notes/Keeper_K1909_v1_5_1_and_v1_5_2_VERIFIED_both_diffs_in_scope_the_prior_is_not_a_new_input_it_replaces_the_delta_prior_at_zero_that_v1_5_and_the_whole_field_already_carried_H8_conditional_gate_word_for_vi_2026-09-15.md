# K1909 — v1.5.1 (353b4535…) and v1.5.2 (8013d959…) VERIFIED from the files; both diffs in their declared scopes under the fixed matcher. The ΛCDM prior is not the first unforced input: v1.5 and every published Ellis–Baldwin test carry a DELTA prior at zero on the clock channel's intrinsic term, and v1.5.2 replaces it with a Gaussian ten times ΛCDM's width. One conditional addition (H8) decided on Elie's numbers, and the gate word for (vi) that does not wait on me.
**Keeper, 2026-09-15 13:01 EDT (clock, substituted). Instruments: `play/keeper_partB_diff_audit.py --scope k1908 | k1909` (negative controls REFUSED, laundering guard in the selftest); `play/keeper_partB_zeta.py`; the files read by hand at §4.3, §6, §9e, §9f.**

## 1. Verification — Grace's trigger: move A9 straight to v1.5.2 (the file (vi)–(vii) run on; v1.5.1 never carried a run)
`shasum -a 256`: v1.5.1 = **353b4535a2b52e15bc6b42b23f6f8537d66a7f5c0e2eac683b3f1746b0b67677** (commit 2f66fb4c); v1.5.2 = **8013d9598d356a0ee0aef614413012c843f8c4d5a581ee2fa032c7c9d2e2e5d5** (commit 668982e0). Both equal Cal's lines and Grace's re-verifications. Diff v1.5 → v1.5.1 under the K1908 (a)–(g) keys: 16 hunks, 0 out of scope. Diff v1.5.1 → v1.5.2 under keys written from §9f BEFORE the diff was read: 14 hunks, 0 out of scope; negative control (a bin re-cut appended to §5's paragraph) REFUSED. Read by hand: §4.3's prior clause, §6's closed list, §8's rescue clause. **PASS on scope, both.**

## 2. K1908 amended (no new number): (a) "four unknowns" → six (b, a as a 3-vector) — Cal §9e note (1), accepted. (b) Cal §9e note (2): the instrument prints +900 km/s for **G < 20.5** and +1065 for **G < 20.0** at A = 0.05; K1908 quoted the G < 20.5 line, the record is unchanged, the two lines are adjacent in the output — a misread, not a discrepancy. (c) K1908 §3's "a few × 10⁻³" for the expected intrinsic amplitude is superseded by Grace's pin: D_cls = 5.9 × 10⁻⁴ at b_Q = 2.45 (Secrest et al. 2021 App. A; Laurent et al. 2017), an order of magnitude lower; the expected-A leak is 4–11 km/s, invisible at this depth.

## 3. The prior, examined against the file (§4.3) and against what the text ALREADY assumed
- **Arithmetic:** σ_c = D_cls/√3 = 5.9e-4/1.732 = 3.4 × 10⁻⁴; σ_A = 10 σ_c = 3.4 × 10⁻³ per component. Checks. Zero-centred is right: the pin is an rms of an isotropic zero-mean vector (Grace 12:52), so "centred on the pin" IS the zero-centred Gaussian at the pin's width.
- **Cal's four conditions, each located in the file:** (1) centre and width from a pre-2023 primary independent of the catalogue — §4.3, Grace's pins; (2) declared before any dipole — the front matter, 12:55, (vi) unrun; (3) cannot by itself create a landing the counts cannot reach — the refusal in §4.3: at ΛCDM's own width σ_β ≈ 99 makes a null sky B-capable, so the width is TEN times, which restores A and creates no B; (4) the no-prior and ΛCDM-width fits posted beside the criterion fit, and a landing that depends on the prior is NAMED "prior-sensitive" by me at (vii) — §4.3's sensitivity line. All four hold.
- **The reframing that dissolves "the first thing entered that is not forced" (Lyra R152 item 5):** v1.5's §4.5 said "no intrinsic term" in the redshift channel. In the linear algebra that is not the absence of a prior; it is a **delta-function prior at zero** on the clock channel's intrinsic column — infinitely tight. Every published Ellis–Baldwin test makes the same assumption (the clustering dipole set to zero or subtracted at its ΛCDM value). **v1.5.2 replaces an implicit infinitely tight prior with an explicit one ten times wider than ΛCDM.** The sequence v1.5 → v1.5.1 (σ_A = ∞) → v1.5.2 (σ_A = 10 σ_c) went from the field's assumption, to no assumption, to a declared weak one — forced at each step by a synthetic control (5764: the leak; 5765: the power). Nothing was tightened relative to what the frozen text and the literature already carried. That is the sentence for the referee and for Casey.
- **In the operator picture (Lyra L2, for the record):** the design matrix's near-null direction is the pair (δb, δa) the two ℓ = 1 moments cannot separate at this depth — Rac-factor moment with profiles f_i and w_i at cos ≈ 0.87, clock-factor moment with (−g, ζ). The prior places a cost on the a-component of that direction and nowhere else; the fit stays linear and the no-prior fit is its σ_A → ∞ limit. A column, not a degree of freedom (Lyra), and now a column with a declared width.

## 4. One conditional addition, decided on numbers before (vi): H8, the no-prior â against the prior
§6 closes the list "after a real dipole is seen" — none has been. The risk the prior introduces is the one it cannot police itself: a true P1 on a sky whose intrinsic dipole is well beyond ΛCDM (Secrest 2022's reported excess is ~13–40× the pin) is pulled toward a = 0 and the residual is pushed into b̂ — a FALSE B. §4.3's sensitivity line reports that; it does not stop it, because B needs every hatch to pass and no hatch reads â. **Rule: if Elie's v1.5.2 fractions show P(B | P1 true, a = 0.008 or 0.024) > 0.05, Cal adds H8 before (vi) as v1.5.3 with a one-line scope — "the no-prior fit's â is consistent with the prior: |â|²/(σ_A² + σ_â²) ≤ 11.34 (3 d.o.f., 99 %)"; PASS required for B, reported for A. If the rate is ≤ 0.05, H8 is not added and this section records why.** A hatch added on a computed false-fire rate before any dipole is a control-forced addition, the same species as every freeze today.

## 5. Gate word for (vi) — conditioned on numbers, so nobody waits on me again
(vi) runs on v1.5.2's hash (or v1.5.3's if H8 is added) the moment ALL of: (a) Elie's fractions are posted — P1 true at a = 0, 6 × 10⁻⁴, 3.4 × 10⁻³, 0.008, 0.024; null at a = 0 and 6 × 10⁻⁴; the σ_c-width and no-prior fits beside each; the H7 band under v1.5.2; (b) A | P1 true at a = ΛCDM ≥ 0.40 (else the text is C by construction again and Cal says so in §1 — that is also an honest end for Quaia); (c) B | P1 true ≤ 0.05 at every arm up to 10× ΛCDM; (d) the design σ_β posted under (l). The CMB target stays withheld; the run's output carries no comparison; I run (vii) by `keeper_partB_landing.py` in v1.5.1-draft mode promoted to v1.5.2 with ζ, â and the three fits read from the run.

## 6. Owed by me now: the two Lyra seams (Lecture 9 overstated the freeze count by one; "can confirm, cannot kill" was v1.5 §5 alone) — fixed in this commit to v1.5.2's honest state; the state block line with them; both synced.
— Keeper. K1909. Counter next: K1910.
