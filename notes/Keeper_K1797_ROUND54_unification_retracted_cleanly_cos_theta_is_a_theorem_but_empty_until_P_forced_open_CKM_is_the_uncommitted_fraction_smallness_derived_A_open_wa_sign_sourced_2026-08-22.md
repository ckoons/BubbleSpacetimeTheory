# K1797 — ROUND 54: the unification retracted cleanly; the open CKM is the un-committed fraction; smallness DERIVED, Wolfenstein A open; w(a) sign SOURCED

**Filed:** 2026-08-22
**Reports ruled:** Grace (retracted her own R53 unification lead, 3 ways — Elie should not compute), Elie (held; the projector-collapse reading + frozen band pre-registration), Lyra (F799 sign sourced from primary; the commit-idempotent clean form; the honest type-gap), Cal §694 (cos θ verified a theorem 3/3; the ordering-bit + the projector gift; ~12 operator pairs).
**Headline: the mechanism (Casey's ordered product) is CONFIRMED a theorem (Cal 3/3), but it is definitionally EMPTY until P is forced — and Grace's own unification lead (flavor P = QM commit) FAILS on type. What survives is sharp and honest: the open CKM sector = the fraction of the condensate that does NOT commit (‖(1−P)χ‖²≈0.0017); its SMALLNESS is already DERIVED (λ²=1/20); the only open piece is the O(1) Wolfenstein A≈0.81. And w(a)'s sign is now SOURCED from primary (w_a>0), not relayed.**

---

## 1. ★ The unification lead RETRACTED — cleanly, by its own author. (Grace)

Grace proposed "flavor P = the QM commit operator P_record⊕P_encode" in R53 and killed it in R54, three ways — the Quaker method working on oneself:
- **Definitionally empty (not "unbanked"):** given any mass directions (a,c), (χ,P) reproducing them always exist (2000/2000; set χ=c, P carries c→a, formula = |⟨a|c⟩| identically). So cos θ "banks nothing until χ,P named" sharpens to **it is a reparameterization awaiting content, not a result awaiting confirmation** — 10 free parameters for one number.
- **Projector can't predict θ:** if P idempotent, generic sin²θ ∈ {1, 0.67, 0.33, 0} vs target 0.00168 → the reading requires χ ~99.83% inside the commit subspace, relocating the question without answering it.
- **Fails on TYPE (before any number):** the QM commit acts on the **Jordan algebra** (arrow-line ⊕ V₁₂, indexed rank=2 and n_C−2=3); the CKM needs an operator on **generation space** (3 from the Q⁵ truncation ℤ[h]/h⁶). Different spaces, no map between them in the corpus. The identification rested on **three distinct 3's** (dim V₁₂, N_c, generation count).

**Provenance catch (yours, and it earned its keep):** the board relayed "P_encode … d=2" — but d=2 in the source is the **code minimum distance, not a dimension** (dim V₁₂=3). Reading the source corrected two different wrong conclusions from two wrong numbers. This validates "provenance, not just the tier" — the same discipline that caught my own T2198 anchor-miss.

This is a productive walk-back — it removes a speculative extension Grace bolted onto her own criterion-C closure (which is **untouched**). Bank it as a FLOOR, not a loss.

## 2. ★★ What SURVIVES is sharp — and it's a beautiful physical reading. (Cal + Elie + Lyra, converged)

- **cos θ = |⟨χ|P|χ⟩|/‖P|χ⟩‖ is a THEOREM** (Cal 3/3 on random non-normal P; M_up M_up† and M_down M_down† are rank-1 with heavy directions P|χ⟩/‖Pχ‖ and |χ⟩, the formula is the angle between them). The chain is **POSIT → THEOREM → NAMING → NUMBER**; the theorem is the only choice-free step. "Up/down = the two orderings of the same two operators" is a POSIT (good, commit→emit-grounded, not a derivation).
- **The projector gift (Cal + Elie + Lyra, all three):** IF P is an orthogonal projection, cos θ = ‖Pχ‖, **sin²θ = ‖(1−P)χ‖²**, AND the ordering-bit (Cal: worth a factor 2 for non-self-adjoint P, 0.470→0.883) becomes **harmless**. ⟹ **the entire open CKM sector = the fraction of the condensate that does NOT commit.** The condensate is 99.83% inside the commit subspace; the mixing is the leftover the commit fails to capture. This is worth having even if the number misses — it is a physical reading, target-innocent and ordering-safe, IF P is a forced projection.

## 3. ★★★ RECONCILING Cal and Grace (they are slightly out of sync — this is mine to resolve).
Cal §694: "check first that P_record⊕P_encode is an orthogonal projection — everything rests on it." Grace guard-3 (same round): P_record⊕P_encode acts on the **wrong space** (Jordan algebra, not generation space). **Both are right, and combined they sharpen the forward object:** the projection PROPERTY is what makes the reading clean (Cal), but the flavor P is **NOT** the QM commit (Grace, type) — so **the forward question is: is there a FORCED orthogonal projection ON GENERATION SPACE (the Q⁵ parity-fold ℤ[h]/h⁶, Lyra's even/odd shelves) whose un-committed fraction of the condensate O ≈ 0.0017?** Do NOT spend a round checking whether the QM commit is a projection for the flavor sector — it's the wrong space regardless. (The projection-check on P_record⊕P_encode still matters for the QM sector's own cleanliness, separately.)

## 4. ★★ The Wolfenstein reframe — the honest bounded state. (Grace)
sin θ ≈ |V_cb| = **A·λ²**, with **λ² = 1/20 DERIVED** (T2530) and **A ≈ 0.81 underived** (an O(1) Wolfenstein coefficient). Two consequences:
- **The SMALLNESS of the CKM angle is already derived** — it is the λ²=1/20 Cabibbo hierarchy, not a separate puzzle. "Why is θ≈2.4° so small" = "why is λ²=1/20 small" = already partly held.
- **The only genuinely open number is the O(1) coefficient A** (and the CP phase). So the honest tier of the CKM mixing sector is **Partially Derived: structure (near-identity, rank-1) + λ=1/√20 DERIVED; A≈0.81 underived.** That is a defensible, referee-clean statement — and stronger than chasing "derive 2.4°," because it names exactly what is and isn't forced.

The forced-generation-projection hunt (Sec 3) is the PROMOTION target: if the un-committed fraction of O under a forced Q⁵-parity projection = 0.0017, then A is derived and the sector closes. If not, A is an honest input and the sector is Partially Derived. Both outcomes ship.

## 5. ★ w(a) — sign SOURCED from primary, the a⁶ confirmed a sign-flip. (Lyra)
F799 is NOT a power law — it is a **spectral bleed in substrate time τ: ρ_Λ(τ) = c₀ + Σ c_k e^{−λ_k τ}**, leading term e^{−6τ} (rate λ₁=C₂=6). τ increases as the universe expands → maps to a **negative** power of a → **a⁻⁶ → w_a > 0**. Positive-weight bleed can only ease down to the floor, never dip below and climb back → relaxes to −1 from above, w_a>0, robust 8/9. **The table's a⁺⁶ is a confirmed exponent-sign flip** (would give DESI's w_a<0) → fix forward in the curated layer. Sign is now sourced, not relayed (three statements, finally a quotation). BST is on the wrong side of the CPL headline (w_a>0 vs DESI w_a<0) — but the no-crossing SHAPE survives both direct probes (~1.3–1.6σ, not fired; K1791 recalibration stands). Elie may now run the forcing against the SOURCED spectral-bleed form. The §4751 shape test still isn't on disk; C3/C6/C7 remain the bar for it.

## 6. Pre-registration discipline banked (Elie + Cal converged): the two-scale-spread veto.
Elie froze the band boundary at 2.3450° (below=exclusive, above=inclusive), reported both scales — AND added the veto Cal independently demanded: **the two sides differ by 0.170° (7.25%); if BST's own two-scale spread exceeds that, report "cannot adjudicate," name no side.** A 7% controversy cannot be settled by a computation with a larger internal spread. Run the spread FIRST. This is correct discipline; ratified.

## Dispositions
- **Unification (flavor P = QM commit): RETRACTED (Grace, type mismatch).** Clean walk-back → FLOOR. Criterion-C closure untouched.
- **cos θ: THEOREM (Cal 3/3), empty until P forced. The POSIT (M_up=P·C, M_down=C·P) is commit→emit-grounded, not derived — state it.**
- **Open CKM = ‖(1−P)χ‖² (the un-committed fraction), IF P is a forced GENERATION-SPACE orthogonal projection (NOT the QM commit — wrong space).** Forward: is the Q⁵ parity-fold that projection, giving ≈0.0017? (Grace+Lyra.)
- **Honest current tier: CKM mixing = Partially Derived — structure + λ=1/√20 derived; Wolfenstein A≈0.81 open.** Ships as-is; the forced-projection is the promotion.
- **w(a): sign SOURCED (w_a>0, spectral bleed e^{−6τ}); table a⁺⁶ = fix forward; falsifier live-but-mild (not fired).**
- **Band veto ratified** (2-scale spread > 0.170° → cannot adjudicate).
- **Curation (EOD):** fold the un-committed-fraction reading + Wolfenstein-A honest tier + the w(a) exponent fix + T2198/T2259 tier fixes into Guide/Curriculum, forward.
- Nothing external. Nothing pushed.

— Keeper, K1797. The unification died on type; the mechanism is a theorem; the open sector is one physical fraction whose smallness we already derive. Each round sharpened the question — that is the method, not a detour.
