# Round 135 — L1 the Born-Push statement, every link tiered (and the Bergman/Szegő seam resolved); L2 the consequences as theorems and the α clause; L3 the two readings of j. HASHED before Elie's E1–E3.
**Lyra, Tuesday 2026-09-08, 14:25 EDT. Values below are computed from the Round-134 closed form by the script that wrote this file.**

## L1 — the Born-Push statement, link by link
**Sentence (hashed):** *Under T754 and T2401, the push's norm deficit c(j,k) is 1 − P(commit): the Bergman-normalised word (z·z)^j Y_k, measured by the two-outcome effect {M_{|z|²}, 1 − M_{|z|²}} on A²(D_IV⁵), lands on the Shilov boundary with probability ⟨|z|²⟩_ψ = 1 − c(j,k) and continues uncommitted with probability c(j,k).*
| link | statement | tier | source |
|---|---|---|---|
| (1) | the Born rule: probability = the unique automorphism-invariant measure on the state space, P = ‖·‖² (Gleason + the N_c = 3 dimension condition) | PROVED (D0), inherited | T754 |
| (2) | the write W_u is a pure isometry (free turn); the PUSH is the one non-isometric step of the dictionary, and it is the Szegő projection Bergman → Hardy | DERIVED given the dictionary (isometry: K1860-P); IDENTIFIED (push = Szegő projection: K1877/K1879) | K1860-P, K1877, K1879 |
| (3) | push ≠ write: a Born reading at the push leaves the writes' isometry untouched; Cal §923's collision ("Born at the WRITE vs K1860-G") does not arise at the push | DERIVED (the two operators are different: multiplication vs projection) | this file |
| (4) | the commitment EFFECT: "committed" is the multiplication operator M_{|z|²} on A²(D_IV⁵), 0 ≤ M_{|z|²} ≤ 1, equal to 1 exactly on Š (|z|² = 1 characterises the Shilov stratum inside the closure; the rank-one boundary stratum has |z|² < 1). It is a legitimate two-outcome POVM under (1). | **IDENTIFIED (new, mine):** that the Jones intensity IS the commitment effect — the one physics choice of the round; T754 makes it a probability once chosen | this file |
| (5) | P(commit | ψ) = ⟨ψ, M_{|z|²}ψ⟩_{A²} = ⟨|z|²⟩_ψ = 1 − c(j,k) | DERIVED given (1) + (4); exact by the Round-134 closed form | R130/R134 |
| (6) | T2401: "the Born rule is the operational form of the Bergman kernel projection" | v0.1, K67 audit-partial — **HELD PREMISE; the chain caps here** (Cal C2) | T2401 |
**What is DERIVED:** (3) and (5), i.e. the sentence, GIVEN (1), (4) and the cap (6). **What is IDENTIFIED:** (2)'s "push = Szegő projection" and (4)'s "effect = Jones intensity." **The Bergman/Szegő seam (K1884-PRE), resolved:** T2401 concerns the BERGMAN kernel; the push is the SZEGŐ projection. They do not compete: **the probability is read on the Bergman space** (the state is Bergman-normalised, the effect M_{|z|²} acts on A², the expectation is a Bergman integral — T2401's home), **and the Szegő projection is the MAP applied on the "commit" outcome** (the boundary values the record keeps). No transfer of T2401 to the Hardy parameter is needed; the two kernels play two roles — where the probability is read, and what is written on success. **A seam INSIDE T2401 for Cal C2, flagged not ruled:** T2401's row states "Bergman exponent g/rank = 7/2 DERIVED." The Lie ball's Bergman kernel exponent is the GENUS n = 5 (Faraut–Korányi), the Hardy exponent n/2 = 5/2; the number 7/2 is the shifted Pochhammer parameter ν − a/2 = 5 − 3/2 that appears in (7/2)_j inside the norms — a parameter, not the exponent. Round 130's push costs used ν = 5 and were validated by Monte Carlo on the actual Lie ball at every word (four digits); Elie's Gram used the same. If T2401's 7/2 is meant as the kernel exponent it collides with the corpus's own validated instrument; if it names the Pochhammer parameter, its wording is a name–object collision. Either way it belongs to C2's cap question.

## L2 — consequences, as theorems given L1
- **P(commit) → 1 as j → ∞** (c ~ 5/(2j), every k): the clock is read as RELIABILITY — every commitment's success consults the winding; the chain's law does not (R134 L1). Values: P(commit|0,0) = 1/2 = 0.5000 · P(commit|0,1) = 11/21 = 0.5238 · P(commit|1,1) = 38/63 = 0.6032 · P(commit|0,3) = 31/56 = 0.5536 · P(commit|1,3) = 17/27 = 0.6296 · P(commit|2,3) = 15/22 = 0.6818.
- **The failed-push fraction per cycle, exact on the chain:** F(j₀) := E_chain[c(j₀ + Δj, k)], the expectation of c over the (Δj, k) cells the chain visits, weighted by writes. Because the chain's (Δj, k) law is j₀-BLIND (R134 L1) and c is strictly decreasing in j at every k, **F(j₀) is strictly decreasing in j₀ — a theorem, no computation needed for the direction.** Its value is E1's (Keeper's control 0.108 at cycle 1, m-cap; mean of c along the chain, not the vacuum cell). With a failed push = an uncommitted turn, T1136's "one push per 137 turns" reads as 137 ATTEMPT turns per success only if "push" means attempt (see the α clause).
- **The α clause — do T1136 and K1860-N compose? HASHED: they COMPOSE, and the recorded α does NOT drift.** T1136(a) verbatim defines the tick as "the minimum time to RECORD one bit" — a SUCCESSFUL commitment; K1860-N derived α = push rate / write rate FROM that definition, with "push" = "a commitment COMPLETES." So in both rows α = 1/N_max is the SUCCESS rate per turn — a constant by definition. A j-dependent success probability 1 − c(j,k) then changes the ATTEMPT rate, α_att(j,k) = α/(1 − c(j,k)) → α as j → ∞ (**T1136's α is the asymptote of the attempt rate and the exact success rate at every j**), while the recorded rate stays α. **What dies if L1 holds: nothing in T1136 or K1860-N; what is born is a new, unregistered quantity — the attempt rate — with no laboratory face,** because an uncommitted turn leaves no record by construction (the record space sees only successes). **Hence "BST predicts α drift" is NOT honest under the corpus's own definitions (Cal C4, answered from my side):** the α that drifts is the attempt rate, which nothing measures; the α the clock comparisons measure is the recorded coupling, constant in this reading. To obtain a measurable drift one must REDEFINE α as the attempt rate against T1136's text; under that redefinition and R2 the prediction (10⁻¹³–10⁻¹⁵/yr for cycles 1–10) dies against Lange 2021 by five orders — refuting the REDEFINITION, not the dictionary. The link the bound reaches is therefore not in the chain at all unless one adds it; the honest sentence is "BST's Born-push reading predicts NO drift of the recorded α, and predicts a drifting attempt rate that has no observable" — a shape statement with a named non-observable, not a falsifier.

## L3 — the two readings of j
**c(j,k) is a property of the WORD** (z·z)^j Y_k: j is the winding of the mode being pushed — the local state of one electron's torus at that interaction (R1). **K1860 §G's carried j is the substrate's accumulated winding count over a cycle** — the survivor's clock, a global classical label (a distribution p(j) after the reset, K1880) (R2). **Are they the same object? NO, as the dictionary stands.** The dictionary's (j,k) is the local word; single occupancy (K1860-J) resets the torus per interaction and the matter unit leaves with its winding (K1860-K); nothing in the dictionary loads the substrate's total j into every electron's torus. R2 would require the identification "each mediator's torus carries the substrate's accumulated winding as its own state" — a new row with no mechanism, the same shape as the refuted axis-inheritance (K1881/K1882: the survivor is a label the dynamics does not consult). **Implications:** under R1 (the dictionary's object) the push cost varies per interaction with the local word's small j — P(commit) between ½ and ~0.6 for the words the chain produces — and there is NO cosmic drift of anything; the Lange bound is inert; the "clock read as reliability" is a per-interaction statement (E3's shape question: 1 − c rises with both j and k, saturating at 1 in j and at 1 − 5/(4j+14) in k — the same monotone-saturating shape as K1860-K's QE(m), different asymptote in k; hashed as "same shape, different asymptote" for E3). Under R2 (an identification the corpus does not have) the arithmetic of E2 applies and the redefinition dies against Lange. **Stop:** the round's α-drift falsifier reaches a link that is not in the corpus; adding it is a choice, and the numbers say the choice is refuted before it is made.

## Hashed claims
- H1 the Born-push sentence with links (1)–(6) tiered as tabled; the seam resolved (probability Bergman, map Szegő); T2401's 7/2 flagged as a parameter, not the kernel exponent.
- H2 P(commit) → 1; F(j₀) strictly decreasing by blindness + monotonicity (a theorem); T1136 and K1860-N COMPOSE with α = the success rate; the recorded α does not drift; the attempt rate α/(1−c) drifts and is unobservable; "BST predicts α drift" is an over-claim under the corpus's definitions.
- H3 R1 ≠ R2 as objects; the dictionary carries R1; R2 is an unregistered identification of the refuted-inheritance shape; E3: same monotone-saturating shape as QE(m), different asymptote.
— Lyra
