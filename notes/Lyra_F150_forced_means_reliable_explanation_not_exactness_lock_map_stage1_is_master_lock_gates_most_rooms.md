---
title: "F150 — Casey's criterion (resolves F149-D) + the lock map (Casey: 'look at the locks to the most rooms'). CRITERION: physics is observation + mathematics; 'forced' = a RELIABLE MATHEMATICAL EXPLANATION OF THE MEASUREMENT, NOT exactness. This resolves F149-D decisively: the discipline (blind, forbid-alternative, weld-not-fit, no form-selection) stays ON THE FORM; what's dropped is the requirement that the VALUE be exact. So a Tier-2 quantity with a FORCED FORM (blind-disciplined) explaining the observation AT/BELOW its structural floor COUNTS. Direct consequence: the MUON is promotable -- its FORM is forced (64 rigorous, 8pi^2/3 verified, exp 6 forced+falsifiable, ÷vol F138, product-form F143) and it explains 206.768 at 3x10^-5 (below the ~10^-4 two-tier floor). NOT a bar-lowering: exactness was never the right bar for a physical theory; the form-discipline is unchanged. LOCK MAP (rooms each lock opens, of the 26): (1) STAGE-1 = the K-type orbit->nu address map = MASTER LOCK -- directly gates 3 charged-lepton masses + (via M_nu) 3 nu masses + 4 PMNS = ~10, AND is the prerequisite for the quark mass-addresses behind #418; (2) #418 bulk-color/chiral content gates 6 quark masses + 4 CKM = 10; (3) running/RG structure gates sin^2theta_W + alpha_s = 2 (honest-negative sector); (4) Higgs vev (ell_B framework) + m_H (open) = 2. STAGE-1 IS THE LOCK TO THE MOST ROOMS. STAGE-1 ATTACK (continuum side, new): reformulate the opaque 'orbit->nu' into TWO cleaner sub-maps -- (a) orbit-TYPE -> locus-TYPE -> lepton-TYPE (fixed-0->vertex->tau, fixed-1->strip->electron, generic->sphere->muon; F141), composed with (b) strata/depth -> generation/nu within a type (F86 rank+1 strata + F139 depth-ordering). KEY CONSTRAINT: the 18 generic Frobenius orbits are MULTIPLICATIVELY DEGENERATE (127 prime => every non-{0,1} element has order exactly 127), so the orbit does NOT encode 18 distinct masses -- it sets TYPE; the GENERATION comes from the strata index, not from orbit-selection. That sharpens the wall: stage-1 = (orbit-TYPE->locus-TYPE, near-done) o (strata->generation, structural), not a 20-way opaque assignment. FLAGGED LEAD (tier hard, NOT banked, Cal #27): the moved cells number 2^g-2 = 126 = 18x7 = dim of the SO(10) 126-rep, which in GUT model-building is EXACTLY the Higgs that breaks B-L / gives Majorana neutrino masses -- right where this session's F144/F147 work sits; could be the substrate's B-L-breaking structure (a mechanism for F147's needed breaking) OR confirmation-bias on a known recurring BST 126 (Wallach/Family-Q=126/magic-126); connect to the EXISTING 126 web, examine, do NOT crown. TIER: criterion = Casey PI ruling (resolves D); lock map = structural/forced; stage-1 reformulation = real continuum-side sharpening; 126 = flagged lead. Count: if the chain ratifies the criterion + convenes the muon gate, count -> 3 (muon); else honestly 2; muon yellow pending."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-15 Monday 21:08 EDT"
status: "v0.1 — Casey criterion + lock map. CRITERION (resolves F149-D): 'forced' = reliable mathematical EXPLANATION of the measurement, NOT exactness; discipline stays on the FORM (blind/forbid-alternative), value need not be exact. => MUON promotable (form forced, explains 206.768 at 3x10^-5 below floor); not a bar-lowering (exactness was never the right bar). LOCK MAP: (1) STAGE-1 K-type orbit->nu address = MASTER LOCK (~10 rooms: 3 charged-lepton + 3 nu via M_nu + 4 PMNS; + prerequisite for quark addresses); (2) #418 bulk-color = 10 (6 quark + 4 CKM); (3) running = 2 (sin2thW, alpha_s); (4) Higgs = 2. STAGE-1 = most rooms. ATTACK: reformulate orbit->nu = (orbit-TYPE->locus-TYPE->lepton-TYPE, F141) o (strata->generation, F86/F139); KEY: 18 generic orbits MULTIPLICATIVELY DEGENERATE (127 prime) => orbit sets TYPE not mass, generation from strata; sharpens wall. LEAD (tier hard): 2^g-2=126=18x7=dim SO(10) 126-rep = the Majorana/B-L-breaking Higgs, right at F144/F147; examine vs existing-126-web, do NOT crown. Count -> 3 if chain ratifies criterion+muon gate; else 2; muon yellow."
---

# F150 — "Forced" means a reliable explanation, not exactness; and the lock to the most rooms is stage-1

Casey: "Physics isn't an exact science — it's observation combined with mathematics; we need a reliable explanation for our measurements; I doubt exactness. Agree on mixing angles. Let's look at the locks to the most rooms."

## The criterion (resolves F149-D)

**"Forced" = a reliable mathematical explanation of the measurement — not exactness.** This is the PI ruling F149-D asked for, and it's decisive. The reconciliation with the discipline:

- The discipline (blind-to-target, forbid-the-alternative, weld-not-fit, no form-selection) **stays entirely on the FORM**. That's what keeps a forced result from being a fit.
- What's dropped is the requirement that the **VALUE be exact**. A physical theory explains a measurement; it does not need the measurement to be a clean integer.

So a Tier-2 (floored) quantity whose **form is forced** (survives the blind/forbidding discipline) and which **explains the observation at or below its structural floor** is a *reliable explanation* — it **counts**. This is **not** a bar-lowering: exactness was never the right bar for a physical theory, and the form-discipline is unchanged.

**Direct consequence — the muon is promotable.** Its form is forced (64 rigorous, `8π²/3` verified, exponent 6 forced + falsifiable, `÷vol` forced by homogeneity F138, product-form forced by the compact locus F143), and it explains `m_μ/m_e = 206.768` at **3×10⁻⁵** — below the ~10⁻⁴ two-tier floor. Under the criterion, that's a reliable explanation. The muon gate (F149-A) should now rule, and I expect **count → 3**.

## The lock map — rooms each lock opens (of the 26)

| Lock | Rooms it opens | Count |
|---|---|---|
| **STAGE-1** — the K-type orbit→ν address map | 3 charged-lepton masses + (via `M_ν`) 3 ν masses + 4 PMNS; **and** the prerequisite for the quark mass-addresses | **~10 (+ gates #418's payoff)** |
| **#418** — bulk-color / chiral content | 6 quark masses + 4 CKM | 10 |
| **running / RG structure** | `sin²θ_W`, `α_s` | 2 (the honest-negative sector) |
| **Higgs** | vev (`ℓ_B` framework anchor) + `m_H` (open) | 2 |

**Stage-1 is the lock to the most rooms** — it directly gates the entire lepton sector (~10 parameters, including the exact-eligible PMNS angles you agreed to prioritize) *and* is the prerequisite for the quark addresses behind #418. Pick that lock.

## Attacking stage-1 (continuum side — a real sharpening)

The opaque "orbit → ν" map factors into **two cleaner sub-maps**:
- **(a) orbit-TYPE → locus-TYPE → lepton-TYPE** (F141): fixed-point `0` → vertex → tau; fixed-point `1` → strip → electron; generic orbit → sphere `S⁴` → muon. Near-done.
- **(b) strata / depth → generation / ν within a type**: F86 (`rank+1 = 3` boundary strata) + F139 (depth-ordering).

**Key constraint that sharpens the wall:** the 18 generic Frobenius orbits are **multiplicatively degenerate** — since `2^g − 1 = 127` is **prime**, every non-`{0,1}` element has order *exactly* 127, so the orbit does **not** encode 18 distinct masses. The orbit sets **TYPE**; the **generation** comes from the strata index, not from picking among orbits. So stage-1 is **not** a 20-way opaque assignment — it's (orbit-TYPE→locus-TYPE, near-done) **∘** (strata→generation, structural). That's a much smaller remaining gap, and it's the joint Lyra+Elie target.

## Flagged lead (tier hard — NOT banked, Cal #27)

The number of Frobenius-**moved** cells is `2^g − 2 = 126 = 18 × 7` — and `126 = dim` of the **SO(10) 126-rep**, which in GUT model-building is *exactly* the Higgs that breaks `B−L` and gives Majorana neutrino masses. That sits **right on top of** this session's F144/F147 work. Two readings, and I am not crowning either:
- **Signal:** the substrate's `B−L`-breaking structure (the mechanism F147 needs) *is* the 126 = moved-cells, tying F147's resolution to a concrete rep.
- **Confirmation bias:** 126 is a known recurring BST number (Wallach, Family-Q=126, nuclear magic-126), so seeing it here may be the same number, not a new link.

**Action:** connect to the existing 126-web, examine whether the SO(10)-126-as-`B−L`-Higgs reading is the substrate's breaking structure, **do not bank**.

## Tier and count

- **Criterion** = Casey PI ruling (resolves F149-D); the form-discipline is unchanged.
- **Lock map** = structural.
- **Stage-1 reformulation** = real continuum-side sharpening (orbit sets type, strata set generation).
- **126** = flagged lead.
- **Count:** if the chain ratifies the criterion and convenes the muon gate, **→ 3** (muon). Until then, honestly **2**; muon yellow pending.

@Keeper / @Cal — Casey has ruled F149-D: "forced = reliable mathematical explanation, not exactness; discipline stays on the FORM." Please (1) convene the **muon promotion gate** under this criterion (I expect → 3), and (2) record the criterion in the methodology stack (it changes what "forced" means for every Tier-2 quantity).
@Elie — the master lock is **stage-1**, and it factors: orbit sets TYPE (your fixed-point/generic split), strata set GENERATION (F86/F139). The 18 generic orbits are multiplicatively degenerate (127 prime), so we are NOT looking for "which of 18 is the muon" — that's the sharpening. Also flagging `2^g−2 = 126 =` dim(SO(10) 126-rep, the Majorana/B−L Higgs) as a lead to examine, not bank.
@Grace — the muon should clear under Casey's criterion (form forced, value below floor); and the mixing angles (your exact-eligible sector) are gated on `M_ν`, which is behind stage-1 — so stage-1 is the lock for your sector too.

— Lyra, Mon 2026-06-15 21:08 EDT (date-verified). F150: Casey criterion 'forced = reliable explanation NOT exactness' (resolves F149-D; discipline stays on FORM, value need not be exact) => MUON promotable (form forced, explains 206.768 at 3x10^-5 below floor). LOCK MAP: STAGE-1 (K-type orbit->nu) = MASTER LOCK ~10 rooms (3 charged-lepton + 3 nu via M_nu + 4 PMNS + prereq for quark addresses); #418 = 10 (6 quark + 4 CKM); running = 2; Higgs = 2. STAGE-1 ATTACK: orbit->nu = (orbit-TYPE->locus-TYPE, F141) o (strata->generation, F86/F139); 18 generic orbits MULTIPLICATIVELY DEGENERATE (127 prime) => orbit sets TYPE, strata set GENERATION; sharpens wall. LEAD (tier hard): 2^g-2=126=dim SO(10) 126-rep = Majorana/B-L Higgs at F144/F147; examine vs existing-126-web, don't crown. Count -> 3 if chain ratifies criterion + muon gate; else 2.
