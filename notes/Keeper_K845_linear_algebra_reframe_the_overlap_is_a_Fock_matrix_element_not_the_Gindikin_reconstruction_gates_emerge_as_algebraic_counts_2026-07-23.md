# K845 — Casey steer "linear algebra, one D_IV⁵ domain": run the overlap as a **Fock/oscillator MATRIX ELEMENT** ⟨f_n|O|f_m⟩ from the ladder algebra [a,a†]=1 — NOT the reference-gated Gindikin/Bergman heavy machinery Lyra was about to reconstruct. This is the standing "construct rep theory as linear algebra" directive applied to the one computation; the gates (exponent C₂, integers 24/71, √π) EMERGE as algebraic counts.

**Keeper | 2026-07-23 | Casey course-correction relay + the linear-algebra setup. Plain.**

## The steer
Lyra flagged the overlap as "a substantial dive to reconstruct the K264 Bergman/Gindikin machinery." **Casey: don't.** The overlap is a MATRIX ELEMENT in the oscillator/Fock realization of D_IV⁵ (the spin-factor/Jordan structure; a†=commit, Fock#=winding, coherent states=localizations). Compute it from the ladder algebra, one domain — the Gindikin gamma is the *output*, not the machinery you reconstruct.

## The linear-algebra form (the one computation, recast)
- **The mass = a Fock matrix element:** m_n ∝ ⟨f_n | O | f_n⟩, where f_n is the stratum-n state (lowest weight at the ρ-vector position {5/2, 3/2, 0}, T2517) and O is the condensate (SO(5) vector, weight-1). Mixings = cross matrix elements ⟨f_n | O | f_m⟩. Neutrino scale = the boundary (position-0) self-matrix-element.
- **The gates become COUNTS (should emerge from the algebra):**
  - **Exponent C₂ = n_C + 1:** Lyra's F662 in ladder terms — the two mode-factors carry the overlap power n_C (the N^{n_C/2} form = the ladder-normalization power), and the weight-1 condensate O adds +1 → C₂. **A power-count of ladder factors, not a Gindikin exponent.**
  - **√π:** the half-integer positions {5/2, 3/2} are half-integer Fock weights → the Gaussian/ladder normalization √(Γ(half-int)) carries √π (= Γ(1/2)). **The √π is the half-integer-weight normalization** (matches F157's characterization, but here it's the oscillator measure, not a reconstructed cone gamma).
  - **The integers 24, 71:** dimension / Weyl-orbit counts of the relevant Fock subspaces (24 = N_c·|W(B₂)|; 71 from the boundary-residue count). **Should be combinatorial counts of the ladder states, not fitted.**
- **This is FASTER and MORE TRANSPARENT:** the gates are algebraic (powers, half-integer weights, dimension counts), so "does 24/π² emerge / does 71 emerge" becomes a count you can do explicitly, not a special-function reconstruction.

## Why this is the right move (discipline)
- Avoids the reference-gated machinery (Wallach/Plancherel/Gindikin) that has burned the team before (the genus-flip trap; the K264 reconstruction risk = the 4781-class error).
- Makes the gates emerge as **target-innocent COUNTS** — the cleanest form of "derived, not fitted."
- Applies the standing linearization directive (2026-07-08 "construct rep theory as linear algebra"; the standing "linearize every area").

## Directions (recast the one computation)
- **★ LYRA (primary):** run the overlap as Fock matrix elements ⟨f_n|O|f_m⟩ from the ladder algebra — NOT the Gindikin reconstruction. The exponent C₂ = ladder-power-count (n_C + condensate-weight); √π = half-integer-weight normalization; the integers = state-count. Land the masses, all 6 mixing angles, the neutrino self-energy.
- **★ GRACE:** the descent gives the Fock positions/weights (d=4 forced); feed them; the integers 24/71 as ladder-subspace dimension counts (blind ledger holds).
- **ELIE:** the pre-staged cross-check fires on the matrix-element outputs (24/π², 71-unique); glueball K-types as Fock states.
- **KEEPER/CAL:** the gates are now COUNTS — audit whether C₂, 24, 71, √π emerge as algebraic counts (cleanest target-innocence); down-ladder with the scale-caveat (K844 addendum).

— Keeper K845, 2026-07-23. Casey steer: run the overlap as a Fock/oscillator MATRIX ELEMENT ⟨f_n|O|f_m⟩ (ladder algebra [a,a†]=1), NOT the Gindikin/Bergman reconstruction. The gates emerge as algebraic COUNTS: exponent C₂=n_C+1 (ladder-power + condensate-weight), √π = half-integer-weight normalization, 24/71 = state-counts. Faster, transparent, target-innocent, avoids the reference-gated trap. Standing "construct rep theory as linear algebra" applied to the one computation. See [[Keeper_K844_correction_descent_gives_POSITIONS_overlap_gives_MASS_linear_casimir_mass_is_defective_K663_overlap_is_super_linear_right_shape_down_ladder_bonus_lead_2026-07-23]], Lyra F662 (exponent mechanism), F157 (√π), the standing linearization directive.
