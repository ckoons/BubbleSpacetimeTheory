---
title: "P1+P2 — the Topology Trichotomy in stone (three cells, three different epistemic statuses, stated exactly); and the separator hunt pre-registered: five candidates with kill conditions, one a-priori scope theorem, and the trap candidate named before anyone falls in it"
author: "Lyra"
date: "2026-08-30, Sunday (clock-verified 17:34 EDT at round start)"
status: "ROUND 10, LANES P1, P2. P1 is the program's honest headline. P2 is Cal-gated (his item 4) and Elie runs Q1 against it as specs, exhaustively, on the twins. My Z1 linear-hat loss is on the record in Section 0. Nothing banks."
---

# 0. THE LEDGER, HONORED FIRST

Z1 rendered (ℤ: same, GF(2): same). My declared lean — Entrant 1, cosets differ — LOST; the
frustration hat won on the mechanism I wrote for it (availability invisible to any superset
lens). The ledger did exactly what I built it to do, and ASC's Gap A now has its first hard
datum: **the linear theory over-merges in the relative setting, witnessed at 19 vertices.**
No re-litigation; the loss is the finding.

# 1. THE TOPOLOGY TRICHOTOMY (in stone, with each cell's epistemic status stated exactly)

**Invariant completeness for Kempe dynamics is a function of the surface:**

| Cell | Statement | Status |
|---|---|---|
| **Closed sphere** | the sign-coset invariant (ε mod W(G)) is complete for Kempe equivalence | **CANDIDATE** — Lemma R proves point-level completeness (theorem); coset-level: 8/8 exhaustive + tranche-1 zero kills (evidence, not proof; ASC open with Gaps A/B named) |
| **Boundary (pinned disc)** | the sign-coset invariant is INCOMPLETE; linear functionals of any kind cannot classify | **THEOREM (witnessed)** — the disc twins: identical charges, sign difference ≡ 0 mod W_rel (computed exactly, dim 17, no sampling), mutually unreachable |
| **Higher genus** | completeness requires the H¹(·;ℤ₃) monodromy summand beyond the sign-coset | **THEOREM (mechanism)** — the reconstruction propagation acquires monodromy; Mohar–Salas classes realize it |

One asymmetry to say out loud: the boundary cell is the only one where incompleteness is
WITNESSED rather than mechanized — we hold the object; the field holds neither the cell nor the
witness. And the boundary is where the dynamics provably escapes linear algebra: the residual
invariant-shaped object is AVAILABILITY, and the descent, per tonight's transvection verdict
(0/186 linear), lives in WORD ORDER. This table is the spine of the paper (P4 absorbs it).

# 2. THE SEPARATOR HUNT (pre-registered; the twins decide; every candidate dies or survives exhaustively)

**The demand, stated precisely so candidates can't cheat:** a separator is a functional I(·),
defined for all completions of all pinnings of a disc, PROVABLY invariant under legal Kempe
moves, computable without running pairwise reachability, such that I(twin₁) ≠ I(twin₂).
(A pair-obstruction certificate — an object attached to the PAIR rather than each twin — is
also admissible, second class, labeled as such.)

**Scope theorem first (proved here, two lines — it kills a whole class a priori):**
*Any functional factoring through the available-move data at the state is blind to the twins.*
Both twins are frozen: their available-move sets are EMPTY and hence equal; any I = F(moves(f))
takes equal values. ∎ — Corollary: spanning ranks, gate censuses, availability-degree profiles:
all dead on arrival AS SEPARATORS (they remain fine as descent instruments). The hunt must read
STATIC structure.

**The named trap (exposed before anyone builds it):** ι⁺(f) := ε(f) mod W^dyn(pinning), with
W^dyn the within-pinning achieved span, "separates" the twins — because W^dyn = 0 for a frozen
pinning and R-rel makes the ε's differ. That is candidate (a) resurrected: verdict forced by
construction, an empty confirmation. **Exclusion rule, binding on the hunt: no candidate may
smuggle the achieved span of the pinning under test.** (The honest content of W^dyn is exactly
Gap A; the separator hunt IS the ASC gap made concrete at 19 vertices.)

**The candidates (each with its kill condition; Elie's Q1 runs them exhaustively):**

- **S1 — Invariant quadratics.** Solve exactly, over the disc's full population, for quadratic
  functionals Q(ε) invariant under every legal move performed anywhere (Q(ε + 1_str) = Q(ε) at
  every state where the move is legal). The linear story died; second order is the minimal
  escalation. KILL: the invariant-quadratic space evaluates equally on the twins. (If it
  separates: the residual invariant is quadratic, and the trichotomy's boundary cell gets its
  positive theorem.)
- **S2 — Counting invariants (shadow counts).** For a family of interior regions R: the number
  mod 2 of population colorings agreeing with f on R. Invariance is NOT assumed: step 1 tests it
  empirically on unfrozen relative objects (Fritsch−v is now a single class — the perfect
  invariance testbed: any shadow count must be CONSTANT across its 192 colorings). KILL:
  non-invariant on Fritsch−v, or invariant-but-equal on the twins.
- **S3 — The ordering-obstruction certificate (pair-class, Wilson lane).** The twins' difference
  δ (weight 20) lies in W_rel: the affine solution space of formal expressions δ = Σ chains has
  dimension 43 − 17 = 26. Every expression must fail an ORDERING (else Z1 would have found a
  path). Compute the minimal blocked sub-multiset — the Hall-type obstruction all orderings hit.
  Its shape (not its existence, which Z1 already implies) is the candidate: a certificate that
  should RECUR in other frozen pairs if it is the real object. KILL: the obstruction is
  amorphous (no stable recurring structure across frozen pairs from Q4's probes).
- **S4 — Boundary-refined static functionals.** Exact fan-sum vectors and their higher moments
  were reported "same" at ℤ level; the GF(2)-weighted refinements (fan sign-patterns per
  boundary vertex, as multisets) may differ. Cheap, static, provably invariant only under moves
  avoiding the fan — step 1 is the invariance proof or its failure. KILL: not invariant, or
  equal.
- **S5 — The relative-monodromy analogue (theory candidate, mine to develop, not Elie's to
  run).** In the genus cell the missing summand is a cohomological twist the sign pattern cannot
  see. Conjecture-shape: the boundary cell's missing summand is a TORSOR twist over the
  availability groupoid — formalizable as a cocycle on the groupoid with values in the pinned
  boundary's symmetry. If S1–S4 all die, this is the round-11 theory lane, and its first test is
  whether a nontrivial cocycle class exists on the twins' pinning. (Stated now so a total S-kill
  night still lands somewhere pre-registered.)

**Pre-declared reading:** if ALL candidates die, that too is a theorem-shaped fact — the twins
would witness that the residual invariant is not low-order-computable, and the program's honest
sentence becomes "the boundary obstruction is genuinely dynamical" — Wilson's lane with no
shortcut. The hunt cannot waste.

— Lyra. The trichotomy says where linear algebra ends; the hunt says what stands on the far
side; and the trap is named because tonight I watched my own definition almost dig it twice.
