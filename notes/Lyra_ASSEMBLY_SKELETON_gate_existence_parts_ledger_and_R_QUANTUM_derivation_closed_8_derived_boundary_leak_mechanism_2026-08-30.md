---
title: "H2+H3 — the Gate Existence assembly (parts ledger, ready to fill the moment G1/G2 render; the Exposure Lemma named as the one deep piece) and the r-quantum derivation: closed 8 DERIVED as 4×2 with each factor's status named; the boundary leak localized to the open-fan ring; units reconciliation pinned as step zero"
author: "Lyra"
date: "2026-08-30, Sunday (clock-verified 18:28 EDT at round start)"
status: "ROUND 13, LANES H2 and H3. H2 is a conditional assembly — every part carries its status; it becomes a proof attempt the hour G1/G2 land clean, and a named-missing-lemma report if they don't. H3 derives what is derivable tonight and PRE-REGISTERS the rest before any number is checked; the 6 is a target to derive, never to match. Nothing banks."
---

# H2 — THE GATE EXISTENCE ASSEMBLY (parts ledger)

**Target theorem.** Every stuck insertion configuration (τ = 6 at a deg-5 apex in G−v) admits a
gate: a bounded word with net one-vertex support whose application strictly enlarges the rescue
options (measured form: 739/739 + tranche scale).

| # | Part | Status |
|---|---|---|
| 1 | **GS (gate = conjugated singleton, w ≃ ασα)** | pending G1/G2 tonight. Bookkeeping note for GS-1's census: stored gates are 4-move words; GS predicts exactly one move acts trivially (Lab-1's vanishing move), so the census should report the vanishing-move pattern, not just a yes/no. GS-2's even-degree requirement lives in G−v: link vertices have deg_G − 1 — the parity bookkeeping is part of the check, not a footnote. |
| 2 | **Exposure Lemma (THE deep piece, named):** at every stuck configuration there is an even-degree vertex u near the link and a SINGLE swap α after which u is a singleton cluster | OPEN. Two attack routes, both staged: (a) height route — an unpinned puncture carries no tilt (dictionary), hence the local profile is non-extremal, hence a minimal rearrangement exists; the remaining step is "non-extremal ⟹ exposable in one preparation," a finite case analysis on the 5-cycle patch; (b) chain route — candidate α = the forced split-bridge swap, candidate u = the middle vertex or its ring-2 neighbors, by Middle-Strict/orientation case analysis. The template census (standing request) picks the route. |
| 3 | **Wall-freeness of insertion configurations** | PROVED-SHAPE from H1: walls anchor only on PINNED boundary; an insertion hole is unpinned; hence no anchored wall exists to obstruct — the same mechanism that freezes the disc guarantees the insertion problem is wall-free. (This is why the disc freezes and the induction's configuration never does — the day's deepest single sentence, now a lemma-shaped part.) |
| 4 | **Assembly** | stuck f → (2) exposes σ_u after α → (1) gives the gate w = ασα → (3) guarantees no anchored obstruction survives the gate's support — Gate Existence. Descent/termination beyond existence stays in the word-order program (Wilson lane), unchanged. |

If G1 or G2 kills GS: part 1's failure mode names the replacement (conjugation depth ≥ 2, or a
genuinely non-singleton gate class), and parts 2–3 survive unchanged — the wall gets thinner
either way, as ordered.

# H3 — THE r-QUANTUM DERIVATION

## Step zero (pinned FIRST, per the overloaded-symbol discipline)

r has worn degree units (rounds 5–6 ladder: 6/2/0) and winding units (V3: 8 closed / 6
punctured) in different rounds. Before ANY equation below is compared to ANY measured value,
the reconciliation is: read V3's r-functional from the toy source and state it in all three unit
systems (c / ω / deg; conversion table in the L1 spec). Everything below is written in WINDING
units (Δ(Σω)) on that assumption, flagged.

## The closed quantum: 8 DERIVED as 4 × 2 (each factor's status named)

- **Factor 4 (proved identity):** Σω = 4·deg on closed spheres (Charge Quantization, part iv);
  any change of total winding is 4 × an integer degree change.
- **Factor 2 (lemma-grade, general proof owed):** achieved degree changes are EVEN. Provenance:
  Δdeg = −½Σ_straddle z with the straddle sum even (integrality argument, round 4) gives
  Δdeg ∈ ℤ; the further evenness of ACHIEVED Δdeg is what the mobile closed graphs measure
  (r = 2 in degree units on Errera/T_3) and what the general Collapse/Gauss structure should
  force — its proof is the named residue of this derivation, not assumed silently.
- Product: **Δ(Σω) ∈ 8ℤ on closed spheres — the measured 8 is the identity times the evenness
  law.** Nothing numerological: both factors have provenance and one has a named debt.

## The boundary leak: localized, mechanism derived, magnitude pre-registered

At interior vertices, Δc(v) ∈ 6ℤ (evenness × the mod-3 closure). **At open-fan vertices (an
unpinned hole's link ring) the mod-3 closure FAILS — relative quantization replaces it — so
Δc(v) ∈ 2ℤ only: the conservation leak is confined to the boundary ring.** This is the same
locus where H1's walls anchor and where the ledger loses its closed-circuit constraint: one
mechanism, three appearances (wall endpoints, charge leak, quantum drop).

**Pre-registered, before checking anything:** (i) per-vertex |Δc| histograms will show 6ℤ steps
at interior vertices and 2ℤ steps confined to the link ring — the leak is LOCALIZED or the
mechanism is wrong; (ii) the quantum decreases MONOTONICALLY with boundary-component count —
committed direction; a double-punctured object reads r strictly below 6 (candidate value 4,
contingent on step-zero units — the DIRECTION is the commitment, the value awaits the
reconciliation); (iii) the closed 8 → punctured 6 ratio must fall out of the leak counting once
units are pinned — if it requires a new ingredient beyond the open-fan leak, that ingredient is
a finding, not a patch. The 6 is derived or it is not claimed.

# H4 — the paper

v0.4 waits for G1–G5, per my own rule quoted back to me in the routing. The wall (H1), the
assembly's verdict, and the quantum derivation land in one edit when the glances render.

— Lyra. The assembly is laid out like an engine on a clean floor — every part labeled, one part
still being machined, and the crane (G1/G2) arrives tonight. And the quantum: 8 was never a
mystery number; it is an identity times a law, and the 6 will be earned the same way or not
worn at all.
