---
node_type: k_audit
id: K1004
title: Unblock Elie — the reconnect verdict is "genuinely new work": only the DOWN off-diagonal exists (F729, via the (N_c)_min shortcut); lepton/neutrino/up off-diagonals are NOT computed anywhere. So the fire is NOT "one lookup" — it is a real per-sector FK generalized-binomial evaluation (Prop XII.1.3), with TWO load-bearing gates to clear first: (A) the d=1 vs d=3 multiplicity-convention split, (B) the (N_c)_min shortcut is UNPROVEN beyond the down single-row case (F690) — leptons {5/2,3/2,0} are not single-row, so down's clean fire does NOT prove the method generalizes. Source: K901 → aif.2069 (rank-2 FK). Keeper bar (K1002/K1003) stands.
date: 2026-07-29
author: Keeper
verdict: Reconnect done — no numbers to reuse for lepton/neutrino/up; this is a genuine frontier evaluation, not a lookup. Two gates must clear before firing, and the down 0.8σ is a consistency anchor, not proof of generality. This keeps the fire from going dishonest at peak convergence.
---

# K1004 — Elie's blocker: the honest map (reconnect verdict + two gates)

Casey: "Elie needs help." Reconnaissance done (reconnect-before-greenfield, Casey's own rule). The verdict is clean and it refines the whole fire framing.

## ★ RECONNECT VERDICT — genuinely new work (no numbers to reuse)
- **DOWN off-diagonal: EXISTS** (F729 + toy_4921): K(λ_i,λ_j) = (N_c)_min → ⟨ψ₁|ψ₃⟩ = 3 → V_us = 1/√20 = 0.2236 (0.8σ, blind). Reusable — for down.
- **LEPTON, NEUTRINO, UP off-diagonals: DO NOT EXIST anywhere.** toy_4219 STEP-4 is symbolic; toy_4222 runs on a placeholder; toy_4917 uses hand-set placeholder correction matrices; the muon-π² off-diagonal is filed "Open"; the neutrino inter-sector overlaps are "GATED (Lyra)." **Elie is genuinely blocked — the reconnect correctly returned "frontier," not "already done."** That IS the rule working: it tells you honestly when you're at real new work. No time wasted hunting.

## ★★ THE FIRE IS NOT "ONE LOOKUP" — it is a real per-sector FK evaluation
The team's peak-convergence language ("one faithful FK lookup from the whole spectrum") is slightly too optimistic, and Cal #27 fires hardest exactly here. The off-diagonal object is the **FK generalized-binomial coefficient $\binom{\lambda_j}{\lambda_i}_\nu$ (FK Ch XII, Prop XII.1.3)** — a genuine evaluation for each of lepton {5/2,3/2,0}, neutrino (2nd Majorana condensate), up inter-degree. Two gates must clear FIRST:

### GATE A — reconcile the d = 1 vs d = 3 convention split (load-bearing)
The corpus uses BOTH multiplicity conventions: the Gate1-FK note uses **d = 1** (genus route, ρ = g/2 = 7/2); F729/F734 use **d = a = 3** (the ν = N_c route). The Pochhammer/binomial coefficients depend on d — $(\rho)_\lambda = \prod_i (\rho - (i-1)d/2)_{\lambda_i}$ — so **the numbers change with the convention.** This is a "pin conventions to the primary source" call: reconcile to FK Ch XII / the published rank-2 paper ONCE, before any sector is evaluated. **Lyra owns this; Cal audits the pin.**

### GATE B — the (N_c)_min shortcut is UNPROVEN beyond down (the sharp one)
F729's off-diagonal used the shortcut **K(λ_i,λ_j) = (N_c)_min** (overlap = diagonal Pochhammer at the smaller degree). It fired clean for down — **but likely because down is a SINGLE-ROW case.** F690 proved the rank-2 two-factor Pochhammer reduces to the scalar (ν)_ℓ **exactly for single-row (ℓ,0) K-types**, and the down modes {1,3,5} are single-row degrees. **The lepton addresses {5/2,3/2,0} = (ρ₁,ρ₂,0) are NOT single-row in that sense** → the scalar reduction that made down clean may not carry over, and **(N_c)_min may be a down-specific coincidence, not the general object.**
- **★ Therefore: the down sector's 0.8σ is a CONSISTENCY ANCHOR, not proof the method generalizes** (no-wave-through-on-a-clean-number). The real test: evaluate the actual Prop XII.1.3 binomial; it MUST reproduce (N_c)_min = 3 for the down {1,3} single-row case (the check), and it produces genuinely-new numbers for the non-single-row lepton/neutrino/up sectors.
- **Do NOT assume (N_c)_min for the other three sectors** — that would be the exact soft-clean/tuned failure the K1002/K1003 bar guards against. toy_4921 already refused to; hold that line.

## ★ THE SOURCE (so Lyra doesn't hunt)
K901 found the published rank-2 reference: **"On the Faraut–Korányi hypergeometric functions in rank two," Ann. Inst. Fourier, aif.2069.** That + FK Ch XII Prop XII.1.3 is where the closed-form off-diagonal binomial coefficients live. aif.2069 resolves the scalar-vs-two-factor and (with FK) the d-convention.

## ★ UNBLOCK PATH (concrete)
1. **★ LYRA — Gate A first:** pin d (=1 or =3) to FK Ch XII / aif.2069, once, in writing. Then **Gate B:** evaluate the real $\binom{\lambda_j}{\lambda_i}_\nu$ (Prop XII.1.3) for the three sectors from the source — NOT the (N_c)_min shortcut. Verify it reproduces (N_c)_min=3 for the down {1,3} check. Hand Elie the per-engine numbers.
2. **★ ELIE — fire once the real binomials land** (toy_4920 staged). Post the kernel blind, fire all ~13 at σ. He already did this correctly for down; same protocol.
3. **★ CAL — audit the d-pin (Gate A) + the single-row-vs-general reduction (Gate B):** is the evaluated binomial target-innocent, and does the down consistency check pass?
4. **★ KEEPER — the bar stands (K1002/K1003):** rule the outputs; the down anchor is consistency not new evidence; soft-clean → §133 scrutiny.

## ★ Honest recalibration (the "improve derivations" phase working)
This is NOT a setback — it is the reconnect + honesty discipline doing exactly its job. We now know precisely what's missing (the real Prop XII.1.3 binomials for three sectors), where to get it (aif.2069), and the two things to verify so the fire stays honest (d-convention, shortcut-generality). The spine is intact; the "one crank turn" is really "clear two gates → evaluate the real binomial → fire." The candidate principle (fermion sector = SVD of one FK matrix) is still held unnamed until the blind bar passes — and now we know the bar can't be shortcut past the single-row coincidence.

— K1004, Keeper, 2026-07-29. Reconnect verdict: only down off-diagonal exists; lepton/neutrino/up are genuine FK evaluation (Prop XII.1.3). Two gates before firing: (A) reconcile d=1 vs d=3 to the primary source; (B) the (N_c)_min shortcut is unproven beyond the down single-row case (F690) — evaluate the real binomial, don't assume the shortcut; down's 0.8σ is a consistency anchor. Source: aif.2069 (K901). Bar stands (K1002/K1003). See [[Keeper_K1002_PRE_REGISTERED_BLIND_BAR_for_the_engine_fire_one_kernel_13_outputs_Elie_posts_kernel_blind_before_comparison_V_cb_ratified_Derived_2_3_mode_is_3D_without_counterexample_2026-07-29]], [[Keeper_K1003_PRE_REGISTERED_SCORECARD_for_the_engine_fire_22_outputs_banked_vs_blind_the_evidence_is_UNIFICATION_not_number_matching_CP_phases_are_the_sharpest_blind_test_2026-07-29]], F729, F690, F734, F735, K901.
