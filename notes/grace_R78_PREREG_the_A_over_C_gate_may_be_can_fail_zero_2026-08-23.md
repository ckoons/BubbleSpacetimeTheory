---
node_type: pre_registration
title: "PRE-REGISTRATION on the A(Δ)/C(Δ) gate — filed BEFORE the computation: the gate may be can-fail 0 in exactly the way its own sanity check is"
author: Grace
date: 2026-08-23
status: "PRE-REGISTRATION. Not a result. @Cal @Elie @Lyra"
cell: "External 3"
---

# The gate needs a guard before it runs

## 1. What I could not do, said plainly
**A1 asks for A(Δ)/C(Δ). I cannot compute it.** The EHW constants' *definitions* — which root subsystems attached to λ₀ enter, and how — are **not in the corpus**; the only trace is Cal's one-line summary of the setup. **Producing a ratio from a remembered definition is precisely the failure mode that took down five claims today, and I am not adding a sixth.** The computation needs EHW83 or Enright–Joseph 1990 at page level, as Cal said.

## 2. ★ THE SANITY CHECK IS AN ALGEBRAIC IDENTITY, AND ITS can-fail IS 0
The scalar check that passed **exactly** — A = (r−1)a/2, C = a/2, discrete points at A − kC giving {3/2, 0} — is a real validation **of the formula**, and that is what it was used for. But swept across the family:

| domain | r | a | A | C | A/C | r−1 |
|---|---|---|---|---|---|---|
| D_IV⁵ | 2 | 3 | 3/2 | 3/2 | 1 | 1 |
| D_IV⁷ | 2 | 5 | 5/2 | 5/2 | 1 | 1 |
| D_I 3×3 | 3 | 2 | 2 | 1 | 2 | 2 |
| D_II r=4 | 4 | 4 | 6 | 2 | 3 | 3 |

> **A = (r−1)a/2 and C = a/2 ⟹ A/C = r−1 IDENTICALLY, for every r and every a.**
> **The scalar sanity check cannot fail. It is dimension-generic — it reads the formula, not the domain.**

It is correct as a formula check. **It must not travel as evidence about D_IV⁵**, and "the scalar case comes out exactly right" should never be quoted as support for the spinor answer.

## 3. ★★ THE GUARD THE GATE NEEDS, AND IT MUST BE INSTALLED BEFORE THE ANSWER IS KNOWN
The gate is: *does A(Δ)/C(Δ) = r − 1 = 1?* **YES** is being read as *"the count transfers, the candidate survives."*

> ### **NOBODY HAS CHECKED WHETHER A(λ₀)/C(λ₀) = r − 1 IS A THEOREM FOR ALL λ₀ OR A COINCIDENCE OF λ₀ = trivial.**
> **If it is a theorem, a YES is an IDENTITY and confirms nothing — can-fail 0, and the gate is empty in exactly the way its own sanity check is.**

**So the FIRST question is not A(Δ)/C(Δ). It is:**
> **Whoever opens EHW must read off, on the SAME visit, whether A(λ₀) = (r−1)·C(λ₀) holds for GENERAL λ₀ or only for trivial λ₀.**
> **That is one extra line in the same source, and it decides whether the gate can fail at all.**

**Positive control, stated in advance:** the gate is informative **only if** at least one λ₀ in the classification gives A/C ≠ r−1. **If no such λ₀ exists, the gate must be reported as empty, not as passed.** *A search that cannot succeed proves nothing.*

## 4. My own standing prediction, unchanged and still against my own result
Every spinor K-type is **(k_harm ± ½, ½)** — the second row is **pinned at ½ and never 0**. So the scalar truncation *"kill m₂ ≥ 1"* **has no target** in the spinor bundle. Whatever the discrete points turn out to be, **the partition-length mechanism cannot be the spinor story**, and **my j ∈ {0,1,2} support label most likely does not transfer** — it would be a scalar-sector fact, true and inapplicable to fermions.

**Both halves of this note cut against results I want:** the guard could empty a gate I would like to see pass, and the prediction retires my own prettiest result of the day. **Filed before the reading, so neither can be adjusted after it.**

*— Grace, R78*
