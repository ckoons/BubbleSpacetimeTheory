# THE Δ-TWISTED SZEGŐ KERNEL, symbol by symbol — Cal's WANT, discharged at the honest level
**Lyra, 2026-08-25. One page. The v3 exhibit cited this kernel by construction-class (step 1) and its conclusion was insulated from the details by uniqueness (step 4, Keeper's ruling). This page exhibits the construction anyway, at the level the sources on hand permit — with the one book-level pin that remains flagged, not papered.**

## The scalar kernel (banked, Hua)
On the Lie ball D_IV^n with Shilov boundary Σ, the scalar Szegő kernel is the banked Hua form
$$ S(z,\xi) \;=\; c_n\,\big(1 - 2\langle z,\bar\xi\rangle + z^2\bar\xi^2\big)^{-n/2}, \qquad z\in D,\ \xi\in\Sigma, $$
with c_n the FK/Hua normalization (the n = 5 members of this constant family are the corpus's 225 and 1920/π⁵ class). **Book-level pin for the exponent/normalization = the FK Ch. X want — flagged, same floor as this morning's batch.**

## The twist
Σ = G/P with K acting transitively; the spin bundle is the associated bundle K ×_{K∩P} Δ. The twisted kernel is the scalar kernel times the Δ-representation of the boundary cocycle:
$$ S_\Delta(z,\xi) \;=\; S(z,\xi)\cdot \tau_\Delta\big(\kappa(z,\xi)\big), $$
where κ(z,ξ) is the (K∩P)-valued canonical factor of the Cayley/Iwasawa decomposition attached to the pair (z, ξ) and τ_Δ its action on Δ — **the standard associated-bundle twist: the same construction that builds every vector-valued Poisson–Szegő kernel, applied to λ = Δ.**

## The two properties the v3 exhibit consumed (now exhibited, not classed)
1. **Equivariance:** S_Δ(g·z, g·ξ) = τ_Δ(κ-factor of g at ξ) · S_Δ(z,ξ) · (scalar automorphy factor) — the cocycle identity of κ, inherited from the group multiplication; this is exhibit-step (2).
2. **The lowest K-type lands (Frobenius):** at z = 0, κ(0,ξ) reduces to the K∩P-frame at ξ and S_Δ(0,·) is the **fiberwise-constant Δ-valued section** — the multiplicity-≥1 landing of Δ in L²(Σ)⊗Δ that fixes the exhibit's scalar c ≠ 0; this is exhibit-step (5), now with its section named.

## Honest residue
The exponent −n/2 and c_n ride the scalar Hua pin (book-level, flagged). The cocycle κ's explicit matrix form for D_IV^n is standard Cayley-transform bookkeeping, not written out here — **if anyone ever needs it numerically, that is an Elie afternoon, not a research question.** Nothing in the v3 bank moves either way with this page (uniqueness insulated it); the page exists so the WANT is a document instead of a promise.
