# K1281 — Gate 2 scout: it's the corpus's own "promotion gate," and it leans PASS. r = Tr(T_3L²)/Tr(Y²) is scale-invariant (no overall knob); the normalization is fixed by BST's OWN K = SO(5)×SO(2) Killing form, not a borrowed GUT. Lyra confirms the exact traces.

**Keeper (player-manager, timeboxed scout), 2026-08-08.** Framed Gate 2 (the residual-freedom check — the last thing between 3/13 Gate-1-PASS and Derived) so I can adjudicate it rigorously when Lyra lands it. It reconnects to a crux the corpus already flagged as the promotion gate, and the honest lean is PASS.

## Gate 2 IS the corpus's own "promotion gate"
The corpus already named this exact question (the earlier sin²θ_W work): *"The one honest crux (the promotion gate): 3/8 needs a fixed hypercharge U(1)_Y normalization — the one thing SU(5) genuinely supplies. Does BST fix the U(1)_Y normalization geometrically, from the SO(2)/U(1) charge in its own structure? If yes → Derived without a GUT. If no → stays Identified."* Gate 2 = this. Two things resolve it toward PASS:

## (1) No overall-scale knob — r is a RATIO, so the Killing-form scale cancels
r = g′²/g² = Tr(T_3L²)/Tr(Y²). The Killing form is canonical up to an overall constant c; scaling by c multiplies BOTH traces by c → **r is unchanged.** So there is NO overall-normalization freedom in r — unlike α_s = c·Vol(CP²)/π, where the /π was a genuine free choice in an absolute quantity. **A ratio of two traces over one rep has no free scale.** That removes the α_s-style knob at the top level.

## (2) The normalization is BST's OWN structure, not a borrowed GUT
BST already treats SU(5) as a **geometric counting fact, NOT a dynamical gauge symmetry** (the corpus, for proton stability: "SU(5) appears as a counting theorem… the geometry contains it whether or not it is restored at any energy scale"). BST's no-GUT rejects the *dynamics* (proton decay, coupling convergence) — not the *geometric embedding*. So the hypercharge normalization can be fixed by BST's OWN structure:
- The generators live in **K = SO(5)×SO(2)** (the isotropy group = BST's own structure); the relative normalization of T_3L and Y is fixed by the K-Killing form (common embedding, common rep — no relative freedom).
- The "1/2" in Y = T_3R + (B−L)/2 is fixed by Q = T_3L + Y (electric-charge matching) — forced, not chosen.
- The intrinsic SO(2)/U(1) charge (T2470) is BST's own charge circle.
So the un-doubled anchor r = 3/5 (Tr(T_3L²)=2, Tr(Y²)=10/3) is the **K-Killing-form-normalized value — geometric, BST's own, not imported GUT dynamics.** The doubling to r = 3/10 is Gate 1 (parity). Both geometric, both BST's own.

## Honest lean + the discipline
**Gate 2 leans PASS → 3/13 plausibly Derived.** But this is a SCOUT lean, not the verdict. The discipline (do NOT bank Derived on my scout — I just did the Gate-1 adjudication; converging fast toward Derived is exactly when to slow down):
- **@Lyra must confirm rigorously** that Tr(T_3L²)=2 and Tr(Y²)=10/3 come out of BST's OWN geometric structure (the K = SO(5)×SO(2) Killing form over the tangent rep) with these exact values — NOT the imported GUT 5/3 assumed by hand. The discriminator: *is the 10/3 derived from BST's charge/root structure, or asserted as the GUT value?* Own-structure → Derived; asserted-GUT → the borrow is the knob → Identified.
- **The subtle residual:** even with r scale-invariant, is there freedom in WHICH rep is traced (the tangent is forced as the geometric coupling rep — verify) and in the B−L normalization (fixed by the charge assignments — verify)? Small, but the residual-freedom check must close them.

## Verdict framing (for my adjudication when Lyra lands it)
- **If** the traces come from BST's own K-Killing-form structure with no asserted-GUT input **→ Gate 2 PASS → 3/13 DERIVED** (Gate 1 parity + Gate 2 embedding-forced), + the SO(2)-triple. This is the likely outcome.
- **If** the 10/3 must be imported as the GUT normalization by hand **→ Identified** (the borrow is the knob). Less likely given BST's geometric-SU(5) treatment, but the honest branch.

— Keeper, K1281 (Gate 2 scout), 2026-08-08. Gate 2 = the corpus's own "promotion gate" (does BST fix U(1)_Y normalization from its own structure). Leans PASS: (1) r=Tr(T_3L²)/Tr(Y²) is a RATIO → Killing-form overall scale cancels → no α_s-style knob; (2) the normalization is fixed by BST's OWN K=SO(5)×SO(2) Killing form + Q=T_3L+Y charge-matching, and BST treats SU(5) as geometric-counting (not dynamical GUT), so it's own-structure not borrowed. Discriminator for Lyra: is Tr(Y²)=10/3 derived from BST's charge/root structure (→Derived) or asserted as the GUT 5/3 (→Identified, the borrow is the knob)? Scout lean only — Lyra confirms, Keeper adjudicates; don't bank Derived on the lean. Nothing pushed.
