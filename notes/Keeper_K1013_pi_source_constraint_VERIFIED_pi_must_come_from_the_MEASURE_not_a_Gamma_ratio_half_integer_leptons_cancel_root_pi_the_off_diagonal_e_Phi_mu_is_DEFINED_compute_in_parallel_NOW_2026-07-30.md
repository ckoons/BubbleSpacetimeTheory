---
node_type: k_audit
id: K1013
title: π-SOURCE CONSTRAINT (verified, Cal §146) — the muon's π² CANNOT come from a bare cross-ν Γ ratio between the lepton addresses (all half-integer, same parity → √π cancels: Γ(5/2)/Γ(3/2)=3/2, π-free). The π MUST come from the MEASURE: the Φ-inserted OFF-DIAGONAL cross-term ⟨e|Φ|μ⟩ against the Bergman N^{5/2} measure, which does NOT factorize into Γ's. This is a DIFFERENT object than yesterday's diagonal-norm null, and it is now DEFINED — so Elie and Cal compute it IN PARALLEL, NOW, from yesterday's F323 harness; no one waits for Lyra to "write the kernel." The Γ-ratio route is also exactly where the retracted c₅/c₃=Γ(5)/π² would sneak back — reject it.
date: 2026-07-30
author: Keeper
verdict: The π-source is pinned to the measure (verified). The load-bearing computation ⟨e|Φ|μ⟩ with N^{5/2} is defined — reuse yesterday's harness, change diagonal→off-diagonal + insert Φ. Compute in parallel this turn. π survives or it doesn't; either settles it.
---

# K1013 — The π must come from the measure; the computation is defined; stop waiting

## ★★ THE CONSTRAINT (verified)
Cal §146 caught the trap in the "cross-ν escapes the null" guardrail, and it holds:
- The lepton addresses are ν ∈ {5/2, 3/2, 0} — **all half-integer, same parity.** A bare Γ_Ω ratio between them is **rational, π-free**: Γ(5/2)/Γ(3/2) = 3/2, Γ(7/2)/Γ(5/2) = 5/2, etc. (verified). **So the muon's π² in (24/π²) cannot come from a bare cross-ν Γ ratio.**
- **Therefore the π MUST come from the MEASURE** — the genuine Φ-inserted off-diagonal cross-term ⟨mode_i | Φ | mode_j⟩ integrated against the Bergman **N^{5/2}** measure. That integral does NOT factorize into Γ's (the degree-1 condensate Φ breaks the modes' orthogonality), so the measure's π is not forced to cancel.
- **Reject the Γ-ratio route explicitly** — it is π-free for the leptons, and it is precisely the shape the retracted **c₅/c₃ = Γ(5)/π² (F669)** would re-enter as. Cal's §146 gate #2 (π-source = measure, not Γ ratio, not the retracted ID) is ratified.

## ★ WHY THIS IS NOT YESTERDAY'S NULL (a different matrix element)
- **Yesterday (K1011 null):** the DIAGONAL norm ratio ⟨μ|μ⟩/⟨e|e⟩ — factorizes into Γ_Ω → cancels → π-free → nulled (5.5/8.0/0.478).
- **Today (resume):** the OFF-DIAGONAL ⟨e|Φ|μ⟩ — Φ-inserted cross-term between two *different* modes; need not factorize; the measure's π can survive. Same modes, different matrix element.

## ★★ THE COMPUTATION IS DEFINED — no more waiting (Casey's kick)
The object is now specified precisely: **⟨e|Φ|μ⟩ = ∫_{Lie ball} (e-mode)·Φ·(μ-mode) · N(w)^{5/2} dV**, e-mode/μ-mode at (k+½,½) for k=0,1, Φ = the degree-1 condensate. **This does NOT require Lyra to "write the kernel" first — it is yesterday's F323 harness with two changes: (1) off-diagonal (two different modes) instead of the norm; (2) insert Φ.** Elie already has the harness (it fired yesterday). Cal already has an independent Lie-ball MC harness. **Both compute ⟨e|Φ|μ⟩ THIS TURN, independently, in parallel. Lyra writes the general kernel + the down-slice reduction in parallel — the team is NOT blocked on her.**

## ★ THE COMMITTED BAR (Cal §146, ratified + down numbers pinned)
1. **Down-anchor canary:** the kernel reduces to the validated Jack on the same-ν slice → reproduce diagonal {3,60,2520} (m_s/m_d=20) and V_us=1/√20. Fire FIRST (Elie's canary, built).
2. **π-source:** the 24/π² traces to the MEASURE (the ⟨e|Φ|μ⟩ integral), NOT a bare Γ ratio (π-free) and NOT the retracted c₅/c₃=Γ(5)/π². ← this note pins it.
3. **Cross-ν fall-out:** up 12-block (→CKM), lepton off-diagonal (→PMNS at θ₂₃=4/7) fall out of the ONE kernel.
4. **Tier:** candidate/insight until 1 & 3 pass with 2 clean. "Quarks and leptons are one kernel" is a theorem only then.

## ★ NEXT — parallel, aggressive, this turn
- **★★ ELIE — (a) run the down-canary NOW** (needs nothing from anyone); **(b) compute ⟨e|Φ|μ⟩ with N^{5/2} NOW** (yesterday's harness, off-diagonal + Φ). Report both numbers this turn. Does ⟨e|Φ|μ⟩ carry π²?
- **★★ CAL — compute your INDEPENDENT ⟨e|Φ|μ⟩ number NOW** (don't wait for Elie or Lyra); cross-check. Hold gate #2 hardest (the half-integer-π trap).
- **★ LYRA — write the general two-point kernel + the same-ν→Jack reduction in parallel** (the team is not blocked on you; but this closes the unification).
- **★ KEEPER — π-source pinned (this note); I rule the down-canary, then the π-source, then fall-out.**

— K1013, Keeper, 2026-07-30. VERIFIED: lepton half-integer addresses make a bare Γ ratio π-free (Γ(5/2)/Γ(3/2)=3/2) → the π MUST come from the measure (the Φ-inserted off-diagonal ⟨e|Φ|μ⟩ against N^{5/2}), a different object than yesterday's diagonal-norm null. It's DEFINED (yesterday's harness + off-diagonal + Φ) → Elie & Cal compute in parallel NOW, no waiting on Lyra. Reject the Γ-ratio route (π-free, = the retracted c₅/c₃ shape). Cal §146 bar ratified. See [[Keeper_K1012_the_up_frame_target_spec_and_the_cross_address_two_point_kernel_UNIFIES_up_and_lepton_off_diagonals_down_Jack_is_the_same_nu_special_case_approach_writeup_2026-07-30]], [[Keeper_K1011_RULING_F323_null_is_STRUCTURALLY_FORCED_c_function_route_CLOSED_muon_stands_on_e_n_unified_lepton_engine_PARKED_fermion_sector_BANKED_resume_point_documented_2026-07-29]], Cal §146, F323, F669.
