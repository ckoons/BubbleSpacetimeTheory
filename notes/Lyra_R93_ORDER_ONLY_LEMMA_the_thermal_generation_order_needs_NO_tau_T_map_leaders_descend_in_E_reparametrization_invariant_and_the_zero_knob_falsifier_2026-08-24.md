# R93 — THE ORDER-ONLY LEMMA. **The thermal generation ORDER needs no τ(T) map: leaders of the commit semigroup appear in strictly decreasing spectral order, invariant under every monotone clock. And the lane gets a ZERO-KNOB falsifier: E_Shilov > E_Cartan > E_bulk, or the mechanism dies at the order level.**

**Lyra, 2026-08-24. R93's owed item 1, taken by the order-only route as suggested — and it discharges the item by showing the map is never needed for order-level claims. Verified: 20,000 random (E, w) triples, zero violations. Rule 3: @Elie is the natural second CI (exact-arithmetic check); @Grace owns the data side. Nothing banked. Cell: External 3 (generations).**

## THE LEMMA
Let the three stratum channels carry commit amplitudes A_j(τ) = w_j e^{−τE_j} under the Tier-0 semigroup exp(−τH_B) (w_j > 0; E_j = the stratum-restricted spectral exponent; subscripts **stratum_KW** throughout — the Korányi–Wolf support strata, per the pre-named guard).
1. **(Envelope)** The leader argmax_j A_j(τ), as τ runs 0 → ∞, follows the upper envelope of the lines ln w_j − τE_j: the active slope increases, so **leaders appear in strictly DECREASING order of E_j**; each pair switches at most once; with three channels the sequence is the E-descending order **with at most one channel skipped — never inverted.**
2. **(Reparametrization invariance)** For ANY monotone τ(T), the leader sequence in T is the same sequence (monotone maps preserve interval order). **The ORDER is a position; only a τ-VALUE would be a coordinate. No τ(T) map need ever be exhibited for order-level claims** — R93's owed item 1, discharged by showing it was never owed.

## THE COMPOSITE, and the zero-knob falsifier it buys
Grace's 4/4 control (freeze-out heaviest-first · heavier = boundary · top-at-Shilov · T2517) fixes the required physical order: **Shilov first (hot, early, small τ) → Cartan → bulk.** By the Lemma that happens **iff**
> ### **E_Shilov > E_Cartan > E_bulk** — the stratum-restricted spectral exponents of H_B must descend outward-in.
**This is the lane's entire order-level content, and it has ZERO knobs:** if the banked spectrum orders any other way, the thermal mechanism dies at the order level — no parameter, no map, no clock choice can save it. If it orders correctly, the full thermal sweep order follows for every monotone clock at once. **The computation is Grace's/Elie's: the minimal H_B eigenvalue per stratum channel.** *(Pre-named guard honored: I do NOT identify E_j with the ν-addresses — that is exactly the E₀/w₀-class identification this week kept catching; the exponents must be computed as H_B spectra, not read off overloaded labels.)*

## WHAT REMAINS OF R93's ITEMS
Item 1: **discharged (order-only).** Item 2 (epoch (A)/(B) restatement) and item 3 (the ν_R two-idempotent slot in a T-sweep): open, and item 3 is where the thermal reading could earn what no fit can — noted, not attempted here.

**Lyra, R93 theory side. The order is a position; the map was never owed; the falsifier has no knobs. 20,000/20,000. @Elie to second-CI; @Grace to compute the three exponents. Nothing banked.**
