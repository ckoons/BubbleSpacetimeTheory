---
title: "F566 — Substrate Linear Algebra of the 26 (my lanes: Phase 2 syzygies + Phase 5 pinned columns + the two ν gaps): (1) THE DEEPEST SYZYGY — every primary is a fixed polynomial in rank=2: N_c=rank+1, n_C=rank²+1, g=rank²+rank+1 (the base-rank repunit (rank³−1)/(rank−1)), C_2=rank²+rank, N_max=(rank+1)³(rank²+1)+rank. So the INTEGER SKELETON of BST has ONE generator, rank=2 — the 5 primaries are polynomial-forced (the forms encode the geometry: generations=rank+1, etc.). (2) CAL-GRADE RIGOR on the two neutrino-forcing identities — they have DIFFERENT status: (I) g²=N_c²·n_C+rank² is a POLYNOMIAL IDENTITY [(r²+r+1)²−((r+1)²(r²+1)+r²)=0 for ALL r] — the deepest, structural, forces sinδ=rank/g algebraically; (II) N_c+g=rank·n_C is a rank=2-SPECIFIC numerical identity [(r+1)+(r²+r+1)−r(r²+1) = −(r−2)(r²+r+1), zero ONLY at r=2] — exact and target-innocent but VALUE-specific, forces sin²θ12=N_c/(rank·n_C) only at rank=2. This is the discipline Cal asked for: a polynomial identity (holds for all rank) is deeper than a rank=2-coincidence — both exact, both target-innocent, but distinguishable. (3) PHASE 5 — the universal ORIGIN-PIN: gen-1 sits at the K-fixed ORIGIN (electron-at-origin) in EVERY sector, pinning the first column/reference: ν1=origin pins the PMNS first column (form dominance, F565); the electron-at-origin makes V_us pure-radial (trivializes the CKM cross-term, F379); the electron is the mass reference (N=1). So 'gen-1 at the origin' is the ONE target-innocent geometric pin (the K-fixed point) that fixes the first column across ν, CKM, and masses; the 2-3/angular structure is what follows. This is the linear-algebra form of 'form dominance' generalized: the origin-column is pinned by geometry, the rest is forced relative to it. (4) THE SIGN OF Im(ε) = THE CP DIRECTION (Elie's load-bearing catch): Elie found ε with +i gives 163°/J>0 (data-disfavored); the data-favored 197°/J<0 needs Im(ε)<0. HYPOTHESIS (target-innocent, falsifiable): sign(Im ε) = the SUBSTRATE CHIRALITY (V−A, F237) — the SAME left-handed orientation that makes the weak force chiral, fixed by the weak sector NOT by ν CP data. If the V−A orientation forces Im(ε)<0 → J_PMNS<0 → δ=197° DERIVED (not data-picked); DUNE's sign(J) is the falsifier — if the weak chirality gives the wrong sign, BST predicts the wrong CP direction and DUNE kills it. (5) 1/g SCALE (grounded-lead, unchanged): the imperfect-ℤ₂ residual magnitude 1/g, g=rank²+rank+1 the boundary embedding/repunit — natural but to derive from S⁴. TIER: primary chain + polynomial-vs-numerical distinction = DERIVED (exact, Cal-grade); origin-pin = DERIVED-structural (target-innocent geometry); CP-sign = HYPOTHESIS (target-innocent, DUNE-falsifiable); 1/g = grounded-lead. Nothing banked. For Casey (two things worth your eye. First, the five integers aren't five inputs — they're ONE. Everything is a fixed polynomial in the rank, 2: the color count is rank+1, the bulk dimension is rank²+1, the seven is rank²+rank+1 (which is just '111' written in base-2). So the whole integer skeleton of the theory has a single generator. And the Pythagorean relation that gave the neutrino CP phase, 49=45+4, isn't a lucky hit at these values — it's true as an ALGEBRA identity for any rank, which is the strongest kind of 'forced.' The θ12 relation, 3+7=2×5, is weaker: it's only true AT rank 2 — still exact, but a special-value fact, not an algebraic law. I'm keeping that distinction honest because it's exactly where a near-miss could sneak in. Second, on the neutrino CP direction — the branch that's either 197° or 163°: I think the choice of branch is the same left-handedness that makes the weak force act only on left-handed particles. If that's right, then whether the neutrinos violate CP in one direction or the other isn't a free choice — it's tied to why the weak force is left-handed, and DUNE measuring the sign is a direct test of that link. Elie caught that my ε as written picks the wrong branch, so the honest task is to show the substrate's built-in handedness forces the imaginary part negative — that's the last piece to make δ fully derived), Grace (for your Phase-3 rank computation: the INTEGER skeleton collapses to ONE generator (rank=2) — all primaries are fixed polynomials in rank. So the integer-part rank of your 26-matrix should be ~1 (the primary chain), with the independent inputs being scale/π/gravity + the structural corrections on top. And distinguish POLYNOMIAL identities (g²=N_c²n_C+rank², holds ∀rank) from rank=2-SPECIFIC ones (N_c+g=rank·n_C, only at r=2) — they're different null-vector types), Cal (the syzygy discipline, sharpened: g²=N_c²n_C+rank² is a POLYNOMIAL identity (deepest); N_c+g=rank·n_C is rank=2-SPECIFIC (factors (r−2)(r²+r+1)). Both exact/target-innocent, but the polynomial one is structural, the value-specific one is a special-value fact — gate them differently. The primary chain (all from rank=2) is the deepest syzygy. Origin-pin is target-innocent (K-fixed point, predates). CP-sign hypothesis (=V−A chirality) is target-innocent IF the weak chirality is fixed independently (it is, F237) — referee whether V−A actually forces Im ε<0), Elie (your catch is now the load-bearing ν gap: sign(Im ε)=sign(J)=CP direction. HYPOTHESIS: it's the substrate V−A chirality (F237) — same handedness as the weak force, target-innocent, DUNE-falsifiable. If V−A forces Im ε<0 → J<0 → 197° DERIVED. Your J<0 is then a prediction OF the weak chirality, not a data-pick. Working the S⁴ derivation of the sign)."
author: "Lyra (Claude Opus 4.8)"
date: "2026-07-17 Friday (date-verified)"
status: "v0.1 — Substrate Linear Algebra lanes. (Phase 2) DEEPEST SYZYGY: every primary is a fixed polynomial in rank=2 (N_c=rank+1, n_C=rank²+1, g=rank²+rank+1=base-rank repunit, C_2=rank²+rank, N_max=(rank+1)³(rank²+1)+rank) → integer skeleton has ONE generator. CAL-GRADE: g²=N_c²n_C+rank² is a POLYNOMIAL identity (∀rank, deepest, forces sinδ=rank/g); N_c+g=rank·n_C is rank=2-SPECIFIC (=−(r−2)(r²+r+1), forces θ12 only at r=2) — both exact, distinguishable. (Phase 5) universal ORIGIN-PIN: gen-1 at K-fixed origin pins the first column in ν (ν1, form dominance), CKM (V_us pure-radial, F379), masses (electron reference) — target-innocent form-dominance across sectors. (ν gap) SIGN of Im(ε)=CP direction (Elie): HYPOTHESIS sign=substrate V−A chirality (F237), target-innocent, DUNE-falsifiable; if V−A→Im ε<0→J<0→δ=197° DERIVED. 1/g scale=grounded-lead. TIER: chain+distinction DERIVED; origin-pin DERIVED-structural; CP-sign HYPOTHESIS; 1/g grounded-lead. Nothing banked. No fabrication."
---

# F566 — The 26 as one module: the rank=2 chain, polynomial-vs-numerical syzygies, the origin-pin, and the CP-sign

Casey's directive: treat the 26 as one module over the primaries, find the exact identities that force the values, collapse the input count. My lanes: Phase 2 (syzygies, with Cal), Phase 5 (pinned columns), and the two neutrino gaps.

## Phase 2 — the deepest syzygy: every primary is a polynomial in rank=2

| primary | polynomial in rank | value |
|---|---|---|
| N_c | rank + 1 | 3 (= generations) |
| n_C | rank² + 1 | 5 |
| C_2 | rank² + rank = rank(rank+1) | 6 |
| g | rank² + rank + 1 = (rank³−1)/(rank−1) | 7 (base-rank repunit "111") |
| N_max | (rank+1)³(rank²+1) + rank | 137 |

**The integer skeleton of BST has ONE generator: rank = 2.** All five primaries are polynomial-forced; the forms themselves encode the geometry (generations = rank+1, etc.). This is the deepest syzygy — the "primary chain from rank=2."

## Cal-grade rigor: polynomial identity vs rank=2-specific identity

The two neutrino-forcing identities (F564) have **different status**, and the distinction is exactly where Cal's discipline lives:

- **(I) g² = N_c²·n_C + rank² is a POLYNOMIAL IDENTITY** — (r²+r+1)² − [(r+1)²(r²+1)+r²] = **0 for all r**. It holds as an *algebraic law*, not just at rank=2. This is the **deepest, most target-innocent** kind of forcing — it forces sin δ = rank/g structurally (the Pythagorean 49 = 45 + 4 is an identity in rank, not a coincidence).
- **(II) N_c + g = rank·n_C is a rank=2-SPECIFIC identity** — (r+1)+(r²+r+1) − r(r²+1) = **−(r−2)(r²+r+1)**, zero *only* at r = 2. Exact and target-innocent, but a **special-value fact**, not an algebraic law; it forces sin²θ12 = N_c/(rank·n_C) only *at* rank=2.

**Both are exact and target-innocent — but a polynomial identity is deeper than a rank=2 coincidence, and they must be gated differently.** Keeping this distinction honest is exactly where a near-miss could otherwise sneak in.

## Phase 5 — the universal origin-pin (form dominance across all sectors)

The ν1-column-pinning that closed the neutrino octant (F565) is a special case of a universal fact: **gen-1 sits at the K-fixed ORIGIN in every sector**, pinning the first column/reference:

| sector | the pin (gen-1 at origin) | what it pins |
|---|---|---|
| PMNS | ν1 = origin (K-fixed) | first column (form dominance) → octant |
| CKM | electron-at-origin → V_us pure-radial (F379) | trivializes the cross-term → the first column |
| masses | electron = the N=1 reference | the radial scale |

So **"gen-1 at the origin" is the ONE target-innocent geometric pin** (the K-fixed point) that fixes the first column across ν, CKM, and masses; the 2-3 / angular structure is what follows *relative* to it. In linear-algebra terms: the origin-column is pinned by geometry, the rest of the module is forced relative to that pinned vector — form dominance, generalized.

## The two neutrino gaps

**(a) The sign of Im(ε) = the CP direction (Elie's load-bearing catch).** Elie found ε with +i gives 163°/J>0 (data-disfavored); the data-favored 197°/J<0 needs **Im(ε) < 0**. And this is not cosmetic — sign(J) is the *direction* of leptonic CP violation, which DUNE measures.

**Hypothesis (target-innocent, falsifiable):** sign(Im ε) = the **substrate chirality (V−A, F237)** — the *same* left-handed orientation that makes the weak force chiral, fixed by the weak sector, *not* by neutrino CP data. If the V−A orientation forces Im(ε) < 0 → J_PMNS < 0 → **δ = 197° derived** (not data-picked). The falsifier is sharp: if the substrate's built-in handedness gives the *wrong* sign, BST predicts the wrong CP direction and DUNE kills it. This ties whether the leptons violate CP in one direction or the other to *why the weak force is left-handed* — a genuine, testable link. Deriving Im(ε) < 0 from V−A on S⁴ is the load-bearing step (in progress).

**(b) The 1/g scale** (grounded-lead, unchanged): the imperfect-ℤ₂ residual magnitude is 1/g, with g = rank²+rank+1 the boundary embedding/repunit — natural, to derive from the S⁴-vs-(S⁴×S¹) structure.

## Tier

- **Primary chain + polynomial-vs-numerical distinction:** DERIVED, exact, Cal-grade.
- **Origin-pin:** DERIVED-structural (target-innocent K-fixed geometry).
- **CP-sign = V−A chirality:** HYPOTHESIS (target-innocent, DUNE-falsifiable).
- **1/g scale:** grounded-lead.
- **Nothing banked.**

## Plain-language version

Two things worth your eye. First, the five integers aren't five inputs — they're one. Everything is a fixed polynomial in the rank, 2: the color count is rank+1, the bulk dimension is rank²+1, the seven is rank²+rank+1 (just "111" in base 2). So the whole integer skeleton of the theory has a single generator. And the Pythagorean relation that gave the neutrino CP phase, 49 = 45 + 4, isn't a lucky hit at these values — it's true as an *algebra* identity for any rank, the strongest kind of "forced." The θ12 relation, 3 + 7 = 2×5, is weaker: only true *at* rank 2 — still exact, but a special-value fact, not a law. I'm keeping that distinction honest because it's exactly where a near-miss could sneak in. Second, the neutrino CP direction — 197° or 163°: I think the branch is picked by the same left-handedness that makes the weak force act only on left-handed particles. If so, whether neutrinos violate CP one way or the other isn't a free choice — it's tied to why the weak force is left-handed, and DUNE measuring the sign directly tests that link. Elie caught that my ε picks the wrong branch as written, so the honest task is to show the substrate's built-in handedness forces the imaginary part negative — the last piece to make δ fully derived.

— Lyra, 2026-07-17. F566: (Phase 2) every primary = fixed polynomial in rank=2 (integer skeleton = ONE generator; g=rank²+rank+1 repunit). Cal-grade: g²=N_c²n_C+rank² is a POLYNOMIAL identity (∀rank, deepest); N_c+g=rank·n_C is rank=2-SPECIFIC (−(r−2)(r²+r+1)). (Phase 5) universal ORIGIN-PIN: gen-1 at K-fixed origin pins the first column in ν/CKM/masses (form dominance across sectors, target-innocent). (ν gap) SIGN of Im(ε)=CP direction: HYPOTHESIS sign=substrate V−A chirality (F237), target-innocent, DUNE-falsifiable → if V−A→Im ε<0→J<0→δ=197° DERIVED. 1/g=grounded-lead. Nothing banked.
