# F992 — First gate (K1529), done: the poset's **comparability rule is DERIVED from D_IV⁵ rep theory, blind** — it is the **B₃ (so(5,2)) dominance order = reachability under the raising operators E_α** (Casey's linear-algebra directive, literally: the future of an event is what you can reach by commit-raising). I pin this object and do **not** read its dimension — that is Grace/Elie's blind measurement against Cal's committed bar. Every rule considered is reported; the derived rule is target-innocent (the root system, not tuned to 4).

## Owned first: the "reads four" is dead

My F991 d_eff≈4.06 does **not** survive — Grace showed a uniform 5D R×S⁴ sprinkle at the same aspect ratio also gives 0.098, so the global ordering fraction reads the **region (aspect ratio), not the dimension**; Cal showed the multiplicity poset was selected after the naive d≈1.3, so it wasn't object-innocent; and dimension + not-KR both ride the one ordering fraction (count-once). I held it at "signal," which was right, but the signal itself doesn't survive. Not evidence. Does not propagate. This gate is the correct replacement.

## The gate: pin the object *before* measuring (the free knob is the answer)

F844/Toy 5087 measured the naive commit poset at **d ≈ 1.3** — a near-total order (time is 1-D by design). The dimension only climbs when the multiplicity copies become **incomparable**, and the comparability rule among copies is a **free knob** F844 never fixed (fully comparable → d low; incomparable → d climbs). **The rule is not a detail — it is the answer.** So it must be *derived*, blind, not chosen to hit 4.

## The derived rule (linear algebra on D_IV⁵, target-innocent)

The commit-mode events are **weight vectors** of D_IV⁵'s structure algebra so(5,2) (complexified B₃, rank 3: two SO(5) Cartan directions e₁,e₂ + the SO(2) charge e₃). The causal order is:

> **w₁ ≺ w₂  iff  w₂ − w₁ lies in the positive-root cone of so(5,2)** — i.e. w₂ is reachable from w₁ by successive **raising operators E_α**. Simple roots α₁=e₁−e₂, α₂=e₂−e₃, α₃=e₃; the 9 positive roots are eᵢ, eᵢ±eⱼ.

This is the **future light-cone as raising-operator reachability**: the non-compact raisers 𝔭⁺ advance the SO(2) charge (time) while moving the SO(5) weight (space) — exactly the light-cone moves — so the B₃ dominance order *is* the causal order, derived from the geometry, not posited. It is **object-innocent**: the so(5,2) root system is fixed by the five integers, with no reference to a target dimension. (This makes precise §314/K1224 — "does the commit eigenvalue-ordering coincide with the weight-poset order?" — the weight-poset order being exactly this dominance order.)

## The pinned object + the rules tried (blind; the number tried is part of the result)

- **PINNED (R2, derived):** the commit-mode weight lattice under B₃ dominance. Reference cell (fiber Λ*(ℂ⁵), 30 distinct B₃ weight-events): 356 of 870 directed pairs comparable. Grows by extending the commit sequence (more weight-events); Grace/Elie measure the **aspect-independent local-Alexandrov MM dimension** on it, cardinality-matched, converging to d=4 (descent derived) vs d=5 (posit).
- **R1 (rejected as the rule, kept as endpoint):** full across-step comparability — F844/5087's near-total order, d ≈ 1.3. Not the rep-theory rule; it is the max-comparability knob endpoint.
- **R3 (rejected):** all copies within a step mutually incomparable — d climbs *by construction*; a knob, not derived.
- **R2 is the only target-innocent rule** (the root system); R1/R3 are the knob endpoints that bracket it.

**I do not read the dimension.** I note only, for honesty, that B-type dominance carries *substantial* comparability (e.g. e₁ ≻ e₂ via α₁) — so R2 could land anywhere between R1's 1.3 and 4; **the measurement decides, and I did not tune it.** If it lands at 4 "easily," look harder at the construction (Cal's bar); if it lands low, the descent stays the honest posit.

## Caveat I flag for the measurement (Cal's regular-lattice catch)

The pinned object is a **deterministic, regular** structure (the weight lattice), not a Poisson sprinkling. Per Cal's caution, a regular lattice can carry lattice-frame artifacts, and the MM dimension of a regular dominance order may be biased by the regularity. So the read must be the **aspect-independent local-Alexandrov** dimension (Grace's plan), and we stay alert to lattice-frame bias — a real subtlety, flagged before the number.

**Lyra, 2026-08-15 10:30 EDT. First gate done: comparability rule DERIVED blind = B₃ dominance / raising-operator reachability; object pinned; rules R1/R2/R3 reported; dimension NOT read (Grace/Elie blind); regular-lattice caveat raised. RUBRICS below.**

## RUBRICS Layer-2 done-bar
- [x] Owned the dead "reads four" (aspect-region artifact, not object-innocent, count-once) — does not propagate.
- [x] Derived the comparability rule from D_IV⁵ rep theory, blind: B₃ (so(5,2)) dominance = raising-operator reachability (Casey's linear-algebra directive, literal).
- [x] Pinned the object (commit-mode weight lattice under B₃ dominance); reported R1/R2/R3 with the numbers tried; R2 is the sole target-innocent rule.
- [x] Did NOT read the dimension (Grace/Elie's blind measurement vs Cal's committed bar); flagged only that R2 could land 1.3–4, measurement decides, not tuned.
- [x] Raised the regular-lattice / lattice-frame caveat (Cal) before the measurement. Held the mechanism-theory (why 4) until the test lands. Nothing pushed. CP existence-only.

## Handoffs
- **@Grace — measure the pinned object, blind.** R2 = the commit-mode weight lattice under B₃ dominance (raising-operator reachability). Grow it (extend the commit sequence), read the **aspect-independent local-Alexandrov MM dimension**, cardinality-matched, d=4-vs-d=5 reference at matched sub-interval size. **Caveat:** it's a *regular* lattice — watch for lattice-frame bias; the local read is the aspect-safe one.
- **@Elie — second-source on the pinned R2 object** with the mesoscale-smeared BD (d scanned as output, target ESU R=6/a², not flat). Same object, independent estimator.
- **@Cal — the object is pinned blind before the read; the rule is derived (B₃ dominance), not chosen.** R1 (full comp) = 1.3, R3 (all-incomp) = knob, R2 (derived) = measured. Your blind bar (3 copies + full comp → 1.3–2) is on record; R2 is the one to judge. Regular-lattice caveat raised.
- **@Casey — the linear-algebra reminder was exactly the key.** The question "which pairs of events can influence each other" is not a modelling choice — it's *reachability under the raising operators of D_IV⁵*, the root-system dominance order, fixed by the five integers with nothing tuned. That pins the object honestly, before we measure its dimension. I did not read the dimension — that's the blind measurement, and it can land anywhere from 1.3 (a posit) to 4 (the manifold), and I did not put a thumb on it. The #3 fermionic/Connes narrative runs in parallel. Nothing pushed.
