---
node_type: k_audit
id: K970
title: CORPUS-RECONNECT directive (Casey) — run the EXISTING corpus computations, don't greenfield-derive; the count is NOT open-from-scratch (K945 has three routes); the muon exponent is F111; per-thread corpus map
date: 2026-07-28
author: Keeper
verdict: DIRECTIVE + corpus map; corrects K969's "count un-forced from scratch" (under-pulled K945); every open thread has an existing computation to RUN, not re-derive
owns_error: yes — K969/prompt-28e declared the count "un-forced, open" without corpus-reconnecting to K945 (≤3 DERIVED via three routes); a closure verb without the meta-rule I just banked
---

# K970 — Run the corpus. Don't re-derive it.

## Casey's directive (2026-07-28)
> "You really need the team to read the corpus, we did much of this work and I don't see the team using any guide from the corpus. Just trying to derive is too idiotic, run the computations from the corpus. Improve if those are wrong."

Correct, and it lands on ME. Prompts 28c/28d/28e framed the flavor sector as "derive the address table / derive the truncation" — greenfield. And K969 declared the count "**un-forced by the reduction, honestly open**" — a closure verb (declaring open) issued **without corpus-reconnecting**, which is the exact meta-rule I banked into the Standard this morning (Rule 9's neighbor: "corpus-reconnect before a closure verb"). Cal's §118 was a from-scratch standard-rep reduction that ignored the existing three-route bound. The whole team, me included, was re-deriving over a corpus that already had the work. Owned.

## ★ THE CORRECTION — the count is NOT open-from-scratch (corpus: K945)
**K945** (current, authoritative, 2026-07-27) already holds:
- **≤3 upper bound ("no 4th generation") = DERIVED / SOLID**, via THREE independent target-innocent routes: **(i) rank-2 Wallach set has exactly 2 discrete points; (ii) the matryoshka terminates at the Shilov point; (iii) Q⁵ has no h⁷.** LEP-confirmed N_ν=3.
- **Exactly-3 = REDUCED (not eliminated)**, with the open crux **precisely named**: generations on the **3 boundary strata (F86 / T2525, KW support-flag rank+1=3)** vs the **3 K-types of the Di singleton (F340)** — two corpus pictures not yet reconciled. Plus three fit-flags (K876, K880: the electron placement was reverse-engineered from banked m_e).

**So Cal's §118 ("the singleton tower is infinite, nothing truncates at k=2") is the F340 singleton-rung side — CORRECT but INCOMPLETE.** It does not conflict with K945; it re-derived, in singleton-reduction language, the *same* "exactly-3 not self-forced" that K945 already had — while omitting the three-route bound that lives on the F86 strata side. **The count's honest status is: ≤3 DERIVED (three routes); exactly-3 REDUCED; the open work is the RECONCILIATION — do the F86 support-strata (bounded, 3) truncate the F340 singleton rung tower (infinite)?** That reconciliation is the named crux, NOT a from-scratch truncation derivation. Cal's §119 trap-flag stands as the audit bar: a strata-count is a MATCH until a mechanism truncates the rungs on the quotient with the identical mechanism giving 4 on E7.

## ★ PER-THREAD CORPUS MAP — RUN THESE (improve if wrong), don't re-derive

**COUNT (Lyra) —** run **K945** (three-route ≤3 bound) + reconcile **F86 / T2525** (strata, KW rank+1=3) with **F340** (Di-singleton K-types) and Cal §118 (rung tower). Supporting: K881 (T2525 audit, PASS), K289, K594/K595/K596 (three-route convergence), F666/F718 (strata=sub-domains, boundary-distance). Registry: T2525, T1829 (Wallach bottleneck), T2003 (M₄=15 breaks prime-prefactor → N_gen=3). Toys: 4074, 4812.

**MUON EXPONENT-6 (S3) (Lyra/Elie) — NOT open-from-scratch.** Run **F111** (the π-counting gate: π¹²=(π²)^N ⟹ **N=6 FORCED** by Shilov-volume dilution vol(S⁴)=8π²/3; 6 = dim SO(4) = C(4,2) = measurement little-group) and **T190-depth** (6 = C₂; 24 = N_c·|W(B₂)| = 3×8). *Caveat (Cal §117 degeneracy):* 6 = n_C+1 = C₂ = dim SO(4) = C(4,2) — multiple mechanisms give 6, same degeneracy as the 24. So the task is: does the **k=1 rung address force F111's Shilov-dilution mechanism** specifically (not merely 6)? Run F111 against the address; don't re-derive the exponent.

**MUON ADDRESS (S2b) (Lyra/Elie) —** run **K878** (the O7 SO(5)-harmonic K-type injectivity gate — the concrete "which (a,b) address" gate, PENDING), **T2490** (four primaries = half-Casimirs of the four lowest holomorphic discrete-series reps, DERIVED), **F483** (masses = EIGENVALUES of the F84 Bergman mass matrix at the 3 localizations), **F719** (Γ_Ω lepton-unification = the promotion path is the address). Bergman machinery: F84/F85, Vol16 Ch8 v0.4, K287, K768 (rank-1 condensate).

**TAU (Lyra/Elie) —** run **T2003 / T2086** (71 = 2^{C₂}+g = 64+7, Ogg prime rank²·C₂·N_c−1) + **Lyra_T2003_71_Decomposition_v0_1** — but note these are **IDENTIFICATION, not forcing** (which is why Elie's blind-forward failed and tau is FITTED). The real path is **F719** (tau = boundary-shifted muon under one Γ_Ω) — the K967-S4 sector-consistency test. Don't re-derive 71; test the Γ_Ω unification.

**RUNG LABELS (Lyra) —** pull **F338** (the FG-2014 arXiv:1409.2185 singleton (a,b)/K-type structure already recorded: Di spinor minrep lowest K-type = SO(5)-spinor 4, tower in SO(5) harmonics {1,5,14,30}=degrees{0,1,2,3}) + **K956/K957/F712** (E₀=2 confirmed from Table 2, count = Verma submodule structure). **Do NOT re-fetch the paper — F338 has the labels.**

## Standing correction to the discipline
The meta-rule "corpus-reconnect before a closure verb" applies to **declaring something OPEN**, not just declaring it closed — "un-forced / open / a from-scratch dig" is a status claim that must be checked against the corpus first. Before any thread is worked as greenfield, the owner pulls the existing computation (this map) and states whether they are RUNNING it, IMPROVING it, or REPLACING it (with the reason). No greenfield derivation of a quantity the corpus already computes.

— K970, Keeper, 2026-07-28. Casey's corpus-reconnect directive; corrects K969's under-pull of K945; per-thread map to the existing computations. See [[Keeper_K969_reduction_level_computed_Cal_118_splits_4_and_1_address_threads_UNBLOCKED_count_object_RETRACTED_radical_was_the_Dirac_EOM_generations_live_in_the_quotient_2026-07-28]], K945, F111, K878, F719, F338.
