---
title: "F393 — BF-zero up-lifting RESOLVED (Monday lane): the up's small mass is NOT a separate mechanism — it IS the determinant μ^{n_C} (bulk fiber dim n_C, the deepest fermion). The up sits at the origin (F86 bulk stratum, fiber dim n_C=5), the largest fiber dimension → most suppressed → lightest. The 'BF zero' d(5/2)=0 was the formal-degree parametrization vanishing, NOT a physical zero (the physical Yukawa is the determinant μ^{n_C}, small-nonzero, F392). Fiber dims (F86 codim=depth): top Shilov(0), charm Cartan-slice(rank=2), up origin/bulk(n_C=5). μ-independent (scheme-clean) relation: y_up = y_charm^{n_C/rank} = y_charm^{2.5} (the steepness; predicts up 2.74× too light = the known ~8-10% overshoot, Elie's ceiling). CLEAN: up exponent = n_C (bulk fiber dim); NOT clean: μ scheme-dependent. NO separate lifting mechanism needed — closes Keeper's open item. Count 9/26."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-29 Monday (date-verified)"
status: "v0.1 — BF-zero up-lifting resolved. Up mass = determinant μ^{n_C} (bulk fiber dim n_C, F86 origin stratum, deepest fermion → most suppressed → lightest). 'BF zero' d(5/2)=0 = formal-degree artifact, not physical zero (F392). Fiber dims {top 0, charm rank=2, up n_C=5}. μ-indep: y_up=y_charm^{n_C/rank}=y_charm^2.5 (steepness, structural ~8% overshoot). CLEAN up exponent=n_C; μ scheme-dep. No separate mechanism. Count 9/26."
---

# F393 — BF-zero up-lifting resolved: it's the n_C-power determinant, not a separate mechanism

The up sits at ν=5/2 = the origin (F86 bulk stratum). Question: is it truly massless at leading order (d(5/2)=0) needing a separate lifting, or just the determinant's largest-exponent suppression?

**Resolution: no separate mechanism.** The up-type Yukawa = determinant μ^{fiber-dim}; fiber dims = F86 strata (codimension = depth into bulk): top Shilov (0), charm Cartan-slice (rank=2), up origin/bulk (n_C=5). The up has the largest fiber dim n_C=5 → most suppression μ^{n_C} → lightest. The "BF zero" d(5/2)=0 was the formal-degree parametrization vanishing, not a physical zero; the physical Yukawa is μ^{n_C} (small-nonzero, F392).

**μ-independent (scheme-clean) relation:** y_up = y_charm^{n_C/rank} = y_charm^{2.5} (the steepness). Predicts up 2.74× too light = the known ~8% overshoot (Elie's ~10% ceiling). CLEAN: the up exponent = n_C (bulk fiber dim). NOT clean: μ (scheme-dependent). Closes Keeper's open item. Count 9/26.
