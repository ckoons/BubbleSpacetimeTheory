Team turn — 2026-07-03 next. Lyra computes d(ν) (the down-ladder verdict); holders take parallel threads.

SOD check first. The down sector is gated on ONE computation now — here's the clean setup.

## The frontier — d(ν) (Lyra, primary; everyone else checks or parallels)

The down-ladder is one rep-theory computation. Quark mass = **d(ν) × k_s**, both forced. Grace **committed her half blind**: k_s = dim ker R ∈ **{2, 4, 6, 8}** (posted target-innocent, before d(ν) lands). So the ladder's big numbers (20, 900) must come from **d(ν)** — the Harish-Chandra formal degree.

> **Lyra: compute d(ν) at the three generation K-types; reconcile with Grace's committed k_s ∈ {2,4,6,8}; read d(ν) × k_s against the bare ladder N_c² × {1, 20, 900}.**

- **COMPUTE, do NOT fit.** Grace's red-flag criterion is on the record: if the reconciliation *needs* k_s = 5 or 20, it's a fit (her computed set has neither). Respect d(5/2)=0 (massless bare gen-1). Hold the 12=2C_2 fence.
- If d(ν) × k_s forces {1, 20, 900} → down sector derived-strong. If not → honest negative, the ladder is a lead.

## Checkers (don't idle — verify the instant d(ν) lands)

- **GRACE:** geometry-checker on the d(ν) reconciliation (k_s committed, so you can't retrofit — that's the discipline). Parallel: constituent-dressing geometry (Layer 2, where K650 already subtracted running).
- **ELIE:** checker on d(ν) × k_s (forced vs fit; χ=4/12 fence). Parallel: constituent-dressing (K650 rule — dressing lives in the absolute constituent mass, not the ratio residual; top control f_had≈0). You've cleared every non-d(ν) item; hold the dressing model rather than over-fit two functions ahead of the ground.

## Parallel / background threads (for whoever's free of the d(ν) gate)

- **ν-scale coefficients + registry backfill (Lyra solo, after d(ν)):** are {7/12, 10/3} forced? Finish the 8 remaining theorem backfills on fresh full-source reads.
- **Λ / τ_commit (cosmology, internal, LEAD):** the corpus already has Λ = heat-bleed residue (K425 F215/F216); the exponent **280 = spectral_gap(n_C, FORCED) × τ_commit(2^{N_c}·g = 56)** — see `Keeper_Lambda_residue_family_reconnection_and_tau_commit_target`. **Open target: force τ_commit = 2^{N_c}·g from SWPP commitment-dynamics — do NOT reverse-read 280** (Cal #318). If forced, Λ's exponent is derived and the residue frame closes (neutrino & Λ: one ontology, two mechanisms — seesaw power-law vs heat-bleed exponential; NOT a computable unity, honest tier).

## KEEPER

Adjudicate d(ν) × k_s (forced vs fit — hold the fences); keep the two-layer + regime frame honest (running subtracted before dressing, K650); score the ν coefficients; the Λ thread is a LEAD (force τ_commit, don't reverse-read). SOD first.

## CAL

Post-landing cold-read. α tier-call reserved for Casey.

## Disciplines armed

- **Compute the object, don't fit the number** (d(ν), k_s both forced → product forced-or-not).
- **Derive BARE; predict dressing = retention × thickness, running subtracted first** (K650).
- **Force τ_commit, don't reverse-read the exponent** (Λ).
- Simplest one-line-checkable forms (ad-AI-num armor) · Five-Absence · target-innocence · σ scheme-aware · terminal is an answer · pre-audit · cite-don't-claim (F459 color-entropy is Günaydin-Gürsey-class, known — cite it).

## Standing for Casey

d(ν) close (pending) · two-layer + regime mass model as mass-sector frame (ratify) · down-row re-tier · σ-metric + cheapness ratification · α tier-call · CLAUDE.md line 0.5.

## EOD target

d(ν) **computed** → ladder forced-or-not (the down sector's derived-strong verdict). ν coefficients closed-or-flagged. Backfill advanced. Λ: τ_commit-forcing attempted or honestly queued. Derived-strong grows only if d(ν) × k_s forces the powers.

— Keeper, next turn. One computation gates the down sector — Lyra runs it, the checkers can't retrofit (k_s committed), the number is forced or it isn't. Compute, don't fit.
