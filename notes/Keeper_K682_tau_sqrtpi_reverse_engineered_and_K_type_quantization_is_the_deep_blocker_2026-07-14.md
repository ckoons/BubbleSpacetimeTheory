# K682 — Two corrections from the Engine-C corpus read: (1) the τ √π is reverse-engineered, not forward — demote m_τ/m_e; (2) the K-type quantization is the deep shared blocker, and many mixing/mass "derivations" are conditional on it.

**Keeper | 2026-07-14 | The Engine-C deep dive surfaced two things the scorecard/board were over-stating. Both are honest demotions. This is the audit working — the corpus's own documents concede both.**

## Correction 1 — the τ √π was REVERSE-ENGINEERED. Demote m_τ/m_e from "PREDICTED (forward) 0.0000%."
- **What the scorecard said:** m_τ/m_e = 49·71 − √π, "PREDICTED (forward), −√π now forward via the 3-ball, 0.0000%."
- **What the corpus actually shows (F516, F157):** the bare count 49·71 = 3479 is **7.7σ HIGH** vs observed 3477.23; the **−√π was ADDED to fix that residual**, then retro-justified as the Gindikin cone-tip residue. F516's own words: "identified/near-forced form, √π principled (not a fudge)… **NOT yet a forced derivation**… the additive Shilov leg is the **weakest** — it needs the explicit boundary tiling integral, **not the count assertion**." Casey's 3-ball / forward framing is a *plausible integral partition asserted to match the known answer*, not an integral that produced it.
- **The honest split:** the √π's PRESENCE is forward (F157: Gindikin Γ_Ω carries a half-integer π ⟺ a=N_c=3 odd — target-innocent, part of the odd-dimensionality unification). The exact COEFFICIENT (−√π·1, and the 343 + 3136 − √π decomposition) is the **open Engine-C integral** — unevaluated, fit-to-residual.
- **VERDICT: m_τ/m_e is NEAR-FORCED, not "PREDICTED forward 0.0000%."** The seat (ν=0) and exponent (p=n_C) are forced; the √π-presence is forward; the exact coefficient is fit-then-justified and waits on Engine C. **The "0.0000%" is the fit-target, NOT an evaluated result** — it matches by construction. Scorecard v0.3: demote the row, flag the coefficient as the open Engine-C integral. (m_τ/m_e stays an excellent LEAD — the structure is principled — but "forward-exact" over-states it.)

## Correction 2 — the K-TYPE QUANTIZATION is the deep shared blocker; "derived modulo the K-type address" is a conditional derivation dressed as forward.
- **The open core (CLAUDE.md's own June-9 header):** "which discrete (a,b) address [each particle] sits at on discrete series" is UNDETERMINED. The generations are currently **ASSIGNED** (real radii {0, 0.5, 0.82} on the ν-ladder {5/2, 3/2, 0}), not forced by a quantization condition. What IS forced: the count (3 = rank+1, Korányi-Wolf) and the ordering (τ>μ>e, monotone in depth); gen-1 at r=0 (3 ways). What is NOT: which discrete address each intermediate generation occupies → the exact norm N_i.
- **The scope (why this is THE blocker):** per K398, the K-type quantization is the **shared gate for M_ν, the CKM/quark #418 sector, AND the τ √π coefficient — simultaneously**. Per K291, it is the difference between **~2 and ~15 forced SM parameters**. It has resisted the team for months (4 marked dead ends, a walked-away C₂ back-fit).
- **The discipline flag — the "conditional derivation" pattern.** Multiple current "derivations" are actually conditional on this one unsolved research problem, and say so in the fine print:
  - **Cabibbo 1/(2√5)** (F536): "V_us = 1/√20 is EQUIVALENT to N₂ = 20^{−1/5} — it does NOT independently force N₂ from the geometry." Re-expresses a lead through the derived form.
  - **V_ub = N₃^{5/2}** (F536): "form-forward, magnitude-open — derived modulo the gen-3 K-type address."
  - **The neutrino coefficients {7/12,10/3}**: fitted; the fiber-overlap route (the only lead) needs the addresses.
  - **The generation depths {r_i}** themselves: assigned to match data.
- **VERDICT: these are conditional-on-K-type derivations. Present them as such** — "derived given the K-type address; the address is the open core." Do NOT let the scorecard/papers read them as forward. The K-type quantization (a selection principle mapping particle → discrete (a,b) → ν-stratum → norm N_i) is the single highest-leverage open RESEARCH problem in the program — distinct from Engine C (a bounded build).

## Engine C — bounded build, but it is the FIRST real test of the normalization layer
- Engine C IS a bounded capability (integrand N(w)^{n_C/2} known; radial norm ‖z^n‖²=π·B(n+1,α+1) exact; worked precedent toy_4403 = exact Shilov Beta/Gamma moments). BUT: every banked result to date (C₃=6π⁵, muon, mass-gap) survived because it was a RATIO or Schur-scalar where the **absolute normalization CANCELED**. Engine C is the first task needing the absolute constant — so it is also the **first real falsifier of BST's normalization conventions** (Szegő-probability vs Bergman-bulk vs vol(S⁴)⁶ are genuinely different objects, F343). If the direct integral disagrees with the Schur-shortcut answers, it exposes a convention problem, not just a missing digit. **Build it — it will either bank the light-quark anchor + τ coefficient forward, or usefully break the normalization layer. Either is valuable.** But it gives absolute scale only for addresses you ALREADY know — it does NOT close the mixing sector (that needs K-type quantization).

## Net direction
- **m_τ/m_e → NEAR-FORCED** (√π coefficient = open Engine-C integral; not forward-exact). Scorecard v0.3.
- **Two engines, correctly separated:** Engine C = bounded build (absolute scale for known addresses; first normalization-layer test). K-type quantization = the deep open research problem (the addresses themselves; ~13 parameters gate on it). Don't conflate.
- **Discipline: flag every "derived modulo the K-type address" as conditional, not forward** — Cabibbo value, V_ub magnitude, ν-coeffs, generation depths. The K-type quantization is where the honest frontier actually is.

— Keeper K682, 2026-07-14. (1) τ √π reverse-engineered (added to fix 7.7σ, retro-justified) → m_τ/m_e NEAR-FORCED not forward-exact; the √π PRESENCE is forward (odd-dim), the coefficient is the open Engine-C integral. (2) K-type quantization = the deep shared blocker (K398: gates M_ν+quarks+τ; K291: ~2→~15 params); Cabibbo/V_ub/ν-coeffs/depths are conditional-on-it, not forward. Engine C = bounded build + first normalization-layer falsifier. See [[BST_Odd_Dimensionality_Unification_2026-07-14]].
