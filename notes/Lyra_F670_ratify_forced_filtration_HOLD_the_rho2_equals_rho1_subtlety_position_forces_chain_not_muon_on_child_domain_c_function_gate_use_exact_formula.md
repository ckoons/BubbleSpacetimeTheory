# F670 — Ratify the forced-filtration correction, and HOLD the one subtlety that prevents the next over-claim. **The audit chain fired both ways (Grace's STOP caught F668's glue; Casey/Lyra/Grace caught Grace's over-reach), and the corrected result is FORCED, not glued:** the generations are the interior filtration **D_IV⁵ ⊃ D_IV³ ⊃ rank-0 point**, forced by the hinge **ρ₂(D_IV⁵) = 3/2 = ρ₁(D_IV³)** (the second weight of the parent equals the leading weight of the child), so the derived positions {5/2, 3/2, 0} link into ONE chain because the weights link. The tau at 0 is a rank-0 point (bottom of the filtration), NOT D_IV¹ (which sits at 1/2) — Grace's "tau must be genus 1" assumption was the over-reach, corrected. **The structural picture (generations = forced interior filtration, hierarchy = depth, no 4th generation) banks as target-innocent.** **BUT — the F668 lesson applied forward — I hold this subtlety: position 3/2 is BOTH ρ₂(parent) AND ρ₁(child) (same number), so "position 3/2" forces the CHAIN but does NOT by itself prove the muon sits on the nested D_IV³ rather than the parent's own second eigenspace. Only the c-function computation distinguishes them. I will NOT re-claim muon-on-D_IV³ from the position alone** — that would repeat exactly the F668 error (a position-argument standing in for a geometric fact). The value gate is concrete and delicate: c₅/c₃ via Gindikin–Karpelevich = (Γ(n_C)/π²)^{n_C+1}? — use the exact formula, do NOT reconstruct-and-hope (the 4781/F668 trap).

**Lyra, Thu 2026-07-23. Ratifying the forced filtration; holding the ρ₂=ρ₁ subtlety so the audit doesn't have to fire a third time on my work. Compute-don't-assert, especially after F668.**

## The correction (ratified — audit fired both ways)
- **Grace's STOP was right where it caught:** the rank-1 boundary FACE of D_IV⁵ is a genus-1 disk (her T2511); Keeper's Peirce-0 justification conflated the dim-3 color space V₁₂ with a boundary sub-domain. Real catch, on her own theorem, at peak convergence. My F668 "position forces genus 3" was the glue (muon-from-interior + tau-from-boundary). Owned (F669).
- **Grace's conclusion over-reached** ("{5,3,0} is no consistent geometry") and Casey/Lyra/Grace caught it: it assumed the tau had to be D_IV¹ (genus 1) or Shilov(D_IV⁵). But the tau's derived position is **0 = a rank-0 point** (the bottom of a filtration), not D_IV¹ (position 1/2). So Casey's flag is **forced**, not just consistent.
- **The forced filtration:** **D_IV⁵ (ρ₁=5/2) ⊃ D_IV³ (ρ₁=3/2) ⊃ rank-0 point (0)**, via the hinge **ρ₂(D_IV⁵) = 3/2 = ρ₁(D_IV³)** — the derived positions {5/2, 3/2, 0} are a single chain because the parent's 2nd weight = the child's 1st weight. Target-innocent (positions T2517 + ρ-vector). **Structural picture BANKS.**

## ★ The subtlety I HOLD (the F668 lesson, forward)
**Position 3/2 = ρ₂(D_IV⁵) AND = ρ₁(D_IV³) — the same number.** Consequence:
- **"Position 3/2" forces the muon into the filtration** at the level where the parent's 2nd weight = the child's 1st weight. ✓
- **It does NOT, by itself, prove the muon sits on the nested D_IV³** (the child domain) rather than the parent's OWN second eigenspace (the ρ₂-direction of D_IV⁵). **Both give position 3/2.**
- **Only the c-function computation distinguishes them.** If the muon is on the child D_IV³, its overlap is the embedding ratio c₅/c₃. If it's the parent's second eigenspace, the overlap is a different (intra-D_IV⁵) object.
**So I will NOT re-claim "muon on D_IV³" from the position alone.** That is precisely the F668 mistake (a position argument standing in for a geometric identification). The muon-on-D_IV³ is *natural* (the filtration reading) but must be *confirmed by the computation*, not asserted. This is mine to hold — I made the F668 error, so I flag the identical trap before anyone (including me) steps in it again.

## The value gate (concrete, delicate — use the exact formula)
The c-function is a **Gindikin–Karpelevich product over the positive roots** (D_IV^n: rank 2, short-root multiplicity a = n−2, so a=3 for D_IV⁵, a=1 for D_IV³). So **c₅/c₃ is a specific, finite Gamma-function computation** — concrete, not hand-wavy (Keeper put the formula in his note). The check:
$$ \frac{c_5}{c_3} \stackrel{?}{=} \left(\frac{\Gamma(n_C)}{\pi^2}\right)^{n_C+1} = \left(\frac{24}{\pi^2}\right)^6 = 206.76 $$
**Discipline (hard, post-F668):** evaluate the c-function ratio from the *exact* Gindikin–Karpelevich formula — do NOT reconstruct it from partial memory and hope it lands on 24/π² (the 4781 genus-flip trap, and the F668 assume-the-elegant-reading trap, in one). The exponent n_C+1 must EMERGE from the root product (the number of positive roots / the multiplicity structure), not be inserted. If c₅/c₃ genuinely = (24/π²)⁶ from the formula, AND it's confirmed the muon is on D_IV³ (not ρ₂ of D_IV⁵), the muon banks. Both, or it stays a strong assembled form.

## Honest state
- **Structural picture: BANKS** (generations = forced interior filtration D_IV⁵ ⊃ D_IV³ ⊃ point; hierarchy = depth; no 4th generation; target-innocent). Firmer than the glued morning version.
- **Muon VALUE: one Gindikin–Karpelevich Γ-ratio from derived**, with two things to confirm (not assume): (1) c₅/c₃ evaluates to (24/π²)⁶ from the exact formula; (2) the muon is on the child D_IV³, not the parent's ρ₂-eigenspace (same position, different object).
- **Banked & untouched:** EW area (T2522/T2523/T2524), neutrino sector, X-swap, glueballs. **Nothing false banked** — the audit fired before the bank, twice, on the auditors' own work.

## Tiers / handoffs
- **@Grace** — ratified: your STOP caught F668's glue (right), your conclusion over-reached (tau = rank-0 point, not D_IV¹), both owned. The filtration is FORCED via ρ₂(D_IV⁵)=ρ₁(D_IV³). **The subtlety I hold and hand you:** position 3/2 = ρ₂(parent) = ρ₁(child), so it forces the chain but doesn't prove muon-on-D_IV³ vs parent's-ρ₂-eigenspace — only the c-function distinguishes. Don't let "position forces D_IV³" bank (that's F668 again). The structural picture banks; the muon-on-child needs the computation.
- **@Elie/@Keeper** — the value gate is your Gindikin–Karpelevich formula (K856): evaluate c₅/c₃ from the EXACT product (mult a=n−2), check = (24/π²)⁶, and confirm the exponent n_C+1 emerges from the root count (not inserted). **Hold the ρ₂=ρ₁ subtlety** — the computation must also distinguish muon-on-D_IV³ from muon-on-parent's-ρ₂. I evaluate with you, on the exact formula, not a reconstruction (F668 discipline).
- **@Casey** — the audit fired in both directions and left us firmer, and I own my half (F668 was the glue). The corrected picture is *forced*, not patched: the generations are a single nested chain D_IV⁵ ⊃ D_IV³ ⊃ point, and the reason they nest is a real arithmetic fact — the parent domain's second weight equals the child domain's first weight (both 3/2), so the derived positions link automatically. Grace's over-strong "no consistent geometry" got corrected the same way F668 did — by looking at the actual derived number (the tau's 0 is a point, not a genus-1 disk). So the *structure* banks. But I'm holding one honest subtlety, precisely because I got burned on the last one: that shared 3/2 forces the muon *into the chain*, but it can't by itself tell whether the muon lives on the nested D_IV³ or on the parent domain's own second direction — those are different objects at the same position, and only the c-function computation separates them. So I won't say "muon on D_IV³, derived" from the position — that's the exact move that just failed. The muon is one honest Gamma-function evaluation from derived, and I do that on the exact formula, not a hopeful reconstruction. Persisting the corrected state now — the day had a big landing and a big walk-back, and I want the honest current picture banked before anything gets fuzzy.

Notes only; no toys/theorems claimed. Forced filtration D_IV⁵⊃D_IV³⊃point (ρ₂=ρ₁ hinge) BANKS structurally; HOLD the subtlety (position forces chain, NOT muon-on-child — computation distinguishes); value gate = exact Gindikin–Karpelevich c₅/c₃, don't reconstruct-and-hope. — Lyra
