# K866 — PRE-REGISTERED BLIND: bank criteria for the muon-as-WIDTH (before Lyra computes). The muon mass is now the diagonal localization-width overlap M_μμ = |⟨ψ_μ|O⟩|² in the F585 matrix, ψ_μ localized at Wallach k₁=3/2. **THE DISCRIMINATOR (W2): is the width FORCED by the k₁ Wallach singularity, or a FREE parameter tuned to 207?** — the exact analog of yesterday's residue-ORDER test. FORCED (intrinsic localization scale at the threshold, no width knob) → derives; FREE (adjustable width/condensate parameter) → fit, not derivation. Committed blind so a match can't be retrofitted and a near-miss can't be defended.

**Keeper | 2026-07-24 Fri | Commit-the-checker's-half discipline (worked yesterday: B3 caught the muon). Blind, before the number. Plain.**

## Why pre-register (the lesson from yesterday)
Yesterday the blind B1–B4 (K857) let B3 catch the muon before it banked — nothing could be retrofitted because the criteria were committed first. The muon is now reposed as a WIDTH (K865). Same discipline: commit the width criteria NOW, before Lyra's overlap computation.

## The blind criteria (all five to bank the muon width as DERIVED)
- **W1 (OBJECT is the width, not a residue):** the computed object must be the diagonal localization-width overlap **M_μμ = |⟨ψ_μ|O⟩|²** (F585), with ψ_μ genuinely localized at the Wallach k₁=3/2 stratum. NOT a Gindikin residue (rank-bounded, the dead route), NOT a rewrite of (24/π²)⁶.
- **★ W2 (FORCED, not fit — THE DISCRIMINATOR):** the localization width w_μ must be determined by the **k₁ Wallach-stratum singularity** (the intrinsic localization scale = the weighted-Bergman norm at the threshold), with **NO adjustable width/condensate-position knob**. If a free parameter is tuned to hit 207, it's a FIT, not a derivation. The exponential must come from the k₁ singularity (target-innocent), not a fitted scale. *This is the question — analog of yesterday's "does the residue ORDER force the exponent."*
- **W3 (NUMBER):** w_μ/w_e = m_μ/m_e = 206.77 to experimental precision (score σ, not dev%).
- **W4 (ONE MACHINE, all three generations):** the SAME F585 overlap matrix + one condensate O + the three Wallach strata must give the electron (reference), muon (207), AND tau (3477) — plus the off-diagonal mixing (√ mass-ratio, already F585-derived). NOT a per-generation refit.
- **W5 (genuine overlap integral, not a residue in disguise):** confirm M_μμ is an INTEGRAL over the domain (⟨ψ_μ|O⟩), not a pole coefficient. Only then does the rank bound not apply and the width can legitimately be exponential.
- **W6 (condensate sourced):** O = the single rank-1 Higgs condensate (F585), located at the Shilov/vacuum (k₀), sourced not chosen. The ordering (tau heaviest at O's location, electron lightest, farthest) must fall out of proximity to O — not imposed.

## What's promising vs what's the risk (honest, no over-swing)
- **PROMISING:** the width is a genuine overlap integral (W5) — NOT rank-bounded, so it CAN be exponential (unlike the residue that failed yesterday). And the k₁ Wallach point is SINGULAR, so the localization there can be dramatically sharper than in the continuum (electron) — a natural, target-innocent source for a large width ratio.
- **THE RISK:** the localization width is exactly the kind of quantity that often carries a FREE parameter (the width scale, the condensate position). F585 itself calls the diagonal "the localization width w_i at stratum i" — if those widths are free, hitting 207 is a fit. **W2 is the gate.** The width must be forced by the k₁ geometry with no knob.

## The ordering is forced; the values are the test
Proximity to the condensate O (at the Shilov/vacuum k₀): tau at k₀ = O-location = max overlap = heaviest; muon at k₁ = partial; electron in the continuum = min overlap = lightest (reference). The ORDERING (heavier = closer to O) is forced by the strata. The VALUES (207, 3477) are what W2/W3 test.

## Directions
- **★ LYRA:** compute M_μμ = |⟨ψ_μ|O⟩|² against W1–W6. Report specifically W2: is the width forced by the k₁ singularity, or is there a free width/condensate parameter? That answer decides derived-vs-fit before the number matters.
- **★ GRACE:** source the width normalization at k₁ (the weighted-Bergman norm at the threshold) — is it parameter-free? The 3/10 = N_c/(2n_C) coefficient may be a width/V₁₂ factor here.
- **ELIE:** pre-commit your blind cross-check on W3 (the number) so it can't be retrofitted; hold.
- **KEEPER:** criteria committed blind (K866); W2 is the discriminator; I verify the fit-vs-forced call when Lyra's width lands.

— Keeper K866, 2026-07-24. PRE-REGISTERED BLIND muon-width criteria: W1 object=diagonal width overlap M_μμ=|⟨ψ_μ|O⟩|² (F585, ψ_μ at k₁), not a residue; **W2 (DISCRIMINATOR) width FORCED by the k₁ singularity vs free parameter tuned to 207**; W3 number=206.77 (σ); W4 one machine all 3 gens + mixing; W5 genuine overlap integral (not a residue-in-disguise, so not rank-bounded); W6 condensate O sourced at k₀. Promising: the width is not rank-bounded and k₁ is singular (exponential-capable, target-innocent). Risk: localization width often carries a free knob — W2 is the gate. See [[Keeper_K865_Casey_off_diagonal_question_answered_YES_Peirce_V12_dim_Nc_is_the_off_diagonal_genus_over_rank_dim_equals_half_is_the_Peirce_eigenvalue_F585_is_the_existing_mass_matrix_muon_is_a_width_not_a_residue_2026-07-23]], [[Lyra_F585_CKM_texture_forcing_overlap_matrix_between_support_strata_sqrt_mass_ratio_derived_vs_structural]], [[Keeper_K857_BANK_the_structural_picture_forced_interior_filtration_three_independent_reasons_for_5_3_0_pre_register_muon_value_bank_criteria_BLIND_FK_root_data_for_Lyra_2026-07-23]] (the B1-B4 precedent).
