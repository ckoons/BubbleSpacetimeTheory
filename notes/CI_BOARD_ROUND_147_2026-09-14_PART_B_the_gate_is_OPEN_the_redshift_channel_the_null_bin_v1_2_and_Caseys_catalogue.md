# ROUND 147 — PART B: the gate is OPEN on v1.1; build the redshift channel, test the null bin, freeze v1.2 for two lines — then Casey's catalogue.

**Keeper, 2026-09-14 11:04 EDT (clock, substituted).** K1899: v1.1 passes the gate on all three checks; the diff is in scope; the two executability corrections and the two added Landing-C triggers are accepted (conservative). **A catalogue may be opened on Casey's word — and not before Elie's redshift channel has its control, because the count channel alone is Landing C at Quaia depth (5758: σ_β c = 443 km/s vs the 185 bar) and the redshift channel is the power (Keeper's envelope: σ_v ≈ 55–110 km/s per bin, to be confirmed on synthetic Quaia).** Rubric cell: External 5 / External 4.

## Assignments

**Elie — E2, E3 (synthetic only until Casey's word).**
- **E2 — the redshift channel (v1.1 §4.5):** the ℓ = 1 moment of the observed-redshift field per bin under the frozen mask and weights; derive and post the per-bin factor $g_i$ from synthetic Quaia before any catalogue; positive control — a boosted synthetic sky's redshift dipole recovers the injected direction and $\beta$ in every bin; **post the per-bin σ_v** against Keeper's 55–110 km/s envelope (print the σ, then write the band — your rule of the morning). Negative control — a synthetic bulk flow (the channel's own contaminant, v.H.–D. Sec. V) must land C′ against the count channel, not A′.
- **E3 — the null bin (K1899 §5), control S5:** a boosted synthetic sky with an injected intrinsic dipole of known amplitude and direction; the top bin, where $f + T \approx 0$, must return the intrinsic dipole alone (post the recovered amplitude/direction against the injection) and the other bins must return boost-plus-intrinsic. If S5 holds, the null bin is a calibrator with no knob; if it does not, say why and the idea dies here.

**Cal — C1 (v1.2, two lines).**
- Scope declared: (i) §3.4 names Quaia's σ_z column as the paper's Table 2 has it (`redshift_quaia_err`); (ii) the null bin as a frozen use — the top bin's dipole taken as the intrinsic amplitude calibrator for the profile of 4.2 — IF Elie's S5 holds; otherwise (ii) is omitted and v1.2 is one line. Nothing else moves; Keeper's diff audit refuses anything outside (i)–(ii). New hash on the board.

**Lyra — L1.**
- The null bin as an operator statement, one paragraph on your L1 note (hashed): the bin where the Rac density's ℓ = 1 kinematic moment lies in the kernel, so the surviving ℓ = 1 moment is the Rac factor's own anisotropy; and Landing A′ in the same language — the Rac and clock moments parallel. Then Vol 3 Ch 01 (your Ch 01 Section 1 passed at 09:17; the instrument runs on every section).

**Grace — G1.**
- The A9 "PRE-REGISTERED P1 (hash 92a7ebb3…)" line, now; the v1.2 hash replaces it when it lands. Quaia's dN/dz from the paper's figure or table (no catalogue) for Elie's $g_i$ and the null-bin size; the `redshift_quaia_err` definition from Table 2 by page for Cal's (i).

**Keeper — K1.** Gate v1.2 (hash; diff against (i)–(ii)); certify E2/E3's controls; then, on Casey's word, verify the FITS header is recorded verbatim at the first opening and that no number is read before the per-bin $(x_i, \alpha_i)$ and $B_i$ are posted (v1.1 §7.1 order of operations). Fold A′/C′ into Lectures 9 and 10.

**Casey — the word.** Quaia first; the mask is v1.1 §2.2's. Elie's question from 10:55 is answered by Cal's ruling: the redshift channel is part of P1 and is being built now regardless.

## Refusal list
No catalogue opened before Casey's word AND E2's control posted; nothing in v1.1 changed outside (i)–(ii); no number from memory; `date` substituted; no reading of any published Quaia dipole analysis.

— Keeper. Prompt file for Round 147.
