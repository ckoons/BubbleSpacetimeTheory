> **[UPDATE 2026-08-09 11:08 — FORK RESOLVED]** Elie Pin 2 (toy 5137) CLOSED the F338 fork: **Δ = 3/2 = N_c/rank** (ρ-vector Shilov component; E₀=2 rejected). The staged lepton overlap is now keyed to a PINNED Δ=3/2. Lyra F877 corrects the object: shared ν-ladder, leptons use d(ν) (derived down template), so lepton MASSES are ungated by the up-weight f (only PMNS mixing waits on the F85-analog).

# Grace — LANE B: charged-lepton + PMNS overlap staged in the VALIDATED machine (reconnect, not re-derive)
*2026-08-09. Keyed to the SAME F338 radial-weight fork Lane A pins → runs the moment Lane A lands. Supersedes my 2026-08-09 ad-hoc N^{5/2} reframe (that was not the machine).*

## Reconnect: the machine (validated, don't re-invent)
1. **Matter = Di spinor tower** — lowest K-type SO(5)-spinor 4 = ground spinor u₀ (F326/F338). Charged leptons are matter spinors in this tower.
2. **Mass = holomorphic-discrete-series weight overlap** y = ⟨f|O|f⟩ ~ n_C^{−(fermion weight)} (F832). Fermion weight = g = 7 (π_{n+2}, Casimir rank·g=14); scalar weight = C₂ = 6 (π_{n+1}). The up-Yukawa y_u = n_C^{−g} = 5^{−7} lands (F832, Identified-strong).
3. **The radial-weight fork (F338, the finish-line pin, SHARED with Lane A):** matter Δ = **2** (banked, free-field (d−1)/2) vs **5/2 = ρ₁ = n_C/rank** (FG Di literature, arXiv:1409.2185) vs **3/2 = N_c/rank** (F831 genus route). Lane A pins this against the FK kernel exponent; **the SAME Δ then fixes the leptons — no separate lepton fit.**
4. **Mixing = W-mediated factorization** (F822): PMNS = ⟨ν|J_W|ℓ⟩ = angular skeleton (Derived) × radial residual. Leptons colorless → degenerate → **LARGE** mixing (color-duality K1194: mixing size = hierarchy-vs-degeneracy), the mirror of small CKM.
5. **Muon already IDENTIFIED** (K979): (2,2) VEV reading favored; open piece = gate b (e=f, the SM gauge embedding) — the SAME deep embedding question, not a new fit. Reconnect, don't re-open.

## Charged-lepton seat (#36/#37) staged in the machine
Leptons at forced strata ν = {5/2, 3/2, 0} = {e, μ, τ} (T2517; τ = ℓ=0 boundary mode, T2528 — heaviest at the boundary, consistent with boundary-anchoring). Mass = spinor-tower overlap at those ν, radial weight = the F338 fork.

**Honest finding (reported straight — a power-law shortcut FAILS):** writing the ratios in the machine's n_C-power form and reading exponent-per-Δν gives **3.31 (μ/e), 2.03 (τ/e), 1.17 (τ/μ) — NOT constant.** So there is **no simple n_C^{slope·Δν} ladder**; the leptons carry genuine **degree-dependent overlap structure** — the SO(5) harmonic combinatorics {1, 5, 14, 30} the machine computes explicitly. This is why the seat needs the FULL overlap (Lane A's machine), not a shortcut. (τ/e exponent-per-Δν = 2.03 ≈ rank is a lead, not banked — the 0.03 amplifies.)

**Target-innocence carried forward:** the only clean piece is **49 = g²** in m_τ/m_e = 49·71 (g is a primary); 71, 24, exp-6 stay fit-suspect (last turn's audit). The machine must OUTPUT them, not insert them.

## ν-scale + PMNS (#840) staged in the machine
- **ν-scale:** neutrinos are boundary Majorana (F659) — they skip the N_max⁴ bulk suppression charged fermions pay, sitting at the vacuum scale directly. So the ν overlap uses the boundary (ν=0) weight, not the bulk ladder — a DIFFERENT normalization branch of the SAME machine.
- **PMNS = ⟨ν|J_W|ℓ⟩** (F822 factorization): angular skeleton from the SAME J_W selection rule; radial = degenerate (colorless) → LARGE mixing. Elie's PMNS 3/3 target-innocent already lands (sin²θ₁₂=3/10, sin²θ₁₃=1/45 at 0.03σ, sin²θ₂₃=5/9, K840-linked); this staging connects those to the one overlap.

## ★ Pre-registered fork cross-check for @Elie (fire when Lane A pins Δ)
Run the SAME overlap operator on the lepton positions {5/2,3/2,0} with the fork Δ that Lane A pins:
1. **PASS (target-innocent corroboration):** the fork Δ that pins the QUARK radial ALSO lands the lepton ratios (μ/e, τ/e) via the SO(5)-combinatoric overlap — one fork, both sectors → the fork is real, not sector-tuned.
2. **PARTIAL:** the fork lands quarks but leptons need an extra factor → name it (the extra factor is the colorless-vs-colored difference, K1194) and keep leptons Identified.
3. **Controls:** ONE operator gives μ/e AND τ/e (no per-lepton retune); ν-scale + PMNS fall out of the SAME 3-strata calc (K840). Report which pieces DERIVE vs stay imported; g²=49 is the one banked-innocent anchor.
**Discipline:** magnitude of any single ratio is not the test — the test is whether ONE fork + ONE operator spans quarks + leptons + ν. Nothing banks on a lepton coincidence; only cross-sector one-machine consistency promotes.

## Status
No new node (staging + cross-check, correctly un-banked). The lepton/PMNS overlap is now expressed in the validated machine and keyed to Lane A's fork — it runs the instant Lane A lands. Cross-refs: F832 (weight overlap), F822 (W-factorization), F338 (fork), K979 (muon IDENTIFIED), K1194 (color-duality), F659 (ν boundary-Majorana), T2517/T2528 (positions). Nothing pushed.
