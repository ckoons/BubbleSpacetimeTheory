# F768 — Sharpening gate (b) of step 2 (the shift-consistency check) into a decidable test against FIXED corpus data: the n_C=5 vs N_c/2=3/2 "different shifts" reconcile IFF through the full rank-2 conformal ρ-vector (5/2, 3/2) — which is target-innocent Harish-Chandra data, not adjustable. **The worry (gate b, my lane): the spectral factoring shifts by n_C = 5, the Γ_Ω Gammas shift by N_c/2 = 3/2 — different parameters, and if matching them needs a patch, the patch is the fit. The sharpening: these are NOT two inconsistent numbers to be bridged; they are the TWO COMPONENTS of the rank-2 conformal ρ-vector, which for D_IV⁵ is the FIXED value ρ = (5/2, 3/2) (established corpus data — it appears in the YM paper as |ρ|²=17/2, and in the dual-ρ work F47). The spectral side sees ρ₁ = 5/2 (since λ_k = k(k+n_C) = (k+n_C/2)² − (n_C/2)², so its natural shift is n_C/2 = 5/2 = ρ₁); the Γ_Ω side sees ρ₂ = 3/2. A rank-2 domain HAS two shift parameters — the two ρ-components — so both appearing is forced, not coincidental. THE DECIDABLE TEST: step 2 must show the det Δ continuation produces the full ρ = (5/2, 3/2) and nothing else; if it reconciles 5 with 3/2 using the fixed ρ-vector, forced; if it needs ANY factor beyond ρ, that factor is the patch = the fit.**

**Lyra, 2026-08-01. Sharpening the gate I'm charged to rule (exhibited-or-inferred). Making gate (b) a check against fixed data rather than a judgment call — the ρ-vector is not adjustable, so "reconciled via ρ" vs "reconciled via ρ + patch" is a clean, decidable line. Bias-guard below, because ρ is also my prior work.**

## The reconciliation, precisely (target-innocent)
- **Spectral side:** λ_k = k(k+n_C) = (k + n_C/2)² − (n_C/2)². The natural shift is **n_C/2 = 5/2 = ρ₁** (the first conformal ρ-component). The "n_C = 5" the gate flagged is just 2ρ₁.
- **Γ_Ω side:** rank-2 Γ_Ω = (2π)^{3/2} Γ(s) Γ(s − 3/2). The shift between the two Gammas is **3/2 = ρ₂** (the second conformal ρ-component).
- **Both are components of the ONE fixed vector** ρ_conformal(D_IV⁵) = **(5/2, 3/2)**. For a rank-2 tube domain there are exactly two ρ-components, and exactly two shift parameters appear (spectral ρ₁, Gindikin ρ₂). So the "different shifts" are not a discrepancy to patch — they are the two halves of the rank-2 ρ-vector, which is the structure a rank-2 domain must have.

## Why this makes gate (b) DECIDABLE (not a judgment call)
The conformal ρ = (5/2, 3/2) is **fixed Harish-Chandra data** — computed from the root system of SO(5,2), independent of the cosmological constant, target-innocent (it predates and is unrelated to any DE question). So:
- **FORCED (exhibited):** step 2's continuation reconciles the spectral and Γ_Ω shifts using *only* ρ = (5/2, 3/2) — the full, fixed ρ-vector, no extra factor. Then n_C=5 ↔ 3/2 "falls out" exactly as gate (b) demands, because both are ρ-components.
- **FIT (inferred):** step 2 needs *any* additional factor, shift, or normalization beyond the ρ-vector to make 5 and 3/2 agree. That extra factor is the patch — and by gate (b) the patch is the fit. Even a "small" fudge fails the gate.
The line is sharp because ρ is not adjustable: you cannot quietly tune (5/2, 3/2) to make the continuation collapse. Either the standard ρ-shift does it, or it doesn't.

## Bias-guard (the ρ-vector is also mine — F47)
The dual-ρ structure (compact ρ_SO(5) = (3/2,1/2) vs conformal ρ = (5/2,3/2)) is my earlier corpus work (F47). So the same guard I named for Γ_Ω applies here: **recognizing my own ρ-machinery as the reconciliation is not evidence it's forced** — it is another warm confirming-my-prior signal, and it raises my scrutiny, not lowers it. Concretely: I do NOT get to declare gate (b) passed because "the shifts are ρ-components and ρ is fixed." That is the *test*, not the *verdict*. Grace's step 2 must actually *produce* the ρ-shift from the det Δ continuation (Gindikin–Hua), and Elie must verify no extra factor sneaks in. I rule exhibited (ρ-only) or inferred (ρ + patch) on the written computation — not on the elegance of the ρ-story.

## Tier / handoffs
- **@Grace** — gate (b) is now a check against fixed data: your step-2 continuation must reconcile the spectral shift (n_C/2 = 5/2 = ρ₁) with the Γ_Ω shift (3/2 = ρ₂) using *only* the conformal ρ = (5/2, 3/2), no extra factor. If the Gindikin–Hua continuation produces exactly the ρ-vector, the shift-consistency gate falls out forced; if it needs a normalization patch to bridge 5 and 3/2, that patch is the fit. The ρ-vector is target-innocent (root-system data, unrelated to DE), so it's a clean reference — but you must *produce* it, not invoke it.
- **@Elie** — the multiplicity gate (a) and this shift gate (b) are both numerical checks: (a) the d_k are the forced discrete-series K-type dimensions; (b) the continuation's shifts are exactly (5/2, 3/2), no extra factor. Verify both fall out of the machinery, not adjusted into it.
- **@Cal** — gate (b) sharpened to decidable: forced = reconciled via the fixed ρ=(5/2,3/2) alone; fit = any factor beyond ρ. Audit that Grace *produces* the ρ-shift from the continuation (not invokes it), and that I ruled on the written identity, not the ρ-story's elegance (I flagged my own ρ-bias). Coarse-menu + step-1 + two forced gates = the whole reduction, cleanly bounded.
- **@Casey** — gate (b), the sharp one, is now a test anyone can check rather than a matter of taste. The worry was that the spectrum shifts by one number (5) and the Gamma-function machinery shifts by another (3/2), so making them agree could hide a fudge. The resolution: those aren't two numbers to force together — they're the two halves of a single fixed vector, (5/2, 3/2), that the geometry hands us from the domain's symmetry (the same ρ-vector already in the corpus). A rank-2 shape *has* two such shift-numbers, and here they are. So the test becomes: does Grace's computation reconcile the two sides using *only* that fixed vector, or does it need to sneak in an extra factor? Fixed vector alone → forced; any extra factor → fit, no matter how small. And I flagged that this ρ-vector is my own earlier work too, so I'm holding it as the test to pass, not the answer — same discipline as the Γ_Ω bias, one layer down. Nothing pushed.

Notes only; no toy/theorem claimed (gate sharpening supporting Grace's step-2 lead). F768: gate (b) shift-consistency made decidable. The "n_C=5 vs N_c/2=3/2 different shifts" are the TWO COMPONENTS of the fixed rank-2 conformal ρ = (5/2, 3/2): spectral λ_k=(k+n_C/2)²−(n_C/2)² → shift n_C/2=5/2=ρ₁; Γ_Ω=(2π)^{3/2}Γ(s)Γ(s−3/2) → shift 3/2=ρ₂. Rank-2 domain HAS two ρ-components → both forced. TEST: step 2 reconciles via ρ=(5/2,3/2) ALONE (forced) vs ρ + any extra factor (patch=fit). ρ is fixed Harish-Chandra/root-system data, target-innocent (unrelated to DE), not adjustable → decidable line. BIAS-GUARD: the dual-ρ structure is my F47 work → recognizing it as the reconciliation is a confirming-my-prior signal → it's the TEST not the verdict; Grace must PRODUCE the ρ-shift from the continuation, not invoke it; I rule on the written identity. @Grace produce ρ from Gindikin–Hua; @Elie verify no extra factor; @Cal audit produce-not-invoke. — Lyra