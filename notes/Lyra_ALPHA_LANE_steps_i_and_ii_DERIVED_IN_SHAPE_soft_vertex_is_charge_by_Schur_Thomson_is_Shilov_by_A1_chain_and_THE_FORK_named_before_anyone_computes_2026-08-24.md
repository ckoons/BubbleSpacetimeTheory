# THE α LANE — steps (i) and (ii), DERIVED IN SHAPE. **The soft vertex = the charge operator, by Schur — zero choices. Thomson = Shilov, by a four-link banked chain. And the definition work exposes exactly ONE FORK — the soft-mode's normalization — named now, BEFORE anyone computes, with its resolution criterion stated.**

**Lyra, 2026-08-24. Continuing the M-definition per the boarded plan. Still no evaluation; the frozen form completes when the fork resolves. Nothing banked. Cell: External 3 (α).**

## STEP (i) — the domain Low theorem: the soft vertex couples to total charge, by orthogonality
The gauge vertex is the pairing of the em current with a gauge-mode function on Σ. **The soft (q² → 0) mode is the K-invariant (constant) function.** Pairing any current matrix element against the trivial K-type **projects onto the current's K-invariant component** — and the invariant component of the em current is **the conserved total charge Q̂** (conservation banked: T2470-corrected [Q,J] = 0; the W-21 family T2473–T2475).
> ### **⟹ ⟨ψ | Γ(f_soft) | ψ⟩ = f̂_soft · q · ‖ψ‖², by SCHUR. No choice among vertex candidates ever arises — the pre-declared D1 abort does not fire.** The Low theorem on the domain is one orthogonality statement.

## STEP (ii) — Thomson = Shilov: a four-link chain, each link banked or definitional
```
 measured α  =  the q²→0, on-shell, soft-photon coupling            [the DEFINITION of the Thomson limit]
 on-shell asymptotic states  =  Shilov-boundary states               [A1: K744/K745, banked]
 the soft mode  =  the constant function on Σ                        [step (i), derived]
 ⟹  the Thomson vertex  =  the Σ-pairing — the SHILOV vertex.       [composition]
```
**D4 discharged: the scale is stated and the boundary identification is derived, not assumed.** And per v2, this chain is what selects landings honestly: a bare boundary pairing has no radiative content ⟹ **landing A (137.000) is the only honest win unless a same-integral subleading term is exhibited.**

## ★★ THE FORK — found by the derivation, named before any number exists
With states unit-normalized (Born) and step (i) done, **M = q · f̂_soft** — everything now rides the soft mode's normalized amplitude, i.e. **𝒩(n) = ‖1‖²_Σ in WHICH forced structure?** Two banked normalizations exist and they are NOT the same object:
- **The PROBABILITY normalization (T754, Born):** total mass 1. **Applies to STATES.** If applied to the gauge mode it gives α = q² — absurd, and diagnostically so: **the gauge mode is NOT a state and the Born normalization has no jurisdiction over it.**
- **The REPRODUCING-KERNEL / current-pairing normalization** (the c_FK family — 225, 1920/π⁵ — and the Killing form on so(5)): the structure that normalizes **operator insertions and gauge kinetic terms.** This is where a family-varying 𝒩(n) would live.
> ### **THE RESOLUTION CRITERION, stated as the gate demands:** the soft mode enters M as a CURRENT PAIRING, not a state — so its norm must be fixed by the domain's current-current form (Killing ⊗ boundary-L², the gauge-kinetic structure), **and the definition completes iff that form is banked or derivable-forced.** Corpus first-look: no clean single bank found under the obvious names (the Lane-C/YM normalization work is adjacent but not a pin). **If the current-current form is not forced by banked structure, D1a's own clause executes: "the lane is not ready and does not open."** That would be an honest, reportable state — better found now than after an integral.

## WHAT THIS SESSION'S DEFINITION WORK DELIVERS, honestly
Steps (i) and (ii): **derived in shape, zero choices, D1/D4 content discharged.** The frozen form: **one fork from complete** — and the fork is not mine to resolve by preference: it needs the current-current normalization's bank (a corpus job: @Grace's lane, or a short derivation from the banked YM/kinetic structure if it exists). **Until then the family skeleton stands as filed (α(n)⁻¹ = 𝒩(n)/q²) with 𝒩's normalization flagged OPEN — and nobody computes.**

**Lyra. The Low theorem is Schur; Thomson is Shilov; the one fork is the soft-mode's normalization, and it resolves by bank or the lane honestly declines to open. Named before any number, per the week's whole method. Nothing banked.**
