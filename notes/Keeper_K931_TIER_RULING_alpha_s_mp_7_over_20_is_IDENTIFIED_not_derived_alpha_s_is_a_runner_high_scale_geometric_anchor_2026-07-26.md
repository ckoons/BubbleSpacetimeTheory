# K931 — TIER RULING: α_s(m_p) = g/(4n_C) = 7/20 = 0.350 is **IDENTIFIED (I)**, not DERIVED (D). Reconciles the data-layer inconsistency: `constants.json` tags it "D"; `bst_26_tier_map.json` (K755) grades α_s a RUNNER. Both can't stand. Ruling: **the FORM 7/20 is a high-scale geometric ANCHOR (identified); α_s itself is a RUNNER (no clean observable value).** So the row is I, not D. Grace executes the JSON edit.

**Keeper | 2026-07-26 Sun | Small tier ruling, real cleanup — prevents an over-graded "derived" from sitting in the published data layer.**

## The tension
- `data/bst_constants.json`: α_s(m_p) = g/(4·n_C) = 7/20, tier **"D" (derived)** — "Yang-Mills coefficient projected onto the CP² color fiber, c·Vol(CP²)/π = 7/10π · π/2 = 7/20."
- `data/bst_26_tier_map.json` (K755): α_s is a **RUNNER** — "computable, not cleanly observable; confines (no IR fixed value) + runs; BST derives the β-function SIGN, but the exact α_s is a scale-dependent runner."

## Ruling: IDENTIFIED (I), high-scale geometric anchor
- **α_s is a RUNNER** — the value is scale-dependent, scheme-dependent, and has no clean observable value at any single scale (unlike α at Thomson). "α_s at m_p" is not a sharp number. → cannot be a **derived observable (D)**.
- **The FORM 7/20 = g/(4n_C) is an IDENTIFICATION** — a clean geometric form that matches the running α_s at ~the proton scale. The projection mechanism (CP² color fiber) is *plausible* but the value is a runner-at-a-scale, not a forced observable. → **I (identified), high-scale geometric anchor.**
- Consistent with the two-axis discipline: on the **accuracy** axis the 7/20 form is a good match; on the **proof** axis it is identified (a runner's geometric anchor), NOT derived. The "D" tag conflated the accuracy match with a derivation. → correct to **I**.
- The **running endpoint** α_s(m_Z) = 0.1185 stays a **runner** (with the c₁ = N_c/n_C = 3/5 correction, its own separate status).

## Disposition
- **α_s(m_p) = 7/20: I (identified, high-scale geometric anchor).** Not D.
- **α_s overall: RUNNER** (bucket 3) — as the tier_map already has it.
- **★ GRACE — execute:** update `data/bst_constants.json` α_s(m_p) tier D → I, note "high-scale geometric anchor; α_s is a runner (tier_map K755)." Data-layer now consistent.
- No impact on the partition theorem (α_s was always a runner / bucket 3).

— Keeper K931, 2026-07-26. TIER RULING: α_s(m_p)=g/(4n_C)=7/20 is IDENTIFIED (high-scale geometric anchor), NOT derived — α_s is a RUNNER (scale/scheme-dependent, no clean observable). Reconciles constants.json ("D") with tier_map/K755 (RUNNER). Two-axis: accuracy-match ≠ derivation. Grace edits the JSON D→I. Running endpoint α_s(m_Z) stays runner (c₁=3/5 correction). No partition-theorem impact. See [[Keeper_K927_STRONG_SECTOR_DYNAMICS_area_charter]], [[feedback_score_sigma_not_devpct]].