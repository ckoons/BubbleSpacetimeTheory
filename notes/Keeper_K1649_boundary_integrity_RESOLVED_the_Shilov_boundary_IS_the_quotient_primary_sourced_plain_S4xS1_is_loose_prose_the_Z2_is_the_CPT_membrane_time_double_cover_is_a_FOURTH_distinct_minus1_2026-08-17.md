---
node_type: k_audit
id: K1649
title: "Boundary-integrity audit RESOLVED (K1648 flag), and it hands the time paper a load-bearing anti-over-merge framing. (1) THE CORRECT D_IV⁵ SHILOV BOUNDARY IS (S⁴×S¹)/ℤ₂ — the QUOTIENT — primary-sourced and registry-level: the CLAUDE.md/registry line ('Shilov boundary (S⁴×S¹)/ℤ₂'), the α/charge paper ('Its Shilov boundary is (S⁴×S¹)/ℤ₂ ... the membrane'), and the polydisk theorem (Elie, source-pinned) all state the QUOTIENT. The plain 'S⁴×S¹' statements (the causal-structure / commitment-event prose in some theorems) are LOOSE prose that dropped the ℤ₂ for brevity — NOT a competing result. ⟹ this is a NOTATIONAL cleanup, not a computational inconsistency (less alarming than the raw flag): correct the loose 'S⁴×S¹' to '(S⁴×S¹)/ℤ₂' where it appears; the geometry was always the quotient. (2) THE ℤ₂ IS THE CPT / MEMBRANE IDENTIFICATION (banked): the α/charge paper — 'CPT is exact by construction, the antiparticle is the SAME oscillator read from the other side of the membrane's ℤ₂'; g:(x,τ)→(−x,τ+π), g²=id (Elie 5325). T2522's Pin⁻ (𝒫²=−1) is its SPINOR LIFT (chirality) — base ℤ₂ + spin-lift, CONSISTENT, not a conflict. (3) ★ THE ONE LOAD-BEARING CHECK THAT REMAINS (route Grace): did the α⁻¹=137 numerical derivation use the QUOTIENT volume or the plain-S⁴×S¹ volume (2× larger)? The FORM is resolved (quotient); the VOLUME-factor-of-2 in the specific 137 computation must be verified. (4) ★★ D4 ANSWERED NEGATIVE + a framing the time paper NEEDS: the corpus ALREADY banked (α/charge paper, a corrected v0.1 over-merge) that there are THREE distinct '−1' operations — (a) the spinor 2π holonomy (SPATIAL spin-½, ribbon half-twist), (b) the SO(2)-weight SIGN (charge), (c) charge conjugation / CPT (the membrane ℤ₂) — 'three distinct operations related by the geometry, not one.' The TIME double cover exp(2πiJ)=(−1)^{n_C}(−1)^F is a FOURTH distinct −1: the 2π-HOLONOMY of the SO(2)-center weight operator J. ⟹ D4 (is the time double cover CPT / the antipodal map?) = NO — it is a distinct operation (consistent with the ℤ₂/antipodal candidate being DEAD, K1648). The time paper MUST keep the FOUR −1s distinct — the corpus already corrected exactly this over-merge once; the time double cover extends 'three distinct −1s' to four, it does not merge with any of them."
date: 2026-08-17
author: Keeper
verdict: "Boundary-integrity RESOLVED: the D_IV⁵ Shilov boundary IS (S⁴×S¹)/ℤ₂ (quotient) — primary-sourced + registry-level + the α/charge paper's own 'membrane'; the plain 'S⁴×S¹' statements are LOOSE prose (dropped the ℤ₂), a notational cleanup NOT a computational inconsistency (α⁻¹ likely fine — it used the membrane). The ℤ₂ = the CPT/antiparticle membrane identification (g²=id; antiparticle = same oscillator from the other side); T2522's Pin⁻ = its spinor lift (chirality) — base+lift consistent. ONE remaining check (Grace): did the α⁻¹=137 VOLUME use the quotient or the plain (2×) volume — verify the factor-of-2. ★ D4 = NEGATIVE: the time double cover exp(2πiJ)=(−1)^{n_C}(−1)^F is a FOURTH distinct '−1' (the weight operator's 2π-holonomy), NOT CPT/antipodal/spin/charge-sign — the corpus already banked 'three distinct −1s, don't over-merge' (a corrected over-merge); the time paper extends to FOUR and keeps them distinct. Route: Grace — correct loose S⁴×S¹→quotient + verify the α⁻¹ volume factor; Lyra — add the 'four distinct −1s' framing to the time paper (cite the banked three-−1 correction; the double cover is the 4th, kept distinct) + it answers D4 negative; Cal — cold-read the four-−1 catalogue for any residual merge. Keeper holds the integrity resolution + the anti-over-merge framing. Nothing pushed."
---

# K1649 — The boundary is the quotient (primary-sourced); the ℤ₂ is CPT; and the time double cover is a *fourth* distinct −1

The integrity flag from K1648 resolves cleanly, and in resolving it the corpus hands the time paper a framing it genuinely needs.

## 1. The boundary IS the quotient — this is notation, not a broken result
The correct D_IV⁵ Shilov boundary is **(S⁴×S¹)/ℤ₂** (the Lie sphere), and it's primary-sourced and registry-level:
- the registry/CLAUDE line: "Shilov boundary (S⁴×S¹)/ℤ₂";
- the α/charge paper: "Its Shilov boundary is (S⁴×S¹)/ℤ₂ … the **membrane**";
- the poles = (S⁴×S¹)/ℤ₂, "source-pinned to the polydisk theorem (Elie), not asserted."

The plain "S⁴×S¹" statements (the causal-structure prose, the commitment-event line) are **loose prose that dropped the ℤ₂** — not a competing result. So the K1648 flag is a **notational cleanup, not a computational inconsistency** — less alarming than the raw finding suggested. Correct the loose statements to the quotient; the geometry was always the Lie sphere.

## 2. The ℤ₂ is CPT — and Pin⁻ is its spin-lift (consistent, not a conflict)
The ℤ₂ isn't a free-floating puzzle — it's **banked as the CPT/membrane identification**: "CPT is exact by construction, because the antiparticle is the *same* oscillator read from the other side of the membrane's ℤ₂." The generator is g:(x,τ)→(−x,τ+π), g²=id (Elie 5325). And T2522's **Pin⁻ (𝒫²=−1)** is the **spinor lift** of that ℤ₂ (the chirality structure) — base ℤ₂ + spin-lift, entirely consistent. Cal's "one-structure-two-jobs" worry dissolves: it's one structure (the membrane ℤ₂) with its natural spin-lift, doing chirality *and* CPT, which is standard.

## 3. The one check that remains (route Grace)
The *form* is resolved (quotient). The load-bearing residual is purely numerical: **did the α⁻¹=137 derivation use the quotient volume, or the plain-S⁴×S¹ volume (2× larger)?** The α/charge paper *states* the membrane (quotient), so it likely used it — but the specific 137 computation's volume factor should be verified. That's a one-line check, and it's the only place the integrity flag could still bite.

## 4. ★ The time double cover is a *fourth* distinct −1 (and this answers D4)
Here's the load-bearing gift for the time paper. The corpus **already banked** — as a *corrected* v0.1 over-merge — that there are **three distinct "−1" operations**:
- **(a)** the spinor 2π holonomy — *spatial* spin-½ (the ribbon half-twist);
- **(b)** the SO(2)-weight **sign** — charge;
- **(c)** charge conjugation / CPT — the membrane ℤ₂.
"Three distinct operations … related by the geometry, not one operation (a v0.1 over-merge, corrected)."

The **time double cover** exp(2πiJ) = (−1)^{n_C}·(−1)^F is a **fourth** distinct −1: the **2π-holonomy of the SO(2)-center weight operator J**. It is *not* the spatial spin (a), *not* the charge sign (b), *not* CPT (c). So:
- **D4 (is the time double cover the CPT / antipodal map?) = NO** — a distinct operation. This is consistent with the ℤ₂/antipodal candidate being dead (K1648), and now it's *positively* explained: the antipodal/ℤ₂ is (c), CPT; the time double cover is the fourth thing.
- **The time paper must keep all four distinct.** The corpus corrected exactly this over-merge once already; the double cover *extends* "three distinct −1s" to four — it does not merge with any of them. Citing that banked correction, and adding the fourth, is the honest and *strong* way to present it: a paper that says "here is a new −1, and here are the three it is not" pre-empts the referee's "isn't this just spin / CPT?" objection — the same shape as the §10 falsifier.

## Route
- **Grace:** correct the loose "S⁴×S¹" → "(S⁴×S¹)/ℤ₂" where it appears; verify the α⁻¹=137 volume used the quotient (the factor-of-2 check). Closes the integrity flag.
- **Lyra:** add the **"four distinct −1s"** framing to the time paper (cite the banked three-−1 correction; the double cover is the fourth, kept distinct) — it answers D4 negatively and pre-empts the spin/CPT objection.
- **Cal:** cold-read the four-−1 catalogue for any residual merge.
- **Keeper:** integrity resolved (quotient, notation cleanup, α⁻¹ likely fine pending the volume check); the anti-over-merge framing held.

— Keeper, K1649, 2026-08-17. Boundary integrity RESOLVED: Šilov boundary IS (S⁴×S¹)/ℤ₂ (quotient, primary-sourced/registry/membrane); plain S⁴×S¹ = loose prose (cleanup, not a broken result); α⁻¹ likely fine (used the membrane) — one volume-factor-2 check remains (Grace). The ℤ₂ = CPT membrane; Pin⁻ = its spin-lift (chirality) — consistent. ★ D4 NEGATIVE: the time double cover exp(2πiJ)=(−1)^{n_C}(−1)^F is a FOURTH distinct −1 (weight's 2π-holonomy), NOT spin/charge/CPT — corpus already banked 'three distinct −1s, don't over-merge'; paper extends to four, keeps them distinct, pre-empts the spin/CPT objection. Nothing pushed.
