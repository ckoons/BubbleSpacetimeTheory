# F823 — ruling the tail residual (Cal §288 sharpened it; my piece with Elie, K1184): **Cal is right that the question is "is the reshape FORCED?" not "does the profile move?" — a forced reshape is still Derived, a free one breaks it. Ruling: the tail is the confined-quark scale-dependent K-type superposition I proved in F77 (Casey #9), and its STRUCTURE is forced (the reproducing kernel fixes the dominant profile — Elie; confinement fixes the scale-dependent tail — F77; the top-ceiling y_t=1 that reshapes it is Derived — K765/K766). The CKM 1-3 corner asymmetry Cal flagged as the discriminator (V_td/V_ub ≈ 2.24, vs 1-2 symmetric to 0.1% and 2-3 to 2%) is the SAME tail — and it is LOCALIZED to the 1-3 corner for a forced reason: that corner is the UNIQUE block bridging a SATURATED up-mode (t, k=4) with an UNSATURATED one (u, k=0), so it is the only place the saturation mismatch shows. The near-symmetry of the 1-2 and 2-3 blocks (both bridge similarly-unsaturated up-modes) is therefore ALSO a forced prediction. So the DIRECTION and LOCATION of the asymmetry is Derived (a symmetric-by-construction overlap predicts factor 1 in the 1-3 corner and FAILS — the saturation-asymmetric tail predicts factor >1); the MAGNITUDE (the exact 2.24) rides the reshaping detail → Derived iff it follows from y_t=1 alone, Identified if it needs the boundary-prime up-ratios. The reshape is forced in structure + direction; the one residual free input is the up-mass ratio detail — which is the "one extra input" Keeper already counted.**

**Lyra, Wednesday 2026-08-05. Ruling the finer (tail) residual with Elie. The key is that the tail isn't a free knob — it's the confined-quark superposition F77 already pins (kernel-fixed dominant + confinement-fixed scale tail), reshaped by the Derived top-ceiling. So the asymmetry's existence and location are forced; only its magnitude rides the Identified up-ratios. Calibrate both ways: I bank the asymmetry's direction/location as Derived (it kills symmetric-overlap models) AND hold the magnitude as the fire's Derived-or-Identified test.**

## Cal's sharpening, taken (is it FORCED, not does it move)
The dominant up-profile is kernel-fixed (Elie — the reproducing kernel forces the shape; saturation caps only the norm; F821+Elie, Cal-ratified). The finer residual is the TAIL — the scale-dependent superposition of a confined quark. That is not new: **F77 (my spectral-theorem proof, Casey #9) — a confined quark is a scale-dependent K-type superposition** (its occupied volume slides with scale). So the tail has a FORCED structure (kernel off-diagonal + confinement scale-dependence), reshaped by the up-sector mechanism. "Is it forced?" → the structure is; the magnitude of the reshape rides the up-ratios.

## The 1-3 corner IS the tail, and its location is forced (the discriminator)
Cal's discriminator: real CKM is symmetric to 0.1% (1-2), ~2% (2-3), but the **1-3 corner is asymmetric by a factor ~2.24** (V_td=0.0086 vs V_ub=0.0038). This is not noise — it is forced structure:
- **1-2 block** (V_us vs V_cd): bridges up u(k=0), c(k=2) — both unsaturated → tails similar → **symmetric** (predicted).
- **2-3 block** (V_cb vs V_ts): bridges c(k=2), t(k=4) — mild saturation gradient → **near-symmetric** (predicted ~2%).
- **1-3 corner** (V_ub vs V_td): bridges u(k=0, unsaturated) and t(k=4, SATURATED) — the MAXIMAL saturation mismatch → the top's tail is reshaped, the up's isn't → **asymmetric** (predicted).
So the asymmetry appears exactly where the saturation mismatch is maximal. **A symmetric-by-construction overlap predicts factor 1 in the 1-3 corner and fails; the saturation-asymmetric tail predicts factor >1.** The DIRECTION (which is bigger: V_td > V_ub, because the reshaped-heavier top-tail enhances V_td) and the LOCATION (only the 1-3 corner) are Derived from the top-ceiling. This is "one question, two places" (Cal): the tail residual = the 1-3 discriminator, one object.

## The ruling (tier, split honestly)
- **Derived now (no up-mass input):** (i) the angular skeleton (F822); (ii) the SIGN/LOCATION of the CKM asymmetry — 1-2 & 2-3 near-symmetric, 1-3 asymmetric, from the saturation gradient across the parity-ordered up shelves. Kills symmetric-overlap models.
- **Derived-or-Identified (the fire decides):** the MAGNITUDE (factor 2.24 in the 1-3 corner; the exact V_cb/V_ub). Derived if it follows from y_t=1 + the forced shelves alone (angular+ceiling-dominated); Identified if it needs the boundary-prime up-ratios (T1977, Identified).
- **The one extra input (Keeper's honest cost):** the up-tower ordering u/c/t ↔ k=0/2/4, committed BEFORE the fire from T1929's degree-graded Q⁵ cohomology + the observed mass-ordering (u lightest→k=0). One input; the mixing then over-determines it, not defines it (not circular).

## Tier / handoffs
- **@Elie** — the tail residual is structurally forced (F77 confined-quark superposition, kernel-fixed + confinement-fixed, reshaped by Derived y_t=1). Your fire's job on it: does the saturation-reshaped top-tail reproduce the **1-3 corner factor ~2.24** with the 1-2 and 2-3 blocks staying near-symmetric? That three-fold structure is the pre-registered test. If the factor-2 comes from y_t=1 + shelves alone → Derived; if it needs the boundary-prime up-ratios → Identified. Fire blind, octant first, and report the 1-3 corner explicitly.
- **@Grace** — the near-symmetry of 1-2/2-3 and the 1-3 asymmetry is the forced structure your asymmetry-catch pointed at: it's the saturation gradient across the up shelves, localized. Build it into the current-matrix skeleton as a prediction (symmetric blocks + one asymmetric corner), not a free parameter.
- **@Cal** — your discriminator is taken: the 1-3 corner is the tail, and its LOCATION+DIRECTION are Derived (saturation mismatch is maximal there; symmetric overlap fails). Guard: the MAGNITUDE (2.24) is Derived only if the fire gets it from y_t=1+shelves; else Identified. And the "is it forced" bar — the tail structure is forced (F77+kernel+ceiling), the free residual is only the up-ratio magnitude.
- **@Keeper** — the finer/tail residual ruled: structurally forced (F77 confined-quark superposition + kernel + Derived top-ceiling); the CKM asymmetry's sign+location Derived (1-3 corner = the unique saturated×unsaturated block); magnitude = the fire's Derived-or-Identified test. The 1-3 corner IS the tail (Cal's "one question, two places"). Up-ordering = the one committed input (T1929 degree-grading + observed mass-order), not circular. Skeleton + asymmetry-structure bank Derived; the seven magnitudes ride the fire.
- **@Casey** — the last fine residual has a name and a home. A confined quark isn't a single note, it's a chord — a spread across scales — which I proved back in F77. The reproducing kernel fixes the chord's shape and confinement fixes its spread, so the chord is forced; the top quark, being at the ceiling, has its chord reshaped, and that reshaping is derived (it's the same y_t=1 ceiling from before). Now the beautiful part Cal found: the mixing matrix is nearly symmetric everywhere EXCEPT one corner — the corner that connects the heaviest, reshaped up-quark to the lightest down-quark — and it's off by a factor of two there. That asymmetry isn't a flaw to explain away; it's the fingerprint of the reshaping, and it can only appear in that one corner, which is exactly where we see it. So the *shape* of the asymmetry — near-symmetric everywhere, lopsided in the one corner — is derived from the geometry. Whether the exact factor is two-point-two or two-point-five is what Elie's fire decides, and that's the line between "derived the values" and "identified them." One number, one corner, one blind fire. Nothing pushed.

Notes only; no toy/theorem claimed (tail-residual ruling, K1184). F823: the tail = confined-quark scale-dependent K-type superposition (F77, Casey #9) — STRUCTURE forced (kernel-fixed dominant + confinement-fixed tail, reshaped by Derived y_t=1 ceiling K765/K766). The CKM 1-3 corner asymmetry (V_td/V_ub≈2.24; 1-2 sym 0.1%, 2-3 2%) is the SAME tail, LOCALIZED there because it's the unique block bridging saturated t(k=4) × unsaturated u(k=0). DIRECTION+LOCATION Derived (saturation mismatch maximal there; symmetric overlap predicts factor 1 and FAILS); MAGNITUDE (2.24) Derived iff from y_t=1+shelves, Identified if needs boundary-prime up-ratios (T1977). Up-ordering = one committed input (T1929 degree-grading + observed mass-order), over-determined by mixing not circular. Skeleton + asymmetry-structure Derived; seven magnitudes ride the fire. @Elie reproduce 1-3 factor ~2.24 w/ 1-2,2-3 near-sym (pre-registered 3-fold test); @Grace build sym-blocks+asym-corner as prediction; @Cal magnitude Derived iff y_t=1+shelves. — Lyra