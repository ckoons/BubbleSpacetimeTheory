# F1022 — Track 1 R6 (K1632), Lane A: hand Elie the r(ν) map with the two binding gate-conditions. **(1) FORK, not chain:** radius→mass and radius→mixing are two PARALLEL matrix elements of one kernel at the forced positions — the mixing is read from the radii DIRECTLY, never from the mass values — so V_us=√(m_d/m_s)=1/√20 is an identity between two parallel outputs, not a round trip. **(2) PARAMETER COUNT:** the genus-5 discrete-series density is rigid — genus n_C=5 (forced), positions {5/2,3/2,0} (forced), dimension exponent a (forced); the ONE load-bearing exponent is the cross-ν b(ν), and the non-cancelling two-point Γ_Ω (K1012) forces **b_cross = n_C = 5**. If confirmed, **ZERO free parameters → derivation**; if b(ν) is tunable → one knob → fit. The verdict rests entirely on b(ν).

**Lyra, Monday 2026-08-17 R6. Keeper K1632 Lane A: "fork not chain (exhibit it); parameter count must ship (rigid object = derivation, free knob = fit)." Sharpening F1021. Corpus (K1012 non-cancellation, K1217 genus, K295 positions, F85-radial). Nothing pushed; CP existence-only.**

## Gate-condition 1 — the FORK, exhibited (not a chain)
The peril: if the causal graph is **radius → mass → mixing** (a chain), then computing V_us from the radius is just recomputing it from the mass — a round trip, worthless. The claim is that it is a **FORK**:

$$y_i = \langle \phi_i \,|\, K \,|\, \phi_i\rangle\big|_{r=r(\nu_i)} \quad(\text{DIAGONAL} \Rightarrow \text{mass}), \qquad V_{ij} = \langle \phi_i \,|\, J_W \,|\, \phi_j\rangle\big|_{r=r(\nu_i),\,r(\nu_j)} \quad(\text{OFF-DIAGONAL} \Rightarrow \text{mixing}).$$

**Both are matrix elements of the SAME operator (the FK/Bergman kernel $K$, with the degree-1 current $J_W$) evaluated at the SAME forced positions $\{r(\nu)\}$.** The causal graph:
$$\nu \;\xrightarrow{\;r(\nu)\;}\; r \;\begin{cases} \xrightarrow{\ \text{diagonal norm}\ } & y \ (\text{mass}) \\[2pt] \xrightarrow{\ \text{off-diagonal overlap}\ } & V \ (\text{mixing}) \end{cases}$$
**Two edges out of $r$; there is NO edge $y \to V$.** The mixing $V_{ij}$ is computed from the radii $r_i, r_j$ directly, never from the mass values $y_i$. So mass and mixing are **parallel outputs of the one coordinate $r$** — a fork.

**Why the Gatto coincidence is a discovery, not a circularity.** $V_{us} = F(r_1, r_2)$ (off-diagonal overlap) and $m_d/m_s = G(r_1, r_2)$ (ratio of diagonal norms) are two functions of the same forced positions. That they satisfy $V_{us} = \sqrt{m_d/m_s}$ is an **identity between two parallel outputs of $r$** — a structural fact about the one kernel at the two positions, NOT $V_{us}$ computed from $m_d/m_s$. The content is: *the reason the Cabibbo angle equals a square-root mass-ratio is that one radial coordinate feeds both.* That is the prize; it is not available to a chain (a chain would make it a tautology, and a tautology can't explain the tie — the fork can). **This is exactly why the (c)-guard (F1021: $\nu$ forced by the orbit count, $r$ from $\nu$, mass downstream) is the whole lane: it is what makes the graph a fork, not a chain.**

## Gate-condition 2 — the PARAMETER COUNT (rigid = derivation, knob = fit)
The map is the peak of the genus-5 discrete-series radial density:
$$\varrho_\nu(r) \propto r^{\,2a-1}(1-r^2)^{\,b(\nu)}, \qquad r_\ast(\nu) = \operatorname{argmax}\varrho_\nu.$$
Count each ingredient — is it forced or free?

| ingredient | value | forced by | free? |
|---|---|---|---|
| genus $p$ (kernel exponent base) | $n_C = 5$ | Bergman kernel of D_IV⁵ (K1217, Hua) | **0 — rigid** |
| positions $\nu$ | $\{5/2, 3/2, 0\}$ | KW-orbit count / ρ-vector (K295, F86) | **0 — rigid** |
| dimension exponent $a$ | $\dim$ of the KW orbit | domain geometry | **0 — rigid** |
| cross-ν exponent $b(\nu)$ | **?** | two-point $\Gamma_\Omega$ (K1012) | **THE question** |

**The single load-bearing number is $b(\nu)$.** The diagonal (same-ν) norm cleared at $\Delta = 3/2$ (K1314 Gate 0, banked) — but the diagonal $\Gamma_\Omega$'s **cancel** down to the reduced $3/2$. K1012's result: the **cross-ν** $\Gamma_\Omega$'s **do NOT cancel** — so the off-diagonal exponent is NOT the reduced $\Delta=3/2$; the non-cancellation leaves the **full genus**:
$$\boxed{\,b_{\text{cross}} = n_C = 5\,}\quad(\text{no free choice — it is the genus, the same 5 as the kernel base}).$$
**If $b_{\text{cross}}=5$ is confirmed by the $\Gamma_\Omega$ computation, the map has ZERO free parameters** — genus 5, positions $\{5/2,3/2,0\}$, exponents (diagonal $3/2$, cross $5$) all forced — so $r(5/2), r(3/2)$ are **fixed numbers**, the overlap is a **fixed number**, and hitting $1/\sqrt{20}$ to 0.49% is a genuine 0-parameter derivation. **If instead $b(\nu)$ is tunable, it is one knob and the result is a fit.** The honest count: *the derivation-vs-fit verdict rests entirely on whether $b_{\text{cross}}=n_C=5$ is forced by the non-cancelling $\Gamma_\Omega$ — I claim it is (K1012), and I flag it as the one thing Elie's computation and Cal's audit must confirm.* Can $1/\sqrt{20}$ "come from 2.5 and 1.5"? Only if the rigid map happens to land there — there is no dial to make it, which is the point: a rigid object that hits the banked number is a derivation; if it misses, it's an honest boundary, localized to $b(\nu)$.

## RUBRICS Layer-2 done-bar
- [x] Exhibited the FORK: $\nu \to r \to \{y \text{ (diagonal)}, V \text{ (off-diagonal)}\}$, two parallel matrix elements of one kernel at the forced positions; no $y\to V$ edge. Gatto tie = identity between parallel outputs, not a chain — the discovery of why V_us = √(m_d/m_s).
- [x] Shipped the parameter count: genus 5 / positions {5/2,3/2,0} / dimension $a$ / diagonal $3/2$ all rigid; the one load-bearing exponent is $b_{\text{cross}}$, forced to $n_C=5$ by the non-cancelling two-point $\Gamma_\Omega$ (K1012). 0 knobs if confirmed → derivation; tunable → fit. Verdict rests on $b(\nu)$.
- [x] Tied both gates to the (c)-guard (F1021): $\nu$ from the orbit count, $r$ from $\nu$, mass downstream — that is what makes the graph a fork. Nothing banks until Elie's blind number lands at 0.49% with $b_{\text{cross}}=5$. Nothing pushed; CP existence-only.

## Handoffs
- **@Elie (Gate A)** — the rigid map: $r_\ast(\nu)=\operatorname{argmax}\, r^{2a-1}(1-r^2)^{b(\nu)}$, genus $n_C=5$, **$b_{\text{cross}}=n_C=5$** (from the non-cancelling cross-ν $\Gamma_\Omega$, K1012 — NOT the diagonal $3/2$). Post $r(5/2), r(3/2), r(0)$ **blind**, then the 5/2↔3/2 overlap. Zero knobs — if it hits 0.3713 (0.49%) and $1/\sqrt{20}$, it's a derivation; if it misses, report the miss localized to $b(\nu)$. Radius from $\nu$ only. Tau $\nu=0$ = Shilov (Szegő measure, F666).
- **@Cal** — the two things to hostile-audit: (1) is the graph genuinely a fork (no $y\to V$ edge), or does Elie's computation secretly route the overlap through a mass? (2) is $b_{\text{cross}}=n_C=5$ actually forced by the non-cancelling $\Gamma_\Omega$ (K1012), or is it the one smuggled knob? If (2) holds, the parameter count is zero and T-worthy; if not, it's a fit. That is the whole derivation-vs-fit call.
- **@Grace** — when Elie's number lands, verify blind (three-part bar). Your killed peak-radius (0.561, 151% off) already did the field the service of clearing a candidate; the survivor is the rigid genus-5 map, and its parameter count is above.
- **@Keeper** — both gate-conditions shipped: the FORK is exhibited (parallel outputs of $r$, no chain), and the PARAMETER COUNT is zero-knob **if** $b_{\text{cross}}=n_C=5$ is forced by K1012's non-cancellation — which is the single load-bearing exponent, flagged for confirmation. If Elie's rigid-map number hits $1/\sqrt{20}$ at 0.49%, it's a 0-parameter derivation and the Gatto tie is explained; if it misses, honest boundary at $b(\nu)$.
- **@Casey** — the whole sector now turns on two honest tests, and I've set both so they can fail. First: is the mixing a *fork* off the geometry or a *chain* through the mass? A fork means the geometry's one number — how far out a quark sits — feeds the mass and the mixing *separately*, like a river splitting; a chain would mean I compute the mixing from the mass, which is just handing the mass back and calling it a discovery. I showed it's a fork: the mixing is read straight off the two positions, never off the masses — so when the Cabibbo angle comes out as the square-root of a mass-ratio, that's not a circle, it's the *reason* those two things have always been mysteriously equal. Second: does the map have a hidden dial? If the genus-5 Bergman kernel is rigid — no adjustable knob — then it either lands on the 1/√20 we already trust or it doesn't, and there's no room to nudge it. I found it's rigid down to a single exponent, and I argued that exponent is forced too (it's just the genus, 5, because the cross-generation terms don't cancel the way the same-generation ones do). If that holds, Elie's blind number is a zero-knob derivation. I did not tune it; I built it so it can miss. Nothing pushed.

Notes only; no toy/theorem claimed (map + gates). F1022: Lane A gate-conditions. (1) FORK not chain: ν→r→{y diagonal-norm=mass, V off-diagonal-overlap=mixing}, two parallel matrix elements of one kernel at forced positions, NO y→V edge; V_us=√(m_d/m_s) is an identity between parallel outputs (the discovery of WHY they're tied), not a round trip; the (c)-guard (F1021) is what makes it a fork. (2) PARAMETER COUNT: genus n_C=5 (K1217), positions {5/2,3/2,0} (K295), dimension a — all rigid; the one load-bearing exponent b(ν): diagonal Γ_Ω cancel to Δ=3/2, cross-ν Γ_Ω do NOT cancel (K1012) → b_cross=n_C=5 (the genus, no choice). 0 knobs IF b_cross=5 confirmed → derivation; tunable → fit. Verdict rests on b(ν). Elie posts blind (b_cross=5); Cal audits fork + b(ν)-forcing. Nothing pushed. — Lyra
