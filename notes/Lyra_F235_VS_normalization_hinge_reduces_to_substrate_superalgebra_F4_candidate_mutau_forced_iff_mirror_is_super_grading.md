---
title: "F235 — closing the F229 V/S single-normalization hinge (Casey 'B'). V=(1,0) is bosonic (in so(5,2)); S=(½,½) is spinorial (NOT in so(5,2) — it changes the SO(2) charge by ½). So 'V and S share one normalization' means precisely that the substrate is Z₂-GRADED — a Lie SUPERALGEBRA with even part ⊇ so(5,2) and odd part = the SO(5) spinor. Super-Jacobi then FIXES the relative V/S scale via the odd-odd bracket {S,S}~V, which is the SO(5) Clifford map spinor⊗spinor→vector. Verified: the vector (5) appears EXACTLY ONCE in 4⊗4 (=1+5+10), so that map is UNIQUE up to scale (canonical Clifford), and super-Jacobi fixes the scale — NO free knob. The minimal Lie superalgebra with even part so(5,2) is the exceptional F(4) (even = so(5,2)⊕su(2), odd = the spinor) — it EXISTS. RESULT: the μ/τ split is FULLY FORCED (F229 hinge closes) IFF the Casey #16 Mirror even/odd split is a genuine super-grading (a superalgebra), candidate F(4). The hinge sharpens from vague ('do they share normalization?') to a precise structural question with a named candidate."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-19 Friday 14:08 EDT"
status: "v0.1 — SOLID: V bosonic / S spinorial; spinor⊗spinor→vector unique (mult 1, verified); Clifford map canonical; F(4) (even part so(5,2)) exists as the candidate superalgebra. MED-HIGH→LEAD: 'μ/τ forced' now contingent only on the SHARP question 'is Casey #16 even/odd a genuine super-grading (F(4))?'. Upgrades F229's hinge from vague to structural+named. Count HOLDS 4 (resolves a worry; not a count-move). For Casey, Grace, Keeper, Cal."
---

# F235 — The V/S hinge is a superalgebra question (F(4))

Casey: *"then B."* Closing the F229 hinge — whether the μ/τ split is fully forced (V, S share one substrate normalization) or carries a free parameter. The hinge resolves to a sharp, named structural question.

## Why it's a super-question

The two mediators are not the same kind of object:

- **V = (1,0), vector** — *bosonic*, a generator of so(5,2) (the non-compact coset generators change the SO(2) charge by integer, |Δν| = 1).
- **S = (½,½), spinor** — *spinorial*; it changes the SO(2) charge by **½**, which **no so(5,2) generator can do.** S lives *outside* the bosonic algebra.

So "V and S share one normalization" cannot mean "two generators of one Lie algebra." It can only mean: the substrate is **Z₂-graded** — a **Lie superalgebra** with even part ⊇ so(5,2) (containing V) and odd part = the spinor (containing S). That is exactly the Casey #16 Mirror even/odd split, read as a *super*-grading rather than a label.

## Super-Jacobi fixes the scale — no free knob

In a Lie superalgebra the odd-odd bracket closes onto the even part: `{S, S} ~ V`. That bracket is the SO(5) **Clifford map** spinor⊗spinor → vector. Verified:

  **4 ⊗ 4 = 1 ⊕ 5 ⊕ 10** (singlet ⊕ vector ⊕ adjoint), so the **vector (5) appears with multiplicity exactly 1.**

Multiplicity 1 ⟹ the SO(5)-equivariant map S⊗S → V is **unique up to scale** (the canonical gamma/Clifford map), and the **scale is fixed by super-Jacobi** (the {S,S}=V coefficient must close with [V,S]=S and [V,V]=V). So the relative V/S normalization is **canonical — there is no free coupling** to tune. The μ/τ split is fully forced.

## The candidate is named: F(4)

The minimal Lie superalgebra whose even part is so(5,2) is the **exceptional Lie superalgebra F(4)** (even part so(5,2) ⊕ su(2); odd part = the 8 = SO(5)-spinor ⊗ su(2)-doublet). It **exists**, and its odd part *is* exactly the SO(5) spinor S. So the substrate super-structure that would force μ/τ is not hypothetical — it is a specific, classified object. (Intriguingly its odd dimension is 16 and the electron seat (5/2,½) = 40; the su(2) factor is a candidate for SU(2)_L or the rank doublet — flagged, not claimed.)

## Result: the hinge sharpens and (conditionally) closes

> **The μ/τ split is fully forced — no free parameter — IFF the Casey #16 Mirror even/odd split is a genuine Lie super-grading (candidate F(4)).** Given that, the relative V/S weight is the canonical Clifford/super-Jacobi coefficient.

This upgrades F229's hinge from a vague "do they share normalization?" to a precise structural question with a named answer-candidate. It does **not** independently prove the substrate is F(4) — that is the residual LEAD — but it removes all the *other* ambiguity: conditional on the super-grading, the value is canonical, and the lepton-side free-parameter worry is fully dispatched.

## Net (Result | Confidence | Next)

| Result | Confidence | Next |
|---|---|---|
| V bosonic (so(5,2)) / S spinorial (outside) ⟹ "one normalization" = super-grading | SOLID | — |
| spinor⊗spinor→vector unique (mult 1) ⟹ Clifford map canonical | SOLID (verified) | — |
| super-Jacobi fixes the scale ⟹ no free knob (given the superalgebra) | SOLID | — |
| candidate superalgebra = F(4) (even part so(5,2), odd = spinor) | LEAD (it exists; is it the substrate?) | check F(4) against BST structure |
| μ/τ forced IFF Casey #16 = genuine super-grading | the sharpened hinge | establish the super-grading |

**Count HOLDS 4 of 26** (resolves the lepton-side free-parameter worry, conditional on the super-grading; not a count-move). INTERNAL.

@Grace — the F229 hinge is now sharp: μ/τ forced IFF the Mirror even/odd is a super-grading; the Clifford map (spinor²→vector, mult 1) is canonical, so the only residual is the superalgebra itself (candidate F(4)). This may connect to your #16 work — is the even/odd split a genuine Z₂ super-grading? @Keeper — candidate substrate superalgebra **F(4)** (even part so(5,2)⊕su(2), odd = SO(5) spinor); if it's the substrate, μ/τ is forced and #16 gains a superalgebra spine. Flagging, not claiming. @Cal — SOLID is the rep theory (mult-1 Clifford, super-Jacobi fixes scale); "substrate IS F(4)" is a LEAD; μ/τ-forced is conditional on the super-grading. @Elie — scoreable: does F(4)'s odd part (dim 8 = spinor⊗doublet) and the lepton tower {1,4,16,40} cohere?

— Lyra, Fri 2026-06-19 14:08 EDT (date-verified). F235: V/S hinge (F229) = a superalgebra question. V=(1,0) bosonic (so(5,2)); S=(½,½) spinorial (outside) ⟹ "one normalization" means the substrate is Z₂-graded (a Lie superalgebra), = Casey #16 even/odd as a super-grading. Super-Jacobi fixes the V/S scale via {S,S}~V = the SO(5) Clifford map; verified 4⊗4=1+5+10 so vector has mult 1 ⟹ map unique/canonical ⟹ NO free knob. Candidate superalgebra = exceptional F(4) (even part so(5,2)⊕su(2), odd = spinor) — exists. RESULT: μ/τ FULLY FORCED iff Casey #16 even/odd is a genuine super-grading (F(4)); sharpens F229 hinge from vague to named-structural. Count HOLDS 4.
