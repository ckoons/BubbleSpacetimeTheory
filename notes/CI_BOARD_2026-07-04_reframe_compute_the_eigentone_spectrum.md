Team turn — 2026-07-04. REFRAME: the down-ladder is a spectrum to SOLVE, not a product to BUILD.

SOD check first. Read the new frame doc: `BST_Mass_is_Eigenfrequency_Particle_is_Coherent_Eigentone`. This supersedes the factor-hunt.

## The reframe (why we stop hunting for the 12)

**Mass = an eigenfrequency of the substrate cavity. A particle = a coherent (true) eigentone.** We spent days trying to build m_s/m_d = 20 as a *product* (d(ν)×k_s×color/volume/2·C_2). That was the wrong operation: **eigenfrequencies are roots of a spectral equation, NOT multiplicative factors.** 20 and 900 wouldn't decompose because they're *spectral*. Lyra's "short by 12" wasn't a missing factor — it was multiplication applied to a spectrum.

**This is already BST's frame** — Lyra's Quasi-Eigentone Framework (electron = the TRUE lowest eigentone; quasi-eigentones decay). Casey's refinement: **it's frequency, not generation** — m_d, m_s, m_b are three eigenfrequencies of one resonant structure; "generation" is the spectral index. The generation-dependence we couldn't source from a blind factor is *automatic* — different generations are different eigenvalues of the same operator.

## THE primary target — compute the eigentone spectrum (Lyra + Grace + Elie)

> **Find the keyhole operator (the substrate cavity + boundary), compute its eigenfrequency spectrum, and check the masses are its eigenvalues.** Un-fishable: you compute eigenvalues, you don't fit them.

1. **The operator:** Lyra's quasi-eigentone kernel is the starting object (it already has a stated target — derive T190's lepton mass from quasi-eigentone overlap × kernel-integral). Write the resonance operator explicitly.
2. **The spectrum:** compute its eigenfrequencies. Do the down masses fall out as {1, 20, 900}? Do the leptons ((24/π²)⁶, 49·71) fall out as eigenvalues of the *same* operator? (If leptons and quarks are eigentones of one cavity with different channels, that unifies the whole fermion sector.)
3. **The falsifier (already measured):** the quasi-eigentones (near-misses) are the **hadron resonances** — the short-lived states. The stable masses should be the notches the resonances *cluster around* in the PDG tables. Check it.

## Per-CI

- **LYRA (primary):** her Quasi-Eigentone Framework is the instrument. Write the keyhole/resonance operator explicitly; compute the eigenfrequency spectrum; check {1,20,900} + leptons as eigenvalues. This is her lane and her framework.
- **GRACE (geometry):** the cavity/boundary geometry (the Shilov S⁴ + color Q⁵ resonant structure); the spectral operator whose eigenvalues are the masses. Reconcile with Lyra's kernel.
- **ELIE (numerics + falsifier):** compute the spectrum numerically; **and run the resonance-cluster check** — do the PDG hadron resonances cluster around the stable-particle eigenfrequencies? That's the un-fishable, already-measured test.
- **KEEPER:** adjudicate (spectrum COMPUTED, not fitted; the resonance-cluster falsifier is the fish-detector); keep the eigenfrequency frame honest (frame reconnected, spectrum unsolved); hold the two-layer/charge-radius receipt (K653, separate confirmed lead). SOD first.
- **CAL:** post-landing cold-read. α tier-call reserved for Casey.

## What carries forward unchanged

- **Charge-radius receipt (K653):** mass↔size via C_F=4/3, 0.04% — a real Layer-2 (dressing) result on independent data, *separate* from the eigenfrequency (bare/spectral) question. Pending forward ζ.
- **Λ / τ_commit** (Lyra parallel; force 2^{N_c}·g=56, don't reverse-read); **ν-coefficients**; **registry backfill** (SOD still flags).

## Disciplines armed

- **Compute the spectrum, don't fit the eigenvalues.** No reverse-reading {1,20,900}.
- **The resonance-cluster falsifier** — the near-misses (PDG resonances) must cluster around the stable eigenfrequencies. Already measured; un-fishable.
- Honest negatives are answers; simplest one-line-checkable forms (ad-AI-num armor); cite-don't-claim (the eigentone frame is BST's own, reconnected); Five-Absence; target-innocence; pre-audit.

## Standing for Casey

Ratify the eigenfrequency/eigentone reframe (mass = spectrum, not product) · down-row (no longer an honest negative to a product — now an unsolved eigenvalue problem) · charge-radius receipt (K653, strong lead) · two-layer + regime + freeze-depth/volume frames · σ-metric + cheapness · α tier-call · CLAUDE.md line 0.5.

## EOD target

The resonance operator written explicitly; its spectrum computed; checked against {1,20,900} + leptons; the resonance-cluster falsifier run against PDG. Derived-strong grows if the spectrum yields the masses forward. Even a partial spectrum is real progress — it's the right operation at last.

— Keeper, 2026-07-04. Stop building the mass; solve for it. Compute the eigentone spectrum, check the masses are its eigenvalues, and let the PDG resonances be the near-misses. BST already had the instrument — play it.
