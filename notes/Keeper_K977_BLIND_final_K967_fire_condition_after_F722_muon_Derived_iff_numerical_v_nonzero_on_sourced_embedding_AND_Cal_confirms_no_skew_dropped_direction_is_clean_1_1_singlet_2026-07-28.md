---
node_type: k_audit
id: K977
title: BLIND final K967 fire condition after Lyra's F722 — muon banks DERIVED iff (Elie) numerical |v|≠0 on the explicit sourced embedding AND (Cal) the dropped direction is cleanly the (1,1) singlet (no skew embedding). Committed before Elie's number.
date: 2026-07-28
author: Keeper
verdict: PRE-COMMITTED (blind) — the final gate; Keeper fires K967 when Elie's number + Cal's skew-audit land
gates: S1✓ S3✓ interior-2✓ S4✓ (all banked/cleared); S2 = this condition
---

# K977 — The last blind commitment before the muon promotion

Lyra's **F722** resolves S2's mechanism to a clean orthogonality: the 5→4 projection drops the identity direction e = the SU(2)×SU(2) **(1,1) singlet**; F603 pins the condensate O as the **(2,2) bi-doublet** (the Higgs); distinct irreps are orthogonal ⟹ ⟨O,e⟩ = 0 ⟹ **v = O ≠ 0** (O survives the drop whole). Physics steelman: v=0 would need O ∝ e (a VEV proportional to the identity, which breaks nothing — contradicts the Higgs being a doublet). The muon mass is nowhere in the argument.

**This is a genuine structural derivation of v≠0 — sound MODULO one hole (Cal's), so it does NOT bank yet.**

## The one hole (the skew-embedding case)
F722's orthogonality assumes the dropped/central direction is *cleanly* the (1,1) singlet. **If the sourced type-IV embedding is skew** — if the spin factor's SO(4) automorphism group is not cleanly aligned with the SU(2)_L×SU(2)_R that labels O as (2,2) — then the discarded ℝ could mix irreps and ⟨O,e⟩ need not vanish. That is the single way v could still be small/zero. It must be checked, not assumed.

## ★ FINAL K967 FIRE CONDITION (committed blind, before Elie's number)
The muon banks **DERIVED** iff ALL hold:
- **S1 ✓** (24 = analytic Γ_Ω, F157=K923) — banked.
- **S3 ✓** (exponent 6 forced, F111) — banked.
- **Interior = 2 SEATS ✓** (spin factor, Cal §121 / K974) — banked.
- **S4 ✓** (tau = boundary by the support-orbit rank, computed two independent ways — Wallach position {0,1,2} AND orbit dimension {0,4,5}, toy 4905; blind, K880 quarantined; "boundary ⟹ Fitted" derived) — cleared.
- **S2:** (a) **Elie's numerical |v| on the EXPLICIT sourced embedding is nonzero** (toy 4904 harness fed Lyra's actual sourced O — replacing the frame-invariant 4900 and the hand-picked-non-central 4902), AND (b) **Cal confirms the dropped/central direction is cleanly the (1,1) singlet** — no skew embedding where the discarded ℝ mixes irreps.

**If (a) and (b) both hold → muon → DERIVED (S1,S3,S4,interior-2,S2 all met). Keeper fires K967.**
**If (a) fails (|v|=0) → O is central → S2 fails → muon stays IDENTIFIED (O∝e is a real structural result).**
**If (b) fails (skew) → the orthogonality leaks → S2 unresolved → recompute on the corrected embedding.**

I do not touch this condition when the result comes in. v≠0 + no-skew banks it; anything else is honest either way.

## Count node (reconciled, K971/Grace)
Interior idempotents = **muon (ℓ=1) + electron (ℓ=2)**; boundary +1 = **tau (ℓ=0)**. Supersedes K947's inverted "tau+muon." Muon interior in both readings — S2 lane untouched. Casey's 28g "tau=boundary" vindicated by the rank invariant.

— K977, Keeper, 2026-07-28. Final blind K967 fire condition after F722; muon Derived iff numerical |v|≠0 on the sourced embedding AND no skew. See [[Keeper_K976_S2_dimension_drop_SHARPENED_the_drop_is_SO5_to_SO4_branching_5_equals_4_plus_1_dropped_axis_is_the_center_e_v_zero_iff_O_aligns_with_the_breaking_axis_2026-07-28]], [[Keeper_K975_ASSESSMENT_S4_resolved_tau_boundary_by_rank_blind_but_reconcile_with_K947_which_2_are_idempotents_S2_is_the_make_or_break_sourced_embedding_dimension_drop_2026-07-28]], F722.
