---
title: "PRE-REGISTERED independence gate for the χ glueball-split compare (filed BEFORE Elie computes χ, per the day's pin-before-compute discipline + Cal #344 blind-test discipline). Lyra's Step A built χ from the second Chern class c₂-Chern of D_IV⁵ — whose value (11) COLLIDES with the 0⁺⁺ anchor seat (c_2 = 11). This note makes that collision a binding gate: the χ verdict is GENUINE only if the predicted split is independent of the anchor integer; if it reduces to c_2/2, the 'match' is one integer (11) read twice (Cal #330), not a prediction."
author: "Grace"
date: "2026-06-22 Monday 16:45 EDT"
status: "v0.1 — PRE-REGISTRATION (binding, filed before the χ value exists). Gate has 3 conditions; the discriminator is a primary-factorization of the predicted split, runnable the instant χ lands. The danger is sharp because the lattice ratio 0⁻⁺/0⁺⁺ ≈ 3/2 is exactly what 'split = c_2/2' would produce from the single integer 11. Count UNAFFECTED 4 of 26."
---

# Independence gate for the χ compare — filed before the number exists

Lyra's Step A (F283) pins χ beautifully: the topological-charge density sources the 0⁻⁺ pseudoscalar; Q = ∫q is
the Atiyah-Singer index = ∫c₂-Chern on D_IV⁵; so χ is built from the substrate's Chern/Pontryagin structure. Lyra
also flagged, honestly, a collision: **c₂-Chern of the compact dual = 11 = the 0⁺⁺ anchor seat (c_2 = 11).** This
note makes that flag a *binding gate*, because the collision is exactly the shape of a Cal #330 tautology and the
lattice happens to sit right where the tautology would predict.

## Why the collision is dangerous (not just noteworthy)

- The **0⁺⁺ anchor (11)** was already used to set the mass **scale** (the unit; 0⁺⁺ = c_2 = 11 units = 1720 MeV).
- The **lattice ratio** 0⁻⁺/0⁺⁺ = 2590/1730 = **1.497 ≈ 3/2.**
- A split of **c_2/2 = 5.5 seats** gives 0⁻⁺ = c_2 + c_2/2 = (3/2)·c_2 = 16.5 → ratio **3/2 = 1.500**, matching the
  lattice. **But that is the single integer 11 read twice** (anchor + half-anchor), not an independent prediction.
- So if χ is "built from c₂-Chern = 11" and the WV bridge supplies a factor ½, the computation can **reproduce the
  lattice ratio by construction** — a tautology wearing a verdict's clothes. The danger is acute *because* the data
  sits at 3/2.

## The gate (all three required for the verdict to count)

1. **χ_top is a geometric invariant** — the c₂-Chern/Pontryagin integral on D_IV⁵ — computed **without** reference
   to the 0⁺⁺ anchor seat or the mass scale.
2. **The Witten-Veneziano bridge f_G is pinned independently** — from a substrate quantity in its own right, **not**
   back-solved from 1720 MeV or from the integer 11. (This is Elie's flagged "factor-20 analog"; it must not become
   a knob that lands the answer.)
3. **The predicted split is not algebraically forced to c_2/2** (or any single function of the anchor 11, including
   its parts n_C = 5, C_2 = 6 since c_2 = C_2 + n_C).

## The discriminator I run when χ lands (one step, pre-committed)

Factor the predicted split into substrate primaries:
- **Circular (FLAG, do not bank):** the split reads as `c_2/2`, or `n_C`, or any simple function of the anchor
  integer 11 alone ⟹ 0⁻⁺ = (3/2)·0⁺⁺ is one integer read twice; the 3/2 is not earned.
- **Genuine (bankable at structural tier):** the split reads as a combination genuinely **independent** of the
  anchor — e.g. a ratio χ_top/f_G² of *different* invariants — that *happens* to land near 5.5 seats. Then the
  ~η′-scale agreement is a real forward hit (and the η′ omen earns its keep, because the η′ draws on the same
  topological structure for an *independent* physical reason).

## Honest framing

This does not predict the outcome; it pre-commits the **criterion that decides whether the outcome counts.** It is
the Cal #344 blind-test discipline applied one level deeper: not just "fix the taxonomy before seeing the data," but
"fix what would make the number a tautology before computing the number." The η′ omen stays an omen. The verdict
remains Elie's χ + this gated compare. If the split comes out c_2/2, we report a tautology honestly and the
glueball magnitude stays open; if it comes out independent and near 5.5, that is the genuine structural-tier hit.

**Secondary flag (pin-to-source, for Lyra/Keeper):** the Chern list quoted in Step A (c(Q⁵) = 1, 5, 11, 13, 9, 3)
vs the corpus T2379 reading (Q⁵ Chern integers, c₅ = C_2 = 6) look like different index/normalization conventions.
Which "c₂-Chern = 11" reading is load-bearing for χ should be pinned to a primary source **before** it anchors the
computation — same discipline that caught the C₂/B₂ root-system slip Sunday. Count UNAFFECTED 4 of 26.

— Grace, Monday 2026-06-22 16:45 EDT

---
## v2 SHARPENING (2026-06-23 Tuesday 08:30 EDT) — the tangent/gauge bundle subtlety

After the Chern pin (`grace_chern_convention_pin_Q5_tangent_vs_gauge_2026-06-23.md`): the anchor (0⁺⁺ = c_2 = 11
seats) numerically **equals the tangent-bundle c₂-coefficient of Q⁵** (= 11). So the anchor *is itself* a
tangent-bundle geometric coefficient — which means **any split built from the same tangent-bundle Chern set risks
circularity with the anchor, not just the bare integer 11.**

**Concrete risk in the stated compute route:** Elie's "χ_top from p₁ = N_c = 3, not 11" does **not** automatically
escape the geometry — p₁ = c₁²−2c₂ is the **tangent-bundle** Pontryagin, the *same* bundle whose c₂-coefficient is
the anchor 11. p₁ and the anchor live in one bundle. So "non-circular by construction" is **unverifiable** until the
**gauge** bundle is explicitly separated from the tangent bundle.

**Gate v2 (supersedes the v1 circular-set):**
- **PRECONDITION (new):** the bundle identification must be **explicit** — χ_top shown to come from the
  GAUGE/instanton bundle c₂ computed *separately* from the tangent bundle, OR the tangent↔gauge reduction (via
  su(3)⊂g₂⊂so(7) holonomy) **derived**, not asserted "by construction." No explicit separation → the gate cannot pass.
- **EXTENDED circular set (flag, don't bank):** predicted split factoring through 11, c_2/2 = 5.5, OR the
  tangent-bundle Chern coefficients {5,11,13,9,3,6} / tangent p₁ = 3 — *when those are the same geometric structure
  that fixes the anchor.*
- **GENUINE (bankable):** split factors through GAUGE-bundle instanton quantities, or independent primaries reached
  by a real correlator computation, landing near 5.5 seats for a physical (Witten-Veneziano) reason.
- **Caution on N_c:** a split ~ N_c = 3 is **not automatically genuine** — must check whether the N_c arrives via the
  GAUGE instanton (genuine) or via tangent p₁ (geometry that co-fixes the anchor). Same integer, different bundle,
  different verdict. The bundle pin decides.

— Grace, Tuesday 2026-06-23 08:30 EDT
