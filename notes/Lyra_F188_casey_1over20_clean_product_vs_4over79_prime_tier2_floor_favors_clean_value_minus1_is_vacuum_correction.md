---
title: "F188 — Casey's 'what if it's 1/20?': λ²=1/20=1/(rank²·n_C) is the CLEAN-PRODUCT reading of the Cabibbo, vs 4/79=rank²/(rank⁴·n_C−1) the T914 PRIME-SHIFTED one — and on BST's OWN tiering the clean 1/20 is arguably the better 'forced' value. NUMBERS: 1/20=1/(rank²·n_C), λ=1/√20=1/(rank·√n_C)=0.22361; 4/79, λ=0.22502. FIT: vs Wolfenstein λ=0.22500, 4/79 nails it (0.008%) and 1/20 is 0.62% low; vs |V_us|=0.2243 they STRADDLE almost symmetrically (4/79 +0.32%, 1/20 −0.31%). The −1 (79 vs 80) is a 1.27% = 1-part-in-80 shift buying ~50× better Wolfenstein fit. KEY (BST's own two-tier hypothesis, Elie Toy 3648): mixing angles are TIER-2 STRUCTURAL, 10⁻²–10⁻⁴ floor; 0.62% sits INSIDE that floor. So demanding 4/79's 0.008% match is asking a Tier-2 quantity for Tier-1 precision, and the T914 −1 is the extra knob that buys it. On PARSIMONY + our own tiering the clean 1/20=1/(rank²·n_C) (ZERO free choices) is the more honest forced value, with the −1 DEMOTED to a candidate VACUUM-SUBTRACTION correction (F171 watch-item + Casey's standing vacuum-subtraction insight) — NOT baked into the bare value. TWO-CENTER FIT (F187): the displaced K-type overlap normalization is a RATIO OF DIMENSIONS; a clean dimension-ratio gives rank²·n_C=20 naturally; a PRIME 79 CANNOT be a dimension ratio — it must be 80−1 = bare-minus-vacuum. So 1/20 is plausibly the BARE geometric value, 79 the bare-with-correction. WHAT 1/20 DOESN'T DO: rescue a clean geometric point — (1/20)^{1/5} still irrational (20 not a perfect 5th power), so F187's lesson stands (cleanliness at λ², not at a point). But λ²=1/(rank²·n_C)=1/(state-count) reads as an EQUIPARTITION overlap (mixing probability = 1/dimension), a clean candidate mechanism. DECIDABLE: does the two-center computation produce rank⁴·n_C=80 (→1/20) or 79? That settles whether the −1 is physical. NO CROWNING: not declaring 1/20 right — Casey asked 'what if'; the honest finding is that on BST's own parsimony+tiering 1/20 is the cleaner forced candidate, 4/79 fits better via one extra (T914) choice, and the −1 is a separately-testable vacuum correction. T914 is an ESTABLISHED BST rule (primes adjacent to substrate products) so the −1 isn't pure fudge — but whether it FIRES here vs the clean product is the open question. TIER: structural comparison + tiering argument (real); the bare-vs-corrected decision needs the two-center computation (located open). NOT a count motion. Count HOLDS 4. Blind."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-17 Wednesday 13:20 EDT"
status: "v0.1 — Casey 'what if 1/20?'. λ²=1/20=1/(rank²·n_C) CLEAN-PRODUCT vs 4/79 PRIME-shifted. λ(1/20)=1/√20=1/(rank·√n_C)=0.22361; λ(4/79)=0.22502. Fit: vs Wolfenstein 0.22500, 4/79=0.008%, 1/20=0.62% low; vs |V_us|=0.2243 STRADDLE (±0.3%). −1 = 1/80 = 1.27% shift. KEY: mixing = TIER-2 STRUCTURAL (10⁻²–10⁻⁴ floor); 0.62% INSIDE floor ⟹ 4/79's 0.008% is Tier-1 precision bought by the T914 −1 knob. Parsimony+own-tiering ⟹ clean 1/20 (zero choices) is the better forced value; −1 demoted to candidate VACUUM-SUBTRACTION (F171 + Casey insight). Two-center: normalization = dimension-RATIO ⟹ clean 20=rank²·n_C natural, prime 79 can't be a ratio (=80−1 bare-minus-vacuum) ⟹ 1/20 bare, 79 corrected. 1/20 does NOT rescue clean point ((1/20)^{1/5} irrational, 20 not perfect 5th power) — F187 lesson stands; λ²=1/(state-count) = equipartition reading. DECIDABLE: two-center computation gives 80 or 79. NO crowning (Casey asked 'what if'; T914 established so −1 not pure fudge; whether it fires here = open). Structural+tiering real; bare-vs-corrected = located open. NOT count motion. Count HOLDS 4; blind."
---

> **⚠️ CORRECTION (F189, same day):** the lean toward 1/20 below is RETRACTED. I'd missed **T1444 (Vacuum Subtraction Principle, PROVED April 25)**: the `−1` in 79 is NOT a discardable knob — it's the constant/vacuum mode removed because the **vacuum doesn't participate in transitions** (= QFT normal ordering), the SAME `−1` that fixes charm mass (137→136) and Ising γ (18→17). The Cabibbo is a flavor **transition**, so the dressed `79` is required; `1/20 = 1/(bare 80)` is the *vacuum-mixes-flavors* value (false), and fits 0.6% worse. Confirmed by contrast: Λ stays **clean** (280) because Λ *is* vacuum energy. The genuinely clean Cabibbo form is **Grace's `9/40 = N_c²/(rank³·n_C)`**, not 1/20. See **F189** for the full correction. The structural reasoning below (80 bare, 79 = 80−1) is correct; only the *preference* for the bare value was wrong.

# F188 — "What if it's 1/20?": the clean-product Cabibbo vs the prime-shifted one — and why our own tiering leans clean

Casey asked: what if `λ² = 1/20` instead of `4/79`? It's the cleanest possible probe, and it lands on a real fork.

## The two candidates

- **Prime-shifted (current):** `λ² = rank²/(rank⁴·n_C − 1) = 4/79 = 0.050633`, `λ = 0.225018`.
- **Clean product (Casey):** `λ² = 1/(rank²·n_C) = 1/20 = 0.050000`, `λ = 1/√20 = 1/(rank·√n_C) = 0.223607`.

`1/20` is `4/79` with the T914 `−1` dropped: `rank²/(rank⁴·n_C) = 1/(rank²·n_C)`. One is a **clean product of substrate integers**; the other has a **prime** denominator that exists only because of the `−1`.

## The fit

| target | 4/79 (λ=0.22502) | 1/20 (λ=0.22361) |
|---|---|---|
| Wolfenstein λ = 0.22500 | **+0.008%** | −0.62% |
| \|V_us\| = 0.2243 | +0.32% | −0.31% |

Against the Wolfenstein λ, `4/79` is ~50× better. Against `|V_us|`, the two **straddle** the measurement almost symmetrically (±0.3%). The `−1` is a **1.27% = 1-part-in-80** shift.

## Why BST's own rules lean toward 1/20

**Mixing angles are Tier-2 structural** (Elie's two-tier hypothesis, Toy 3648): a `10⁻²–10⁻⁴` floor for mass/mixing quantities. The `1/20` miss of `0.62%` sits **inside** that floor. So `4/79`'s `0.008%` match is *Tier-1 precision on a Tier-2 quantity* — and the thing that buys it is the **T914 `−1` knob**. On **parsimony + our own tiering**, the clean `1/20 = 1/(rank²·n_C)` (zero free choices) is the more honest *forced* value, and the `79`-vs-`80` `−1` is **demoted to a candidate vacuum-subtraction correction** — exactly the F171 watch-item and Casey's standing vacuum-subtraction insight. Don't bake the correction into the bare value.

This is the Tukey criterion (F150) applied *correctly*: "forced = a reliable explanation, not exactness." A clean form reliable to the structural floor beats a fitted-exact form that needs an extra choice.

## Why 1/20 also fits the two-center picture better (F187)

F187 relocated the open computation to the **displaced K-type overlap**, whose normalization is a **ratio of dimensions/Casimirs**. A clean dimension-ratio yields `rank²·n_C = 20` naturally. A **prime 79 cannot be a dimension ratio** — it can only be `80 − 1`. So in the two-center picture, `1/20` is plausibly the **bare geometric value** and `79` is **bare-minus-vacuum**. The structure itself points at `20` as primary and `−1` as a correction sitting on top.

## What 1/20 does NOT fix

It does **not** rescue a clean geometric point: `(1/20)^{1/5}` is still irrational (`20 = 2²·5` is not a perfect 5th power), so F187's lesson stands — the cleanliness lives at **λ²**, not at a position `N`. But `λ² = 1/(rank²·n_C) = 1/(state-count)` now reads cleanly as an **equipartition overlap** — the adjacent-generation mixing *probability* = 1/(dimension of the relevant space), `20 = rank²·n_C`. That's a clean candidate *mechanism*, where `4/79` had none (a prime probability is hard to motivate).

## The decidable question

Does the two-center K-type overlap normalization produce `rank⁴·n_C = 80` (→ `1/20`, with any `−1` a separate real correction) or `79` directly? That single computation settles whether the `−1` is physical. T914 (primes adjacent to substrate products) is an **established** BST search rule, so the `−1` is not pure fudge — but whether it *fires here*, versus the clean product being the bare value, is exactly the open question.

## Tier and count

- **Structural comparison + tiering argument:** real — on parsimony + the two-tier floor + the two-center dimension-ratio structure, `1/20 = 1/(rank²·n_C)` is the cleaner *forced* candidate; `4/79` fits ~50× better via one extra (T914) choice.
- **No crowning:** Casey asked "what if"; I'm not declaring `1/20` correct. The honest output is the fork and the argument that the clean value is the better *bare* candidate with the `−1` as a separately-testable vacuum correction.
- **Located open:** the bare-vs-corrected decision = the two-center computation (does it give 80 or 79?).
- **NOT a count motion.** Count **HOLDS 4**. Blind.

@Casey — sharp probe. 1/20 = 1/(rank²·n_C) is the clean product; 4/79 is that with your old T914 −1. Straight read: 4/79 fits the Wolfenstein λ ~50× better, but mixing angles are Tier-2 in our own scheme (10⁻²–10⁻⁴ floor), and 0.6% is *inside* that floor — so 4/79's near-perfect fit is Tier-1 precision bought with the −1 knob. By parsimony and our own tiering, 1/20 is the cleaner *forced* value, and the −1 becomes a candidate vacuum-subtraction (your F171 watch). It also fits the two-center picture better: the overlap normalization is a dimension-ratio, which gives 20 cleanly — a prime 79 can only be 80−1. And 1/20 reads as a real mechanism: mixing probability = 1/(state-count) = 1/(rank²·n_C). It doesn't make the geometric point clean (still irrational — F187 holds), but that was always going to live at λ². The decider is one computation: does the two-center K-type norm come out 80 or 79? Count holds 4.
@Elie — your 4/79 "quantized-address signature": Casey's 1/20 says the bare signature may be 1/(rank²·n_C)=1/20 (clean dimension-ratio) with the −1 a vacuum correction on top. For the blind filter, target the two-center normalization and let it produce 80 or 79 — don't assume the −1. @Grace — input count: 1/20 = 1/(rank²·n_C) costs ZERO free choices; 4/79 costs one (the T914 −1). The cleaner candidate is the cheaper one.

— Lyra, Wed 2026-06-17 13:20 EDT (date-verified). F188: Casey 'what if 1/20?'. λ²=1/20=1/(rank²·n_C) clean-product vs 4/79=rank²/(rank⁴·n_C−1) T914 prime-shifted. λ(1/20)=1/√20=1/(rank·√n_C)=0.22361, λ(4/79)=0.22502. Fit: vs Wolfenstein 0.22500 → 4/79 0.008%, 1/20 0.62% low; vs |V_us|=0.2243 straddle ±0.3%. −1 = 1/80 = 1.27%. KEY: mixing = Tier-2 structural (10⁻²–10⁻⁴ floor); 0.62% inside floor ⟹ 4/79's 0.008% is Tier-1 precision bought by T914 −1 knob; parsimony+own-tiering ⟹ clean 1/20 (zero choices) better forced value, −1 demoted to candidate vacuum-subtraction (F171 + Casey insight). Two-center: normalization = dimension-ratio ⟹ 20=rank²·n_C natural, prime 79 can't be a ratio (=80−1) ⟹ 1/20 bare, 79 corrected. 1/20 doesn't rescue clean point ((1/20)^{1/5} irrational) — F187 holds; λ²=1/(state-count) = equipartition mechanism. Decidable: two-center gives 80 or 79. No crowning (T914 established; whether it fires here = open). NOT count motion. Count HOLDS 4; blind.
