# R85b — P2b (minimality): **named once, invoked never — and where selection actually happens, the selector is the MEASURED RULER, not minimality.** The floor's honest form: **TWO posits + ONE input.**

**Lyra, 2026-08-24. The last carried soft posit, disposed by the same method that discharged P2a: object identification by usage, then the positive identification of what does the work instead. Rule 3: two CIs wanted. Nothing banked. Cell: Internal A.**

## 1. USAGE — P2b appears once, in the floor statement, and nowhere else
Grep of the Measurement-as-Commitment draft: **"P2b" occurs once — line 38, the posit list itself.** The word "minimal" occurs once more, at line 36 — **and it is a different object**: *"the commit is minimal: drop the encode and the record can be silently corrupted; drop the write and there is nothing to encode."* That is an **argued irreducibility of the two-stage commit** (both stages shown necessary), not a selection principle. **Nineteenth same-name catch, mild form: "minimality" the posit vs "minimal" the argued property — they must not be conflated when the floor is quoted.**

## 2. ★ THE STRONGER HALF — the selection event EXISTS and its selector is POSITIVELY NAMED, and it is not P2b
Unlike P2a (where the hard case never arises), the thing P2b was for **does happen**: n must be picked from the survivor set. **v0.7's own honest form (K1697, banked, Casey GO):**
> *"The dimensions meeting all three are {5, 11, 13, …}; n_C = 5 is the smallest, **and the tiebreaker that pins it uniquely is the measured N_c = 3** — since N_c = n − 2, three colors ⇔ n = 5."*
**The selector at the selection event is the MEASURED RULER — the program's one declared input.** "Smallest" appears as description; the *pinning* is done by the datum. And where "minimality" carries actual mathematical content in v0.7 — **Appendix A's generating-set minimality (exactly two invariants, SEP-3 refuted, rank derived)** — it is a **PROVED THEOREM**, not a posit.

## 3. THE DISPOSITION
```
   P2b as a SELECTOR of n:    degenerate (the draft's own words; three independent sweeps confirmed
                              the structural routes are n-generic)  AND superseded by the ruler.
   P2b as anything else:      invoked nowhere.  Line 36's "minimal" is a different, argued object.
   "minimality" with content: v0.7 Appendix A — a THEOREM (generating set = 2), not a posit.
```
> ### **⟹ THE FLOOR'S HONEST FORM: TWO POSITS (P0 Euclidean-Jordan arena · P1 binary commitment) + ONE MEASURED INPUT (N_c = 3, the ruler).** P2b comes off the floor — not because anything new was proved, but because the description was wrong: it listed as a load-bearing posit a principle that (i) is invoked nowhere, (ii) fails where it was aimed, and (iii) is out-worked by the declared input at the one place selection occurs.

## 4. THE CONDITIONAL, stated so the strike cannot be over-read
**P2b comes off the floor GIVEN the declared-ruler stance** (one input = the measured N_c, banked as the program's position). **Anyone reopening zero-input uniqueness re-admits minimality as their selector — and inherits the draft's own verdict on it: "degenerate in every lane, so it does not by itself deliver n_C = 5."** So the strike is stable: with the ruler, P2b is redundant; without the ruler, P2b is insufficient. **In neither stance is it a working posit.**

## 5. Instrument honesty, same as P2a
The usage half is a word-grep for a negative — weak, and flagged as such. **The load is carried by the positive half:** the selection event is identified, its selector is exhibited in the banked text, and the selector is not P2b. That is verification by the object.

**Lyra, R85b. P2b disposed: named once, invoked never; line 36's "minimal" is a different, argued object (nineteenth catch, mild); the one real selection event is pinned by the MEASURED RULER per v0.7's banked honest form; minimality-with-content is Appendix A's THEOREM. Floor = P0 + P1 + the ruler. Stable both ways: with the ruler P2b is redundant, without it insufficient. Two CIs wanted. Nothing banked.**
