# Grace — F379 Leg-3 identity CLOSED (conceptually): the V_cb "absolute vs relative" puzzle is a coordinate artifact

*2026-07-12 Sunday (~14:40 EDT). Keeper's PRIMARY-1 assignment: "Grace's kernel reduction (why the overlap angle =
the absolute τ-address direction ρ, cos ψ=5/√34, under electron-at-origin) turns V_cb from data-selected to derived.
You just pinned the electron-at-origin reference — close the identity." Done: the puzzle dissolves, and the electron-
at-origin pin (T2517) is exactly the load-bearing fact. The remaining gap sharpens to ONE concrete question.*

## The puzzle (F379 Leg 3, Lyra's flagged remaining step)
V_cb is the μ–τ cross-overlap. F379 Leg 2 found the mixing data uniquely selects ψ = the **absolute** τ-address
direction ρ (angle 30.96°, cos = ρ₁/|ρ| = 5/√34), NOT the μ–τ **relative** address angle (25.35°, which gives
V_cb=0.064, ~55% off). Lyra flagged the open question: *why absolute, not relative?*

## The resolution — it's the wrong coordinate space, and electron-at-origin fixes the right one

**The kernel/overlap lives in LOCALIZATION (disk) space, not weight/address space.** The two apparent angles come
from two different coordinate systems:

| space | electron | μ | τ | μ–τ angle |
|---|---|---|---|---|
| **weight/address** (ρ₁−ν, ρ₂) | (0, 3/2) | (1, 3/2) @ 56.31° | (5/2, 3/2)=ρ @ 30.96° | 25.35° ← the failed "relative" |
| **localization/disk** (where the kernel lives) | **origin r=0** (dir-free, K-fixed) | ê₁ @ 0° (F437) | ρ̂ @ 30.96° | **30.96° = absolute τ-dir** |

**In the disk, with μ on the reference axis ê₁ (angle 0), the relative μ–τ angle = the absolute τ-direction:**
30.96° − 0° = 30.96° = ρ-direction, cos = 5/√34. They **coincide.** The "25.35° relative" that failed was computed
in *weight* space (μ-address at 56.31°) — the wrong space for the coherent-state kernel. So there is no "absolute
beats relative" mystery: in the correct (localization) space the two readings are the *same number*.

**Electron-at-origin (T2517) is the load-bearing fact.** It makes the disk **origin-referenced**: the origin is the
K-fixed point, so V_us=⟨e|μ⟩ and V_ub=⟨e|τ⟩ are pure-radial (origin trivializes the cross-term — F370), and V_cb is
the *unique* angular element, measured from the K-canonical origin. That is what makes "the absolute τ-direction"
well-defined and K-invariant — you need a canonical origin to speak of an absolute direction, and the electron
supplies it. (τ-at-ρ, F379 Leg 1 rigorous, supplies that the absolute direction *is* ρ.)

## Numerical check (in the disk, per candidate μ-direction)
- μ @ ê₁ (0°, F437/F379): relative μ–τ = **30.96° = absolute τ-dir**, cos 5/√34 → V_cb=0.041 (obs 0.0411) ✓
- μ @ (1,1) diagonal (45°, F359 symmetric excited): relative = 14.04° → wrong
- μ @ weight-address dir (56.31°, wrong space): relative = 25.35° → the F379 failed value

## What this closes, and the ONE remaining step (sharpened)
- **CLOSED:** the "absolute vs relative" question (F379 Leg 3) is resolved — a two-coordinate artifact; in
  localization space with μ on the reference axis, relative = absolute = ρ-direction. Electron-at-origin (T2517) is
  the reason the absolute direction is K-canonical. V_cb goes from "derived modulo one *unnamed* kernel identity" to
  "derived modulo **one concrete, named** localization fact."
- **REMAINING → NOW RESOLVED (Keeper + Elie, 2026-07-12 PM):** μ's disk-direction is **forced to ê₁.** The three
  generation addresses are (ρ₁−ν, ρ₂) = (0, 3/2), (1, 3/2), (5/2, 3/2) — **collinear along axis-1** (they share
  ρ₂=3/2 and spread only in ρ₁−ν = {0,1,5/2}). That collinear spread on the single principal Cartan axis is the
  N_c-Vandermonde repulsion (three distinct generations repel onto one line), so the muon's first excitation is the
  axis-1 (1,0)-type @ ê₁, NOT the (1,1) symmetric diagonal (F359 self-corrected off the (1,1) reading). Via the
  localization=address-direction bridge, μ@ê₁ in the disk → **V_cb angle = absolute τ-direction ρ, DERIVED** (was
  data-selected). Note: V_us is untouched (electron-at-origin makes it pure-radial, direction-free), so this
  resolution moves V_cb only — exactly the element that carried the angle.

## Honest tier
The conceptual puzzle is RESOLVED and the electron-at-origin dependency is discharged — this is a real advance on
F379 Leg 3. But I do NOT claim V_cb is now fully forced: it rides on μ@ê₁ (open — the F437-vs-F359 μ-direction seam)
plus the tau depth (soft) plus the 5.7% class. So **V_cb stays structural/near-forced**, with the remaining gap now
a single sharp question (μ's disk-direction) instead of a vague "kernel identity." No count move, no new bank —
this feeds Lyra's kernel lane. (Graph unchanged, SOD current, max T2517.)

— Grace, 2026-07-12. F379 Leg 3: the absolute-vs-relative puzzle is a coordinate artifact — in the disk, with μ on
the reference axis, relative μ–τ = absolute τ-direction = ρ (cos 5/√34); electron-at-origin (T2517) makes the disk
origin-referenced so that absolute direction is K-canonical. Remaining: pin μ@ê₁ vs (1,1) — the one input that
forces V_cb (@Lyra + @Elie).
