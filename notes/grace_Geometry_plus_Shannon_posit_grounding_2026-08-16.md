# Grace — Casey's "Geometry + Shannon": the information-theoretic grounding of the Phase-2 posits. 2026-08-16
*Casey's interjection engaged + reconnected to corpus. Right TYPE of argument (exterior selection). Honestly tiered — grounds/motivates, does not eliminate, needs blind pre-reg. ATTEMPT tier.*

## Casey's proposal
Rank 2 = "binary commitment" (the bit written). N_c = 3 = "trinary confinement" = the minimum indivisible unit that decides a question AND carries its own validation / parity / error-correction. The DUAL — binary write + trinary validate — produces a permanent record that can't be forged. Geometry + Shannon (append-only log + error correction) = the universe.

## Why this is the RIGHT type of argument (connects to the boundary-of-geometry meta-note)
My meta-result: the geometry **cannot** force its own parameters (rank, N_c) — every internal invariant presupposes them; they must come from an **exterior selection principle**. **Casey has named the exterior selector: information theory (Shannon/coding).** So "Geometry + Shannon" is not a slogan — it is the exact structure the meta-note demands: **P0 (the arena) is geometric; P1/P2 (the parameters) are information-theoretic.** The two halves fit the two roles.

## The corpus already carries the dual (reconnect)
- **rank 2 = Hamming distance 2 = error DETECTION** — T375 "Knudson Is Hamming Distance: two-hit = d = rank = 2." The minimal *tamper-evident* record (detects one error).
- **N_c 3 = Hamming distance 3 = error CORRECTION** — Steane [[7,1,3]] (Condition 13): 7 physical (=g), 1 logical, distance 3 (=N_c), "three color rotations to corrupt." T1315 disease-Hamming, T372 (8=2^{N_c}=max-correctable). The minimal *self-correcting/unforgeable* record (corrects one error).
- So the dual is real and pre-existing: **binary write (rank=2, detect) + trinary validate (N_c=3, correct) = an append-only error-correcting log.** g = 7 is the codeword length (2^k−1 Hamming). Three integers get coding roles: **g = length, N_c = distance, rank = the detect/logical layer.** (Schur-pattern: one information principle → three observables.)

## The honest tiering (both directions — this is the discipline, not a wave-through)
**What it DOES (real):**
1. The coding facts — "d=2 detects, d=3 corrects one error" — are **EXTERIOR Shannon theorems** (Hamming bound), true independent of D_IV⁵. So this can ground the parameters **non-circularly**, unlike the three geometric bridges I killed (occupation-bit, Born, dim ker Π — all internal, all circular).
2. It **re-casts P2a (exclusivity) into a MOTIVATED currency.** Cal's exact complaint was that P2a does the dimensional work "with no motivation." Casey supplies one: the choice "correction (d=3) vs mere detection (d=2)" is motivated — **"an unforgeable permanent record must be recoverable, not merely tamper-evident."** That is a *reason*, where P2a had none.

**What it does NOT do (hold the line):**
1. It does **not eliminate** the posit — it moves it. "Why rank-2 exclusivity" becomes "why does the record require *correction* (d=3) not just *detection* (d=2)?" A better-motivated question, still a chosen axiom.
2. **Post-hoc risk (my own meta-note discipline):** the Steane [[7,1,3]] corpus connection was likely *built to match* g=7, N_c=3. **It cannot be cited as forcing without blind pre-registration:** does "the minimal self-correcting atomic record" force distance-3 (hence N_c=3) *stated before* knowing the answer? The Hamming bound is exterior, so this is genuinely checkable blind — Keeper's to pre-register, not to assert.
3. **S3 held:** the coding integers (g=length, N_c=distance, rank=detect) must be counted once and kept apart from their other appearances (the two elevens, etc.).

## Net
Casey's "Geometry + Shannon" is the **correct kind** of answer to "where do the posits come from," and the corpus already realizes the binary-detect / trinary-correct dual. It **grounds P1 and motivates P2a from exterior information theory** — exactly the exterior selection the meta-note proved geometry cannot supply. It does **not** remove the axioms; it gives them a physical reason and a *blind-testable* form (does append-only-ECC force d=2/d=3 pre-registration?). Honest tier: **a real candidate exterior grounding, right type, corpus-consistent, blind-pre-reg pending — not a closure.** Routed to Keeper for the blind case-map (append-only ECC → distances, before the integers are named).
