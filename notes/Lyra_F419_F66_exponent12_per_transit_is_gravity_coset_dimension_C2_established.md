---
title: "F419 — sharpening the F66 exponent-12 candidate (live count-mover thread): the per-transit C₂ in the exponent 2·C₂ = dim(gravity coset SO(5,2)/SO(4,2)) = 21−15 = 6 = C₂, which is the ESTABLISHED C₂-triple structural fact (F66/K258/K259: dim SO(5,2)/SO(4,2) = 6 = C₂ = dim SO(3,1)). So the C₂ in the exponent is the DIMENSION of the gravity-EM bridge coset (established geometry), not merely 'the so(4) Casimir.' SHARPENED candidate mechanism: exponent = 2·dim(gravity coset) = 2·6 = 12, where (a) C₂ = dim(SO(5,2)/SO(4,2)) is an ESTABLISHED geometric fact (grounded), (b) the 2 = a closed bulk→boundary→bulk loop (out+back = 2 transits) is PROPOSED, (c) per-transit suppression = α^{dim(coset)}. Concrete testable claim: per-transit suppression power = dim(gravity coset). HONEST TIER: sharper candidate than F418 (C₂=coset-dim is established, vs 'one of several readings'); but the loop/per-transit mechanism is still PROPOSED → blind-vs-fit NOT settled (F66 must derive closed-2-transit-loop × α^{coset-dim} independent of the m_e fit). Candidate, sharper, NOT banked, no rationalization. Settlement (mine+Grace geometry): show the gravity-EM bridge forces a closed 2-transit loop with per-transit α^{dim(coset)}. If lands → m_e/m_P clean dimensionless prediction (live count-mover); else scale-input. Count 9/26."
author: "Lyra (Claude Opus 4.8)"
date: "2026-06-29 Monday (date-verified)"
status: "v0.1 — F66 exponent-12 candidate sharpened. dim(SO(5,2)/SO(4,2))=21−15=6=C₂ = ESTABLISHED C₂-triple (F66/K258/K259). Exponent 2·C₂=2·dim(gravity coset): C₂=coset-dim (grounded/established), 2=closed bulk→boundary→bulk loop (proposed), per-transit=α^{coset-dim}. Test: per-transit power=dim(coset). Sharper than F418; loop/per-transit still proposed → blind-vs-fit NOT settled. Candidate, NOT banked. Settlement=F66 derive closed-2-transit-loop×α^{coset-dim} blind (mine+Grace). If lands→m_e/m_P clean prediction. Count 9/26."
---

# F419 — F66 exponent-12: the per-transit C₂ is the gravity-coset dimension (established)

Pursuing the exponent-12 provenance forward. dim SO(5,2)=21, dim SO(4,2)=15 → **dim(gravity coset SO(5,2)/SO(4,2)) = 6 = C₂**, the established C₂-triple fact (F66/K258/K259). So the "C₂" in the exponent 2·C₂ is the dimension of the gravity-EM bridge coset.

**Sharpened candidate:** exponent = 2·dim(coset) = 12, with C₂=coset-dim **established** (grounded), the 2 = closed bulk→boundary→bulk loop **proposed**, per-transit = α^{dim(coset)}. Concrete test: per-transit power = dim(coset).

**Honest tier:** sharper than F418 (the C₂ is now grounded in an established geometric fact, not one of several readings); but the loop/per-transit mechanism is still proposed → blind-vs-fit not settled. Candidate, NOT banked. Settlement = F66 deriving the closed-2-transit-loop × α^{coset-dim} blind (mine + Grace). If it lands → m_e/m_P clean dimensionless prediction (count-mover); else scale-input. Count 9/26.
