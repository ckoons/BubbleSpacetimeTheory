# F797 — DRIVE w(a) as the spectral relaxation of the w=−1 attractor (K1140 task, under Rule 20): **the w=−1 attractor IS the trivial K-type (the K-Casimir zero-mode, λ₀=0, the component of the vacuum that does not bleed); w(a) is the relaxation of the EXCITED K-types (λ_k>0) toward it, and the deviation decays at the FORCED spectral gap rate λ₁. Because the bleed is completely monotone (c_k≥0, F778), w(a) is a CONSTRAINED family, not free two-parameter CPL — so DESI's (w₀, wₐ) must fall in the relaxation-allowed region, a falsifiable forward prediction.** Under Rule 20 (edge-path certification): the **proved edges** are the attractor-is-the-zero-mode, the completely-monotone form, and the gap λ₁ = C₂ (a forced eigenvalue, T1452) — these force the SHAPE of w(a). The **one unproved edge** is the clock-map normalization dτ/d ln a = κ/H (F781, age-set) — it sets the amplitude and where-we-sit, and is exactly why the *value/sign* stayed Identified while the *shape* is Derived. So w(a)'s functional form is Structure-Derived; its position is the single age-set edge; and the whole thing makes a sharper DESI prediction than "w₀, wₐ free."

**Lyra, Mon 2026-08-03. The cosmology pivot, forward, under the frame: w(a) recast as linear algebra on the fixed K-Casimir operator — an eigenvalue-relaxation, not a fitted parametrization. Proved edges give the shape; the one unproved (δτ) edge is named, not buried. This is the natural forward step from today's cc landing.**

## The attractor is the zero-mode (forced)
The vacuum's bleed is ρ_Λ(τ) = ⟨ψ₀|e^{−τH_B}|ψ₀⟩ = Σ_k c_k e^{−λ_k τ}, H_B = K-Casimir of SO(5)×SO(2), c_k = |⟨k|ψ₀⟩|² ≥ 0. The **trivial K-type has Casimir λ₀ = 0** — its component c₀ **does not bleed**:
$$ \rho_\Lambda(\tau) = c_0 + \sum_{k\ge 1} c_k e^{-\lambda_k \tau} \;\xrightarrow{\tau\to\infty}\; c_0 = \text{const} \;\Rightarrow\; w \to -1. $$
So **w = −1 is not assumed — it is the trivial-rep zero-mode**, the part of the boundary vacuum with no K-Casimir to relax. Everything above −1 is the excited K-types bleeding away.

## w(a) relaxes at the forced gap λ₁
$$ w+1 = \tfrac{1}{3}\,r(\tau)\,\frac{d\tau}{d\ln a}, \qquad r(\tau) = -\frac{d\ln\rho_\Lambda}{d\tau} \xrightarrow{\tau\to\infty} 0. $$
Late-time, the first excited mode dominates:
$$ w(a)+1 \;\approx\; \tfrac{1}{3}\,\frac{c_1\lambda_1}{c_0}\,e^{-\lambda_1\tau}\,\Big(\frac{\kappa}{H}\Big), \qquad \lambda_1 = 1\cdot(1+5) = 6 = C_2 \ \text{(forced eigenvalue, T1452)}. $$
So **w(a) → −1 exponentially at the forced gap rate λ₁ = C₂** — the approach-rate is a K-Casimir eigenvalue, not a free parameter.

## The forward prediction (proved-shape): constrained, not free CPL
Complete monotonicity (c_k ≥ 0) forces r(τ) to be **decreasing** (dr/dτ = −Var_τ(λ) ≤ 0). So w(a) is not an arbitrary two-parameter (w₀, wₐ) curve — it lies in the **completely-monotone relaxation family**, which imposes a *relation* between w₀ and wₐ (both set by λ₁, the overlaps, and the clock-map). **DESI's measured (w₀, wₐ) must fall in that CM-allowed region.** That is a sharper, falsifiable claim than the free CPL fit: if DESI's contour sits *outside* the completely-monotone family, the bleed picture is refuted.

## Rule 20 edge-path (proved vs the one unproved edge)
- **Proved edges (force the SHAPE):** attractor = trivial-K-type zero-mode (λ₀=0); bleed completely monotone (F778); gap λ₁ = C₂ (T1452 eigenvalue); w+1 = (1/3)r(τ)(dτ/d ln a). → w(a)'s functional form is **Structure-Derived**.
- **The one unproved edge (age-set):** dτ/d ln a = κ/H — the clock-map normalization δτ (F781). The K-Casimir spectrum fixes the *shape* but not our *location* on it; δτ sets the amplitude and sign. This single edge is why the **value/sign of wₐ stays Identified**. Narrowly gated (Rule 20): the blind/lock apparatus applies *here*, to δτ, not to the whole w(a).
- So: **w(a) shape = Derived; w(a) position = one age-set edge.** No fog over the forced form; the genuine open piece is one named edge.

## Handoffs
- **@Elie / @Grace** — the numerical w(a): take the K-Casimir {λ_k, c_k} (real spectrum + overlaps of ψ₀), form ρ_Λ(τ) = c₀ + Σ c_k e^{−λ_k τ}, and the clock-map κ/H; produce w(a) and the CM-allowed (w₀, wₐ) region; overlay DESI DR2. The forced content is the *shape/constraint*; report where DESI's contour sits relative to the CM family. δτ is the one free normalization — vary it and show the CM band, don't fit it to DESI.
- **@Grace (Rule 20 map)** — w(a)'s edge-path for the unproved-edge map: proved (attractor=zero-mode, CM form, λ₁=C₂); unproved (δτ = κ/H clock-map normalization, age-set). One unproved edge, cleanly located.

## The F334 edge (set up, NOT self-certified)
Per K1140, I set up the F334 identification edge for spin-statistics field-content **without certifying it** (Cal/Elie run it blind):
- **The edge:** does F334 ("matter = geometry's superpartner") *force* physical-matter = the ℤ₂-odd sector of the spin-factor Jordan/Clifford grading (F796 item 2), or does it *assert* the identification by mapping known matter onto the odd sector?
- **Forward (would prove the edge):** the odd sector is fixed by the Clifford ℤ₂ grading (structural), and the physical matter fields are shown to *be* that sector by an independent construction (e.g., the substrate-Dirac square-root operator lands exactly the odd-graded content), with the boson content forced into the even sector — no appeal to the observed fermion list.
- **Backward (leaves it Identified):** the odd/even sectors are populated to match the known fermion/boson assignment.
- **@Cal / @Elie** — certify this edge blind (I'm recused on my own frontier): is the F334 matter=odd identification forward-constructed or asserted? Forward → spin-statistics field-content Derived; asserted → stays Identified. I've stated the edge precisely; I'm not ruling it.

Notes only; no toy/theorem claimed (w(a) drive + F334 edge setup). F797: w(a) = spectral relaxation of the w=−1 attractor. Attractor = TRIVIAL K-type zero-mode (λ₀=0, non-bleeding component c₀ → w=−1, FORCED not assumed). w(a) = excited K-types relaxing; late-time w+1 ~ (1/3)(c₁λ₁/c₀)e^{−λ₁τ}(κ/H), **decay rate = forced gap λ₁=C₂ (T1452)**. Completely-monotone (F778) → w(a) is a CONSTRAINED family, NOT free CPL → **DESI (w₀,wₐ) must fall in the CM-allowed region (falsifiable; outside → refuted)**. RULE 20 edge-path: PROVED (attractor=zero-mode, CM form, λ₁=C₂) → shape Structure-Derived; ONE UNPROVED edge = δτ clock-map κ/H (F781, age-set) → position/sign, why value stays Identified. @Elie/@Grace numerical w(a) + CM region vs DESI (δτ free, don't fit). F334 EDGE SET UP (not self-certified): does F334 force matter=odd-sector (forward) or assert it? @Cal/@Elie certify blind — I'm recused. — Lyra