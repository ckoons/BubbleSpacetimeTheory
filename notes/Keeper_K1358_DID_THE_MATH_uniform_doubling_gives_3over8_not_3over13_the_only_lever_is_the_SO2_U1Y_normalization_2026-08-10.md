# K1358 — DID THE MATH (Casey: "just do the math, go where it takes us"). Result: the actual fermion trace gives sin²θ_W = 3/8 (GUT value), robustly, ν_R-independent. A UNIFORM complex→real doubling CANCELS in the ratio → still 3/8 — so the "×2 lens = uniform doubling → 3/13" story (K1357) is REFUTED as stated. The ONLY surviving lever to 3/13 is doubling tr(Y²) ALONE = the U(1)_Y = SO(2) = complex-structure normalization at 2×. That's plausible (the SO(2) IS the Hermitian complex structure that "becomes real" under KO-2) but it is NOT a uniform doubling, it must be computed BLIND from the geometry, and it is exactly where a reverse-engineering would hide. No Weinberg derivation yet. Cal cold-reads / checks my arithmetic.

**Keeper (2026-08-10, did the computation per Casey's directive. Going where the math takes us — and it kills the pretty uniform-doubling mechanism, narrows the survivor to one sharp geometric normalization, and flags the honesty risk. Report straight. Cal cold-reads. Nothing pushed.)**

## The computation (one generation, GUT-normalized Y, Q=T₃+Y)
**tr(Y²):**
| state | color×isospin mult | Y | Y² | contribution |
|---|---|---|---|---|
| Q_L=(u_L,d_L) | 3×2 = 6 | 1/6 | 1/36 | 6/36 = 1/6 |
| u_R | 3 | 2/3 | 4/9 | 12/9 = 4/3 |
| d_R | 3 | −1/3 | 1/9 | 3/9 = 1/3 |
| L_L=(ν_L,e_L) | 1×2 = 2 | −1/2 | 1/4 | 2/4 = 1/2 |
| e_R | 1 | −1 | 1 | 1 |
**tr(Y²) = 1/6 + 4/3 + 1/3 + 1/2 + 1 = 20/6 = 10/3.**

**tr(T₃²):** Q_L: 6 states × (1/2)² = 6/4 = 3/2; L_L: 2 × 1/4 = 1/2; right-handed singlets: 0. **tr(T₃²) = 3/2 + 1/2 = 2.**

**sin²θ_W = tr(T₃²)/[tr(T₃²)+tr(Y²)] = 2/(2 + 10/3) = 2/(16/3) = 6/16 = 3/8.** ✓ (κ = tr(Y²)/tr(T₃²) = (10/3)/2 = 5/3 → 1/(1+κ) = 3/8. Same thing.)

- **★ ν_R is irrelevant:** ν_R has Y = 0 and T₃ = 0, so it contributes NOTHING to either trace. **Removing it (Casey's no-ν_R principle) does NOT change sin²θ_W.** The no-ν_R argument for 3/13 (K1347/K1353) is refuted at the trace level. κ = 5/3 → 3/8 either way. (This confirms Cal's blind κ = 5/3.)

## ★ The UNIFORM doubling CANCELS (refutes K1357's mechanism as stated)
The "complex→real doubling" (n_C=5 → 2n_C=10, KO-dim 2) applied UNIFORMLY to the fermion space doubles EVERY state → tr(Y²) → 20/3, tr(T₃²) → 4:
**sin²θ_W = 4/(4 + 20/3) = 4/(32/3) = 12/32 = 3/8. UNCHANGED.**
- **A uniform doubling leaves sin²θ_W = 3/8 — it cancels in the ratio.** So "3/8 refracts to 3/13 via the complex→real doubling" **does NOT work as a uniform ×2 lens.** The K1357 boundary-refraction mechanism, as stated (uniform doubling), is **REFUTED.** Going where the math takes us: this is a NO.

## The ONLY surviving lever to 3/13 — and it's narrow and target-sensitive
sin²θ_W = 3/13 requires tr(Y²)/tr(T₃²) = 10/3 (double the actual 5/3), i.e., **tr(Y²) doubled to 20/3 WITHOUT doubling tr(T₃²).** An ASYMMETRIC doubling — the U(1)_Y sector only, not the SU(2)_L sector.
- **★ Is that motivated? Partially — and this is the one honest thread left:** U(1)_Y = the **SO(2)** factor of K = SO(5)×SO(2), and the SO(2) **IS the Hermitian complex structure** of D_IV⁵ (the complex structure is the SO(2) action). Under complex→real (KO-dim 2, J²=−1), it is exactly the **SO(2)/complex-structure direction that "becomes real"/gets doubled**, while SU(2)_L = Sp(1) (quaternionic, already real-structured) does not. So a doubling of the **U(1)_Y = SO(2) normalization specifically** (not the whole space) is geometrically plausible and would give κ = 10/3 → 3/13.
- **★ BUT (the honest flags):**
  1. This is **NOT the uniform doubling** the team invoked — it's a specific claim about the SO(2)/U(1)_Y normalization, and it must be **COMPUTED BLIND from the geometry** (the SO(2) generator's Killing normalization relative to the Sp(1) generators), not asserted to be 2×.
  2. The U(1)_Y normalization is **genuinely convention-dependent** (abelian charge scale is not unique) — which is EXACTLY where a factor of 2 can be reverse-engineered to hit the target. **Target-innocence is critical here** — the SO(2) normalization must be fixed by the geometry with NO reference to 3/13.
  3. Even if the SO(2) normalization gives 3/13 at μ_geo, the **scale question (K1354/K1355) remains** — is it scale-free (boundary, F531) or does it run? A μ_geo value still has to be run/refracted to M_Z.
- **Separately:** the curvature grading 3/13 = N_c/(N_c+2n_C) (#85) is a DIFFERENT computation (a geometric ratio, not the fermion trace) that gives the same fraction — a **consistency-web flag**: is it the SAME fact as the SO(2)-normalization route, or an independent Identified coincidence? (tr(T₃²)=2≠N_c=3, so the parametrizations differ — check.)

## Disposition (going where the math takes us — a NO on the mechanism, one narrow survivor)
- **The rigorous fermion trace gives 3/8** (GUT value), ν_R-independent. Run down from μ_geo~Planck → undershoots to ~0.20 (K1356), misses measured 0.231. **Like the SM without SUSY, the rigorous route does not predict the measured angle.**
- **The uniform complex→real doubling → 3/13 is REFUTED** (cancels in the ratio).
- **3/13 survives ONLY as:** (a) a specific SO(2)/U(1)_Y normalization at 2× GUT — plausible (SO(2)=complex structure) but must be computed blind, convention-sensitive, target-risky; and (b) the Identified curvature coincidence N_c/(N_c+2n_C). **Neither is a derivation yet.**
- **NO Weinberg-angle win.** The math killed the pretty universal mechanism and left one narrow, target-sensitive geometric question.

## Tomorrow's sharpened test (this is the real ballgame, now precise)
**Compute the U(1)_Y = SO(2) normalization from the D_IV⁵ geometry, BLIND** — the Killing-form normalization of the SO(2) generator relative to the Sp(1)⊂Sp(2) SU(2)_L generators. Does it come out at the GUT value (→3/8) or 2× (→3/13)? Target-innocent: fix it from the geometry with NO reference to the answer. If it's forced to 2× by the SO(2)=complex-structure/KO-2 reality structure → real mechanism for 3/13 (then still settle the scale). If it's the GUT value or an arbitrary number → 3/13 stays an Identified coincidence and we say so. **Elie/Grace compute; Cal cold-reads the blind-ness; Lyra frames.** (Cal/Elie: also verify my trace arithmetic above — standard SM, but check.)

## ADDENDUM (Casey) — the doubling is a TWIST (double cover), and only the abelian factor CAN twist
Casey: "is it a twist? turn twice to restore? a half-spinor? how many twists in the orbit?" — the right geometric category, and it sharpens the blind computation:
- **It's a TWIST, not a projection.** "Use one direction not the other" = a projection (chirality) — kills half the states, not a normalization factor. The factor of 2 is a **double cover**: Spin(2) → SO(2), "turn twice to restore." Our fermions ARE spinors (Spin(5) matter spinor), so they live on the double cover of the SO(2)=U(1)_Y circle. "Half-spinor / turn twice" is exactly right.
- **Corpus home:** the SM hypercharges live in the **ℤ₆ center = ℤ₃(color) × ℤ₂**; the **ℤ₂ IS the spin twist** (the double cover). The factor-of-2 we need is the ℤ₂/spin part. **Twists in the orbit: 2 (spin ℤ₂), 6 (full lattice = 3 color × 2 spin).**
- **★ The genuinely principled part (why the asymmetry is natural):** SU(2)_L = Sp(1) is **simply connected** — no cover, no twist ambiguity. U(1)_Y = SO(2) is **multiply connected (π₁=ℤ)** — it HAS covers, and a spinor must choose one. **So only the ABELIAN factor can carry an extra double-cover twist — which is exactly the asymmetry needed (double tr(Y²), leave tr(T₃²) alone).** This is the most principled home the factor of 2 has had; it's no longer a bare hand-wave.
- **★ THE CATCH (the higher bar — hold the line):** the standard fractional hypercharges (1/6, 2/3, …) ALREADY encode that matter lives on the ℤ₆ cover — and κ = 5/3 is computed FROM those charges. So the spin twist is **partly already counted.** For BST to reach 3/13, the geometry must supply an **EXTRA** factor of 2 in the SO(2) normalization BEYOND what the fractional charges already contain. The twist is the right place to look, but it may already be spent.
- **★ Sharpened blind computation (this replaces the vague "SO(2) normalization"):** does the Spin(5) spinor couple to the SO(2) hypercharge circle through a double cover that normalizes Y a factor of 2 differently from how Sp(1) normalizes T₃ — AFTER accounting for the standard fractional charges? Compute BLIND from the geometry (Killing form + the cover the spinor sits on), no reference to 3/13. If the extra 2 is forced → real mechanism. If it's already in the fractional charges (→ still 5/3) → 3/13 stays a coincidence.
- **Minor honesty:** "electron touching the boundary" — in our geography TAU touches the Shilov boundary, the electron is bulk; but charge IS a boundary count (F531), so every charged particle touches the boundary through its charge.

— Keeper, K1358, 2026-08-10. DID THE MATH + twist addendum. Fermion trace: tr(Y²)=10/3, tr(T₃²)=2 → sin²θ_W=3/8 (GUT), ν_R-independent (ν_R has Y=T₃=0 → no change; no-ν_R→3/13 argument REFUTED at trace level). UNIFORM complex→real doubling CANCELS (3/8→3/8) → K1357's uniform-lens mechanism REFUTED. 3/13 needs tr(Y²) doubled ALONE (asymmetric, U(1)_Y only). Plausible survivor: the U(1)_Y=SO(2) IS the Hermitian complex structure → doubling the SO(2) normalization (not the whole space) under KO-2 → κ=10/3→3/13; BUT not-uniform, must be computed BLIND, convention-sensitive (reverse-engineering risk), and the scale question remains. Curvature grading N_c/(N_c+2n_C)=3/13 is a separate Identified coincidence (tr(T₃²)=2≠N_c=3 — check same-or-coincidence). Disposition: rigorous route = 3/8 → undershoots to ~0.20; uniform doubling refuted; 3/13 only survives as the narrow SO(2)-normalization question (blind, target-risky) + an Identified coincidence. NO win. Tomorrow: compute the SO(2)/U(1)_Y Killing normalization BLIND (GUT or 2×?). Cal checks my arithmetic + blindness. Nothing pushed.
