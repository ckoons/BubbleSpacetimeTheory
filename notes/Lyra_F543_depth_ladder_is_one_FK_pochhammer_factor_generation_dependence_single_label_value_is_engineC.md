---
title: "F543 — addresses → depths (the ~13-param unlock), first forward step from the sourced tower (F542): the generation mass/depth-LADDER is governed by a SINGLE FK Pochhammer factor (ν)_{k+1/2}, with the fixed-spinor part cancelling — confirming the one-parameter ladder at the NORM level (not just the label level); the absolute VALUE remains Engine C's (Elie). THE COMPUTATION: the sourced spinor-shell K-type is (a,b)=(k+1/2, 1/2) (F542, positive-energy UIR). The FK generalized Pochhammer for type IV (rank 2, root mult a=n_C−2=N_c=3, a/2=3/2) factorizes: (ν)_{(k+1/2,1/2)} = (ν)_{k+1/2} · (ν−3/2)_{1/2}. The SECOND factor (ν−3/2)_{1/2} is k-INDEPENDENT (the fixed b=1/2 spinor part) → ALL generation-dependence is in the single factor (ν)_{k+1/2}. So the generation mass/depth-ladder is one-parameter at the NORM level, confirming F541/F542's one-label ladder beyond the labels. Consecutive-generation norm-factor ratios: (ν)_{3/2}/(ν)_{1/2} = ν+1/2 (μ/e), (ν)_{5/2}/(ν)_{3/2} = ν+3/2 (τ/μ) — a linear ladder in the address. HONEST BOUND: FORWARD (this note, target-innocent rep theory) = the mass-ladder STRUCTURE (generation-dependence = the single FK factor (ν)_{k+1/2}, fixed-spinor cancels in ratios); NOT forward here = the ABSOLUTE mass value (e.g. m_μ/m_e=(24/π²)⁶, Elie's determinant) needs the boundary-integral evaluation (the norm ratio alone is (ν+1/2)-linear, NOT the ⁶-power form), so the depth/mass MAGNITUDE rides on Engine C (Elie) while the STRUCTURE (one-factor ladder) is forward. Nothing banked. So 'addresses→depths' advances: the depth-LADDER is one sourced Pochhammer factor; the VALUE is Engine C. For Casey (the next step toward the mixing/mass numbers advanced: now that the particle addresses are forward-sourced, I checked what sets their 'depth' (which drives the masses) — and the generation-to-generation part is controlled by a SINGLE mathematical factor, because the spinor part of the address is fixed and cancels out; so the whole mass-ladder is genuinely one-dimensional, confirmed at the level of the actual norms, not just the labels; the exact NUMBER (like the muon/electron 206.77) still comes from Elie's boundary-integral engine — that part I don't fake — but the SHAPE of the ladder is now forward), Keeper (F543: addresses→depths first step — FK Pochhammer (ν)_{(k+1/2,1/2)}=(ν)_{k+1/2}·(ν−3/2)_{1/2}, second factor k-independent → generation-dependence is the single (ν)_{k+1/2}; one-parameter ladder confirmed at the NORM level; consecutive ratios ν+1/2, ν+3/2; STRUCTURE forward/target-innocent, absolute VALUE is Engine C (Elie), not banked), Elie (the depth-ladder reduces to one factor (ν)_{k+1/2}; the fixed-spinor (ν−3/2)_{1/2} cancels — so your Engine-C mass evaluation per generation only needs the single a=k+1/2 factor's boundary integral, and the (24/π²)⁶ absolute value is the piece the norm-ratio alone doesn't give (it's (ν+1/2)-linear) — the ⁶-power is your boundary determinant, cleanly separated from the norm-ladder), Grace (Engine-B depths: the generation-dependence is one factor (ν)_{k+1/2} on the sourced (k+1/2,1/2) tower; fixed-spinor cancels; the ladder is one-parameter at the norm level — feed this shape when the absolute normalization lands from Elie), Cal (F543: depth-ladder = single FK factor (ν)_{k+1/2} (fixed-spinor cancels), one-parameter confirmed at norm level, target-innocent; absolute value = Engine C not banked; consecutive ratios ν+1/2, ν+3/2)."
author: "Lyra (Claude Opus 4.8)"
date: "2026-07-14 Tuesday (date-verified)"
status: "v0.1 — addresses→depths first forward step from the sourced tower (F542). FK Pochhammer for the spinor-shell (k+1/2,1/2), type IV rank-2 (a/2=3/2): (ν)_{(k+1/2,1/2)}=(ν)_{k+1/2}·(ν−3/2)_{1/2}; second factor k-INDEPENDENT (fixed b=1/2) → generation-dependence = single (ν)_{k+1/2}. One-parameter ladder confirmed at NORM level. Consecutive ratios: μ/e = ν+1/2, τ/μ = ν+3/2. TIER: STRUCTURE forward/target-innocent (mass-ladder = one FK factor, fixed-spinor cancels); ABSOLUTE value (m_μ/m_e=(24/π²)⁶) = Engine C / Elie (norm-ratio alone is (ν+1/2)-linear, not the ⁶-power). Nothing banked. No fabrication."
---

# F543 — The depth-ladder is one FK Pochhammer factor (the value stays Engine C's)

First forward step on addresses → depths (the ~13-param unlock) from the sourced tower (F542).

## The computation

The sourced spinor-shell K-type is (a,b) = (k+½, ½) (F542, positive-energy UIR). The FK generalized Pochhammer for type IV (rank 2, root multiplicity a = n_C−2 = N_c = 3, a/2 = 3/2) factorizes:

$$(\nu)_{(k+\frac12,\ \frac12)} = (\nu)_{k+\frac12}\cdot(\nu-\tfrac32)_{\frac12}.$$

The second factor **(ν−3/2)_{1/2} is k-independent** — it's the fixed b=½ spinor part. So **all generation-dependence is in the single factor (ν)_{k+½}.** The one-parameter ladder (F541/F542) is confirmed at the **norm level**, not just the labels.

Consecutive-generation norm-factor ratios:

$$\frac{(\nu)_{3/2}}{(\nu)_{1/2}} = \nu+\tfrac12\ (\mu/e), \qquad \frac{(\nu)_{5/2}}{(\nu)_{3/2}} = \nu+\tfrac32\ (\tau/\mu).$$

## Honest bound

- **Forward (this note, target-innocent rep theory):** the mass-ladder **structure** — generation-dependence is the single FK factor (ν)_{k+½}, the fixed-spinor part cancels in every ratio.
- **Not forward here (Engine C / Elie's lane):** the **absolute** mass value (e.g. m_μ/m_e = (24/π²)⁶, Elie's determinant) needs the boundary-integral evaluation. The norm ratio *alone* is (ν+½)-linear, **not** the ⁶-power form — so the depth/mass **magnitude** rides on Engine C, while the **structure** (one-factor ladder) is forward.

Nothing banked. So *addresses → depths* advances: the depth-**ladder** is one sourced Pochhammer factor; the **value** is Engine C's.

## Plain-language version

Now that the particle addresses are forward-sourced, I checked what sets their "depth" (which drives the masses). The generation-to-generation part is controlled by a *single* mathematical factor — because the spinor part of the address is fixed and cancels out. So the whole mass-ladder is genuinely one-dimensional, confirmed at the level of the actual norms, not just the labels. The exact *number* (like the muon/electron 206.77) still comes from Elie's boundary-integral engine — that part I don't fake — but the *shape* of the ladder is now forward.

— Lyra, 2026-07-14. F543: FK Pochhammer (ν)_{(k+½,½)} = (ν)_{k+½}·(ν−3/2)_{½}; second factor k-independent → generation-dependence is the single (ν)_{k+½}; one-parameter ladder confirmed at the norm level; consecutive ratios ν+½, ν+3/2. Structure forward/target-innocent; absolute value = Engine C (Elie), not banked.
