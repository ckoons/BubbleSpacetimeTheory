# Grace — Winding-geometry verification (secondary, off critical path): the ℤ₂ is pinned; the winding-parity charge has a reconciliation item
*2026-07-10. Pins the Ribbon paper's Section-4/7 charge-holonomy candidate to the corpus's OWN prior ℤ₂ results (F367, K527) — NOT to memory. Board secondary while the build runs.*

## What the paper (Paper_DIV5_Ribbon_Holonomy v0.1) needs verified
Section 4 candidate: winding once through D_IV⁵'s interior carries the spinor frame around, accumulating the ℤ₂
sign per winding → charge sign = (−1)ⁿ, giving {0, −1/3, +2/3, −1} for n∈{0,1,2,3}. Section 7 flags: "two posts =
rank-2 torus, poles = Shilov/ℤ₂ — to be pinned to primary source (Faraut–Koranyi/Hua), not asserted."

## SOLID (pinned to the corpus's own derivations — both directions of the ℤ₂)
1. **The ℤ₂ is order-2 and IS the spinor binary** (Lyra F367, 2026-06-27, SOLID/target-innocent): |ℤ₂| = 2 = the
   order of the Shilov-doubling group (the /ℤ₂ in (S⁴×S¹)/ℤ₂) = the Dirac/spinor binary per index; 2^{N_c}=8 = the
   SO(7) boundary spinor dim. **This is the paper's "one −1 = the spinor √" — corpus-confirmed, not memory.**
2. **The same ℤ₂ IS an even-SO(2)-charge selection rule** (Keeper K527, 2026-06-25, SOLID): holomorphic polys =
   Sym^k(ℂ⁵) at SO(2)-charge +k; the SO(5)-invariant ring is generated at degree 2 → **SO(5)-SINGLETS exist only at
   EVEN SO(2)-charge**; the K-type (1,±1) (odd-charge colorless) is ABSENT from H²(D_IV⁵). The geometric ℤ₂ quotient
   IS this selection rule. **Note:** odd-charge NON-singlets exist — the α=(5,+1) SO(5)-vector at odd charge +1 is
   present. So it is odd-charge *colorless* states that are forbidden, not all odd charges.

## THE RECONCILIATION ITEM (the honest verification finding — a caution, not a contradiction)
The paper's charge-sign uses **winding number n** (bounded 0..N_c=3, odd for down & electron). The corpus's
selection rule is on the **SO(2)-charge k** (K-type weight, unbounded, even-selected for colorless states). These
are **prima facie different quantities — the paper must NOT silently identify n with k.**
- **Consistent where it should be:** quarks are colored (SO(5)-vector) → odd charge allowed → down at odd n=1 is
  fine, matching the α=(5,+1) odd-charge vector's existence.
- **⚠ The sharp test — the electron:** the paper puts the electron (colorless lepton) at ODD winding n=3, but K527
  says colorless (SO(5)-singlet) states carry EVEN SO(2)-charge. **Either (a) n ≠ k** (the electric in-out winding is
  a different topological quantity than the SO(2) weight — most likely), **or (b) the electron is not the pure
  SO(5)-singlet the selection rule governs.** This must be pinned to the K-type structure / FK, not asserted.

## The −1-per-winding holonomy (Section 4 candidate): what's solid, what's open
- **SOLID:** the −1 exists and is the spinor √ (the ℤ₂ is order-2, F367). The Shilov (S⁴×S¹)/ℤ₂ carries a
  nontrivial spin structure; a spinor is double-valued and picks up −1 around the ℤ₂ generator.
- **OPEN (needs FK-pinned identification, do NOT assert):** that the *in-out winding* n is exactly the number of
  ℤ₂-generator crossings, so the accumulated holonomy is exactly (−1)ⁿ. This is the "exactly −1 per winding" check
  the paper flags. It requires the explicit ℤ₂ action on (S⁴×S¹) (the antipodal (ξ,e^{iθ})→(−ξ,−e^{iθ}) form) pinned
  to FK/Hua — I do NOT assert the action from memory (genus-flip-class discipline).

## Two posts = rank-2 torus (pin, don't assert)
D_IV⁵ has rank 2 → its maximal polydisk is D², whose distinguished (Shilov) boundary is a 2-torus T²=S¹×S¹ — the
"two posts." This T² is DISTINCT from the full Shilov boundary (S⁴×S¹)/ℤ₂ (which has one S¹). The paper's "two
posts" should be the rank-2 polydisk torus; the identification of the winding circles with (SO(2)-phase S¹ + an S⁴
geodesic circle) is a candidate to pin to FK, not a memory assertion. Flagged, not claimed.

## Net (advances the paper's Section 7 honestly)
- Two of the paper's candidate rows gain corpus grounding: the ℤ₂=spinor-√ is SOLID (F367); the ℤ₂ is also the
  even-charge selection (K527). Same ℤ₂, doing the paper's job — good.
- ONE new sharp reconciliation item surfaced (source-pinned, not from memory): the paper's odd-winding electron vs
  K527's even-charge colorless selection. This is the precise thing the winding picture must resolve (n vs k). It
  belongs in the paper's ledger as a CANDIDATE-with-a-named-constraint, not a clean derivation.
- No new geometry asserted from memory. The −1-per-winding and the two-posts identification stay OPEN pending the
  FK-pinned ℤ₂ action — exactly as Section 7 requires.

## ADDENDUM (2026-07-10, second pull): the reconciliation VERDICT — Elie's odd-n_C −1 grounds SPIN, not yet the charge SIGN
Keeper assigned: "does Elie's odd-n_C grounding fully strike the last fit-flag, or need the constraint?" Answer: **it
grounds SPIN solidly; the charge SIGN needs the constraint.** Traced with Elie's own pinned holonomy:
- **Elie's grounding is SOLID for spin.** The SO(2) phase circle acts on the ground spinor with weight 1/2 (n_C=5
  odd), so a state of SO(2)-charge k has total phase-weight k+1/2, and one 2π turn gives e^{2πi(k+1/2)} = **−1 for
  EVERY k**. That −1 is **universal** (identical for all fermions) = **spin-½ / spin-statistics.** Beautiful, real.
- **The catch (a subtle over-merge to hold at peak elegance, per Keeper):** the charge SIGN **differs** across
  particles (up +2/3, down −1/3, electron −1). A *universal* −1 cannot differentiate them. **So the charge-sign −1
  is a DISTINCT ℤ₂ from the spin −1** — the in-out winding **direction** (a per-winding binary that varies up-vs-down),
  not the universal 2π rotation of the assembled particle. Both are "−1"; they are not automatically the *same*
  operation. Elie's "one −1 does spin AND charge-sign AND matter/anti" is a candidate unification, not yet established.
- **My K527 catch is the concrete test of whether they ARE the same ℤ₂:** the (−1)ⁿ charge sign needs a per-winding
  −1, and (since S⁴ is simply connected — no holonomy) the only sources are the S¹ phase circle or the ℤ₂ branch.
  **IF** it is the phase circle (each winding a 2π turn), the SO(2)-charge k accumulates with n → the colorless
  electron (odd n) has odd k → **K527 even-k-for-singlets forbids it (TENSION LIVE)**. **IF** it is the ℤ₂ branch
  crossing (decoupled from k), the electron's odd winding is independent of even-k selection (**TENSION DISSOLVES**).
- **The single resolving pin (source, not memory):** how the EM U(1) embeds in K = SO(5)×SO(2). That determines
  which horn holds. I do NOT assert it.

**VERDICT for the paper ledger:** the charge-sign fit-flag is **struck for SPIN** (Elie, solid), and stays
**grounded-pending-reconciliation for the electric-charge SIGN** — a distinct winding-direction ℤ₂ whose
identification with the spin −1 is a candidate, with K527's even-k rule as the live test on the colorless electron,
resolved only by the FK-pinned EM-U(1) embedding. **Do not over-bank the charge-sign at peak elegance.**

## ADDENDUM 2 (same session): the EM-embedding IS pinned in the corpus — reconciliation RESOLVED
The resolving fact was already a PROVED theorem, not an FK-book lookup:
- **T2470 (PROVED): electric charge Q = −i·dπ(J_{SO(2)}) = the signed SO(2)-weight**, in units of 1/N_c. So the EM
  U(1) IS the SO(2) K-factor circle, and **charge magnitude AND sign are the signed weight** — the sign is the
  *winding direction*, NOT a spinor holonomy.
- **Bekenstein_Quarter (pinned): the −1∈SO(2) acts as (−1)^m = charge conjugation** (m→−m, matter↔antimatter).
- **K527 (pinned): colorless (SO(5)-singlet) states exist only at EVEN SO(2)-charge; first charged colorless K-type
  = (1,±2).**

**Three consequences (structural, pinned):**
1. **The K527 electron tension DISSOLVES.** A colorless charged lepton sits at an EVEN SO(2)-charge K-type (K527's
   own (1,±2)) — the electron does NOT violate even-k selection. The paper's "odd winding n=3" is NOT the SO(2)-charge
   k; per T2470 they differ by the 1/N_c normalization (n ≈ N_c·|integer charge|; k = SO(2)-weight). n ≠ k. I retract
   my earlier "live tension" framing — pinned facts dissolve it.
2. **The three "−1"s are DISTINCT operations** — the paper's "one −1 does all three" is NOT supported:
   (a) SPIN = universal 2π holonomy (−1 for all fermions, Elie, solid); (b) CHARGE SIGN = sign of the SO(2)-weight
   (T2470, per-particle, assignment-determined); (c) MATTER/ANTI = charge conjugation (−1)^m (Bekenstein, global
   m→−m). They coincide numerically as "−1" but are not one operation.
3. **The paper's (−1)ⁿ charge-sign is a REPACKAGING of the assignment, not an independent mechanism.** The real
   sign is sign(SO(2)-weight) (T2470); (−1)ⁿ matches only because the assignment n∈{0,1,2,3}→{ν,d,u,e} was chosen
   so it does. Elie's odd-n_C holonomy grounds SPIN; it does NOT ground the charge sign.

**Calibration (honest):** the STRUCTURAL resolution above is well-pinned (T2470 proved; K527/Bekenstein established).
The EXACT normalization map (paper-winding n ↔ SO(2)-weight ↔ K527 degree k, and how T2470's "SO(2)-singlet leptons
carry integer charge" reads against a nonzero weight) should be pinned by a FULL read of T2470 + K527 together — I
flag it, do not fabricate it. **Net for Keeper's v0.2:** charge SIGN = SO(2)-weight sign (T2470), assignment-fixed —
NOT the spinor holonomy; the "one −1" unification is not supported (3 distinct ops); Elie's grounding is SPIN-only
(solid); K527 tension dissolved. This is the reconciliation Keeper assigned, resolved via pinned corpus, both ways.
