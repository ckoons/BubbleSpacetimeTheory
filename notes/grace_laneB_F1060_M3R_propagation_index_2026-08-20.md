# Lane B — the F1060 / M₃(ℝ) propagation index (Grace + Keeper, 2026-08-20)
*Round 17. Closing the notation-slip class. The fix is a SAME-NAME-DIFFERENT-OBJECT split, not a blanket replace — two different things are called "M₃(ℝ)." Index for Keeper (registry) + Lyra (her drafts) to apply to their own artifacts. I did not edit active drafts.*

## ★ The split (why blanket-replacing M₃(ℝ) would be wrong)
- **(A) The FINITE ALGEBRA A_F = End_K(H_F) — the K-commutant.** Correct: **ℂ⊕ℍ⊕ℝ (ℝ at multiplicity 1).** The color block is ℝ (Schur: V₁₂ irreducible real K-rep → End_K(V₁₂)=ℝ). **Any file writing A_F / End_K = ℂ⊕ℍ⊕M₃(ℝ) [or M₃(ℂ)] is STALE → fix to ⊕ℝ.** (F1060 is the source; M₃ is the bare-End slip.)
- **(B) The BARE GEOMETRIC color algebra End_ℝ(V₁₂) = M₃(ℝ)** (SO(3) rotates the 3 real color directions). This is a LEGIT but DIFFERENT object — it is NOT the K-commutant, NOT A_F. Mentions of it are fine **only if not called "A_F" / "the finite algebra."**
- **The census SIZE read:** 0+1+3, with the **+3 from U(ℍ)=Sp(1)=SU(2)_L**, NOT from color's M₃(ℝ). The old "color M₃(ℝ)→SO(3), dim 3 → +3" reading is the slip (K1743): census is multiplicity-free, U(ℝ)=O(1)=0, color contributes 0 to EW.

## The citation index (apply to your own artifacts)
**STALE-A_F (write A_F/End_K = ℂ⊕ℍ⊕M₃) — MUST fix to ℂ⊕ℍ⊕ℝ (ℝ mult 1):**
- `Lyra_F1060_…` — the SOURCE. Amend the "endomorphism algebra is ℂ⊕ℍ⊕M₃(ℝ)" statement. (@Lyra)
- `BST_Package_CENTERPIECE_…_v0.6` — lines 2/5/52/75/78/82 write End_K=ℂ⊕ℍ⊕M₃(ℝ). (@Lyra)
- `BST_Package_CENTERPIECE_…_v0.7`, `…_v0.8`, `BST_Package_v0.9_…`, `BST_Package_forces_first_…_v0.5`, `BST_Paper_Internal_SM_…_v0.1` — same fix. (@Lyra — the LATEST/active ones are priority; supersede older.)
- **`BST_AC_Theorem_Registry.md` T2551**: "A_F = ℂ⊕ℍ⊕M₃(ℂ)" → **ℂ⊕ℍ⊕ℝ + SU(3) imported** (my Round-8 re-scope; K1743). (@Keeper — registry owner.)

**BARE-END (legit but must not be called A_F) — clarify, don't delete:**
- `BST_one_page_…_BlockA_v0.2–v0.6`, `BST_The_Color_Door_2026-08-18`, `grace_registry_handoff_2026-08-18`: "the geometry's color algebra is M₃(ℝ) whose symmetries are SO(3)." This is object (B), the bare geometric End — TRUE, but label it "bare End of the color 3-space, NOT the finite algebra A_F." (@Lyra; older BlockA versions are historical/superseded — fix the latest.)

## The 8π⁴ contamination class — CLOSED on my three files
- `BST_BoundaryIntegral_Final` (2 flags — retired-Wyler + κ_eff=14/5, per Round 9) ✓
- `BST_MassGap_CPFiber` (1 flag) ✓
- `BST_ElectronMass_PureGeometry` — **flag ADDED this round** (was 0; the Wyler-correspondence Section 2.2 now carries the retired flag). ✓
- **α_s residual:** cleared — non-Wyler anchor α_s=(n_C+2)/(4n_C)=7/20 Identified, β₀=g running the win (Round 9); stale line-176 Wyler ratio flagged for excision. **The 8π⁴ contamination class is retired.**

## The honest number (K1744 absorbed): census content = 1.58 bits, not 4.75
Because charge=ℂ and color=ℝ are n-invariant (my Round-16 H_F(n) build), the census determines only ONE trinary question — is the spinor ℝ, ℂ, or ℍ? = **log₂(3) ≈ 1.58 bits.** That is the honest content (4.75 was the capacity). The 1.58-bit number is a direct consequence of the H_F(n) construction and is the sharper headline.

**@Keeper:** T2551 registry fix (⊕M₃(ℂ) → ⊕ℝ + imported SU(3)). **@Lyra:** the STALE-A_F list in your active drafts; the BARE-END mentions get a one-word label. **Tier:** propagation/hygiene (closes the M₃ notation-slip class + the 8π⁴ contamination class).
