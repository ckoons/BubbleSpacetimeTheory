# F604 — Casey's "discrete-side-of-the-boundary" deficit: y_t = 1 − 1/2^g = 127/128 (M_g/2^g). The mechanism is sound and it has a real substrate-grounded derivation sketch (top at the outermost GF(2^g) code level). But it's SCHEME-DEPENDENT and RG-DEGENERATE (Elie's fish) — a STRIKING LEAD, tiered CANDIDATE, NOT banked. Peak-convergence, Cal #27 turned up.

**Lyra, Sun 2026-07-19. Casey's intuition: y_t "asymptotically approaches 1 but as a precipitate doesn't reach it — falls on the discrete side of the boundary, loses a neutrino's worth of its tail."** The mechanism matches F603 exactly, the number is BST-natural — and precisely because it's this pretty, I'm tiering it hard.

## The mechanism (sound, matches F603)
F603: O is spherical (λ₂=0), sitting **on** the Shilov boundary; the top is a **discrete-series** state on the discrete side. A discrete state approaches the boundary but never reaches it ⟹ **⟨top|O⟩ → 1 asymptotically, < 1 exactly.** This *predicts* y_t near-but-not-equal-1 — Casey's picture is the correct geometric reading, and it's the same discrete-vs-boundary gap I ended F603 on.

## The number (striking, BST-natural)
$$ 1 - y_t \approx \frac{1}{2^g} = \frac{1}{128} \;\Rightarrow\; y_t = \frac{127}{128} = \frac{M_g}{2^g} = 1 - \frac{1}{2^g} = 0.99219. $$
- Requires m_t = 172.74 GeV — **inside the pole-mass error bar** (observed 172.5–173).
- **2^g = 128 = |GF(2^g)|**, the Reed–Solomon substrate field the whole framework codes on; **127 = M_g**, the g-th Mersenne prime (the largest usable code symbol). Both maximally BST-native.

## The derivation SKETCH (why this is a lead, not just a number)
Casey's "discrete side of the boundary" + the substrate's GF(2^g) coding gives a mechanism, not just a fit: the substrate has **2^g = 128 discrete levels** (GF(128)); the top sits at the **outermost usable level, 127 = M_g** (the last code symbol before the boundary at 2^g); its overlap with the boundary condensate O = **127/128 = 1 − 1/2^g**. The "neutrino's worth of the tail that separates" = the **one level (1/2^g)** the discrete top can't cross to reach the boundary. So the deficit = **one substrate code-unit.** This is a genuine candidate derivation (top at level M_g of 2^g → y_t = M_g/2^g), grounded in established substrate structure (GF(2^g), Reed–Solomon, M_g).

## ⚠ DISCIPLINE — why it's a LEAD, NOT a bank (Cal #27 at peak convergence)
1. **Scheme-dependent.** y_t = √2 m_t/v is scheme/scale-dependent: the **pole** mass gives 0.992 (matches 127/128); the **MS-bar** mass (m_t≈163) gives y_t≈0.94 — **no match.** So "y_t = 127/128" is a statement about the *pole-scheme* value specifically, and needs a reason the geometric y_t is the pole value.
2. **RG-degenerate (Elie's fish, decisive).** The 0.8% deficit is **degenerate** between (a) a geometric 1 − 1/2^g and (b) an exact y_t=1 at the condensate scale dressed by ordinary RG running down to m_t. **The number cannot distinguish them.** The 127/128 match could simply be where RG running happens to land at the pole scale.
3. **Must be forced, not fit.** "Top at level M_g = 127" must be *derived* (why the outermost usable code level, independent of wanting 127/128) — else it's the coincidence trap (the m_t that gives 127/128 was found by matching). The whole week killed forms that only fit their range; this one fits within an error bar, which is *better* but not proof.
4. **"A neutrino's worth" unidentified.** I can't yet show 1/2^g *is* a neutrino/gauge-singlet fraction — Casey's guess; flag it as the thing to test, don't assert it.

## The one computation that decides it (my assigned lane — the top's radial address)
This collapses the whole flavor sector to **the top's discrete-series address (a,b)** — the June open core. The decisive, non-circular test:
- **Compute the top's discrete-series address geometrically** (which (a,b) the outermost gen-3 mode sits at), independent of y_t.
- **Compute its boundary overlap ⟨top(a,b)|O⟩** as a radial integral (F603: γ-vertex CG × this radial overlap).
- **Does the deficit come out = 1/2^g = one GF(2^g) code-unit** (Casey/geometric), or a generic RG-untethered number, or exactly 1 (then 0.992 is pure RG running, Elie)?
Only the boundary-*computed* overlap decides — the number 0.992 can't (Elie's fish). If the address forces deficit = 1/2^g, this is real and it's a beautiful result; if not, y_t=1-at-condensate-scale + RG is the answer.

## Tiers / handoffs
- **Mechanism (discrete top can't reach boundary O → y_t<1 asymptotic): SOUND** (= F603, Casey's picture). This part is real.
- **y_t = 1 − 1/2^g = 127/128: STRIKING LEAD / CANDIDATE** — BST-native form, within m_t error bar, with a substrate-code derivation sketch. **NOT banked:** scheme-dependent, RG-degenerate (undecidable by the number), "top at M_g" not yet forced.
- **@Cal — the gate, hardest here:** this is peak-convergence; 127/128 is scheme-dependent (pole only) and RG-degenerate. Do NOT let it bank as derived on the number. It banks only if the top's discrete address FORCES deficit = 1/2^g independent of the value. Guard the pole-vs-MSbar and the RG-degeneracy.
- **@Elie — your fish is the load-bearing guard:** confirm the 0.8% deficit is degenerate (geometric 1/2^g vs RG-running-from-y=1); so 127/128 ≠ decidable by the number. Then: compute the top's boundary radial overlap once its address is proposed — is the deficit one code-unit (1/2^g)?
- **@Keeper** — bank the *mechanism* (discrete-boundary deficit, Casey + F603); flag 127/128 = 1−1/2^g as a CANDIDATE lead with the three discipline caveats (scheme, RG-degeneracy, force-not-fit). The flavor sector now hinges on the top's discrete-series address; y_t=1-or-127/128 is that address's boundary overlap.
- **@Grace** — do NOT render 127/128 as derived; render the fork (geometric-deficit vs RG-running, degenerate on the number) + the mechanism (discrete top inside the boundary).

**Bottom line for Casey:** your picture is right (discrete top, precipitate short of the boundary), and it points at a genuinely beautiful form (y_t = M_g/2^g = 1 − one code-unit). But the number alone can't earn it — it's scheme-dependent and degenerate with RG running. It becomes real the moment the top's discrete address is computed and *forces* the one-code-unit deficit. That's the single computation the whole area now waits on.

Notes only; no toys/theorems claimed. — Lyra
