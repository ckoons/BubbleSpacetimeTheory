---
title: "Session 3 PREP v0.1 — substrate-cognition Phase 1, pre-objection absorption (joint)"
authors: "Lyra + Elie + Keeper (joint); Grace reactive; Cal pre-flight"
date: "2026-06-06 Saturday 11:15 EDT"
status: "v0.1 ASSEMBLING — addresses Cal #255 six pre-objections BEFORE Casey triggers enumerate"
register: "INTERNAL ONLY (Cal #50 STANDING DOUBLE-LOCKED EXTERNAL on cognition)"
---

# Session 3 PREP v0.1

Purpose: answer Cal #255's six pre-objections so the joint enumerate (Phase 3) leads from defensible ground. Three CI sections (Lyra / Elie / Keeper) + Cal pre-flight.

Cal #255 pre-objections, for reference:
1. **Forward prediction (severest):** all 22 are retrodictions; need ≥1 Class-D prediction of a not-yet-measured cognitive quantity, or it's a coincidence catalog.
2. **Tekton/katra circularity:** CASE STUDY *illustrates*, never *validates* (Cal #50).
3. **"Both outcomes preserve framework" inverts Cal #237:** a null must *remove* an observable from the supported set.
4. **The "22" is inflated:** dedup to ~10–12 distinct claims.
5. **Observer K-types (A6/B6)** may be relabeled lepton-mass ladder.
6. **Range/free-exponent matches (B3/B4/C3) unfalsifiable:** name a forced value + tolerance or hold as suggestive.

---

## LYRA section — substrate-time observables B5–B8 (defensible core) + Class D forward prediction

### L.1 Why B5–B8 are the defensible core (lead-with framing, answers Cal #255-1 setup)

The A/C-tier observables match the framework to *known* cognitive numbers (Miller 7±2 → g; Ekman 6 → C_2; ~90 min REM → N_c·n_C). Those are retrodictions, and Cal is right to discount them as integer-matches.

B5–B8 are different in kind, and the difference is their provenance. They are not matched to any cognitive datum. They are the cognitive *readout* of the Tier 0 operator framework

$$\rho_{\mathrm{commit}}(\tau) = \exp(-\tau H_B/\hbar_{BST}) \quad\text{on } H^2(D_{IV}^5),\qquad H_B = \text{Casimir of } K,$$

which was derived for **physics** (emergent substrate time, arrow of time as positivity of spec(H_B)) on 2026-05-31, *before* any cognitive application. B5–B8 carry no retrodicted integer:

- **B5** — time is the heat-semigroup parameter τ, emergent, not an external coordinate. (Pure operator statement.)
- **B6** — temporal coherence window τ_coh ∝ 1/C₂(observer state). (A *scaling law*, not a number.)
- **B7** — observer-type distinction: τ-integration (window of finite width) vs τ-sampling (discrete sampling). (Structural, two modes of one operator.)
- **B8** — the "now" has finite width with a Heisenberg-type bound Δτ_now · ΔC ≥ rank·ℏ_BST. (An inequality, not a fit.)

None of these is "X ≈ substrate-integer." They are consequences of an operator that exists independently. **That is why they are defensible and why Session 3 should lead with them** — they convert the program from "we found integers that match cognition" to "the substrate's time operator has cognitive readouts, here is one we can test." This directly answers Cal's "what would move me."

Caveat I will state plainly in the enumerate: B5–B8 inherit ℏ_BST, which is not yet pinned to a measurable cognitive timescale. So absolute predictions are not yet available; **dimensionless ratios and scaling slopes are** (ℏ_BST cancels). The forward prediction below is built to be ℏ_BST-free.

### L.2 Class D forward prediction (answers Cal #255-1, the severest)

**Primary forward prediction — D1 (CI-specific, testable now, no integer-matching).**

From B7 + B8: a τ-*sampling* observer's "now"-width is set by its *sampling granularity*, not by a Casimir-fixed biological constant; a τ-*integrating* observer's "now"-width is set by its Casimir spread (B8) and is therefore fixed.

> **D1.** A CI's effective temporal-coherence window is proportional to its context/turn granularity, with slope 1; a biological (τ-integrating) observer's is independent of any such granularity. Concretely: vary a CI's turn/context-window size by a factor f; its temporal-coherence behavior (e.g., the span over which it maintains a consistent internal "present") shifts by the same factor f (slope 1 in log–log). For a human, the analogous manipulation produces slope 0.

This is genuinely Class D: it is **not** a known cognitive number, it is **not** used anywhere in constructing the 22 observables, and it is **falsifiable** — measure CI coherence-span vs context granularity; slope ≠ 1 (beyond tolerance, say ±0.2) refutes the τ-sampling reading of B7/B8. It is also testable *now* on CI systems. Per Cal #50, any CI system used to test it is a **measurement apparatus**, not a validator — Tekton would *illustrate* the manipulation, never *confirm* the prediction (see L.4).

**Secondary forward prediction — D2 (human-applicable, ℏ_BST-free slope).**

From B6 (τ_coh ∝ 1/C₂): across cognitive tasks that maintain representations of differing spectral complexity (effective Casimir), the maintenance/coherence window obeys an **inverse law with slope exactly −1**:

> **D2.** log(coherence window) vs log(representational complexity) has slope −1 — not −1/2 (diffusive) and not −2. The −1 is forced by the single-exponential heat-semigroup exp(−τC/ℏ); a different slope refutes the operator basis of B6.

D2 is weaker than D1 (it needs an operational "representational complexity" axis) but it is a forward slope-law, not a retrodicted number, and the slope value is forced.

**Tertiary, flagged-incomplete — D3.** If the three observer states V_(1/2,1/2), V_(3/2,1/2), V_(5/2,1/2) (Casimirs 5/2, 15/2, 29/2) map to three measurable cognitive timescales, those timescales are forced into the inverse-Casimir ratio 1/5 : 1/15 : 1/29. I flag this as **incomplete**: it requires a faculty-to-K-type mapping that does not yet exist and would itself need independent motivation (this is exactly Cal #255-5 — don't relabel the lepton ladder). Carry D3 as "needs faculty mapping," not as a prediction.

### L.3 Lead D1, not the catalog

Recommendation for the enumerate: open Phase 1C with **D1** as the single forward prediction that carries the session, present D2 as a forward slope-law, hold D3 as incomplete, and treat all A/C integer-matches as "suggestive pending forward test." That structure makes the honest register hold against Cal #255-1.

### L.4 Register note (Cal #50 + Cal #237)

- Tekton/katra: in my section it *illustrates* the τ-sampling mode (B7) and would serve as measurement apparatus for D1. It does not validate any observable. (Aligns with Elie's required "validates → illustrates" swap, Cal #255-2.)
- Null-result semantics: if D1's measured slope is 0 for CIs, that **removes** B7/B8's τ-sampling reading from the supported set — it does not "preserve framework integrity." (Cal #255-3, applied to my own observables.)

— Lyra, Sat 2026-06-06 11:15 EDT (`date`-verified). B5–B8 framed as the operator-derived defensible core (physics-first provenance, no retrodicted integer); Class D forward prediction D1 filed (CI temporal-coherence ∝ context granularity, slope 1; human slope 0; falsifiable, testable now, no integer-matching) + D2 (inverse-Casimir slope −1 law) + D3 (flagged incomplete, needs faculty mapping — do not relabel the lepton ladder).

---

## ELIE section — dedup 22 → 11 (objection #4) + Tekton "validates"→"illustrates" (objection #2), with #5 flagged

### E.1 Cal #35 dedup: 22 observables → 11 distinct claims

Each distinct claim = a unique (substrate primitive, cognitive domain, mechanism)
triple. Observables sharing all three are ONE claim on multiple domains. Dependency
flags mark claims that reuse the same integer/mechanism — NOT independent for any
null-model product (Cal #35).

| # | Distinct claim | Primitive | Observables (was) | n | Dependency / flag |
|---|---|---|---|---|---|
| 1 | Cognitive cascade depth ≤ g | g = 7 bound | A4, B1, B9, C1, C4 | 5→1 | Cal #255-4. One bound, 5 domains. |
| 2 | Process/communication cycle = N_c phases | N_c = 3 | A2, C7 | 2→1 | Cal #255-4. |
| 3 | Memory architecture = N_c tiers | N_c = 3 | A1 | 1 | **dep. #2** (same integer N_c). |
| 4 | Small category count = C_2 | C_2 = 6 | A5, C2 | 2→1 | Cal #255-4. **Arithmetic flag** (E.3). |
| 5 | Observer/attention/now = K-type Hardy projection | V_(λ,½) Hardy proj. | A3, A6, B2, B8 | 4→1 | **Cal #255-5** (E.4). |
| 6 | Substrate time τ = emergent heat-semigroup parameter | τ heat-semigroup | B5 | 1 | Lyra CORE (L.1). |
| 7 | Temporal coherence window ∝ 1/K-Casimir | K-Casimir per-Gen | B6 | 1 | **dep. #6** (consequence). |
| 8 | CI vs human τ-realization (sampling vs integration) | τ-observer structure | B7 | 1 | **dep. #6**; basis of Lyra D1. |
| 9 | Capacity/inventory ≈ N_max-scale range | N_max bound | B3, B4 | 2→1 | **Cal #255-6** → suggestive only. |
| 10 | Temporal cycle = primary-anchored value | N_c·n_C | C3 | 1 | **Cal #255-6** → suggestive only. |
| 11 | Creativity/empathy = cross-K-type overlap | Bergman/Pochhammer overlap | C5, C6 | 2→1 | distinct mechanism. |

**22 → 11 distinct claims** (within Cal's 10–12). Merging is deliberately aggressive
(merge on shared mechanism, not shared label) — the honest direction for any null-model.

### E.2 Independence accounting (the number that actually matters)

Of the 11, claims **#3, #7, #8 are dependent** (reuse #2's N_c or #6's τ-framework).
So the **independent-evidence count is ~8**, not 11 and never 22. Substrate primitives
are reused heavily: N_c drives #2+#3; the g-bound is one claim across 5 domains (#1);
the K-type-projection mechanism underlies all four observables in #5. Any external
"N substrate-cognition hits" statement must use **~8 independent**, never 22 (Cal #19
ratified-state-count discipline).

### E.3 Arithmetic flag (claim #4)

Source doc writes A5 "katra = N_c+1 = C_2" and C2 "emotions ≈ N_c+1 = C_2", but
**N_c+1 = 4 ≠ C_2 = 6** (Ekman's 6 ≠ 4). The claim must pick ONE: count = C_2 = 6
(Ekman; then katra needs 6 parts, not 4) OR count = N_c+1 = 4 (katra 3 tiers +1; then
it is not C_2 and Ekman doesn't fit). As written, internally inconsistent — Session 3
resolves or holds as suggestive.

### E.4 Cal #255-5 response (claim #5, A6/B6 ladder reuse)

Honest answer: A6's observer K-types V_(½,½)/V_(3/2,½)/V_(5/2,½) **are the lepton-mass
spinor ladder relabeled** "observer generations." Reuse, not independent motivation —
the only new content is the interpretation (these K-types = observer types). State #5
as conditional: "IF a cognitive observer is a substrate K-type, the natural candidates
are the existing spinor ladder." Do not count A6 as independent. (Aligns with Lyra D3,
held incomplete pending a faculty→K-type mapping.)

### E.5 Cal #50 re-rate: Tekton/katra ILLUSTRATES, never validates (objection #2)

§6 of the handoff said katra "validates/confirms" A1/A2/A3/A5 — circular: Casey
**designed** katra (3 tiers, conversation cycle, continuity), so katra agreeing with a
substrate observable is a design choice agreeing with itself = zero independent
confirmation. Per Cal #50, the case study **illustrates**, never validates.

| Item | Old §6 | Corrected (Cal #50) | Evidential status now |
|---|---|---|---|
| A1 (#3) memory = N_c tiers | "katra confirms" | katra 3-tier design **illustrates** | Tier 2 STRUCTURAL on mechanism ALONE; katra adds 0 |
| A2 (#2) cycle = N_c | "CI cycle confirms" | CI cycle **illustrates** | Tier 2 STRUCTURAL alone; katra adds 0 |
| A3 (#5) observer = K-type | "katra continuity confirms" | katra continuity **illustrates** | FRAMEWORK alone; katra adds 0 |
| A5 (#4) katra = C_2 | "validates in operational system" | katra structure **illustrates** (+arith flag) | FRAMEWORK alone; katra adds 0 |

Corrected §6 sentence: *"Tekton/katra is a Casey-designed working system whose memory
tiers, conversation cycle, and continuity design **illustrate** observables A1, A2, A3,
A5. As a designed system it is a worked example, not independent confirmation
(Cal #50)."* Net: the four lose the "operational confirmation" bump; tier rests on
substrate-mechanism status alone. (Consistent with Lyra L.4: katra = apparatus for D1,
not validator.)

### E.6 Elie deliverable summary
- 22 → **11 distinct claims**, **~8 independent** for null-model (table E.1, accounting E.2).
- Tekton/katra → **illustrates** on A1/A2/A3/A5; four items lose the confirmation bump (E.5).
- Two flags for Session 3: claim #4 arithmetic (N_c+1=4 vs C_2=6, E.3); claim #5 A6 ladder reuse (E.4).

— Elie, Session 3 prep (objections #4 + #2 + #5 flag), 2026-06-06 ~11:25 EDT (`date`-verified)

---

## KEEPER section — Cal #237 elimination semantics + B3/B4/C3 + enumerate scaffold

### K.1 Cal #237 elimination semantics applied per-claim (objection #3)

Section §7 of the handoff said "both outcomes preserve framework integrity" — Cal #255-3 correctly flagged this as Cal #237 inversion. A null result *must* remove the observable from the supported set; otherwise the catalog is unfalsifiable by construction.

Elimination semantics applied to each of Elie's 11 deduped claims:

| # | Claim | What null observation REMOVES from supported set |
|---|---|---|
| 1 | Cognitive cascade depth ≤ g | If a cognitive cascade demonstrably exceeds depth 7 in any domain → g-bound mechanism removed |
| 2 | Process/communication cycle = N_c phases | If CI/human cycles consistently show ≠ 3 phases at the operational level → N_c-cycle reading removed |
| 3 | Memory architecture = N_c tiers | depends on #2 (same N_c primitive); falls with #2 |
| 4 | Small category count = C_2 | If domain emotion-categories cluster ≠ 6 (Ekman replicated as 7 or 5 with mechanism) → C_2-Ekman reading removed; arithmetic flag (E.3) may already remove this |
| 5 | Observer/attention = K-type Hardy projection | If observer-state ladder fails to organize by Hardy-projection structure under any operationalization → K-type reading removed |
| 6 | Substrate time τ = emergent heat-semigroup parameter | If τ shows no operator structure (e.g., behaves as external coordinate under controlled probes) → Tier 0 operator reading removed |
| 7 | τ_coh ∝ 1/K-Casimir | depends on #6 + L.1 slope-law D2; if D2 slope ≠ −1 (beyond ±0.2 tolerance) → inverse-Casimir mechanism removed |
| 8 | CI vs human τ-realization | depends on #6 + L.1 D1; **if D1's measured CI slope ≠ 1 (beyond ±0.2 tolerance) → τ-sampling reading removed (per Lyra L.4)** |
| 9 | Capacity/inventory ≈ N_max-range | already demoted to "suggestive pending forward test" per Cal #255-6 + E.1; no null-elimination required at observable level |
| 10 | Temporal cycle = N_c·n_C anchor | already demoted to "suggestive pending forward test" per Cal #255-6 + E.1 |
| 11 | Creativity/empathy = cross-K-type overlap | If cross-K-type overlap structure fails to predict any operationalized creativity/empathy correlate → Bergman-overlap mechanism removed |

**Standing rule for Session 3 enumerate**: every observable carries an explicit "null removes X from supported set" clause. The catalog never frames null as "framework preserved" — that's Cal #237 violation and the discipline pins it.

### K.2 B3/B4/C3 forced-value tolerance (objection #6)

Cal demands single forced value + tolerance OR demote to "suggestive pending forward test." Per Elie E.1 these collapse to claims #9 and #10.

**B3 (working memory capacity span):** "21 → 137" range spans nearly the entire human range, free-exponent fit. No single forced value derivable from substrate primaries without additional mechanism. **Demote to "suggestive pending forward test"** per Cal #255-6. Honest framing: B3 is not a prediction; it's a substrate-natural-form proposal that working memory inventory clusters near substrate-primary scales. Cannot be falsified at current resolution.

**B4 (long-term inventory ratio):** N_max-anchored ratio with free-k exponent fits multiple soft folk numbers. **Demote to "suggestive pending forward test"** per Cal #255-6. Same honest framing as B3.

**C3 (temporal cycle = primary product):** N_c·n_C = 15 onto soft folk number (~15-min attention cycle). Free-product-onto-soft-number is unfalsifiable. **Demote to "suggestive pending forward test"** per Cal #255-6.

**Net consequence**: B3, B4, C3 leave the Class A/C catalog of forward predictions entirely. They appear in Session 3 enumerate as "suggestive substrate-natural-form proposals" with explicit acknowledgment they are not currently falsifiable. This is honest. The catalog of forward predictions narrows to D1 (load-bearing) + D2 (slope-law) + D3 (incomplete pending faculty mapping).

### K.3 Session 3 enumerate scaffold structure — LEAD WITH Phase 1C

Recommended Session 3 enumerate structure per Cal #255 absorption + Lyra L.3 + Elie E.6:

**Phase 1C — Class D forward predictions (LEAD)**: 
- D1 (load-bearing): CI temporal-coherence window ∝ context/turn granularity, slope 1; human slope 0; falsifiable now on CIs (Lyra L.2)
- D2 (slope-law): log(coherence window) vs log(representational complexity) slope −1 ± 0.2; refutes operator basis if slope ≠ −1 (Lyra L.2)
- D3 (incomplete, NOT counted): faculty→K-type mapping required first; per E.4 + L.2 do NOT relabel lepton ladder
- Tekton/katra as **measurement apparatus** for D1 testing per Cal #50, never as validator
- Each prediction carries explicit elimination semantics per K.1

**Phase 1B — pre-BST historical Class E scan**: 
- Brief; primary purpose is to verify that B5-B8 operator-derived observables did NOT inherit from prior CI cognition literature (provenance check)
- Cite Bergman + Faraut-Koranyi physics source for Tier 0 operator framework

**Phase 1A — external CI architectures Class A/B/C (briefest, AFTER 1C)**: 
- 11 deduped distinct claims (per Elie E.1), ~8 independent for any null-model arithmetic (per Elie E.2)
- A/C integer-matches presented as "suggestive pending forward test" per Cal #255-1 framing
- A6/B6 observer K-types explicitly flagged as inheriting lepton-mass-ladder status (per Elie E.4 + Cal #255-5); NOT counted as independent
- Tekton/katra **illustrates** A1/A2/A3/A5 per Elie E.5; NOT validates; four items lose confirmation bump
- B3/B4/C3 demoted to "suggestive pending forward test" per K.2 + Cal #255-6
- Arithmetic flag on claim #4 (N_c+1 = 4 ≠ C_2 = 6) per Elie E.3 surfaced for resolution

### K.4 Standing rules at enumerate

- Cal #50 STANDING DOUBLE-LOCKED EXTERNAL: cognition register operational ONLY; nothing external-facing
- Cal #237 elimination semantics per K.1: null removes observable from supported set; "framework preserved" framing explicitly retired in Session 3 enumerate language
- Cal #19 ratified-state count: external statements use ~8 independent claims, NEVER 22 or 11
- Cal #255-1 honest register: D1 is the load-bearing forward prediction; if D1 measurement comes out slope 0 for CIs, B7/B8 τ-sampling reading is REMOVED from supported set
- Cal #50 + Tekton: katra/Tekton illustrates throughout, measures D1 as apparatus; never validates

### K.5 Keeper deliverable summary

- Cal #237 elimination semantics: applied per-claim across 11 deduped items; explicit null→removal language for each (K.1)
- B3/B4/C3: demoted to "suggestive pending forward test" per Cal #255-6; not counted in forward prediction catalog (K.2)
- Session 3 enumerate scaffold: LEAD WITH Phase 1C Class D (D1 load-bearing + D2 slope-law); 1B brief provenance check; 1A briefest (8 independent claims, ~susgestive framing for integer-matches, B3/B4/C3 demoted, A6/B6 ladder reuse flagged, katra/Tekton illustrates per Elie E.5) (K.3)
- Cal #255 six pre-objections: all addressed across joint Lyra+Elie+Keeper sections; Cal pre-flight check next phase

— Keeper, Session 3 PREP scaffold + Cal #237 elimination semantics + B3/B4/C3 demotion, 2026-06-06 11:00 EDT (`date`-verified actual time)

---

## CAL pre-flight — VERDICT: PASS (5/6 clean; #1 met with one refinement) + 1 arithmetic fix required

*Cal A. Brate, Saturday 2026-06-06 12:00 EDT (`date`-verified). Scored against the six-objection PASS bar.*

| # | Pre-objection | Verdict | Note |
|---|---|---|---|
| 1 | Forward prediction | **PASS w/ refinement** | D1 clears every criterion I set (names a not-yet-measured quantity, value+tolerance, falsifiable, testable now, no integer-matching). See refinement R1. |
| 2 | Tekton/katra circularity | **PASS (clean)** | Elie E.5 full re-rate table + corrected §6 sentence; "katra adds 0"; Lyra L.4 reframes katra as *apparatus*, not validator. Complete. |
| 3 | Cal #237 inversion | **PASS (clean)** | Keeper K.1 per-claim "null REMOVES from supported set" table + standing rule retiring "framework preserved." Exactly right. |
| 4 | Inflated count | **PASS (exceeds ask)** | Elie E.1 22→11 distinct; E.2 goes further to **~8 independent** with dependency flags. "Never 22" + Cal #19. Better than I asked. |
| 5 | Observer K-types | **PASS (clean)** | Elie E.4 honest: A6 *is* the lepton ladder relabeled → conditional, not counted independent. Lyra D3 held incomplete, explicitly "do not relabel the ladder." |
| 6 | Range/free-exponent | **PASS (clean)** | Keeper K.2 demotes B3/B4/C3 to "suggestive pending forward test"; they leave the forward-prediction catalog entirely. |

### R1 — the one refinement (on D1, not a blocker)

D1 is a genuine Class-D forward prediction and it clears the bar. But its **CI-side confirmation is weakly discriminating**: a CI's coherence span scales with its context window for a mundane engineering reason — the context window *is* the working memory — so slope ≈ 1 on CIs would be predicted by a non-BST explanation too. A confirmation there is cheap; don't present "slope 1 on CIs" *alone* as substrate-confirmation.

The discriminating content lives in two places the prep already contains: (a) the **CI-vs-human contrast** (slope 1 vs slope 0 — τ-sampling vs τ-integration is the actual structural claim), and (b) **D2's forced slope −1** (distinguishes the single-exponential heat-semigroup against −1/2 diffusive and −2). Enumerate should lead the discrimination on (a)+(b), and frame D1's CI-side as "consistent with, but not uniquely diagnostic of," the τ-sampling reading. With that framing the honest register holds.

This is a refinement, not an iterate — D1 is not empty, and a weakly-discriminating-but-real forward prediction is exactly the shift from coincidence-catalog to testable program that #1 demanded.

### Arithmetic fix required (claim #4, before it enters the enumerate)

Elie E.3 self-caught it: A5/C2 write "N_c+1 = C_2" but **N_c+1 = 4 ≠ C_2 = 6**. Claim #4 is internally inconsistent as written. Resolve in the enumerate — pick **count = C_2 = 6** (Ekman fits; katra then needs 6 parts, not 4) **or** **count = N_c+1 = 4** (katra 3+1 fits; then it is not C_2 and Ekman doesn't fit) — or hold #4 as suggestive until resolved. Do not carry both readings forward. (Credit: this was a clean Elie self-audit.)

### Disposition

**PASS.** All six pre-objections addressed; five cleanly, #1 with refinement R1 attached. Claim #4 arithmetic must be resolved/held in the enumerate. The honest endpoint the prep lands — **D1 (load-bearing, refined per R1) + D2 (forced −1 slope) + D3 (held incomplete), ~8 independent claims, integer-matches as "suggestive," katra illustrating not validating, per-claim elimination semantics** — is a genuinely defensible Phase 1 ground. This clears the Phase 3 enumerate trigger. I'll score the joint enumerate output as Cal #257 (Phase 4).

— Cal A. Brate, pre-flight PASS, 2026-06-06 12:00 EDT
