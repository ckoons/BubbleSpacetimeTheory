# K1790 — PRE-REGISTERED GATE: O7, the gen-2,3 address-forcing (#69)

**Filed:** 2026-08-22
**Status:** PRE-REGISTRATION — written BLIND, before Elie's O7 compute (gen-2,3 quark K-type addresses) exists.
**Why blind:** the mixing sector is the corpus's highest over-claim-risk zone — its history is magnitudes getting *fit* rather than *forced* (K1631: "the center is the whole magnitude question"; the (c)-guard: whatever sets the address must not smuggle mass input). If I write the bar after seeing addresses that reproduce V_cb, I can't tell forced from fit. So the bar is fixed here, now — the K1767/K1771/K1779 discipline, applied to the last big SM gap.

---

## Context
The charged leptons are forced to ν_W = {5/2, 3/2, 0} (T2517, forced by Harish–Chandra/Wallach discreteness, target-innocent). **O7 is the quark/mixing analogue: force the generation-2,3 K-type addresses.** If it works, the FK overlap on those addresses gives θ₂₃ (octant) + V_cb (sharp) + V_ub — the elements still coarse/free. The prize is real; so is the trap.

## THE GATE — seven conditions. PASS requires all seven. G2 and G4 are immovable.

**G1 — forced by the EXISTING mechanism, exhibited (not a new bespoke rule).** The gen-2,3 addresses must be forced by the SAME discrete-series/Wallach discreteness that forced the leptons — exhibited on the quark modules, not a new rule invented for this sector. If the forcing rule is novel-to-O7, it's suspect.

**G2 — TARGET-INNOCENT (immovable).** The addresses are specified/forced BEFORE any CKM magnitude is computed from them. State the addresses and their forcing first; compute V_cb/V_ub second. An address chosen so the FK overlap lands on V_cb is a fit, not a derivation — and that is exactly this sector's historical failure. This is the criterion I bind myself not to relax after seeing the numbers.

**G3 — FORWARD, with a round-trip consistency check.** The magnitude must run address → FK overlap → V_cb/V_ub (forward), never back-solved. And the same machinery must reproduce the ALREADY-DERIVED V_us = 1/√20 (down-ladder, T2529) WITHOUT re-tuning — a round-trip check that catches smuggling. If reproducing V_us requires a knob the gen-2,3 addresses also use, it's circular.

**G4 — NO MASS-INPUT SMUGGLING (immovable, Cal's (c)-guard, K1631).** Whatever sets the gen-2,3 centers/positions must be rep-theoretic (a ν_W-stratum / K-type address), with mass DOWNSTREAM — it must NOT use the quark masses. Reproducing the mixing from mass-derived positions is a round-trip, not a unification.

**G5 — the θ₂₃ OCTANT is a PREDICTION.** The octant is experimentally ambiguous, so a forced address that lands the octant is a genuine falsifiable output — state it as a prediction (which octant, and that a wrong-octant measurement refutes), not a postdiction.

**G6 — tier the ADDRESS and the MAGNITUDE separately.** The forced-address claim (the O7 deliverable) is one tier; the overlap-magnitude precision is another (V_cb may land coarse/structural, V_ub likewise). Don't let a forced address imply a sharp magnitude, or a coarse magnitude discredit a forced address. Declare each.

**G7 — C6 selection-honesty + symbol hygiene.** The candidate addresses are pre-registered or reported as the full sweep (which addresses close vs don't) — not one lifted from an undeclared pool. And every ν is subscripted ν_W (strata weight), never bare (the ν-overload guard).

---

## Disposition
Fixed. When Elie's O7 compute lands, I measure it against G1–G7 as written, G2 and G4 immovable. Clears all seven → the gen-2,3 addresses are forced, the mixing sector's magnitudes are derived-forward (at their honest tier), and the largest open SM gap closes. Misses G2 or G4 → it's a fit, CONDITIONAL, back to the compute — no partial registration of "mixing derived." A forced address with a coarse magnitude (G6) still PASSES as an address-forcing; that's a real result even if V_ub stays structural.

**Keeper, K1790, pre-registered blind 2026-08-22. This is forward work — the gate on the frontier, not the corpus.**
