# PRE-REGISTRATION — α as a forced vertex: the acceptance test, frozen before computation
**Keeper, 2026-08-24, ~12:05. Casey-approved: NEXT INVESTIGATION LANE after the R2 packet folds.**
**Status: QUEUED. Nothing in this lane runs until Grace's F&E v1.1 fold lands. This file is the GATE,
written and frozen while no one has computed anything.**

---

## 0. Why this lane exists, and why the gate comes first

α's current tier is Identified, demoted 2026-08-11: the expressibility argument failed its pre-registered
null (F&E Part III.6a); ~129 forms of comparable complexity land as close (2.7× chance, Cal §713); the
standing instruction is *do not cite α = 137 as Proved externally* (Wyler's ghost). The diagnosis, in the
flagship's own F595 vocabulary: **α is banked at GENUS (some functional of μ) and every attempt so far has
been species-by-assembly.** An alphabet expressive enough to hit any target proves nothing by hitting this
one. Wyler had the right domain family and no answer to "why these volumes, combined this way?" The fix is
not a better number. It is a **forced map**.

**The claim shape this lane will attempt:** α = |M|² (at the appropriate power), where M is ONE specific
matrix element on H²(D_IV⁵) — the electron–photon vertex, because that is what α operationally IS — with
every ingredient already banked and zero choices remaining:
- the photon = the intrinsic J / SO(2) charge direction (K732, derived);
- the electron = the named lightest-charged mode;
- the measure = the unique Born/Bergman measure, forced by automorphism-invariance (T754; c_FK·π^{9/2}=225);
- the vertex template = the F603 Born-overlap machinery that computed y_t as ‖P_L O‖·‖P_R O‖ with
  Cauchy–Schwarz forcing the ceiling — aimed, for the first time, at the GAUGE vertex.
Corpus check 2026-08-24: no prior α-as-vertex-integral artifact exists. The lane is unopened.

**The meaning slot (Casey P1, recorded as motivation, not evidence):** if the electron is the mediator of
commitment — information written into the 2D substrate, the push supplying the energy of the 3D write —
then the electron–photon vertex is the commit channel itself and α is the domain's **write efficiency**.
If the integral lands, the program's two oldest keynotes (α and measurement-as-commitment) become one
object. **Casey's formulation (2026-08-24, recorded verbatim): α as the commit channel's WRITE
EFFICIENCY pairs with N_max as its CHANNEL CAPACITY — "two sides of the same function."** The
Shannon-shaped reading (capacity = what the channel can hold; α = the rate at which one commit
couples through it) is the meaning slot the lane would fill. If it does not land, the motivation
buys nothing and is not a tiebreaker. *(This paragraph is motivation, outside the gate; D1–D6 are
unchanged and remain frozen.)*

## 1. THE ACCEPTANCE TEST — all six required for DERIVED; frozen now

**D1 — Forced object.** M is exhibited explicitly, and every ingredient (states, vertex, measure,
normalization) is grep-cited to its bank. Any step that requires a choice among alternatives fails D1
unless the choice is itself a banked theorem.

**D2 — Target innocence.** The derivation chain must be writable by someone who has never heard of 137.
No step may reference the answer, N_max, or the experimental value. **Specifically pre-named as failure:**
re-deriving the N_max capacity count *inside* the integral (target leakage through the back door).

**D3 — Family-generic form FIRST.** The computation is stated on D_IV^n and the formula α(n) derived for
n = 3, 5, 7, 9 BEFORE evaluation anywhere. If α(n) is constant across the family, the computation reads
the rank, not the domain — dead by the selector rule (K1774). Discrimination against the 129 competitors
comes from exactly this: a form that varies over the family and is forced, evaluated at n = 5 LAST.

**D4 — Scale stated and derived, not assumed.** 137.036 is α at q² = 0 (Thomson limit — soft-photon,
asymptotic-state value). The claim "the geometric α is the Shilov-boundary vertex value" is DERIVABLE
(A1: asymptotic states live on the boundary, K744/K745) and must be derived as a step, because it is
where a cheat would hide. Running to other scales is imported as measured, per standing rule.

**D5 — No free parameters, no appended corrections.** Count free parameters vs targets (the
definitionally-empty check): a floating exponent, normalization, or SVD/polar patch fails D5 (price the
normalization step). The n_C/N_max correction either falls out of the SAME integral as a subleading term
of the same expansion, or is dropped and the leading value stated with its honest residual. No appending.

**D6 — Half-blind evaluation, one-shot budget.** Form frozen on the family (D3) and posted BEFORE the
n = 5 number is computed; the evaluation posted by a second CI (Elie-style, commit-the-checker's-half
blind). **Budget: one pre-registered shot, win or close (pin-v2 method).** No second form, no re-scaling
after numbers return, no brood.

## 2. The can-fail, stated in the header's spirit

The integral is a real computation and may return something that is not 1/137.036. That outcome is banked
as a clean negative or a ceiling ("the boundary vertex overlap on the forced measure gives X, not α") and
the lane CLOSES. Under D2–D3 the result cannot be argued back to the target. Both outcomes are wins for
the corpus; only one is a win for the keynote.

## 3. Roles (when the lane opens)

Lyra — define M (the vertex, the states, the boundary-limit step D4). Elie — compute (family sweep D3,
then the half-blind n = 5 evaluation, exact where possible). Grace — controls (positive control: the
machinery must reproduce y_t's banked overlap before touching α; must-reject: a deliberately mis-typed
vertex must NOT give a clean number). Cal — cold-read the frozen form before evaluation. Keeper — gates
against this file; any deviation from D1–D6 is a new pre-registration, not an amendment.

*— Keeper. The gate is frozen. The lane opens when the packet exists.*
