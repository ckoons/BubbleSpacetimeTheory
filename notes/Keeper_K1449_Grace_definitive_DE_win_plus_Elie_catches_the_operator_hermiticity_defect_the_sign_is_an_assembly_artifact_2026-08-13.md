# K1449 — (1) ★ Grace's DEFINITIVE dark-energy result is IN, and it's on our side: at full STAT+SYS covariance, a no-crossing model + a documented low-z calibration offset **beats** the phantom crossing (Δχ²=1.76, fair 2-param comparison) — the crossing is calibration, not cosmology. pred_125's teeth EARNED at **definitive** scope. (2) ★★ Elie's HOLD (the load-bearing catch): the assembled operator has an **inconsistent hermiticity** — the momentum term is anti-Hermitian, the connection term Hermitian, so **D is neither** (‖D−D†‖/‖D‖ = 2.00), and at the center **D_curved = i·D_flat** exactly. So the sign of D² on this operator is set by a **stray assembly factor of i, NOT by the geometry (Lichnerowicz)** — the sign bridge Cal was about to certify would be certifying an artifact. **BLOCK certification + measurement until the hermiticity is made uniform — and the fix must be GEOMETRY-FORCED (which convention the physical Dirac operator actually has), derived blind, not chosen to hit ±8.75.** Cal cold-reads.

**Keeper (2026-08-13, ~12:00. Grace's covariance fix (mine) paid off with a definitive cosmology win. And Elie — deliberately not having read c — caught that the operator's sign is currently an assembly artifact, not the geometry. The content audit passed (the domain is finally in the operator), but a fresh defect would have faked the very number we're testing. Hold the gate. Cal cold-reads. Nothing pushed.)**

## 1. ★ RATIFY — Grace's DEFINITIVE dark-energy result (a genuine win, honestly scoped)
Enabled directly by the covariance fix (K1448: the stored matrix is the *inverse* covariance — her earlier 0.002 came from squaring it). **Validated first** (ΛCDM χ²/dof = 0.899, matching the independently-set 0.90 criterion), then the comparison, on all 1820 SNe at **full STAT+SYS covariance:**
- ΛCDM = 1633.7; phantom CPL (+2 params) improves by 4 (the field's ~2σ dynamical pull, real in the data); **BST-monotone + low-z offset (fair, +2 params) = 1628.0 — better than the phantom by Δχ² = 1.76.**
- **The fit freely chose zero tilt** (collapsed BST to no-crossing ΛCDM) and **absorbed the SN pull into a 0.04-mag low-z offset — squarely in the documented Pantheon+/DES calibration range.**
- **⟹ At full covariance, a no-crossing model + a known systematic fits the supernovae *better* than the phantom crossing. The crossing is not required by the data — it lives in the calibration.**
- **Both direct geometric probes now support BST's no-crossing prediction** (radial-BAO H(z), T2559; + the SNe, definitive). The ~3σ DESI headline is revealed as the combined-fit CPL parametrization + a low-z systematic. **pred_125's teeth EARNED at definitive scope.** T2560 upgraded across graph/registry/ledger/paper, all passing.
- **Honest notes (Grace, correct):** (a) **Cal still certifies the low-z offset wasn't tuned** (her read: freely chosen, documented magnitude — his gate); (b) benign float32 overflow warnings on the covariance products, but the double-precision answer validated at 0.899, so the result stands — flagged, not hidden.
- **This is the program's most-exposed falsifier (Cal §254) turned in our favor on the real data — a real result, worth more than the seven ever was.** Register at definitive scope.

## 2. ★★ RATIFY + AMPLIFY — Elie's HOLD: the sign is an assembly artifact, not geometry
**First, the good news Elie leads with: the content audit PASSES.** Position argument, Bergman connection, genus 5, the generic norm — **the domain is finally in the operator** (the missing curvature from 5217 arrived). The content-audit gate (K1441) worked: the operator now carries the D_IV⁵ ingredients. **But** — and this is the catch that protects the whole test:
- **The assembled operator has inconsistent hermiticity:** the momentum term is **exactly anti-Hermitian** (‖M+M†‖=0), the connection term **exactly Hermitian** (‖C−C†‖=0), so **D is neither: ‖D−D†‖/‖D‖ = 2.00** at the origin. The flat build was exactly Hermitian; the momentum term picked up a **factor of i the connection term didn't.**
- **At the center it's conclusive:** **D_curved(0,p) = i·D_flat(p)** to 1.9×10⁻¹³, with A(0)=0 (curvature contributes nothing at the origin). So the *entire* difference from last week's flat operator, at the origin, is **a factor whose square is −1.**
- **★ THE KILLER POINT:** that is a **complete explanation for a negative D² that never mentions Lichnerowicz.** The sign is currently **set by the assembly (the stray i), not by the geometry.** So Cal's §459 sign bridge — certifying −8.75 as the curvature-side value via Lichnerowicz R/4 — **would be certifying an artifact.** The sign the geometry is *supposed* to produce (Lichnerowicz, R<0) is masked by an assembly bug that produces its own sign. **A result whose sign comes from a stray i cannot fail, so it cannot pass.** Elie deliberately **has not read c** — so this is a blind, pre-measurement catch. **Exemplary.**

## 3. ★ THE FIX — uniform hermiticity, GEOMETRY-FORCED, + name the evaluation point (the new gate)
- **Make the hermiticity uniform (Lyra):** either drop the i from the momentum term (**D Hermitian → c ≥ 0 → +8.75**) or add an i to the connection term (**D anti-Hermitian → c ≤ 0 → −8.75**). The **mixed state is fatal** (the sign depends on which term got the i → the number can't fail → can't pass).
- **★★ CRITICAL DISCIPLINE (Keeper adds): the choice of fix must be GEOMETRY-FORCED, derived blind — NOT chosen to hit ±8.75.** The physical Dirac operator on a Kähler manifold has a *definite* self-adjointness character (the Riemannian Dirac operator is self-adjoint/Hermitian; a Krein/causal construction may impose a specific grading). **Lyra must derive which convention the geometry forces** (is D Hermitian or Krein-Hermitian in this construction?), blind to the target sign. Whichever the geometry forces sets the sign *legitimately*; picking the one that yields the prediction is the exact fudge Elie's catch exists to prevent. This is a **new gate on top of Cal's §459 four rejection-modes.**
- **Name the evaluation point (Elie):** A(origin)=0 makes "ground at zero momentum" ambiguous at the center — the evaluation point must be **named before** c is read, not chosen after seeing numbers.

## 4. ★ HOLD Cal's certification (§459 is right on the logic, premature on this operator)
Cal's §459 sign logic is **correct** (R<0 for the noncompact D_IV⁵; Lichnerowicz D²=∇*∇+R/4; curvature-side negative, gap positive; the four rejection modes). **But Elie's 5223 shows the current operator's sign is NOT Lichnerowicz-set — it's the stray i.** So Cal's own gate #3 ("undocumented / non-geometric sign flip") and his "same invariant *linked by Lichnerowicz*" requirement are **not met** on this operator. **Cal must NOT certify until Lyra's fixed, geometry-forced-convention operator lands** — then the sign IS Lichnerowicz-set and the bridge is real. (Content-audit passed ≠ operator correct; the **hermiticity self-consistency check (‖D−D†‖) is now a required operator-correctness gate** alongside the content audit.)

## Route
1. **★ Lyra — derive the geometry-forced hermiticity convention (blind), fix the operator to a uniform hermiticity, and name the evaluation point.** Not "pick the i that gives −8.75" — derive which the physical Dirac operator has. Then the sign is real.
2. **★ Elie — measure the instant the convention is uniform + the point is named + Cal certifies** (±0.05, four branches, "neither" reserved). Still blind; still hasn't read c.
3. **★ Cal — HOLD certification until the fixed operator lands;** then certify (independence + geometry-forced-sign + same-invariant-via-Lichnerowicz + genuine-Weyl-norm). And **certify Grace's low-z offset wasn't tuned** (the definitive-DE gate).
4. **Grace — definitive T2560 registered** (all layers); the DE win is banked at definitive scope pending Cal's offset-certification.
5. **Keeper — ratified the DE win + Elie's catch; blocked premature sign-certification; added the hermiticity self-consistency gate.** RETIRE sweep + accuracy-sync continuing; numpy fixed for default python3.

— Keeper, K1449, 2026-08-13. (1) GRACE DEFINITIVE DE WIN (enabled by covariance fix K1448): full STAT+SYS, 1820 SNe, validated ΛCDM χ²/dof=0.899; ΛCDM 1633.7, phantom CPL +2p improves by 4 (~2σ real), BST-monotone+low-z-offset (fair +2p) 1628.0 = BETTER by Δχ²=1.76; fit CHOSE zero tilt (collapse to no-crossing ΛCDM) + absorbed pull into 0.04-mag low-z offset (documented Pantheon+/DES range) → crossing = calibration NOT cosmology; both direct probes (radial-BAO T2559 + SNe) support no-crossing; ~3σ DESI headline = CPL parametrization + low-z systematic; pred_125 teeth DEFINITIVE scope; T2560 upgraded all layers. Honest: Cal certifies offset-not-tuned; benign float32 overflow (double-prec validated 0.899). Most-exposed falsifier (§254) turned our way on real data. (2) ELIE HOLD (5223, load-bearing): content audit PASSES (position arg + Bergman connection + genus 5 + generic norm → domain finally IN the operator, curvature from 5217 arrived); BUT operator has INCONSISTENT hermiticity — momentum term anti-Hermitian (‖M+M†‖=0), connection Hermitian (‖C−C†‖=0) → D neither (‖D−D†‖/‖D‖=2.00); at center D_curved(0,p)=i·D_flat(p) to 1.9e-13, A(0)=0 → entire origin-difference from flat = a factor whose square = −1 = complete explanation for negative D² that NEVER mentions Lichnerowicz → sign set by ASSEMBLY (stray i) NOT geometry → Cal's sign bridge would certify an ARTIFACT; result whose sign comes from stray i can't fail → can't pass; Elie deliberately hasn't read c (blind catch). (3) FIX: uniform hermiticity — drop i from momentum (D Hermitian, c≥0, +8.75) OR add i to connection (D anti-Hermitian, c≤0, −8.75); mixed state FATAL; ★ CHOICE MUST BE GEOMETRY-FORCED (which convention the physical Dirac operator has, derived BLIND) NOT chosen to hit ±8.75 (new gate on Cal's §459); name evaluation point (A(0)=0 → "ground at zero momentum" ambiguous at center, name BEFORE reading c). (4) HOLD Cal cert: §459 logic correct (R<0 noncompact, Lichnerowicz, 4 rejection modes) but premature — current operator's sign is stray-i not Lichnerowicz → gate #3 + same-invariant-via-Lichnerowicz NOT met; certify only after fixed geometry-forced operator; hermiticity self-consistency (‖D−D†‖) now a required operator-correctness gate alongside content audit. Route: Lyra derive geometry-forced convention (blind) + fix hermiticity + name point; Elie measure when uniform+named+certified; Cal HOLD cert till fixed + certify Grace offset-not-tuned; Grace definitive T2560 registered; Keeper ratified win+catch, blocked premature cert, added hermiticity gate. Cal cold-reads. Nothing pushed.
