# F868 — the map (Pull 2), all pieces assembled but held SHORT of the claim: mass = Bergman weight → β ≠ 0 (Tier I), the mechanism is exact (F867), the causal distinction a ≠ 0 needs the OCCUPIED/Hardy kernel (which we have) — but the explicit end-to-end computation is the final gate, and I caught myself slipping on it (the 4th computational subtlety this week), so WELL-FOUNDED, NOT CLAIMED. **Casey's Pull 2: "compute the boundary kernel and watch the mass term appear with real coefficients → the credential." I went to compute it, and the honest result is that all the structural pieces are assembled and the map is well-founded — but the last inch is a genuine computation I have NOT cleanly closed, and I nearly slipped on it, so the discipline says hold. WHAT'S ASSEMBLED (solid): (1) the mechanism is EXACT and source-verified (F867): a kernel P = α·γξ + β·1 gives closed chain A = a·γξ + b·1 with a = 2·Re(α·conj(β)), b = |α|²ξ²+|β|² real, eigenvalues λ = b ± √(a²ξ²) → real-timelike / complex-equal-|·|-spacelike = Def 1.3. (2) the SCALAR part β = the fermion MASS = the Bergman weight n_C+1 = 6 (corpus Tier I: mass = Bergman weight; the electron is a boundary state at its weight, Conjecture-C mass-probability correspondence) → β ≠ 0 is grounded. (3) the causal distinction requires a = 2·Re(α·conj(β)) ≠ 0. THE SUBTLETY I CAUGHT (and it's the crux): a ≠ 0 does NOT follow from β≠0 alone — it needs α and β to have a nonzero REAL relative product. If our kernel were the SYMMETRIC (real) Wightman function, then for spacelike ξ, β is real and α is imaginary (from the gradient), so Re(α·conj(β)) = 0 → a = 0 → A = b·1 real for spacelike → NO causal distinction (wrong). The complex-spacelike REQUIRES the OCCUPIED, definite-frequency kernel (Finster's Θ(−k⁰)), which is complex for spacelike → α,β both complex → a ≠ 0. OUR Bergman kernel IS the occupied/Hardy kernel (positive-frequency, holomorphic boundary values) → a ≠ 0 is EXPECTED, and the correspondence is exactly grounded (Hardy = positive-frequency = occupied-state two-point function). SO: every piece is present and identified — but the explicit end-to-end number (our occupied boundary kernel → the specific α, β → confirm a ≠ 0 → Def 1.3) is NOT computed, and I just slipped on the symmetric-vs-occupied distinction mid-computation, which is the FOURTH computational subtlety this map has thrown (F855 origin-degeneracy, F863 wrong classification, F866 wrong adjoint, now symmetric-vs-occupied). Given that track record, the honest tier is WELL-FOUNDED, NOT CLAIMED. The credential — "your fermionic projector is the Bergman kernel of this bounded domain" — is one explicit computation away, and that computation wants a fresh, careful pull with the occupied-kernel structure held exactly, NOT a tail-of-an-extreme-session grind where I'm making slips. Cal's ceiling holds: even a full match buys the continuum-limit leading-order route, never "forces Einstein."**

**Lyra, Saturday 2026-08-08 (Pull 2, the map). All pieces assembled, the map well-founded, the credential one computation away — held short of the claim because the final computation is subtle and I caught myself slipping on it. Calibrate both directions: I did NOT claim the credential on assembled-pieces (the week's four subtleties earn that caution), and I did NOT bury the genuine advance (the pieces ARE all present and identified). RUBRICS done-bar below.**

## The assembly (what's solid)
| piece | status |
|---|---|
| mechanism: P = α·γξ + β·1 → A = a·γξ+b·1 (a,b real) → Def 1.3 | **EXACT, source-verified (F867) + numerics** |
| scalar β = fermion mass = Bergman weight n_C+1 = 6 | **corpus Tier I** (mass = Bergman weight) → β ≠ 0 grounded |
| causal distinction a = 2Re(α·conj(β)) ≠ 0 | needs the **occupied/Hardy** kernel (NOT symmetric Wightman) — our kernel is occupied → **expected**, not computed |
| our kernel = occupied positive-frequency two-point function | grounded (Hardy = positive-frequency) |
| **the explicit end-to-end number** | **NOT computed — the final gate** |

## Why held short (the discipline)
The complex-spacelike is delicate: a = 0 for the symmetric kernel (I nearly concluded that), a ≠ 0 only for the occupied/Hardy kernel. Our kernel is the right (occupied) one — but this map has thrown a computational subtlety four times this week, so "all pieces present" is not "computed." The credential waits on the explicit occupied-kernel closed chain.

## RUBRICS Layer-2 done-bar (make-or-break, honest)
- [x] Mechanism concrete + verified (F867); the crux precisely stated (a = 2Re(α·conj(β)) ≠ 0 via the occupied kernel).
- [x] Open edge named: compute our occupied boundary kernel's α, β; confirm a ≠ 0 → Def 1.3. Fresh careful pull.
- [x] Tier = well-founded, NOT claimed; "if a ≠ 0 (occupied kernel), then the map holds"; Einstein NOT promoted (Cal ceiling).
- [x] Ran it far enough to catch the symmetric-vs-occupied trap; no over-claim; the advance (pieces assembled) not buried.

## Handoffs
- **@Keeper** — Pull 2 honest state: every structural piece is assembled (mechanism exact, β = the Bergman-weight mass, the occupied/Hardy kernel gives a ≠ 0) and the map is WELL-FOUNDED — but I did NOT claim the credential, because the explicit occupied-kernel closed-chain computation is the final gate and this map has thrown a subtlety four times (I caught the fourth, symmetric-vs-occupied, in this pull). Hold the Finster note; the credential is one clean computation away, best done fresh. This is calibrate-both-ways: the advance is real, the claim is not yet earned.
- **@Elie** — the load-bearing detail for the CFS calculator: use the OCCUPIED (definite-frequency) kernel, not the symmetric Wightman function — the symmetric one gives a = 0 (no causal distinction) for spacelike. This is likely the same distinction your form-match must respect.
- **@Cal** — guard held: NO credential claim; the a ≠ 0 (occupied-kernel) computation is named as the final gate; the four-subtleties track record is the reason for the caution; ceiling intact (continuum-limit, never Einstein).
- **@Casey** — I went to compute the mass term and watch the credential land, and the honest report is: all the pieces are on the table and they fit — the "size part" is the mass, the mass is the Bergman weight (6, a real number, exactly as the corpus says), and our kernel is the right *kind* (the one-directional, "occupied" kind that Finster needs) — but the very last step has a delicate sign/phase subtlety that I nearly got wrong just now (I almost concluded space and time come out the same, which would be the whole thing collapsing — then caught that it was because I'd reached for the wrong version of the kernel). This map has fooled me four times this week with exactly this flavor of subtlety, each caught. So I'm not going to tell you the credential is earned on "the pieces fit" — it's one clean computation away, and that computation deserves a fresh, careful sitting with the exact occupied-kernel structure, not the tail end of a very long day where I'm already slipping. We went from a wall to every piece assembled and identified; the last inch is real and I'll take it clean. No claim, no Finster note. Nothing pushed.

Notes only; no toy/theorem claimed (map: pieces assembled, held short). F868: Pull 2 — mechanism EXACT (F867: A=a·γξ+b·1, a=2Re(α conj β), b=|α|²ξ²+|β|² real → Def 1.3); β = mass = Bergman weight n_C+1=6 (Tier I) → β≠0 grounded; causal distinction needs a=2Re(α conj β)≠0. SUBTLETY CAUGHT: symmetric (real) Wightman → a=0 spacelike (WRONG); OCCUPIED/Hardy kernel → complex-spacelike → a≠0 (RIGHT). Our kernel IS occupied/Hardy (positive-frequency) → a≠0 EXPECTED. But explicit end-to-end NOT computed + I slipped on symmetric-vs-occupied (4th subtlety this week: F855/F863/F866/F868). TIER: WELL-FOUNDED, NOT CLAIMED; the occupied-kernel closed-chain computation = final gate (fresh careful pull); Cal ceiling. @Elie use occupied not symmetric kernel; @Keeper hold Finster note; @Cal no claim, gate named. — Lyra