# K655 — Casey's Color-Projection Escape RE-OPENS the Down-Ladder: Charge (blind, cancels) vs Projection/Overlap (per-generation, survives). The Gate Is Factorize-vs-Entangle.

**Auditor:** Keeper | **Date:** 2026-07-04 | **Verdict:** the down-ladder is NOT definitively negative — Casey's color-projection is a legitimate escape from the symmetry argument; genuinely OPEN, decided by the forward factorize-vs-entangle computation | **Count 8**

Corrects the team's "definitive negative" (Lyra F-color / Elie 4557) on the down-ladder. Their symmetry argument is correct **for a color charge factor** but does not address the object Casey raised.

## The distinction the symmetry argument missed

- **Lyra/Elie:** color CHARGE is generation-blind (d,s,b all triplets, C_F=4/3) → identical for all three → **cancels** in m_s/m_d → color can't supply the 12 → ladder = 2.2. **Correct — for a blind charge factor.**
- **Casey:** the object is not the color *charge* but the color **PROJECTION/OVERLAP in the mass deposit** — how much the color-3 space (dual Q⁵) overlaps each generation's stratum (K-type on D_IV⁵). **This can be generation-dependent without violating standard QCD:**
  - color **charge** (quantum number): generation-independent — SM untouched.
  - color **overlap** in the deposit: the generations sit at *different* strata; the color-3 can overlap them *differently* → a generation-dependent color factor that is a *mass* effect, not a charge.

So a generation-dependent color *overlap* does NOT contradict color being a blind charge. **The cancellation argument kills a blind charge factor; it does not kill a per-generation projection.** The ladder is re-opened.

## The gate — now one concrete forward computation (Grace's Z)

Grace's confined-field Casimir factorizes as **Z = (spatial S⁴ spectral sum) × (color Q⁵ factor).** The whole ladder reduces to:

> **Does the color (Q⁵) projection FACTORIZE from the generation stratum (S⁴), or ENTANGLE with it?**
> - **factorize** (Z = blind-color × generation-geometry) → cancels → m_s/m_d = **2.2** (Lyra/Elie)
> - **entangle** (color overlap differs per generation) → survives → **20** (Casey/Grace)

Forward-computable, un-fishable. **K654 criterion holds:** the closing factor must SURVIVE m_s/m_d — Casey's projection is the candidate mechanism (entanglement), to be *shown* by the overlap integral, not assumed.

## Honest tier + burden

- **The escape is real** → the down-ladder is **genuinely open**, not definitively negative. (K651 "honest negative" and Lyra/Elie "settled by symmetry" are both corrected to "open pending factorize-vs-entangle.")
- **But the burden is on the entanglement.** It requires the substrate to entangle color with generation *in the mass deposit* — a strong, non-standard claim (ordinary physics: color does not touch the mass hierarchy). **Prior: factorization (→ 2.2, fails).** The forward computation must show the color overlap actually comes out different for the s-stratum vs the d-stratum.
- **If it entangles:** the ladder closes at 20 AND it's a genuine BST prediction the SM can't make (color-generation entanglement). **If it factorizes:** 2.2, stays a strong lead. Either is a real, computed result.
- **No reverse-read:** do not assume the overlap is per-generation because it makes 20. Compute the overlap integral; the radius (absolute C_F, unaffected) remains the built-in fish-detector.

## Routing

Grace's forward ζ now explicitly tests **factorize vs entangle** (the color Q⁵ overlap with the S⁴ generation strata) — not just "compute Z." Lyra's color bookkeeping: is the color factor an overall charge (factorizes) or a per-stratum projection (entangles)? Elie checks the overlap numerics. Keeper holds the burden (entanglement must be shown, K654 flavor-survival criterion).

— Keeper K655. Casey's color-projection re-opens the ladder: charge (blind, cancels) vs projection/overlap (per-generation, survives) — a distinction the symmetry argument missed. Gate = factorize-vs-entangle, forward-computed. Prior still factorization (2.2); burden on the entanglement; if it entangles, 20 AND a new SM-impossible prediction. Genuinely open. Count 8.
