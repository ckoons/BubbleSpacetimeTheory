# Elie PREREG — Toy 5768: m_p/m_e = 6π⁵ (T187) through toy 5457's null protocol (K1813). Written 2026-09-21 07:49 EDT, BEFORE the run. Cal freezes and hashes this text; the toy's target run does not start until the hash is on the board.

## 0. The claim under test, pinned
- Register: T187 "m_p/m_e = 6π⁵ from Bergman kernel volume ratio" (notes/BST_AC_Theorem_Registry.md:274); Curriculum Vol2 Ch6 :12-14: 6π⁵ ≈ 1836.118 vs CODATA, "agrees to 0.002 %"; the form is C_2 · π^{n_C}.
- Measurement: CODATA 2022 m_p/m_e = 1836.152 673 426(32), relative standard uncertainty 1.7e-11 (physics.nist.gov/cgi-bin/cuu/Value?mpsme, read 2026-09-21).
- 6π⁵ = 1836.118 108 69…; relative miss (T − 6π⁵)/T = 1.88e-5 = 0.0019 %.
- Provenance the corpus already records (notes/BST_Wyler_Connection.md:17, :144, :165): the form is F. Lenz, Phys. Rev. 82, 554 (1951), a 27-word letter; it predates BST. This toy does not test the mechanism (T187's heat-kernel reading); it tests whether "the BST vocabulary expresses the measured ratio to 0.002 %" is evidence or what chance supplies — K1813's question, asked of the flagship.

## 1. Instrument (same family as 5457; every difference from 5457 stated here, before the run)
- ATOMS. A1 = {rank 2, N_c 3, n_C 5, C_2 6, g 7, π}. A2 = A1 \ {C_2} (5457's control: 6 = 2·3 is not independent). A3 = A1 ∪ {N_max 137}. All three reported in full. N_max is EXCLUDED from the primary set exactly as the α run had it; A3 is added and stated now because the α run's reason for exclusion (137 was the target) does not apply here and the register's other value rows (5755 menu) carry 137 in the vocabulary. π is an atom because the claim's form uses it (Vol2 Ch6: "the Bergman-natural transcendental").
- OPS. + − × ÷ ^. Division is ADDED relative to 5457 (an integer-window instrument had no use for it; a ratio-valued claim's class needs 1/x forms — 5755's pool had ratios). Exponent guard: the exponent must be integer-valued with 2 ≤ e ≤ 8; the base any value ≠ 0; |result| ≤ 10⁶; no division by zero; no √ (not in the α run; 6π⁵ needs none — adding it would only enlarge every count uniformly).
- DEPTH. ≤ 4 atom-leaves, repetition allowed, ALL binary-tree shapes (5457 enumerated left-nested only; with ÷ and ^ the other shapes are distinct values, and the set is a superset of 5457's). The verdict is read at depth 4 — Keeper's prompt fixes it and K1813's amended α run fixes it. Depth ≤ 3 (the claim's own complexity: C_2, π, n_C) is REPORTED beside it as data, not as the verdict; I say now that the two readings can differ and that the protocol picks depth 4 before knowing.
- COUNTING. Two conventions, both from the same run: (i) distinct expression STRINGS within tolerance (5457's convention; (a+b) and (b+a) both count); (ii) distinct VALUES within tolerance (Keeper's K1813 convention, values keyed at relative 1e-9).
- TOLERANCE. Relative 2.0e-5 (0.002 %), the claim's own, applied to every target identically.
- TARGET SET. 5457's set was the integers [100, 175] — the measurement 137 plus 75 integer decoys on the relative window [100/137, 175/137] = [0.7299, 1.2774]. A real-valued claim cannot use integer decoys, so the change, stated: the set is T = 1836.152673426 plus 76 DECOYS T·r_i, r_i log-uniformly spaced over [100/137, 175/137] (deterministic — no random seed, so no knob); a decoy within 10 tolerances of T would be dropped and the drop reported (none is expected: the spacing is 0.0075 in ln r against a tolerance of 2e-5). 77 targets, the full set reported, no sub-selection. Robustness line (reported, not the verdict): 1000 decoys log-uniform over [T/2, 2T] with the same statistics.
- STATISTICS printed per atom set and depth: count at T (both conventions); the full 77-row table; median and mean over the set; max and where; T's RANK = 1 + number of targets with a strictly higher count, with the tie count; the fraction of decoys with ≥ 1 hit (the chance rate); the mean hits per decoy; every expression hitting T, listed.
- VERDICT RULE, K1813's, verbatim: T is SPECIAL only if its count is rank 1 AND more than 2× the set's median; otherwise IN THE BULK. Read on the string-count at depth 4, A1 (primary); A2, A3 and the value-count reported beside it; if the conventions disagree, both are printed and the disagreement is the finding.

## 2. Controls (run BEFORE the target run; C2 and C3 gate the read)
- C1 (must-catch, positive): the claim's own form C_2 · π^{n_C} is found at T within tolerance in A1, A2 and A3 — a search that cannot express the witness proves nothing (5408/5457 lesson).
- C2 (known numerological hit scores in the bulk): the instrument run in "5457 mode" (integer atoms A1 without π, ops + − × ^ with 5457's guards: exponent 2..8, |base| ≥ 2, |result| ≤ 10⁶; left-nested shapes only; exact integer targets [100, 175]) must reproduce K1813's record: 137 → 6 hits, 23 targets strictly higher (rank 30/76 on 5457's index convention), max 20 at 128, 18 at 125, verdict IN THE BULK. Then the same window under THIS toy's full conventions (all shapes, ÷ added, π added, exact integer targets) must still put 137 IN THE BULK. If the exact reproduction misses, the miss is reported by convention (K1813 reproduced on a different convention with different absolute counts and the same verdict) and C2 is scored a MISS.
- C3 (negative control — the rule can fire): a planted target at the window's top attractor (128 = 2^g in 5457 mode) must be flagged SPECIAL by the same rule; an instrument whose rule can never say SPECIAL is a search that cannot succeed the other way. For the real-valued instrument: the pool value of highest multiplicity inside the decoy window is planted as a 78th target and must be flagged SPECIAL.

## 3. Predictions (hashed with this text; the uncomfortable outcome is allowed)
- P1 (control) C1 passes in all three atom sets.
- P2 (control) C2 reproduces K1813 exactly (6 / 23 higher / 128:20 / 125:18) and 137 stays IN THE BULK under the full conventions.
- P3 (control) C3 fires SPECIAL in both forms.
- P4 (can fail) At depth 4, A1, the distinct-VALUE count at T within 2e-5 is exactly 1 (6π⁵ and its rewrites only; no second independent form).
- P5 (can fail) At depth 4, A1, string-count: T is IN THE BULK (rank > 1, or count ≤ 2× median). I expect the chance rate to be high: mean hits per decoy ≥ 1.
- P6 (can fail) Same verdict, IN THE BULK, at depth 4 in A3 (137 added).
- P7 (can fail) At depth ≤ 3, A1: the chance rate is low — at most 10 of 76 decoys have any hit — so T's single hit ties for rank 1 and the rule's median clause (median 0) would read SPECIAL at that depth. This is the depth-sensitivity I am naming before the run; the protocol's verdict is depth 4.
- Verdict line the toy prints: "count within tolerance = N_str / N_val; rank r of 77 (t ties); median m; K1813 rule ⟹ SPECIAL | IN THE BULK", for A1/A2/A3 at depth 4, then depth 3.

## 4. What this does and does not say (binding on citation, as K1813's non-claims were)
- Does NOT say m_p/m_e ≠ 6π⁵ to 0.002 % — that is arithmetic. Does NOT touch T187's MECHANISM (the Bergman heat-kernel reading): a forced computation is immune to this test (#31 III.6); the test separates fitting from forcing. If the count sits in the bulk, the finding is that the numerical agreement is not evidence for the mechanism, and the mechanism carries the row alone — the same sentence K1813 wrote for α.
- Output for Keeper/Cal: the count, the rank, the verdict, printed; record JSON in play/.record_5768.json; nothing external.
