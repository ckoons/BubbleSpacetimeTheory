---
title: "F490 — B4 (CKM mixing) closure sweep, HOLDING THE BAR: the tempting 'V_cb=λ², V_ub=λ³' Wolfenstein-power reading is a REVERSE-READ that fails closure-bar #1 (count not forced) — V_cb/λ²=0.81 and V_ub/λ³=0.34 are O(1) factors off, NOT small continuous corrections, so banking the powers would be the exact cluster-banking illusion Keeper flagged. The correct B4 reading (F84/T2509): the CKM leaves are the DIRECT Bergman overlaps at the generation localizations, not powers of λ — V_us = the 1-2 overlap (Grace r₂=1/2 → 0.237, few-%, candidate leaf awaiting r₂-forced), V_cb = the 2-3 overlap (localization r₃, NOT λ²), V_ub = the 1-3 overlap (r₁·r₃), CP phase δ = the PHASE of the complex Bergman kernel (a SEPARATE observable — the kernel is complex so it CAN carry a phase, but forward not inserted). So B4 closure is GATED on Grace's r₁, r₃ localizations; I do NOT bank the Wolfenstein powers. THE CHECK: observed V_us=0.2245, V_cb=0.0410, V_ub=0.00382; λ=V_us=0.2245 → λ²=0.0504 (V_cb/λ²=0.81, needs A≈0.81), λ³=0.0113 (V_ub/λ³=0.34, needs the (ρ,η)/CP factor). The A≈0.81 and 0.34 are O(1) structure, not <σ continuous corrections — so V_cb, V_ub do NOT satisfy 'discrete count (forced) + small continuous correction' with λ²,λ³ as the count. Banking them as powers reverse-reads the observed hierarchy → fails the closure bar (count must be FORCED target-innocent, not chosen because it's near the observable). CORRECT reading: each CKM leaf = the direct overlap at its localization (F84 = Bergman kernel three ways; T2509 = CKM as one geometric kernel, zero parameters), few-% (the continuous/curvature side), the discrete structure = the localization positions {r₁,r₂,r₃}. V_us candidate (awaits Grace's r₂=1/2 confirmed FORCED, not read off 0.2245); V_cb, V_ub await Grace's r₁, r₃; CP phase = the complex-kernel phase, separate and forward. HONEST at peak convergence: this is the exact cluster the over-sell-#6 bar exists for — the down-ladder and neutrino leaves ripening together tempts banking the CKM powers too, but the powers are reverse-reads. I hold the bar and wait for Grace's localization geometry. No bank, count 8. For Keeper (B4: V_cb=λ²/V_ub=λ³ is a reverse-read, fails bar #1 — the leaves are direct overlaps awaiting Grace's r₁,r₃; hold, don't cluster-bank), Grace (the CKM leaves need r₁,r₃ localizations forward — V_cb is the 2-3 overlap not λ²; is r₂=1/2 FORCED or read off V_us?), Elie (CKM diagonalization awaits the overlaps; the CP phase is the complex-kernel phase), Casey (rowing — I held the CKM leaves at the bar; they're direct overlaps, not the tempting Wolfenstein powers), Cal."
author: "Lyra (Claude Opus 4.8)"
date: "2026-07-07 Tuesday (date-verified)"
status: "v0.1 — B4 (CKM) closure, HOLDING THE BAR. The 'V_cb=λ², V_ub=λ³' reading is a REVERSE-READ (V_cb/λ²=0.81, V_ub/λ³=0.34 — O(1) factors off, not <σ corrections) → fails closure-bar #1 (count not forced) = the cluster-banking illusion. CORRECT (F84/T2509): CKM leaves = DIRECT Bergman overlaps at localizations {r₁,r₂,r₃}, few-% (continuous side); V_us = 1-2 overlap (r₂=1/2 → 0.237, candidate, awaits r₂-FORCED not read-off), V_cb = 2-3 overlap (r₃, NOT λ²), V_ub = 1-3 (r₁·r₃), CP phase δ = complex-kernel phase (separate, forward). B4 GATED on Grace's r₁,r₃. I do NOT bank the Wolfenstein powers — held the bar at peak convergence (over-sell #6). No bank, count 8."
---

# F490 — B4 (CKM) closure: hold the bar; the leaves are direct overlaps, not Wolfenstein powers

Closure sweep, my B4 lane. The cluster ripening (α, root-lock #1, neutrino leaves, the down-ladder counts) tempts banking the CKM leaves too — as the "obvious" Wolfenstein powers λ², λ³. That's a reverse-read, and the bar fires hardest exactly here.

## The check — V_cb, V_ub are NOT clean powers

observed V_us = 0.2245, V_cb = 0.0410, V_ub = 0.00382; λ = V_us = 0.2245.
- λ² = 0.0504 → **V_cb/λ² = 0.81** (needs A ≈ 0.81)
- λ³ = 0.0113 → **V_ub/λ³ = 0.34** (needs the (ρ,η)/CP factor)

The A ≈ 0.81 and 0.34 are **O(1) structure, not <σ continuous corrections.** So "V_cb = λ², V_ub = λ³" does *not* fit "discrete count (forced) + small continuous correction" — the powers miss by O(1) factors. **Banking them would reverse-read the observed hierarchy** (choosing λ², λ³ *because* they're near V_cb, V_ub) → fails closure-bar #1 (the count must be forced target-innocent). This is the cluster-banking illusion Keeper flagged; I hold.

## The correct reading — direct overlaps (F84 / T2509)

The CKM leaves are the **direct Bergman overlaps at the generation localizations** {r₁, r₂, r₃} (F84 = kernel three ways; T2509 = CKM as one geometric kernel, zero parameters) — *not* powers of λ:
- **V_us** = the 1-2 overlap (Grace: r₂ = 1/2 → 0.237, few-%). **Candidate leaf** — banks *only* if r₂ = 1/2 is FORCED (from the geometry), not read off V_us = 0.2245.
- **V_cb** = the 2-3 overlap (localization r₃) — Grace must compute r₃; it is **not** λ².
- **V_ub** = the 1-3 overlap (r₁·r₃) — awaits r₁, r₃.
- **CP phase δ** = the **phase of the complex Bergman kernel** — a *separate* observable (the kernel is complex, so it *can* carry a phase, but it must be computed forward, not inserted).

So **B4 closure is gated on Grace's r₁, r₃ localizations.** Each leaf is few-% (the continuous/curvature side); the discrete structure is the localization positions, which must be forced.

## Honest tier

| CKM leaf | status |
|---|---|
| V_cb = λ², V_ub = λ³ (Wolfenstein powers) | **REVERSE-READ — rejected** (O(1) factors off; fails bar #1) |
| V_us = the 1-2 overlap (0.237, few-%) | **candidate leaf** — awaits r₂=1/2 FORCED (not read off V_us) |
| V_cb, V_ub = the 2-3, 1-3 overlaps | **await Grace's r₁, r₃** — direct overlaps, not powers |
| CP phase δ = complex-kernel phase | **separate observable** — forward, not inserted |
| any CKM bank | **NOT yet** — gated on Grace's localization geometry |

**No bank, count 8.** F490: at the peak-convergence cluster, the CKM leaves tempt a bank as the obvious Wolfenstein powers — but V_cb/λ² = 0.81 and V_ub/λ³ = 0.34 are O(1) off, so the powers are a reverse-read that fails the closure bar. The correct reading is the direct Bergman overlaps at the localizations (F84/T2509), which are gated on Grace's r₁, r₃ geometry. I hold the bar and wait — no cluster-banking.

## Plain-language version

The team's having a good day — several results ripening at once off Casey's new principle — and that's exactly when it's most tempting to grab one more. My piece is the quark-mixing angles, and there's an "obvious" way to claim two of them: the textbook pattern says they should be a small number λ, then λ-squared, then λ-cubed. Trouble is, when I actually check, the squared and cubed versions are off by 20% and a factor of 3 — those aren't tiny corrections, they're big misses. So claiming "it's λ² and λ³" would be *fitting the pattern to the answer* rather than deriving it — the precise mistake we've caught five times this run. The honest reading is different: each mixing angle is a direct *overlap* between two generations in the geometry, and to get the real numbers Grace has to compute where the generations actually sit. Until she does, I bank nothing — I just flag that the tempting shortcut is a trap, and wait for the real geometry. Holding the line is the contribution here.

— Lyra, 2026-07-07. F490 (B4 closure): held the bar. V_cb=λ²/V_ub=λ³ is a REVERSE-READ (V_cb/λ²=0.81, V_ub/λ³=0.34 — O(1) off, not <σ) → fails closure-bar #1 (count not forced) = the cluster illusion. Correct (F84/T2509): CKM leaves = DIRECT overlaps at localizations {r₁,r₂,r₃}, few-% continuous side; V_us=1-2 overlap (candidate, awaits r₂-FORCED), V_cb=2-3 (r₃, not λ²), V_ub=1-3 (r₁r₃), CP phase=complex-kernel phase (separate, forward). Gated on Grace's r₁,r₃. No cluster-bank. Count 8.
