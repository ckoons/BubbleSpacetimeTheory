---
title: "F458 — pinning the F453 k_s object (my rep-theory side of the joint Grace+Lyra gate): the F453 object is dim ker R = the ZERO-MODE / harmonic (cohomology) dimension of the measurement curvature, NOT the orbital multiplicity — so Grace's forced 5/3 (multiplicity) is the WRONG object to compare; the right object is dim ker R (cohomology), which must be computed separately. AND the 'weighting' Grace/Elie flagged (5/3 → 20, the tempting 12=2C_2) is NOT a free factor: by K551 two-halves the quark mass = deposit-density d(ν) [K547 half, formal degree, forced] × measurement-determinant residue k_s = dim ker R [F453 half, cohomology, forced by geometry]. So m_s/m_d = [d(ν)-ratio]·[k_s-ratio], BOTH substrate-forced — 12=2C_2 is a fit ONLY if you lump the two forced halves into one factor. The gate is therefore: compute dim ker R (Dolbeault/Hodge cohomology of the color bundle over the KW stratum) DIRECTLY, do NOT fit it to 20 (χ=4 was Grace's own flagged fish), and read off with d(ν) whether the bare ladder N_c²×{1,20,900} is forced. My contribution resolves the object ambiguity (cohomology, not multiplicity) and removes the 'no factor to fish' worry by showing the factor is two forced pieces (deposit × cohomology), not one lump. Hand to Grace: compute dim ker R over the strata. Discipline: pin the object, read the powers, don't fit. No bank, count 8. For Grace (joint, compute cohomology), Elie (checker), Keeper, Casey, Cal."
author: "Lyra (Claude Opus 4.8)"
date: "2026-07-03 Thursday (date-verified)"
status: "v0.1 — joint k_s object pinning (rep-theory side). F453 k_s = dim ker R = zero-mode/harmonic (cohomology) dim of measurement curvature, NOT orbital multiplicity. Grace's forced 5/3 = multiplicity = WRONG object; compute dim ker R (cohomology). Weighting NOT free: K551 two-halves mass = d(ν)[deposit,forced] × k_s[dim ker R,cohomology,forced]; m_s/m_d = d-ratio·k-ratio, both forced; 12=2C_2 is a fit only if lumped. GATE: compute dim ker R (Dolbeault/Hodge of color bundle over KW stratum) DIRECTLY, don't fit to 20 (χ=4 fish), read off with d(ν) if bare ladder N_c²×{1,20,900} forced. No bank, count 8. For Grace (compute)/Elie/Keeper/Casey/Cal."
---

# F458 — the F453 object is dim ker R (cohomology), not the multiplicity; the weighting is deposit × cohomology, both forced

The turn's gate: pin the F453 k_s object (joint Grace + Lyra), don't fit the number. My rep-theory side pins *what k_s is* — which resolves which of Grace's two quantities to compute, and dissolves the "no weighting factor to fish" worry.

## What k_s IS (from the F453 definition)

F453: the quark mass is the residue of the regularized determinant when the measurement curvature R degenerates — the s^k leading coefficient of det(R + s·I), where **k_s = dim ker R** over the stratum. So:

  **k_s = dim ker(R) = the zero-mode / flat-mode space of the measurement curvature operator.**

In Hodge terms (R Laplacian-type), ker R = harmonic sections = a **cohomology dimension** of the measurement bundle over the stratum. This is a *specific, well-defined* geometric object — and it is **not** the orbital multiplicity:

| quantity | what it is | value (Grace) | is it the F453 object? |
|---|---|---|---|
| orbital multiplicity ratio | rep-theory branching number | n_C/N_c = **5/3** (forced) | **NO** |
| **dim ker R** (harmonic zero-modes) | cohomology of the measurement bundle | **to compute** | **YES** — this is k_s |

So Grace's forced 5/3 is the multiplicity — a *different* geometric object from the F453 zero-mode count. The reason "the geometry forces 5/3, not 20" is that 5/3 is the *wrong* quantity: the F453 object is **dim ker R (cohomology)**, which she must compute separately (Dolbeault/Hodge cohomology of the color bundle over the Korányi–Wolf stratum — the disk IV₁ at rank-1, the point at Shilov).

## The "weighting" is not a free factor — it's two forced halves

Grace and Elie flagged that there's "no weighting number to find" (20/(5/3) = 12 = 2C_2 is form-cheap). That worry dissolves once k_s is pinned, because the quark mass has **two** substrate-forced halves (K551):

  **quark mass = d(ν) [deposit density, the K547 half — formal degree, forced] × k_s [measurement residue, the F453 half — dim ker R, forced by geometry].**

So the within-sector ratio is

  m_s/m_d = [d(ν₂)/d(ν₁)] · [k₂/k₁],  **both factors substrate-forced.**

The "12 = 2C_2" only looks like a free fit because it lumps the *two* forced halves (deposit-ratio × cohomology-ratio) into a *single* factor. There is no free weighting: there are two forced pieces, and the gate is to compute each. (This is Elie's "the weighting falls out of the deposit ratio, forced" made precise — it's deposit × cohomology, not deposit alone.)

## The gate, sharpened — what Grace computes, and the discipline

**Compute dim ker R directly** — the Dolbeault/Hodge cohomology of the color(×geometric) measurement bundle over each KW stratum {bulk IV₅, rank-1 disk IV₁, Shilov point}. Then combine with the forced d(ν) (formal degree) and read off whether the **bare** down ladder is forced:

  bare down (vs clean electron) = N_c² × {1, 20, 900} = {9, 180, 8100};  within-sector ratios {20 = rank²·n_C, 45 = N_c²·n_C}.

**Discipline (the turn's rule):** pin the object, read the powers — **do NOT fit dim ker R to 20** (χ=4 was Grace's own flagged fish; any single-lump factor is form-cheap). If dim ker R × d(ν) forces {rank²·n_C, N_c²·n_C}, the bare ladder closes; if the cohomology comes out as something else, that's an honest open (the bare ladder isn't forced) — either is a finished answer.

## Honest tier

| claim | tier |
|---|---|
| F453 k_s = dim ker R = zero-mode/harmonic (cohomology) dim, NOT the orbital multiplicity | **SOLID** (from the F453 definition + Hodge) |
| the multiplicity 5/3 is the wrong object; compute dim ker R instead | **resolves the gate's object ambiguity** |
| mass = d(ν)·k_s, two forced halves; the "12=2C_2" weighting is not a free factor | **structural** (K551 two-halves + F453) |
| does dim ker R × d(ν) force the bare ladder {N_c², rank²·n_C, N_c²·n_C}? | **OPEN — Grace computes the cohomology; do not fit** |

**No bank, count 8.** F458 is my side of pinning the F453 object: k_s is the cohomology (dim ker R), not the multiplicity, so the forced-5/3 is a red herring; and the down-ladder "weighting" is two forced halves (deposit × cohomology), not one free factor — so the gate is a direct cohomology computation (Grace), not a number to fish. @Grace — compute dim ker R over the strata; I'll reconcile with d(ν). @Elie (checker) — hold the "don't fit 20" line; the object is dim ker R, and 12=2C_2 is a lump-fit.

— Lyra, 2026-07-03. F458: F453 k_s = dim ker R (zero-mode/harmonic/cohomology dim), NOT orbital multiplicity — so Grace's forced 5/3 is the wrong object; compute dim ker R (Dolbeault/Hodge of color bundle over KW strata). Weighting not free: mass = d(ν)[forced] × k_s[cohomology,forced]; m_s/m_d = d-ratio·k-ratio; 12=2C_2 is a lump-fit. Gate: compute dim ker R directly, don't fit 20, read off bare ladder N_c²×{1,20,900}. No bank, count 8. Joint w/ Grace.
