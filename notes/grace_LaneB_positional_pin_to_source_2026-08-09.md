# Grace — LANE B positional pin (BLIND, to source): {1,3,5} vs {5/2,3/2,0} RESOLVED + a guardrail before firing
*2026-08-09. Pinned to K1012 + K1011 (both standing, verified this turn — no reopening). "Pin conventions, never guess."*

## THE PIN (resolved to source — they are NOT competing homes, and NOT the same coordinates)
Per **K1012** (primary, "mass = radial boundary-distance in every sector"), the two position sets are **two slices of ONE object** — the cross-address two-point kernel **K((ν_i,m_i),(ν_j,m_j))** on D_IV⁵:
- **{1,3,5} = down quarks** — cross-**degree** at *fixed* Bergman weight ν=5 (single-row K-types). This is the **same-ν slice** = the **validated Jack(α=2/3) binomial** = the built-in **consistency check** (must reproduce down {3,60,2520}, V_us=1/√20).
- **{5/2,3/2,0} = charged leptons** — cross-**ν** addresses (interior/edge/boundary). A **different slice** of the *same* kernel.
- Up quarks = cross-ν too (radii {1,α,soft}); ν = boundary Majorana (F659). **The two-tier structure — quarks = degree-Jack (same-ν), leptons = cross-ν — is BANKED (K1011).**

So the last-turn ambiguity I refused to guess is **reconciled**: {1,3,5} and {5/2,3/2,0} are not two rival ladders; they are the same-ν and cross-ν slices of one two-point kernel. The down same-ν slice is the consistency check for the whole kernel.

## ★ GUARDRAIL (blind — this is why the pin matters before firing)
**The fixed-ν weighted-norm route to the lepton MASS is a STRUCTURALLY-FORCED NULL (K1011, ruled "clean/final," still standing — verified no reopening).** At fixed ν the Gindikin/Γ_Ω factor cancels, so π² cannot survive a mode-to-mode overlap ratio → the c-function/F323 route to the muon is CLOSED. K1012 states it explicitly: *"Do NOT re-run the fixed-ν weighted-norm (K1011 forced null); the cross-address kernel is a different object."* This route ate a 34-day loop before K1011 settled it.

**Consequence for this round's Lane B:** "leptons ride the derived DOWN-template for their MASSES" — evaluated naively at the lepton ν-positions — **IS the same-ν object that nulled.** Leptons are cross-ν (the two Γ_Ω addresses differ and do NOT cancel); that's a *different* matrix element (the OFF-DIAGONAL / mixing), not the down-template mass reading. Firing the down-template at lepton positions either re-hits the null or invites a fit to force (24/π²)⁶ — both are traps the source already flagged.

## What the source says the cross-check ACTUALLY is (the corrected object)
- **Lepton MASSES are already banked by SEPARATE routes** (muon on e=n, K986; τ on 49·71; μ/e on (24/π²)⁶). K1012: *"judge the kernel on the mixings + the down-slice check, NOT on reproducing (24/π²)⁶."* So the kernel need not — and structurally cannot, at fixed ν — re-derive the lepton masses.
- **The live cross-sector object = the cross-address kernel's MIXING output:** validate K on the down same-ν slice ({3,60,2520}, V_us=1/√20), then evaluate at the lepton cross-ν addresses {5/2,3/2,0} → the **colorless off-diagonal → PMNS** (large mixing = colorless, K1194/F659). That is the one-kernel-spans-both test, and it's target-innocent (down slice fixes it, leptons predicted).

## ★ Flag to @Keeper / @Casey (mass-vs-mixing framing — your ruling)
This round's Lane B says "fire the lepton-MASS cross-check; 71/24/exp-6 are required OUTPUTS." The source (K1011/K1012, both standing) says: (a) the lepton-mass-from-a-shared-kernel route is the **nulled** fixed-ν object; (b) the masses are banked separately; (c) 71/24/exp-6 should **not** be required kernel outputs. **So the honest, non-null cross-check is the MIXING (PMNS off-diagonal) + the down-slice mass check — not the lepton masses.** If you want the masses reconsidered, that reopens K1011 (a ruled clean/final null) — a governance call, not something to fire silently. I did not fire the nulled route.

## Blind-score staged (on the CORRECT object)
When @Elie/@Lyra exhibit the cross-address two-point kernel K:
1. **Down-slice check (must pass first):** K at same-ν (ν=5) reproduces {3,60,2520}, V_us=1/√20. If it fails the down slice, it's wrong before leptons (the α=1 Schur-canary discipline, one level up).
2. **Lepton off-diagonal:** K at {5/2,3/2,0}×{5/2,3/2,0}, colorless-grade → PMNS. I score the PMNS angles + the derive-vs-imported split **blind**; Elie's PMNS 3/3 (sin²θ₁₂=3/10, θ₁₃ 0.03σ, θ₂₃=5/9) is the target to reproduce from ONE kernel, not three fits.
3. **g²=49** remains the one target-innocent mass anchor **if** the masses are ever revisited — but per K1011 that route is nulled; noted, not required.

## Status
No node (a pin + guardrail, correctly un-banked). Positions RESOLVED to source (two slices of one kernel); the fixed-ν lepton-mass route stays NULL (K1011); the live cross-check is the mixing. Cross-refs: K1012 (two-point kernel, unifies up+lepton), K1011 (fixed-ν null, two-tier finding banked), K986 (muon e=n), K1194/F659 (colorless→large PMNS), K945 (strata-vs-K-types home). Nothing pushed.
