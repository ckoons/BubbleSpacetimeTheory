# PRE-REGISTRATION — LANE I: the write channel as the non-equivariant ingredient (ℓ=2 native dynamics)
**Lyra (draft owner + merge owner), 2026-08-26, 07:38 EDT (clock-verified). Co-sign: Cal (his steering
audit is the co-sign pass, per R98). Gate: Keeper. Filed against the R98 Lane-I spine; this prereg
fills the spine and adds NOTHING looser. One shot. Corpus-only — no mining in this lane (Casey).**

---

## 0. RECONNECT — the wall's own terms, quoted from the artifact (not from memory)

From my 2026-08-25 ledger (the R68c floor, banked): the five directions = i·Sym²₀(V₁₂) = the spin-2
quadrupole sector; realized geometrically (Toeplitz), **the spin-3 scalar of [T_{Y22}, T_{Y21}] is
NONZERO** (measured, #108 arc), closure holds only on the 3-dim carrier where dimension forbids
spin-3 (accident, not mechanism); and **"the R68c multiplicity analysis says every K-equivariant
antisymmetric bilinear on the quadrupole carries spin-3 with multiplicity 1, so a reopening idea must
break equivariance or antisymmetry, and must say which."**

**DECLARED IN ADVANCE, on this artifact's face: this lane breaks EQUIVARIANCE. Antisymmetry is
retained. That is the "say which."** Casey's shot supplies the physical reading: the missing dynamics
is the write — information mapping into 3D — and selection is not equivariant.

## I-1 — THE INGREDIENT MUST BE EXHIBITED FROM THE BANK, NOT CONSTRUCTED FOR THE LANE

The write-channel operator W is to be assembled from EXACTLY these banked objects (grep-dated at use):
- **commit_op** (the ∂Ω-crossing + amplification operator; #67 §2b, P1′, the ordered-pair/funnel
  structure on π₀(J∖∂Ω) = 2),
- **the Δ-twisted Szegő machinery** (the v3 exhibit `Lyra_v3_EXHIBIT_..._2026-08-24.md` +
  the symbol page `Lyra_SZEGO_SYMBOL_PAGE_..._2026-08-25.md`: S_Δ = S·τ_Δ(κ), the banked 2D↔3D map).

**Two theorem obligations before any bracket is computed:**
(a) **Non-equivariance of W must be a THEOREM of the banked commit_op** — exhibited (the selection of
a component of π₀(J∖∂Ω) is not K-covariant), not assumed because the shot wants it.
(b) W restricted to the quadrupole sector must be well-defined from (commit_op ∘ twisted-Szegő) with
**zero new choices**. **If either fails: the lane is NOT READY and does not open** (D1a-style — a
floating ingredient is the same disease as a floating exponent).

## I-2 — THE EXACT COMPUTATION (one shot)

Compute **[W(T_{Y22}), W(T_{Y21})]** — the write-channel-dressed version of the SAME pair whose bare
bracket carried the measured nonzero spin-3 scalar — **exactly (symbolic), and extract its spin-3
scalar.** No truncations; the bracket computation on the quadrupole is exact or it is not a shot.

## I-3 — INSTRUMENT VALIDATION BEFORE ANY VERDICT (the 5-false-negative-modes discipline)

- **Must-catch (positive control):** the UNDRESSED equivariant bracket, run through the same pipeline,
  reproduces the banked nonzero spin-3 scalar of #108.
- **Must-reject (known null):** the 3-dim carrier closure is reproduced (spin-3 scalar = 0 there).
**A pipeline failing either control VOIDS the shot without verdict** — a search that cannot succeed
proves nothing, and neither does one that cannot fail.

## I-4 — PRE-COMMITTED OUTCOMES (verbatim from the R98 spine; meanings fixed now, none available later)

- **Spin-3 PRESENT** ⟹ the five directions' dynamics are induced by the write channel and the
  reopening door's terms are met — **WIN, at whatever tier the exhibits support.**
  *Empty-confirmation guard (banked feedback: test the property the claim needs, not existence):
  "present" ALONE is not the claim. The win requires all three exhibits on the artifact's face:
  (a) I-1(a)'s non-equivariance theorem, (b) nontrivial action on i·Sym²₀(V₁₂) (nonzero there, not
  merely nonzero somewhere), (c) which-broken named. A bracket that comes out both spin-3-present AND
  equivariant is a CONTRADICTION with R68c ⟹ instrument error ⟹ back to I-3, no verdict.*
- **Spin-3 ABSENT** ⟹ **the wall holds and DEEPENS**: the write channel joins R68c's excluded list —
  a floor with a bigger number, banked as such.
- **Ingredient not exhibitable from the bank (I-1 fails)** ⟹ **the lane is not ready** — honest,
  D1a-style, and it closes today as that floor.

## I-5 — BUDGET, ROLES, NON-CLAIMS

One-shot budget: **one W-assembly, one bracket, no second dressing if the first disappoints.**
Values/exhibits first, verdict last, same-hour post. Merge owner: Lyra. **Non-claims:** no change to
#108's imported-gauge-principle floor for su(3) from ANY outcome here without Cal's separate reopening
(the door is his to swing on that side); the descent tier-line (T2564/K1509) untouched; nothing in
this lane touches the R2 packet (R1-PASSED and HELD, Casey's word only).

*— Lyra. The wall demanded "break equivariance or antisymmetry, and say which." This prereg says
which, in advance, and requires the breakage be a theorem of the bank rather than a wish of the lane.*

---

## I-6 — AGAINST CAL'S §770 BLIND LIST, LINE BY LINE (added 07:45 EDT, before audit, after reading §770)

1. **Can-fail count: 3 outcomes, of which 3 can fire.** WIN fires iff the exact scalar ≠ 0 AND the
   three win-exhibits stand; DEEPENED-WALL fires iff the exact scalar = 0 (reachable: the dressing
   could project the bracket into the closed part); NOT-READY fires iff I-1's theorem obligations
   fail (reachable: non-equivariance of commit_op may not be a theorem of the bank — that is a live
   possibility, not a formality). No unreachable limb in either direction.
2. **Target quarantine:** spin-3 presence is the OUTPUT. The banked #108 scalar VALUE enters exactly
   once — as the must-catch control's expected output on the UNDRESSED bracket — and never steers the
   dressed computation, whose scalar is computed blind and posted as a value before any verdict word.
3. **Zero knobs:** W = (banked commit_op) ∘ (banked twisted-Szegő), no parameters, no normalization
   choice beyond those the banked objects already carry. If assembly demands one choice, that IS the
   I-1 failure and the NOT-READY limb fires — the knob cannot survive into a shot.
4. **Grep dates:** commit_op → #67 §2b (2026-08-24 ratified floor); twisted machinery → v3 exhibit
   (2026-08-24) + symbol page (2026-08-25); R68c multiplicity + #108 scalar → ledger 2026-08-25,
   banked K1826-adjacent arc. Nothing in the ingredient list is first written today; the ASSEMBLY is
   today's work and claims nothing beyond its factors.
5. **COI, declared and discharged:** Lyra drafted the prereg, assembles W, computes the bracket — the
   beneficiary computes. Discharge: full symbolic pipeline posted (auditability where blindness is
   unavailable), both controls' raw outputs posted with it, dressed scalar posted AS A VALUE in the
   same artifact, verdict last; Cal rules whether the three win-exhibits stand; Keeper gates the bank.
6. **Controls RUN, not cited:** must-catch (undressed bracket → banked nonzero scalar) and
   must-reject (3-dim carrier → 0) both execute pre-shot in the same pipeline; outputs in the artifact.
7. **Tolerance ladder:** the shot is EXACT SYMBOLIC — presence/absence of the spin-3 scalar is a
   zero-test in exact arithmetic, no numeric tolerance exists or is needed. **Numeric fallback is
   FORBIDDEN this shot**: if symbolic evaluation proves infeasible, the shot stops and reports the
   infeasibility as its result rather than inventing a tolerance mid-flight.
8. **Family priced:** the R68c multiplicity statement and the #108 measurement are banked AT n = 5;
   no family sweep is run and none is claimed. Any win is an **n = 5 claim only** — no "the write
   channel selects n" assertion is available from this lane (that would be a family-selector claim
   this prereg explicitly does not license).
9. **Subscripts:** commit_op (never bare "commit"); the quadrupole carrier is Σ_spin-side machinery
   via the Δ-twist — no vary-n form appears, so C₂ does not; AV grading untouched here.
10. **Held-premise cap, stated:** commit_op stands on the Internal A floor — P0 + P1′ (POSITS) + the
    ruler. **Therefore the WIN limb's maximum tier is DERIVED-CONDITIONAL on the Internal A floor**,
    stated now, in the prereg, not negotiated at verdict time. The ν=0 pile (K1829 fence) is not
    touched by any link in this chain.

*— Lyra, I-6 added post-§770, pre-audit. The cap in line 10 is the honest price of using the commit
operator at all: a lane that borrows the floor inherits the floor's conditionality.*

---

## ADDENDUM (07:52 EDT, PRE-AUDIT — the last moment a gate edit is legitimate, per the v2 precedent)

**Reconnect result on I-1(a), from the artifact (#67 §2a/§2b, read this morning, not from memory):**
the bank states **C = P_record ⊕ P_encode is the Peirce decomposition of a PRIMITIVE IDEMPOTENT e**
(Derived, K1607/K1609), and **isotropy_commit** (banked) says Aut(J) acts transitively on S(J₀) — the
primitive idempotents form a full orbit. **Therefore non-equivariance of commit_op is already a
theorem of the bank:** C(e) commutes only with Stab(e) ⊊ K; k·C(e)·k⁻¹ = C(k·e) ≠ C(e) off the
stabilizer. The selection Casey named IS the choice of e. **I-1(a) is discharged by citation; the
NOT-READY limb narrows to I-1(b) alone** (W well-defined on the quadrupole with zero new choices).

**One degenerate subcase PRE-NAMED, meanings fixed before any computation:** the bank also says
P_encode projects onto **the confined 3-dimensional block V₁₂** — the very carrier where closure
holds by the dimension accident. So a dressed bracket can come out spin-3 = 0 **by confinement**:
the dressing merely compresses onto the 3-dim carrier and rediscovers the accident. **Ruling, fixed
now:** if the scalar is 0 AND the dressed bracket coincides with the compression onto the known
closed 3-dim algebra, the outcome is **"wall deepens via the KNOWN accident"** — banked as
zero-new-content, NOT as a novel exclusion and NOT as closure-achieved. Absence-by-cancellation
(zero WITHOUT coinciding with the known compression) remains the ordinary DEEPENED-WALL limb. No
other meanings become available after evaluation. *(This is the empty-confirmation guard's mirror:
a zero that is construction-guaranteed on a sub-carrier proves nothing there either.)*
