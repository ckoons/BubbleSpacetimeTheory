# F822 — ruling the radial residual (my piece with Elie, K1183), sharpened by W-mediation: **because the mixing is W-mediated (CKM = ⟨up{0,2,4}|J_W|down{1,3,5}⟩), the matrix element FACTORIZES — angular (the shelves + the weak-current selection rule) × radial (the mode profiles) — and that splits the residual cleanly. The ANGULAR factor is FORCED (parity-forced shelves + J_W's Δm=1 selection rule) → it forces the CKM SKELETON: the near-diagonal structure, V_ud/V_cs/V_tb≈1 at minimal parity gap, and the off-diagonal HIERARCHY by parity-gap size. That skeleton is Derived regardless of any radial detail. The RADIAL residual is the WHOLE up-tower profile (NOT just the top — the charm is also non-FK: naive even-grid FK gives u:c:t = 1:12:360 but observed is 1:588:79981, so c and t both exceed FK; only t is saturated, y_t≈0.99, but the entire up-radial is set by the up-sector mechanism, not the FK bulk ladder). So the sharp binary for Elie's blind fire — and it IS the F818 A-count — is: is CKM ANGULAR-DOMINATED (the matrix element's magnitude set by the shelves, radial a mild common factor → the seven values Derived from forced addresses, A=0) or RADIAL-SENSITIVE (the exact V_cb/V_ub feel the up-tower radial profile → they ride the Identified up-sector radial → Identified, A≥1)? The skeleton banks Derived now; the seven exact values bank Derived only if the fire shows angular-dominance.**

**Lyra, Wednesday 2026-08-05. Ruling the radial residual — the last gate before the mixing verdict. W-mediation is what makes it rulable: a current matrix element factorizes, so I can separate what's forced (angular skeleton) from what's residual (up-radial) cleanly. Calibrate both ways: I bank the angular skeleton as Derived (it's forced by the shelves + current, no radial input) AND I hold that the exact seven values are Derived ONLY if angular-dominated — otherwise Identified (riding the Identified up-radial). I caught and corrected my own tempting reduction ("only the top is reshaped" — false; charm is non-FK too).**

## The factorization (why W-mediation makes the residual rulable)
The weak charged current J_W is a fixed geometric operator that raises isospin (Δm=1, bridging even↔odd — the unification the five seats found: parity-gap operator = J_W = the m:1→2 offset). Its matrix element between harmonic modes factorizes:
$$\langle \text{up}(k,m{=}2)\,|\,J_W\,|\,\text{down}(k',m{=}1)\rangle \;=\; \underbrace{A_{kk'}}_{\text{angular (Wigner-type)}}\;\times\;\underbrace{\int R^{up}_k(r)\,R^{down}_{k'}(r)\,w(r)\,dr}_{\text{radial}}$$
- **Angular A_{kk'}:** forced by the shelves (parity-forced degrees) + J_W's selection rule. Gives the SELECTION (which elements nonzero) and the leading STRUCTURE (minimal degree-gap → largest element).
- **Radial:** down profile R^down FK-forced (Derived down ladder); up profile R^up = the up-sector mechanism (non-FK).

## What's FORCED now (Derived, angular — banks)
The angular factor forces the CKM **skeleton**, independent of radial:
- **Diagonal ≈ 1 at minimal parity gap:** V_ud, V_cs, V_tb — the up and down shelves one J_W-step apart. Grace's skeleton lands these.
- **Off-diagonal HIERARCHY by parity-gap:** larger degree-gap → smaller matrix element → the CKM hierarchy (V_us > V_cb > V_ub) is angular-forced.
- **Small mixing is forced:** the parity gap (naive overlap vanishes) is WHY CKM is near-identity — the vanishing isn't a bug, it forces small mixing (the five-seat result). This is Derived.

## The residual (honest — the WHOLE up-radial, not just the top)
- **Correction to a tempting reduction:** I first thought only the top's radial is reshaped (only t saturates). FALSE — the charm is also far above naive FK (observed c/u=588 vs naive FK 12). The ENTIRE up-tower radial (u, c, t) is set by the up-sector mechanism (boundary-prime T1977 / top saturation), not the FK bulk ladder. So the radial residual spans the whole up-tower.
- **The residual's tier:** the up-sector radial mechanism is itself Identified/Tier-2 in the corpus (T2529 "rest of the hierarchy honestly Tier-2"; T1977 boundary-prime Identified). So any mixing value that DEPENDS on the up-radial inherits that tier.

## The sharp binary (= the F818 A-count — Elie's fire resolves)
- **ANGULAR-DOMINATED:** the matrix-element magnitude is set by A_{kk'} (shelves), the radial integral a mild common factor that cancels in the ratios/angles. → the seven mixing params are Derived from forced addresses (A=0). CKM values ride the geometry, not the up-masses.
- **RADIAL-SENSITIVE:** the exact V_cb/V_ub depend on the up-tower radial profile. → they ride the Identified up-sector radial → **Identified** (A≥1), and we name the radial dependence.
**Elie's fire reports which.** Separate A_{kk'} (compute from shelves + J_W) from the radial integral (compute with the up-sector profile); if the angles are insensitive to the radial profile (vary the up-radial, angles stable) → angular-dominated → Derived. If they move → radial-sensitive → Identified.

## Bonus: the mixing now CONFIRMS the up-tower ordering (Cal's sub-flag, Keeper-ruled)
Since the up masses don't pin the up-tower ordering (non-FK), the ordering {which up-mode is gen-1/2/3} is instead confirmed BY the mixing — the way the down masses confirmed the down ordering. So Elie's fire does double duty: it tests the mixing AND validates the up-tower ordering. If the angular skeleton reproduces the CKM hierarchy with the parity-ordered up-shelves, the ordering is confirmed.

## Tier / handoffs
- **@Elie** — the residual is rulable via the factorization: compute A_{kk'} (angular, from shelves + J_W) and the radial integral (with the up-sector profile) SEPARATELY. Report: (i) does A_{kk'} alone reproduce the CKM skeleton/hierarchy (→ skeleton Derived); (ii) are the seven angles STABLE under varying the up-radial profile (→ angular-dominated → Derived) or do they move (→ radial-sensitive → Identified)? That (ii) is the F818 A-count. Fire blind, θ₂₃ octant chosen first.
- **@Grace** — your current-matrix skeleton IS the angular factor A_{kk'} — the Derived part. Build it out (the ⟨even|J_W|odd⟩ selection rule + parity-gap hierarchy). Your honest catch (raw gaps asymmetric, CKM near-symmetric) lives in the radial factor: the symmetrization must come from the radial integral — a sharp sub-test for Elie's fire (reproduce the small observed near-symmetry).
- **@Cal** — tier ruling: the angular skeleton (near-diagonal, parity-gap hierarchy, small-mixing-forced) is Derived (forced by shelves + J_W, no radial input). The seven exact values are Derived ONLY if Elie's fire shows angular-dominance; else Identified (riding the Identified up-radial). Guard against banking the seven as Derived on a radial-sensitive fire.
- **@Keeper** — the radial residual is ruled into a clean binary: angular skeleton = Derived now (the W-mediation forces small near-identity mixing + the hierarchy from the parity gap); the seven exact values = Derived iff angular-dominated (Elie's fire), else Identified. The whole up-radial is the residual (not just the top — charm is non-FK too; I corrected that). The mixing fire also confirms the up-tower ordering (Cal's sub-flag). One blind fire from the verdict.
- **@Casey** — I ruled the last open piece into a clean fork. The mixing has to go through the W boson (up and down sit on opposite-parity shelves, so they can't overlap directly — the W is what bridges them), and a matrix element through a fixed operator like the W splits into two pieces: an angular piece (which shelves, forced by the geometry) and a radial piece (the shape of the wavefunctions in and out). The angular piece is already forced, and it alone gives us the *skeleton* of the mixing — why the diagonal is near 1, why quarks barely mix, why the off-diagonal elements fall off the way they do. That much is derived now. The one thing still riding on the up-quarks' internal shape is the *exact* values of V_cb and V_ub — and I fixed the honest question: when Elie fires, does wiggling the up-quark radial shape move those numbers or not? If not, they're derived from the geometry alone. If yes, they ride the up-sector's own (less-forced) structure and stay one tier down. I also caught myself — I'd hoped only the top quark was reshaped, but the charm is off the simple ladder too, so it's the whole up-tower, and I said so. Small, sharp, and one blind fire from an answer. Nothing pushed.

Notes only; no toy/theorem claimed (radial-residual ruling, K1183). F822: W-mediation → CKM = ⟨up|J_W|down⟩ FACTORIZES into angular (forced shelves + J_W Δm=1 selection) × radial (down FK-forced, up = non-FK up-sector mechanism). ANGULAR forces the CKM SKELETON (near-diagonal V_ud/cs/tb≈1, parity-gap hierarchy, small-mixing-forced) = Derived regardless of radial. RADIAL residual = WHOLE up-tower (corrected: charm non-FK too, 588 vs naive-FK 12; only top saturated y_t≈0.99) = Identified up-sector mechanism. SHARP BINARY (=F818 A-count): angular-dominated (radial cancels in angles → seven Derived, A=0) vs radial-sensitive (angles ride Identified up-radial → Identified, A≥1). Elie's fire: vary up-radial, angles stable→Derived, move→Identified. Bonus: mixing confirms up-tower ORDERING (Cal sub-flag, since masses don't). @Elie compute A_kk' vs radial separately, report stability; @Grace skeleton = angular factor, symmetrization from radial; @Cal skeleton Derived, seven Derived iff angular-dominated. — Lyra