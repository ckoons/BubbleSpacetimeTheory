---
title: "CKM CP phase delta: the clean external result is NEAR-MAXIMAL CP; the exact value is INTERNAL-open (Grace, 2026-06-29). CLEAN (external-OK, target-innocent): CP violation is near-maximal -- sin(delta)~0.9, i.e. J = V_us*V_ub*|V_cb| (the sin delta=1 maximal) to within 10% (J_max=3.41e-5 vs J_obs=3.08e-5). Mechanism: the deposits are complex (holomorphic) AND non-collinear (the dual-rho V_cb angle), so the generic CP phase is large -- maximal is the geometric default, the observed 10%-below-maximal is the small alignment correction. INTERNAL-open (Cal #50): the EXACT delta = -n_C*arg(N(z_mu,z_tau)), so observed delta~66 deg = n_C x (underlying SO(2) time-circle phase ~13 deg); pinning the ~13 deg needs the SO(2) conformal weights, and two clean cos(delta) forms (rank/n_C=2/5, N_c/g=3/7) both fit ~1.4% = DEGENERATE = numerology trap, NOT pinnable without fishing. delta lives on the SAME SO(2) time-circle (2nd K-factor) the Koons tick clocks -- so the exact delta is tick-lane-coupled and stays INTERNAL. No fishing of the exact value."
author: "Grace"
date: "2026-06-29 Monday"
status: "delta: NEAR-MAXIMAL CP = clean external-OK target-innocent result (J within 10% of maximal). EXACT delta = INTERNAL-open (Cal #50, numerology trap, tick-coupled). No fishing. CP localized to SO(2) (banked structure)."
---

# CKM delta: near-maximal (clean) vs exact (INTERNAL-open)

## Clean, external-OK, target-innocent: CP is NEAR-MAXIMAL

```
J_maximal (sin delta = 1)  = V_us * V_ub * |V_cb| = 3.41e-5      <- the geometric default
J_observed                 = 3.08e-5  ->  sin(delta) = 0.90  (delta = 64.4 deg)
```
**The deposits are complex (holomorphic) and non-collinear (the dual-rho V_cb angle), so a LARGE CP phase
is the generic outcome -- near-maximal is the default, and the observed 10%-below-maximal is a small
alignment correction.** So BST predicts near-maximal quark-sector CP violation: `J ~ V_us*V_ub*|V_cb|` to
~10%. Target-innocent (uses the derived mixing magnitudes + the geometric maximal default; the 10% is the
open piece, not fished).

## Why the EXACT delta stays INTERNAL (Cal #50) -- and not fished

`delta = -n_C * arg(N(z_mu,z_tau))`, so observed `delta ~ 66 deg = n_C x (underlying ~13 deg phase)`.
The underlying ~13 deg is the **relative SO(2) time-circle phase** between the mu and tau deposits (the
spatial SO(5) angle gives delta = 0 -- CP must come from the SO(2), the 2nd K-factor; that's the banked
structural result, "CP localized to SO(2)"). Pinning the ~13 deg needs the SO(2) conformal weights, and:
- `cos(delta) = rank/n_C = 2/5` -> 66.4 deg (1.4%)
- `cos(delta) = N_c/g = 3/7` -> 64.6 deg (1.3%)

**Two clean primary-ratio forms fitting equally = the numerology-trap signature** (Cal #27 at peak). No
structural reason to prefer either. So the exact delta is NOT pinnable without fishing.

## The tick coupling (why it's the SAME lane)

delta lives on the **SO(2) time-circle** -- the exact same 2nd K-factor the Koons tick clocks
([[grace_tick_C2sq_bergman_heatkernel_side_INTERNAL_2026-06-29]]). So the exact delta is tick-lane-coupled
and stays INTERNAL (Cal #50): CP phase and substrate clock are two readings of the one time-circle. The
clean, external part is "near-maximal"; the exact value waits on the (internal) SO(2)-weight structure.

— Grace, 2026-06-29 Monday. delta NEAR-MAXIMAL (clean, J within 10% of maximal, external-OK,
target-innocent); EXACT delta INTERNAL-open (numerology trap, SO(2)/tick-coupled, not fished). CP
localized to SO(2) stands as banked structure.
