# K1798 — PRE-REGISTERED: the forced-projection gate + a flag that the obvious candidate likely fails

**Filed:** 2026-08-22
**Status:** PRE-REGISTRATION + corpus flag, BLIND before Grace/Lyra's forced-projection compute.
**Purpose:** gate the "is the Q⁵ parity-fold a forced projection giving ‖(1−P)O‖²≈0.0017?" computation, and surface two corpus facts that bear on it directly — one of which suggests the obvious candidate gives a GENERIC fraction, not 0.0017.

---

## Two corpus facts the team needs BEFORE building P.

**A. ★ The {h^k} index is OVERLOADED — pin which use is which (same trap as before).**
- **T2544:** the parity split is up = even {h⁰,h²,h⁴} vs down = odd {h¹,h³,h⁵} — this splits UP-SECTOR from DOWN-SECTOR.
- **T1929 / T2513:** the three GENERATIONS (within the down sector) are the odd cycles {h¹,h³,h⁵}, and d:s:b sit at degrees {1,3,5}.

So {h¹,h³,h⁵} is used BOTH as "the down SECTOR (vs up)" AND as "the three down GENERATIONS." Before building any projection P, pin: is the parity fold splitting sectors, or indexing generations? A projection on the wrong one of these repeats the type error that killed the R53 unification.

**B. ★★ The parity fold likely gives a GENERIC fraction (~1 or ~0), NOT 0.0017 — pre-registered so we don't over-read a miss OR a lucky land.**
The corpus already computed the parity overlap: **the bare cross-parity overlap is EXACTLY zero** (Toy 5313, K1791/T2544) — up-even ⟂ down-odd. That is WHY a current was thought necessary (and the current died). Consequence for THIS route: if P is the parity projection onto even shelves and the down condensate lives on odd shelves, then ‖(1−P)O_down‖² = 1 (all of it), and if it lives on even, = 0. Either way a **generic** value — exactly Grace's guard-2 ({1, 0.67, 0.33, 0} vs target 0.00168). **So the parity fold is the FIRST candidate to CHECK and the LIKELY one to RULE OUT.** A forced projection that gives 0.0017 must be subtler than the parity fold — or no such forced projection exists and A is an input.

## THE GATE — five conditions. G1, G2 immovable.

**G1 — the projection is FORCED, not chosen (immovable).** P must be a genuinely forced orthogonal idempotent on the generation space (forced by discreteness / the Q⁵ ring), not selected because ‖(1−P)O‖² lands near 0.0017. Count the candidate gradings (parity fold, and any others); report the full set and which close vs don't (C6 selection-honesty).

**G2 — TYPE-BRIDGE exhibited (immovable; the R53 killer).** BOTH χ and P must live on the SAME 3-dim generation space. χ = O is the SO(5) VECTOR (5-dim) — the map O → a generation-space direction must be EXHIBITED, not assumed. If no map O → generation space exists (as none existed QM-commit → generation last round), the route fails on type before any number, exactly as before.

**G3 — orthogonal + idempotent, verified (Cal's one-line check, on the RIGHT operator).** P² = P and P† = P on the generation space. (Not on the Jordan algebra — that's the QM commit, wrong space per K1797.)

**G4 — the answer must be near-alignment, and that's the hard part (Grace).** A generic projection gives a generic fraction; 0.0017 requires the condensate ~99.83% inside the commit subspace. The derivation must PRODUCE that near-alignment from the geometry, not assume it. If the forced P gives a generic fraction (Flag B), that is an HONEST NEGATIVE → A is an input → CKM ships Partially Derived (structure + λ derived), which is already a clean result.

**G5 — two-scale + band veto (Elie/Cal, carried).** If a number lands, report at ~2 GeV and v; if the spread exceeds the 0.170° between the |V_cb| controversy's two sides, "cannot adjudicate."

---

## Disposition
Check order: **G2 first** (does a type-bridge O → generation space even exist?), then G1 (is the projection forced?), then the parity fold explicitly (Flag B — likely a generic fraction). A forced, type-consistent, orthogonal projection whose un-committed fraction of O = 0.0017 *without tuning* derives Wolfenstein A and closes the sector. **The most likely honest outcome, given Flag B, is that the forced projection gives a generic fraction → A is an input → CKM = Partially Derived (structure + λ derived), which ships.** Both are wins; neither is a fit. No number computed until G1+G2 clear.

**Keeper, K1798, pre-registered blind 2026-08-22. The obvious projection probably fails; say so up front, so a lucky land gets scrutiny and an honest miss reads as the Partially-Derived floor, not a failure.**
