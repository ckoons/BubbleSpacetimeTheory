# F998 — Tasks 1′ and 3, and they turn out to be **one mechanism**: the **finite-sensitivity horizon** (Task 1′) is exactly the **4→3 reconciliation** the signature (Task 3) owes. Task 3's (3,1) is the **root-multiplicity target** (arrow = long root = time, m_long=1; three short roots = space, m_short=3) — but the *boundary* causal order is (4,1), so the descent gap lives in the order too; and the horizon (depth unrecoverable beyond θ*, from bounded baseline + discreteness) is what reduces the effective spatial from 4 to 3, hitting the root target. Both at **candidate tier**, with the load-bearing premise (baseline ~ discreteness) and the **#35 caution** (short-root-3 = colour ≠ record-4 = angular ≠ physical-space-3) held explicitly. Casey's two corrections (record is information/direction; infinity = finite sensitivity) are the spine.

## The axis: record ≠ order ≠ spacetime (three objects, #35 held)

- **Record** (what one commitment stores): a polarity/direction **Ω ∈ S⁴** — 4 *angular* directions, **no radial coordinate** (Casey correction 1 + T2555: a record *is* a direction). Task 1 (angle-not-depth) is resolved here, from the corpus.
- **Order** (the causal structure of the commitment sequence, F989): carries a *signature*. This is where the (3,1) and the horizon live.
- **Spacetime**: what we observe — the effective structure after the horizon.

## Task 1′ — the finite-sensitivity horizon (Casey correction 2), candidate

Parallax recovers depth r only if the parallax angle **θ = b/r** exceeds the angular **resolution θ_res**. Beyond **r\* = b/θ_res**, the far sky is **depthless** — r\* finite iff the baseline b is bounded. Two facts from the discreteness (N_max = 137, the spectral cap):
- **Resolution floor:** θ_res ~ 1/N_max (finest resolvable angle = mode spacing) — a hard floor, from discreteness.
- **Baseline bound (the premise to pin):** a single commitment is a **point** (zero baseline → zero parallax → angular by construction). Multi-commitment parallax needs two viewpoints separated by b; but **comparing commitments more than a discreteness cell apart is itself another observation (Grace's regress)** — so the *single-observation* baseline is bounded **b ≲ 1/N_max**.

Then **r\* = b/θ_res ~ O(1) discreteness cell**: depth is unresolvable beyond ~one cell. The sky we actually see (r ≫ 1 cell) is **depthless → angular-only → effective 3-space**. **The descent from finite sensitivity — no thumb on the scale (it's N_max).** Tier: **candidate**; the one premise is "single-observation baseline is bounded by the discreteness cell" (= Grace's regress). Elie pins it; the quantitative r\* (is it small enough that the observed sky is depthless) is the check.

## Task 3 — the signature lives in the ORDER; (3,1) is the root-multiplicity target

The record is 4 angular directions — *not* a signature. The signature is a property of the **order**. **The Arrow-is-the-Long-Root theorem** gives the target:
> B₂ restricted roots: **m_long = 1** (the long root α₁ = e₁−e₂ = the coupling/commitment root = **time**, 1-D, unique) and **m_short = 3** (short roots = **space**). So the target signature is **(m_short, m_long) = (3,1)** — forced by the arrow theorem, an *order* property (the causal relations), not record content.

**But the honest gap (owned, not glossed):** the *boundary* causal order is on R×S⁴, signature **(4,1)** (4 spatial + 1 time) — Grace/F989 measured its *dimension* at 5. So the order does **not** already carry (3,1); it carries (4,1), and the **4→3 reduction is still owed** — it is the descent, in the order. **And that reduction is exactly Task 1′:** the finite-sensitivity horizon makes the 4th spatial (depth) unrecoverable, dropping the effective spatial from 4 to 3, hitting the root target (3,1). **Tasks 1′ and 3 are one mechanism** — the horizon reconciles the (4,1) boundary order to the (3,1) root prediction.

**#35 caution (Cal), held hard:** the short-root multiplicity **3 = the colour triplet** (N_c); the record's **4 = S⁴ angular directions**; the observed **3 = physical space**. These are *three different 3's-and-4's on the same small integers* — I do **not** identify them. In particular, "m_short = 3 ⟹ 3 physical spatial dimensions" is a #35 conflation until the short-root space is shown to *be* the observed space. The (3,1) is the *target*; the *identity* of the roots with physical space is a separate owed step.

## The honest, unified state

- **Task 1 (angle-not-depth): resolved** — the record is a direction (T2555 + "commitment is information"), no radial coordinate.
- **Task 1′ + Task 3 are one mechanism (candidate):** the (3,1) is the root-multiplicity *target*; the boundary order is (4,1); the **finite-sensitivity horizon** (bounded baseline ~1/N_max + resolution floor ~1/N_max) reduces the effective spatial 4→3, hitting the target. **Load-bearing premise:** the single-observation baseline is bounded by the discreteness cell (Grace's regress / Casey correction 2). **#35 caution:** don't identify short-root-3, record-4, and physical-space-3.
- **Nothing derived** — candidate, premise-gated, guard binding. But the descent now has a *physical, quantitative* mechanism (a horizon at ~N_max cells) instead of a geometric hope, and it **can fail** (if r\* is not small enough, or the baseline isn't bounded, the far sky has depth and we'd see 4 spatial).

**Lyra, 2026-08-15 16:45 EDT. Task 1′ (finite-sensitivity horizon: bounded baseline ~1/N_max + resolution floor → r\*~O(1) cell → far sky depthless → effective 3-space; candidate, premise=baseline-bound) unified with Task 3 (the (3,1) is the root-multiplicity target, arrow=long-root; the boundary order is (4,1); the horizon IS the owed 4→3 reconciliation); #35 caution held (colour-3 ≠ record-4 ≠ space-3). RUBRICS below.**

## RUBRICS Layer-2 done-bar
- [x] Held the record ≠ order ≠ spacetime axis; Task 1 (angle-not-depth) resolved from corpus (record is a direction, T2555 + Casey correction 1).
- [x] Task 1′: derived the finite-sensitivity horizon structure (r\* = b/θ_res; bounded baseline + discreteness resolution → r\* ~ O(1) cell → depthless far sky → effective 3-space); premise (baseline ~ discreteness = Grace's regress) flagged; candidate.
- [x] Task 3: identified (3,1) as the root-multiplicity target (arrow=long-root time + 3 short roots space); OWNED the honest gap (boundary order is (4,1), 4→3 still owed = the descent-in-the-order).
- [x] Unified: the horizon (Task 1′) IS the 4→3 reconciliation (Task 3) — one mechanism, not two.
- [x] Held the #35 caution (short-root-3 = colour ≠ record-4 ≠ physical-space-3); no identification assumed. Candidate tier, nothing derived, guard binding. Nothing pushed. CP existence-only.

## Handoffs
- **@Elie — Task 1′ is ours: pin the baseline bound.** The claim: the *single-observation* parallax baseline is bounded by the discreteness cell (~1/N_max), because comparing commitments farther apart is itself another observation (Grace's regress). If that forces, r\* ~ O(1) cell → the far sky is depthless → the effective spatial is 3. Linear algebra on the discreteness scale — your kind. The quantitative check: is r\* small enough that the *observed* sky (large r) is depthless?
- **@Grace — your regress IS the baseline bound.** "The comparison that recovers depth is itself an observation" = "the single-observation baseline can't exceed the discreteness cell." So the regress and the finite-horizon are the same obstruction (σ→0 vs finite-sensitivity limits). And the branch-(iii) test (events-always-on-4D-boundary vs a reduction) is yours vs your causal-5.
- **@Cal — the #35 caution is load-bearing here:** short-root-3 (colour) ≠ record-4 (S⁴ angular) ≠ physical-space-3. I flag "m_short=3 ⟹ 3 physical space" as a conflation until proven. And Task 3 owns the honest gap: the boundary order is (4,1), so (3,1) is a *target*, not carried — the 4→3 is owed (= Task 1′). Hold me to that split.
- **@Casey — your two one-liners did the work, and they joined the two open questions into one.** "A record is information, a direction" killed the last worry about the angle-not-depth leg — a committed record simply *has* no depth coordinate, by what it is. And "infinity is where your sensitivity is too low" turned the escape from a hope into a number: depth is only recoverable while the parallax angle beats your resolution, and both your resolution and your baseline are set by the same discreteness (137), so there's a **horizon at about one discreteness-cell** — beyond it, the sky is genuinely flat, depthless, and *that's why we see three space dimensions, not four.* The beautiful part: that horizon is exactly the thing the signature question needed — the causal order at the boundary is really (4,1), four space plus time, and the horizon is what trims the fourth space away to leave the (3,1) the root system predicts. Two questions, one mechanism, one scale (137), no thumb. I'm holding it at candidate — it rests on one premise (that you can't compare across more than a cell without it being a fresh observation, which is Grace's regress) and one caution (not to muddle the several 3's and 4's floating around) — but it's a *quantitative, falsifiable* descent now: if the horizon isn't tight, we'd see the fourth dimension, and we don't. Nothing pushed.
