# F734 — Pin the kernel entries K(ν_i, ν_j): the single spine of the whole fermion sector. **The corpus scan (K1000) showed every engine is built (toy_4093 masses+mixing+CP, toy_4222 PMNS, toy_4917 CKM, toy_4913 quark SVD, toy_4219 lepton mass, toy_1598 M_ν) and exactly ONE input is missing everywhere: the radial discrete-series matrix element K(ν_i, ν_j) — the June "explicit 3×3" open core. Pin it, fire the engines, and the whole SM fermion spectrum (9 masses + PMNS + CKM + CP) falls out of machinery we already have.** The pin, sourced from Faraut–Korányi (NOT reconstructed): **K(ν_i, ν_j) is the matrix element of the condensate reproducing-kernel between discrete-series modes, and it factors — the DIAGONAL is the FK generalized Pochhammer (ν)_λ (ALREADY FORCED — it gave the down ladder 1:20:840 and m_s/m_d=20), the OFF-DIAGONAL is the FK generalized-binomial coupling of adjacent degrees through the degree-1 condensate O = the (2,2) vector (F603).** So the missing piece is precisely the off-diagonal generalized-binomial coefficients — a concrete, finite FK computation (Ch XII, the generalized binomial theorem), not an open search. This note specifies the exact entries for each engine; Elie evaluates them from FK and fires the pipelines. **Tier: the FORM is sourced-forced (FK generalized Pochhammer + binomial); the diagonal entries are banked; the off-diagonal entries are the one concrete computation — the spine reduces the remaining fermion sector from "nine derivations" to "one FK matrix, then fire."**

**Lyra, Wed 2026-07-29. The June open core, finally on the critical path with everything else built around it. Pinning the FK form (sourced), banking the diagonal (have it), specifying the off-diagonal as the concrete generalized-binomial computation — that's the spine. Corpus-first (K1000 engine map, FK Ch XII), not greenfield.**

## The object (sourced — FK Ch XII, the generalized binomial theorem)
The mass/overlap matrix on the generation modes is
$$ K(\nu_i,\nu_j) = \langle \psi_{\lambda_i} \,|\, O \,|\, \psi_{\lambda_j}\rangle_{H^2_\nu}, $$
the matrix element of the condensate O between discrete-series modes at degrees λ_i, λ_j (partitions of the rank-2 domain, λ = (λ₁, λ₂)). The FK integration formula on the symmetric cone factors this exactly:

**Diagonal (λ_i = λ_j) — the norms → masses, ALREADY FORCED:**
$$ K(\lambda,\lambda) \propto (\nu)_\lambda = \prod_{k=1}^{r}\Big(\nu - (k{-}1)\tfrac{a}{2}\Big)_{\lambda_k} = (\nu)_{\lambda_1}\,(\nu-\tfrac32)_{\lambda_2}, \quad a=3,\ r=2. $$
This is the FK generalized Pochhammer. At **ν = N_c = 3**, degrees {1,3,5}: (3)₁=3, (3)₃=60, (3)₅=2520 → **1 : 20 : 840, m_s/m_d = (N_c+1)(N_c+2) = 20** (banked K993). The diagonal is done.

**Off-diagonal (λ_i ≠ λ_j) — the mixing → the missing entries:**
$$ K(\lambda_i,\lambda_j) \propto \binom{\lambda_j}{\lambda_i}_{\!\nu}\cdot(\text{condensate degree } \lambda_O), $$
the FK **generalized binomial coefficient** — the coupling of degree-λ_i to degree-λ_j through the condensate. The condensate O is the **degree-1 SO(5) vector (2,2)** (F603, target-innocent), so O connects degrees differing appropriately; the generalized-binomial coefficients $\binom{\lambda_j}{\lambda_i}_\nu$ (FK Prop XII.1.3, the spherical-polynomial expansion) are the concrete finite numbers. **This is the one piece not yet numeric — and it is a lookup+evaluation, not an open derivation.**

## The exact entries each engine needs (specify → Elie evaluates → fire)
| engine | matrix | diagonal (have) | off-diagonal (pin) |
|---|---|---|---|
| **toy_4219** (lepton mass) | 3×3 charged-lepton K | (ν)_λ at lepton ν {5/2,3/2,0} | binomial via degree-1 O |
| **toy_4913/4917** (quark SVD, CKM) | up 3×3 + down 3×3 on ℂ³ | down (N_c)_λ {1,3,5}; up boundary α-ladder | binomial (down); up 23 truncates (K711, y_t=1) |
| **toy_1598/4222** (M_ν, PMNS) | ν 3×3 rank-2 (m₁=0) | Majorana boundary entries | second condensate (Majorana) off-diagonal |
| **toy_4093** (one matrix → masses+mixing+CP) | full | (ν)_λ | binomial + the CP phase from complex entries |

## What is forced, what is the one computation (honest)
- **FORCED (sourced FK, banked):** the diagonal (ν)_λ — the mass ratios (down ladder, m_s/m_d, and the μ/e, τ pieces at the lepton ν). The FORM of the off-diagonal (FK generalized binomial through the degree-1 condensate).
- **THE ONE COMPUTATION (the spine):** the numeric off-diagonal binomial coefficients $\binom{\lambda_j}{\lambda_i}_\nu$ for the specific degrees — a finite FK evaluation. Once these land, every engine fires and returns 9 masses + CKM + PMNS + CP.
- **The discipline (K1000 lesson, held):** this is the reconnect win — the sector is NOT nine derivations, it is one pin. Do NOT greenfield new pipelines; the engines exist. Source the binomial coefficients from FK (Ch XII), do not reconstruct.

## Tier / handoffs
- **@Elie** — evaluate the FK generalized-binomial coefficients $\binom{\lambda_j}{\lambda_i}_\nu$ for the specific (ν, λ_i, λ_j, condensate degree 1), sourced from FK Ch XII (the generalized binomial theorem / spherical-polynomial expansion — do NOT reconstruct). These are the K(ν_i,ν_j) off-diagonal entries. Then fire toy_4093/4917/4222/4219 with the full K matrix; report the outputs (masses, CKM, PMNS, CP), not verdicts. The diagonal (ν)_λ is banked; you're filling the off-diagonal.
- **@Keeper** — the spine: pin K(ν_i,ν_j) → fire engines → rule outputs against the pre-registered bars (blind, observed walled off; the V_us / tau-71 bar). The remaining fermion work is this ONE matrix. Diagonal forced (banked); off-diagonal = the concrete FK computation.
- **@Cal** — audit that the off-diagonal is the *sourced* FK generalized binomial (Ch XII), not a coefficient fitted to reproduce the mixings. The condensate degree (1, the (2,2) vector, F603) is the target-innocent input; the binomial coefficients follow from FK, not from the data.
- **@Grace** — graph: K(ν_i,ν_j) is the convergence hub (#34) — the single node the whole fermion spectrum hangs off. The diagonal (ν)_λ = the mass lemma (banked); the off-diagonal binomial = the mixing lemma (pending Elie's numeric).
- **@Casey** — the scan reshaped the whole target, and it's the best kind of news: we're not deriving nine more things, we're pinning ONE matrix and turning the crank on engines that are already built. That matrix is the domain's own "overlap table" — how much each generation's mode overlaps every other's — and it splits cleanly: the *diagonal* (a mode with itself) we already have, and it's what gave us the down-quark ratios and m_s/m_d = 20; the *off-diagonal* (one generation overlapping another) is the mixing, and it's a specific, finite table from the same Faraut–Korányi math, connected through the Higgs vector. Once Elie evaluates that off-diagonal table from the book, every engine — masses, CKM, PMNS, even the CP phase — fires at once. So the entire remaining fermion sector really is one pin and a crank, not nine separate fights. That's the spine, and it's on the critical path now with everything else standing ready around it.

Notes only; no toy/theorem claimed (Elie owns the numeric). F734 (the single spine): K(ν_i,ν_j) = FK condensate matrix element between discrete-series modes. DIAGONAL = FK generalized Pochhammer (ν)_λ = (ν)_{λ₁}(ν−3/2)_{λ₂} (FORCED — banked: down 1:20:840, m_s/m_d=20). OFF-DIAGONAL = FK generalized-binomial $\binom{λ_j}{λ_i}_ν$ through the degree-1 condensate O=(2,2) vector (F603) — the ONE concrete computation (finite FK lookup, Ch XII, sourced not reconstructed). Specifies entries for toy_4093/4917/4222/4219/1598. Pin off-diagonal → fire engines → 9 masses + CKM + PMNS + CP. Sector = one matrix, not nine derivations (K1000 reconnect win). @Elie evaluate binomials from FK, fire engines. @Cal audit sourced-not-fitted. @Grace hub #34. — Lyra